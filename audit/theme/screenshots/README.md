# Theme Audit Screenshots

Theme audit screenshots live here while comparing light and dark mode.

Expected layout:

```text
screenshots/
├── light/
├── dark/
└── compare/
```

Use the naming rules in `../SCREENSHOT_MATRIX.md`.

Screenshots are internal evidence and are not deployed unless moved into `public/` or imported by app source. Usually commit only selected evidence screenshots; keep full screenshot dumps outside the repo if they are large.

