# DEEPFIX empirical census — 26SM live cohort (READ-ONLY, 2026-07-13)

Generated by `scripts/cs/deepfix-census.mjs` (read-only). Raw rows: `census_rows.json` (530 flagged),
per-class config: `census_classes.json`, log tallies: `syslogs.json`. **Every number here is live 26SM
Firebase data.** H1 caveats are called out — do NOT quote a raw count as a bug-count without its caveat.

## Cohort shape
- 32 classes matching /26SM/i · 816 distinct students · 3 lists (Base Camp `RmNNkuLP` 1200 / Ascent `dVliNv0p`
  1600 / Summit `AObYOowh` 800) · 3009 (student,class,list) enrollments · **1121 started** (csd>0 or twi>0).
- Threshold config: `{92/0.92 ×84, 90/undefined ×6, 92/undefined ×4, 90/0.92 ×2}` — NOTE: the CS-2026-07-04b
  "all 92/0.92" normalization did NOT fully hold: **8 assignments still have passThreshold 90** and **10 have
  `newWordRetakeThreshold` UNDEFINED** (→ falls back to 0.95 client-side = live #5 exposure). Worth a config re-sweep.
- `#7 assignedLists:[] split-brain: 0 live classes` (ensure-all-lists cleaned it; the CODE bug at db.js:502 remains latent).

## Signature counts (started students) — with H1 status
| Signature | Count | Status / caveat |
|---|---|---|
| **reviewOnly:listEnd (#11)** | **183** | REAL. The dominant live bug. (169→172→183 across scans; grows as more students finish.) |
| reviewOnly:throttle (#11) | 1 | REAL but rare (needs 3 consecutive low reviews; the Junseo case). |
| **dualEnrollSameList (#6/#12)** | **98** | REAL + MAJOR. 2+ class_progress docs on ONE list. Many DIVERGENT (see below). Live carry substrate. |
| **manualPatched (P population)** | **82** | REAL. manualOverride/`_manual` attempts. ~10% of started students are hand-patched. **25 of them are ALSO back at the #11 wall** (patched→re-stuck → patches aren't durable). |
| testSizeMismatch (#13) | 257→**~30-50 real** | ⚠️ OVER-COUNTED. Big buckets (q35→exp30, q50→exp30) are dual-enroll CROSS-CLASS attempts mis-attributed to the other class on the shared list. **Real #13 = the small-q bucket only** (q9–q15→exp30/35: 이혜성 10/30, 이서현 15/30 dup). Needs a precise recount that pins each attempt to the class it was TAKEN under + excludes retakes. |
| reviewNoNewPass | 31 | Mostly the benign review-only artifact (Junseo d5 + list-end finishers) + list-end. Not corruption. |
| invalidAnchor | 0 | CLEAN cohort-wide. |
| csdImplausible / twiOverList / twiOverMastery / ghostProgress / noClassAttempt / missingPSD | ~0 (1,1,0,0,0,1) | CLEAN. **The data is NOT corrupt — the problems are structural/behavioral, not corruption.** |

## dualEnrollSameList detail (the R-B substrate) — 98 students, many divergent
Sample (uid, list, per-class csd/twi):
- **OCzwBwAb (이주헌)** dVliNv0p: `Adv A1 csd11/twi880` + `Final A csd5/twi500` — **recurred after the 06-30 consolidation** (dropped then re-split). Structural, not one-off.
- **d8DzmjMP** dVliNv0p: `Adv B1 csd19/twi1520` + `Final B csd2/twi200` — massive divergence (17 days).
- **ob0Ngt48** dVliNv0p: `Adv A1 csd5/twi400` + `Adv[한] csd14/twi1120` — 9-day divergence (a live #12 strand candidate).
- **kT4E0rVo**: dual on BOTH lists across Adv B1 + Final B.
Some are benign (both finished, e.g. gdBo9TYz 1600/1600). Breakdown of benign-vs-divergent is in census_rows.json
(`dualDocs`). **Takeaway: 98 live students carry split progress on a shared list — the exact condition the
student-owned-progress convergence dissolves. It RECURS after CS drops because nothing prevents it structurally.**

## system_logs — deploy-state signal (last 4000 events, 2026-07-12 04:34 → 07-13 12:52 ≈ 1.35 days)
| event `type` | count | note |
|---|---|---|
| **impossible_phase_detected** | **3088** | ⚠️ DOMINANT (77%). NEED_TO_FIX #10 flagged this family at "406 recent"; now 8× that. **Investigate: what fires it, is it benign, whose (26SM vs 07-12 fleet-audit sandbox)?** Not yet attributed. |
| csd_twi_reconciled | 853 | Normal LIST_SCOPED_RECON reconciliation firing. Expected. |
| grading_attempt_failed | 19 | LOW — grading is stable now. |
| **day_guard_rejected_session_cleared** | **17** | #10 self-race signature. ⚠️ CONTRADICTS SESSION_CONTEXT "0 live / all sandbox." **Attribute: real 26SM (→#10 fires in prod) vs 07-12 fleet-audit runs** before concluding. Window starts 07-12 04:34 (audit ran then). |
| attempt_write_failed_client | 12 | The save-fail family (CS-06-29 A). Low. |
| attempt_day_fallback | 10 | LOW (answers Codex's open Q — not high-volume). |
| csd_anchor_invalid / implausible / query_error | 0 | CLEAN — no invalid-anchor or bad-CSD events live. |

## Headline empirical findings (for prioritization)
1. **183 students at the #11 list-end wall — the single largest live impact.** (Fix built, uncommitted, undeployed.)
2. **98 dual-enroll-same-list students, many divergent — the live R-B/#6/#12 substrate; recurs after CS drops.**
3. **82 hand-patched students, 25 re-stuck at the wall — manual CS is not durable; the treadmill quantified.**
4. **impossible_phase_detected fires 3088×/1.3 days — a large, under-investigated signal (top TODO).**
5. **Real #13 (undersized tests) is ~30-50 attempts** (small-q bucket), NOT 257 — the rest is dual-enroll attribution noise.
6. **Data is NOT corrupt** (invalidAnchor/csdImplausible/ghost = 0). The failures are structural/behavioral/deploy-state.
7. **Config drift:** ~10 assignments still 90-threshold or missing `newWordRetakeThreshold` (live #5 residue).

## Open empirical TODOs (for the INVESTIGATION_PLAN / 1.6, orchestrator-run read-only)
- Attribute impossible_phase_detected (3088) + day_guard_rejected (17): pull a sample with uid/class → 26SM vs sandbox; find the emitter in code.
- Precise #13 recount: pin each 'new' attempt to the class it was taken under (classId on the attempt), exclude retakes, count true undersized.
- dualEnroll divergent-vs-benign split + how many are live #12 strand risks (one doc far behind the cross-class anchor).
- manualPatched → the 25 re-stuck: which bug re-caught them (list-end? throttle? carry?).
- Config re-sweep: the residual 90-threshold + undefined-newWordRetakeThreshold assignments.
