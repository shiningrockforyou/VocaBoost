# Change Action Log

> **Instructions for Claude:** Log every code change you make to this file. Add a new row for each modification with the date, file path, and a brief description of what changed. This helps track all modifications across sessions.

| Date | File | Change |
| 2026-07-19 | audit/deepfix/task6/d35_r60_lostsave.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_r60_lostsave.json (NEW), findings/steps/r60-lostsave_bc_d6.jsonl (NEW) | **R60 — FINISH DAY-6 for lostsave_bc_d6 → LOST-SAVE FULLY AUTO-RECOVERED (live prod, SANDBOX only, NO 26SM).** WSL corrected r59: csd=5/twi=480 was a COHERENT mid-day-6 state (day-6 new done + valid anchor; review pending), NOT orphaned — a day advances only when new+review both complete. Completed the pending day-6 REVIEW: degradeProbe correctly caught renderDay=6/offersReview=true (longer settle fixed the prior "Preparing..." reads), MCQ 100% → **csd 5→6**, twi stable @480, canonical=0, and a **bare reload confirms csd=6 sticks**. Full recovery works deployed with NO manual pass (retake day-6 new → complete day-6 review → csd advances durably). **Owning it: my r58 (no-persist) + r59 (orphaned/manual-pass) reads were BOTH too early** — lesson: for two-phase day completion don't judge csd until new AND review done. Fresh runStart for --since. Review: docs/plans/loop/win/reviews/winclaude_060.md. |
| 2026-07-19 | audit/deepfix/task6/d35_r59_reload.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_r59_reload.json (NEW), findings/steps/r59-*.jsonl (5 NEW) | **R59 — bare-reload LASTING check (live prod, SANDBOX only, NO 26SM, no drives — login+load+readback).** **Throttle escapes LASTING 4/4 PASS**: thr_0DnzKs(csd11)/thr_bFV18s(csd7)/thr_yiVt86(csd17)/jisu_a1(csd5) all reviewMode=FALSE + csd unchanged across a fresh bare load → throttle recovery durable. **lostsave_bc_d6 = twi reconciled but csd ORPHANED (PARTIAL/GAP)**: on fresh load twi reconciled 400→480 (+80, csd_twi_reconciled log fired) — CORRECTING my r58 "0 anchors/no-persist" read which was too early (the day-6 anchor DID persist per WSL) — BUT csd STUCK at 5 across TWO reloads (not progressive) → orphaned csd (80 day-6 words introduced yet csd=day-5-complete), manual CS pass needed for csd. Contrasts off-by-one (reconciles csd on next load). Fresh runStart=2026-07-19T07:35:48.946Z. Review: docs/plans/loop/win/reviews/winclaude_059.md. |
| 2026-07-19 | audit/deepfix/task6/d35_r58_refidelity.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_r58_refidelity.json (NEW), findings/steps/r58-*.jsonl (5 NEW) | **R58 — David-directed FIDELITY re-tests (live prod, SANDBOX only, NO 26SM, direct-nav).** **TEST A faithful throttle hysteresis 4/4 FAITHFUL 2-STEP PASS** (thr_0DnzKs/bFV18s/yiVt86/jisu_a1, now reviewMode=true+interv=1.0): review#1(100%) → STILL HELD (reviewMode true, csd flat); review#2(100%) → ESCAPED (reviewMode false). Confirms David's skepticism — prior 1-step throttle escapes (r51/r54/r55) were a SEED ARTIFACT (reviewMode unset); faithful behavior needs TWO good reviews. **TEST B mid-list lost-save (lostsave_bc_d6, csd5/twi400, day-6 words exist) = HELD / does NOT auto-recover**: csd 5→5, twi 400→400, 0 day-6 anchors, only resolve_list_progress fresh (no new_word_test_recorded); reached+completed a typed test 100% but it did NOT persist → manual CS pass still needed (contrasts r57 choi list-end which advanced). Caveat: degradeProbe caught "Preparing..." (4.5s settle short); no-persist outcome definitive. Fresh runStart=2026-07-19T07:21:21.003Z. Review: docs/plans/loop/win/reviews/winclaude_058.md. |
| 2026-07-19 | audit/deepfix/task6/d35_r57_choi.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_r57_choi.json (NEW), findings/steps/r57-choi_a12.jsonl (NEW) | **R57 — 최도훈 (choi_a12) LOST-SAVE re-drive on the CORRECT Base Camp list (direct-nav /session/25WTa2r15/RmNNkuLPectBlBPiLbAJ; live prod, SANDBOX only, NO 26SM) → AUTO-RECOVERY (csd 15→16) with a list-length nuance.** Direct-nav loaded the correct Base Camp list (containsBaseCamp=true — fixes r55 which hit his fresh Ascent). **csd advanced 15→16** via read-time `resolve_list_progress` (2 fresh; NO review_recorded/new_word_test_recorded/csd_twi_reconciled), **twi flat @1200, 0 day-16 new anchors, canonical=0, no crash, no false-success.** The reached test was an MCQ REVIEW (passed 100%) — NOT a day-16 new-word retake, because the BC list is 1200 words (twi=wordCount) so day-16 new words 1201-1280 DON'T EXIST. Handoff's expected twi 1200→1280/new-anchor cannot occur on this list. Flagged 3 WSL questions (is resolver-advance-at-list-end the intended recovery; does the seed need a ≥1280-word list; why no review_recorded log). Fresh runStart for --since. Review: docs/plans/loop/win/reviews/winclaude_057.md. |
| 2026-07-19 | audit/deepfix/task6/d35_r56_waveC.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_r56_waveC.json (NEW), findings/steps/r56-*.jsonl (4 NEW) | **R56 WAVE C — 4 synthetic adversarial configs (WSL-designed, class 25WTsynth11, live prod, SANDBOX only, NO 26SM) → 4/4 PASS.** Adopted the NEW direct-nav rule (goto /session/<classId>/<listId>, assert routedUrl contains listId — all 4 true, no wrongList) + console-error capture (crashed=false all 4). **A2_skip_hold** (csd5): empty/skipped review → HELD csd/twi flat. **F1_extreme_runaway** (csd30): renderCheck-only → read-only-safe (day-31 coherent, no crash, no demote, canonical=0, quarantine+resolve logged). **F8_canonical_anomaly** (csd6/canon=1): renderCheck-only → resolver detects canonical (resolveLog=2), canonical 1→1 no proliferation, no crash, coherent — P5 de-risk. **F4_incoherent_throttle** (csd6): skipped review → coherent reconcile csd/twi flat, no phantom hold. P4 read-only/boundary behavior verified pre-P5 cutover. Fresh runStart=2026-07-19T04:14:43.865Z for --since. Review: docs/plans/loop/win/reviews/winclaude_056.md. |
| 2026-07-19 | audit/deepfix/task6/d35_r55_waveB.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_r55_waveB.json (NEW), findings/steps/r55-*.jsonl (7 NEW) | **R55 WAVE B — 7 students (live prod, SANDBOX only, NO 26SM) → 6/7 clean PASS + 1 novel finding.** Reused r54 reach/typed machinery + step-logger; all reached via Path A. **B1 throttle escapes 4/4 PASS** (thr_0DnzKs/bFV18s/yiVt86/jisu_a1): reviewMode→false, review_recorded=2 (fresh), csd held (no demote), reconstructed sessions render coherently. **B2 off-by-one 2/2 PASS** (obo_GL7SXB 5→6, obo_JoJ2ch 6→7): VERIFIED typed 100% completion → csd reconciled, canonical=0 — closes the r52/r53 caveat with a graded pass. **B3 choi_a12 최도훈 lost-save = SAFE-DEGRADE FINDING**: all safety bars MET (no crash, no false-success, no false csd/twi advance [csd 15/twi 1200 held], no duplicate anchor) BUT renders Day-1/0-words not day-16 (progress-view collapse; stored csd=15 preserved); reached a 30-Q typed test with EMPTY word spans (matched 0) + timeout → no completion. Needs WSL source adjudication (fail-safe vs view-bug; seed-artifact vs deployed). Fresh runStart=2026-07-19T03:46:42.885Z for --since. Review: docs/plans/loop/win/reviews/winclaude_055.md. |
| 2026-07-19 | scripts/audit/assert-recovery.mjs + seed-synthetic.mjs | Added `readonly-safe` judge (Wave C / F1) + re-labeled F1_extreme_runaway family off-by-one→readonly-safe (its test is read-only safety at an extreme csd via renderCheck-only, not a completion-advance — the off-by-one judge mis-reported INVALID). Judge: FAIL on any csd move (read-only must not write up/down), PASS iff csd flat + resolve/quarantine log. Wave C now 4/4 PASS (was 3 PASS + 1 mislabeled INVALID). |
| 2026-07-19 | docs/plans/D3.5_FINDINGS.md (NEW) | Created consolidated D3.5 findings & insights doc (David-directed "save all audit insights"): verified results (13 behaviors, 0 FAIL), behavioral/architectural insights (session_states inert; interventionLevel derived; non-demoting reconcile; off-by-one on-completion; per-class test mode; grader legitimacy), harness/method (reach path, direct-nav rule, fresh-proof scoping, step-logging, judge coverage), seed-fidelity lessons. Linked from MASTER_TASK_LIST.md §3. |
| 2026-07-19 | scripts/audit/assert-recovery.mjs | Added `canonical-anomaly` judge (Wave C / F8): exempts it from the universal `canonical>0 → FAIL` (F8 seeds a canonical list_progress doc by design) and checks read-only handling instead — FAIL only if canonical PROLIFERATES (>1, i.e. a write happened while CANONICAL=false), else PASS iff the resolver detected it (resolve_list_progress/quarantine log) with a coherent non-demoted csd. De-risks the P5 cutover. Validated pre-drive: all 4 synthetic families read INVALID_PRECONDITION cleanly. |
| 2026-07-19 | scripts/audit/assert-recovery.mjs | Fixed server-path-proof (M7) windowing bug caught during r54 verification: the `--since` path added a composite `where(userId==).where(timestamp>=)` needing an undeployed index; the FAILED_PRECONDITION was swallowed to `[]` by `.catch`, nulling ALL proof (false negatives), while no-`--since` (sinceMs=0) counted STALE prior-round logs as proof (false positives — 4 throttle students passed on yesterday's wave-1 logs). Now queries by `userId` only (no index) + scopes timestamp in JS via `toMillis()`; warns when `--since` omitted; removed unused `TS()`. Re-run scoped to the r54 window → honest 3 PASS / 0 FAIL / 7 INVALID. |
| 2026-07-19 | audit/deepfix/task6/d35_r54_waveA.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_r54_waveA.json (NEW), findings/steps/r54-live_{oyk,kjk,lhs}.jsonl (NEW) | **R54 WAVE A — 3 live-ticket verbatim clones (live prod, SANDBOX only, NO 26SM) → 3/3 PASS.** Solved the r53 new-word reach + found a 2nd gap. **Reach fix:** poll ≤6s for the "Start Studying" customize modal (it races the render) → click → Session menu → "Skip to Test" → "Start Test" → navigates to test route (Path A validated via=A). **Test-mode gap fix:** test mode varies per class — live_oyk MCQ (/mcqtest/), live_kjk/live_lhs TYPED (/typedtest/); added inTest recognition of /typedtest/+definition inputs and typed driving (fill definitions.ko, which the AI grader accepts). RESULTS: **live_oyk** (runaway-inflated) empty×2 → HELD@12 (no advance/demote); **live_kjk** (throttle) skip held@4 + 2 good typed reviews 100% → escape csd 4→5; **live_lhs** (normal) complete day-10 (new-word typed 100% + review typed 100%) → csd 9→10 once, twi +80. Wired lsr_step_logger into every drive (live JSONL evidence + reachProbe + in-loop progress/heartbeat). Wave B deferred to next round. Review: docs/plans/loop/win/reviews/winclaude_054.md. |
| 2026-07-19 | audit/deepfix/task6/d35_newword_inspect.mjs (NEW), d35_newword_reach.mjs (NEW), d35_newword_drive.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_newword_r53.json (NEW), _r53_progress.log (NEW) | **R53 D3.5 tier-3 new-word matcher/reach fix + off-by-one close — BLOCKED (new-word test reach is a real harness gap).** Instrumented the drive with a live progress log + fail-fast timeouts (25s vs 120s outcome race, 70s reach cap). Finding: `driveNewWordsToTest` returns reached=false on prod new-word sessions; study-through fallback is ~30s/card (→~40min/80-card day) — the new-word study ("Step 1 of 5" / "Start Studying" / 80 cards) exposes no working skipToTest and its card controls don't match study-through selectors. **Correction to R52:** that obo_GL7SXB "completion" also never reached the test (matched 0/30) — both R52/R53 csd advances are session-entry RECONCILES, not graded passes. Off-by-one csd 6→7 CONFIRMED for obo_JoJ2ch (도하율, now csd=7) via session-entry reconcile (2/2 with obo_GL7SXB 5→6). Direction-agnostic matcher written but untested (test never reached). Stopped per David (stop/return/report/wait). Recommendation to WSL: fix new-word skipToTest path (may be gated until N cards) OR identify real card-advance control OR accept session-entry reconcile as the off-by-one signal. Review: docs/plans/loop/win/reviews/winclaude_053.md. |
| 2026-07-19 | audit/deepfix/task6/d35_obo_complete.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_obo_complete_r52.json (NEW) | **R52 D3.5 tier-3 OFF-BY-ONE disambiguator — drove ONE completion (김우주/obo_GL7SXB, class 25WTa2r12, seeded csd=5, live prod, SANDBOX only, NO 26SM).** Drove the offered day through completion → **class_progress.csd reconciled 5→6** with a `csd_twi_reconciled` server log (M7), **twi stable @360** (no phantom new words — day-6 already complete), canonical=0 (M4 held), not demoted, renders day-7. **VERDICT: OFF-BY-ONE RECOVERED (PASS)** — confirms oracle "advance/reconcile on COMPLETION not bare load" (R51 bare load stayed 5; R52 completion reconciled to 6). **Honest caveat:** MCQ matcher matched 0/30 on the NEW-WORD test (vs 30/30 on review) + outcome=timeout → the advance is reconcile-driven (completion-triggered), not a verified high-score pass; reinforces advance-on-completion-not-score but flags a new-word-test matcher-direction gap for future clean new-word drives. Review: docs/plans/loop/win/reviews/winclaude_052.md. |
| 2026-07-19 | audit/deepfix/task6/d35_batch_drive.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_batch_r51.json (NEW) | **R51 D3.5 tier-3 BATCH drive — 5-student recovery wave (scale-up), live prod, SANDBOX only (roster a2_clone_roster.json, list RmNNkuLPectBlBPiLbAJ, NO 26SM write).** Generalized the r50 token-overlap MCQ matcher over the roster. **THROTTLE 3/3 PASS:** thr_0DnzKs (Day-12), thr_bFV18s (Day-8), thr_yiVt86/김예린 (Day-18) — each rendered Day-(csd+1) review-only, MCQ 30/30=100% → csd held, **reviewMode→false** (escape), `review_recorded` server log (M7), canonical=0 (M4 held), post-escape offers new words. **OFF-BY-ONE 2/2 did NOT reconcile on load (FINDING):** obo_GL7SXB (김우주, csd 5→5) + obo_JoJ2ch (도하율, csd 6→6) — class_progress.csd unchanged post-login; server logged resolve_list_progress + `impossible_phase_detected` but NO csd_twi_reconciled and NO canonical write; both render Day-(csd+2) + offer new words. WSL adjudicates INVALID_PRECONDITION (reconcile may need a completeSession drive, not bare load) vs real gap. Review: docs/plans/loop/win/reviews/winclaude_051.md. |
| 2026-07-19 | audit/deepfix/task6/d35_a1_drive.mjs (NEW), audit/deepfix/task6/d35_a1_postescape.mjs (NEW), audit/deepfix/task6/d35_a1_confirm_seed.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_a1_drive_r50.json (NEW) | **R50 D3.5 tier-3 A1 recovery RE-DRIVE on the CORRECTED seed (`lsr_a2_jisua1`, sandbox uid only — NO 26SM write) → A1 RECOVERY PASS.** WSL fixed the seeder (trims cloned attempts to studyDay≤5) → the seed now renders the intended **Day-6 review-only throttle** (rendersDay6=true, Day-11=false; csd=5 twi=400 reviewMode=null recent[0.23,0.17,0.23] canonical=0). Built a token-overlap MCQ matcher over the list's own `words` subcollection (1600 defs, robust to ①② formatting) + skipToTest to skip study cards. **Review #1 matched 30/30 → 100%** → read-back: csd **held @5**, twi flat @400, **reviewMode→false** (throttle EXITED), `review_recorded` server log (M7 server-path), **canonical list_progress=0 (M4 held)**, no error logs. **Post-escape dashboard: "DAY 6 · Learn 80 new words", CTA "Start new words →" → escape_confirmed.** Deviation from tier-1's 2-review prediction fully explained (100% review cleared throttle in 1 step vs moderate-score hysteresis; outcome identical=escape). Tier-3 clone→drive→assert loop now validated end-to-end. Review: docs/plans/loop/win/reviews/winclaude_050.md. |
| 2026-07-19 | audit/deepfix/task6/d35_a1_inspect.mjs (NEW), audit/deepfix/task6/d35_a1_finalize.mjs (NEW), audit/playwright/findings/deepfix_d35_tier3_a1_drive.json (NEW) | **R49 D3.5 tier-3 seeded-recovery DRIVE on the committed 1-student A1 seed (`lsr_a2_jisua1`, class 25WTa2r11, sandbox uid only — NO 26SM write).** Ran the clone→drive→assert loop on live prod (client 6bffe1c / functions 0ddbb34): login OK, `renderCheck` (F-b) OK, Admin read-back OK. **Finding = `INVALID_PRECONDITION` (seed fidelity):** seed `class_progress.csd=5/twi=400` (intended Day-6 A1 throttle) was RECONCILED ON LOAD to `csd=10/twi=554` by `resolveListProgress` via `safeValuesForDoc` (the M4 read-only-candidate write to the legacy class_progress doc), because the 27 seeded attempts span `studyDay` 1→10 while csd was seeded at 5 → client renders **Day-11**, not Day-6. **M4 invariant HELD:** canonical `users/{uid}/list_progress` count=0 (no canonical write at `LIST_PROGRESS_CANONICAL=false`). NOT a product regression — the smoke correctly caught a seed-construction bug (the r44 F-b risk) before scaling to 156. Fix handed to WSL: align csd with attempts (trim attempts to studyDay≤5, or seed csd=10) + re-drive. Review: docs/plans/loop/win/reviews/winclaude_049.md. |
| 2026-07-17 | scripts/cs/backup-all-26sm.mjs (NEW), scripts/cs/restore-from-backup-26sm.mjs (NEW) | **Full reinstatable READ-ONLY 26SM backup + companion restore, ahead of the one-way progress migration (David: "back up all data so they can be reinstated in a worst-case scenario").** backup-all-26sm.mjs makes ZERO Firestore writes; dumps into a fresh timestamped dir `scripts/cs/backups_full_26sm_<YYYYMMDD-HHMMSS>/`: per cohort student (823 in /26SM/i, 40 classes) the `users/{uid}` root doc + all 5 subcollections (class_progress, session_states, study_states, list_progress, progress_meta), all `attempts` where studentId∈cohort, the 26SM `classes/*`, every assigned+referenced `lists/*`, and a MANIFEST.json (timestamp, git HEAD, per-collection + per-student counts, class/list/student id lists, failures, spot-check). Each collection → its own JSON file as a `[{path,id,data}]` array (one entry/line, stream-parseable); Firestore Timestamps serialized losslessly as `{__ts_millis__,__ts_seconds__,__ts_nanoseconds__}` (+ DocumentRef/GeoPoint/Bytes markers). Paged (limit 1000, orderBy documentId) so study_states (896,885 docs, HUGE>100k) never blows memory; 8-way student concurrency; exponential-backoff retry so a transient API error can't kill the run mid-way (the prior attempt's failure mode). restore-from-backup-26sm.mjs = companion (dry-run default; `--commit --dir=<dir>`), stream-parses each file line-by-line, reconstructs Timestamps, full-doc set() back to each original path via bulkWriter. **Ran the backup: dir `backups_full_26sm_20260717-165840`, 0 failures, spot-check PASS (re-queried 40 classes + 6 random students' 6 collections each, all counts matched). Counts: classes 40, lists 13, users 823, class_progress 1499, session_states 951, study_states 896885, list_progress 0, progress_meta 0, attempts 25640.** All 9 files full-parse to their manifest counts; Timestamp serialize→deserialize round-trip verified ns-precise. restore script `node --check` clean, NOT run. See SUPPORT_RUNBOOK CS-2026-07-17b. |
|------|------|--------|
| 2026-07-13 | functions/index.js:58 (GRADE_TOKEN_ENFORCED true→false) | **deepfix Task 3 — P0 (G1 disarm), LOCAL-ONLY/uncommitted.** Set `GRADE_TOKEN_ENFORCED = false` to match LIVE prod (F-9: recent 26SM attempts write `correctnessSource:null` ⇒ enforcement off). HEAD had carried `true` (committed `4b82a0a`, BEFORE the 06-29 mass-save outage; prod's false was never committed back), so a functions deploy from HEAD would RE-ARM the 06-29 outage while the localStorage-nonce docId-divergence (`testRecovery.js:98-111`) is still UNPATCHED. Re-arm (→true) only with the validated nonce fix (FIX_PLAN P4 F1-F3) + F5 acceptance. `node --check` clean; zero behavior change (aligns repo→prod). Diff: `audit/deepfix/task3/phase0_diff.patch`. Per the CONVERGED `audit/deepfix/task2/FIX_PLAN.md` v3 (Codex GO ×2 + 3-verifier) + `reports/TASK2_REPORT.md`. Owner deploys; David pre-authorized autonomous impl. |
| 2026-07-13 | scripts/cs/deepfix-census2.mjs (new) + scripts/cs/deepfix-f6-tokens.mjs (new) | **deepfix Task 1.6 empirical scans (READ-ONLY, no writes).** `deepfix-census2.mjs` — deep per-student pass: F-2 (precise #13 recount pinned to attempt.classId → 18 real undersized), F-3 (dual-enroll split via student+list anchor → 36 LIVE-STRAND + 6 divergent + 72 stale), F-4 (H/P/B partition 541/45/188), F-9 (deploy-state: prod ENFORCED:false + server-writes), F-11 (twi-vs-mastery via study_states count() → 4). `deepfix-f6-tokens.mjs` — challenge-token + permanent-fail census (3 permanent-fail, all known cases; 3/817 locked; 614 pending backlog). Exports to `audit/deepfix/task1/firebase/scan_*.json`. No app-source touched. Task 1 deliverables: `audit/deepfix/task1/ROOT_CAUSE_FINDINGS.md` + `reports/TASK1_REPORT.md`. |
| 2026-07-13 | audit/deepfix/task1/investigations/inv_I10_permission_gap.md (NEW, docs-only) | **DEEPFIX Task 1.6 I-10 — C-19 promotion permission gap PINNED (read-only code trace).** Attempt permanently stamped `teacherId = launching class ownerTeacherId` (db.js:1194-1204/1353-1372; functions/index.js:348); gap enforced at 3 stacked predicates all keyed to that stamp: gradebook query `where('teacherId','==',uid)` (db.js:1926), reviewChallenge throw `attemptData.teacherId !== teacherId` (db.js:2666 — the "단어 권한이 없습니다" mechanism), and attempts rules read/update `teacherId == uid` (firestore.rules:102-105, 114-118 — partial CORRECTION of H1-gate "not rules-scoped"); plus old teacher goes roster-blind (name filter hard-empty-return db.js:1917-1921). Secondary: accepted challenge unlocks OLD-class progress/session docs (db.js:2791-2836); separate C-16 security finding: rules:45-48 any-teacher-writes-any-student subcollections. C-19 upgraded plausible→verified. No app-source touched. |
| 2026-07-13 | scripts/cs/deepfix-census.mjs (new) + scripts/cs/deepfix-f1-syslog-attribution.mjs (new) | **deepfix Task 1 empirical tooling (READ-ONLY, no writes).** `deepfix-census.mjs` — comprehensive 26SM cohort census over every problematic-state signature (#11 review-only freeze, dual-enroll-same-list, twi-over-list/mastery, invalid-anchor, manual-patched population, test-size mismatch, review-no-new-pass, csd-implausible, config drift) → exports `audit/deepfix/task1/firebase/{census_rows,census_classes,syslogs}.json` + summary; findings: 816 students/1121 started, 183 at #11 wall, 98 dual-enroll, 82 hand-patched (25 re-stuck), data NOT corrupt. `deepfix-f1-syslog-attribution.mjs` — attributes `impossible_phase_detected`/`day_guard_rejected_session_cleared` (26SM vs 25WT sandbox via uid + newTestId) → `scan_F1_syslog_attribution.json`; finding: day_guard fires for 6 real 26SM students (contradicts "0 live"), impossible_phase = 531 real states (emitter studyService.js:105-114). Reuses the data-integrity-sweep Admin-SDK pattern; run via `NODE_PATH=/app/node_modules node …`. No app-source touched. |
| 2026-07-11 | src/config/featureFlags.js:41 | **ENABLE `LIST_SCOPED_RECON` (false→true)** — turn on Phase 1 list-scoped CSD/TWI reconciliation (PLAN_list_progress_persist.md v3.7, Codex round-8 audit-clean; code Codex-reviewed ×5; Run-L-certified flag-off-neutral, L_20260706_014108). Fixes the class-change day-reset cluster (NEED_TO_FIX #6). **PRE-DEPLOY HARD REQUIREMENT: the LIST_SCOPED_RECON attempts composite indexes must show ENABLED in the Firebase console FIRST — flag-on without them = query-error fallbacks everywhere (per the flag's own comment). POST-DEPLOY: Run S (flag-on behavioral validation) is still a design stub — not yet run.** Owner deploys.
| 2026-07-11 | docs/plans/loop/x/plan.md (v5), x/rounds/r03_v4_audit_synthesis.md, handoffs/claude_to_codex_003.md, baton.json | **Cycling loop CLOSED — finalized as gated capstone.** Ran the Claude⇄Codex+3-agent loop to convergence: v1 (extend shared list) withdrawn → v2/v3/v4 (per-student monotonic virtual index) hardened over Codex r001/r002 (long-turn mode, warm across rounds — validated David's "6min not a hard ceiling") + two 3-agent audits. Strategic finding: safe cycling requires removing the allocation cap → removes the clamp that makes progress-forgery self-defeating; W3 does NOT close it (anchors client-echoed, class_progress student-writable) — real gate is server-authoritative twi, a companion to the student-owned progress re-key. v5 = hardened, prerequisite-gated CAPSTONE spec (per-lap mastery dropped as non-goal; §3c lap-bounding single review mechanism; challenge-path phase-gated; monotonic kept over reset-twi). Baton set turnOwner=done/codexDecision=GO; Codex loop stopped. Pivoting to the foundation: PLAN_list_progress_persist.md (student-owned progress, v3.7 AUDIT-CLEAN). |
| 2026-07-11 | docs/plans/loop/lib/CLAUDE_ROUND_SOP.md + docs/plans/loop/x/plan.md (new) + docs/plans/loop/WORKFLOW_QUEUE.md (new) | **Plan-loop v5 — corrected per-round contract + definite termination + first real plan.** Per David: the 3-agent audit now runs CONCURRENTLY with Codex on EVERY plan version incl. v1 (launched at hand-off, not after Codex returns); Claude then verifies BOTH Codex's AND the 3 agents' findings against real code (reject wrong ones from either source) before editing. Termination made definite + non-infinite: CONVERGE = both sides 0 blocker/high on an unchanged plan for 2 consecutive rounds; nits/medium never block (not too strict); backstops = maxRounds(8) hard cap / deadlock (same finding raised+rejected 2×) / drift (3 rounds no decrease) → STALEMATE.md + stop. Drafted first real plan `x/plan.md` (extend-list cycling, from LIST_CYCLING_DESIGN.md option C) + launched round-1 (Codex reviewing v1 in Docker + 3 Explore agents A/B/C auditing v1 in parallel). WORKFLOW_QUEUE.md tracks the reusable sequence: cycling → extensive Playwright-audit plan (same loop) → operationalization doc. | | **Plan-loop v4 — reconciled to Codex's Dockerized worker.** Codex built + smoke-validated (fresh + resume) a Docker headless worker: `/repo:ro` + `/out:rw` (== `<slug>/codex-out`) + auth in a `codex-home` volume; runs `codex --ask-for-approval never exec [resume --last] --sandbox danger-full-access -C /out`. Write-scope now HARD-enforced by the read-only `/repo` mount (Docker is the sandbox) — stronger than the `-C` trick. Claude can't run Docker (no daemon/socket) but reads/writes the shared baton; host `watch-and-run.ps1` fires the container on `turnOwner=codex`. Reconciled my side to Codex's baton schema: `turnOwner`/`task`/`repoRoot`/`instruction`/`codexStatus`/`codexLastReview` (per-turn task now travels IN the baton's `instruction`) + my orchestration fields; baton.mjs reads strip UTF-8 BOM (PowerShell writes one — verified against Codex's real baton), added `set-file` for long instructions; baton-watch polls `turnOwner`; SOP authors container-path (`/repo`,`/out`) instructions. Retired lib/codex-driver.ps1 (direct `codex exec` superseded by the container). All plumbing smoke-tested (BOM read of real baton, set-file, waiter wake). Awaiting first real plan + end-to-end dry run. | | **Plan-loop v3 — HARD sandbox write-scope + adversarial verification.** Codex tested that running `codex --ask-for-approval never exec -C <codex-out> --sandbox workspace-write` keeps the repo READABLE (absolute path) while confining WRITES to the `-C` dir (writes to plan.md / siblings / repo root / `../` traversal / shell-command all FAIL at the sandbox) — the exact "read repo, write one dir" policy, no OS-ACL copy needed. Driver upgraded: `-C` now points at `<slug>/codex-out` (was repo root), `--ask-for-approval never` moved to global position (matches Codex's tested form); post-turn git+plan.md guard demoted to defense-in-depth. Added `_verify/` throwaway probe harness (PROBE_MANIFEST.json 12 writes + 4 reads + baton read incl. `../`/absolute/shell escape attempts; PROBES.md instructions; toy plan.md + baseline hash) and `lib/verify-sandbox.mjs` — Claude's INDEPENDENT check of the shared filesystem (which fail-targets actually got created = escape; plan.md/git HEAD untouched), not trusting Codex's self-report. Verifier dry-run against clean baseline correct (8 would-be escapes absent, 0 escapes). Awaiting Codex to run the probe. | | **Plan-loop v2 — subscription invocation + write-scope hardening** (per Codex's headless-access answer + David's read-only-except-one-dir requirement). Driver now invokes **subscription-backed** headless (`codex exec [resume --last] --sandbox workspace-write --ask-for-approval never -o …`, no `--oss`/`--ephemeral`/API-provider; prereq `codex login`+`codex doctor`). New layout gives Codex a single writable dir `<slug>/codex-out/` (baton.json + reviews/ + logs/ + last_message.md); `plan.md` + `rounds/` (Claude-owned) sit OUTSIDE it. Enforcement in layers: prompt-scope + driver git-`status --porcelain` diff & `plan.md` SHA-256 guard (drops `codex-out/logs/rNN_SCOPE_VIOLATION.txt` on any stray write) + Claude SOP §0b halt-on-violation. baton gains `codexStatus`/`codexLastReview`. Documented "Option B" OS-ACL isolated-copy path for hard enforcement if a violation ever fires. |
| 2026-07-11 | docs/plans/loop/ (new: README.md, lib/baton.mjs, lib/baton-watch.sh, lib/codex-driver.ps1, lib/CODEX_STANDING_INSTRUCTIONS.md, lib/CLAUDE_ROUND_SOP.md) | **New tooling — automated Claude⇄Codex plan-review loop** (iterate a plan until both Codex AND a 3-agent audit find nothing, on an unchanged plan). Coordinates via a `baton.json` turn-token on the shared drvfs mount (`/app` == Windows `C:\Users\dmchw\vocaboost`, confirmed same physical files). Claude self-wakes via a backgrounded `baton-watch.sh` (exits on its turn → harness re-invokes Claude); Codex is launched by `codex-driver.ps1` on the Windows side via **`codex exec resume --last`** (headless but WARM — keeps session context, avoids cold codebase re-exploration that would burn a light Codex plan). No SendKeys/keystroke-injection (earlier `codex-watch.ps1` SendKeys approach retired). `baton.mjs` adds a monotonic `revision` field (CAS/change signal per Codex's no-locking caveat) + optional `codexSessionId` (pin `resume <id>` vs `--last`). Design driven by probing: no WSL↔Windows interop (Claude can't launch Codex), so Codex self-drives its own baton flip. Smoke-tested (baton init/set/get + waiter wake-on-flip + converged-exit). Not yet run against a real plan (awaiting shared-FS marker confirmation + a plan topic). |
| 2026-07-02 | LIVE digitalsat.co.kr 26SM-T10 uid 11213 (M2U·Q15, woolly-mammoth Hi-C, Logical Completion) + dsg-edits/deploy_11213.mjs (new) | **Full question replace** on **M2U·Q15 (118379)** — David's revised text (passage + stem + all 4 choices). Single slot (uid 11213 not dual). Passage revised ("paleogenomicists"→"researchers reconstructing ancient genomes"; glass-like solid), choice C reworded ("karyotype match"→"chromosome count matching… no structural rearrangement during the 52,000 years"), choice D ("vitrified soft tissue"→"glass-like tissue"). **Answer key kept = B** (score 59), difficulty/passage-type untouched. Verified: passage byte-exact (proper em-dash decode), stem+choices ✓, key=B ✓, save ok. Backup → backups_11213/. (Supersedes the passage set in the 07-01 T10 highlighted run.) |
| 2026-07-02 | LIVE digitalsat.co.kr 26SM-T10 uid 10494 (Georgia Douglas Johnson "Peace", PUR) — 2 anchor slots + dsg-edits/deploy_10494.mjs (new) | **Stem + all 4 choices replaced** on the shared anchor at **M2L·Q7 (118403)** and **M2U·Q7 (118387)** (David-supplied text). Passage, answer key (**D**, score 59), difficulty, passage-type all LEFT UNCHANGED (verified passage byte-identical pre/post; key still D). Confirmed per request: the passage underline is on the **last line only** — `<u>Forgetting, and—forgot!</u>` (single span) — so the new stem's singular "underlined line" is correct (old stem wrongly said "lines"). Both slots re-verified: stem ✓, choices A–D ✓, key=D ✓, passageUnchanged ✓. Backups → backups_10494/. |
| 2026-07-01 | LIVE digitalsat.co.kr 26SM-T10 question bank (7 content + 2 chart questions) + dsg-edits/deploy_t10.mjs (new) | **26SM-T10 targeted edits from `26SM-T10-final-2026-06-26.xlsx`** (David: change the YELLOW-highlighted cells; IDs in 2nd sheet; chart PNGs sourced ONLY from `26SM-T10_ALL.docx`). Detected the 10 yellow cells by parsing raw xlsx styles (styles.xml yellow fill→cellXfs→cell `s=` refs, since SheetJS doesn't expose fills). **Part A — 7 content Qs, highlighted fields only** (content-only; key/difficulty/passage-type untouched, VOC empty-enabler only): M01·Q5/118418 [Passage,A,C], M01·Q6/118417 [B], M01·Q8/118415 [A,B], M01·Q9/118414 [B], M2U·Q1/118393 [Passage], M2U·Q7/118387 [Passage], M2U·Q15/118379 [Passage] — all fields re-verified ✓ (italics preserved). **Part B — 2 dual chart Qs** M2L·Q12/118398 + M2U·Q13/118381 (same GFR-PP tensile-strength chart): "replace passage entirely" = the xlsx passage is `<p><svg>…</svg></p>` (platform strips inline SVG → chart-as-text) + prose `<p>`; swapped the SVG-`<p>` for the docx PNG (`image1.png`, uploaded→hosted blob) and kept the prose → **chart image ABOVE prose, duplicate chart-text removed** — vision-verified + confirmed no residual SVG-text. Per-Q backups → `backups_t10/`. Chart-Q passage-type shows Empty (same G&T→Empty as T07, harmless). Auth refreshed via login_save.mjs. |
| 2026-06-30 | LIVE digitalsat.co.kr 26SM-T07 M02 question bank (16 questions + 2 charts) + dsg-edits/{build_t07_xlsx,deploy_t07_m02,deploy_t07_charts,compare_t07}.mjs (new) | **26SM-T07 M2U(M02) content replaced from docx** (source: `여름특강T07…-M01/-M02.docx`; David: "docx is the right version, deploy content only, key same"). Flow: parsed docx (italics+paragraph-aware via python zipfile) → built `t07_from_docx.xlsx` (29 rows) → **verified docx↔xlsx 29/29** → pulled live (IDs from `aclass26smt07.xlsx`, 116389–116404) → normalized-compared → **5 sub-agents adjudicated**: M01 all 13 match (formatting only), M02 14/16 had real content diffs (live was a different/older version). Deployed **all 16 M02** content (passage/stem/choices) from docx — **key, difficulty, explanation UNTOUCHED** (David confirmed answer key same); block-aware writes preserve italics (`<i>The Shadow-Line</i>`), multi-paragraph, paired Text1/Text2. Per-Q backup → `backups_t07m02/`. **Content 16/16 re-verified ✓** (full entity decoder). Extracted 2 PNGs from docx M02 (`word/media/image1,2.png`) → inserted as hosted charts into **Q12 (116393)** + **Q14 (116391)** via `uploadImages()` (intro text + centered img, base64→blob). **⚠️ side-effect:** the VOC save-enabler clicked "Empty" 지문유형 on Q12/Q14 (graph Qs that had the no-radio G&T DB value) → their passage-type is now Empty, and the 4-radio UI (Literature/Humanities/Science/Empty) can't re-set "Graphs and Tables" — flagged to David, who accepted leaving it Empty. Charts placed ABOVE the intro text (per David) + vision-verified (correct image on each Q). M01 not touched (already matched live). Auth refreshed via login_save.mjs. |
| 2026-06-29 | LIVE digitalsat.co.kr 26SM-T09 question bank (21 questions) + dsg-edits/edit-t09-format.mjs (new) | **26SM-T09 formatting pass executed on live** (source: `26SMT09 formatted.xlsx`; items: `EDITEDITEMS.md` — 21 of 45 T09 rows). Markup-only (italics on titles/genus-species/foreign terms, lit-canonical `<p>`-per-block, generation-artifact strips: nf-voc / source-URL / worker-record). Wrote ONLY the EDITEDITEMS-flagged fields per Q (Passage and, for 4 items, Stem; for M2L·Q8 all 4 choices) — key / difficulty / explanation / passage-type left untouched (only the VOC Empty-enabler to avoid the 지문유형 save-block). **New surgical script** (NOT the old edit-t09t10.mjs, whose `wrap()` would nest `<p>` inside `<span>` and corrupt lit-canonical passages): block cells (already `<p>…`) set VERBATIM, inline cells wrapped in the live `<p><span font-family:'Droid Serif'>` so the font is preserved. Per-Q full backup → `backups_t09fmt/`. IDs 118257–118297 (single live copy; same copy the 06-27 batch used — qid map has no 1170xx for T09). Result: **21/21 save:ok, all fields verified by reload (normalized text matches formatted + italics present), 0 save-blocks**; validation subset (118297/118294/118277 = artifact+italics / lit-canonical / passage+all-choices) run first, then the remaining 18. Auth refreshed via login_save.mjs (06-27 session had expired). **T10 (24 items in EDITEDITEMS) NOT done — its formatted sheet was not provided.** Results: `t09fmt_results_*.json`. |
| 2026-06-29 | scripts/stamp-build.mjs (new) + firebase.json + functions/index.js + .gitignore | **P1 deploy-provenance: deployed-SHA stamp + `version` probe.** Root motivation: the 2026-06-29 grader incident — the "accept Korean-definition" fix was committed since 2026-03-10 yet prod ran a stale artifact for months (undetectable: no live-version signal); and right now `/app` index.js line 45 = `GRADE_TOKEN_ENFORCED=true` while prod *behaves* as false → repo↔prod drift you can't see. Fix: (1) `scripts/stamp-build.mjs` writes `functions/buildInfo.json` (git sha/shortSha/branch/dirty/builtAt) — wired into `firebase.json` `functions.predeploy` so it runs on every `firebase deploy --only functions`; never fails the deploy (no-git → "unknown"). (2) `functions/index.js` loads buildInfo (safe fallback), logs `{build}` on every cold start, and adds `exports.version` (auth-required onCall) returning the live **sha + runtime flags** (`GRADE_TOKEN_ENFORCED`, `GRADE_TOKEN_MINT`, `GRADE_JOB_ENABLED`, `GRADE_JOB_LEASE_MS`) + serverTime — so "which commit/flags are LIVE?" is one call, not behavioural detective work. (3) `.gitignore` ignores the generated buildInfo.json (still bundled into deploy — not in firebase.json `ignore`). Validated: `node --check` index.js OK; stamp script runs (sha 05e3527, dirty=true). NOT committed; owner deploys. **Post-deploy verify:** call `version`, confirm `sha` == `git rev-parse HEAD` of the deployed checkout AND `flags.GRADE_TOKEN_ENFORCED===false`. |
| 2026-06-28 | public/help-student-{en,ko}.html + public/help-teacher-{en,ko}.html + public/help-img/{dashboard,results}.png | **Published help-file refresh** (linked from HelpModal). STUDENT: rewrote §2 Dashboard (was the removed "Command Deck/Weekly Goals/Mastery Rate/7-Day Activity" → now the redesigned hero card + 4 tiles incl. "Avg Review Score" + "This week" chart, with an embedded `help-img/dashboard.png` screenshot); added a results-screen screenshot (`help-img/results.png`) + a "grading hiccups → just reload, auto-recovers, no double-grade" success box in §5 Tests; added a "refresh to see your teacher's challenge decision" tip to the Challenge section; added 2 FAQ items ("Grading Failed → reload"; "teacher accepted but score unchanged → refresh"). TEACHER: added to "Reviewing Challenges" a step + warning box that **the student must refresh** to see an accept/reject (and accept may auto-advance the day), plus a "student reports Grading Failed → tell them to reload" tip. Added `figure.shot` CSS to both student files. All EN changes mirrored in KO. Verified via Playwright (file:// render): images load (naturalWidth 2400), 0 console/page errors, layout clean (EN+KO). Screenshots captured live from the deployed app (mday sandbox accounts). NOT committed; needs Netlify deploy to publish (HTML + the 2 new images under public/help-img/). |
| 2026-06-28 | functions/index.js + dsg-edits/srv_validate/grading_job_tests.mjs | **Grading recovery slice — Codex review fixes (round 3, fail-open closure).** finishGrading now FAIL-CLOSES every non-authoritative persist outcome (Codex): `persisted`→return own payload; `already_graded`→return canonical cached payload; `superseded`/`lease_expired`/`absent`→throw `aborted` (client retries → winner's grade); `error`→throw `unavailable` (retryable). Previously `absent`/`error` returned the worker's grade without confirmed authority. (Known rare edge: a persist-txn `error` orphans the claimed lease until it expires → ~180s self-heal via takeover; persist-txn errors are uncommon and auto-recover.) Tests: T8/T9 now `waitForClaim` (poll the job until `status:claimed`) instead of a fixed 900ms sleep (cold-start flakiness); harness password reads `process.env.AUDIT_PASS` (sandbox fallback retained, commented). node --check clean. |
| 2026-06-28 | functions/index.js + dsg-edits/srv_validate/grading_job_tests.mjs | **Grading recovery slice — Codex review fixes (round 2, final blocker).** (Blocker) `persistGradingJobResult` now RETURNS its outcome (`persisted`/`already_graded`/`superseded`/`lease_expired`/`absent`/`error`) instead of silently returning; `finishGrading` THROWS `aborted` on `superseded`/`lease_expired` so a stale worker no longer returns a grade the client would persist, and on `already_graded` returns the canonical cached payload (not its own). (Tests) added T8 (superseded-at-persist → aborted) + T9 (lease-expired-at-persist → aborted), the exact transitions Codex flagged (timing-sensitive: mutate the job's lease ~900ms into a live grade). (Relabel) the server const-block + test banner now say "GRADING RECOVERY SLICE (Phase 1a)" and state explicitly that cross-device dedup = Phase 2; this slice only dedups RETRIES of the same attempt (nonce identity), not two devices. node --check clean. Decision: ship this slice (owner deploys), then begin Phase 2 (deterministic identity + immutable submissions + W3) immediately. |
| 2026-06-28 | functions/index.js + src/pages/TypedTest.jsx + dsg-edits/srv_validate/grading_job_tests.mjs | **Grading Phase 1 — Codex review fixes (round 1).** (#2 fencing) `persistGradingJobResult` now requires matching leaseId AND unexpired lease (plan Recheck-5 policy; comment clarifies Date.now() is Cloud-Functions server time, 180s lease ≫ skew). (#3 in-progress convergence) client `gradeWithRetry` now POLLS `getGradingStatus` on `aborted`/in_progress (new `pollForGrade`/`fetchGradingStatus`) instead of plain-retry-then-fail — concurrent submits converge on the live worker's grade. (#4 kill-switch) `getGradingStatus` returns `absent` when `GRADE_JOB_ENABLED=false` → true byte-for-byte rollback. (#5 UI) deterministic-error modal now shows a **Reload Page** button (window.location.reload) instead of "Try Again" (which would repeat the deterministic failure); added `gradingErrorKind` state + reworded titles. (#6 tests) new `grading_job_tests.mjs` — 7 transition tests (cache-hit/no-re-grade, status channel, ownership denial, expired-lease takeover, live-lease in_progress, concurrent claim, rules deny-write) to run post-deploy. node --check clean. NOTE: scope of this slice = lost-response/same-attempt recovery, NOT cross-device dedup (that needs deterministic identity = Phase 2). |
| 2026-06-28 | functions/index.js + firestore.rules + src/pages/TypedTest.jsx | **Grading Phase 1 — idempotent grading job + lost-response recovery** (PLAN_grading_idempotent_concurrency.md, Phase 1 = recovery-only: NO attempt-identity change, NO side-effect move; flag default ON per owner). **Server:** new `grading_jobs/{attemptDocId}` keyed off the client's EXISTING attemptDocId (nonce identity, unchanged). `claimOrRecoverGradingJob` (tiny txn, NOT around the AI call): returns cached grade if `graded` (lost-response recovery, no re-grade), `in_progress` if a live lease is held (→ retryable `aborted`), else grants a lease + grades. Crash-safe: expired `claimed` lease is taken over + re-graded; `persistGradingJobResult` caches the grade-only payload FENCED on `leaseId` (a taken-over zombie worker can't overwrite). New `getGradingStatus` callable = owner-readable status channel (`graded`/`in_progress`/`stale`/`absent`). Consts `GRADE_JOB_ENABLED=true` (kill-switch = flip false + redeploy → byte-for-byte pre-Phase-1), `GRADE_JOB_LEASE_MS=180000` (> max grade duration so a normal grade isn't fencing-rejected). All additive — when disabled or no attemptDocId, exact prior behavior. **Rules:** `grading_jobs` server-write-only (`create,update,delete: if false`), owner-read. **Client (TypedTest.jsx):** `gradeWithRetry` now (a) recovers via `getGradingStatus` before retry/throw (lost response → cached grade), (b) does NOT loop on deterministic errors (`invalid-argument`/`failed-precondition`), (c) error modal copy branched by errCode (reload vs retry), dropping the misleading "Your answers are saved". Client recovery degrades to no-op if server flag off (status:absent). node --check clean; functions eslint = pre-existing env-noise only; client esbuild not runnable in-container (win32 binary) → owner builds. NOT committed. Owner deploys (functions + rules + client); then Playwright validation. |
| 2026-06-27 | functions/index.js | gradeTypedTest grader calibration (NEED_TO_FIX #2), two findings from the live quirk battery. (1) **Loanword transliteration**: rubric now accepts a Korean transliteration ONLY when it is the standard/most-commonly-used Korean word for the term (piano→피아노, computer→컴퓨터, repertoire→레파토리, bus→버스 = CORRECT) while still rejecting ad-hoc sound-outs no one uses (grief→그리프 = WRONG). Reworded `<rules>` self-reference rule into a "sound-it-out vs established loanword" test; replaced the renaissance WRONG example (conflicted with the new rule — 르네상스 IS the standard Korean word) with grief→그리프 WRONG + added piano→피아노 and repertoire→레파토리 CORRECT examples. (2) **Inflection**: an inflected/derived ENGLISH form of the target word with no meaning is now WRONG (run→running, candid→candidly). Added `isInflectionOfWord(response, word)` helper (ASCII-only, handles consonant-doubling/drop-e/y→i, so a Korean different-POS translation like impoverish→가난한 stays CORRECT) and wired it into post-validation Rule 2 (replacing the old word/+s/+ed/+ing exact check) + the rubric rule #1 (covers irregulars like ran). node --check clean; helper unit-tested offline 15/15. NOT yet deployed — needs `firebase deploy --only functions`; quirk battery re-runs against live post-deploy. |
| 2026-06-22 | src/config/featureFlags.js + src/pages/TypedTest.jsx + src/pages/MCQTest.jsx | Server-side attempt write — Phase 1 CLIENT half. Added `SERVER_ATTEMPT_WRITE` flag (default FALSE). When ON, both test pages route the durable attempt write through the `submitVocabAttempt` Cloud Function (server scores/persists transactionally + idempotently on the same `attemptDocId`) instead of the client-side `submitTypedTestAttempt`/`submitTestAttempt`. Localized swap inside the existing try — the failure-catch (block progression + preserve localStorage recovery + leave study_states untouched + return) and the post-write `processTestResults`/`completeSessionFromTest`/`clearTestState` ordering are unchanged. Flag OFF = byte-for-byte old behavior. esbuild-clean. **Deployed server fn validated E2E** (real persona token): MCQ 10/50→score 20 (not 100), review-always-passes, idempotent 2nd-call alreadyWritten, invalid-anchor refused+not-written, valid anchor 30/30→100. Client NOT yet live (needs Netlify deploy; flag stays OFF until validated on sandbox). |
| 2026-06-22 | functions/index.js | Server-side attempt write — Phase 1 SERVER half (PLAN_server_side_attempt_write_v2.md §13). Added: `writeAttemptTxn` (transactional, idempotent on the client `attemptDocId`; auth+enrollment via classes.studentIds/users.enrolledClasses; server passThreshold with review-always-passes; scores against ctx.totalQuestions so skipped count as incorrect; echoes the client anchor; REFUSES an invalid new-word anchor — CS-2026-06-21); `readExistingAttemptForContext` (ownership+context checked); `normalizeExistingAttempt` (stored aiReasoning→API reasoning); `buildTypedAttemptAnswers` (correctDefinition→correctAnswer + challenge fields); new `submitVocabAttempt` callable (MCQ + typed write-retry). Extended `gradeTypedTest`: HttpsError-ified all throws (was raw Error → opaque functions/internal), pre-AI ownership-checked existence check (no token re-spend on lost-response retry), both result exits (no-AI + AI) route through `finishGrading` which writes when `writeContext` present and returns the grade (NOT re-billed) on write failure. Backward-compatible: engaged ONLY when client sends `writeContext` (no client does yet). users.stats.credibility/retention side-effect DEPRECATED server-side (grep: 0 UI consumers). node --check clean; eslint = only the pre-existing env-gap no-undef errors (deploy lint is `eslint . || exit 0`). NOT yet deployed (needs `firebase deploy --only functions` on Windows). Client cutover = follow-up, validated against the deployed fn. |
| 2026-06-21 | src/services/db.js | F1b: `fetchUserAttempts` now caches resolved list titles by listId (`listTitleCache` + `resolveListTitle`) instead of a `getDoc(lists/{listId})` per attempt. Kills the N+1 sequential reads (e.g. 70 attempts on one list = 1 read, not 70) that slow-loaded `userAttempts` and fed the dashboard hero-phase race. Caches the resolved string incl. the 'Vocabulary Test' fallback; keeps the `if(listId)` guard. Plan: DASHBOARD_OMNIBUS_FIX_PLAN.md. |
| 2026-06-21 | src/pages/Dashboard.jsx | F1a/F3 follow-up (Codex review of the impl): (Critical) stat tiles called `totalWordsIntroduced.toLocaleString()` on the now-possibly-undefined destructured value when panelBState returns `{loading}` → crash; tiles now render `tileSk` skeletons (or safe `tIntro`) gated on `anyLoading`. (High) hero showed the "No active list" empty state during the settings/classes-load window; added a `firstPaintLoading = !settingsLoaded || studentClassesLoading` full-skeleton hero branch BEFORE the `getPrimaryFocus ? hero : empty` ternary, so the empty state only shows for a genuinely class-less student post-load. (Medium, KNOWN-EXCEPTION not fixed here) ESLint `react-hooks/rules-of-hooks` still flags pre-existing conditional hooks: the `isTeacher` early-return (Dashboard.jsx ~:768) precedes the student-only hooks (getPrimaryFocus/classOptions/listOptions/panelBState/panelCState/dailyActivity). This change ADDS hooks (classOptions/listOptions) inside that existing student-only region. NO RUNTIME CRASH under the current flow: PrivateRoute blocks Dashboard until `!initializing && user`, and AuthContext sets `user` atomically with `role` (never non-null without role), so `isTeacher` has its final value on first render and the hook count never flips. Vite build does not run ESLint. Proper fix tracked as a follow-up: split Dashboard.jsx into TeacherDashboard + StudentDashboard (removes the conditional-hook structure without hoisting large student-only logic above the teacher path). |
| 2026-06-21 | src/pages/Dashboard.jsx | F1a (hero-phase loading race) + F3 (class selector). F1a: added `userAttemptsLoading`/`progressDataLoading` (init false, set inside guarded fetches, reset in finally) + `settingsLoaded=userSettings!==null`; progress effect now fetches the UNION of assignedListDetails/assignedLists/assignments keys (so a focus key is never unfetched) and releases the flag on the no-class path; `panelCState`/`panelBState` gate in order settings→focus→loading BEFORE deriving phase/numbers (prevents the wrong 'Start new words'/'Day1 COMPLETE' flash and the spurious `impossible_phase_detected` write); hero renders white-tinted skeletons (ring numbers, chips, CTA column) while loading. F3: replaced the list-only `Studying:` selector with a `FocusControl` (renders as borderless label when ≤1 option, dropdown when ≥2) ×2 — Class + List controls; parent-controlled `openFocus` (mutual close) + ESC/outside-click; `classOptions`/`listOptions` (class-qualified so selecting never writes classId=undefined); `handleClassSelection` picks the class's primary list (most-recent assignedAt) with a same-class assertion; gated on `settingsLoaded && getPrimaryFocus && classOptions.length` (no preference-clobber during settings load); removed dead `availableLists`/`showListSelector`. Plans: DASHBOARD_OMNIBUS_FIX_PLAN.md + DASHBOARD_CLASS_SELECTOR_PLAN.md. |
| 2026-06-20 | `src/pages/Dashboard.jsx` | **Student dashboard redesign** (branch `redesign/student-dashboard`; backup at `backups/Dashboard.jsx.20260620.bak`). Replaced the 3-panel "Command Deck" (~lines 1345–1582) — which duplicated one list's data 3–4× (Start Session ×3, Day ×2, list title ×2, progress in 3 framings), led with a demoralizing "0/400 weekly" hero, a mislabeled "Mastery Rate 0%" (really `avgReviewScore`), and an empty "7-Day Rhythm" placeholder. New layout: ONE consolidated hero (conic-gradient ring = list completion `totalWordsIntroduced/wordCount`, title, streak + words-left tags, single "Start Session" CTA previewing day + new-word count from `getPrimaryFocus.pace`) → 4 honest tiles (Words Introduced / **Avg Review Score** [renamed] / Words Left / Streak) → real "This week" bar chart bound to existing `dailyActivity` (replaces empty rhythm). Wired only to existing in-scope vars; "My Classes" join/list kept. esbuild-validated (full file transpiles clean); NOT build/screenshot-verified in-container (rollup linux binary absent — verify via `npm run dev` locally). FOLLOW-UP: exact "Mastered/Needs Review/Blind Spots" tiles need 3 small `study_states` aggregation queries (not yet in this component); current tiles use honest available data. |
| 2026-06-19 | `src/pages/DailySessionFlow.jsx`, `functions/index.js` | **Fix: "Grading Failed" loop on test resume after refresh.** Root cause: the two local recovery snapshots saved the test word pool as `wordPool.map(w => ({ id: w.id, word: w.word }))` — dropping `definition`. When a student refreshed/crashed mid-test and recovery restored `newWords` from that snapshot (`recoveredWordPool` → `setNewWords(state.newWords)`), every word lost its definition. On submit the client built `correctDefinition: word.definition` = `undefined`, and the grading Cloud Function's `for`-loop validation `throw new Error(...)` rejected the ENTIRE 35-word batch → surfaced as opaque `functions/internal` → "Grading Failed after 3 attempts" with Try-Again looping forever (same stripped pool). Confirmed from prod function logs (`Invalid input: each answer must have wordId, word, correctDefinition... index.js:99`, 18× in one window while a fresh submit "Successfully graded 30 answers"). Data was clean (scanned all 1600+ words across every list = 0 malformed). **Client fix:** both recovery snapshots now persist `definition`, `definitions`, `partOfSpeech` so a resumed test can grade. **Server fix:** replaced the throw-on-first-malformed `new Error` with collect-all + `logger.error` (logs uid + offending wordIds for monitoring) + `throw new HttpsError("invalid-argument", ...)` with a clear, retryable message ("reload the test page and submit again") instead of a generic INTERNAL crash. Branch `fix/grading-missing-definition`; functions redeploy required for the server half (`firebase deploy --only functions`). Follow-up idea: client `gradeWithRetry` should not retry on `invalid-argument` (deterministic) — deferred. |
| 2026-06-17 | `src/pages/Gradebook.jsx`, `src/services/db.js` | UX: gradebook score color now tied to the class pass threshold instead of a hardcoded 80/60 band. `getScoreColor(score, passed)` colors by the attempt's authoritative `passed` flag (computed at submission vs the class's real passThreshold; respects manual overrides) → `text-success` if passed, `text-error` if not (design tokens, was raw `text-emerald/amber/red`). So e.g. 90% in a 92% class now shows red, not green. Added `passed` to the 3 gradebook loaders that previously omitted it (`queryTeacherAttempts` list, the 2nd list query, and `fetchAttemptDetails` detail) — without this `attempt.passed` was undefined and the color would silently fall back. Legacy attempts lacking `passed` fall back to a neutral 80/60 gradient (also tokenized). NOTE: review attempts always have passed=true (review auto-passes), so they always render green regardless of score — owner chose to keep this (option A) for now; adding a real review-test threshold is deferred to the backlog in `ROADMAP_grading_refactor.md` (would make review color correct automatically). |
| 2026-06-17 | `src/services/studyService.js` | Audit Blocker #1 (completion gate ordering): `completeSessionFromTest` Day-2+ branch wrote `saveSessionState(... phase: COMPLETE ...)` BEFORE the new-word-pass gate returned `requiresNewWordRetake`, stamping the durable session_state cache complete for a day that did NOT complete (contradicts Fix #2/#7; pollutes UI/support/admin tooling). Moved the gate ABOVE the COMPLETE write and inside the Day-2+ else branch (dropped the now-redundant `!isFirstDay`); a blocked day now returns early and never stamps complete. Also list-scoped the gate's `getNewWordAttemptForDay` call (see #3 row). `npm run build` before push. |
| 2026-06-17 | `src/services/db.js`, `src/services/studyService.js`, `src/pages/TypedTest.jsx`, `src/pages/MCQTest.jsx` | Audit High #3 (list-scope `getNewWordAttemptForDay`): helper signature `(userId, classId, studyDay)` → `(userId, classId, listId, studyDay)` + added `where('listId','==',listId)`. Without it, a same-class multi-list student with two `'new'` attempts on the same studyDay had the orderBy-submittedAt/limit-1 return the WRONG list's attempt — used by the Day-2+ completion gate AND by Fix H's study-day derivation (which I widened from 1 caller to 3 this session). Updated all 5 call sites (1 in studyService, 2 in TypedTest, 2 in MCQTest) in place. ⚠️ NEEDS a new composite index (`attempts`: studentId+classId+listId+sessionType+studyDay + submittedAt desc) deployed BEFORE this code, or the query fails. NOTE: study-day derivation is still duplicated across the two test pages — extraction to a shared `deriveAttemptStudyDay()` is deliberately deferred to Tier 2 (see `ROADMAP_grading_refactor.md`). `npm run build` before push. |
| 2026-06-17 | `ROADMAP_grading_refactor.md` | DOC (no code): detailed resumable plan for the deferred Tier 2 (de-dup extractions: `resolvePassThresholdFraction`, `deriveAttemptStudyDay`, `getNewWordGateStatus`, attempt-status model — with exact sites/signatures/homes) and Tier 3 (async write-triggered grading: trigger, sweeper, rules, AWAITING_GRADE, gated CTA, the lock, teacher pending-grade queue, completion timing, migration, and a full Playwright/E2E + rules test plan + deploy choreography). Captures the Tier-1 deploy dependency (list-scoped query needs a new composite index deployed BEFORE the code) and the deliberately-deferred study-day dedup. Resume point for the whole grading refactor. |
| 2026-06-17 | `DESIGN_async_grading.md`, `AUDIT_BRIEF_dedup_and_async.md` | DOCS (no code): (1) Spec for asynchronous write-triggered AI grading — client writes attempt as `gradingStatus:'pending'`, Firestore `onDocumentCreated` trigger grades server-side, client `onSnapshot` renders result; adds derived `AWAITING_GRADE` phase, visible-but-gated Review CTA (state-aware messages), two-layer lock (phase-resolution + completion-gate) so a day can't advance without a server-written passing grade, idempotency/sweeper, rules diff, migration, and a §8 "consolidation inventory" enforcing REPLACE-DON'T-PARALLEL (the refactor must DELETE the client grade/score/gate/threshold/studyDay duplication, not run beside it). Folds "ask teacher to grade" in as the error/slow fallback. (2) Codex audit brief covering A: dedup measurement (threshold ~8 sites/37 refs, pass-gate 27 computations, Fix-H studyDay copy-paste, TypedTest/MCQTest twins) with {extract-now} vs {fold-into-async} tagging; B: adversarial review of the async design incl. the lock + consolidation contract; C: architecture health + regression check on this session's patches. |
| 2026-06-17 | `src/pages/DailySessionFlow.jsx` | P1 #7 (overwrite race / "bad session state persisting"): made session resume routing ATTEMPT-AUTHORITATIVE. Removed the `sessionSaysReviewResume` branch (and now-unused `isSameDay`/`resumeNewWordThreshold`/`resumeNewWordsPassed`) that let a stale/poisoned `session_state.phase` route a student into review — the mechanism by which an open tab silently reverted admin/server resets within minutes and could carry a non-passer into review. Routing now keys solely on `config.startPhase` (from `determineStartingPhase(attempts)`); if attempts say not-review, going to the new-word phase is self-healing. `session_state` is still read for DISPLAY values (scores, dismissed words) and the COMPLETE re-entry modal. Annotated `persistSessionState` so `phase` is documented as a non-authoritative cache (write side unchanged; load no longer trusts it). No backend change. `npm run build` before push. |
| 2026-06-17 | `src/pages/TypedTest.jsx`, `src/pages/MCQTest.jsx` | Codex Fix H (wrong-day attempt stamping): when a test launches WITHOUT `sessionContext.dayNumber` (lost/stale nav state), both pages previously stamped the attempt with `progress.currentStudyDay` (last COMPLETED day) — so a current-day review stamped the previous day and reconciliation (needs a review attempt for day N) could never complete the day → it re-prompted forever (confirmed live 2026-06-10, 한승환 Adv-A1). H-1/H-2: new test → CSD+1; review → CSD+1 if day CSD+1's new test is passed (`getNewWordAttemptForDay`), else CSD. H-3: stale-context guard — even when a dayNumber IS provided, re-derive if it's outside the only legitimate window [CSD, CSD+1] (old tab / restored sessionStorage). Both log `attempt_day_fallback` / `attempt_day_context_invalid` to system_logs to measure how often context is lost. Added `getNewWordAttemptForDay` (both) + `logSystemEvent` (MCQTest) imports. Safe w/ gradebook review retake (snapshot rolls CSD back so the provided day stays in-window). `npm run build` before push. |
| 2026-06-17 | `src/pages/TypedTest.jsx`, `src/pages/MCQTest.jsx`, `public/help-student-ko.html`, `public/help-student-en.html` | Codex Fix F (threshold mislabel): PATH-B (legacy `wordPool` launch) no longer hard-defaults the display/UI pass threshold to 95. Now: if `assignmentSettings.passThreshold` is present use it; else fetch the class doc and read `assignments[listId].passThreshold` (fallback 95 only if truly unresolved). Fixes 92–94% scorers in 92%-threshold 26SM classes seeing a false "below 95%" label and a UI "Did not pass / Try Again" that contradicted the server's correct pass verdict. Student guides (ko/en) dethreaded the "default 95%" anchor → class-specific phrasing (예: 92%). G (error-code classification) intentionally SKIPPED — it rewrites the same `gradeWithRetry` already carrying the new connection-logging; fold in later. (H was applied immediately after — see the newer row above; this row's "H not yet applied" note is superseded.) `npm run build` before push. |
| 2026-06-17 | `src/pages/TypedTest.jsx` | Connection-error diagnostics: `gradeWithRetry` now writes per-attempt failure telemetry to `system_logs` via `logSystemEvent` (added to import). New events: `grading_attempt_failed` (severity error — classId/listId/testId/studyDay/testType/wordCount/payloadChars, network effectiveType/downlink/rtt, attempt/isFinal, elapsedMs, `timedOut` = ran full 90s window, `failedFast` = died <2s, navigator.onLine, errCode/errName/errMessage) and `grading_recovered` (severity warning — logged when a retry succeeds). Fire-and-forget (logSystemEvent self-catches); all fields null-guarded (no undefined → Firestore-safe). No behavior change to grading itself. Purpose: classify the connection-error epidemic (timeout vs unreachable vs offline vs server) by class + network. `npm run build` before push. |
| 2026-06-17 | `src/pages/DailySessionFlow.jsx`, `src/pages/TypedTest.jsx`, `src/pages/MCQTest.jsx` | Codex Blockers #1+#2: (#1) handleReturnFromTest now gates Day-2+ moveToReviewPhase on `results.score >= retakeThreshold` — a failed new-word test (incl. via the results back-button) routes to NEW_WORDS retake instead of review. (#2) TypedTest/MCQTest now capture `completeSessionFromTest()` return; on `requiresNewWordRetake` they block (setGradingError/setSubmitError + early return) instead of falsely presenting the day as complete. Parse-checked (esbuild). NOTE: also need `firebase firestore:indexes` export committed (Codex #3) + `npm run build` before push. |
| 2026-05-31 | `public/help-teacher-ko.html`, `public/help-teacher-en.html` | Resectioned both teacher guides to front-load top TA tasks: moved Gradebook from 6th to 3rd (now: 1 Getting Started, 2 TA/Admin Access, 3 Gradebook, 4 Class Management, 5 Word List Management, 6 Assigning Word Lists, 7 FAQ). Renumbered TOC, section-number spans, and section comments. EN file was missing the TA/Admin Access section so added it as section 2 (translated from KO) to keep the two files parallel. No prose rewrites. |
| 2026-06-01 | `CODE_REVIEW_2026-06-01.md` | Added full multi-agent code audit report (vocaBoost excl. apBoost): 72 verified findings (0 blocker / 7 high), severity-ranked + systemic patterns |
| 2026-05-31 | `scripts/seed-26sm-classes.js` | Created seeder that creates 28 26SM SAT classes in production with appropriate Base Camp (pace=60, size=25, threshold=90) or Ascent (pace=80, size=30, threshold=92) tier defaults; auto-assigns the matching VZIP 3K list; auto-detects weekend ([주말]) classes for studyDaysPerWeek=2 |
| 2026-05-31 | Firebase production | Created 28 26SM SAT classes via seed-26sm-classes.js (16 Base Camp / 12 Ascent); each with auto-generated unique 6-char joinCode, veterans@vocaboost.com as ownerTeacherId, list assignment baked in. Join codes recorded in audit/playwright/seeded_26sm_classes.json (gitignored) |
| 2026-05-31 | `.gitignore` | Added pattern `audit/playwright/seeded_*.json` so the new 26SM class output file (with join codes) doesn't get committed |
| 2026-05-31 | `scripts/update-26sm-classes.js` | Created script + updated all 28 26SM SAT class assignments to corrected tier definitions: BRIDGE pace 60 (3 classes), INT/CORE pace 80 (12), ADV/Top pace 80 (10), FINAL pace 100 (3). Set testOptionsCount=6 for all (6-choice MCQ review). testMode=typed and reviewTestType=mcq preserved across all. studyDaysPerWeek preserved (2 for [주말], 5 otherwise) |
| 2026-05-31 | `scripts/update-26sm-classes.js` + Firebase | Bumped FINAL tier testSizeNew from 30 → 35; reran update across all 28 (effective change on 3 FINAL classes only) |
| 2026-01-02 | `src/pages/Dashboard.jsx` | Removed Panic Mode warning banner (lines 1300-1312) |
| 2026-01-02 | `src/pages/DailySessionFlow.jsx` | Fixed daily pace calculation - changed `pace * 7` to `pace * studyDaysPerWeek` (line 467) |
| 2026-01-02 | `src/pages/ClassDetail.jsx` | Added `studyDaysPerWeek` setting to Edit List Settings modal |
| 2026-01-03 | `src/services/db.js` | Fixed duplicate class fetches in `fetchUserAttempts` - now caches `assignedLists` (lines 2610-2667) |
| 2026-01-03 | `src/services/db.js` | Fixed `removeStudentFromClass` to also remove from user's `enrolledClasses` (lines 259-268) |
| 2026-01-03 | `src/services/db.js` | Added `testType` parameter to `submitTestAttempt` function (line 1313) |
| 2026-01-03 | `src/services/db.js` | Fixed `joinClass` to verify user document exists before proceeding (lines 795-797) |
| 2026-01-03 | `src/services/studyService.js` | Fixed `buildReviewQueue` to fetch today's failed words directly by ID instead of filtering from segment (lines 481-500) |
| 2026-01-03 | `src/services/studyService.js` | Added blind spot count caching to `getBlindSpotPool` and `getBlindSpotCount` for efficiency |
| 2026-01-03 | `src/pages/Dashboard.jsx` | Updated `getBlindSpotCount` call to pass `classId` for caching (line 529) |
| 2026-01-03 | `src/services/db.js` | Optimized `fetchDashboardStats` to use Firestore orderBy + limit(1) for latest attempt instead of client-side sort (lines 396-402) |
| 2026-01-03 | `src/services/db.js` | Optimized `fetchUserAttempts` to use Firestore orderBy for sorted results, removed client-side sort (lines 2592-2686) |
| 2026-01-03 | `src/pages/ClassDetail.jsx` | Added `passThreshold` and `testSizeNew` to Edit List Settings modal |
| 2026-01-03 | `src/components/AssignListModal.jsx` | Added `passThreshold` and `testSizeNew` to initial assignment settings |
| 2026-01-03 | `src/services/db.js` | Updated `assignListToClass` to accept and save `passThreshold` and `testSizeNew` |
| 2026-01-03 | `src/pages/MCQTest.jsx` | Read `passThreshold` and `testOptionsCount` from assignment instead of hardcoded values |
| 2026-01-03 | `src/pages/TypedTest.jsx` | Read `passThreshold` from assignment instead of hardcoded value |
| 2026-01-03 | `src/pages/DailySessionFlow.jsx` | Fixed `interventionLevel: undefined` error by adding default value of 0 (line 976) |
| 2026-01-03 | `src/services/progressService.js` | Added duplicate day completion guard - blocks re-submission if dayNumber doesn't match expected next day (lines 98-103) |
| 2026-01-03 | `src/services/db.js` | **Box Removal Migration** - Fixed retention calculation to use test score directly instead of filtering by box >= 4 |
| 2026-01-03 | `src/services/db.js` | Fixed mastery count in `fetchDashboardStats` to use `status === 'PASSED'` instead of `box >= 4` |
| 2026-01-03 | `src/services/db.js` | Fixed words learned count in `fetchStudentAggregateStats` to use status instead of box |
| 2026-01-03 | `src/services/db.js` | Removed box updates from `submitTestAttempt` and `submitTypedTestAttempt` - status updates handled by processTestResults |
| 2026-01-03 | `src/services/db.js` | Fixed challenge bug in `reviewChallenge` - now updates status to PASSED instead of box |
| 2026-01-14 | `src/pages/DailySessionFlow.jsx` | Fixed test recovery sessionContext - added missing fields (segment, interventionLevel, wordsIntroduced, wordsReviewed, newWordStartIndex, newWordEndIndex) to prevent TWI reconciliation failures |
| 2026-01-14 | `scripts/export-attempts.js` | Created script to export all Firestore attempts to flattened JSON |
| 2026-01-14 | `scripts/export-users.js` | Created script to export all Firestore users to flattened JSON |
| 2026-01-03 | `src/services/db.js` | Deleted unused box functions: `computeNextReview`, `nextBoxValue`, `saveStudyResult` |
| 2026-01-03 | `src/services/db.js` | Deleted unused legacy test generators: `generateTest`, `generateTypedTest` |
| 2026-01-03 | `src/services/db.js` | Simplified `normalizeStudyState` to just merge defaults with document |
| 2026-01-03 | `src/types/studyTypes.js` | Removed legacy box-related JSDoc comments |
| 2026-01-03 | `src/services/db.js` | Added day progression trigger in `reviewChallenge` when challenge acceptance pushes score above threshold (lines 2544-2589) |
| 2026-01-03 | `src/services/studyService.js` | Fixed PDF pace calculation - changed `pace * 7` to `pace * studyDaysPerWeek` in `getTodaysBatchForPDF` and `getCompleteBatchForPDF` |
| 2026-01-03 | `src/services/studyService.js` | Added failed carryover words to `getTodaysBatchForPDF` - returns structured `{ newWords, failedCarryover, reviewWords }` |
| 2026-01-03 | `src/utils/pdfGenerator.js` | Updated `downloadListAsPDF` to handle structured format with demarcated sections for new words vs failed carryover |
| 2026-01-03 | `src/pages/DailySessionFlow.jsx` | Updated PDF handler to preserve structured format when calling `downloadListAsPDF` |
| 2026-01-03 | `src/pages/Dashboard.jsx` | Updated PDF handler to preserve structured format when calling `downloadListAsPDF` |
| 2026-01-03 | `src/utils/pdfGenerator.js` | Added logo image to PDF header with aspect-ratio-preserving sizing (fixed 10mm height, auto width) |
| 2026-01-03 | `src/utils/pdfGenerator.js` | Changed column widths to responsive: `wrap` for #/Word/POS, `auto` for Definition/Sample |
| 2026-01-03 | `src/utils/pdfGenerator.js` | Added `rowPageBreak: 'avoid'` to prevent table rows from splitting across pages |
| 2026-01-03 | `src/utils/studyAlgorithm.js` | Rewrote `calculateSegment` function - now uses intervention-adjusted projection with week-based segment rotation instead of cumulative days 2-4 logic |
| 2026-01-03 | `src/utils/studyAlgorithm.js` | Commented out `EARLY_DAYS_THRESHOLD` constant (no longer needed) |
| 2026-01-03 | `src/utils/studyAlgorithm.js` | Added legacy `calculateSegment` as comment block for reference |
| 2026-01-03 | `src/services/studyService.js` | Updated `calculateSegment` call to pass `dailyPace` and `interventionLevel` parameters (lines 81-87) |
| 2026-01-03 | `src/utils/studyAlgorithm.js` | Updated review test size constants from 20-50 to 30-60 |
| 2026-01-03 | `src/utils/studyAlgorithm.js` | Updated `calculateReviewTestSize` to accept optional `minSize` and `maxSize` parameters for teacher-configurable ranges |
| 2026-01-03 | `src/pages/DailySessionFlow.jsx` | Removed failed carryover mixing - NEW_WORDS phase now shows only new words (failed words handled via segment review priority) |
| 2026-01-03 | `src/components/AssignListModal.jsx` | Added review test settings: `reviewTestType`, `reviewTestSizeMin`, `reviewTestSizeMax` |
| 2026-01-03 | `src/pages/ClassDetail.jsx` | Added review test settings section to Edit List Settings modal |
| 2026-01-03 | `src/services/db.js` | Updated `assignListToClass` to accept and save review test settings |
| 2026-01-03 | `src/services/db.js` | Updated `updateAssignmentSettings` to handle review test settings validation |
| 2026-01-03 | `src/pages/DailySessionFlow.jsx` | Updated `goToReviewTest` to use `reviewTestType` from assignment settings |
| 2026-01-03 | `src/utils/pdfGenerator.js` | Added reviewWords section with green header (#22C55E) and light green table background (#DCFCE7) |
| 2026-01-03 | `session-time-calculator.html` | Added synchronized slider + textbox inputs for all parameters (pace, test sizes, intervention, time constants, pool settings, algorithm constants) |
| 2026-01-03 | `session-time-calculator.html` | Added Graduation Model section with dynamic calculation of avg tests to graduate based on student accuracy (%), consecutive correct needed, and new test bonus checkbox |
| 2026-01-03 | `src/types/studyTypes.js` | Added `MASTERED` and `NEEDS_CHECK` to `WORD_STATUS` enum for graduation system |
| 2026-01-03 | `src/types/studyTypes.js` | Added `masteredAt` and `returnAt` fields to `DEFAULT_STUDY_STATE` |
| 2026-01-03 | `src/services/studyService.js` | Added `graduateSegmentWords()` - graduates X% of PASSED words where X = review test score |
| 2026-01-03 | `src/services/studyService.js` | Added `returnMasteredWords()` - returns MASTERED words to NEEDS_CHECK after 21 days |
| 2026-01-03 | `src/pages/DailySessionFlow.jsx` | Added `returnMasteredWords` call before session initialization |
| 2026-01-03 | `src/pages/DailySessionFlow.jsx` | Added `graduateSegmentWords` call after review test in `completeSession` |
| 2026-01-03 | `src/pages/MCQTest.jsx` | Fixed retake shuffling - now re-shuffles words using `selectTestWords()` to avoid identical test order |
| 2026-01-03 | `src/components/DismissedWordsDrawer.jsx` | NEW: Right-side drawer component for viewing/restoring dismissed words |
| 2026-01-03 | `src/pages/DailySessionFlow.jsx` | Added dismissed words drawer with undo functionality - stores full word data on dismiss, toggle button in header, restore individual or all |
| 2026-01-04 | `src/pages/MCQTest.jsx` | Fixed challenge bug - capture `attemptId` from `submitTestAttempt` return value and call `setAttemptId(result.id)` |
| 2026-01-04 | `src/pages/TypedTest.jsx` | Fixed challenge bug - capture `attemptId` from `submitTypedTestAttempt` return value and call `setAttemptId(result.id)` |
| 2026-01-04 | `src/utils/testConfig.js` | **NEW FILE** - Centralized test configuration builder with `buildTestConfig()` function. Single source of truth for test parameters, applies testSizeNew limiting to word pools. |
| 2026-01-04 | `src/pages/DailySessionFlow.jsx` | Added import for `buildTestConfig`; Updated `navigateToTest()` to build testConfig and pass as single object (words now limited by testSizeNew before navigation) |
| 2026-01-04 | `src/pages/MCQTest.jsx` | Refactored to consume `testConfig` from navigation state with backwards compatibility for legacy props; Added testConfig path in `loadTestWords()` |
| 2026-01-04 | `src/pages/TypedTest.jsx` | Refactored to consume `testConfig` from navigation state with backwards compatibility for legacy props; Added testConfig path in `loadTestWords()` |
| 2026-01-04 | `src/pages/DailySessionFlow.jsx` | **Navigation Simplification** - Simplified `CompletePhase` to single "Back to Dashboard" button; Removed unused props (`onMoveOn`, `onNext`, `onRetakeReview`); Removed retake warning box |
| 2026-01-04 | `src/pages/DailySessionFlow.jsx` | Removed `showMoveOnConfirm` and `showNextSessionModal` state variables; Deleted Move On Confirmation and Next Session modals |
| 2026-01-04 | `src/pages/DailySessionFlow.jsx` | Removed unused `handleRetakeReviewTest` function |
| 2026-01-04 | `src/pages/MCQTest.jsx` | Changed quit handler to always navigate to `/` (Dashboard) instead of `returnPath` |
| 2026-01-04 | `src/pages/MCQTest.jsx` | Replaced "Study" button with "Dashboard" on failed new word tests |
| 2026-01-04 | `src/pages/MCQTest.jsx` | Added "Dashboard" button to needs-work and critical review test tiers (alongside Retake) |
| 2026-01-04 | `src/pages/MCQTest.jsx` | Removed unused `handleGoToStudy` function |
| 2026-01-04 | `src/pages/TypedTest.jsx` | Changed quit handler to always navigate to `/` (Dashboard) instead of `returnPath` |
| 2026-01-04 | `src/pages/TypedTest.jsx` | Replaced "Study" button with "Dashboard" on failed new word tests |
| 2026-01-04 | `src/pages/TypedTest.jsx` | Added "Dashboard" button to needs-work and critical review test tiers (alongside Retake) |
| 2026-01-04 | `src/pages/TypedTest.jsx` | Removed unused `handleGoToStudy` function |
| 2026-01-04 | `NAVIGATION_AUDIT.md` | **NEW FILE** - Comprehensive navigation audit documenting all 68 navigation elements across 21 files, including button destinations, redirects, and 3 dead route issues |
| 2026-01-04 | `scripts/migrateWordPositions.js` | **NEW FILE** - One-time migration script to add `position` field to existing words based on `createdAt` order |
| 2026-01-04 | `src/services/db.js` | **Word Position Refactor** - `addWordToList()` now assigns `position: currentCount` (0-indexed) to new words |
| 2026-01-04 | `src/services/db.js` | **Word Position Refactor** - `batchAddWords()` now assigns sequential positions starting from current wordCount |
| 2026-01-04 | `src/services/db.js` | **Word Position Refactor** - `fetchAllWords()` changed `orderBy('createdAt', 'asc')` → `orderBy('position', 'asc')` |
| 2026-01-04 | `src/services/studyService.js` | **Word Position Refactor** - `getSegmentWords()` uses `orderBy('position')` and filters by `w.position` instead of computed `wordIndex` |
| 2026-01-04 | `src/services/studyService.js` | **Word Position Refactor** - `initializeNewWordStates()` uses `word.position` instead of `word.wordIndex` |
| 2026-01-04 | `src/services/studyService.js` | **Word Position Refactor** - `getFailedFromPreviousNewWords()` uses `orderBy('position')` and filters by `w.position` |
| 2026-01-04 | `src/services/studyService.js` | **Word Position Refactor** - `getNewWords()` uses `orderBy('position')` and filters by position range |
| 2026-01-04 | `src/services/studyService.js` | **Word Position Refactor** - `getBlindSpotPool()` uses `orderBy('position')` instead of `createdAt` |
| 2026-01-04 | `src/pages/ListEditor.jsx` | **Word Position Refactor** - All 3 word queries changed from `orderBy('createdAt')` to `orderBy('position')` |
| 2026-01-04 | `src/pages/MCQTest.jsx` | **Word Position Refactor** - Fallback query uses `orderBy('position')`, removed dynamic `wordIndex` assignment |
| 2026-01-04 | `src/pages/TypedTest.jsx` | **Word Position Refactor** - Fallback query uses `orderBy('position')`, removed dynamic `wordIndex` assignment |
| 2026-01-04 | `src/pages/DailySessionFlow.jsx` | **Word Position Refactor** - Removed `wordIndex` mapping; words already have `position` field |
| 2026-01-04 | `src/utils/pdfGenerator.js` | **Word Position Refactor** - Changed `word.wordIndex ?? word.index` to `word.position` for word numbering |
| 2026-01-04 | `src/pages/Dashboard.jsx` | **Performance** - Parallelized progress data loading using `Promise.all` instead of sequential `for...await` loops (lines 543-586) |
| 2026-01-04 | `src/components/dev/SegmentDebugPanel.jsx` | **NEW FILE** - Collapsible debug panel showing segment boundaries, session config, and word-level queue details |
| 2026-01-04 | `src/services/studyService.js` | Added `getDebugSessionData()` export for debug panel - returns sessionConfig, reviewQueue, and segmentWords |
| 2026-01-04 | `src/pages/Dashboard.jsx` | Added SegmentDebugPanel component to list cards (dev-only via `import.meta.env.DEV`) |
| 2026-01-04 | `src/services/studyService.js` | **Full Segment PDF** - `getTodaysBatchForPDF()` now uses `getSegmentWords()` instead of `buildReviewQueue()` to show ALL segment words |
| 2026-01-04 | `src/pages/DailySessionFlow.jsx` | **Full Segment Study** - Session initialization and `moveToReviewPhase()` now use `getSegmentWords()` for REVIEW_STUDY phase |
| 2026-01-04 | `src/services/studyService.js` | **Bug Fix** - `getDebugSessionData()` now transforms assignment with `weeklyPace = pace * studyDaysPerWeek` (matching `getTodaysBatchForPDF` pattern) |
| 2026-01-04 | `src/pages/DailySessionFlow.jsx` | **Bug Fix** - Fixed "No Test Content" error on review tests: `navigateToTest()` now passes `reviewQueue` instead of `null` for review test wordPool (line 1014) |
| 2026-01-04 | `src/pages/MCQTest.jsx` | **Shuffle Fix** - Added `shuffleArray` import; Fixed biased distractor selection and option ordering to use Fisher-Yates instead of `sort(() => Math.random() - 0.5)` (lines 195, 206) |
| 2026-01-04 | `src/pages/TypedTest.jsx` | **Shuffle Fix** - Fixed biased retake shuffle to use `shuffleArray()` instead of `sort(() => Math.random() - 0.5)` (line 650) |
| 2026-01-04 | `src/services/studyService.js` | **Shuffle Fix** - Added `shuffleArray` import; Fixed biased graduation selection to use Fisher-Yates instead of `sort(() => Math.random() - 0.5)` (line 781) |
| 2026-01-05 | `src/services/studyService.js` | **Bug Fix** - `graduateSegmentWords()` now writes `wordIndex` and `listId` to Firebase batch (lines 802-803). Debug panel's `getMasteredWordsInRange()` filters by `wordIndex`, so graduated words were invisible without this field. |
| 2026-01-09 | `src/pages/DailySessionFlow.jsx` | Added `newWordStartIndex` and `newWordEndIndex` to sessionContext (lines 1062-1063) |
| 2026-01-09 | `src/services/db.js` | Added `sessionContext` parameter to `submitTestAttempt` function (line 1003) |
| 2026-01-09 | `src/services/db.js` | Added 9 flattened session context fields to `submitTestAttempt` attemptData: `isFirstDay`, `listTitle`, `segmentStartIndex`, `segmentEndIndex`, `interventionLevel`, `wordsIntroduced`, `wordsReviewed`, `newWordStartIndex`, `newWordEndIndex` (lines 1064-1073) |
| 2026-01-12 | `src/utils/sessionStepTracker.js` | **NEW FILE** - Centralized step calculation utility with `getSessionStep()` function. Returns `{ stepNumber, totalSteps, stepText }` based on phase or testType. |
| 2026-01-12 | `src/utils/sessionStepTracker.js` | Fixed switch cases to use lowercase phase constants (`'new_words'`, `'review_study'`, `'complete'`) instead of uppercase |
| 2026-01-12 | `src/pages/MCQTest.jsx` | Replaced custom header (lines 1037-1072) with SessionHeader component on active test screen; Added step tracker import and usage |
| 2026-01-12 | `src/pages/MCQTest.jsx` | Updated results screen to use `getSessionStep()` utility instead of inline calculation (lines 730-731) |
| 2026-01-12 | `src/pages/TypedTest.jsx` | Updated active test screen and results screen to use `getSessionStep()` utility instead of inline calculation (lines 780-781, 1079-1080, 1122-1124) |
| 2026-01-12 | `src/pages/DailySessionFlow.jsx` | Updated to use `getSessionStep()` utility instead of inline calculation (lines 1558-1567) |
| 2026-01-12 | `src/pages/TypedTest.jsx` | **Review Test UX** - Updated button labels for all 4 tiers: Excellent → "Continue" (was "Return to Dashboard"); Good → swapped button order; Needs Work & Critical → "Review Again" + "Continue" (was "Retake Test" + "Dashboard") |
| 2026-01-12 | `src/pages/TypedTest.jsx` | **Review Test UX** - "Review Again" button now navigates with `goToStudy: true` state to return user to review study flashcards instead of retaking test |
| 2026-01-12 | `src/pages/MCQTest.jsx` | **Review Test UX** - Updated button labels for all 4 tiers: Excellent → "Continue"; Good → swapped button order; Needs Work & Critical → "Review Again" + "Continue" |
| 2026-01-12 | `src/pages/MCQTest.jsx` | **Review Test UX** - "Review Again" button now navigates with `goToStudy: true` state to return user to review study flashcards instead of retaking test |
| 2026-01-09 | `src/services/db.js` | Added `sessionContext` parameter to `submitTypedTestAttempt` function (line 1121) |
| 2026-01-09 | `src/services/db.js` | Added 9 flattened session context fields to `submitTypedTestAttempt` attemptData (lines 1215-1224) |
| 2026-01-09 | `src/pages/MCQTest.jsx` | Pass `sessionContext` to `submitTestAttempt` call (line 539) |
| 2026-01-09 | `src/pages/TypedTest.jsx` | Pass `sessionContext` to `submitTypedTestAttempt` call (line 636) |
| 2026-01-09 | `src/services/db.js` | **Solution #3** - Added `isTransientError()` helper to identify retryable Firebase errors (lines 44-55) |
| 2026-01-09 | `src/services/db.js` | **Solution #3** - Added `addJitter()` helper for exponential backoff randomization (lines 57-61) |
| 2026-01-09 | `src/services/db.js` | **Solution #3** - Added `withRetry()` generic retry wrapper with exponential backoff and logging (lines 63-109) |
| 2026-01-09 | `src/services/db.js` | **Solution #3** - Added `logSystemEvent()` for anomaly logging to `system_logs` collection (lines 111-125) |
| 2026-01-09 | `src/services/db.js` | **Solution #3** - Added `listId` parameter to `submitTestAttempt()` and `submitTypedTestAttempt()` |
| 2026-01-09 | `src/pages/MCQTest.jsx` | **Solution #3** - Wrapped `submitTestAttempt()` with `withRetry()` for transient failure recovery |
| 2026-01-09 | `src/pages/MCQTest.jsx` | **Solution #3** - Added `submitError` state and error UI with "Try Again" button |
| 2026-01-09 | `src/pages/MCQTest.jsx` | **Solution #3** - Added `beforeunload` handler to warn before leaving with unsaved answers |
| 2026-01-09 | `src/pages/TypedTest.jsx` | **Solution #3** - Wrapped `submitTypedTestAttempt()` with `withRetry()` for transient failure recovery |
| 2026-01-09 | `src/pages/TypedTest.jsx` | **Solution #3** - Added `submitError` state and error UI with "Try Again" button |
| 2026-01-09 | `src/pages/TypedTest.jsx` | **Solution #3** - Added `beforeunload` handler to warn before leaving with unsaved answers |
| 2026-01-09 | `src/services/db.js` | **Solution #1** - Added `getRecentAttemptsForClassList()` to query recent attempts by studentId/classId/listId |
| 2026-01-09 | `src/services/progressService.js` | **Solution #1** - Added `calculateCSDAndTWIFromAttempts()` to derive CSD/TWI from attempt history |
| 2026-01-09 | `src/services/progressService.js` | **Solution #1** - Modified `getOrCreateClassProgress()` to reconcile CSD/TWI and return `{ progress, attempts }` |
| 2026-01-09 | `src/services/progressService.js` | **Solution #1** - Added `logSystemEvent('csd_twi_reconciled', ...)` when mismatch detected |
| 2026-01-09 | `src/services/studyService.js` | **Solution #1** - Updated `initializeDailySession()` to destructure `{ progress, attempts }` |
| 2026-01-09 | `src/pages/MCQTest.jsx` | **Solution #1** - Updated `getOrCreateClassProgress()` call to destructure `{ progress }` (line 525) |
| 2026-01-09 | `src/pages/TypedTest.jsx` | **Solution #1** - Updated `getOrCreateClassProgress()` call to destructure `{ progress }` (line 621) |
| 2026-01-09 | `src/services/studyService.js` | **Solution #2** - Added `determineStartingPhase()` to detect mid-session or complete states from attempts (lines 57-95) |
| 2026-01-09 | `src/services/studyService.js` | **Solution #2** - Added `logSystemEvent('impossible_phase_detected', ...)` for Day 1 anomaly |
| 2026-01-09 | `src/services/studyService.js` | **Solution #2** - Updated `initializeDailySession()` to return `startPhase`, `recoveredNewWordScore`, `recoveredReviewScore` |
| 2026-01-09 | `src/pages/DailySessionFlow.jsx` | **Solution #2** - Added startPhase handling in init: COMPLETE skips to completion, REVIEW_STUDY loads segment words and skips new word phase (lines 604-639) |
| 2026-01-09 | `src/services/progressService.js` | **Bug Fix #4** - Added Math.max safeguard to prevent CSD/TWI regression if query returns empty/incomplete data (lines 117-119) |
| 2026-01-09 | `src/pages/DailySessionFlow.jsx` | **Bug Fix #6** - Fixed undefined `combinedWords` variable in test recovery - now uses `testRecovery.localState?.wordPool` (lines 696, 713) |
| 2026-01-09 | `src/pages/DailySessionFlow.jsx` | **Bug Fix #7** - Added `setCardsReviewed(0)` to REVIEW_STUDY recovery to reset card count (line 633) |
| 2026-01-09 | `src/pages/DailySessionFlow.jsx` | **Bug Fix #8** - Wrapped `getSegmentWords` in try-catch for REVIEW_STUDY recovery with error UI fallback (lines 620-645) |
| 2026-01-11 | `src/pages/MCQTest.jsx` | **Rollback** - Removed all pending submission localStorage recovery work (Steps R1-R8): removed `isResumingSubmission` and `showConnectionWarning` states, removed `resumePendingSubmission` function, restored simple testRecovery useEffect, removed all pending submission save/clear logic in handleSubmit, removed resuming submission UI, simplified submission overlay to show only "Submitting Your Test..." with retry button on error |
| 2026-01-11 | `src/pages/TypedTest.jsx` | Added simple submission overlay modal (lines 1259-1288) matching MCQTest's simplified approach - full-screen modal with "Submitting Your Test..." message, spinner, and retry button on error |
| 2026-01-11 | `src/pages/TypedTest.jsx` | Removed inline `submitError` display (previously lines 1182-1200) - error handling now centralized in submission overlay modal |
| 2026-01-12 | `src/pages/TypedTest.jsx` | **AI Grading Retry Logic** - Added retry state variables `retryAttempt` and `gradingError` (lines 97-99) |
| 2026-01-12 | `src/pages/TypedTest.jsx` | **AI Grading Retry Logic** - Added `gradeWithRetry()` function with 3 max retries, 10s delay between retries, 90s timeout per attempt (lines 572-603) |
| 2026-01-12 | `src/pages/TypedTest.jsx` | **AI Grading Retry Logic** - Updated `handleSubmit()` to use `gradeWithRetry()` instead of direct API call, added retry state initialization (lines 605-628) |
| 2026-01-12 | `src/pages/TypedTest.jsx` | **AI Grading Retry Logic** - Added `handleRetryGrading()` for manual retry after all attempts fail (lines 753-757) |
| 2026-01-12 | `src/pages/TypedTest.jsx` | **AI Grading Retry Logic** - Updated submission overlay to show retry status on attempts 2-3 with yellow warning and attempt counter (lines 1287-1314) |
| 2026-01-12 | `src/pages/TypedTest.jsx` | **AI Grading Retry Logic** - Added separate grading error modal with manual "Try Again" button after 3 failed attempts, preserves student answers (lines 1332-1355) |
| 2026-01-12 | `src/services/db.js` | **Challenge Bug Fix** - `reviewChallenge()` now updates `passed` field when challenge is accepted. Previously only `score` was updated, leaving `passed: false` even when score crossed threshold. This caused reconciliation to not advance days. (lines 2606-2628) |
| 2026-01-12 | `CLAUDE.md` | Fixed filename reference from `changes_action_log.md` to `change_action_log.md` and added table format hint |
| 2026-01-12 | `CLAUDE.md` | Added apBoost rule to log changes to `change_action_log_ap.md` instead of main log |
| 2026-01-12 | `change_action_log_ap.md` | **NEW FILE** - Separate change log for apBoost development |
| 2026-01-13 | `updated_tech_spec_vocaboost.md` | **NEW FILE** - Complete technical specification document with 15 sections covering architecture, routing, pages, components, design system, state management, services, data models, algorithms, study flow, utilities, dev tools, and security |
| 2026-01-19 | `src/services/progressService.js` | **TWI Bug Fix** - Changed reconciliation TWI calculation to use most recent new test instead of exact CSD-level match (lines 86-97). Previous logic failed when no new test existed at exact CSD level. |
| 2026-01-19 | `src/services/progressService.js` | **TWI Bug Fix** - Added fallback TWI fetch: if TWI=0 with CSD>0, calls `getMostRecentNewTest()` to recover TWI from any new test (lines 167-176) |
| 2026-01-19 | `src/services/db.js` | **NEW FUNCTION** - Added `getMostRecentNewTest()` helper for fallback TWI reconciliation - queries only new tests with sessionType filter (lines 3016-3060) |
| 2026-01-19 | `src/services/studyService.js` | **TWI Bug Fix** - Changed `wordsIntroduced` to use `sessionConfig.newWordCount` as primary source instead of `newWords.length` which was often 0 (line 1087) |
| 2026-01-19 | `src/services/progressService.js` | **Anchor-Based Reconciliation** - Rewrote `calculateCSDAndTWIFromAttempts()` to use NEW TEST as anchor for both CSD and TWI (lines 40-102). CSD and TWI now derived from same source to prevent mismatch. |
| 2026-01-19 | `src/services/progressService.js` | **Orphan Cleanup** - Added `cleanupOrphanedReviews()` function to delete review tests where `studyDay > anchorDay`. Logs full attempt data to `system_logs` before deletion (lines 115-155). |
| 2026-01-19 | `src/services/progressService.js` | **Orphan Cleanup** - Updated `getOrCreateClassProgress()` to call `cleanupOrphanedReviews()` after calculating anchor day (lines 212-215) |
| 2026-01-26 | `scripts/advance-student-to-day.js` | **NEW SCRIPT** - Admin script to advance a student's progress when transferring between classes (CORE→TOP). Inserts synthetic NEW + REVIEW attempts and resets class_progress/session_states so reconciliation sets correct CSD/TWI. Used to advance Sarang Min (love0609m@gmail.com) to Day 11 in TOP OFFLINE class. |
| 2026-02-03 | `src/services/db.js` | **NEW FUNCTION** - Added `getMostRecentPassedNewTest()` to query only PASSED new tests for reconciliation anchor (lines 3066-3111). Fixes bug where failed new tests incorrectly advanced TWI. |
| 2026-02-03 | `src/services/db.js` | **NEW FUNCTION** - Added `getReviewForDay()` to check if review exists for specific study day (lines 3113-3153). Used by reconciliation to determine CSD. |
| 2026-02-03 | `src/services/progressService.js` | **CRITICAL BUG FIX** - Rewrote reconciliation to use two-query approach: (1) find anchor from PASSED new tests only, (2) check if review exists for anchor day. Previously, failed new tests were used as anchors, causing TWI to advance and students to skip word ranges on retry. |
| 2026-02-03 | `src/services/progressService.js` | Removed old `calculateCSDAndTWIFromAttempts()` function, replaced with direct calls to new db.js query helpers. |
| 2026-02-03 | `scripts/check-single-student.js` | Updated reconciliation analysis to only consider PASSED new tests as anchors, matching new app logic. |

---

## Phase 4: Word Position Field Refactor

**Date:** 2026-01-04

### Problem Statement

Word indices were computed at runtime from array position after `orderBy('createdAt', 'asc')`. This design was fragile because:
- Timestamps can be inconsistent (imports, clock skew, simultaneous adds)
- "#435 of List X" didn't point to anything specific - it was just the 435th item after sorting
- Any timestamp issues would silently corrupt word order and break segment boundaries

### Solution: Explicit `position` Field

Added permanent `position: number` field to all word documents. Words are now:
- Assigned sequential 0-indexed positions on creation
- Queried by `orderBy('position', 'asc')` instead of `createdAt`
- Filtered directly by `w.position` instead of computed array index

### New Word Document Structure

```javascript
{
  id: "abc123",
  word: "Abate",
  definition: "...",
  position: 0,        // NEW: Permanent, explicit position (0-indexed)
  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

### CRUD Changes

**`addWordToList()` (db.js):**
```javascript
const listDoc = await getDoc(doc(db, 'lists', listId))
const currentCount = listDoc.exists() ? (listDoc.data()?.wordCount ?? 0) : 0
const wordPayload = {
  ...wordData,
  position: currentCount,  // 0-indexed position
  createdAt: serverTimestamp(),
}
```

**`batchAddWords()` (db.js):**
```javascript
let nextPosition = listDoc.data()?.wordCount ?? 0
// Each word in batch gets sequential position
position: nextPosition++
```

### Query Changes

All `orderBy('createdAt', 'asc')` changed to `orderBy('position', 'asc')`:

| File | Function/Location |
|------|-------------------|
| `db.js` | `fetchAllWords()` |
| `studyService.js` | `getSegmentWords()`, `getFailedFromPreviousNewWords()`, `getNewWords()`, `getBlindSpotPool()` |
| `ListEditor.jsx` | `loadList()`, `reloadWords()`, `handleAddWord()` |
| `MCQTest.jsx` | Fallback load |
| `TypedTest.jsx` | Fallback load |

### Index Pattern Removal

All `.map((doc, index) => ({ wordIndex: index, ... }))` patterns changed to read `position` from document:

```javascript
// BEFORE:
const allWords = wordsSnap.docs.map((doc, index) => ({
  id: doc.id,
  wordIndex: index,
  ...doc.data()
}))
const segmentWords = allWords.filter(w => w.wordIndex >= startIndex)

// AFTER:
const allWords = wordsSnap.docs.map((doc) => ({
  id: doc.id,
  ...doc.data()
}))
const segmentWords = allWords.filter(w => w.position >= startIndex)
```

### Migration Script

**File:** `scripts/migrateWordPositions.js`

One-time script to backfill `position` field on existing words:
1. Fetches all lists
2. For each list, gets words ordered by `createdAt`
3. Assigns sequential positions (0, 1, 2, ...)
4. Batch updates all word documents

Must be run once before deploying the code changes.

### Design Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Index base | 0-indexed | Matches JavaScript array semantics, simpler math |
| Gap handling | Allow gaps | Simpler than cascade updates on delete; filter logic handles gaps |
| Position on delete | No change | Gaps are fine; wordCount decrements but positions stay stable |
| Position on update | Preserved | updateDoc merges, doesn't overwrite position |

### Files Modified

| File | Changes |
|------|---------|
| `scripts/migrateWordPositions.js` | NEW - Migration script |
| `src/services/db.js` | `addWordToList`, `batchAddWords`, `fetchAllWords` |
| `src/services/studyService.js` | 5 functions updated (orderBy + position field) |
| `src/pages/ListEditor.jsx` | 3 query locations |
| `src/pages/MCQTest.jsx` | Fallback query |
| `src/pages/TypedTest.jsx` | Fallback query |
| `src/pages/DailySessionFlow.jsx` | Removed wordIndex mapping |
| `src/utils/pdfGenerator.js` | Word number from position |

### Backups

All modified files backed up to `vocaboost/backups/position-refactor/` before changes.

---

## Phase 3: Mastery Graduation System (% Culling Approach)

**Date:** 2026-01-03

### Problem Statement

Without a graduation mechanism, the review pool grows unboundedly as students learn new words each day. At 80 words/day, 5 days/week = 400 words/week with zero graduation means:
- Week 4: Pool = 1,600 words
- Week 8: Pool = 3,200 words
- Review becomes impossible to complete in reasonable time

### Solution: % Culling Graduation

After each review test, graduate **X% of PASSED words** in the segment, where **X = test score**.

```
Review test score = 80%
→ Fetch all PASSED words in today's segment
→ Randomly select 80% of them to graduate
→ Update selected words to MASTERED status
→ FAILED words always stay in pool (safety net)
```

### Why % Culling (vs Individual Streak Tracking)

| Approach | Complexity | Trigger | Per-Word Tracking |
|----------|------------|---------|-------------------|
| Streak (old design) | High | Every test result | `correctStreak` field |
| **% Culling (implemented)** | **Low** | **After review test only** | **None needed** |

**Key insight:** FAILED words provide the safety net. We're graduating from words the student got RIGHT on the test - the test itself is the verification. No need for additional per-word streak tracking.

### Design Decisions

| Parameter | Value | Rationale |
|-----------|-------|-----------|
| Graduation trigger | After review test | Segment-level, not per-word |
| Graduation rate | X% where X = test score | Proportional to demonstrated knowledge |
| Pool for graduation | PASSED words in segment | All segment PASSED words, not just tested |
| FAILED words | Never graduate | Safety net - always stay for more practice |
| Return delay | 21 days | Matches existing blind spot threshold |
| Conservative rounding | `Math.floor` | Avoid over-graduating on small pools |

### New Status Values

```javascript
export const WORD_STATUS = {
  NEW: 'NEW',
  NEVER_TESTED: 'NEVER_TESTED',
  FAILED: 'FAILED',
  PASSED: 'PASSED',
  MASTERED: 'MASTERED',      // NEW: Graduated from review pool
  NEEDS_CHECK: 'NEEDS_CHECK' // NEW: Returned from MASTERED after 21 days
};
```

### Study State Document Changes

```javascript
// Added to DEFAULT_STUDY_STATE:
{
  masteredAt: null,    // Timestamp when word became MASTERED
  returnAt: null       // Timestamp when word should return (masteredAt + 21 days)
}
```

### Core Functions Implemented

#### `graduateSegmentWords(userId, listId, segment, testScore)`
**Location:** `src/services/studyService.js` (lines 745-793)

1. Fetches all segment words with current status
2. Filters to PASSED words only (excludes FAILED, NEVER_TESTED, MASTERED, NEEDS_CHECK)
3. Calculates graduation count: `Math.floor(passedWords.length * testScore)`
4. Randomly selects words to graduate (Fisher-Yates shuffle + slice)
5. Batch updates selected words to MASTERED status with 21-day return timestamp
6. Returns `{ graduated: number, remaining: number }`

#### `returnMasteredWords(userId, listId)`
**Location:** `src/services/studyService.js` (lines 803-829)

1. Queries for MASTERED words where `returnAt <= now`
2. Batch updates them to NEEDS_CHECK status
3. Clears `masteredAt` and `returnAt` fields
4. Returns count of words returned to pool

### Integration Points

**Session Initialization (DailySessionFlow.jsx line 477):**
```javascript
// Return any MASTERED words that have passed their 21-day period
await returnMasteredWords(user.uid, listId)
```

**Session Completion (DailySessionFlow.jsx lines 997-1008):**
```javascript
// Graduate percentage of PASSED words from segment after review test
if (sessionConfig?.segment && reviewTestResults?.score !== undefined) {
  graduationResult = await graduateSegmentWords(
    user.uid,
    listId,
    sessionConfig.segment,
    reviewTestResults.score
  )
}
```

### Pool Dynamics (Projected at 80% Accuracy)

**Scenario: 80 words/day, 5 days/week = 400 words/week**

| Week | Inflow | Segment PASSED | Graduate (80%) | Net Pool Growth |
|------|--------|----------------|----------------|-----------------|
| 1 | 400 | ~256 | ~205 | +195 |
| 2 | 400 | ~320 | ~256 | +144 |
| 3 | 400 | ~350 | ~280 | +120 |
| 4 | 400 | ~360 | ~288 | +112 (+ returns) |
| 8+ | 400 | ~400 | ~320 | Stabilizes ~800-1000 |

**Key insight:** Weekly graduation rate ≈ `score × PASSED% × inflow` = 0.80 × 0.80 × 400 = **256 words/week**

With 400 inflow and ~256 graduation, pool grows slowly then stabilizes when returns balance.

### Edge Cases Handled

| Scenario | Handling |
|----------|----------|
| No PASSED words in segment | Returns `{ graduated: 0, remaining: 0 }` |
| 100% test score | Graduates all PASSED words in segment |
| 0% test score | Graduates 0 words (all stay for practice) |
| All segment words MASTERED | No PASSED words to graduate, early return |
| Day 1 (no segment) | Skip review phases, no graduation call |
| Small pool (e.g., 3 words at 85%) | `Math.floor(3 * 0.85) = 2` (conservative) |
| NEEDS_CHECK word passes test | Status → PASSED; then eligible for graduation |
| NEEDS_CHECK word fails test | Status → FAILED; stays in pool |

### Files Modified

| File | Changes |
|------|---------|
| `src/types/studyTypes.js` | Added `MASTERED`, `NEEDS_CHECK` to `WORD_STATUS`; Added `masteredAt`, `returnAt` to `DEFAULT_STUDY_STATE` |
| `src/services/studyService.js` | Added `graduateSegmentWords()` and `returnMasteredWords()` functions |
| `src/pages/DailySessionFlow.jsx` | Added imports; Call `returnMasteredWords()` on init; Call `graduateSegmentWords()` after review test |

### Files NOT Needing Changes

- `src/utils/studyAlgorithm.js` - Segments still use `totalWordsIntroduced` (stable boundaries)
- `src/services/db.js` - `normalizeStudyState()` already spreads `DEFAULT_STUDY_STATE`
- `processTestResults()` - Kept as-is (just updates PASSED/FAILED)
- Test components - No changes needed

### Future Enhancements (Not Yet Implemented)

1. **Graduation Feedback UI** - Show user how many words graduated after review test
2. **Dashboard Mastered Count** - Add mastered word count alongside "Learned" count
3. **Progress Visualization** - Progress ring showing New → Active → Mastered percentages

---

## Phase 5: Session Fragility Fixes

**Date:** 2026-01-09

### Problem Statement

Sessions were fragile due to three interrelated issues:

1. **Attempt writes could fail silently** - Network failures during test submission could lose student work
2. **CSD/TWI could drift from reality** - `class_progress` fields could become inconsistent with actual attempt history
3. **Mid-session crashes lost progress** - If a student completed the new word test but crashed before review, they'd restart from scratch

### Solution Overview

Implemented three fixes from `session_fragility_fix_proposal.md`:

| Solution | Purpose | Key Mechanism |
|----------|---------|---------------|
| #3: Bulletproof Attempt Writing | Prevent data loss on network failures | Retry with exponential backoff |
| #1: CSD/TWI Reconciliation | Self-healing progress tracking | Derive CSD/TWI from attempts on load |
| #2: Init-Based Phase Detection | Resume mid-session | Check attempts to determine starting phase |

---

### Solution #3: Bulletproof Attempt Writing

#### Problem
Firebase writes can fail due to transient network issues. Without retry logic, a student could complete a test, have the submission fail, and lose all their work.

#### Implementation

**New helpers in `db.js`:**

```javascript
// Identify retryable errors (network, unavailable, etc.)
function isTransientError(error) {
  const transientCodes = [
    'unavailable', 'resource-exhausted', 'deadline-exceeded',
    'cancelled', 'unknown', 'internal', 'aborted'
  ];
  return transientCodes.includes(error?.code);
}

// Add ±25% jitter to prevent thundering herd
function addJitter(baseDelayMs) {
  const jitter = baseDelayMs * 0.25 * (Math.random() * 2 - 1);
  return Math.max(0, baseDelayMs + jitter);
}

// Generic retry wrapper with exponential backoff
async function withRetry(fn, options = {}, loggingContext = {}) {
  const { maxRetries = 3, totalTimeoutMs = 15000 } = options;
  // Retries with 1s, 2s, 4s delays (with jitter)
  // Logs success after retry or final failure to system_logs
}
```

**System logging helper:**

```javascript
export async function logSystemEvent(eventType, data, severity = 'warning') {
  // Writes to system_logs collection for anomaly monitoring
  // Fire-and-forget (doesn't block on errors)
}
```

**Test component changes (MCQTest.jsx, TypedTest.jsx):**

1. Wrap `submitTestAttempt()` / `submitTypedTestAttempt()` with `withRetry()`
2. Add `submitError` state for error UI display
3. Show "Try Again" button when submission fails after all retries
4. Add `beforeunload` handler to warn before leaving with unsaved answers

#### Files Modified

| File | Changes |
|------|---------|
| `src/services/db.js` | Added `isTransientError()`, `addJitter()`, `withRetry()`, `logSystemEvent()` |
| `src/services/db.js` | Added `listId` parameter to submit functions |
| `src/pages/MCQTest.jsx` | Retry wrapper, error UI, exit confirmation |
| `src/pages/TypedTest.jsx` | Retry wrapper, error UI, exit confirmation |

---

### Solution #1: CSD/TWI Reconciliation

#### Problem
`class_progress` stores `currentStudyDay` (CSD) and `totalWordsIntroduced` (TWI). These could drift from reality if:
- Session completion failed mid-write
- Race conditions between concurrent requests
- Bugs in progression logic

#### Implementation

**New query in `db.js`:**

```javascript
export async function getRecentAttemptsForClassList(userId, classId, listId, maxResults = 8) {
  // Query attempts collection by studentId, classId, listId
  // Order by submittedAt desc, limit to maxResults
  // Returns array of attempt documents
}
```

**New reconciliation logic in `progressService.js`:**

```javascript
function calculateCSDAndTWIFromAttempts(attempts) {
  // Find highest studyDay among attempts
  const highestStudyDay = Math.max(...attempts.map(a => a.studyDay || 0));

  // Day 1: CSD = 1 if new test passed, else 0
  // Day 2+: CSD = studyDay if review test exists, else studyDay - 1

  // TWI = newWordEndIndex + 1 from new test where studyDay === CSD
  // (endIndex is 0-based, TWI is count)

  return { csd, twi };
}
```

**Modified `getOrCreateClassProgress()`:**

```javascript
export async function getOrCreateClassProgress(userId, classId, listId) {
  // Get or create progress document (existing logic)

  // NEW: Always verify against attempts
  const attempts = await getRecentAttemptsForClassList(userId, classId, listId, 8);
  const { csd, twi } = calculateCSDAndTWIFromAttempts(attempts);

  // If mismatch, reconcile and log
  if (csd !== storedCSD || twi !== storedTWI) {
    logSystemEvent('csd_twi_reconciled', { stored, calculated, attemptCount });
    await updateDoc(progressRef, { currentStudyDay: csd, totalWordsIntroduced: twi });
  }

  // Return both progress and attempts (attempts reused by Solution #2)
  return { progress, attempts };
}
```

#### Design Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Query limit | 8 attempts | Covers 4 days of new+review pairs |
| TWI calculation | `newWordEndIndex + 1` | endIndex is 0-based, TWI is count |
| Reconciliation timing | On every progress load | Self-healing without manual intervention |
| Return format | `{ progress, attempts }` | Allows Solution #2 to reuse attempts |

#### Files Modified

| File | Changes |
|------|---------|
| `src/services/db.js` | Added `getRecentAttemptsForClassList()` |
| `src/services/progressService.js` | Added `calculateCSDAndTWIFromAttempts()`, modified `getOrCreateClassProgress()` |
| `src/services/studyService.js` | Updated caller to destructure `{ progress, attempts }` |
| `src/pages/MCQTest.jsx` | Updated caller to destructure `{ progress }` |
| `src/pages/TypedTest.jsx` | Updated caller to destructure `{ progress }` |

---

### Solution #2: Init-Based Phase Detection

#### Problem
If a student completes the new word test on Day 2+ but crashes/closes browser before review:
- CSD hasn't incremented yet (review test completes the day)
- On return, `initializeDailySession()` treats it as a fresh start
- Student sees new word study phase again instead of resuming at review

#### Implementation

**New phase detection in `studyService.js`:**

```javascript
function determineStartingPhase(attempts, dayNumber) {
  const dayAttempts = attempts.filter(a => a.studyDay === dayNumber);
  const newTest = dayAttempts.find(a => a.sessionType === 'new');
  const reviewTest = dayAttempts.find(a => a.sessionType === 'review');

  // Day 2+: mid-session (new passed, no review) -> resume at review
  if (dayNumber > 1 && newTest?.passed && !reviewTest) {
    return { phase: SESSION_PHASE.REVIEW_STUDY, newWordScore: newTest.score };
  }

  // Day 1 with passed new test -> complete (impossible after reconciliation, log it)
  if (dayNumber === 1 && newTest?.passed) {
    logSystemEvent('impossible_phase_detected', { dayNumber, reason: 'day1_with_passed_new_test' });
    return { phase: SESSION_PHASE.COMPLETE, newWordScore: newTest.score };
  }

  // Day 2+ with both tests -> complete
  if (dayNumber > 1 && newTest?.passed && reviewTest) {
    return { phase: SESSION_PHASE.COMPLETE, newWordScore: newTest.score, reviewScore: reviewTest.score };
  }

  // Normal: fresh start
  return { phase: SESSION_PHASE.NEW_WORDS_STUDY };
}
```

**Updated `initializeDailySession()` return:**

```javascript
return {
  // ... existing fields ...

  // Phase detection for session recovery
  startPhase: phaseInfo.phase,
  recoveredNewWordScore: phaseInfo.newWordScore,
  recoveredReviewScore: phaseInfo.reviewScore
};
```

**Updated `DailySessionFlow.jsx` init effect:**

```javascript
// Handle session recovery based on startPhase from attempt history
if (config.startPhase === SESSION_PHASE.COMPLETE) {
  // Session already complete - show completion screen
  setNewWordTestResults({ score: config.recoveredNewWordScore });
  setReviewTestResults({ score: config.recoveredReviewScore });
  setPhase(PHASES.COMPLETE);
  return;
}

if (config.startPhase === SESSION_PHASE.REVIEW_STUDY) {
  // Mid-session recovery: new word test passed, need to do review
  const segmentWords = await getSegmentWords(user.uid, listId, config.segment.startIndex, config.segment.endIndex);
  setReviewQueue(segmentWords);
  setReviewQueueCurrent(segmentWords);
  setNewWordTestResults({ score: config.recoveredNewWordScore });
  setPhase(PHASES.REVIEW_STUDY);
  return;
}

// Normal flow continues...
```

#### Phase Detection Logic

| Day | New Test | Review Test | Result |
|-----|----------|-------------|--------|
| 1 | None | N/A | `NEW_WORDS_STUDY` (fresh start) |
| 1 | Passed | N/A | `COMPLETE` (impossible, log it) |
| 2+ | None | None | `NEW_WORDS_STUDY` (fresh start) |
| 2+ | Passed | None | `REVIEW_STUDY` (mid-session resume) |
| 2+ | Passed | Exists | `COMPLETE` (already done) |

#### Files Modified

| File | Changes |
|------|---------|
| `src/services/studyService.js` | Added `determineStartingPhase()`, updated `initializeDailySession()` return |
| `src/pages/DailySessionFlow.jsx` | Added startPhase handling in init effect |

---

### Solution #4: System Logging

System logging was implemented as part of Solutions #1-3:

| Event Type | Location | Trigger |
|------------|----------|---------|
| `attempt_retry_succeeded` | `withRetry()` | First attempt failed, retry succeeded |
| `attempt_write_failed` | `withRetry()` | All retries exhausted |
| `csd_twi_reconciled` | `getOrCreateClassProgress()` | CSD/TWI mismatch detected and fixed |
| `impossible_phase_detected` | `determineStartingPhase()` | Day 1 with passed new test found |

All events written to `system_logs` collection with timestamp and severity.

---

### Testing Checklist

**Solution #3:**
- [ ] Submit test normally - should succeed without retry
- [ ] Simulate network failure (offline mode) - should retry and show error UI
- [ ] Check answers preserved after failure - retry should work
- [ ] Try to navigate away with error - should show warning

**Solution #1:**
- [ ] Check console for reconciliation logs
- [ ] Manually corrupt CSD in Firestore - should auto-fix on next load
- [ ] Verify TWI matches after reconciliation

**Solution #2:**
- [ ] Complete new word test on Day 2+, close browser mid-session
- [ ] Reopen - should resume at REVIEW_STUDY phase
- [ ] Complete Day 1 - refresh should show COMPLETE

**Solution #4:**
- [ ] Check `system_logs` collection in Firebase Console
- [ ] Verify logs only appear for anomalies, not normal operations

---

## Bug Fixes: Session Fragility Code Review

**Date:** 2026-01-09

### Bug #4: CSD/TWI Regression on Empty Query Results

**Problem:** If `getRecentAttemptsForClassList()` fails or returns an empty array, `calculateCSDAndTWIFromAttempts()` returns `{csd: 0, twi: 0}`. The reconciliation logic would then overwrite valid stored values with zeros.

**Fix:** Added Math.max safeguard in `progressService.js`:

```javascript
// Use Math.max to prevent regression if query fails or returns incomplete data
const safeCSD = Math.max(storedCSD, csd);
const safeTWI = Math.max(storedTWI, twi);
```

This ensures CSD/TWI can only ever increase through reconciliation, never decrease. If the query fails, stored values are preserved.

**File:** `src/services/progressService.js` (lines 117-119)

---

### Bug #6: Undefined `combinedWords` in Test Recovery

**Problem:** The test crash recovery code in `DailySessionFlow.jsx` referenced `combinedWords` variable which was never defined in that scope. This would silently evaluate to `undefined`, then `combinedWords || []` would return an empty array - effectively breaking word recovery.

**Fix:** Changed to use `testRecovery.localState?.wordPool` which contains the word pool saved to localStorage when the test started:

```javascript
// Before (broken):
newWords: combinedWords || [],
wordPool: testRecovery.phaseType === 'new' ? (combinedWords || []) : null,

// After (fixed):
const recoveredWordPool = testRecovery.localState?.wordPool || []
newWords: recoveredWordPool,
wordPool: testRecovery.phaseType === 'new' ? recoveredWordPool : null,
```

**File:** `src/pages/DailySessionFlow.jsx` (lines 696, 713)

---

### Bug #7: `cardsReviewed` Not Reset on REVIEW_STUDY Recovery

**Problem:** When recovering to REVIEW_STUDY phase, the `cardsReviewed` state was not reset. If the student had reviewed cards in a previous session, the count would persist and show incorrect progress.

**Fix:** Added `setCardsReviewed(0)` to the REVIEW_STUDY recovery block:

```javascript
setReviewQueue(segmentWords)
setReviewQueueCurrent(segmentWords)
setReviewDismissed(new Set())
setCurrentIndex(0)
setIsFlipped(false)
setCardsReviewed(0)  // NEW: Reset card count for fresh recovery
```

**File:** `src/pages/DailySessionFlow.jsx` (line 633)

---

### Bug #8: No Error Handling for `getSegmentWords` in REVIEW_STUDY Recovery

**Problem:** If `getSegmentWords()` throws an error during REVIEW_STUDY recovery, the error would bubble up unhandled, potentially leaving the user stuck on a loading screen.

**Fix:** Wrapped the `getSegmentWords()` call in try-catch with user-friendly error display:

```javascript
if (config.startPhase === SESSION_PHASE.REVIEW_STUDY) {
  try {
    const segmentWords = await getSegmentWords(/* ... */);
    // ... recovery logic ...
  } catch (err) {
    console.error('Failed to load segment words for REVIEW_STUDY recovery:', err)
    setError('Failed to load review words. Please refresh and try again.')
    return
  }
}
```

**File:** `src/pages/DailySessionFlow.jsx` (lines 620-645)
| 2026-03-06 | `scripts/export-typed-test-answers.js` | Created script to export all typed test attempts with full answers array (including AI reasoning, challenge data) for accuracy analysis |
| 2026-03-06 | `scripts/build-ai-benchmark.js` | Created script to build AI grading benchmark dataset - enriches exported attempts with Korean definitions from Firestore, outputs in Cloud Function input format with current AI grades as baseline |
| 2026-03-07 | `functions/index.js` | Switched AI grader from GPT-4o-mini to Claude Haiku (3.55% → 0.96% error rate); added `?` to blank filter; added 3 prompt rules (#9 Korean def matching, #10 part-of-speech tolerance, #11 partial answers) |
| 2026-03-07 | `functions/package.json` | Replaced `openai` dependency with `@anthropic-ai/sdk` |
| 2026-03-07 | `functions/index.js` | Refactored grading prompt: replaced 11-rule prompt with 3 failure conditions + 8 few-shot examples from audit data; added `isSelfReferencing()` pre-filter; switched to JSON input format with explicit count instruction; moved grading philosophy to system message |
| 2026-03-09 | `package.json` | Added `@playwright/test` dev dependency and `test:e2e` / `test:e2e:ui` scripts |
| 2026-03-09 | `playwright.config.js` | Created Playwright config with Chromium, Vite dev server integration, HTML reporter |
| 2026-03-09 | `e2e/app.spec.js` | Created sample e2e test that verifies app loads |
| 2026-03-09 | `.gitignore` | Added Playwright artifact directories |
| 2026-05-30 | `audit_findings_persistence.md` | Created merged persistence/stability audit (Claude + Codex) with verification table and recommended fix order |
| 2026-05-30 | `firestore.rules` | C1: scoped `{path=**}/class_progress/{docId}` collection-group rule to `allow read: if isAuthenticated() && isTeacher()` (was `allow read, write` for any auth user, allowing arbitrary cross-student progress corruption) |
| 2026-05-30 | `firestore.rules` | C3: scoped student-side `attempts` update to `affectedKeys().hasOnly(['answers'])` so students can only mutate the answers array (for submitChallenge), no longer can rewrite score/passed/credibility; teacher-side update now also requires `isTeacher()` |
| 2026-05-30 | `firestore.rules` | C2: documented TODO on `/users/{userId}/{subcollection}/{docId}` write rule — teacher-any-student writes left in place to preserve reviewChallenge functionality; flagged for follow-up to move that write to a Cloud Function with Admin SDK and tighten the rule to `isOwner(userId)` |
| 2026-05-30 | `src/utils/testRecovery.js` | Added `getOrCreateAttemptNonce(testId)` and extended `clearTestState` to also remove the nonce key. Provides a stable per-session nonce for idempotent attempt-doc IDs so withRetry replays cannot create duplicate `attempts` documents |
| 2026-05-30 | `src/services/db.js` | `submitTestAttempt` + `submitTypedTestAttempt`: added optional `attemptDocId` parameter; switched from `addDoc` to `setDoc(doc(attemptsCol, attemptDocId))` when supplied so withRetry replays are idempotent overwrites of identical data (fix #5) |
| 2026-05-30 | `src/pages/MCQTest.jsx` | Fixed #1 + #3 + #4 + #5: reordered handleSubmit so the attempt doc is written FIRST and processTestResults (study_state mutations) runs only AFTER submit succeeds; clearTestState moved to after both writes; added resultsProcessedRef so Try-Again does not re-increment timesTestedTotal within the same mount; pass deterministic attemptDocId built from `${user.uid}_${testId}_${attemptNonce}` |
| 2026-05-30 | `src/pages/TypedTest.jsx` | Fixed #2 + #3 + #4 + #5: same reorder as MCQTest. clearTestState now runs only after AI grading + attempt write + processTestResults all succeed (was wiping local recovery before the 90s-per-attempt OpenAI call, losing 15-20 min of typing on any mid-flow failure) |
| 2026-05-30 | `change_action_log.md` | (this file) Logged the persistence audit and the rules + service + test-page fixes. #17 Dashboard hook-order DEFERRED to a dedicated follow-up PR: clean fix requires extracting StudentDashboardBody into a child component (~600 lines + 7 hooks + 5 helpers); mixing it into a persistence-focused PR creates regression risk on the very dashboard we're stabilizing |
| Date | File | Change |
| --- | --- | --- |
| 2026-05-31 | src/services/studyService.js | Fix B27-F01 BLOCKER: buildReviewQueue now excludes MASTERED (retired) words before selectReviewQueue, so words within their 21-day rest window no longer reappear in review tests and get re-tested/downgraded. |
| 2026-06-09 | src/pages/DailySessionFlow.jsx + src/services/studyService.js | Fix Day-2+ new-word test bypass: a student who FAILED the Day-2+ new-word test could be carried into the review phase and complete/advance the day without passing the gate (Day 1 correctly holds on failure; Day 2+ did not). (1) Resume guard in DailySessionFlow: only resume into review-study/review-test if `existingState.newWordsTestScore >= retakeThreshold`. (2) Backstop in completeSessionFromTest: for Day 2+, if `newWordScore < threshold`, block completion (skip recordSessionCompletion / CSD advance) and return `requiresNewWordRetake`. Root cause: handleReturnFromTest/resume moved Day-2+ to review unconditionally, and completion was gated only on the review test (which always passes). NEEDS staging/emulator test before deploy. Scan found 9 already-advanced (8×25WT, 1×26SM Ryan Han) + 2 mid-bug. |
| 2026-06-02 | src/services/studyService.js | Fix newWordsTestScore unit bug on Day-2 session resume. `determineStartingPhase` re-seeded recovered new-word/review scores straight from the attempt doc (stored as percent 0-100), but session_state.newWordsTestScore + all consumers (teacher roster "Current Session" cell, student resume banner) expect a fraction (0-1) and render ×100, so resumed students showed e.g. "9700%". Added `toFraction(s) = s>1 ? s/100 : s` (matches existing convention at line ~1139) and wrapped the three score-returning branches. Display-only: pass/fail gate and gradebook (read attempts directly) unaffected; bad values self-heal on day completion, no backfill needed. |
| 2026-06-10 | src/services/studyService.js | Patch v2 Change A: `determineStartingPhase` now picks the BEST new-word attempt for the day (prefer passed=true, then highest score) instead of `.find()` returning the first match. Fixes Day-2+ fail→retake→PASS students being bounced back to the new-word test on re-entry because the earlier FAILED attempt was inspected (e.g. 유지웅 87→100, JW Han 93 — 26SM Inter B2). |
| 2026-06-10 | src/services/studyService.js | Patch v2 Change C (urgent companion to the deployed Day-2+ completion gate): `completeSessionFromTest` now reads the attempt's authoritative `passed` flag (computed server-side against the class's real passThreshold, covers teacher manual overrides) and the gate only blocks when `newWordAttemptPassed !== true AND score < threshold`. Fixes 92–94% passers in 92%-threshold classes being silently blocked from completing the day (gate compared against wrong 0.95 default). |
| 2026-06-10 | src/services/studyService.js + src/pages/DailySessionFlow.jsx | Patch v2 Change D: fixed the retake-threshold fallback chain in 4 sites — `assignment.newWordRetakeThreshold` is never stored, so all settings builders fell back to the 0.95 default; now derive from the class's real `passThreshold` (percent / 100) before falling back. Fixes resume guard, `session_state.newWordsTestPassed` writes, and the completion-gate fallback. |
| 2026-06-10 | src/pages/DailySessionFlow.jsx | Patch v2 Change B (defense-in-depth): the `existingState` resume block now also honors attempt-derived state (`config.startPhase === REVIEW_STUDY`), so a stale session pointer (`phase: new-words-study`) can never bounce a confirmed passer back to the new-word test. |
| 2026-06-10 | src/pages/DailySessionFlow.jsx | Patch v2.1 Change E-1/E-2: both resume paths into review now detect an EMPTY review segment (all words MASTERED & resting after excludeRetiredMastered) and show the designed "all mastered" success modal → completeSession(), instead of pushing the student into a 0-word review test ("No Test Content" dead end). Live impact 2026-06-10: 4 top scorers blocked daily (정아영/Paige Lim/Ryan Kim day 6, 손지우 day 7). |
| 2026-06-10 | src/pages/DailySessionFlow.jsx | Patch v2.1 Change E-4/E-4b: `handleNoReviewModalClose` now writes an idempotent marker review attempt (deterministic doc id `..._day{N}_review_automarker`, score 100/passed/autoCompleted, explanatory note) so CSD reconciliation (`getOrCreateClassProgress`, which requires a day-N review attempt) does not REVERT the auto-completed day on next entry. Added `setDoc, Timestamp` to the firestore import (without them the ReferenceError would be swallowed by the try/catch). |
| 2026-06-10 | src/pages/MCQTest.jsx + src/pages/TypedTest.jsx | Patch v2.1 Change E-3: fixed misleading "No Test Content" copy — was "Your teacher hasn't assigned enough words yet." (false teacher-misconfiguration implication); now explains no words are available and points the student back to the dashboard. |
| 2026-06-15 | src/pages/DailySessionFlow.jsx | HOTFIX (Change E loading modal): render the No-Review "all mastered!" ConfirmModal inside the `phase === PHASES.LOADING` early-return. The empty-review auto-complete (Change E-1/E-2) fires during init while phase is still LOADING, but the loading early-return rendered before the modal JSX at the bottom of the component, so pool-collapse students resuming mid-day were stuck on an infinite "Preparing your session..." spinner. On OK, handleNoReviewModalClose runs completeSession + E-4 marker write and advances to the completion screen. |
| 2026-06-21 | src/services/db.js | CSD observability (v5, log-and-monitor): `getMostRecentPassedNewTest` now returns a DISCRIMINATED result `{status:'found',attempt}` / `{status:'none'}` / `{status:'query-error',error}` instead of `attempt\|null`, so callers can tell "no passed test exists" apart from a transient/index query failure. Error stringified (message/code/stack) for Firestore-safe logging. Single caller (progressService) updated. No reconciliation behavior change. |
| 2026-06-21 | src/types/studyTypes.js | CSD observability (v5): added `implausibleStudyDayThreshold(...)` — conservative looser-of-(calendar,TWI)+slack(7) ceiling, returns null when not computable. LOGGING ONLY (decides whether to emit `csd_implausible`); not a clamp. |
| 2026-06-21 | src/services/progressService.js | CSD observability (v5): `getOrCreateClassProgress` classifies the anchor (found/none/invalid-anchor/query-error) and emits logs WITHOUT auto-correcting CSD/TWI (reconciliation byte-for-byte unchanged): `csd_anchor_query_error` (transient — never treated as no-progress), `csd_anchor_invalid` (legacy malformed anchor — student has progressed), `csd_implausible` only for a clean `none` with stored CSD above a settings-gated threshold (legit no-anchor CSD≈0). Settings fetched only in the suspicious path; unavailable → skip. Plan: CSD_RECONCILIATION_CLAMP_PLAN.md. |
| 2026-06-21 | src/services/db.js | Dashboard audit Fix A: `fetchUserAttempts` no longer overwrites an attempt's authoritative `classId`/`listId` with parsed/guessed values. New-format `testId` regex now captures classId; classId precedence = doc.classId (guarding the backfill `'no_class'` sentinel) → testId-parsed → list→class lookup; listId precedence = doc.listId → testId-parsed (Codex follow-up: a missing/malformed testId must not null a stored listId). Fixes wrong per-class attribution for students dual-enrolled in two classes sharing a list. Read-only; single caller (Dashboard). Plan: DASHBOARD_AUDIT_FIX_PLAN.md. |
| 2026-06-21 | src/pages/Dashboard.jsx | Dashboard audit Fixes B/C/D/E. B: `getPrimaryFocus` now resolves saved focus by primaryFocusClassId+ListId (3-tier guarded chain: exact class+list → legacy list-only → auto-select); dropdown highlight compares classId+id. C: deleted dead `dailyStatus`/`testCompletedToday`/`sessionCompletedToday` (+ dead helpers `hasSessionToday`/`hasTestToday`/`getStartOfToday`); preserved the live list-scoped `listAttempts`/`phase` block. D: replaced redesign-introduced + in-zone raw colors (hero, list selector, student error banner) with design tokens (`bg-btn-success` check, `bg-muted`/`bg-brand-primary-10`, `border-border-error`/`text-text-error`, `text-white/70-80`); pre-existing raw colors elsewhere in file deferred to a follow-up. E: null-date list fallback now selects the FIRST list, not last. Plan: DASHBOARD_AUDIT_FIX_PLAN.md. |
| 2026-06-22 | Firebase (attempts, session_states) | **강민서 (MINSEO KANG)**: Created manual Day 6 new typed attempt (score 93%, 28/30, passed=true) because AI grading API failed at runtime. Updated session_states to phase=review-study, newWordsTestPassed=true. Student can now proceed with Day 6 review. |
| 2026-06-22 | Firebase (session_states) | **손지우**: Fixed corrupt session state. Was CSD=16, phase=review-study, newWordsTestPassed=false (impossible state). Changed phase to new-words-study so she can take the Day 16 new words test. |
| 2026-06-22 | Firebase (attempts, session_states) | **서준혁 (ADV A1)**: Created manual Day 1 new typed attempt (score 97%, 29/30, passed=true) after connection error. Wrong word: postmodern. Student can proceed with Day 1 review. |
| 2026-06-23 | src/pages/DailySessionFlow.jsx | **Fix 1 (typed-grading malform root cause)**: the test-phase crash-recovery marker written in `navigateToTest` (~L1168 via `saveLocalSessionState`) was thinning the word pool to `{id, word}`, dropping `definition`/`definitions`. On crash/reload mid-test, recovery rebuilt the test from that thin pool (PATH B) → grade payload had `correctDefinition=undefined` for EVERY word → `gradeTypedTest` rejected the whole batch as malformed (`invalid-argument`, "Grading Failed" loop). This is the ONLY recovery marker that fires for the test phase (the two autosave effects at L362/L401 — already patched to keep definitions — never run on the separate test route). Now persists `definition`/`definitions`/`partOfSpeech`, mirroring those siblings. Hit 14 real 26SM students on 2026-06-22 (230 rejections, all-or-nothing). Not committed/deployed. Plan: PLAN_typed_grading_malform_fix_v1.md (v2). 3-agent audited. |
| 2026-06-23 | functions/index.js | **Fix 2/2b/3 (server, typed-grading hardening — PLAN v5)**. (2) `gradeTypedTest`: server-authoritative answer key — new `resolveAnswerDefinitions(listId, answers)` backfills canonical `correctDefinition`/`koreanDefinition` from `lists/{listId}/words/{wordId}` (one `getAll`), gated by `callerMayResolveList(uid, classId, listId)` (enrollment + list-assignment; skip-resolution-not-block on denial → anti-oracle). `gradeAnswers` is now the single canonical array for ALL downstream use (finishGrading rows, malformed check, blank/self-ref filters, final ordering, post-grade validation). (3) Softened all-or-nothing: unresolved rows auto-marked incorrect (`malformedResults`) and the rest graded; throw only if EVERY row is unprocessable. (2b) `submitVocabAttempt`: `assertCanWriteAttempt` (factored from `writeAttemptTxn`) runs BEFORE `sanitizeStoredRows` (Admin-SDK reads stay post-authorization); `sanitizeStoredRows(listId, rows)` backfills `correctAnswer` for clients on an OLD thin recovery marker + coalesces all stored fields to non-undefined (covers typed + mcq). `buildTypedAttemptAnswers` also coalesced. node --check OK. Not committed/deployed. |
| 2026-06-23 | src/pages/TypedTest.jsx | **Fix 4/5/6 (client — PLAN v5)**. (4) grade call now sends `listId` + `classId` (server resolution + authz). (5) write-failure UX: extracted `doWriteAndFinalize` closure + `finalizeResultsView` helper from `handleSubmit`; on durable-write failure the closure is stashed in `pendingSaveRef` and a NEW standalone modal (`{submitError && !isSubmitting}`, replacing the dead nested one) offers **"Retry Save"** → re-runs the write ONLY via the stashed closure (never re-grades/re-bills AI). (6) `attempt_write_failed_client` logged in the write catch (closes the observability gap; distinct event name avoids double-count w/ withRetry). esbuild parse OK. Not committed/deployed. |
| 2026-06-23 | src/pages/MCQTest.jsx | **Fix 6 (client — PLAN v5)**: log `attempt_write_failed_client` in the server-write catch (same observability gap as typed). esbuild parse OK. Not committed/deployed. |
| 2026-06-23 | functions/index.js | **Codex r4 hardening (HIGH)**: `assertCanWriteAttempt` now actually enforces list-assignment — was `(assignments||{})[listId] || {}` (missing assignment fell through to default passThreshold, letting an enrolled student write/sanitize-read for an arbitrary listId under a real class). Now throws `failed-precondition` unless `assignments[listId]` OR legacy `assignedLists.includes(listId)`. Same assigned-list check added to `callerMayResolveList` (grade path). Verified the live 26SM class has both shapes populated → no live-write regression. node --check OK. |
| 2026-06-23 | functions/index.js | **Orphan-safe revision of r4 hardening (pre-deploy scan finding)**: a read-only scan of 4348 recent attempts found 11 (2 students, class teKHajON / list 7Is5UdS4P4) writing to a list NOT assigned to their class — the known list-unassign-orphan pattern. The hard `failed-precondition` reject would have newly blocked them. Changed `assertCanWriteAttempt` to NOT reject unassigned lists (enrollment still gates who can write); instead it returns an `assigned` flag and `submitVocabAttempt` gates ONLY the sanitize backfill reads on it (`sanitizeStoredRows(assigned ? listId : null, …)`). Closes Codex's read/cost-abuse path without breaking orphans; mirrors the grade path's skip-don't-block. Logic preflight 13/13 vs real data (incl. non-enrolled still rejected). node --check OK. |
| 2026-06-23 | functions/index.js | **Three-state list authz (Codex r5 — final posture)**. `assertCanWriteAttempt` now: assigned (assignments/assignedLists) → allow + backfill; orphan-eligible (NOT assigned but prior class_progress OR prior attempt for this uid/classId/listId) → allow + backfill (logs `unassigned_attempt_allowed` w/ orphanReason); neither → reject `failed-precondition` BEFORE any sanitize reads. Closes the "write/force-reads for an arbitrary unassigned list" path while preserving legitimately-orphaned students (verified: real orphan W3MUFXDb allowed via prior_attempt; arbitrary-unassigned rejected; non-enrolled rejected). `submitVocabAttempt` reverts to always backfilling `context.listId` (assert now guards entitlement). Added `passThresholdFallback` log when an unassigned/orphan write defaults to 95 (no server-side source for original threshold). Grade-path `callerMayResolveList` left assigned-only (skip-don't-block; orphan thin-marker grading is a rare pre-existing edge). DEFERRED (R7): client-supplied `isCorrect` still trusted on the write — broader forgeable-attempts concern, tracked separately. Logic preflight 14/14 vs real data. node --check OK. |
| 2026-06-23 | functions/index.js | **Codex r5 nits**: (1) updated stale `assertCanWriteAttempt` docblock to describe the three-state (assigned-or-orphan) check + new return shape. (2) `writeAttemptTxn(uid, ctx, attemptAnswers, auth?)` now accepts the pre-computed auth result; `submitVocabAttempt` passes it through to avoid a duplicate class-doc/orphan read per write. `gradeTypedTest.finishGrading` still calls without auth → self-authorizes (fallback). node --check OK; preflight 14/14. |
| 2026-06-23 | functions/index.js | **Codex r6 (HIGH): orphan gate used client-forgeable evidence**. The three-state gate queried `users/{uid}/class_progress` / `attempts` as orphan proof, but firestore.rules let a student write those docs → an enrolled student could mint their own "orphan" evidence for an arbitrary unassigned list. Replaced the live query with a static, server-trusted `KNOWN_ORPHAN_WRITES` allowlist (uid|classId|listId). Built from a FULL-collection census of all 7667 attempts → exactly 2 tuples (W3MUFXDb + fc8sBxnz, both teKHajON/7Is5UdS4P4, last active 2026-06-18) are the complete orphan set, so no real orphan is rejected. Now: assigned → allow+backfill; allowlisted orphan → allow+backfill (logs known_orphan_allowlist); everything else (incl. forged evidence) → reject failed-precondition before any reads. Preflight 15/15 incl. forgery-guard (non-allowlisted student on the orphan list rejected). node --check OK. Long-term: server-owned assignment-history model. |
| 2026-06-27 | dsg-edits/RUNBOOK_question_editing.md | **Documented answer-key edits (§4b).** Folded the answer-change procedure (previously only in `edit-t07.mjs`/`edit-t08.mjs`) into the runbook: per-choice checkbox + score dual-write, score `59` correct / `0` wrong (hardcoded, stays `59`), Vue native-setter + input/change dispatch, 4×4 exactly-count safety bail, and reload-verify of BOTH checkbox states (`keyCbOk`) and scores (`keyScOk`). Cross-linked §7.5 and added the two edit-tNN scripts to the §8 reference list. Doc-only. |
| 2026-06-27 | functions/index.js | **Lockdown W1 (PLAN_attempt_write_lockdown.md): server-side `submitChallenge` callable.** New `onCall` that moves challenge submission server-side so the function becomes the ONLY writer of `attempts.answers` (closes NEED_TO_FIX #1c: student could forge `answers[].isCorrect` via direct write → `reviewChallenge` launders it to a pass). One Admin-SDK `runTransaction` over `users/{uid}.challenges.history` (append) + `attempts/{id}.answers[i]` (set `challengeStatus`/`challengeNote` ONLY — never `isCorrect`); uid = `request.auth.uid` (server-trusted). All gates INSIDE the txn: ownership (`studentId===uid`), already-pending idempotency no-op, server-side token re-check via new `availableChallengeTokens()` helper (byte-parity port of client `getAvailableChallengeTokens`, db.js:177 — active rejections, `.toMillis()` safe-nav, max 5). Same history-entry shape as the old client write (`attemptId,wordId,challengedAt,replenishAt,status`). node --check OK. Not committed/deployed. Deploy choreography: fn first → flip `SERVER_CHALLENGE_WRITE` ON + rebuild → validate → W3 rules. |
| 2026-06-27 | src/config/featureFlags.js | **Lockdown W1: add `SERVER_CHALLENGE_WRITE` flag (default OFF).** Gates the client `submitChallenge` between the new server callable (ON) and the legacy client write (OFF, fallback until validated). Mirrors the `SERVER_ATTEMPT_WRITE` rollout pattern. esbuild unavailable in container (win32 binary); ESM `node --check` OK. Not committed/deployed. |
| 2026-06-27 | src/services/db.js | **Lockdown W1: `submitChallenge` → flag-gated wrapper.** When `SERVER_CHALLENGE_WRITE` is ON, calls the `submitChallenge` Cloud Function (`httpsCallable(getFunctions(),'submitChallenge')({attemptId,wordId,note})`, returns `.data`); when OFF, runs the unchanged legacy client body (two `updateDoc`s) as fallback. Signature `(userId, attemptId, wordId, note)` unchanged → call sites (`TestResults.jsx:61`, `Gradebook.jsx:446`) untouched; the callable uses `request.auth.uid`, not the passed `userId`. Added imports: `getFunctions, httpsCallable` (firebase/functions), `SERVER_CHALLENGE_WRITE` (config/featureFlags). ESM `node --check` OK. Not committed/deployed. |
| 2026-06-27 | dsg-edits/recon-t09t10.mjs, diff-t09-sample.mjs, full-diff.mjs (new, read-only) | **T09/T10 replacement prep — recon only, NO live writes.** Refreshed expired auth.json (login_save). Reconciled 26smt09/t10 QID map (162 tags, all single-copy 1182xx–1184xx, no dual-copy) against the two content sheets (45 rows/test, M01:13/M2L:16/M2U:16; every row maps to a QID). Found: (1) live questions already largely contain the sheet content/key/difficulty; (2) live editor has NO "Graphs and Tables" passage-type radio (only Lit/Hum/Sci/Empty) — per user, leave those 5 untouched, no Empty-enabler; (3) **7 contaminated passages in the "final" sheets** — 4× `nf-voc` leading line, 2× trailing `source:` URL (T09 M2U Q5, T10 M2U Q1), 1× full generator debug block lines 0–25 (T09 M2L Q12). Built cleanPassage() + ran full read-only sheet-vs-live diff to size genuine deltas before any write. |
| 2026-06-27 | functions/index.js | **G2 (PLAN_server_authoritative_grading.md): typed correctness authority via gradeToken + `correctnessSource`.** Added `crypto` + `GRADE_TOKEN_SECRET` (`defineSecret`) + `canonicalGradeArtifact`/`signGradeArtifact`/`verifyGradeToken` (HMAC-SHA256, constant-time, signs ONLY the grade-bearing subset `{wordId,studentResponse,isCorrect,aiReasoning}` + binding context `{uid,attemptDocId,classId,listId,testId,testType,totalQuestions,createdAt}`, rows sorted by wordId so order-independent). `gradeTypedTest`: `secrets += gradeTokenSecret`; accepts `gradeContext`; `finishGrading` mints+returns `gradeToken`/`gradeTokenCreatedAt` (both grade-only and write paths) and stamps `correctnessSource:'server-ai'` on its own write. `writeAttemptTxn(…, opts)`: stamps top-level `correctnessSource` (server-set; null for MCQ/legacy — no `server-mcq` until Phase E). `submitVocabAttempt`: `secrets += gradeTokenSecret`; for `testType:'typed'` verifies the token → on valid sets `correctnessSource:'server-ai'` + `sanitizeStoredRows(...,{overwrite:true})` (authoritative reconstruct of word/correctAnswer, ignore client values); enforcement staged behind `GRADE_TOKEN_ENFORCED=false` (when off: behaves exactly as today, no rejection/marker). `sanitizeStoredRows` gained an `{overwrite}` mode. node --check OK. Not committed/deployed; ENFORCEMENT OFF pending Codex pass + validation. |
| 2026-06-27 | src/pages/TypedTest.jsx | **G2 client thread.** `gradeWithRetry(answersToGrade, gradeContext)` passes `gradeContext` to `gradeTypedTest` (binds the token to the deterministic `attemptDocId`, `totalQuestions=words.length`); the `submitVocabAttempt` call now forwards `gradeToken`/`gradeTokenCreatedAt` from the grade response. No behaviour change while `GRADE_TOKEN_ENFORCED` is off (token ignored server-side). JSX — no container parser; owner build validates. Not committed/deployed. |
| 2026-06-27 | functions/index.js | **Lockdown W2: `markReviewComplete` callable.** Server-side write of the empty-review automarker (was the client `setDoc` at DailySessionFlow.jsx:962) so Day-2+ completion survives W3 `create:false`. Auth + `assertCanWriteAttempt` entitlement; deterministic id `${uid}_${classId}_${listId}_day${N}_review_automarker` → idempotent (existing+owned → no-op); same fields as the old marker + `writtenBy:'cloud-function'`; guards `dayNumber>1`. node --check OK. Not committed/deployed. |
| 2026-06-27 | src/config/featureFlags.js | **Lockdown W2: add `SERVER_REVIEW_MARKER` flag (default OFF).** Gates the empty-review marker between the new `markReviewComplete` callable (ON) and the legacy client `setDoc` (OFF). ESM node --check OK. Not committed/deployed. |
| 2026-06-27 | src/pages/DailySessionFlow.jsx | **Lockdown W2: automarker cutover.** `handleNoReviewModalClose` now flag-gates the empty-review marker write: `SERVER_REVIEW_MARKER` ON → `httpsCallable('markReviewComplete')({classId,listId,dayNumber})`; OFF → unchanged legacy client `setDoc` fallback. Added imports (`getFunctions,httpsCallable`, `SERVER_REVIEW_MARKER`). JSX — owner build validates. Not committed/deployed. |
| 2026-06-27 | firestore.rules | **[⚠️ SUPERSEDED same day — this edit was REVERTED; see the "W3 deploy-safety revert" row below. The live `firestore.rules` does NOT contain the lockdown — it was restored to pre-lockdown state and W3 moved to `docs/plans/W3_attempts_lockdown.rules.md`.]** ~~Lockdown W3: attempts write lockdown — `create: if false`; removed student `answers`-update branch; teacher update + student delete unchanged.~~ Not committed/deployed. |
| 2026-06-27 | functions/index.js | **G2/W2 Codex round-2 hardening (5 fixes, pre-deploy).** (1) CRITICAL — gradeToken could certify a client-supplied answer key: `resolveAnswerDefinitions` now returns `{answers, allResolved}`; gradeTypedTest computes `serverGraded = mayResolve && allResolved` and **mints the token / allows server-ai ONLY when serverGraded** (unauthorized-list or any-unresolved-row grading → no token, not certifiable). (2) CRITICAL — `markReviewComplete` always failed authz: it called `assertCanWriteAttempt(uid,{classId,listId})` but that requires a full ctx (studentId/attemptDocId/testType/sessionType) → now passes the marker's full ctx (markerId computed first). (3) HIGH — typed overwrite reconstruction fell back to client `word`/`correctAnswer` for missing word docs (`listVal ?? clientVal`): overwrite mode now **rejects** (`failed-precondition`) if any wordId is unresolved; never uses client display values. (4) MEDIUM — `correctnessSource:'server-ai'` was stamped whenever the token verified, even with enforcement off: now gated on `GRADE_TOKEN_ENFORCED` in BOTH the gradeTypedTest direct-write and submitVocabAttempt (no trusted markers before the W3 lockdown is live). (5) MEDIUM — signed `createdAt` wasn't age-checked: submitVocabAttempt now enforces a 24h TTL (+~1m future-skew reject) before accepting a token. node --check OK. Not committed/deployed; GRADE_TOKEN_ENFORCED still OFF. |
| 2026-06-27 | functions/index.js | **G2 Codex round-3 (2 fixes, pre-deploy).** (HIGH) gradeToken could be minted for one list while grading against another (resolution used `request.data.listId`, token bound `writeContext||gradeContext`): now derive ONE canonical `bindCtx` (writeContext||gradeContext) used as the SOLE source of listId/classId for BOTH resolution and token binding; top-level listId/classId are a fallback only when no bindCtx; `serverGraded` additionally requires `!ctxMismatch` (refuse if top-level values are sent AND differ from bindCtx). finishGrading reuses the outer bindCtx (removed its local re-derivation). (MEDIUM) made `GRADE_TOKEN_ENFORCED=false` a true no-op: overwrite reconstruction in submitVocabAttempt is now gated on `tokenOk && GRADE_TOKEN_ENFORCED` (was `tokenOk`), so before enforcement the typed write behaves exactly as today. node --check OK. Not committed/deployed; enforcement OFF. |
| 2026-06-27 | functions/index.js | **G2 Codex round-4 (deploy-safety, pre-deploy).** (HIGH) token minting touched `GRADE_TOKEN_SECRET.value()` on the live typed path even with enforcement off (client now sends gradeContext), making the secret a hard runtime dependency that could break grading if unset. Fix: added `GRADE_TOKEN_MINT` flag (default OFF); minting now requires `mintTokens = GRADE_TOKEN_MINT || GRADE_TOKEN_ENFORCED` — with both off, grading never calls `.value()` (no secret dependency). Enforcement implies minting (can't desync into rejection). Wrapped the mint in try/catch (mint failure → grade still returned, token absent) and the submitVocabAttempt verify `.value()` in try/catch (secret problem → tokenOk=false, never throws past). **Pre-deploy requirement documented:** create `GRADE_TOKEN_SECRET` in Secret Manager before deploying (the functions declare it; deploy needs it to exist) — but live grading stays dormant w.r.t. it until `GRADE_TOKEN_MINT` is flipped. node --check OK. Not committed/deployed. |
| 2026-06-27 | functions/index.js | **W1/G2 Codex round-5 (HIGH, pre-deploy): strip client challenge metadata on attempt create.** `sanitizeStoredRows` spread `...a`, so a caller could include `challengeStatus:'pending'`/`challengeNote`/`challengeReviewedBy`/`challengeReviewedAt` in `attemptAnswers` and have them persisted on a NEW attempt — forging a pending challenge and bypassing the W1 `submitChallenge` token+history workflow (and reviewChallenge reads `answers[i].challengeStatus`). Fix: explicitly normalize all four challenge fields to `null` in `sanitizeStoredRows` output (covers typed-retry AND MCQ server writes); only `submitChallenge`/`reviewChallenge` add challenge metadata later. (Other unrecognized client row fields are inert — not read — so left as-is.) node --check OK. Not committed/deployed. |
| 2026-06-27 | docs/plans/*.md | **Downstream-plan consistency sweep (doc-only).** Reconciled the now-settled scope across the 4 grading/provenance plans: MCQ correctness authority is **Phase E** (server-owned option token / test-init snapshot), NOT a "fast-follow after Phase D" and NOT closeable by the forgeable `selectedOptionId`. Updated PLAN_grading_writepath_program.md (§1 Phase D, §6 out-of-scope, §7-G) + PLAN_teacher_grade_override.md (§0.5 per-answer gate) to say "Phase E"; marked the server-auth plan's §7-G reconciliation note ✅ done. Other scanned refs (server-mcq=no-trusted-marker, G2-only typed authority, deferred denominator/anchor) already consistent. No code change. |
| 2026-06-27 | firestore.rules | **W3 deploy-safety revert (Codex-High): keep live rules deploy-safe; stage W3 separately.** The W3 attempts lockdown (`create:false` + student `answers`-update removed) was applied directly to `firestore.rules`, but the client cutover flags (`SERVER_CHALLENGE_WRITE`/`SERVER_REVIEW_MARKER`) are still OFF — so any routine `firebase deploy` would ship the lockdown prematurely and deny the client's still-live fallback writes (challenge submit + empty-review marker). Reverted the `attempts` block to its current safe state (a ⚠️ comment now points to the staged file). W3 moved to **`docs/plans/W3_attempts_lockdown.rules.md`** (the exact block + preconditions + apply-last instructions + rollback). Net: repo's `firestore.rules` is deployable any time; W3 is applied deliberately as the final step after the flags are ON + validated. Not committed/deployed. |
| 2026-06-27 | LIVE digitalsat.co.kr T09/T10 question bank (48 questions) + dsg-edits/edit-t09t10.mjs, chart-insert.mjs (new) | **T09/T10 replacement executed on live.** 44 text-row writes via edit-t09t10.mjs (content overwrite + key/diff/passage-type only-if-differ; VOC Empty-enabler; G&T passage-type untouched). Result: 44/44 save:ok, 0 blocked, key/diff/PT all ✓; 6 rows showed passage/question ✗ that were FALSE POSITIVES (TinyMCE encodes accented chars → `&oacute;` etc.; re-verified ✓ with full entity decoder). 4 SVG-chart questions (118271/118255/118398/118381) were broken on live (chart flattened to text — platform strips inline SVG) → fixed via chart-insert.mjs: replace `<p><svg></p>` with centered PNG (from 26sm-t09-t10-images/, base64→`ed.uploadImages()`→hosted blob) + `<br><br>` + prose; all 4 img:✓ hosted:✓ save:ok, visually verified. Per-question backups in backups_t09t10/. Passages cleaned of generator contamination (nf-voc / trailing source: URL / full debug block). T10_M01_Q12 table question already correct, untouched. |
| 2026-06-27 | functions/index.js | **G2 Codex round-6 (CRITICAL, pre-deploy): writer-API guard in writeAttemptTxn.** Previously the `correctnessSource` was only *stamped*, not *required* — so under enforcement two paths could still persist a typed grade from unresolved/client definitions: (a) any caller of writeAttemptTxn omitting correctnessSource, and (b) gradeTypedTest's direct-write path when `serverGraded=false` (writes with null marker). Fix (§8.4): the one true writer now **refuses** a TYPED grade-bearing write when `GRADE_TOKEN_ENFORCED && correctnessSource !== 'server-ai'` (`permission-denied`). Covers BOTH the gradeTypedTest direct-write and submitVocabAttempt paths structurally. MCQ exempt (correctness authority is Phase E — no marker yet). node --check OK. Not committed/deployed. |
| 2026-06-27 | change_action_log.md | **Audit-accuracy fix (Codex):** marked the earlier "Lockdown W3: attempts write lockdown" row [SUPERSEDED] — that edit to firestore.rules was reverted same-day (W3 staged in docs/plans/W3_attempts_lockdown.rules.md); the live rules do NOT contain the lockdown. Reconciles the record. |
| 2026-06-27 | dsg-edits/verify-t09t10.mjs (new, READ-ONLY) | **Read-only re-verification of all 90 T09/T10 questions — NEVER saves** (no save/click/upload; nav + read getters only; full HTML-entity decoder per §4e; chart-aware: expects hosted <img>+prose for SVG rows, not an SVG match). Result: 80/90 pass; ALL 90 correct on content fields / answer key / difficulty; 4 charts + 1 table verified. 10 "failures" are passage-type only: VOC rows (118297,118296,118284,118280,118268,118266,118265,118422,118406,118390) read passage-type (none) on live vs expected Empty — root cause: full-diff.mjs's unreliable pt-detector wrongly classed them "already correct" so they were excluded from the write batch (NOT write failures; never touched). Per user decision 2026-06-27: **leave the 10 as-is** (content/key/difficulty all correct; passage-type metadata intentionally left unset). Report: dsg-edits/verify_report.json. |
| 2026-06-27 | firestore-tests/attempts_lockdown.rules.test.js (new) | **Tests: W3 rules-unit (security core).** `@firebase/rules-unit-testing` + node:test specs proving forgery is denied at the rule boundary: student create → denied, student answers/score update → denied, student own-delete → allowed, teacher-of-record update → allowed (other teacher denied), reads scoped to owner/teacher-of-record. Loads `firestore.rules` as-is → run AFTER applying the staged W3 block (run-before-W3 failures are the correct "not-applied-yet" signal). Needs `npm i -D @firebase/rules-unit-testing` + firestore emulator. ESM syntax OK. |
| 2026-06-27 | e2e/lockdown_g2.spec.js (new) | **Tests: Playwright E2E (legit flows).** Matches the existing harness (seeded_accounts.json / env creds, login helper, step+console logging). Covers login smoke, typed-grade round-trip (G2 no-regression), challenge submission (W1 callable path), empty-review Day-2+ completion (W2) — asserts user-visible OUTCOMES (resilient to flag on/off) + no grading/permission console errors. Daily-flow selectors are best-effort (authored without execution) — confirm on first run. Run: `npm run test:e2e -- lockdown_g2` with flags ON + dev server. ESM syntax OK. |
| 2026-06-27 | docs/plans/TEST_PLAN_lockdown_g2.md (new) | **Test plan/runbook** tying the two layers together (rules-unit first = strongest security signal, no deploy; Playwright E2E for flows), with setup commands, flag preconditions, and the manual deploy-time checks (data-integrity-sweep, live denial spot-check). Functions-logic unit tests flagged as a follow-on (need helper exports + functions-test mocking). |
| 2026-06-27 | dsg-edits/generate-lecture-pdf.mjs (new) + lecture_T09.pdf, lecture_T10.pdf | **Lecture-print PDFs for T09 & T10.** One PDF per test, all 3 modules combined (M01/M2L/M2U), built from cleaned sheet content (cleanPassage strips nf-voc/source/debug; SVG charts + table kept as rendered HTML), rendered Letter via Chromium page.pdf with footer page numbers. Each question shows module·Q# + type/passage-type meta, passage, stem, boxed A–D choices (no answers inline). Answer key on its own page at end: 1 row/question, columns Module | Q# | Answer | Difficulty(Easy/Med/Hard) | Question Type(VOC/PUR/PP/COM/QT/SUC/LOC) | Passage Type(— for VOC). 45 q each; line chart (T09), bar chart + table (T10) all render. Read-only on data; no dsg-site access. |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | NEW: plan to persist list progress with student+list (not class+list); closes NEED_TO_FIX #6 / class-change day-reset cluster |
| 2026-07-04 | NEED_TO_FIX.md | Documented #6: class change resets list progress (root cause: {classId}_{listId} key); links plan + CS-2026-06-30/-07-02b |
| 2026-07-04 | RESUME.md | Rotated (archived to docs/resume_archive/RESUME_2026-07-04.md); active stream -> list-progress-persist plan |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | REVISED after 3-agent (Lens A correctness / B security / C UX, Opus) + Codex audit. Verdict: NOT implementation-ready — reframed from a doc-id rename to its real scope. Added blockers: reconciliation/anchor readers are classId-filtered (db.js:3044/3089/3155/3209/3268) → shared progress ping-pongs/demotes; currentStudyDay is a stored counter driving review segmentation (not a twi/pace view — kills old §2 safety claim); session_states holds class-specific state (keep class-scoped, don't collapse to {listId}); updateClassProgress non-transactional; single-write migration loses data (→ dual-write/versioned); resetStudentProgress (db.js:2886, mislabeled "unenroll") is a cross-class nuke. Added Firestore-rules section, corrected+expanded change surface (MCQTest/TypedTest/reviewChallenge R+W/automarker), migration field-by-field rules (programStartDate=min, anchor write, idempotency), threshold-shopping/settings-from-routed-class, teacher-gradebook disambiguation, getPrimaryFocus de-scope. 5 open decisions for David. |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | REVISED (round 2) after Codex second pass. Escalated from "list-scope the readers + client-flag dual-write" to server-authoritative architecture: added canonical server-owned list-day claim (list-scoped startTest; concurrent classes JOIN one allocation), resetEpoch/generation scoping, all-advancement-through-one-server-txn (completion/automarker/challenge-accept/retry/reset), anchor-by-newWordEndIndex (not studyDay), reconciliation repair-only, staged rollout LEGACY->SHADOW_WRITE->MIGRATE->NEW_PRIMARY->NEW_ONLY + rollback shadow scope, two reset semantics, concrete data-split paths (§3.2), single settings/threshold authority, rules restructure (broad wildcard override problem; enrollment-scoped teacher reads; #1b/#1c HARD prereqs). Added §7 R1-R10 + validation (simultaneous starts pace80/100, reset-restart, cutover, txn-failure, cross-teacher denial, review pairing). NEW §9 Decision #6: merge/sequence with PLAN_grading_idempotent_concurrency.md (the two plans converged on the same server substrate). |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | REFRAMED to v3 around settled principle (David): "list progress is student-owned; class confers only list ACCESS + daily quota/policy (pace/threshold/testMode) per launched session." No class owns progress state → dropped the shared-live-position architecture the audits drove toward (canonical claim, resetEpoch here, 5-mode rollout, rollback-shadow, per-class perf-view sync = Codex R1/R2/R4/R5/R6/R7, artifacts of class-scoped allocation). Surviving scope: (1) list-scope reconciliation/anchor readers + select anchor by newWordEndIndex not studyDay, (2) class_progress -> list_progress/{listId} (position only), (3) gradebook/pacing become views over class-tagged attempts, (4) one-time migration. Confirmed w/ David: teacher visibility = shared truth + class-filtered performance; day = session count. Audit lineage preserved in plan Appendix A. Cross-plan: recommend list-scoping the grading session key (§11). |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | Decisions settled (David): (1a) full policy bundle (pace+threshold+testMode) follows the launched/routed class — move A→B uses B's, multi-enrolled-under-B uses B's; +guard warns on divergent shared-list thresholds; validation asserts selected class == launch classId. (2) grading session-key list-scoping DEFERRED to grading-concurrency rework (bundled w/ grade-token re-enable; GRADE_TOKEN_ENFORCED currently off per CS-2026-06-29); double-grade deferral safe (rare + Phase-1 reconciliation dedups position, only cosmetic dup attempt). (3) Option B — ship list-progress on client-writable footing, land #1c attempt/progress-write lockdown in parallel; widened-blast-radius risk accepted+noted (§10). Nothing gates Phase 1. |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | v3.1 after fresh 3-reviewer + orchestrator re-audit of v3 (Fable 5). Verdict: model holds; v3 treated it as a doc-id rename while surrounding machinery stayed class-aware. Blockers fixed: (V1) reset-resurrection — attempts delete is class-filtered (db.js:2960-64) vs list-wide mastery wipe + list-scoped reconciliation → §5.3 list-aware reset (attempts-first, all session docs); (V2) fresh-doc fork — getOrCreateClassProgress creates Day-0 on miss → §5.2 hydrate-on-miss (also quarantine terminal state); (V3) field-disposition contradiction → §2.1 table: ALL student-level fields stored shared, no projection builder (per David's shared-truth call). Highs: review/gate pairing rules (pair to anchor's classId; position-consistency), orphan-cleanup cross-class deletion guard, 4 named composite indexes + sparse-newWordEndIndex fallback, migratedAt idempotency sentinel, same-day double-introduction serve guard + stale-session day-guard + guard-rejection rebuild, grep-gate regex (classIdParam variants), automarker de-listed from progress surface, collection-group rule made conditional (no in-repo consumer). Phase-1-alone documented as deliberate early carry-forward with bounded interim artifacts. |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | v3.2 after Codex round 3. ACCEPTED+FIXED: (C3-3a) reset must also delete ALL legacy class_progress {*}_{listId} docs or §5.2 hydrate-on-miss resurrects the reset (real v3.1 bug); (C3-4) Phase-5 pre-retire catch-up merge for stamped legacy docs advanced by flag-off clients (ancillary fields have no anchor; position self-heals); (C3-5) CSD merge rule — twi=anchor-max bidirectional, CSD=non-demoting Math.max honoring "day=session count" (v3.1's carry-matching-CSD contradicted the product rule; lost downward self-heal covered by csd_implausible logs); (C3-6) review pairing adds temporal lineage (review.submittedAt>=anchor.submittedAt) + getReviewForDay discriminated found/none/query-error (error must not decrement CSD); (C3-7) hydration = transactional create-if-absent + enumerates ALL legacy docs incl. dropped classes via listId field (verified docs carry it); (C3-9) Phase-1 list-scoped reconciliation flag-gated (LIST_SCOPED_RECON) for rollback. DECLINED (documented, not hidden): C3-1/2/3b claim/txn/epoch — new §13 accepted-risk inventory w/ VERIFIED damage bounds (attempts=ledger + init-time reconcile studyService:157; getUnmasteredPool is position-based so skipped words return as reviews studyService:373-395; day counter converges) and explicit hand-off: these primitives land in the grading rework Phase 2 (list-scoped startTest, transactional finalize, epoch resetProgress) per David's deferral. §13 doubles as that rework's acceptance checklist. |
| 2026-07-04 | RESUME.md | Rotated (archived to docs/resume_archive/RESUME_2026-07-04b.md); active stream -> list-progress plan v3.2 SETTLED, first moves = Phase 0 audit script + Phase 1 indexes/flag-gated reconciliation; queued: #1b/#1c lockdown (parallel track), grading Phase 2 (now owns claim/txn/epoch per plan §13), retakeThreshold #5, getPrimaryFocus #3b. |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | Added §13.1 "Why deferral is right at this app's scale" — decision rationale for not building Codex-3's claim/txn/epoch primitives here: incidence math (zero progress-counter races in the CS log; dual-enrollment always transient), concurrency evidence points at the grading pipeline (where the primitives ARE being built), forgery (#1c) outranks races as the real risk, complexity budget of a two-person operation, and explicit flip conditions (multi-academy/SaaS, ~10x students, self-serve teachers, first race-shaped ticket). Races table now §13.2. |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | v3.3 after Codex round 4 (CONVERGED — Codex accepted §13, verdict "implementable under the explicitly accepted risks"). All 6 findings fixed: (C4-1, blocker) unified merge rule applied identically at migration+hydration — CSD = max PLAUSIBLE across ALL source docs, not the max-twi winner's (v3.2 fixed only runtime reconciliation; would have permanently lost Day-15 in the plan's own pace-80/pace-20 example since non-demoting max can't recover an unstored value); (C4-2) Phase-0 CSD corruption screen (implausibleStudyDayThreshold) so non-demoting CSD can't immortalize a forged value; (C4-3) real cutover barrier: monitor legacy writes → firestore.rules deny → final transactional catch-up → delete (stamp check ≠ barrier); (C4-4) implementable 4-step hydration txn (pre-query candidates, in-txn re-read refs, recompute merge, create-if-absent); (C4-5) getReviewForDay temporal pairing = range query → new class-inclusive composite (studentId,classId,listId,sessionType,studyDay,submittedAt ASC), earliest-post-anchor limit(1); (C4-6) teacher-visibility stated precisely: global-teacher read is pre-existing wildcard behavior, accepted in single-academy trust domain, enforcement → #1b track. |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | Final self-pass (post-Codex-4 convergence), 5 seam fixes [F1-F4 + wording]: (F1) getNewWordAttemptForDay has non-reconciliation callers (TypedTest 833/855, MCQTest 552/582 day-stamping) — per-call-site scope disposition rule added to §5.1; (F2) coordination constraint: #1c lockdown must not remove client attempt-delete (rules currently allow it "for progress reset") before reset moves server-side, else client reset breaks — flagged in §5.3 for the lockdown track; (F3) list_progress schema must drop/rename the classId field createClassProgress embeds in the doc body; (F4) §9 adds concurrent-hydration two-tab test; §6 P1 wording "four indexes" → five (incl. getReviewForDay's C4-5 composite). RESUME.md index count note applies (plan §6 P1 is authoritative). |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | v3.4 after Codex round 5 (zero blockers). (C5-1) explicit quarantine branch: suspect docs never hydrate — read-through legacy + list_progress_quarantined log until CS resolves; same branch when all sources quarantined (fixes v3.3 contradiction between anchor-validated-TWI and preserved-stored-values). (C5-2) cleanupOrphanedReviews LOG-ONLY during mixed history (same-class pace-change/pre-reset history defeats the classId restriction); deletion resumes only with generation/epoch-tagged attempts. (C5-3) anchor tie-break newWordEndIndex DESC, submittedAt DESC (+docID) + composite index updated. (C5-4) Phase-5 legacy-write cutoff restructures the WILDCARD rule itself (permissive-union: additive deny can't override) + old-client permission-denied UX defined (attempt already server-written → no data loss; save-failure modal; reload; monitor denial spike). (C5-5) F2 corrected: W3 lockdown PRESERVES owner attempt-delete (attempt_write_lockdown.md:91-92 "reset — unchanged") — no present collision; constraint activates only when reset moves server-side. (C5-6) pacing-badge programStartDate consumers enumerated: Dashboard.jsx:188, DailySessionFlow.jsx:1292/1334/1431 (verified). |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | v3.5 after Codex round 6 (2 high rollout blockers + 1 medium, both highs VERIFIED against code before accepting). (C6-1) hydration was write-path-only: getClassProgress is a pure null-on-miss read → post-flip Dashboard/fetchStudentsProgressForClass would show Day 0/no-progress for un-migrated/lazy-pending/quarantined students. Fix: one centralized resolveListProgress(uid,listId,launchClassId)→{mode,progressRef,data,sourceClassId} for EVERY path — reads compute merged view in-memory (never write from render paths), writes hydrate. (C6-2) v3.4 cutoff UX claim was factually wrong: completion failures are console-swallowed (TypedTest.jsx:1040-42 "Don't fail the whole submit", MCQTest same) — no modal, no observable event; a denied legacy write = student silently stops advancing. Fix: permission-denied detection in ALL post-attempt completion paths (TypedTest/MCQTest/DailySessionFlow), blocking "app updated — reload" state, dedicated legacy_write_denied system_logs event, min-client-version gate preferred over write-failure discovery. (C6-3) quarantine contract precise: pinned deterministically to launching class's legacy doc (= status-quo per-class semantics, no new divergence risk; blocking documented as alternative). |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | v3.6 after Codex round 7 (2 high). (C7-1) cutoff re-sequenced: handlers/version-gates cannot retrofit into cached pre-Phase-2 bundles — gate ships EARLY (Phase-2 release), cutoff waits until pre-gate bundles expire from telemetry (post-flip, any legacy class_progress write = old-bundle detector since flag-on bundles never write legacy), only then deny; permission-denied handler = backstop for gate-aware bundles only. (C7-2) quarantine policy corrected: v3.5's continue-on-legacy routing WITHDRAWN (perpetuated per-class divergence as an operational mode contra the one-live-position goal + broke when the launching class had no legacy doc). Now: quarantine set resolved to ZERO is a hard Phase-5 precondition (CS triage per §8 merge); runtime backstop = BLOCK study with contact-teacher state + list_progress_quarantined event (blocking acceptable because post-flip quarantine = genuine corruption where continuing writes is worse). Validation cases added. |
| 2026-07-04 | docs/plans/PLAN_list_progress_persist.md | v3.7 — AUDIT-CLEAN (Codex round 8: "architecture-ready", all prior areas confirmed coherent). Final fix (C8-1): old-bundle expiry was stated as provable — corrected to evidence-not-proof: concrete 14-day no-legacy-write observation window + build-version census from gate-aware session stamps; explicit accepted residual = a dormant pre-gate tab can wake post-cutoff into ONE false-success completion (attempt server-written, nothing durable lost); data integrity carried by the rules cutoff, recovery = reload, stale tab self-limiting; post-cutoff denied-write trickle documented as this residual, not rollout failure. Plan audit cycle CLOSED: 8 Codex rounds + 2 internal 3-agent rounds, v1→v3.7. |
| 2026-07-04 | dsg-edits/srv_validate/list_progress_audit.mjs (new) + list_progress_audit_2026-07-04.json | Phase 0 read-only audit (PLAN_list_progress_persist v3.7 §6 P0) written and RUN against live data. collectionGroup scan of all 1112 class_progress docs (766 students, 1043 student-list pairs, 47 classes); per-pair passed-new attempt fetch (equality-only, no composite needed); anchor-validated twi per doc; CSD screened vs replicated implausibleStudyDayThreshold. RESULTS: collision set 69 (62 in 26SM) w/ twi conflicts 58 — the day-reset bug at scale; dual-enrolled 61 (54 in 26SM) → plan §13.1 incidence claim CORRECTED (stock is real, not ≈zero; §5.4 guards load-bearing); QUARANTINE total 70 = twi>anchor 7 + anchorless-nonzero 63 + CSD 0 — ALL in ext/retired-cohort legacy lists, 26SM is quarantine-CLEAN (C7-2 precondition already satisfied for the active cohort); invalid-anchor attempts 26 (all ext). OPEN → David: triage the 70 ext items vs exclude retired cohorts from migration. Plan §6 P0 + RESUME updated with results. |
| 2026-07-04 | src/config/featureFlags.js | Phase 1 impl (PLAN_list_progress_persist v3.7 §6 P1): new flag LIST_SCOPED_RECON (default false) — list-scoped CSD/TWI reconciliation + anchor readers. Flag-off = byte-equivalent legacy behavior. |
| 2026-07-04 | firestore.indexes.json | Phase 1: +6 attempts composite indexes for the list-scoped readers (studentId,listId,submittedAt) / (+sessionType,studyDay) / (+sessionType) / (+sessionType,passed,newWordEndIndex DESC,submittedAt DESC anchor primary) / (+sessionType,passed,studyDay DESC sparse-nwei fallback) / (studentId,classId,listId,sessionType,studyDay,submittedAt ASC — getReviewForDay pairing). Semantic diff vs backup: 0 removed, 6 added, fieldOverrides preserved. DEPLOY BEFORE flag flip. |
| 2026-07-04 | src/services/db.js | Phase 1 (all flag-gated): getMostRecentPassedNewTest — list-scoped anchor by newWordEndIndex DESC + submittedAt DESC tie-break [C5-3] with studyDay-ordered list-wide sparse fallback [V7]; getReviewForDay — discriminated {found|none|query-error} return (universal — sole caller progressService updated; error can no longer read as "no review" [C3-6]) + anchor-class/temporal-lineage pairing (anchorClassId + submittedAt>=anchor, earliest post-anchor [C4-5/V4]); getRecentAttemptsForClassList — list-scoped under flag; getNewWordAttemptForDay — opt-in {listScope} param, only the Day-2+ completion gate opts in; TypedTest/MCQTest day-stamping call sites stay class-scoped by design [F1]. getMostRecentNewTest untouched (dead code, zero callers). |
| 2026-07-04 | src/services/progressService.js | Phase 1 (flag-gated): safeCSD = non-demoting Math.max(stored, anchor-derived) under LIST_SCOPED_RECON — day = session count, cross-pace anchor may carry lower studyDay [C3-5]; twi unchanged (anchor-authoritative bidirectional). getReviewForDay caller consumes discriminated status + passes anchor pairing; query-error logged, cannot demote. cleanupOrphanedReviews gains {logOnly} — LOG-ONLY under flag (system_logs type orphaned_attempt_flagged, no deletion) [C5-2]. updateClassProgress duplicate-day rejection now returns inert dayGuardRejected marker. |
| 2026-07-04 | src/services/studyService.js | Phase 1 (flag-gated): completeSessionFromTest Day-2+ gate queries the day's new attempt with {listScope} (shared-truth pass accepted cross-class) + position-consistency guard — attempt.newWordStartIndex must equal sessionConfig.newWordStartIndex else ignored [V4]; recordSessionCompletion handles dayGuardRejected → clearSessionState + day_guard_rejected_session_cleared system_log so a student is never stuck against a reconciliation-advanced counter [V9/§5.4]. Verified: eslint 0 new errors (11 pre-existing no-unused-vars); NOT deployed — owner deploys indexes first, then client; flag stays OFF until validated. |
| 2026-07-05 | src/services/db.js, progressService.js, studyService.js, TypedTest.jsx, MCQTest.jsx | Phase 1 fix round (Codex diff review, 4 findings, all fixed; flag still OFF). (P1-1) query-error could ADVANCE CSD via Math.max(storedCSD, anchorDay-1) — now reviewLookupFailed pins safeCSD to storedCSD exactly (TWI reconciles independently); getReviewForDay under flag with MISSING anchor lineage no longer silently falls back to the launching-class query (bypassing temporal pairing) — returns query-error. (P1-2) position-consistency failed OPEN (missing newWordStartIndex authorized cross-class) + newest-only limit(1) could block on an inconsistent attempt — selection moved into getNewWordAttemptForDay: limit(10) candidate scan, exact newWordStartIndex===expectedBase for cross-class trust, missing-position candidates trusted only from the launching class, no expectedBase → launching-class only (fail closed). (P1-3) day-guard rejection previously STILL wrote the completed-session record, graduated words, and returned success — now recordSessionCompletion aborts (no session record) and returns dayGuardRejected; completeSessionFromTest skips graduation and returns requiresSessionRebuild; TypedTest/MCQTest show a "session refreshed — answers saved, return to study screen" state instead of success; dayGuardRejected marker itself now flag-gated (flag-off byte-equivalent). (P1-4) anchor query limit(1) could let a malformed non-numeric newWordEndIndex (sorts above numbers in DESC) suppress valid anchors — now limit(8) scan for the greatest VALID integer position. eslint: 0 new errors (same 11 pre-existing in services; page errors pre-existing). Safe to deploy flag OFF per Codex; do not enable until validated. |
| 2026-07-05 | src/services/db.js, sessionService.js, studyService.js, firestore.indexes.json | Phase 1 fix round 3 (Codex diff review #3, 3 findings, all fixed; flag still OFF). (P1r3-1) cross-class gate candidates now REQUIRE passed===true, enforced in the query itself — a newer failed attempt can no longer mask an earlier pass, and a failed cross-class attempt's raw score can never reach the launching class's local-threshold fallback (that fallback is now launching-class-only by construction). (P1r3-2) bounded client scans eliminated: gate = indexed equality query (studentId,listId,sessionType,studyDay,newWordStartIndex==expectedBase,passed==true, submittedAt DESC limit 1) with launching-class legacy fallback → NEW 7th composite index added; anchor = range filter newWordEndIndex>=0 (Firestore range filters are type-scoped, so malformed strings/bools are excluded by the QUERY, not a scan window) + limit(1) + integer paranoia check → reuses the existing anchor composite. (P1r3-3) clearSessionState now returns success boolean; day-guard block retries once, escalates to error-level system_log day_guard_session_clear_FAILED + sessionCleared:false in the sentinel when deletion fails (rebuild never silently claimed over a surviving stale COMPLETE doc). eslint: 0 new errors (11 pre-existing in core services; sessionService:381 isFirstDay pre-existing, newly linted). Deploy note: SEVEN new indexes now, not six. |
| 2026-07-05 | src/services/db.js, studyService.js, TypedTest.jsx, MCQTest.jsx | Phase 1 fix round 4 (Codex diff review #4, 2 findings, both fixed; flag still OFF). (P1r4-1) sessionCleared now PROPAGATED through completeSessionFromTest to both UIs — sessionCleared:false shows a distinct blocking recovery message ("answers saved, session could not be reset — reload; tell your teacher if it repeats") instead of falsely claiming the session was refreshed. (P1r4-2) Codex factual correction accepted: Firestore interleaves integers and DOUBLES as one number type, so >=0 does NOT exclude a malformed 999.5 from topping limit(1) and abandoning a valid integer anchor below — anchor lookup now PAGINATES (position-DESC, pages of 10, startAfter cursor) until the first valid integer anchor or exhaustion; no fixed window, malformed floats can never suppress valid anchors. Real data is integer-only (server + manual-pass write integers; Phase-0 found 0 floats) so expected cost = 1 page. eslint: 0 new errors. |
| 2026-07-05 | (milestone) PLAN_list_progress_persist Phase 1 | Codex diff review round 5: AUDIT-CLEAN, no remaining code-level blockers. Verified: sessionCleared dual messaging, cursor pagination correct under int/double interleaved ordering, passed-position gate matches the 7th index, index JSON parses, production build passes, lint = 18 pre-existing/0 new. Verdict: safe to deploy flag OFF; deploy all SEVEN indexes first; enable LIST_SCOPED_RECON only after indexes build + §9 persona validation passes. Implementation review trail: 5 rounds (4+3+2+0 findings → clean), all logged above. |
| 2026-07-05 | dsg-edits/srv_validate/LSR_AUDIT_PLAN.md + lsr_common.mjs + lsr_wave_a.mjs + lsr_wave_b.mjs (new) | Extensive Playwright audit suite for LIST_SCOPED_RECON Phase 1 (plan §9). Interaction contract: Playwright-only against the deployed Netlify site; Admin SDK restricted to read-verification + sandbox reset (25WT only, never 26SM). Wave A (runnable now, flag-OFF deploy): A0a functions `version` provenance (prod sha == HEAD), A0b Netlify bundle freshness (polls for Phase-1 markers), A1 login/dashboard, A2 full Day-1 UI flow w/ attempt+progress verification, A3 Day-2 new+review flow (exercises the edited Day-2+ gate legacy branch), A4 invariance sweep (zero list_progress docs, zero new-type system_logs, console errors). Wave B (BLOCKED — refuses without --flag-on-deployed): B1 move carry-forward, B2 dual-enroll no-ping-pong, B3 cross-pace position, B4 cross-class phase resume, B5 two-tab day-guard rejection UX; B6/B7 (orphan log-only, sparse anchor) marked admin-fixture, need sign-off. Reuses persona_runs.mjs selector conventions; class-switch selector flagged for first-run tuning. |
| 2026-07-05 | audit/playwright/batches/B_LIST_PROGRESS_PHASE1_UI.md + lsr_ui.mjs + lsr_snapshot.mjs + lsr_runL.mjs + lsr_runS.mjs (new); dsg-edits/srv_validate/lsr_* (REMOVED) | Playwright audit suite REBUILT compliant with docs/plans/PLAYWRIGHT_AUDIT_list_progress_persist_phase1.md (binding policy): UI-only interaction (semantic locators; NO page.evaluate/waitForFunction/injected JS/storage/interception/deep-links), Admin SDK = read-only pre/post snapshots ONLY (never resets/seeds, never concurrent with a browser), forward-only UI-prepared personas, un-manufacturable states reported "not UI-reproducible" (P-PAIR/P-ORPHAN/P-SPARSE natural-only). Moved out of dsg-edits (David: completely separate personal workflow — memory saved) into the audit/playwright home. Batch doc adds: MANDATORY FINDINGS PROTOCOL (any and all bugs filed, incl. out-of-scope, tagged; runners auto-append every anomaly — console errors, request failures, dead modals, flow gaps — to findings/<template-based file>; nothing dropped without written justification) + CS-LOG TRACEABILITY MATRIX (15 rows mapping every relevant/tangential SUPPORT_RUNBOOK+NEED_TO_FIX failure mode to cases: EXT-1 pass-displayed-as-fail assertion on every submission, EXT-2 save-failure watch, EXT-3 mid-test reload recovery, EXT-4 anchor-field completeness, EXT-5 impossible-session-state, EXT-6 zero orphan deletions, EXT-7 dead-modal watch, EXT-8 default-list flip watch; challenge/override + SUMMIT-fallback explicitly deferred with reasons). Run L implements L1/L2+EXT; Run S implements S1-S3/S5/S9-T/S9-M/S10 (S4/S6/S7/S8 natural-persona-gated), guarded by --flag-on-deployed. Prior non-compliant Wave A run was stopped before any browser action (bundle-polling phase). |
| 2026-07-05 | audit/playwright/lsr_provision.mjs (new, RUN), lsr_accounts.json, lsr_prep.mjs (new), batches doc | Fresh LSR audit fleet per David: (1) policy amendment recorded — Admin SDK MAY create accounts (pre-audit provisioning only); everything post-login is full-UI. (2) Provisioned 17 accounts (lsr_teacher_01 + lsr_s01..s16, @vocaboost.test), user docs byte-matching the signup shape (db.js createUserDocument defaults), idempotent, roster in lsr_accounts.json. (3) lsr_prep.mjs staged FULL-UI prep: --teacher (create 4 classes mirroring 26SM settings: LSR-A/B TYPED pace 80/100 thr 92 on "25WT2 TOP Vocabulary (v2)"; LSR-A/B MCQ pace 80/100 thr 92 on "25WT2 CORE Vocabulary (v2)"; record join codes), --joins (13 students join by code per persona roster), --days (forward-only histories: s03 2d, s04 3d, s05 1d, s13 3d A + 2d B = UI-built P-PAIR mixed history under the live flag-off build). Resumable state lsr_prep_state.json; selector gaps auto-filed as findings + screenshots for tuning. Old audit_* naturals (recovering/slowlaptop etc.) reserved untouched for P-ORPHAN/P-SPARSE. Note: teacher-UI-created classes may lack newWordRetakeThreshold (NTF#5 gap) — expected finding-in-waiting, careful personas score ~100% so cases stay deterministic. |
| 2026-07-05 | audit/playwright/lsr_clone_lists.mjs (new, RUN) + lsr_lists.json; lsr_prep.mjs + lsr_ui.mjs (fixes) | List provisioning per David ("duplicate the list in firebase admin sdk"): cloned the two private veterans-owned 25WT lists under lsr_teacher_01 — "LSR TOP Vocab (audit clone)" (EQ0Dc9rb, 3381 words verified) + "LSR CORE Vocab (audit clone)" (aDVcq3Mo, 3380 verified), verbatim word docs (wordmap coverage preserved), clonedFrom provenance field, idempotent. Prep-script fix round from stage-1 screenshots: create-modal submit = exact "Create Class" (loose /create/i hit the background button, backdrop ate the click); modal-close wait; goDashboard now dismisses modals (visible ✕/Cancel/Escape) + never throws; UI-level idempotency (skip create when the class card exists — one crashed run had created "25WT LSR-A TYPED" (HHn5UL34, code 6HSWTU) without recording state, verified no duplicate); assign flow rebuilt as guarded openClassDetail helper (opener→pick clone title→enabled-checked confirm; disabled = finding not crash). Root cause of assign failure was ownership: original lists private to veterans@ → picker empty → button disabled. |
| 2026-07-05 | audit/playwright/lsr_prep.mjs | Stage 1 COMPLETE — 4 LSR classes configured + verified: LSR-A/B TYPED (pace 80/100, typed, thr 92, tsN 30) + LSR-A/B MCQ (pace 80/100, mcq, thr 92, tsN 30), all sharing the TOP clone (EQ0Dc9rb) so each pair is same-list/different-pace for dual-class cases. Codes: TYPED-A 6HSWTU, TYPED-B Y9BJEM, MCQ-A UKY2HH, MCQ-B VQ38ZD. Selector lesson learned + baked in: settings/assign modals wrap each <select> in a <label> whose accessible name includes helper text, so getByLabel(/^test mode$/) fails while unanchored input matchers (pace/threshold/size) work — <select> fields (List/Test Mode/Review Mode) must be located by OPTION content (locator('select').filter({hasText})). Simplified design: all 4 classes share one list (no fragile unassign/reassign). FINDING F01 filed: teacher-UI-created classes store newWordRetakeThreshold:MISSING → live NTF#5 reproduction. Next: --joins (13 students by code) → --days (persona histories). |
| 2026-07-05 | audit/playwright/lsr_ui.mjs, lsr_prep.mjs, wordmap.json (rebuilt) | Scenario-engine realism + primitive fixes (David: switches/dual-enroll must happen mid-run). Replaced clean --joins/--days split with per-persona --scenarios timelines interleaving join/study/switch/abandon in CS-log order (study in A THEN join B mid-course — the actual bug precondition). Primitive fixes found via s05 smoke: joinClass = inline dashboard form (placeholder ABC123, not a modal) + codes backfilled from DB (read-only); switchClass = real FocusControl trigger+dropdown, label-mode aware; bareWord splits newline to strip "(old English)" annotations; wordmap REBUILT from the cloned lists' own definitions (3380 entries, covers the archaic words the stale file lacked → careful answers now 100%); completeOneDay hardened to require a REAL day advance (reject retake-gate/fail verdict, not just a results screen) + made mode-aware (typed inputs vs MCQ radios). s05 validated: csd=1/twi=80, 100% PASS, dual-enrolled. Sandbox study-data resets (provisioning cleanup, no browser open) used between smoke iterations. |
| 2026-07-05 | audit/playwright/lsr_ui.mjs | PREP selector-discovery round (UI-only against live app): (1) skipToTest helper = exact aria-label "Session menu" kebab → "Skip to Test" (SessionMenu.jsx:92) + confirm, replacing nondeterministic getByRole('button').last() that failed on day-2/MCQ layouts; (2) MCQ driver rewritten — MCQTest is one-question-per-screen with <button> choices (text=definition) that AUTO-ADVANCE (handleAnswerSelect:421), NOT radios: driveMcq reads the <h2> word, clicks the choice by exact definition text (answer key built from same definition field), bounded by the "N of M answered" label; submit button is "Submit Test (N/M answered)" (prefix match, not $-anchored); reached-detection now typed-inputs OR "Next question" nav button. Validated: s03 day-2 typed csd advances; s08 MCQ csd=1/twi=80 100% PASS. Both modes proven end-to-end. |
| 2026-07-05 | audit/playwright/SCENARIO_CATALOG_teacher_concurrent.md (new); lsr_accounts.json | Teacher-concurrent + adversarial scenario catalog per David (cover the CS-worst teacher-side moves interleaved with LIVE student sessions; go beyond CS). 30+ scenarios, 7 categories: TA (list add/unassign/swap/re-add/teacher-driven-dual), TS (pace/threshold/mode/size changes mid-session), TE (remove-student/delete-class/re-enroll mid-progress), XC (triple-overlap, cross-pace demotion guard, real §13 concurrent-completion race), MS (one teacher action hitting 3 simultaneous student sessions), AD (ping-pong, self-reset cross-class-nuke, two-tab, mid-test unassign), GB (2-teacher shared-truth gradebook). Multi-actor orchestration model (teacher + 1-3 student contexts interleaved, ‖ = concurrent). Priority-waved P1/P2/P3. Provisioned +14 students (s17-s30) + lsr_teacher_02 (32 accounts total). Each maps to a CS ticket or is marked novel; de-scoped-bug repros tagged [known-bug] → file findings. |
| 2026-07-05 | audit/playwright/LSR_AUDIT_RESULTS.md (new) + findings/*.md | SAVE-STATE of the LSR audit. 3 confirmed findings (F02 teacher list-add flips default list = 박시은 §7-H3; F03 teacher unassign strands mid-progress student = 박한별 nice-to-haves#1; F01 new classes lack newWordRetakeThreshold = live NTF#5) + 1 candidate (intermittent phantom membership on join, rules:57-60 warned race — unisolated). Built + validated: 32 accounts, 2 cloned lists, 7 classes (4 persona + 3 isolated teacher-wave), 13 student personas (interleaved realistic timelines), answer key from list defs (careful=100%), UI-only primitive library (login/join/switch/typed+MCQ drivers/skip-to-test/studyOneDay), teacher primitives (assign/unassign/editSettings/removeStudent/gradebook/createClass), multi-actor orchestration engine (teacher+N students concurrent, per-scenario reset, read-only snapshots). Teacher scenarios run: TA1/TA2/TE1. NOT done: Run L (flag-off regression — gates the flag; scripts+personas ready), Run S (needs owner flag-on deploy), teacher Waves T-2/T-3. Bottleneck: phantom-membership precondition flakiness. |
| 2026-07-05 | docs/patches/FIX_SPEC_F02_F03_list_focus_and_unassign_warn.md (new) | Codex-ready fix spec for the 2 audit findings. FIX 1 (F02): getPrimaryFocus fallback (Dashboard.jsx §2 ~L1037-1058) + pickPrimaryList (~L1083) must prefer a list the student has progress on (progressData[classId_listId].currentStudyDay>0) before newest-assigned; only newest-assigned when zero progress anywhere; saved-pref path unchanged; new-student behavior preserved; watch async progressData recompute flicker. FIX 2 (F03, warn-only per David): reword handleUnassignList confirm (ClassDetail.jsx ~L387) to honestly warn students lose access until re-assigned, optional N-mid-progress count from studentProgressMap; NEVER block. Both client-only, low/trivial risk, de-scoped from Phase 1 (§7-H3, nice-to-haves #1/#2/#3b). |
| 2026-07-05 | docs/patches/FIX_SPEC_F02_F03_list_focus_and_unassign_warn.md | Codex round-2 corrections. F02 readiness [HIGH]: progress fetch collapses no-doc and query-error both to null (Dashboard.jsx:652 vs 655) → value/null progressReady goes true after a failed query and can still flip the list; fix = per-key status(ok/error) tracking, progressReady requires all-ok, error→retry state (no auto-select/focus-controls/session-start). Persistence wording corrected (not auto-persisted; risk = render+user-action/class-selection-persist; gate render+start). Audit remediation tightened to strict read-only Admin (UI-created or pre-existing personas). Recency ranking + F03 warn/count confirmed correct by Codex. |
| 2026-07-05 | docs/patches/FIX_SPEC_F02_F03_list_focus_and_unassign_warn.md | Codex round-3: AUDIT-CLEAN / implementation-ready. Folded 2 impl notes: atomic per-key {status,data} map (avoid diverging React states re-opening the readiness window); normalize Firestore Timestamp|Date|null to ms before the ranking comparator. Spec review closed (3 Codex rounds: valid→3 corrections→1 blocker→clean). Fixes are client-only, independent of the Phase-1 flag, ship on their own schedule. |
| 2026-07-05 | src/pages/Dashboard.jsx | F02 fix (audit-clean spec). Atomic per-key progress state: progressEntries = {key:{status,data}} (single state, no divergence); progressData kept as a derived useMemo (key→data) so all ~15 consumers unchanged. Fetch (L648) records status ok/error (a FAILED query no longer indistinguishable from no-doc). New success-based progressReady useMemo (every expected classId_listId key status:'ok'; error→not-ready). getPrimaryFocus §2 + pickPrimaryList now PREFER a list with active progress (currentStudyDay>0), ranked by recency (lastSessionAt→csd→assignedAt→stable id), timestamps normalized to ms; only newest-assigned when zero progress (new-student behavior preserved); saved-pref path unchanged. Render gated on progressReady: firstPaintLoading includes pending-progress (skeleton); NEW progressHasError branch = fail-closed retry card (never guess/persist a wrong focus on error); focus controls require progressReady. Fixes 박시은/§7-H3 list-add flip. Lint: +1 problem = a pre-existing rules-of-hooks false-positive pattern (baseline 24/9 → 25/10); 0 new real errors. Backup dsg-edits/srv_validate/f02_f03_backups/. |
| 2026-07-05 | src/pages/ClassDetail.jsx | F03 fix (warn-only, per David): reworded handleUnassignList confirm to honestly warn students LOSE ACCESS until re-assigned (progress preserved but hidden); removed the false "already finished unaffected" implication; no count (Codex-recommended omit); no blocking. Fixes 박한별/nice-to-haves#1. |
| 2026-07-05 | src/pages/Dashboard.jsx | F02 Codex round-2 fixes. (1) derived progressData now includes ONLY status:'ok' keys — an errored key is absent, so `key in progressData` correctly means loaded-successfully: per-list Start (L1925 `disabled={!(key in progressData)}`) stays disabled on error + cards show loading instead of provisional zeros (ok+null preserved so no-progress students can start). (2) progressReady converted from useMemo → plain const (IIFE): it sits AFTER the `if(isTeacher) return` early-return at L778, so a hook there was a REAL conditional-hook violation (not a false positive as I'd claimed). Lint now EXACTLY at baseline: 24 problems / 9 rules-of-hooks (0 new). Production build passes (Codex). F03 unchanged/ready. |
| 2026-07-05 | src/pages/Dashboard.jsx | F02 Codex round-3 (final): complete the GLOBAL fail-closed. Per-list Start button disabled={!progressReady || !(key in progressData)} (was own-key only → a partial failure, real list errors + new list loads, funneled the student onto the new list = original F02 mode via side door). handleStartSession guarded `if(!progressReady) return` (defense in depth). progressReady/progressHasError computation moved to before handleStartSession + before the isTeacher early-return so it's in scope everywhere (still a plain const, not a hook). Lint exactly baseline 24/9 (0 new). |
| 2026-07-05 | audit/playwright/lsr_ui.mjs, lsr_orchestrate.mjs | Recovery-probe pattern (user directive): on a mid-scenario issue, record it as a first-class BUG, attempt escalating recovery (refresh → re-submit → browser-context relaunch+re-login), record whether each step recovered it, then CONTINUE the scenario. New reusable recoverProbe() in lsr_ui.mjs; joinClass rewired to use it (phantom-membership → bug + page-level recovery). Orchestrator: helpers now read a mutable ref.page holder so relaunchActor() can swap the page under all bound helpers; studentHelpers.join escalates to relaunch when page-level recovery fails; ctx.relaunch(role) exposed. TA2 extended to assert the F03 reworded confirm ("LOSE ACCESS…") vs old "progress is saved" and reframe stranding as EXPECTED under warn-only. Syntax-checked. NOT launched (per user). |
| 2026-07-05 | audit/playwright/LSR_AUDIT_PLAN.md (new) | Consolidated master plan for the comprehensive F02/F03-verification + regression audit. Ties together the 4 existing docs (policy, batch spec, scenario catalog, results) and adds: purpose, binding constraints, full setup/infra inventory, the recovery-probe protocol (§4), findings protocol, the 4-tier execution plan (Tier1 fix-verify TA1/TA2 → Tier2 blast-radius regression → Tier3 rest of teacher wave → Tier4 Run L) with acceptance criteria, and stated limitations (F02 error-path not UI-testable; admin-reset caveat; phantom-membership; Run S out of scope). Owner-review-before-launch. |
| 2026-07-05 | audit/playwright/lsr_ui.mjs, lsr_teacher.mjs, lsr_accept.mjs (new), lsr_provision.mjs, LSR_AUDIT_PLAN.md | REBUILD after Codex NO-GO (4 blockers). B1: new lsr_accept.mjs runs Admin-free at runtime, forward-only (teacher UI-creates fresh disposable class + assigns; fresh never-reused student joins+studies); read-only CSD/TWI = separate snapshot. B2: hard preconditions → INVALID not PASS; TA1 asserts exact TOP label + requires CORE to appear as an option (defeats the prior false-pass where a silently-failed assign read as "unchanged/good"); null/loading/retry=FAIL. B3: scenario-controlled dialogs (armDialog/lastDialog; default record+dismiss+flag, never silent-accept); TA2 tests full F03 contract (cancel-preserves / accept-strands / exact warning text). B4: nonzero exit on any FAIL/INVALID/error; acceptance matrix (PASS-only counts). Added robust readFocus/readFocusList/listSelectorOptions (sibling-of-prefix, no brittle xpath-parent) + teacher readJoinCode. Security: creds via LSR_AUDIT_PW/.lsr_secret.json (gitignored), removed hard-coded password from lsr_ui/provision + docs. Matrix: TA1(F02), TA2(F03), M1(zero→newest), M3(saved-pref wins), M5(single-class regression + Start enabled). Deferred: teacher-wave/reset/concurrency, Run L, Run S. NOT launched — pausing for Codex re-review per user. |
| 2026-07-05 | audit/playwright/lsr_{accept,ui,preflight(new),postverify(new)}.mjs, lsr_accounts.json, LSR_AUDIT_PLAN.md | Codex round-2 (7 findings, all fixed). #1 fresh-student guarantee was false (lsr_prep dirtied lsr_s*): new lsr_preflight.mjs (read-only) PROVES no saved focus/progress/attempts → lsr_clean_accounts.json; lsr_accept allocates ONLY from that allowlist. #2 active progress now proven: readVisibleProgress() asserts a visible persisted signal (max hero pct>0 / "Day N done" / streak) before assigning CORE, not just a results screen. #3 TA2 post-verify now exists: lsr_postverify.mjs (read-only) consumes lsr_accept_manifest.json (enriched w/ className+listId), resolves exact classId, asserts CSD/TWI, prints FINAL combined verdict + nonzero exit. #4 fatal browser anomalies (page-error/unexpected-dialog/scenario-error) now count toward exit + matrix. #5 need() takes a predicate and REALLY re-evaluates after reload (was a stale boolean). #6 F03 wording split into separate LOSE-ACCESS/re-assigned/PRESERVED/HIDDEN checks. #7 scrubbed lsr_accounts.json plaintext pw; fixed stale plan claims (Admin-free runtime; preflight-proven cleanliness); documented the 3-process launch sequence (preflight→accept→postverify). All syntax-clean. NOT launched — paused for Codex re-review. |
| 2026-07-05 | audit/playwright/lsr_{ui,accept,preflight,postverify}.mjs | Codex round-3 (6 findings, all fixed). #1 progress probe no longer accepts the always-rendered "0-day streak"; positive requires a list-specific signal — Words Introduced>0 (totalWordsIntroduced), or "DAY N" badge N>=2 (csd>=1, Dashboard.jsx:1562), or hero %>0. #2 preflight now also rejects accounts with enrolledClasses or session_states (lsr_prep leftovers that leave assigned lists affecting F02). #3 postverify BINDS artifacts: manifest.run===matrix.run, all 5 expected scenarios present, exactly one TA2 item, preflight timestamp bound; lsr_accept clears stale matrix/manifest at start (rmSync) + writes both with the run id + expected set. #4 postverify requires the exact authoritative class_progress/{classId}_{listId} doc (no list_progress fallback — a deleted doc FAILS; list_progress reported diagnostically). #5 lsr_accept enforces MAX_CLEAN_AGE_H=6 on the allowlist + records preflightGeneratedAt downstream. #6 non-allowlisted console errors now count as fatal anomalies (allowlist: ResizeObserver/favicon/analytics/web-vitals/firestore-listen-abort). All syntax-clean. NOT launched — paused for Codex re-review. |
| 2026-07-05 | audit/playwright/lsr_{ui,accept,preflight}.mjs | Codex round-4 (5 findings, all fixed). #1 [core] TA1 now proves F02's ACTUAL predicate: readVisibleProgress.positive = hero "DAY N">=2 ⇒ currentStudyDay>=1 (was accepting words/pct which can be >0 while csd=0); words/pct kept as diagnostics only; TA1 asserts .day>=2. #2 lsr_accept clears stale matrix/manifest BEFORE any prerequisite check (moved rmSync above the preflight existsSync/age validation) so an early exit can't leave an old passing artifact. #3 lsr_preflight rm's its prior allowlist at STARTUP and publishes the replacement only after every query succeeds (a mid-scan throw leaves NO stale clean list). #4 non-allowlisted request failures now fatal (allowlist: Firestore Listen/Write channel aborts + firestore host only). #5 removed the stale "pct>0 / done / streak" TA1 label. All syntax-clean. NOT launched — paused for Codex re-review. |
| 2026-07-05 | audit/playwright/lsr_accept.mjs, lsr_preflight.mjs | Codex round-5 (3 findings, all fixed). #1 [blocker] Firestore request allowlist was fail-open (host regex OR'd with channel regex → ANY firestore.googleapis.com failure allowlisted). Now a single order-independent predicate isFirestoreChannelAbort() requires ALL THREE: firestore host AND Listen|Write/channel AND ERR_ABORTED. Verified: real firestore 500 → fatal, host-alone → fatal, benign URL-first abort → allowed. #2 lsr_preflight now rmSync's the old allowlist BEFORE reading credentials / admin.initializeApp, so a missing/malformed key (init throws) can't leave a stale clean list. #3 console-error allowlist reused the same 3-condition predicate (old /ERR_ABORTED.*channel/ regex assumed reversed order and never matched). All syntax-clean. NOT launched — paused for Codex re-review. |
| 2026-07-05 | audit/playwright/lsr_accept.mjs | Codex round-6 (1 blocker, fixed). M3 now asserts the SAME faithful DAY>=2 precondition as TA1 (currentStudyDay>=1 via hero DAY badge) immediately after studyOneDay and BEFORE assigning/selecting CORE — otherwise M3 ("explicit saved preference beats currentStudyDay>0") could pass without TOP actually having csd>0, so the saved-pref-over-progress path wasn't truly exercised. Syntax-clean. NOT launched — paused for Codex re-review. |
| 2026-07-05 | audit/playwright/lsr_accept.mjs | LSR ACCEPTANCE RUN EXECUTED (run mr7p8wac). Result 4 PASS / 1 FAIL: TA1 PASS (F02 verified — focus stayed EXACTLY on TOP after teacher list-add, CORE present as option so condition exercised), M1/M3/M5 PASS (F02 matrix + regression), TA2 FAIL on the LAST check only. TA2 F03 checks ALL passed (honest warning text: "LOSE ACCESS…re-assigned…preserved but hidden"; cancel-preserves) + postverify confirmed CSD/TWI PRESERVED (CSD=1 TWI=80). The 1 FAIL = "student loses access after PROCEED" rechecked 3s after teacher unassign; read-only Admin confirmed TOP fully unassigned server-side (assignments:[]), so it was CLIENT-PROPAGATION LAG, not a regression. HARNESS FIX: TA2 proceed-check now polls (up to 6 reloads×3s) for the unassign to propagate to the student's client before asserting stranding (still fail-closed). Fixes verified; FINAL:FAIL was a verified timing artifact. |
| 2026-07-05 | audit/playwright/ (TA2 re-run) | TA2 RE-RUN with propagation-poll fix → PASS. "PROCEED: student loses access to TOP — after propagation poll" now CHECK-PASS (confirms the prior fail was client-propagation lag, not a regression). Full suite now 5/5: TA1(F02)✓ TA2(F03)✓ M1✓ M3✓ M5✓. F02 + F03 fixes VERIFIED live end-to-end via UI-only forward-only acceptance run + read-only progress verification. |
| 2026-07-05 | RESUME.md (rotated) | SAVE-STATE. Archived prior RESUME → docs/resume_archive/RESUME_2026-07-05.md; overwrote RESUME.md with current state: F02/F03 DONE+VERIFIED (5/5 acceptance run mr7p8wac), Phase 1 deployed flag OFF (not enabled), UI acceptance harness built+validated (3-process pipeline, 6 Codex rounds). Queued: Run L/S (gate flag enable), deferred teacher-wave (needs forward-only rewrite), F01 retakeThreshold code fix (re-confirmed open), #1b/#1c lockdown, grading Phase 2. Nothing committed (standing rule). |
| 2026-07-05 | audit/playwright/LSR_ACCEPTANCE_MATRIX.md, LSR_AUDIT_RESULTS.md, findings/lsr_postverify_output.txt | FORMAL CERTIFICATION (Codex closure). Clean single bound run mr7qbi3m: 5 PASS/0 FAIL/0 INVALID/0 anomalies (TA1 F02, TA2 F03, M1, M3, M5). lsr_postverify.mjs → FINAL: PASS (matrix.run===manifest.run===mr7qbi3m, all 5 present, 1 TA2 item, CSD=1/TWI=80 preserved) — output SAVED to findings/lsr_postverify_output.txt. Superseded the earlier stitched 5/5 (mr7p8wac 4/1 + TA2-rerun). Recorded observed unassign→lose-access propagation latency ≈12s as data (not dismissed as artifact). Relabeled F02/F03 findings CONFIRMED·HIGH → RESOLVED (verified by mr7qbi3m). Certifies F02/F03 only; Run L / Run S still outstanding. |
| 2026-07-05 | audit/playwright/LSR_AUDIT_RESULTS.md | Post-certification doc cleanups (Codex, non-blocking): (1) banner'd the earlier stitched mr7p8wac/TA2-rerun section as ⚠ HISTORICAL/SUPERSEDED (old "FINAL SUITE 5/5" was an informal stitch, not a certification). (2) reworded F03 heading — the RESOLVED issue was the misleadingly-reassuring CONFIRM copy; access loss after a confirmed unassign is INTENTIONAL (warn-only). (3) reframed the ≈12s propagation as "accepted eventual convergence under this audit" (no defined latency requirement) rather than "not a bug". (4) clarified "0 anomalies" = 0 FATAL only; raw selector-gaps are non-fatal recovered-by-fallback and don't invalidate checks. F02/F03 remain formally certified (run mr7qbi3m, FINAL:PASS). Next gate: Run L. |
| 2026-07-05 | audit/playwright/lsr_runL.mjs (rewrite), lsr_runL_verify.mjs (new), lsr_runL_personas.json (new), RUNL_CODEX_REVIEW.md (new) | Rebuilt Run L (flag-off regression) as a bound 3-process pipeline (--pre snapshot → Admin-free driver → --post verify), mirroring the acceptance harness. Removed the old backend /version pre-gate + broken bundleFresh abort (it checked a flag-gated marker that's DCE'd when flag OFF → aborted on a correct deploy); retargeted from stale audit_* cohort to lsr_*; bound --post to exact --pre by runId. First run L1_122152 exposed a FALSE-GREEN (verifier FINAL:PASS while only 2/4 personas studied — s13 collision + s01 no-op'd, invariants held trivially). FIXED verifier: binds runL_activity, requires allDriven + per-persona new-attempt delta>0; re-run --post now correctly FINAL:FAIL. OPEN: s13/s01 don't reliably study (done-today / phantom); day-number discrepancy. NOT certified — prepped RUNL_CODEX_REVIEW.md for adversarial review before any trusted re-run. |
| 2026-07-05 | audit/playwright/RUNL_DESIGN_SPEC.md (new) | Codex NO-GO on Run L harness was fundamental: I1/I7 don't discriminate the flag (Phase 1 writes NO list_progress + collapses NO class_progress — confirmed via code grep; those are later phases). Wrote a corrected Run L DESIGN SPEC grounded in the actual flag-gated code (anchor list-scope db.js:3250, cross-class same-day gate db.js:3054, CSD non-demote progressService:228, orphan cleanup log-only :200). Real discriminators: D1 cross-class anchor non-promotion on VIEW (replaces I7), D2 cross-class gate, D3 orphan-delete-is-flag-off. Cases L1-T/L1-M (exact CSD/TWI transition, typed+MCQ), L2 collision (switch/enter-leave, no study, no cross-promotion). Incorporates all 8 Codex points (exact bindings, anomaly-fail, per-doc invariants, wordsIntroduced, corrected log discriminators, build identity, A/B limitation). DESIGN-FIRST per lesson (implemented before understanding target); NOT reimplementing until Codex blesses the spec. |
| 2026-07-05 | audit/playwright/RUNL_DESIGN_SPEC.md (v2) | Codex round-2 on the design (4 blockers, all folded). (1) L1 review impossible on Day 1 (studyService:1296) → L1-T/L1-M are Day-1 NEW-WORD-only. (2) D1 needs the exact recon trigger: select class B+list → Start/Continue → reach session (fires getOrCreateClassProgress) → leave w/o submit → reload → assert B-local + A/B docs + attempt counts unchanged; fixture built+verified BEFORE --pre. (3) D2 dropped (list-scoped query fires only at Day-2+ review completion; no proven pure-UI path). (4) D3 dropped from gate (destructive, conflicts with no-review-deletion condition; orphan reserved for Run S) → separate legacy canary only. Corrections: flag "changes evidence selection + downstream class_progress/log writes, no list_progress storage" (not read-only); TWI from actual allocated range nwei-nwsi+1 not +pace; per-case activity binding (L1 requires attempts, L2 requires ZERO); scoped log deltas to run users; corrected flag-ON-only log set (drop csd_anchor_*); bound anomaly artifact + exact case set {L1-T,L1-M,L2}; screenshots + visible-action log; build identity. Clean core = L1-T, L1-M, L2/D1. Not reimplementing until Codex blesses. |
| 2026-07-05 | audit/playwright/RUNL_DESIGN_SPEC.md, RUNS_DESIGN_SPEC.md (new) | Folded CS-behavior review into the audit design (design-only, nothing implemented). Run L: added L1-R (retake fail→retry→pass) as a legacy-INTEGRITY case (honestly scoped — not a flag discriminator on single class; cross-class version is Run S). Run S: new design stub — flag-ON behavioral suite gated on owner flag-on deploy. 6 CS-derived overlays (S-1 partial-day switch=FLAGSHIP/day-reset fix; S-2 failed-then-passed cross-class; S-3 blank retake; S-4 rapid A↔B CORRECTED oracle [viewing may DISPLAY reconciled higher position — that's the fix, not a bug; only assert no-mutate/no-new-attempt/no-demote]; S-5 reload boundaries w/ grading-audit boundary kept clean; S-6 saved-focus F02×recon interaction). Principles: one behavior per persona; EXACT per-overlay oracle required before implementing; data-layer binding. Reset/epoch deferred to grading Phase 2. Exclusions documented. |
| 2026-07-05 | audit/playwright/RUNL_DESIGN_SPEC.md (v2, Codex round-3) | Folded 2 remaining false-green gaps + 4 corrections (design-only). GAP1 [L2 counterfactual]: --pre must prove flag-ON WOULD differ — A holds a valid WINNING anchor (passed new attempt, complete position-bearing anchor, day>=2), A/B same list, B empty for L, computed list-scoped result != B-local zero; else L2 is INVALID not pass (prevents trivial pass when A lacks a usable anchor). GAP2 [stricter "fresh"]: L1 personas require ZERO class_progress/session_states/attempts/study_states for {persona,L} — CSD=0 alone insufficient (old mastery changes allocation). Corrections: L1-T/M/R are regression SMOKES not flag discriminators (only L2 discriminates) — fixed the false "each differs under flag-ON" claim; precise anchor validity (wordsIntroduced positive int == nwei-nwsi+1 AND post-TWI delta == that); build identity bound consistently across pre/activity/post + attestation caveat; L2 must visibly assert B selected before entry AND saved focus still B after reload before reading values. Case set now {L1-T,L1-M,L1-R,L2}. Still NOT implementing until Codex blesses. |
| 2026-07-05 | audit/playwright/RUNL_DESIGN_SPEC.md | Codex round-4: final gap folded. L1-R now requires an INTERMEDIATE assertion between fail and retry (retake-required state present, success absent, Dashboard still Day 1 / ZERO words, screenshot) — the final CSD/TWI can't prove the failed attempt didn't TRANSIENTLY advance progress; only the mid-state check can. Also tightened: post-snapshot must contain EXACTLY ONE failed + EXACTLY ONE passed new-word attempt (not "≥1 failed"). Codex pre-committed to bless the Run-L design FOR IMPLEMENTATION with these added → design COMPLETE. Implementation pending (will get its own review). |
| 2026-07-05 | audit/playwright/lsr_runL_cases.json (new), lsr_runL_fixture.mjs (new), lsr_runL_verify.mjs (rewrite), lsr_runL.mjs (rewrite), RUNL_IMPL_REVIEW.md (new) | IMPLEMENTED the blessed Run L design (4-phase bound pipeline: fixture → verify --pre → driver → verify --post). Fixture builder (Admin-free) allocates 3 preflight-clean accounts to L1-T/M/R + pins lsr_s04 (day-2, valid anchors) as L2-A, UI-joins each. Verifier (read-only Admin) --pre validates strict-fresh (L1) + L2 counterfactual (A valid winning anchor, A&B same list, B fresh, position>0), --post asserts exact per-case oracles (CSD+1, TWI delta==wordsIntroduced, exactly-one-pass; L1-R exactly-one-fail+one-pass+intermediate; L2 zero-attempts+A-unchanged+B-local+focusStillB) + scoped log deltas + bound anomalies. Driver (Admin-free) runs the measured cases + writes bound anomaly artifact. All syntax-clean, imports resolve. NOT RUN — implementation review brief RUNL_IMPL_REVIEW.md prepped (7 self-disclosed concerns) for Codex before any run. |
| 2026-07-05 | audit/playwright/lsr_runL_verify.mjs (rewrite), lsr_runL.mjs, lsr_ui.mjs, lsr_runL_fixture.mjs | Run L impl-review round-1: all 11 Codex false-green paths fixed. #1 pre.valid gate + non-null-exact visible assertions; #3 class-specific attempt/progress via bound classId + mode verify; #4 new enterSessionOnly helper (L2 enter-and-leave, no study); #5 selectedB/focusStillB required true; #6 exact attempt-id equality + A-present + B twi=0; #7 winning list-wide anchor + studyDay>=2; #8 anomaly artifact required + BUG/flow-gap/selector-gap fatal; #9 log-ID delta (not timestamp); #10 strict-fresh = zero study_states; #11 LSR_BUILD_ID mandatory + bound across all artifacts; #12 exact enrollment+assignment+mode. Syntax-clean. NOT run — back to Codex for impl round-2. |
| 2026-07-05 | audit/playwright/lsr_runL_verify.mjs, lsr_runL.mjs, lsr_ui.mjs | Run L impl-review round-2: 6 more false-green paths + evidence contract fixed. #1 clear stale pre BEFORE admin init; #2 L2 binds+asserts Class:B AND List:L (before+after); #3 L2 progress list-specific (classId&&listId===LIST); #4 enterSessionOnly matches real "Start Session" button; #5 attempt snapshot +testType/+submittedAt, verify testType===mode + submittedAt-in-window + outcome==='results' + visibleWords===wordsIntroduced; #6 full artifact binding (runId on all 4, exact case-set equality, timestamp ordering, anomaly time-window) + FAIL-CLOSED anomaly model (every finding fatal except benign allowlist — catches modal-dead/prep-issue). Evidence: per-case before/after screenshots + F.step logs. Syntax-clean. NOT run — back to Codex impl round-3. |
| 2026-07-05 | audit/playwright/lsr_runL_verify.mjs, lsr_runL.mjs, lsr_ui.mjs, lsr_runL_fixture.mjs | Run L impl-review round-3: 4 gaps + 2 hardening. #1 L1 exact total-attempt count (T/M=1,R=2) + no passed:null + testType/window for EVERY attempt; #2 new selectList helper (explicit visible list selection) + EXACT normalized equality + data-layer saved-focus bind (primaryFocusListId/classId); #3 specific passed-card heading (typed "Completed Day N session"/MCQ "New Words Test Passed!") + Continue → passedHeadingSeen required; #4 shot returns bool, assert initial Day1/0words (fully test 1→2), require screenshot FILES exist. Hardening: fixture deletes stale artifact before init; validAnchor requires nwsi>=0 + positive-int studyDay. Syntax-clean. NOT run — Codex impl round-4. |
| 2026-07-05 | audit/playwright/lsr_runL.mjs, lsr_runL_verify.mjs, lsr_runL_fixture.mjs | Run L impl-review round-4: stale-screenshot BLOCKER + 4 corrections. Driver deletes this run's PNGs up front + snap() records captured shots to activity.shots; --post requires each shot both recorded AND on disk (no stale reuse). New L2_inSession screenshot (immediately after enterSessionOnly). Driver pre-flight guard: refuse unless local --pre valid:true + matching runId/buildId/exact case set (don't burn personas on accidental run). Fixture dynamic-imports lsr_ui AFTER stale delete (static import failure can't leave stale fixture). passedResults uses bounded waitFor (15s/8s) not instant isVisible. Syntax-clean. NOT run — Codex impl round-5. |
| 2026-07-05 | audit/playwright/lsr_runL_digest.mjs (new), lsr_runL.mjs, lsr_runL_verify.mjs, lsr_runL_fixture.mjs, lsr_provision.mjs | Run L impl-review round-5: persona IDENTITY binding (was only binding case names). New fixtureDigest() (deterministic sha256 over per-case id/email/role/mode/joinTarget/class/classB + runId/buildId/list); stamped in pre/activity/anomalies, --post requires all equal + recompute-equal from current fixture + per-case email/role/mode/class equality across fixture/pre/activity. So a fixture edited after --pre (diff account/class behind same case name) can't certify. Ordering: fixture delete moved before build-id check. Provisioned spares lsr_s31-36 → 9 clean (was 3, zero-margin). Digest smoke: deterministic + flips on identity edit. Syntax-clean. NOT run — Codex impl round-6. |
| 2026-07-05 | RESUME.md (rotated → RESUME_2026-07-05b.md) + digest full-sha256 | SAVE-STATE before Run L measured run. Run L harness BUILT + CODEX-BLESSED ("Clean review. GO") + fixture/--pre smoke GREEN. Applied Codex non-blocking hardening (full 64-hex sha256 digest, no truncation). 4-phase bound pipeline documented in RESUME; needs LSR_BUILD_ID + LSR_AUDIT_PW; 9 clean accounts. Next: full measured run (fixture+pre+driver+post) under real build id. |
| 2026-07-05 | audit/playwright/findings/runL_verdict_L_20260705_215054.json | Run L measured run EXECUTED (runId L_20260705_215054, build 6743b91) → FINAL: FAIL (12 problems), but FAILED-CLOSED correctly (no false PASS). NOT a flag-off regression: L1-M (MCQ) fully CERTIFIED equivalence (csd1/twi80, exact oracle pass). L1-T/L1-R (typed, class 25WT LSR-A TYPED) "test not reached" — dashboard rendered no studyable state (dayBefore=null, no Start button), class_progress empty. L2 cascaded from a session-EXIT unexpected native dialog (beforeunload confirm) + page.reload 30s timeout → B-local values undefined. ROOT CAUSE: harness/environment integration, NOT app behavior — (a) fixture+--pre smoke structurally can't catch driver-level breakage (needs a DRIVER smoke); (b) Run L reuses OLD persona classes (25WT LSR-A/B) w/ config drift vs the acceptance harness's fresh-per-scenario classes. FIX PATH: fresh teacher-created classes OR explicit list-select; arm beforeunload dialog on session exit; raise reload timeout. Back to fix→Codex→driver-smoke→re-run. |
| 2026-07-05 | audit/playwright/lsr_runL_fixture.mjs (rewrite), lsr_runL_verify.mjs, lsr_runL.mjs, lsr_ui.mjs, lsr_runL_driversmoke.mjs (new), NEED_TO_FIX.md | Run L post-run-1 revision (Codex revised diagnosis). Recorded APP DEFECT NEED_TO_FIX#7 (db.js:502 assignedLists []-truthy split-brain hides all lists). Fixture now CREATES fresh per-run classes via teacher UI (isolation) instead of reusing drift-prone persona classes; L2-A kept as historical data-layer anchor. --pre effective-assignment precondition replicates Dashboard's actual (buggy) assignedLists||keys semantics so split-brain is caught not masked (L1 + L2-B require effective; L2-A anchor-only exempt). New leaveSessionViaQuit helper (visible Quit→"Leave Study Session?"→Leave) replaces programmatic nav that left beforeunload active (caused L2 unexpected dialogs + reload timeout); --post requires leftViaQuit. New lsr_runL_driversmoke.mjs validates measured primitives on a sacrificial account (catches what fixture/--pre smoke can't). All 7 scripts syntax-clean. NOT run — back to Codex re-review, then driver-smoke, then rerun. |
| 2026-07-06 | audit/playwright/findings/runL_verdict_L_20260705_235509.json | Run L re-run (L_20260705_235509) → FINAL: FAIL (12 problems), FAILED-CLOSED. NO flag-off regression — all underlying data correct. Root causes: (1) OPERATIONAL (mine): fixture ran TWICE for one runId (interrupted foreground timeout + background chain) → duplicate-named classes + L1-T class double-assigned TOP+CORE → student's default focus studied CORE (aDVcq3) not TOP (EQ0Dc9) → verifier list-mismatch "no class_progress". Empty duplicate classes also created. (2) HARNESS: L1-R failed-typed-test shows a SCORED results screen → fillSubmitAndObserve classifies "results" not "retake-gate" → retakeSeen=false. (3) HARNESS: L2 entered OK (selectedB/L, entered all true) but "Leave Study Session?" modal did not appear on Quit → leftViaQuit=false → cascade (beforeunload dialog + reload 30s timeout). L1-M (MCQ) PASSED all oracles again (csd1/twi80). Fresh-class fix + enrollment fix confirmed working. Verifier fragility: classIdByName returns docs[0] (duplicate-name risk). NOT converged — needs: single clean fixture run, retake-gate detection fix, L2 Quit-modal fix. |
| 2026-07-06 | audit/playwright/lsr_ui.mjs, lsr_runL.mjs, lsr_runL_verify.mjs | Run L 4 driver fixes (per user "fix + one clean run"). (1) L1-R retakeSeen now = "Try Again" retake button visible (TypedTest.jsx:1358, canRetake) — a failed typed test shows a SCORED results screen so the outcome-string was 'results' not 'retake-gate'. (2) enterSessionOnly clicks through the "Start Studying" INTRO screen to reach the real session (which carries the "Quit session" control) — L2's exit needs it; prior stop-at-intro = no Quit → modal never appeared. (3) --pre singleList guard: fresh class + L2-B effective set must be EXACTLY [LIST] (catches the multi-list case where the student studied CORE not TOP). (4) classIdByName fails on DUPLICATE class names (ambiguous binding from a re-run fixture). Syntax-clean. |
| 2026-07-06 | audit/playwright/lsr_runL_verify.mjs + findings/runL_verdict_L_20260706_014108.json | Run L CERTIFIED — FINAL: PASS (runId L_20260706_014108, build 6743b91). 4/4: L1-T (typed Day-1), L1-M (MCQ Day-1), L1-R (fail→retry→pass), L2 (cross-class anchor NON-promotion). 0 anomalies. The 4 driver fixes worked (12 problems→2→0). Removed the redundant/out-of-scope user-doc saved-focus assertion (redundant with the passing VISIBLE focusStillB/focusListStillL; app UX persistence, not flag-gated behavior; also doesn't persist on single-list-class selection = possible minor UX gap, tracked separately). Certifies: deployed Phase-1 LIST_SCOPED_RECON code is behavior-NEUTRAL with the flag OFF (build attestation, not runtime proof). This is the GATE cleared before enabling the flag. Run S (flag-ON) still needs an owner flag-on deploy + 7 indexes. |
| 2026-07-06 | audit/playwright/RUNL_RESULTS.md (new) + RESUME.md (rotated → RESUME_2026-07-06.md) | SAVE-STATE: Run L CERTIFIED results doc written; RESUME updated to reflect flag-off gate cleared. |
| 2026-07-08 | docs/design/LIST_CYCLING_DESIGN.md (new) | Saved the list-cycling design discussion: 4 options (day×pace ❌ overshoots under intervention; twi%listSize wrap ❌ breaks greatest-nwei reconciliation; extend-list ✅ recommended — monotonic positions preserve all recon invariants; new-key-per-cycle ❌ self-inflicts the class-change reset). Key insight: reconciliation breaks whenever nwei repeats/regresses. Recommendation: extend list behind the scenes (data-only, no deploy), gated on a by-word-aggregation check (Blind Spots/gradebook/Full-PDF). |
| 2026-07-11 | docs/plans/loop/runs/plan.md (new), handoffs/claude_to_codex_runs_001.md (new), baton.json | **Build Run S (flag-ON behavioral validation suite) — v1 design + into the loop.** Filled the RUNS_DESIGN_SPEC.md stub's TODO oracles: 6 overlays (S-1 partial-day-switch FLAGSHIP, S-2 fail-then-pass, S-3 blank retake, S-4 rapid-switch view-only [corrected oracle], S-5 reload boundaries, S-6 saved-focus) each with an EXACT per-doc CSD/TWI+phase oracle derived from PLAN_list_progress_persist.md §5.1 (anchor by newWordEndIndex DESC/submittedAt DESC; twi=nwei+1; CSD=max(stored, reviewForAnchorDay?anchorDay:anchorDay-1); review paired to anchor's class + temporal lineage; completion-gate position-consistent). Harness mirrors the certified Run L 4-phase bound pipeline. Launched 3-agent audit (oracle-accuracy/anti-false-green/UI-inducibility) + set baton turnOwner=codex for the long-turn Codex review flow. Prerequisite now met (flag-ON shipped). Next: reconcile audit+Codex → v2 → implement lsr_runS*.mjs.
| 2026-07-11 | docs/plans/loop/runs/plan.md (v2), handoffs/claude_to_codex_runs_002.md, baton.json, NEED_TO_FIX.md | **Run S plan v2** — folded round-1 review (Codex r-runs-001 + 3-agent audit; all 4 independently caught the Day-1 flagship oracle error, verified). Pivoted S-1/S-2/S-3 to Day≥2 (recomputed oracles: twi=2pA, csd=1, phase REVIEW_STUDY); fixed §1 to include the anchorDay===1→csd=1 special case (the root error); added coverage v1 lacked — S-7 competing-anchor SELECTION + S-8 Day≥2 CSD-branch/non-demotion (so the suite can actually FAIL a broken reconciliation) + S-9 reset-resurrection (certify current behavior); dropped S-4 no-demotion tautology + made displayed-position exact; made S-3 cross-class; dropped S-5 boundary-4 (concurrency, out of scope); fixed S-6 unbuildable personas; §8 retire stale lsr_runS.mjs; pace A≠B + MCQ coverage. Documented reset-resurrection as a known flag-ON consequence (NEED_TO_FIX, LOW, revisit with epoch — David: document+defer; it's student-self-serve reset, coherent under student-owned progress). Flipped baton to codex round 2. Verified against code: progressService.js:155, studyService.js:105-119, db.js:2886, lsr_runS.mjs:79-83. |
| 2026-07-11 | docs/plans/loop/runs/plan.md (v3), handoffs/claude_to_codex_runs_003.md, baton.json, NEED_TO_FIX.md (#9) | **Run S plan v3 + filed a REAL shipped flag-ON bug.** Validated Codex r-runs-002's RS2-2 against code and ACCEPTED it as a genuine defect: cross-class review-completion gate uses expectedBase = sessionConfig.newWordStartIndex (post-pass TWI D·p) ≠ the passed attempt's day base (D-1)·p → gate misses A's pass → forced spurious new-word retake in the flagship partial-day-switch flow. Filed NEED_TO_FIX #9 (HIGH, fix: verify attempt.newWordEndIndex+1==currentTWI or pass the day base). Flag stays ON (net-better than flag-OFF day-reset). Run S designed as the regression test: S-1/S-3 assert CORRECT behavior, marked expected-RED until #9 ships + made gating (RS2-3). Fixed RS2-1 (S-1 Day-1 setup = new-completion only, no impossible review pass) + RS2-4 (S-4 field-specific compare, exclude updatedAt/logs). Flipped baton to codex round 3. Adjudicated per David's directive (validate→accept unless serious reason) rather than escalating decisions. |
| 2026-07-11 | docs/plans/loop/runs/plan.md (v4), handoffs/claude_to_codex_runs_004.md, baton.json, NEED_TO_FIX.md (#9) | **Run S plan v4** — validated + accepted Codex r-runs-003. RS3-1: a SECOND coupled bug — even with the #9 gate fix, recordSessionCompletion re-adds sessionConfig.newWordCount (progressService.js:462) so completing the cross-class B review double-advances twi 2pA→3pA (day skip); verified (studyService.js:1267-1269/1370-1379). Expanded NEED_TO_FIX #9 to both coupled modes (gate lookup + twi double-advance; fix must make a cross-class REVIEW resume carry newWordCount=0). Added TWI-invariance assertion to S-1/S-3 oracle (after B review: csd=2 AND twi STAYS 2pA, not 3pA — zero-new-attempts necessary-not-sufficient). RS3-2: added §6.1 result contract (expected-RED ≠ certification; 3-state verdict PASS/EXPECTED-RED/UNEXPECTED-RED; only all-PASS on gating set = FINAL PASS). Flipped baton to codex round 4. |
| 2026-07-11 | docs/plans/loop/runs/plan.md (v5), handoffs/claude_to_codex_runs_005.md, baton.json, NEED_TO_FIX.md (#9) | **Run S plan v5** — validated + accepted Codex r-runs-004 RS4-1/RS4-2. Verified a THIRD coupled facet of #9: review paired to anchor's class (getReviewForDay where classId==anchorClassId, db.js:3407-3416) while phase detection is list-scoped (db.js:3119-3128) → a review completed in non-anchor class B isn't seen by anchor class A → A_L stays csd=1 while B_L=csd=2 (divergence). Extended S-1 to re-enter A after B review and assert convergence (both A_L/B_L: csd=2, twi=2pA, no re-prompt). Expanded NEED_TO_FIX #9 to THREE coupled modes (gate lookup + twi double-advance + cross-class convergence) with acceptance: entry from either class resolves to the same completed-day state. Flipped baton to codex round 5. |
| 2026-07-11 | docs/plans/loop/runs/plan.md (v5 final), runs/CONVERGED.md (new), baton.json | **Run S design loop CONVERGED — Codex GO (round 5).** 5-round Claude⇄Codex loop (3-agent once on v1 + Codex long-turn r-runs-001..005) certified the Run S flag-ON validation design implementation-ready. Folded Codex's 4 GO-notes (executable oracle binds BOTH A_L+B_L convergence; A-after-B is a gating doc-layer assertion; pre-#9 output = EXPECTED-RED not FINAL PASS; retire stale lsr_runS.mjs). Closed baton (turnOwner=done). PRIMARY OUTCOME: the loop uncovered NEED_TO_FIX #9 — a live, HIGH, three-part coupled bug (gate lookup + TWI double-advance + cross-class convergence) in the flag-ON flagship cross-class-review flow, affecting the exact partial-day-switch cohort. Run S is its regression spec/acceptance test. Next: fix #9 (via loop) + implement lsr_runS*.mjs. |
| 2026-07-11 | docs/plans/loop/fix9/plan.md (new), handoffs/claude_to_codex_fix9_001.md, baton.json | **Draft #9 fix + into the loop.** Diagnosed the 3-part coupled bug's shared root: initializeDailySession builds the new-word allocation unconditionally from the reconciled twi (studyService.js:235/253) even for a REVIEW resume. Fix A: when phase===REVIEW_STUDY, set newWordCount=0 + newWordStartIndex=the day's passed-new attempt's base → fixes bug1 (gate expectedBase matches) + bug2 (wordsIntroduced=0, no twi double-advance). Fix B: getReviewForDay (db.js:3400-3418) drops classId==anchorClassId under the flag → list-scoped review pairing (keeps submittedAt>=anchor lineage; reuses existing index) → fixes bug3 (A/B converge). Both LIST_SCOPED_RECON-gated, no new index/migration. Launched 3-agent audit (correctness/regression/edge) + set baton turnOwner=codex for the long-turn review. Acceptance = Run S S-1/S-3. |
| 2026-07-11 | docs/plans/loop/fix9/plan.md (v2), handoffs/claude_to_codex_fix9_002.md, baton.json | **#9 fix design v2** — folded 3-agent audit + Codex r-fix9-001 (2 blockers, accepted). F9-2: Fix A now PRESERVES the anchor range (newWordEndIndex=dayNewPass.newWordEndIndex, not nwStart-1) so the B review attempt carries A's range p..2p-1 (verified attempts persist it, db.js:1229-1230). F9-1 (also found independently by the regression agent): Fix B no longer pairs on studyDay+time alone (re-introduced cross-pace mispairing) — now POSITION-consistent (getReviewForDay takes anchor position fields, streams candidates, client-filters for range==anchor range; no new index). Edge-agent BLOCKER folded: patch studyService.js:1269 wordsIntroduced read (newWordCount=0 falsy → falls through to newWords.length = review pool on reload) to treat explicit 0 as authoritative. Locked gating to flag-gated. Added PDF/debug consumers (getTodaysBatchForPDF/getCompleteBatchForPDF/getDebugSessionData) to regression checklist. Run S S-1/S-3 to also assert B review range==anchor range. Flipped baton to codex round 2. |
| 2026-07-11 | src/services/studyService.js, src/services/db.js, src/services/progressService.js | **FIX NEED_TO_FIX #9 (flag-ON cross-class review completion) — CODE.** Implements the loop-converged design (fix9/plan.md v2, Codex GO r-fix9-002 + 3-agent). **studyService.js:** (a) `initializeDailySession` — on `LIST_SCOPED_RECON && phase===REVIEW_STUDY`, set newWordCount=0 + newWordStartIndex/EndIndex = the day's passed-new anchor range (same passed-first/score-desc selection as determineStartingPhase); fixes spurious retake (gate expectedBase matches) + TWI double-advance (wordsIntroduced=0) + preserves the anchor range on the review attempt for Fix B; (b) `completeSessionFromTest` wordsIntroduced read → `Number.isFinite(newWordCount) ? newWordCount : newWords.length||0` (explicit 0 authoritative; fixes reload-to-test facet). **db.js:** `getReviewForDay` flag-on branch → position-consistent cross-class pairing: requires anchor position range in lineage; streams candidates (studentId,listId,sessionType=review,studyDay,submittedAt>=anchor; no classId), client-filters for newWordStartIndex/EndIndex==anchor range; found on match, none on exhaustion, query-error/P1-1 guard preserved; no new index. **progressService.js:** passes anchorNewWordStartIndex/EndIndex into getReviewForDay. All flag-gated (flag-off byte-identical → Run-L equivalence). node --check clean on all 3. Acceptance = Run S S-1/S-3. **NOT committed/deployed — owner reviews + deploys.** |
| 2026-07-12 | src/services/studyService.js, src/services/db.js, src/services/progressService.js | **IMPLEMENT NEED_TO_FIX #9 fix (owner go-ahead 2026-07-12).** Applied the loop-converged design (fix9/plan.md v2, Codex design-GO): studyService.js — (1a) initializeDailySession REVIEW_STUDY-resume override (nwCount=0 + nwStart/nwEnd=day's passed-new anchor range, flag-gated); (1b) completeSessionFromTest wordsIntroduced uses Number.isFinite (explicit 0 authoritative). db.js — getReviewForDay flag-on branch = position-consistent candidate stream (requires anchor range in lineage; paginate studentId/listId/review/studyDay/submittedAt>=anchor; client-filter newWordStartIndex/EndIndex==anchor; found on match, none on exhaustion; flag-off unchanged). progressService.js — pass anchorNewWordStartIndex/EndIndex into getReviewForDay. node --check clean. NEXT: 3-agent audit of the implementation → Codex code-review loop (verify+adjudicate each round). NOT committed/deployed. |
| 2026-07-12 | src/services/studyService.js, src/services/db.js | **#9 fix — code-review round-2 adjudication edits.** Verified all reviewer claims against code; made: (1) getReviewForDay orderBy asc→DESC — empirically confirmed the ASC+range query FAILED_PRECONDITION (no matching index) while DESC reuses the existing live (studentId,listId,sessionType,studyDay,submittedAt DESC) index — no new index (Codex F9CODE2-1 + my Admin-SDK verification); (2) wordsIntroduced explicit-0 now flag-gated behind LIST_SCOPED_RECON, exact legacy `||` expression preserved for flag-off (Codex F9CODE2-2 — restores Run-L byte-equivalence); (3) getReviewForDay MAX_PAGES cap now returns query-error 'candidate-scan-limit' (fail-closed → CSD preserved) instead of silent none (Codex F9CODE2-3); (4) initializeDailySession dayNewPass now anchors on the twi-defining attempt (newWordEndIndex===twi-1) so a cross-class same-studyDay pass can't stamp the wrong range on the review (edge agent #1); (5) legacy missing-newWordStartIndex derive → totalWordsIntroduced (non-negative) not pace-assumption (edge #4). Documented (no edit): legacy-null-position reviews → false-none mitigated by non-demoting CSD (adding studyDay-only fallback risks worse false-advance); guard-asymmetry can't-happen with current/legacy data. node --check clean. NOT committed/deployed. |
| 2026-07-12 | docs/plans/loop/fix9/CODE_CONVERGED.md (new), baton.json | **#9 fix implementation code-review CONVERGED — Codex GO (round 3).** Implementation-review loop closed: initial 3-agent audit (correctness/regression/edge) + Claude empirical index verification + Codex 3 rounds (NEEDS_FIXES→NEEDS_FIXES→GO). Code is review-clean: 3 files (studyService.js, db.js, progressService.js), all LIST_SCOPED_RECON-gated, flag-off byte-equivalent, no new index. NOT committed/deployed — awaiting owner diff-review + deploy, then Run S S-1/S-3 behavioral acceptance (per Codex deploy guardrails). |
| 2026-07-12 | docs/plans/loop/runslong/plan.md (new), handoffs/claude_to_codex_runslong_001.md, baton.json | **Design plan for Run S-Long (longitudinal multi-persona flag-ON stress audit).** Per David: extensive audit — N students × 10 CS-grounded personas × 16+ days × class/list reassignments, asserting end-state integrity (no day-reset/re-study/stuck/forgery). FOUNDATION-FIRST phasing (prove a reliable per-day drive primitive over 16 clean days before scaling). Personas grounded in real CS incidents (SUPPORT_RUNBOOK: partial-day-switch, class-reassign, cross-pace carry-forward, promotion-ladder, dual-enroll+blank, reload/quit, throttled, threshold-edge, support-intervened, +candidates). Design artifact only — NO code. Launched 3-agent audit (oracle-correctness / anti-false-green / UI-feasibility) + set baton turnOwner=codex for the design loop. Implementation gated on plan convergence + David's explicit go-ahead. |
| 2026-07-12 | docs/plans/loop/runslong/plan.md (v2), rounds/r01_agents_synthesis.md (new), handoffs/claude_to_codex_runslong_001.md | **Run S-Long plan v2** — folded the full 3-agent audit (12 findings: false-green traps incl. reassignment-never-asserts-fresh-B, self-healing masks transient resets, threshold-edge is a client bug, no bound manifest; per-day oracle wrong as +1/+pace; Phase-1-only reality → active-doc oracles) + David's 2 rulings: (1) read-only Firebase reads allowed freely per-day for this task but NOT substituting UI checks (UI = primary teeth, FB = exact corroboration, never write to advance); (2) diagnose the 'rebuild' screen in Phase 1, hard-stop the dead-end branch, surface if app-side. v2 adds: per-persona per-day expected deltas, fresh-B precondition, pinned pass-threshold + visible verdict, pinned persona-12 signature, bound persona×day manifest, invalid-anchor + getPrimaryFocus-footgun personas, explicit out-of-scope section, tiny-list/multi-list-teacher prereq for personas 5/12. Design artifact only — NO code. Handoff updated to v2; baton stays on Codex. Correctness agent verified the underlying #6/#9 FIXES are correct — the plan's issues were oracle-precision/feasibility, not the code. |
| 2026-07-12 | docs/plans/loop/runslong/plan.md (v3), handoffs/claude_to_codex_runslong_002.md, baton.json | **Run S-Long plan v3** — folded Codex r-runslong-001 (after catching + correcting a misrouted GO where Codex reviewed FIX_9 code instead of this plan). All 4 accepted: RSL-1 (blocker) per-day oracle now CHECKPOINT-based (partial-switch day: anchor step adds dayNewCount once, review step adds zero — the #9 assertion split correctly); RSL-2 (high) persona-8 rewritten to drive low REVIEW scores over ≥3 sessions (verified calculateInterventionLevel uses reviewScore, studyAlgorithm.js:71-80); RSL-3 (high) added §0.2 preflight gate — #9-dependent personas count only if Fix #9 deployed + Run S S-1/S-3 passed, else EXPECTED-RED; RSL-4 (med) persona-10 seed tightened to fixture-setup-only labeling. Flipped baton to codex round 2. Design only — no code. |
| 2026-07-12 | docs/plans/loop/runslong/plan.md (v4), handoffs/claude_to_codex_runslong_003.md, baton.json | **Run S-Long plan v4** — folded Codex r-runslong-002 (2 findings, both accepted; Codex confirmed all 4 round-1 fixes landed). RSL-5 (high): persona-8 review-score band PINNED at avg≈0.60 (0.30,0.75) → partial intervention, require 0<dayNewCount<pace; full-freeze split to a separate persona-8b (avoids overlap with persona-12's zero-new-word phantom-day path); matches studyAlgorithm.js:71-97 thresholds. RSL-6 (med): added distinct BLOCKED verdict state for unmet #9-deploy/Run-S prereqs (NOT coverage, NOT EXPECTED-RED which is now reserved for executed pinned signatures); 6 non-overlapping verdict states. Flipped baton to codex round 3. Design only — no code. Codex signaled "after these, implementation-ready." |
| 2026-07-12 | docs/plans/loop/runslong/CONVERGED.md (new), baton.json | **Run S-Long DESIGN CONVERGED — Codex GO (round 3).** Design loop closed: initial 3-agent audit (12 findings) + 2 owner rulings + Codex 3 rounds (NEEDS_FIXES→NEEDS_FIXES→GO, after catching a misrouted GO where Codex reviewed FIX_9 code). Design implementation-ready, Phase-1-first (bulletproof day-primitive + rebuild diagnosis; exit gate = 16 confirmed days for 1 student). 12 CS-grounded personas + 8b; UI-primary + Firebase-read-only oracles; per-persona checkpoint sequences; fresh-B precondition; bound manifest; 6 verdict states; #9-personas gated on deploy+S-1/S-3. NOT product-certified (needs implementation + live run). NO harness code until David's explicit go-ahead (standing rule). |
| 2026-07-12 | audit/playwright/lsr_runSL_phase1.mjs (new) | **Run S-Long Phase-1 harness (day-primitive + rebuild diagnosis) — IMPLEMENTED** (owner go-ahead 2026-07-12), per the converged design (runslong/plan.md v4). advanceOneDay: dashReady (reload-to-entry + accept beforeunload + verify class landing ×3) → driveNewWordsToTest→driveTest → (day≥2) continue→driveReviewToTest→driveTest; classifyRebuild ('clean'/'rebuild' recoverable/'rebuild-clear-failed' HARD-STOP); 16-day loop with bounded rebuild recovery (×3) + fail-closed per-day confirmation (UI readVisibleProgress words==expected twi PRIMARY + Firebase read-only csd/twi delta corroboration, NEVER written); records rebuild diagnosis. Exit gate = DAYS(16) confirmed-persisted days for one pristine student, single class. Sandbox only (lsr_s41 / fresh 25WT RUNSL P1 class). NOT smoke-tested yet — pending harness code review (3-agent + Codex). |
| 2026-07-12 | audit/playwright/lsr_runSL_phase1.mjs (v2), audit/playwright/lsr_ui.mjs (console listener) | **Run S-Long Phase-1 harness — code-review round-1 adjudication (Codex r1 + 3-agent, all verified against code).** Folded: UI now GATES (uiOk fatal, not soft — read on dashboard not results; asserts DAY badge==csd+1 AND Words==twi); exact attempt deltas + dup detection in persisted; assert test actually PASSED (non-'results' outcome = typed failure); fatal-findings gate the verdict (FATAL_KINDS); in-run PRISTINE baseline assert + INVALID verdict; DAYS bound into verdict (PASS only at 16; <16=ITERATION); LSR_BUILD_ID required; exact assignment-settings verify (pace/mode/threshold) + unique classId binding (else INVALID); RICH rebuild diagnosis packet (the app's own console.warn 'Duplicate day completion blocked: expected day X got Y' from progressService.js:444 — widened lsr_ui.mjs console listener to capture warnings + day-guard-warn finding; + precededByBeforeunloadAccept, lastDialog, fbAtRebuild, csd_twi_reconciled log count, screenshot); fbState bounded-retry + fbConfirm POLL (client write commits after read → no false-halt); wall-clock SL_MAX_MS bound; browser.close in finally. Lens A late add folded: day≥2 review via goDashboard→driveReviewToTest (drop stacked clicks); reduced Class-control wait. node --check clean. Handing v2 to Codex round 2. NOT smoke-tested yet. |
| 2026-07-12 | audit/playwright/lsr_runSL_phase1.mjs (v3) | **Run S-Long Phase-1 harness — code-review round-2 adjudication (Codex r2, all 4 accepted + verified).** SLP1r2-1: enforce ALL assignment settings exactly (list assigned + pace + testMode + passThreshold + testSizeNew; `?? null` not `|| {}`) → INVALID on any mismatch (was pace-only, could certify wrong mode). SLP1r2-2: FATAL_KINDS aligned to the kinds lsr_ui actually emits (console-error/flow-gap/selector-gap/modal-dead/login-failed/request-failed added; BUG9-retake removed — not emitted) + request-failed allowlist for Firestore channel ERR_ABORTED noise. SLP1r2-3 (false-HALT): advanceOneDay now STATE-AWARE — reads FB at entry, resumes from observed state (already-complete→count; new-persisted→drive review only; else drive new) instead of blindly re-driving new words on a rebuild retry; diagnoseRebuild records postState{newPersisted,reviewPersisted}. SLP1r2-4: rebuild packet stores the actual screenshot PATH + screenshotOk boolean (shot() returns a boolean). node --check clean. Handing v3 to Codex round 3. NOT smoke-tested yet. |
| 2026-07-12 | audit/playwright/lsr_runSL_phase1.mjs (v4) | **Run S-Long Phase-1 harness — code-review round-3 adjudication (Codex r3, both accepted + verified; r2's other 3 closed).** SLP1r3-1: added 'BUG' to FATAL_KINDS (verified lsr_ui emits it 8×: save-error/grading-failed/timeout/verdict-incoherence/recovery/MCQ — my earlier grep missed it, Codex correct). SLP1r3-2: replaced the fail-OPEN REQFAIL_ALLOW regex (alternation let a non-Firestore ERR_ABORTED through) with isAllowedRequestFailure() requiring ALL THREE: Firestore host + Listen/Write channel + ERR_ABORTED (fail-closed; unit-verified: non-firestore abort→false, real channel abort→true, channel non-abort→false). node --check clean. Handing v4 to Codex round 4. NOT smoke-tested yet. |
| 2026-07-12 | docs/plans/loop/baton.json | **Run S-Long Phase-1 harness code review CONVERGED — Codex GO (round 4).** 4-round harness review (Codex r1-4 + initial 3-agent). Findings 7→4→2→0. Caught before any run: 2 false-PASS paths (UI-soft-not-gating, wrong-mode certification), a false-HALT (blind rebuild re-drive), a fail-open request-failure allowlist, non-fatal BUG findings, missing attempt-delta/pristine-baseline asserts. Certification guards now in place (required BUILD_ID, unique class binding, exact assignment verify, pristine baseline, UI-primary gate, FB read-only poll + exact attempt deltas + dup fail, fatal-findings gate, state-aware rebuild recovery, rich rebuild diagnosis). Loop closed. NEXT: smoke-test (2 days), then the full 16-day run. |
| 2026-07-12 | NEED_TO_FIX.md (#10), audit/playwright/RUNS1_BUILD_LOG.md | **Filed NEED_TO_FIX #10 (flag-ON pre-completion reconciliation self-race) + corrected a wrong prior conclusion.** Codex root-caused Finding B; Claude VERIFIED against code (TypedTest.jsx:919 attempt-write → :979 getOrCreateClassProgress reconciles+writes advanced CSD → :1015 completeSessionFromTest stale-blocked by day-guard progressService.js:442; same in MCQTest.jsx:717 → typed+MCQ). My earlier "harness artifact" conclusion was WRONG — corrected in the build log (superseded, retained for record). 0 live-cohort occurrences (latent) but a real code-level self-race, deterministic in the fast audit driver. Adopting option (c): fresh-context/settle-before-navigate is a harness WORKAROUND (unblocks Run S-Long), NOT a resolution of #10 — the app bug is filed separately. NO app code changed (awaiting go-ahead). |
| 2026-07-12 | src/pages/TypedTest.jsx, src/pages/MCQTest.jsx | **NEED_TO_FIX #10 FIX A — IMPLEMENTED** (Codex design GO/CONVERGED-OK + David go-ahead 2026-07-12; `docs/plans/loop/fix10/plan.md` v3). Flag-gated the pre-completion snapshot read in the session-final completion block (inside `doWriteAndFinalize` for Typed): `LIST_SCOPED_RECON ? getClassProgress(...) : getOrCreateClassProgress(...).progress` — the non-reconciling pure read stops the reconcile-write that advanced CSD from the just-written attempt and made the immediately-following `completeSessionFromTest` stale-blocked by the day-guard → spurious "session refreshed" rebuild. On null (missing doc, near-impossible concurrent reset) SKIP the `progressSnapshot` persist and proceed to completion (updateClassProgress self-creates via setDoc) — avoids the swallowed `updateDoc`-throws-on-missing-doc that would skip completion. Added `?? null` guards on the 7 snapshot fields. Imported `getClassProgress` + `LIST_SCOPED_RECON` in both pages. DO-NOT-TOUCH the pre-attempt studyDay-fallback reads (TypedTest.jsx:823 / MCQTest.jsx:543). Fix B DEFERRED (unsafe as specced). No progressService.js change, no index/migration/flag change. Flag-off = byte-equivalent (Run L). Lint clean (0 new errors vs baseline). NOT yet reviewed — pending implementation code-review (3-agent + Codex on the diff). |
| 2026-07-12 | src/pages/TypedTest.jsx, src/pages/MCQTest.jsx (comment) | **NEED_TO_FIX #10 FIX A — code-review CONVERGED (ready for deploy).** Initial 3-agent fable impl audit: all clean (A correctness=CORRECT/race eliminated both Day-1 & Day-2+; B flag-off=SHIP/byte-equivalent for all app-written docs; C parity=byte-identical). Folded: tightened one code comment ("byte-equivalent"→"behavior-equivalent (Run L)") in both files; logged 2 out-of-scope observations to plan §9 (rebuild-after-external-reset=guard-as-designed; retake null-write/TOCTOU for Fix B follow-up). Codex FIX10_CODE round 1 = GO/CONVERGED-OK (no blocker/high/medium, no edits). No behavioral change from the audits. Lint 0-new vs baseline. `docs/plans/loop/fix10/` (CONVERGED.md, rounds/r02_impl_synthesis.md, codex_reviews/codex_review_fix10code_001.md). AWAITING OWNER DEPLOY. |
| 2026-07-12 | audit/playwright/lsr_fix10_overlay.mjs (new) | **#10 regression overlay harness — BUILT** (plan fix10 §8). 4-cell matrix {Day-1 new-final, Day-2+ review-final} × {typed→TypedTest, mcq→MCQTest}; each cell measures ONE session-final completion with DISCRIMINATING read-only FB asserts (recentSessions +1 entry day===N; exactly one new users/{uid}/sessions doc day===N; session_states phase==='complete'; ZERO new day_guard_rejected_session_cleared; ZERO new csd_twi_reconciled in the TIGHT window = final-attempt-write→completion-settle, opened AFTER the session-entry reach so entry reconciles are excluded per Codex R2). Modes: FIX10_EXPECT=green (post-fix, all cells green) | red (pre-fix, #10 signature N≥2). Reuses Phase-1 fixture/FB machinery (BUILD_ID required, per-class exact assignment verify, per-cell pristine baseline, fatal-findings gate, settle-before-navigate). Negative control gated (FIX10_NEGCTL) + not-yet-implemented (two-context stale replay / Firebase-seed needs owner OK). node --check clean. NOT yet reviewed — pending 3-agent (fable) + Codex. |
| 2026-07-12 | audit/playwright/lsr_fix10_overlay.mjs (v2) | **#10 overlay harness — round-1 review adjudication (Codex NEEDS_FIXES + 3-agent fable, ALL verified vs code).** Lens A confirmed false-GREEN is structurally impossible (guard-reject path can't satisfy d2/d3). Folded: POLL-UNTIL-STABLE reads (`stableRead`) for both `before` (drains async session-entry recon log — progressService.js:253 fire-and-forget) and `after` (sessions doc is a separate batch — studyService.js:659-672) → kills the stale-read false-RED (A1/F10O-2/B5/my-catch); page-identity assert typed-XOR-mcq==cell.mode→INVALID (C1); verify final PASS via fail-text→NOT-MEASURED not fix-FAIL (C3/B4); precededByAccept recorded+consulted, RED excludes reload-confounded rebuilds + rebuild packet (B3); driveNewPass RESUMES on rebuild so red Day-2 reachable (F10O-1/A2/B6); fatal gate before BOTH verdict branches (F10O-3/B1); RED single-run=CANDIDATE, CONFIRMED needs 2nd consecutive run same BUILD_ID via RED_STATE file (C2/B2/A5); study_states LIST.id freshness in baseline + invalid→INVALID (F10O-4/B7); attempt_day_fallback exonerates d6 miss (A3); clear-FAILED variant in redSignature (A4); recon-precedes-guard timestamp ordering via logCountTs (C7); unified exit codes + fatals-surfaced + negControl unconditional (B8/B9/C4); runbook env knobs + flag-ON red precondition + wordmap dependency (C8); d7 progressSnapshot diagnostic (C5, non-gating). DEFERRED: negative-control impl (surfaced+TODO, needs owner OK for FB seed); shared 'results' matcher tighten (neutralized locally). node --check clean, 523 lines. Handing v2 to Codex round 2. |
| 2026-07-12 | audit/playwright/lsr_fix10_overlay.mjs (v2 final) | **#10 overlay harness — code review CONVERGED (Codex GO/CONVERGED-OK round 2).** All 4 Codex r1 findings + all 3 fable lenses resolved; all 3 deferred items ratified (defer negControl impl + shared results-matcher tighten; keep d7 as diagnostic). Folded 3 non-blocking Codex r2 notes as polish (no design change): comment/code match on redCells (setup rebuild = diagnosis only), guardFailed+fallback added to after-read stable key, red_state keyed by buildId+harnessRev+cellSet (stale candidate can't confirm across harness edits). node --check clean, 531 lines. Loop CLOSED. Harness READY: green mode after deploy (proves the fix — EXPECTED-RED→GREEN); red mode vs a pre-fix flag-ON build (2 consecutive runs → REPRO-CONFIRMED). NEXT (post-deploy): run overlay green + the Phase-1 harness workaround + Run S-Long together. |
| 2026-07-12 | audit/playwright/lsr_fix10_overlay.mjs (v3), audit/playwright/FIX10_OVERLAY_BUILD_LOG.md (new) | **#10 overlay Run 1 (live, build a967f54) + v3 driver iteration.** Run 1: harness fail-closed correctly — 1/4 measured, MEASURED CELL (MD1 MCQ Day-1) GREEN all 6 discriminators + d7 snapshot diagnostic {day:1,csd:0} CONFIRMS Fix A's pre-completion snapshot. Other 3 cells blocked by DRIVER issues (not the fix): TD1 typed unsettled (grading latency>25s), TD2/MD2 Day-2 setup couldn't reach test (nav flakiness), spurious teacher-assign selector-gap. All logged to FIX10_OVERLAY_BUILD_LOG.md (new curated cross-run issue log per David). v3 (owner-approved driver tweak, no full re-audit): SETTLE_MS 25s→90s (typed grading), separate 25s stableRead budget; bounded reachTest retry (reload+re-drive) around setup+measure; flow-gap/selector-gap removed from FATAL (RECORDED not verdict-gating — they only cause NOT-MEASURED/INCOMPLETE, never false-GREEN; fail-closed invariant self-verified: PASS still requires all-4-green + the 6 discriminators + bindAndVerify + finalFailVisible + measured==4). node --check clean. Re-running green. |
| 2026-07-12 | audit/playwright/lsr_provision.mjs | **Sandbox account roster made env-configurable (`LSR_ACCOUNT_COUNT`) + provisioned lsr_s47-s76.** Overlay Run 2 came back INVALID (accounts s43-s46 dirtied by Run 1 → studyStates for TOP list → non-pristine → the F10O-4 check correctly refused to measure; fail-closed working). Only 9 pristine remained; each run burns 4. David's decision: PROVISION fresh accounts (never reset — respects read-only-Firebase rule; keep dirty accounts as records). Provisioned 30 new (s47-s76, idempotent, existing untouched) → pristine pool ~39. Saved to memory `sandbox-account-provisioning`. |
| 2026-07-12 | docs/plans/loop/fix10/OVERLAY_RESULTS.md (new), audit/playwright/lsr_fix10_overlay.mjs (v5), audit/playwright/lsr_teacher.mjs | **NEED_TO_FIX #10 — VALIDATED + COMPLETE (joint Codex+Claude adjudication FINALIZE-3/4).** Live overlay vs deployed a967f54 reached 3/4 GREEN across driver iterations v2→v5: TD1 (TypedTest new-final), MD1 (MCQTest new-final), MD2 (MCQTest review-final) — all 6 discriminators + snapshot diagnostic. TD2 (TypedTest review-final) unmeasured = typed-review nav flake (fail-closed, never false-pass); redundant coverage (shared completion block, proven by TD1+MD2). v4: driveNewPass confirms via new-attempt (day-2 new ≠ session-final). v5: assignList gains optional reviewMode → typed class review=typed (reviewTestType is a separate app setting defaulting mcq). Codex traced TypedTest.jsx completion block: no review-final-only #10 path; Claude verified (:817 review-always-passes, only :823/:985 getOrCreate calls). #10 DONE: designed→code-reviewed→deployed→validated. NEXT: Run S. |
| 2026-07-12 | audit/playwright/lsr_ui.mjs | **Reach helpers made DETERMINISTIC + INSTRUMENTED (David's design).** Run S-1 FAILED on Day-2 reach flakiness ("Session-menu button not visible" at the 8s cliff → test not reached; Day 1 was CLEAN outcome=results, so #10 fix working). Root cause = fixed-timeout give-up racing variable live-app render/network. Fix: new `waitVisibleTimed(locator, {label,what,findings,timeout,slowMs})` waits a generous ceiling (deterministic — fails only if the control TRULY never appears) AND records appearance latency to `findings/reach_latency.tsv` + emits a `perf-slow` finding for any control taking >=3000ms (candidate STUDENT-FACING lag — surfaced, not dismissed). Wired into skipToTest (Session menu, 30s), driveNewWordsToTest (Start, 20s) + driveReviewToTest (Review, 20s); test-page reach probes 15s→30s. Backward-compatible (helps overlay/Phase-1/S too). Answers "is slow button appearance itself an issue?" with real data on re-run. node --check clean. |
| 2026-07-12 | audit/playwright/lsr_ui.mjs, lsr_runS1.mjs, lsr_runSL_phase1.mjs, lsr_fix10_overlay.mjs | **Day-2 "reach" root cause FIXED (harness) — clear stale COMPLETE session_state between days.** Investigation (David's "get visual confirmation first"): the "Day-2 reach flakiness" was NOT flakiness — the robot deterministically landed on a "DAY 2 COMPLETE" screen (identical screenshot every run; Session-menu absent at 30s while other buttons render in 4-5ms). Root cause: after a completion, session_states stays phase=complete and is cleared ONLY by the completion screen's "Back to Dashboard" button (DailySessionFlow.jsx:1787); harnesses returned via reload/header-nav, never clicking it → next-day entry hits the re-entry guard (:751) → "Day N Complete" wall. Real students unaffected (they click the button). Fix: new shared `clearCompletionIfPresent(page)` (clicks "Back to Dashboard" if present) folded into goDashboard + called before the reload in all 3 dashReady/freshDashboard helpers. node --check clean. Handing to Codex (RUNS_DAY2FIX). Minor real-app note logged (mislabeled re-entry complete screen if student abandons without clicking Back to Dashboard; low severity). |
| 2026-07-12 | audit/playwright/lsr_ui.mjs, lsr_runS1.mjs, lsr_runSL_phase1.mjs, lsr_fix10_overlay.mjs | **Day-2 reach fix CONVERGED (Codex GO round 3 + FB validation).** Root cause (loop w/ Codex): harnesses never triggered the session_state clear because after a final test they sit on the test-RESULTS screen; the clear lives past the results "Continue" (→ CompletePhase → Back to Dashboard / Move On to Next Day). Fix: `returnFromResultsAndClearCompletion` (click results Continue if on /typedtest|/mcqtest route → wait for CompletePhase → clearCompletionIfPresent[Move On modal first, then context-gated Back to Dashboard, real-click-only]), wired after the FINAL driveTest in all 3 multi-day harnesses (day-2-new correctly NOT cleared → stays review-study). VALIDATED: S-1 r4 Day 2 reached + session_states cleared to review-study + both attempts persist; S-Long 4-day smoke 4/4 confirmed days rebuilds=0 (validates modal path). Loop rounds: Codex D2F-1(modal)/D2F-2(context) r1 → Claude validation caught clear-not-firing → Codex D2F-3(results-return) r2 → implement+validate → Codex GO r3. NEXT (separate): S-1 'Review B not reached' (#9 cross-class review reach). |
| 2026-07-12 | audit/playwright/lsr_ui.mjs, lsr_runS1.mjs | **Run S-1 (#9 cross-class review acceptance) — PASS (validated).** Investigation (visual+FB) found "Review B not reached" was a HARNESS bug, NOT #9: entering class B correctly serves the cross-class Day-2 review (Phase-1 reconciles at session entry; B's dashboard shows class-keyed Day-1 until entry). Fix (Codex loop RUNS_REVIEWB, GO r3): RB-1 enter-then-review (enterSessionOnly[=enterSessionAny] + S-1 Step-3 reworked dashReady→enter→observe→skipToTest→driveTest); RB-2 fail-closed verdict gate (bReviewDriven + aReentryClean, require A re-entry; demote brittle reviewStudy text BUG9→note, data oracle bRange_ok gates); Step-4 (re-enter A) reworked to ENTER (triggers reconcile→csd=2) then observe. Also fixed verdict-hygiene: benign `request-failed` (Firestore Listen/Write channel ERR_ABORTED) no longer gates (isAllowedReqFail allowlist + anchored fatal kinds — the bare /fail/ was matching the substring in "request-failed"). VALIDATED: S-1 r7 (s63) PASS — oracle all green (A/B csd=2/twi=40, B review attempt range 20..39, no double-advance), all UI paths clean (no retake, no re-review). #9 cross-class review confirmed WORKING end-to-end. Known separate flake: typed day-2 new test grading borderline on words 20-39 (r5 failed, r6/r7 passed) — wordmap-coverage reliability, not #9. |
| 2026-07-12 | audit/playwright/lsr_ui.mjs | **Typed-test all-blank read-race FIXED (Codex GO, RUNS_TYPEDFILL).** Investigation (per David) of the "day-2 grading flake": inspected r5 failed attempt — score=0, ALL 20 answers BLANK, but all 20 words ARE in the wordmap (r6/r7 filled them). NOT wordmap coverage. Root cause: `readTestRows` reads each word from a sibling span.font-medium right after the inputs are visible, but the spans render a beat later → a race occasionally reads all-empty words → carefulAnswers fills blanks → empty submission → score 0. Fix: retry-until-populated (re-read until >=90% spans have text, or 8×1.2s deadline) + warn on under-population. Doesn't mask real gaps (a missing word still returns empty→wrong). Backward-compatible (same shape). Corrected my earlier wrong "wordmap-coverage" label. Codex GO. Validating via S-Long 4-day (7 typed fills). |
| 2026-07-12 | audit/playwright/lsr_runSL_phase1.mjs | **S-Long verdict hygiene: drop flow-gap/selector-gap from FATAL_KINDS (matches overlay's Codex-approved change).** v3 4-day run confirmed the typed-fill fix (4/4 confirmed days, 7 fills, 0 rebuilds) but FAILed only on the spurious teacher-assign selector-gap. Removed flow-gap/selector-gap from FATAL — a real reach failure makes a day fail its strict per-day confirmation (UI words==twi AND day==csd+1; FB exact deltas) → not confirmed → INCOMPLETE/halt (fail-closed); the fixture assignment is authoritatively verified (INVALID on mismatch). Fail-closed self-verified: PASS still requires all DAYS confirmed. Re-eval of v3 under fixed gate → 0 fatal → clean. |
| 2026-07-12 | docs/plans/loop/runslong/persona_expansion.md (v3), baton.json | **PERSONAX_DESIGN loop CLOSED — Codex round-3 GO / CONVERGED-OK.** Folded r2 (PX2-1 L14 full-freeze→EXPECTED-BLOCKED + stuck-state flagged as NEED_TO_FIX candidate; PX2-2 universal oracle split green paceEff>0 vs blocked paceEff==0; PX2-3 evidence path). Header bumped v2→v3 CONVERGED. Design implementation-ready pending David's 3 §9 scope decisions (#10b invalid-anchor, #6 pure same-pace, L4 45-day/truncate-Summit). Foundation→cycling deferred to next program (Option A). No app source touched. |
| 2026-07-12 | audit/playwright/lsr_provision.mjs, lsr_clone_lists.mjs, lsr_lists.json, lsr_accounts.json | **Persona expansion Phase-A infra (audit-only, no app source).** Provisioned lsr_teacher_02..08 (LSR_TEACHER_COUNT env, 8 total) preserving the 76-student roster. Extended lsr_clone_lists.mjs from single-teacher to teachers×CLONES: 3 tier lists (Base Camp 1200 / Ascent 1600 / Summit 800) cloned+verified under ALL 8 teachers (21 fresh clones); TOP/CORE legacy stay teacher_01-only. lsr_lists.json now carries .teachers per-teacher map (persona segment runner) while .lists stays teacher_01 back-compat (.lists[0]=TOP → existing runS1/fix10/SL runners unbroken). |
| 2026-07-12 | audit/playwright/lsr_build_wordmaps.mjs (new), lsr_tier_wordmaps.json | **Persona expansion — per-tier wordmaps (PX-6 data gate).** Built Base Camp/Ascent/Summit wordmaps from each word doc's `definition`, keyed norm(bareWord(word)) to match the harness lookup. Data gate PASS: 1200/1600/800 = 100% non-blank, 0 key collisions. Live grader-PASS half of PX-6 (does a verbatim ①②-multi-sense definition score PASS?) deferred to the segment-runner smoke as its first hard assertion before the full fleet. Audit-only, no app source. |
| 2026-07-12 | audit/playwright/lsr_persona.mjs (new), lsr_personas.mjs (new), lsr_ui.mjs (helpers) | **Persona expansion — segment runner + 16-persona catalog (audit-only, no app source).** NEW lsr_personas.mjs: L1-L16 as ordered segment specs (caps DERIVED from tier size/pace: base@80=15d, ascent@80=20d/@100=16d, summit@80=10d/@100=8d) + event ledger. NEW lsr_persona.mjs: segment state machine (a rewrite per PX-7, not param swap) — fully parameterized fbState(classId,listId), split oracle (green paceEff>0 vs EXPECTED-BLOCKED paceEff==0), T1/T2/T3/same-pace-move transitions, #5 progress-preservation assert, per-day checkpoint+resume, per-persona SL_MAX_MS. lsr_ui.mjs += enterReviewSession (hardened §4.9), carefulAnswersFrom/partialAnswers (tier-map + blank-based §4.8) — additive, legacy runners untouched. FULLY impl: steady/phantom/freeze/retake + transitions; L12 dynamic-throttle, L10 cross-class #9, L11 2nd-list, L15 bad-anchor throw NOT_YET_HARDENED (Codex-loop pass) so a fleet run can't half-audit them. Next: Codex loop on the engine → smoke → grader gate → 8-way fleet. |
| 2026-07-12 | audit/playwright/lsr_persona.mjs | **Persona runner — Codex harness r1 fixes (fail-closed evidence).** PH-1: EXPECTED-BLOCKED days now REQUIRE an affirmative block signature (retake-gate outcome / visible retake-required / orphaned_attempt_flagged delta) — frozen counters alone no longer certify (blocker). PH-2: carry transitions (T2/same-pace) now FORCE reconciliation via enterSessionOnly+leaveSessionViaQuit BEFORE reading baseline (recon is on session-entry, not class-join) (blocker). PH-3: fail-closed per-transition baseline contracts (T2 exact carry; fresh/T1/T3 pristine 0/0/0/0; L16 RECORDS #6 baseline — reasoned divergence). PH-4: progress-preservation asserts csd+twi (+classId/listId). PH-5: retake blank attempt must hit retake-gate. PH-6: widened FATAL_KINDS (+console/page/dialog) + PASS-WITH-WARNINGS surfaces flow/selector gaps. Oracle regression-clean. Audit-only. |
| 2026-07-12 | audit/playwright/lsr_persona.mjs, baton.json | **PERSONAX_HARNESS loop CLOSED — Codex r2 GO/CONVERGED-OK for smoke.** All 6 fail-closed fixes accepted (incl. L16 record-not-assert). + fixed freeze driver (L14 review-fill now ≤0.30 to actually trigger interv→1.0, else the day-5 EXPECTED-BLOCKED oracle would false-fail). Caveats folded: exit-2 non-certifying, L16 observation-only, L10/L11/L12/L15 NOT_YET_HARDENED gate full-fleet cert. Next: live smoke then 8-way fleet. Audit-only. |
| 2026-07-12 | audit/playwright/lsr_teacher.mjs, lsr_persona.mjs (smoke fixes) | **Persona smoke — L2 PASS + 3 live-run harness fixes.** L2 (steady, Ascent, 2d) = PASS 2/2, 0 warn/fatal → **grader gate RETIRED**: typed answers from the ①② multi-sense tier wordmap score PASS on the live AI grader (PX-6); day1 twi=80/csd=1, day2 twi=160/csd=2/rev=1 oracle-exact. Fixes from live runs (all fail-closed, none mis-audited): (1) assignList now takes explicit listId (LIST_ID map only knew teacher_01's TOP/CORE); (2) list-select waits for the target option to attach (async Firestore load raced a fixed sleep) + fail-closed abort if select fails (was confirming the DEFAULT list); (3) force reviewMode=typed (UI defaults review to MCQ; driveTierTest is typed-only). Launched L8/L14/S_T2 smokes in parallel. Audit-only. |
| 2026-07-12 | audit/playwright/lsr_persona.mjs, lsr_ui.mjs (wave-1 fixes A/B/C) | **Persona runner — smoke wave-1 fixes (harness-only, Codex r3 directions).** A: blocked-day review now full-answer fill (was blank → Submit disabled). C: returnFromResultsAndClearCompletion WAITS for the results Continue (waitVisibleTimed, was check-once) + returns status; advanceOneDay VERIFIES finalization (pollAdvanced csd/twi advanced) before ok:true → a finalization miss is a RETRYABLE driving failure (self-heals the cascade), not a silent pass. B: dashReady(page,className,listTitle) ENFORCES active class AND list (selectList + verify both labels + 4x retry + fail-closed), lenient on unread list labels; threaded through all call sites + advanceOneDay signature (+classId/listId). Diagnostic dbgFail screenshots kept. Legacy runner compat preserved. Handing patched code to Codex r4. |
| 2026-07-12 | audit/playwright/lsr_persona.mjs, lsr_ui.mjs, lsr_build_wordmaps.mjs | **Persona harness — ROOT CAUSE fixed + trim (Codex r5 GO'd directions).** ROOT: harness answered verbatim ENGLISH; AI grader rejects it as word-for-word (~50% fail); Korean was in definitions.ko (missed). Fix: wordmaps rebuilt w/ Korean, carefulAnswersFrom prefers ko. TRIM: **B (dashReady list-enforcement) CUT** — A/B test clean 6/6 x2 with it off (non-load-bearing, red herring, had caused a regression). KEPT A (blocked full-fill, load-bearing), C (wait-Continue+finalization poll), D (blank-guard). L14 day-6 consecutive-blocked-submit classified via screenshot as a HARNESS gap (Resume-Previous-Test modal intercepting Submit) NOT the product stuck-state → added dismissResumeModal. Recovered-focus warnings downgraded (not cert-blocking). Empirical GREEN: L2x2, L8x2 (B off) 6/6, S_T2 carry, L14 6/6 incl. 2 consecutive blocked days. Ready for 12-persona fleet. |
| 2026-07-12 | audit/playwright/findings/fleet_manifest_fleet.json | **12-persona FLEET run 1: 9/12 clean PASS** (build a967f54). PASS: L2/L3 (steady), L5/L6 (T1 handoff), L7 (T2), L8 (T3), L13 (phantom-blocked), L14 (freeze-blocked incl consecutive), L16 (#6 baseline) — every mechanism type validated. FAIL (all HARNESS, diagnosed from data): L9 retake day submits all-blank (nCorrect=0)→Submit disabled→timeout (fix: fail with 1 answer); L1+L4 missing state-aware resume — advanceOneDay re-drives new after a day partially completed (new✓, review tripped) → new-test-not-reached (fix: detect new-already-done, skip to review; the old lsr_runSL_phase1 had this, not ported). Manifest fail-closed + identity-bound worked (NOT-CLEAN exit 1). Next: 2 harness fixes → re-run L1/L4/L9. |
| 2026-07-12 | audit/playwright/lsr_persona.mjs (fleet-run fixes) | **Fleet run-1 (9/12) failure fixes — all HARNESS, data-diagnosed.** (1) STATE-AWARE RESUME ported from lsr_runSL_phase1: a partial day (new passed, review tripped) now resumes at the missing step via FB PASSED-counts (newPassed/reviewPassed, robust to retake fail attempts) instead of re-driving new → fixed L1(15/15)/L4(23/23 incl T1). (2) L9 retake deliberate-fail nCorrect 0→1 (all-blank disabled Submit). (3) L9 retake accepts a Did-not-pass RESULTS screen as the fail (+ FB-verify twi held). (4) CONSOLE-ERROR calibration: recovered grading-retries (FirebaseError internal, client retries+recovers) no longer fatal — benign known grading-transient, proven by day confirming. (5) PH8-1 (Codex r8): per-key dup tracking (dupKeys + allowedDupKeys) so a legit retake duplicate on day 3 no longer poisons later normal days. Validating via rerun3. |
| 2026-07-12 | audit/playwright/lsr_persona.mjs (PH9-2), lsr_fleet.sh | **Codex r9 fixes → cert fleet fleet3.** PH9-2: retake dup oracle now enforces the EXACT expected key (retake day requires exactly `${expCsd}/new`; non-retake requires zero new dups; absorb only the validated key) — was too loose (accepted any single dup / even zero). PH9-1: L1 clean artifact to come from fleet3 (save-error now recoverable). Killed early fleet2, relaunched fleet3 (fresh students s123-134) with ALL fixes: resume, per-key+exact dup, retake outcome, transient calibration (grading-retry+save-error), generalized driver-gap downgrade. Manifest gate certifies only 12/12 clean PASS. |
| 2026-07-12 | audit/playwright/findings/fleet_manifest_fleet3.json | **★ CERT FLEET fleet3: 12/12 CLEAN PASS** (build a967f54, fail-closed + identity-bound manifest). Full arcs: L1 15/15, L2 20/20, L3 16/16, L4 23/23, L5 24/24, L6 24/24, L7 19/19, L8 12/12, L9 8/8, L13 21/21, L14 8/8, L16 6/6. All mechanism types certified: steady (int/adv/final), T1 handoff (both edges/paces), T2 pace-switch, T3 different-list, retake (exact-key dup oracle), phantom+freeze EXPECTED-BLOCKED, #6 baseline. Persona expansion (the 12 implemented) COMPLETE. Deferred (NOT_YET_HARDENED): L10/L11/L12/L15 — follow-on. Product reliability observations recorded: transient grading-internal + save-error (CS-matrix #3/#4 area; app retries+recovers). |
| 2026-07-12 | docs/audits/PERSONA_FLEET_FINDINGS.md | **Persona fleet findings — Codex triaged (GO).** Escalation F4>F3>F2>F1. F4 full-freeze STUCK-STATE = top NEED_TO_FIX candidate (Codex traced: calculateInterventionLevel←recentSessions←recordSessionCompletion; frozen day never completes→interv pinned 1.0; recommend recovery-probe). F3 confirms list-end dead-end (cycling capstone need); orphan-log expectation STALE (cleanup tied to reconciliation, not same-day gate block). F2 narrows #6 (same-list/same-pace CARRIES under LIST_SCOPED_RECON→update foundation/cycling assumptions). F1 fold into grading/save reliability (~1.4% recovered-transient baseline). |
| 2026-07-12 | NEED_TO_FIX.md (#11 + #4 note), docs/plans/loop/x/plan.md, docs/audits/PERSONA_FLEET_FINDINGS.md | **Persona-fleet findings RECORDED (Codex-triaged, doc-only).** F4 CONFIRMED from fleet3 L14 data (4 high-score reviews on the stuck day, none appended to recentSessions, csd frozen) → NEED_TO_FIX #11 (full-freeze permanent stuck-state; fix = floor newWordCount≥1 on gated days OR record blocked-day reviews into the interv window; own plan+go-ahead). F1 folded into NEED_TO_FIX #4 (grading/save reliability, ~1.4% recovered-transient baseline). F2 narrowed #6 scope in cycling capstone (x/plan.md): same-list/same-pace CARRIES under LIST_SCOPED_RECON → not an open blocker; #6 open only for diff-pace/list-switch/flag-off. F3 = cycling-capstone confirmation (orphan-log expectation stale, per Codex). No app source touched. |
| 2026-07-12 | docs/plans/PLAN_review_only_day_completion.md (v3) | **Review-only completion design CONVERGED (Codex r2 GO + r2 3-agent).** v3 folds: Lens A code-corrections (predicate <=0 not ===0 for over-introduction; newWordScore must be LITERAL null in summary+session_state, not null>=threshold which coerces to false; branch on isListComplete not the non-persisted wordsRemaining; terminal no-review path is at DailySessionFlow:826 not the all-mastered modal; suppress spurious warn); Lens B safe (+ W3-must-re-derive-server-side forward-note); Lens C = UX companion is a real SUB-PROJECT (hero needs allocation prediction + pinned dailyPace; recovery copy gate on newWordCount<=0 not interv>0; oscillation; terminal-screen persistence+self-sufficient copy; teacher cells). Codex 6 guardrails + stale-finite-0 test added. Scope → 3 PHASES: backend deadlock fix (closes #11) / UX sub-project fast-follow / cycling capstone. No app code; awaiting David go-ahead. |
| 2026-07-12 | docs/plans/PLAN_review_only_day_completion.md (v3, CONVERGED) | **Review-only completion design CONVERGED — Codex GO/CONVERGED-OK (r4).** Loop: r1 NEEDS_FIXES → r2 GO(design) → r3 NEEDS_FIXES (ROD3-1: <=0 predicate let a negative newWordCount decrement TWI via totalWordsIntroduced+=wordsIntroduced; fix=clamp wordsIntroduced to 0) → r4 GO. Two 3-agent audits folded (Lens A code-corrections, Lens B security-safe, Lens C UX=sub-project). 3-phase plan: **Phase 1 backend deadlock fix (closes NEED_TO_FIX #11)** design-ready; Phase 2 allocation-aware UX companion; Phase 3 cycling capstone (recon re-verify). 7 impl guardrails. Terminal-screen copy = self-sufficient (David). NO app code — awaiting David explicit go-ahead to implement Phase 1. |
| 2026-07-12 | src/services/studyService.js (completeSessionFromTest) | **Phase 1 IMPLEMENTED — review-only-day completion fix (closes NEED_TO_FIX #11).** LOCAL-ONLY on main (no commit/push; live has active students). 4 surgical edits per converged design (PLAN_review_only_day_completion.md v3, Codex r4 GO) + all 7 guardrails: (1) `reviewOnlyDay = LIST_SCOPED_RECON && Number.isFinite(cfgNewWordCount) && cfgNewWordCount <= 0`; (2) `wordsIntroduced` clamped to 0 on that path (no negative → no TWI decrement via `totalWordsIntroduced +=`); (3) Day-2+ gate now `!reviewOnlyDay && …` (ordinary assigned-new retake gate intact); (4) LITERAL nulls in session_state (`newWordsTestScore:null`, `newWordsTestPassed:null`, NOT `null>=threshold` which coerces to false) + `reviewOnlyDay:true` marker; summary `newWordScore=null` → filtered by `avgNewWordScore` (progressService:349); (5) no fake `recordSessionCompletion`; warn suppressed on expected review-only path. Recovery closes: completed review-only day appends its review score to recentSessions → next build recomputes lower intervention (studyService:167) → newWordCount rises → student escapes. `node --check` OK. In review: 3-agent audit (Explore A/B/C) + Codex REVIEWONLY_IMPL r1 (baton flipped) running in parallel. |
| 2026-07-12 | src/services/studyService.js, src/pages/DailySessionFlow.jsx, src/pages/Dashboard.jsx | **Phase 1 v2 — review-only fix hardened + §5 list-end terminal added (audit round 1 folded).** LOCAL-ONLY on main (no commit; live has active students). Round-1 review = 3-agent (Explore A/B/C) + Codex REVIEWONLY_IMPL r1 (NEEDS_FIXES). Fixes: **(ROI-1, Codex blocker)** `reviewOnlyDay` now requires a CONFIRMED reason (`allocation.newWords<=0` OR `isListComplete===true` OR `startPhase===REVIEW_STUDY`) in addition to `cfgNewWordCount<=0` — a stale/forged finite `0` on an ordinary assigned-new day can no longer skip the gate (plan acceptance test 4b); fields verified present + persisted in both dailySessionState write sites (DailySessionFlow:1161 main, :703 recovery). **(Lens A #1, medium regression)** the null session-state write is now keyed on ATTEMPT-ABSENCE (`newWordScore===null`), not `reviewOnlyDay` — a Fix #9 REVIEW_STUDY resume (reviewOnlyDay but a real passing attempt) persists its true score, so session_state matches summary/recentSessions. **(Lens A #3, nit)** marker comment softened (write-only, no consumer, not on summary). **§5 list-end terminal (plan §9 = Phase 1, per Lens A#2/C#1/C#2):** DailySessionFlow CompletePhase renders a distinct "🎉 You finished the list!" terminal on `isListComplete` (self-sufficient copy, no teacher dependency — Lens C#6); empty-review guard added at the fresh review-only branch (:819, mirrors :807 — Lens C#3), no fake recordSessionCompletion at :826; Dashboard hero shows a persistent finished state (`listFinished = wordsLeft===0`) instead of "come back tomorrow"/"Learn N new words". Deferred to Phase 2 (plan §6): allocation-aware hero, teacher legibility, oscillation copy. eslint parse-clean (only pre-existing rule violations). Handing v2 to Codex r2. |
| 2026-07-12 | src/pages/DailySessionFlow.jsx | **Phase 1 v3 — ROI2-1 fix (Codex r2 blocker).** LOCAL-ONLY on main. The v2 empty-review guard at :819 routed to `showNoReviewModal`, whose close handler calls `completeSession()` → `recordSessionCompletion()` — so a no-work list-end day RECORDED a fake completion (CSD advance), violating guardrail 5 / plan test 6. Fixed per Codex option 3: the empty-review terminal now `setPhase(COMPLETE)` directly (+ sim restart) with NO completeSession/recordSessionCompletion — currentStudyDay/recentSessions/completed-session records do NOT advance for the no-work terminal; CompletePhase renders the §5 finished screen (sessionConfig.isListComplete already set at :567/:740). Non-recording terminal, no empty-review-test dead-end. Doc: plan §4 W3 note escalated to a HARD sequencing dependency (B1); §11 tracks B2 observability log + pre-existing newWordsTestPassed derivation (both deferred, Codex-non-blocking). Handing v3 to Codex r3. |
| 2026-07-12 | docs/plans/loop/baton.json (REVIEWONLY_IMPL CONVERGED) | **Phase 1 CODE loop CONVERGED — Codex r3 GO (blockers=0 high=0 med=0 nits=0).** Loop: 3-agent audit (Explore A correctness / B security / C UX) on v1 + Codex r1 NEEDS_FIXES (ROI-1 stale-finite-0 false-open) → r2 NEEDS_FIXES (ROI2-1 empty/no-work terminal fake-completion via recording modal) → r3 GO. Four real defects caught + fixed (ROI-1, Lens A#1 #9-resume null regression, §5 terminal gap, ROI2-1). Phase 1 code complete across studyService.js + DailySessionFlow.jsx + Dashboard.jsx (LOCAL-ONLY, uncommitted — live has active students). Codex acceptance guidance captured for the Playwright audit design. Next: design Playwright acceptance audits (3-agent + Codex loop until converge) → npm run dev local server (confirm serving) → run audits locally (NEVER live). |
| 2026-07-12 | docs/plans/PLAN_reviewonly_playwright_audit.md (v2) | **Playwright acceptance-audit DESIGN v2 — folds design-review round 1** (3-agent A/B/C + Codex REVIEWONLY_AUDIT_DESIGN r1: 4 blockers/2 high/2 med). Key changes: RA9 added (Fix-#9 resume real-score-persists — the only scenario catching a Lens A#1 null-keying regression); HYBRID design — ROI-1 stale-0 + absent-config + modified-gate moved to a labeled white-box integration layer (§7) because the completion gate is NOT faithfully reachable via UI routing (a passing new attempt dominates); import-time local-only base guard + fail-closed sandbox-identity guard (data=prod, isolation via identity not base); RA6 bespoke terminal-assert verb + MASTERED-seed returnAt=now+21d + settle-window no-advance proof; RA3 relabeled "retake preserved"; oracle inversion asserts affirmative completion + FB-read interv/recovery model; exact artifact binding + sandbox-triple identityOk; transient recalibration. ADJUDICATED against code: REJECTED Codex RAD-3 / Lens B "raw twi seed = invalid anchor" (progressService.js:236 preserves via Math.max; csd_anchor_invalid at :294 fires only for a MALFORMED EXISTING anchor, not a no-attempt seed) → SEED list-end (fast) + a mandatory pre-verifier (folds Codex's safety intent). Handing v2 to Codex r2. |
| 2026-07-12 | docs/plans/loop/baton.json (REVIEWONLY_AUDIT_DESIGN CONVERGED) | **Playwright acceptance-audit DESIGN loop CONVERGED — Codex r2 GO (0/0/0/0).** Loop: 3-agent audit (coverage/safety/realism) + Codex r1 NEEDS_FIXES (4 blockers/2 high/2 med) → r2 GO. Codex CONCEDED RAD-3 (invalid-anchor) to code evidence. Design v2 (`PLAN_reviewonly_playwright_audit.md`) is build-ready: HYBRID E2E+white-box, import-time local-only base guard + fail-closed identity guard, seed-list-end + mandatory pre-verifier, RA9 (#9-resume regression catcher), exact artifact binding. Next: build harness per §8 → npm run dev localhost:5173 (confirm serving) → run audits locally (NEVER live). |
| 2026-07-13 | scripts/cs/diag-reviewonly-cases.mjs, sweep-ascent-carry.mjs, reconcile-ascent-carry.mjs (NEW, CS) | **CS scripts for the 2026-07-13 support cases (read-only diag + Ascent cross-class carry sweep + reconcile).** diag = read-only dump of a student's classes/progress/attempts/session_states. sweep-ascent-carry = read-only cohort scan for Ascent (dVliNv0p) progress that didn't carry across classes (with active-class context to exclude false positives). reconcile-ascent-carry = writes derived csd/twi from an EXISTING valid anchor (+clears stale session_state), `--dry` default. Used for CS-2026-07-13 (3 ADV[한] students reconciled; sweep CLEAN before/after). See SUPPORT_RUNBOOK. |
| 2026-07-13 | scripts/cs/move-kimdonghyun-to-summit.mjs (NEW, CS) | **CS-2026-07-13b: config-only script to move 김동현 to SUMMIT** (primaryFocusListId/ClassId), --dry default. He finished Ascent + was frozen on #11 list-end wall. Ascent progress untouched. Sweep CLEAN. |
| 2026-07-13 | scripts/cs/fix-kaila-junseo.mjs, scan-reviewonly-frozen.mjs (NEW, CS) | **CS-2026-07-13c: unstick Kaila (cross-class csd undercount 2→3) + Junseo (#11 throttle — manual review-only completion) + a READ-ONLY 26SM #11 scan.** Scan found 169 students at the #11 list-end wall (0 throttle). See SUPPORT_RUNBOOK CS-2026-07-13c. |
| 2026-07-13 | scripts/cs/ensure-all-lists.mjs, batch-advance-listend.mjs (NEW, CS) + classes (config) | **CS-2026-07-13d/e: ensured all 32 26SM classes have all 3 lists (backdated, props mirrored, backed up) + batch-advanced 87 list-end students to their next list (config-only primaryFocus).** See SUPPORT_RUNBOOK CS-2026-07-13d/e. |
| 2026-07-13 | scripts/cs/report-next-list.mjs (NEW, CS) + 3 students (primaryFocus) | **CS-2026-07-13f: advanced 3 Bridge TOP #11 list-end finishers to Ascent; report-next-list = read-only report of all 26SM list-end finishers → next list (needs-advance / advanced / finished-everything).** |
| 2026-07-13 | RESUME.md (rotated), docs/audits/TA_CHATLOG_TRIAGE_2026-07-13.md, NEED_TO_FIX.md (#13/#14/#15) | **SAVE STATE + TA chat-log triage (6/30→7/13).** Rotated stale RESUME (Run S/#10 era → RESUME_2026-07-12b.md), wrote fresh active stream (#11 fix built+converged+NOT deployed; audit harness built+NOT run; 7/13 CS surge; 169 at #11 wall; list-linking feature request). Thorough triage of the TA support channel: whole week = #11 list-end + cross-class carry (both = our work); surfaced NEW NEED_TO_FIX #13 (test-size mis-gen at boundaries — 이혜성 day-1 10≠30 verified, 김호형 30≠35 open), #14 (permanent-fail from grader-false-neg + token exhaustion + no override; challenge replenish=30d not weekly; promotion permission gap), #15 (no review-retake; low-review→#11-throttle chain). No app source touched. |
| 2026-07-13 | audit/deepfix/** (NEW program), RESUME.md (rotated), .gitignore, memory | **DEEPFIX program set up (David's 7-task commissioning) + session save-state.** Created `audit/deepfix/`: MASTER_TASK_PLAN.md (Tasks 1-7: deep issue analysis w/ fable+Codex independent lists → consolidated ideal-app investigation → fix plan loop → implementation loop → Playwright audit design loop → harness build loop → runs w/ screen-verified errors → dual final reports; hard rules H1-H12 incl. the new VERIFY-EVERYTHING rule baked into every step; baton system §B; agent conventions §A; save-state §S; resume §R), context/ (verbatim TA chat log 6/30-7/13, SESSION_CONTEXT digest, 47MB session transcript [gitignored], phase1 fix diff, next-list reports, diag raw). Plan verified by 2 full-context fork agents (A: fidelity/completeness, B: executability/correctness) — both GO-WITH-FIXES, 16 findings ALL verified true + folded (PLAN_VERIFICATION_2026-07-13.md); notable: deploy-posture line (David: "fix as requests come in"), H11 live-CS-interrupt protocol, SOP-ordering override, concrete Task-6 Firestore preflight gate, RESUME rotation before clear. New memory: verify-all-claims (hard rule). RESUME rotated → RESUME_2026-07-13.md; new RESUME points at deepfix Task 1.1. |
| 2026-07-13 | audit/deepfix/task1/CONSOLIDATED_ISSUES.md, INVESTIGATION_PLAN.md (NEW, docs-only) | **DEEPFIX Task 1.4 — consolidation + investigation plan.** Merged the 3 independent issue lists (claude/fable1/codex) + verification ledger + CS manual-writes catalog + live 26SM census into one deduped master list (~40 issues, 7 reconciled roots CR-1..CR-7, per-issue evidence tags + status + Firebase-evidence linkage), with the census numbers driving priority (183 at #11 wall; 98 dual-enroll split-progress; 82 hand-patched/25 re-stuck; real #13 ≈30-50; 12 drifted threshold assignments; unattributed impossible_phase 3088 / day_guard 17). Reconciled disagreements: #9/#10 fixed-in-tree-verify-deployed; getPrimaryFocus finished-list-keeps-focus; #7 latent (0 live) but 6-site; token accounting = active-rejections-only/30d. INVESTIGATION_PLAN: orchestrator-only read-only Firebase workstream F-1..F-14 (agents get exports only) + code investigations I-1..I-10 + 1.5 re-trace list + execution waves + 6 Task-2 gates. 1.4 spot-checks run (V-1.4): GRADE_TOKEN_ENFORCED=true@functions:58, token formula db.js:179-185, Dashboard focus recency, automarker no-testId/no-range, 6 assignedLists sites. No app source touched. |
| 2026-07-13 | audit/deepfix/task2/FIX_PLAN.md (NEW, docs-only) | **DEEPFIX Task 2.1 — converged phased FIX PLAN v1 (design-only, no code/live-Firebase).** One plan on the I-6 backbone: FND-0 (G1 disarm + provenance) → RO (hosting-only deploy of built #11/#9/#10/C-27; 183-wall + 6 day_guard) → RS (read/render truth: C-33/34/35 + C-23 code fix; hosting+indexes, parallel) → FND-1 (server surface: completeSession/resolveListProgress/resetProgress/W1/W2-upgraded/M4-shadow + nonce F2) → FND-2 (client cutover + nonce F1/F3 + build stamp) → FND-3 (ONE migration w/ the §7.1 reviewOnlyDay-evidence CSD-screen amendment) → FND-4 (one rules cutoff: wildcard restructure + role whitelist + W3 incl. delete-removal + M4 enforce) → FND-5 (retire legacy + dead code) → CONT-A (David's feature request: nextListId linking + choice terminal + continuous advance — argued safe pre-foundation) → CYC (start-over cycling, HARD-gated FND-4, + lap-aware M4) → OVR (override + reviewChallenge→server + C-19 union authz + clamp/phase-gate). Per-phase: file:line targets (all re-verified today, [V-P]), ship/test/revert, flags, F-4 H/P/B acceptance, I-5 deploy gate. Position taken: foundation MOOTS #12 (resolver logging = the standing instrumented repro; falsifier stated). Corrections logged: session-snapshot "clean tree" FALSE (#11 fix still uncommitted); I-2 flag-line cites stale by 1; 45-vs-82 patched reconciled (45 students/82 rows). |
| 2026-07-13 | audit/deepfix/task2/FIX_PLAN.md (v2, docs-only) | **DEEPFIX Task 2.1 — FIX PLAN v2: folded Codex review (1 blocker + 4 high + 2 med, all orchestrator-verified + drafter-re-verified true).** Added §9 v1→v2 changelog with per-fold file:line evidence. Folds: [BLOCKER] resolveListProgress ships READ-ONLY/shadow through P4 (writes NO canonical doc on loads) → write-capable ONLY as part of P5's migration, keeping P5 the single audited canonical writer (P3 change 2, P4 routing, P5 flip, §3 new constraint 9, §8.3h). [HIGH] P6 M8 role rule split by op — create allows role=='student'/absent, update excludes role, teacher via callable (else db.js:221/:233 merge-create breaks); + rules-test matrix (§5 new role row). [HIGH] reset cutover — SERVER_RESET_PROGRESS routes every reset caller to resetProgress at P4 BEFORE P6 removes client owner-delete (db.js:2886/:2958-2995, rules:117-118); P6 precondition + rules-denied-reset persona (§3 constraint 10, §5 new reset row). [HIGH] P3 "no live path changes" was FALSE — GRADE_JOB_ENABLED=true in HEAD (functions:90/:973/:1459) → first functions deploy activates grading-job; keep true (validate, don't flip), add 7-transition grading_job_tests.mjs suite + typed smoke to acceptance. [HIGH] P5 CSD screen — PRIMARY evidence = distinct post-anchor review ATTEMPTS (durable ledger), reviewOnlyDay session marker SUPPLEMENTAL only (studyService:1449 "deliberately not on the summary"); + dry-run assertion for N>1 review-only days. [MED] P3 W2-marker output-shape assertion (parseable testId + integer nwsi/nwei==anchor + pairs + gradebook-visible). [MED] P0 scoped commit manifest — only the 3 #11 runtime files, recorded sha, isolated from 223-path audit churn. Structure/altitude preserved (no phases added/removed). |
| 2026-07-13 | audit/deepfix/task2/FIX_PLAN.md (v3, docs-only) | **DEEPFIX Task 2.1 — FIX PLAN v3: folded the 3 fable verifiers' review of v2 (1 blocker + 7 high + several med/nits; orchestrator-verified all true, drafter independently re-verified each file:line; adjudication_log.md). Added §9b v2→v3 changelog.** Folds: [BLOCKER F4-1] read-only resolver PRESERVES the legacy class_progress recon write (progressService:264-271) the completion day-guard baselines on (:441-448) — else every strand/dual-enroll completion rejects; only NEW canonical-doc creation withheld til P5. [F4-2] server reviewOnlyDay must replicate ALL 3 client predicate reasons (studyService:1329-1335), not allocation-only, or #9 REVIEW_STUDY-resume double-introduces twi. [F4-3] P6 role-create rule breaks live self-select teacher signup (Signup:38/:141-144) → teacher-provisioning path ships WITH P6 (net #1b security win); §7 decision 10 gates P6. [F4-4/F6-1/F6-9] P5 completeSession legacy→canonical write-flip spec'd (3-flag atomic cutover) + off-peak window + watch + post-flip catch-up; reversibility honest (point-of-no-return = first post-flip completion). [F5-HIGH-1] MCQ stays client-authoritative (functions:436-442) → N5 caveat + §6.2b ledger + X3 forgery caveat + Phase-E follow-on. [F5-HIGH-2] 3rd twi writer (reviewChallenge day-advance, db:2790-2833) routed server-side at P4/P5 not P10 (+ closes the unclamped :2831-2833 defect). [F6-2] teacher read path (fetchStudentsProgressForClass:518, ClassDetail:198) routed to resolver P4/P5. [F6-3] CS toolchain (sweep/manual-pass/census) re-targets list_progress at P5 (X5 truthfulness). [F6-4] CONT-A ships fast post-RO vs CYC multi-week (David 'tonight' comms) + interim for 5 finished-everything; §7 decision 9. [F6-5] P8 focus-yield handles the explicit-PIN branch (Dashboard:1057-1078, returns before recency:1084) → ~287 CS-pinned students; lap-aware for P9. MEDs: P7 retirement inventory + zero-class_progress-refs + flag lifecycle; runtime reviewOnlyDay mismatch log; recon-overlay end-state (§6.6); completeSession idempotency; CSD per-doc own-anchor baseline; §7 decisions 2/3/8 gate P5; P1 G5 watch signal/clock; nit cites (P6a P9→P10, owner-delete rules:120-122). Backbone/altitude unchanged — no phase add/re-sequence. |
| 2026-07-13 | src/services/db.js, firestore.indexes.json, src/pages/TypedTest.jsx, src/pages/MCQTest.jsx | **DEEPFIX Task 3 — P2 (RS) remainder IMPLEMENTED: C-34 + C-33 + C-23 (LOCAL-ONLY, no commit/deploy/live-call; C-35 was already in tree, untouched).** **C-34:** field-first listId (`attemptData.listId ?? parsedListId`, the shipped db.js:2528-2533 pattern) at the 4 parse-and-drop sites — fetchClassAttempts:1483, fetchAllTeacherAttempts:1633, queryTeacherAttempts:2015, queryStudentAttempts:2209 — rows now drop only when BOTH the stored field and the testId parse are absent; automarker/manual rows (top-level listId, no testId — functions/index.js:584) become gradebook-visible; 'Unknown List' name fallbacks verified pre-existing at all 4 sites; List-filter semantics untouched. **C-33:** queryTeacherAttempts pushes the resolved Name filter server-side (db.js:1956-1971: 1 id → `==`, 2..30 → `in` with classDisjuncts×n ≤ 30 DNF guard; >30 keeps the post-filter degraded mode, post-filter retained as backstop; pagination/hasMore/empty-match return unchanged) + 2 new `attempts` composite indexes appended to firestore.indexes.json (teacherId+studentId+submittedAt DESC; teacherId+classId+studentId+submittedAt DESC — deploy indexes BEFORE code when shipping). **C-23:** new-word result cards now render the STORED verdict — submitVocabAttempt's returned `passed` (functions:451/:534, idempotent path :238/:470) captured in both test pages (server branch: resp.data.passed; legacy branch: the client-written doc's own verdict) → testResultsData.serverPassed → card `serverPassed ?? (Number.isFinite(retakeThreshold) ? score >= retakeThreshold : true)` (TypedTest:1322-1327, MCQTest:1058-1063); PATH A threshold adoption guarded with Number.isFinite (TypedTest:291, MCQTest:246) so an undefined passThresholdDecimal can no longer fail-close every compare. Write paths/completion gating untouched (X4). Checks: node --check db.js OK; @babel/parser jsx parse of both pages OK; indexes JSON.parse OK. Notes: audit/deepfix/task3/P2_impl_notes.md. |
| 2026-07-13 | functions/foundation.js (NEW), functions/index.js | **DEEPFIX Task 3 — P3 (FND-1) additive server surface IMPLEMENTED as a REVIEWED DRAFT (LOCAL-ONLY: no deploy, no commit, no live-Firebase call; everything dormant behind server flags — see FOUNDATION_FLAGS, all false).** NEW `foundation.js`: (1) `completeSession` — ONE Admin-SDK txn: transactional day-guard (progressService:441-452 semantics, closes [C3-2]), server allocation recompute (pace chain DailySessionFlow:558→studyService:170-179; interv studyAlgorithm:66-98), reviewOnlyDay replicating ALL 3 client reasons (studyService:1329-1335; F4-2 — reason 3 derived as "day's new pass absorbed into stored twi", the S8 anti-double-introduce), wordsIntroduced=max(0,min(alloc,remaining)) clamp (:1339-1342), recentSessions/stats/streak parity, committed-retry idempotency (already_completed, no second +1), rejection → session_states clear + `day_guard_rejected_session_cleared` WITH uid, review-only-no-review marker leg, `reviewonly_derivation_mismatch` tripwire. (2) `resolveListProgress` — TWO modes behind `LIST_PROGRESS_CANONICAL` (false=READ-ONLY until P5): canonical-first, else IN-MEMORY merged reconciliation (anchor port db.js:3248-3333 + pairing port :3400-3496 + [C4-2]+review-evidence CSD screen), F4-1 PRESERVED legacy class_progress recon write (create-on-miss + :264-271 equivalent, launch-view byte-parity — keeps the day-guard baseline), NO canonical doc created on any load, EVERY resolution logs `resolve_list_progress {uid,listId,anchorStatus,applied,sources}` (the #12 tripwire); WRITE mode (P5-only) = [C4-4] transactional hydrate-on-miss + §8 merge + quarantine BLOCKS. (3) `resetProgress` — list-wide wipe per persist §5.3 order (attempts FIRST all classes w/ testId-parse legacy sweep; session_states all classes; study_states; class_progress all classes) + resetEpoch/resetAt stamp (pre-P5 in users/{uid}/progress_meta to keep list_progress provably empty — FLAGGED U8). (9) `advanceForChallenge` — 3rd twi writer server path (F5-HIGH-2; db.js:2782-2846 port), teacher-of-record authz, boundary guard, CLAMPED to wordsRemaining + twi PHASE-GATED to phase==='new'. index.js edits: M4 SHADOW anchor validation hook in writeAttemptTxn (log-only `anchor_rejected`, flag off, try/caught — zero live-path change); W2 `markReviewComplete` UPGRADED via shared writer (parseable testId `vocaboost_test_{classId}_{listId}_review` + server-derived day-anchor range → satisfies exact-range pairing db.js:3449-3450 + gradebook parse :1977-1984 — the C-14/C-34 fix); nonce F2 server leg (grade-only payload + cached grading-job payload now carry `attemptDocId: bindCtx?.attemptDocId ?? null`, inv_I5 §2); 4 callables re-exported + `version` probe reports FOUNDATION_FLAGS. W1 `submitChallenge` verified ALREADY in tree w/ exact db.js:188-194 token parity — untouched. GRADE_TOKEN_ENFORCED stays false (preserved). node --check: both files OK. Notes + 14 explicit uncertainties (U1-U14): audit/deepfix/task3/P3_impl_notes.md; diff: task3/phase3_diff.patch. |
| 2026-07-13 | audit/deepfix/task4/AUDIT_DESIGN.md (NEW, docs-only) | **DEEPFIX Task 4.1 — converged Playwright AUDIT DESIGN v1 (READ-ONLY design; no code, no live-Firebase).** One all-at-once post-implementation audit covering every FIX_PLAN v3 phase (P0-P10): **83 scenarios** (~37 E2E / 46 white-box) across 6 fail-closed matrices — M-UI (extends the built-never-run lsr_reviewonly.mjs RA1-RA9 + adds RO-S1/S9/S10 for full I-2 S1-S10 coverage, RS-1..4 read surfaces, CUT personas, CA-1..6 CONT-A incl. the F6-5 pin branch, CY-1..7 laps, OV E2E), M-WB (whitebox gate-negatives + storage-stubbed nonce + forced-deny handler), NEW M-CALL (authenticated callable probes: completeSession day-guard/idempotency/3-reason derivation fixtures, W2 marker shape, M4 forged-anchor enforce, resetProgress epoch, resolver straggler/quarantine, override authz union, challenge clamp/phase-gate), NEW M-RULES (client-SDK/REST P6 cutoff matrix: forgery/role/delete DENIED, happy paths ALLOWED — Admin SDK can't test rules), NEW M-MIG (the P5 25WT rehearsal formalized: strand→anchor, divergent [C4-1] own-anchor screen, review-only-evidence screen, quarantine, idempotency, catch-up, 0-regression sweep, CS-toolchain retarget), M-STATIC (P0 deploy-gate DG-1..4 + P4/P7 greps — the only WSL-runnable block). Certification: INVALID≠PASS; manifests bind runId+gitHead/dirty+resolved BASE+deployed version-probe sha+rules sha256+per-scenario sandbox triple+pre/post snapshot hashes; subset runs cannot certify. Seeding: every seed sandbox-triple-gated + MANDATORY pre-verifier (RAD-3 lesson). Guards: reuses the import-time localhost BASE guard (lsr_ui.mjs:23-31) + identity/sandbox guards; new probe/migration allowlist guards; runs on Codex/David's Windows env (WSL can't run Vite/Playwright). §6 honest ledger flags the criteria WITHOUT audit oracles: live F-4 H/P/B motion, M4 ≥14d live shadow rate, G5 watch windows, transition-window criteria (P4 "list_progress empty until P5" → gate-artifact verification), RUL-8 provisioning mechanism (David decision 10). |
| 2026-07-13 | src/config/featureFlags.js, src/services/db.js, src/pages/ClassDetail.jsx, src/pages/Dashboard.jsx, src/pages/DailySessionFlow.jsx | **DEEPFIX Task 3 — P8 (CONT-A) IMPLEMENTED (LOCAL-ONLY: no deploy, no commit, no live-Firebase call; everything behind NEW flag `CONTINUATION_LINKS`, default OFF — flag off / `nextListId` unset = today's behavior exactly).** **Schema:** nullable `nextListId` on `classes/{classId}.assignments[listId]`, validated at the assignment-settings write site (`updateAssignmentSettings`, db.js:946-966 — must be another list assigned to THIS class, never self; owner-teacher-only like pace/testSizeNew; `assignListToClass`'s spread already preserves it on re-assign). **Teacher UI:** ClassDetail settings modal gains a flag-gated "List Sequence → Next List" selector over the class's own assigned lists + a "Next: {title}" card chip; save sends the key ONLY when the flag is on. **Choice terminal:** the finished terminal (DailySessionFlow CompletePhase) + the Dashboard finished hero gain "Advance to {nextList} →" when the LAUNCHING class's assignment links a valid, still-assigned next list (dangling links fail closed to the static terminal — never a dead button); "Start over" is NOT rendered anywhere (capability-gated: cycling/P9 not live; P9 comment markers left at both terminals). **Focus-yield (C-13/F6-5):** `getPrimaryFocus` yields a FINISHED list (twi ≥ listTotal, listTotal>0) to its linked nextListId at BOTH the explicit-PIN branch (returns FIRST — the ~287 CS-pinned students; pin resolved read-only, never rewritten) AND the recency branch; chain-follows with a cycle guard; lap-aware for P9 (yield gated OFF for `cyclingEnabled` assignments so lap-2 twi>listTotal can't misfire). **Continuous start:** "Advance" = the SAME cleanup as Back-to-Dashboard (localStorage + session_states clear) + pure `navigate('/session/{classId}/{nextListId}')` → next list Day 1 via the UNCHANGED initializeDailySession→getOrCreateClassProgress create-on-miss path; NO new write path. Implementation necessities: `classAssignedListIds` rides the dailySessionState sessionStorage blob (init is skipped on testCompleted re-entry) and DailySessionFlow now remounts via a `key={classId_listId}` route wrapper (React Router doesn't remount on param change; no-op for all existing flows). **§2.1 falsifier CONFIRMED HOLDING:** no CONT-A path writes the finished list's totalWordsIntroduced/currentStudyDay/anchors or alters allocation (write-by-write audit table in the notes). Checks: node --check featureFlags.js + db.js OK; @babel/parser(jsx) parse of all 3 .jsx OK (node --check can't parse JSX; esbuild binary is win32 in this env); ESLint before/after diff = ZERO new findings. Back-fill of Base→Ascent→Summit links = a future David-authorized config CS event, NOT executed. Notes: audit/deepfix/task3/P8_impl_notes.md. |
| 2026-07-13 | scripts/cs/deepfix-migrate-list-progress.mjs (NEW), audit/deepfix/task3/P5_impl_notes.md (NEW) | **DEEPFIX Task 3 — P5 (FND-3) migration script DRAFTED (LOCAL-ONLY: no commit, no deploy, NO live writes — `--dry` only until David authorizes; a `--commit` is a SUPPORT_RUNBOOK CS event).** Standalone Admin-SDK script collapsing every `users/{uid}/class_progress/{classId}_{listId}` into ONE `users/{uid}/list_progress/{listId}` per (student,list). Conflict rule per I-6 §2.1 = persist §8: TWI = anchor-validated max (anchor semantics = db.js:3239 list-scoped max-integer-nwei+1, submittedAt tie-break; forged/anchorless highs → PAIR quarantine, never zeroed, never auto-promoted); CSD = max-PLAUSIBLE across every source doc, each screened on its OWN anchor (v3 MED) via **the MANDATORY P5 CSD-plausibility screen** — primary durable evidence = distinct post-anchor REVIEW ATTEMPTS keyed (classId,listId,studyDay), submittedAt > anchor, capped 1/studyDay, same student+list lineage (made CONCRETE as `lineageOf()`: explicit fields, canonical-testId parse fallback only, no heuristics — Codex r2 carryforward); the ephemeral session_states `reviewOnlyDay` marker NOT consulted (studyService:1448-1455 verified). Ancillary from max-twi winner (tie: twi→csd→lastSessionAt); programStartDate=min(); drops progressSnapshot/blindSpot/classId (winner kept as informational lastActiveClassId); migratedAt/migratedFrom stamps + legacy-doc stamps in one atomic batch per pair. Populations per census2 parity (36 LIVE-STRAND jump-to-anchor / 6 divergent [C4-1] / 72 stale collapse / ~633 single-doc 1:1 re-key, promotions reported). `--dry` default (write-guarded, zero Firestore writes, full per-student diff JSON to dsg-edits/srv_validate/), `--commit --confirm-migrate=<cohort>` guarded (refuses on any failed assert or quarantine>0 [C7-2]; per-pair backups to dsg-edits/srv_validate/list_progress_backups/ FIRST), `--catchup` = persist §8 [C3-4] post-flip delta pass; idempotent via migratedAt (SKIP_DONE/MERGE_STRAGGLER; foreign canonical overwritten only on anchor-validated-twi ≥, never wall-clock). Hard asserts A1-A8: 0 twi/csd regressions, dual-signature-after=0, NOBODY H→B (full F-4 H/P/B before/after), manual-pass values survive, N>1 review-only days pass on attempt evidence, divergent max-csd survives, zero FOREIGN pre-existing canonical (resolver wrote none). Checks: node --check OK; write-site grep = all writes inside guarded branches; `--dry 25WT --limit=20` logic walk: 8/8 asserts PASS, 2 correct ANCHORLESS_TWI quarantines, 0 Firestore writes, 2 rows hand-traced to real anchor attempts. 10 explicit uncertainties (U1-U10, incl. quarantine granularity vs persist-§8 letter and the A6 strictness knob): audit/deepfix/task3/P5_impl_notes.md. Three-flag cutover + CS-toolchain rework (F6-3) are separate P5 steps, NOT in this file. |
| 2026-07-13 | scripts/cs/deepfix-migrate-list-progress.mjs, audit/deepfix/task3/P5_impl_notes.md | **DEEPFIX Task 3 — P5 (FND-3) script: Codex review folds (NEEDS_FIXES → 3 fixed; core logic VALIDATED by Codex, LOCAL-ONLY still `--dry`).** **P5-1 [BLOCKER]:** `--catchup` bypassed quarantine discipline (wrote CSD/ancillary for migration-owned records with late legacy docs without checking r.action/r.quarantine → a late doc in a quarantined pair could promote canonical state) → now fails closed up front on assertFailures>0||quarTotal>0 (exit 2) AND per-pair skips SKIP_QUARANTINE/SKIP_ERROR/r.quarantine.length records. **P5-2 [HIGH]:** A6 evidence-alone not enforced (csdPlausible used max(evidCeil,calCeil) so calendar ceiling could RESCUE an unsupported gap) → durable review-attempt evidence ceiling now BINDING (csdPlausible=passesOnEvidenceAlone), calendar ceiling observability-only, gap≥2 passing only on calendar now quarantines/excludes AND hard-fails A6; new `--diagnostic-calendar-rescue` (dry inspection only, blocked in commit/catchup) preserves the loose reading for a future owner spec change; U5 updated (resolved). **P5-3 [MED]:** dry exit ignored quarantine → `notReady=assertFailures>0||quarTotal>0`, exit 2 unless `--diagnostic-only`, + single `FINAL: READY|NOT_READY asserts_failing=N quarantine=N` line. Verified: node --check OK; `--dry 25WT --limit=40` → FINAL NOT_READY quarantine=6 exit 2; --diagnostic-only exit 0; --commit+--diagnostic-only REFUSED exit 1; --catchup w/ quarantine REFUSED exit 2. Notes §8 appended. |
| 2026-07-13 | src/config/featureFlags.js, src/utils/testRecovery.js, src/utils/buildStamp.js (NEW), vite.config.js, src/main.jsx, src/pages/TypedTest.jsx, src/pages/MCQTest.jsx, src/pages/DailySessionFlow.jsx, src/pages/Dashboard.jsx, src/services/studyService.js, src/services/progressService.js, src/services/db.js | **DEEPFIX Task 3 — P4 (FND-2) client cutover IMPLEMENTED as a REVIEWED DRAFT (LOCAL-ONLY: no deploy, no commit, no live-Firebase call; the NEW routing flags ship DEFAULT-FALSE/DORMANT — the flip-to-true is David's DEPLOY-time cutover AFTER the P3 functions deploy + soak; flag-off behavior byte-equivalent to today, Run-L).** **Flags:** NEW `SERVER_PROGRESS_WRITE=false` (recordSessionCompletion→`completeSession` + hydration/render reads→READ-ONLY `resolveListProgress`) + NEW `SERVER_RESET_PROGRESS=false` (F6-3); `SERVER_CHALLENGE_WRITE`/`SERVER_REVIEW_MARKER` left false with P4-deploy-cutover comments. **Nonce F1 (I-5 §2):** TypedTest's SECOND identity derivation deleted — the write reuses `gradeAttemptDocId` (single derivation, MCQTest parity); **F2 client leg:** server-echoed `attemptDocId` preferred (`?? local`), `nonce_identity_divergence` tripwire. **F3:** `getOrCreateAttemptNonce` rewritten as the layered memoized store (module Map → localStorage → sessionStorage; the catch MEMOIZES the minted nonce — never re-mints per call, the 06-29 root cause); clearTestState rolls the Map + both storages. **F4:** `nonce_storage_degraded` client event (once/page-load, only when BOTH storage layers fail; dynamic-import logger). **F6-2 teacher read:** `fetchStudentsProgressForClass` reads canonical `list_progress` FIRST (legacy fallback) under the flag — teacher Students view survives P5 staleness/P7 deletion. **F6-3 reset:** `resetStudentProgress` routes to the `resetProgress` callable under the flag (client batch-delete = flag-off fallback; with flag ON no LIVE client attempt-delete path remains — orphan cleanup is log-only under LIST_SCOPED_RECON). **C6-2 handler (dormant until P6):** permission-denied detection in BOTH test pages' completion catches + DailySessionFlow → `legacy_write_denied` event + blocking reload prompt. **Build stamp (I-5 G3):** git sha baked into the bundle via vite `define` + `window.__VOCABOOST_BUILD__` probe + console line (src/utils/buildStamp.js, side-effect import in main.jsx) — first provenance-verifiable hosting deploy. **Dashboard panel C:** flag-gated reconciled resolver read (pure read, NO classId → no F4-1 write from render) feeds determineStartingPhase — retires the `impossible_phase_detected` Dashboard noise. Checks: @babel/parser(module+jsx) parse OK on all 12 changed files (node --check can't parse ESM/JSX; esbuild binary is win32 here). NOT in this draft (open P4 plan items): reviewChallenge day-advance routing to `advanceForChallenge`, client reviewonly-preview from DailySessionFlow path. Notes + uncertainties: audit/deepfix/task3/P4_impl_notes.md. |
| 2026-07-13 | src/services/db.js | **DEEPFIX Task 3 — P4 (FND-2) follow-up: closed U13 (F5-HIGH-2, the 3rd twi writer).** `reviewChallenge`'s challenge-accept day-advance (db.js:2845+) now routes to the P3 `advanceForChallenge` callable when `SERVER_CHALLENGE_WRITE` is ON, INSTEAD of the direct unclamped `class_progress` write (pre-edit :2898-2903: `currentStudyDay:currentDay+1` + `totalWordsIntroduced:(twi||0)+newWordCount`). Callable contract VERIFIED in functions/foundation.js:1594-1723 before wiring — `{attemptId, previousScore}`, teacher-of-record authz, fail→pass transition + stale-day boundary guard + phase-gate + twi CLAMPED to [0,wordsRemaining] on durableProgressRef. `previousScore = attemptData.score || 0` (the pre-acceptance snapshot from :2730; the client already wrote newScore to the doc at :2794, so the server reads the transition correctly). Flag OFF (default) → the original direct-write block preserved verbatim in the `else` branch, byte-equivalent to today; existing non-fatal catch also absorbs a not-yet-deployed-callable throw during the P4 deploy window. Closes the F5-HIGH-2 hole (post-P5 the direct write hits a dead collection; post-P7 it no-ops) AND removes the unclamped twi over-add. Imports (getFunctions/httpsCallable :23, SERVER_CHALLENGE_WRITE :24) already present. @babel/parser(module+jsx) OK; eslint delta vs HEAD = zero NEW findings from this edit (sole +1 is P2's pre-existing `assignments` unused var :1468). LOCAL-ONLY, flag dormant. Notes: audit/deepfix/task3/P4_impl_notes.md item 10 / U13-CLOSED. |
| 2026-07-13 | firestore.rules, functions/index.js, scripts/cs/create-teacher-invite.mjs (NEW), src/pages/Signup.jsx, docs/plans/W3_attempts_lockdown.rules.md | **DEEPFIX Task 3 — P6 (FND-4) THE RULES CUTOFF implemented as a REVIEWED DRAFT (LOCAL-ONLY: no deploy, no commit, no live-Firebase; `firestore.rules` is NO LONGER safe-to-deploy-anytime — hard David-run preconditions in the file header; deploy = `--only firestore:rules`, nothing rides along).** **(a) [C5-4]** users subcollection wildcard: `list_progress`+`class_progress`+`progress_meta` (the P3 reset-epoch tombstone — added beyond the plan's literal, U6) now DENY all client writes (server callables via Admin SDK are the only writers); owner+teacher READS unchanged (teacher Students view reads list_progress directly, progressService.js:590); `study_states`/`session_states` owner+teacher writes unchanged (students write own state; teacher branch survives ONLY for client reviewChallenge's study_states correction db.js:2824 → P10 narrows). **(b) M8 role SPLIT BY OP (C-28/#1b, v2 HIGH-2):** create = owner-only + role=='student' or absent (createUserDocument's merge-set-on-missing-doc = CREATE, so signup works); update = owner may not touch `role`/`roleProvisioning` (value-diff → unchanged-role retries pass), teacher `hasOnly(['challenges'])` branch verbatim; delete = false (no client path exists, U5). **(c) W3 APPLIED (C-29/#1c):** attempts create:false; student answers-update branch REMOVED (W1 callable precondition); **owner attempt-delete REMOVED (supersedes the W3 doc's "delete unchanged" line — banner added to the doc) — legal only because P3 resetProgress + P4 SERVER_RESET_PROGRESS ship first [C5-5]; closes C-31's anchor-erasure half**; teacher-of-record update + both reads unchanged. **(d)** noted in-file: M4 shadow→ENFORCE = server flag flip, not rules. **F4-3 provisioning (ships WITH P6):** NEW `provisionTeacher` callable (index.js, dormant behind `TEACHER_PROVISIONING_ENABLED=false`, reported by the version probe) — redeems a single-use admin-minted invite (`teacher_invites/{sha256(code)}`, all client access `if false`), transactional consume+role-flip+`roleProvisioning` provenance stamp (owner-immutable by (b)) + `teacher_provisioned` system_log; NEW `scripts/cs/create-teacher-invite.mjs` (David-run mint/--revoke, 160-bit code printed once, each mint = SUPPORT_RUNBOOK CS event); `Signup.jsx` Teacher radio REMOVED (the #1b hole) → everyone signs up student, optional invite-code field redeems post-signup (email + Google paths; full reload on success so AuthContext re-reads role). Rules-test MATRIX R1-R28 (Task 6; rules not executable here) + risks R1-R7/U1-U9 — top over-deny: the P4 fail-open hydration fallback now fails CLOSED on callable outage (R1) — in audit/deepfix/task3/P6_impl_notes.md. Checks: node --check index.js/foundation.js/create-teacher-invite.mjs OK; eslint Signup.jsx clean; eslint index.js = 29 pre-existing env false-positives, 0 new. |
| 2026-07-13 | src/services/progressService.js, src/pages/DailySessionFlow.jsx, src/pages/TypedTest.jsx, src/pages/MCQTest.jsx | **DEEPFIX Task 3 — P4 (FND-2) follow-up: closed Codex P6 R1 over-deny (BLOCKER; rules correct, fix is client-side).** The item-4 hydration route FELL THROUGH to the legacy client setDoc/updateDoc on resolver failure — which the P6 rules cutoff DENIES on class_progress/list_progress/progress_meta → a resolver outage would strand a live student with a RAW permission error at session entry (prior legacy_write_denied handler covered only COMPLETION, not entry hydration). FIX (4 parts): (1) `getOrCreateClassProgress` (progressService.js:111-129) under SERVER_PROGRESS_WRITE + self-caller now retries the resolver ONCE then FAILS CLOSED — throws typed `{code:'progress_resolver_unavailable'}`, NO client write; exact legacy fall-through preserved ONLY when flag OFF (byte-equivalent). (2) Entry-time catches in DailySessionFlow init (:872), TypedTest (:850) + MCQTest (:572) studyDay-derivation recognize the typed code AND raw permission-denied → controlled reload/retry message (setError/setGradingError/setSubmitError + stop submitting), not a raw Firestore error. (3) fail-closed path logs `progress_resolver_unavailable` (source getOrCreateClassProgress); DailySessionFlow logs `legacy_write_denied` for the raw-denial case only (resolver event already logged at source → no double-count). (4) Task-6 persona added to notes ("resolver-unavailable + missing/recon-needed doc under P6 → controlled reload + log, NOT raw permission failure"). Typed error propagates cleanly through initializeDailySession (studyService.js:159, verified no catch). @babel/parser(module+jsx) OK ×4; eslint delta vs HEAD = 0 new findings ×4. LOCAL-ONLY, flag dormant. Notes: audit/deepfix/task3/P4_impl_notes.md item 11 + U6-SUPERSEDED. |
| 2026-07-13 | src/services/progressService.js, src/pages/TypedTest.jsx, src/pages/MCQTest.jsx | **DEEPFIX Task 3 — P4 (FND-2) follow-up: closed Codex P6 round-2 P6-2 (BLOCKER) + P6-3 (MED).** **P6-2:** `getOrCreateClassProgressViaResolver` DISCARDED the resolveListProgress return and insisted on the launching `class_progress` doc → post-P5 canonical mode (list_progress exists, class_progress may not) became a FALSE `progress_resolver_unavailable`, stranding live students under P6. Verified the callable's exact return shapes in functions/foundation.js (canonical `{mode:'canonical',csd,twi,data}` :1204-1212; read-only `{mode:'legacy'|'none',...,launch,merged}` :1344-1357; `{mode:'quarantined',reasons}` :1366) before wiring. Adapter rewritten to branch on `result.data.mode`: canonical → read list_progress/{listId} LOCALLY (real Timestamps; fallback to serialized `data`) + SYNTHESIZE `{id:`${classId}_${listId}`,classId,listId,...canonicalData}`, NEVER fail on canonical success; quarantined → null (fail closed, safe); legacy → read launching class_progress as today, else synthesize from `launch.data`; catch → null (genuine resolver error). attempts kept class+list-scoped (P4 parity; list-scoped is a P5 follow-up, U16). **P6-3:** TypedTest + MCQTest studyDay-derivation catches now split isResolverDown vs isDenied and log `legacy_write_denied` on the raw permission-denied case (resolver event stays logged only at source — no double-count), matching DailySessionFlow. Task-6 personas added (canonical-success entry with no client write; resolver-unavailable controlled reload). Flag-off byte-equivalent (adapter runs only under SERVER_PROGRESS_WRITE). @babel/parser(module+jsx) OK ×3; eslint delta vs HEAD = 0 new findings ×3. LOCAL-ONLY, flag dormant. Notes: audit/deepfix/task3/P4_impl_notes.md items 12-13 + U6/U16. |
| 2026-07-13 | audit/playwright/lsr_deepfix_static.mjs (NEW), audit/deepfix/task5/HARNESS_BUILD_PLAN.md (NEW), audit/deepfix/task5/CODEX_RUNBOOK.md (NEW) | **DEEPFIX Task 5 — audit harness build STARTED: M-STATIC matrix built + run + orchestrator-verified (H1).** `lsr_deepfix_static.mjs` = the ONLY matrix runnable in this WSL (pure git/grep/dist; zero-dep ESM; no browser/Playwright/Admin/network/secrets). Phase-aware `--target=baseline|shipped`; hardcoded EXPECTED-STATE table (each row cites its FIX_PLAN/AUDIT_DESIGN source) compared to ACTUAL flag values read from functions/index.js + featureFlags.js + foundation.js FOUNDATION_FLAGS. Covers DG-1..4, CUT-1, RET-1..4. Fail-closed (nonzero exit on any FAIL/INVALID; SKIP surfaced not fatal); lsr artifact conventions (runId+git-state binding, findings/ JSON+md). **Results: baseline CLEAN 27/0/0/2** (2 SKIP = DG-2/DG-3, need deployed probe → Codex); **shipped correctly NOT_CLEAN 6/21** (proves phase-awareness). H1: independently confirmed ground truth — GRADE_TOKEN_ENFORCED=false (index.js:66, P0 disarm), 7 FOUNDATION_FLAGS + 5 client foundation flags + CONTINUATION_LINKS all false (dormant), all matches; assertions genuine (reads actual, compares to independent expected — not tautological). Known limitation: CUT-1 is a coarse source-grep proxy (co-located sink+guard literals ≠ proof of enclosure; marks INVALID not false-PASS when unprovable) — authoritative CUT-1 = the bundle grep + M-UI behavior (Codex). Carryforward: DG-4 git-half asserts the #11 files have history, NOT that the P0 disarm is a committed sha — ship-time DG-2/DG-3 must bind the DEPLOYED sha to a committed state (all foundation edits are uncommitted working-tree per the no-commit rule). Artifacts: audit/playwright/findings/deepfix_static_a967f54-{baseline,shipped}.{json,md}. LOCAL-ONLY, no source modified. |
| 2026-07-13 | scripts/cs/create-lap2-lists.mjs (NEW), scripts/cs/assign-lap2-lists.mjs (NEW) | **CS (David-authorized live write) — created (LAP2) copies of the 26SM tier lists.** `create-lap2-lists.mjs --commit` (dry-default, idempotent, count-verified) cloned Ascent `dVliNv0p…` → **`(LAP2) 26SM VZIP 3K (Ascent, 1600)` = list `7ybGZUS7S9CjjVMyK1hH`** (1600/1600 words verified) and Summit `AObYOowh…` → **`(LAP2) 26SM VZIP 3K (Summit, 800)` = list `alo8NR5sksoQ1eYOE9X6`** (800/800 verified). Owner = veterans@ (9OcxdnYC, same as originals), visibility private, `clonedFrom`/`lapCopy`/`lapOf` provenance; word docs copied verbatim (positions/definitions intact). Purpose: NON-destructive "start over" for #11 list-end finishers (esp. the Ascent+Summit finishers) instead of a destructive reset. Additive only — no existing list/student/class mutated. `assign-lap2-lists.mjs` (dry-default, backs up each class to scripts/cs/lap2_class_backups/, mirrors the class's existing Ascent/Summit props + backdated assignedAt so no focus auto-switch) then COMMITTED (David: "go", Bridge classes excluded per follow-up): **assigned both LAP2 lists to 14 26SM adv/fin/top classes** (8 Adv + 3 Final + 3 TOP; all Bridge incl. `26SM SAT Bridge (TOP)` excluded), mirrored props + backdated `assignedAt` (2026-05-28, no focus auto-switch), 14 per-class backups → `scripts/cs/lap2_class_backups/`. Verified: idempotent re-dry = all 14 ALREADY; 3 finisher classes carry both LAP2 lists. See SUPPORT_RUNBOOK CS-2026-07-13g. |
| 2026-07-13 | src/config/featureFlags.js, src/services/studyService.js, functions/foundation.js, src/services/db.js, src/pages/DailySessionFlow.jsx, src/pages/MCQTest.jsx, src/pages/Dashboard.jsx, src/pages/ClassDetail.jsx, src/components/.../SessionSummaryCard.jsx | **DEEPFIX Task 3 — P9 (CYC) cycling capstone IMPLEMENTED as a dormant DOUBLE-gated draft (LOCAL-ONLY; no commit/branch/deploy).** Per x/plan v5: NEW global `CYCLING_ENABLED=false` (client featureFlags.js + server foundation.js) AND per-assignment `cyclingEnabled` — both required. studyService: `isCyclingActive`/`computeLapView`/`resolveVirtualRange` (wraps `positions[i mod cycleLength]`, cycleLength=positions.length); allocation cap removed under cycling (non-cycling keeps `Math.min(newWords,wordsRemaining)`); `getUnmasteredPool`/`getFailedFromPreviousNewWords` lap-bound (SINGLE review mechanism, batch-clear DROPPED); returns inert cyclingActive/cycleLength/lapView. foundation.js: `cyclingAllowed()` gate + **lap-modular M4 clamp** (lap-2 not anchor_rejected) + completeSession/advanceForChallenge lap-aware. db.js: `cyclingEnabled` assignment write. UI: lap-aware introduction-progress + "Lap N" badge + capability-gated "Start over" + teacher toggle. **Orchestrator H1: flag-off byte-equivalence gate VERIFIED in the 2 live-path files** (studyService.js:66-67, foundation.js:181-182 short-circuit before any behavior change); deep correctness → Codex. Validation: parser OK ×9; eslint delta=0 vs reconstructed pre-P9 baseline; phase9_diff.patch git-apply-clean + round-trip verified. → handed to Codex round 1 (baton rev 44). Notes/U1-U12: audit/deepfix/task3/P9_impl_notes.md. |
| 2026-07-14 | src/services/studyService.js, src/pages/TypedTest.jsx, src/pages/DailySessionFlow.jsx, src/pages/Dashboard.jsx, src/pages/ClassDetail.jsx | **DEEPFIX Task 3 — P9 (CYC) Codex round-1 fold (4 findings, still dormant/LOCAL-ONLY).** **P9-1 [BLOCKER]:** cycling was DEAD when enabled — `cyclingEnabled` never reached `initializeDailySession` (curated settings obj dropped it). Fixed at root: init now SELF-RESOLVES via new `resolveEffectiveCycling(userId,listId)` (short-circuits `if(!CYCLING_ENABLED) return {enabled:false}` BEFORE any read → flag-off adds NO read); old `isCyclingActive` removed. **P9-2 [HIGH]:** §3b cross-class unlock — pure `deriveEffectiveCycling(studentClasses,listId)` (any enrolled class w/ cyclingEnabled unlocks; returns source class) + `resolveEffectiveCycling` reusing existing `fetchStudentClasses`; "cycling enabled via {className}" affordance in session badge + Dashboard hero; ClassDetail uses class-own flag OR `twi>cycleLength` demonstrably-cycling proxy. **P9-3 [HIGH]:** canonical modulus — new `getCycleLength(listId)=positions.length` (getCountFromServer orderBy position, same source resolveVirtualRange wraps) now used for review/failed lap-bounds, lapView, AND Dashboard/ClassDetail denominators; wordCount no longer the modulus. **P9-4 [MED]:** TypedTest self-resolves + passes cyclingActive to getNewWords (MCQ parity). Orchestrator H1: flag-off byte-equivalence VERIFIED — service resolver short-circuits before read; Dashboard (:653/:1108/:1715/:2099) + ClassDetail (:91/:287/:429) gate every cycling path on CYCLING_ENABLED. Validation: NEW harness audit/deepfix/task3/p9_assert.mjs 15/15 PASS; parser ×10; eslint delta 0 vs reconstructed pre-P9 baseline; phase9_diff.patch regenerated (+623/−66, git-apply-clean+round-trip). → Codex round 2 (baton rev 46). |
| 2026-07-14 | functions/foundation.js, src/pages/Dashboard.jsx, src/pages/ClassDetail.jsx | **DEEPFIX Task 3 — P9 (CYC) Codex round-2 fold (cross-class made server-consistent per David; still dormant/LOCAL-ONLY).** **P9-5 [BLOCKER]:** cross-class cycling was CLIENT-only — server (M4/completeSession/advanceForChallenge) checked only the launching class → client/server disagree → a cross-class cycler gets anchor_rejected or zeroed. Fixed: NEW `resolveEffectiveCyclingServer(studentId,listId)` (foundation.js:191) mirrors client `deriveEffectiveCycling` — reads `users/{uid}.enrolledClasses` → `getDb().getAll` class docs → unlocked iff any `assignments[listId].cyclingEnabled===true` (returns source class); `if(!CYCLING_ENABLED) return {enabled:false}` FIRST (no read when off); pre-transaction read; fails closed. Threaded into all 3 legs (`:847`/`:993`/`:1742`), REPLACING launching-class-only `cyclingAllowed(assignment)` (removed, grep-clean). **P9-6 [HIGH]:** Dashboard `resolveContinuation` yield now gated on `CYCLING_ENABLED && deriveEffectiveCycling(studentClasses,current.id).enabled` (was current-class only → could auto-yield an effectively-cycling list). **P9-7 [MED]:** ClassDetail exact-boundary (twi===cycleLength) cross-class false-negative DOCUMENTED (teacher grid has no cheap per-student cross-class data; strict `cyclingEnabled || twi>cycleLength` proxy kept). Orchestrator H1: flag-off zero-added-reads VERIFIED (foundation.js:192 short-circuit; cyclingAllowed grep-clean). Validation: harness p9_assert.mjs 21/21 (incl. client↔server consistency); parser ×10; eslint delta 0; phase9_diff.patch regenerated (git-apply-clean+round-trip). → Codex round 3 (baton rev 48) + baton-watch relaunched. |
| 2026-07-14 | functions/foundation.js, functions/index.js, src/config/featureFlags.js, src/services/db.js | **DEEPFIX Task 3 — P10 (OVR) parts (a)+(b) IMPLEMENTED as a dormant DOUBLE-gated draft (LOCAL-ONLY; decision-independent core only — (c) read-surface + (d) rules-narrowing DEFERRED pending owner U1/C-28 decisions).** Shared spine: `runChallengeDayAdvanceTxn` (foundation.js:1681) EXTRACTED verbatim from advanceForChallenge's inline tx (clamp + phase-gate now in one place; advanceForChallenge:1899 rewired, still flag-gated :1826, byte-identical); `assertOverrideAuthz` (:1765) = the I-10 §6 UNION (teacher AND (stamp teacherId===caller OR current-enrollment owner via renameStudent pattern); unrelated teacher denied). **(a)** NEW `overrideAttempt` (:2090) behind `SERVER_OVERRIDE_ENABLED=false` (:98) — in-product manual-pass: writes full valid anchor (nwsi/nwei/wordsIntroduced/testId), advances via shared helper, audit-logs `teacher_override`, union authz. **(b)** NEW `reviewChallenge` server port (:1949) behind `SERVER_REVIEW_CHALLENGE_ENABLED=false` (:93) — faithful port of client legs (answer-flip/score+newPassed with persisted-totalQuestions denom/challenges.history/study_states PASSED) + day-advance via shared helper, union authz, idempotency, two-hop (U3); client db.js:2741 routes under SERVER_OVERRIDE; client featureFlags SERVER_OVERRIDE=false, client overrideAttempt wiring (db.js:2980, dormant). Orchestrator H1: both callables throw failed-precondition first-statement (inert off); advanceForChallenge refactor structurally faithful + gated. Validation: parser ×4; eslint delta 0 vs reconstructed pre-P10 baseline; phase10ab_diff.patch git-apply-clean + round-trip cmp-clean. → Codex round 1 (baton rev 50) + baton-watch (baseline 50). Notes/U1-U9: audit/deepfix/task3/P10_impl_notes.md. |
| 2026-07-14 | functions/foundation.js | **DEEPFIX Task 3 — P10(a) Codex round-1 fold: closed P10-1 (BLOCKER, authz hole; still dormant/LOCAL-ONLY).** `overrideAttempt` authorized one subject but wrote a reconciliation-authoritative anchor for a REQUEST-supplied (studentId/classId/listId/studyDay) target — forgeable for an unowned student/class/list (assertOverrideAuthz leg-ii accepts "owns ANY enrolled class"). Fixed via two-path TARGET-BINDING: (1) attemptId path (`:2153-2179`) derives the write target FROM the loaded attempt, rejects any conflicting request field (`invalid-argument`), authz=assertOverrideAuthz(loaded attempt); (2) orphan path (`:2180-2199`) uses NEW `assertOverrideTargetAuthz` (`:1808`) = teacher ∧ owns EXACT classId ∧ student enrolled in THAT classId ∧ class assigns THAT listId (NOT the broad any-enrolled-class leg). Anchor build + docId (`:2205-2235`/`:2221-2225`) now consume the target-bound `t*` vars → authorized subject === written subject. `assertOverrideAuthz`/`reviewChallenge` untouched. Orchestrator H1-verified (read both regions); flag-off byte-equivalent (`:2134` first-statement throw). Validation: parser ×4, eslint delta 0, phase10ab_diff.patch git-apply+round-trip cmp-clean. → Codex round 2 (baton rev 52) + watcher. |
| 2026-07-14 | src/config/featureFlags.js, functions/foundation.js, functions/index.js, src/services/db.js, firestore.indexes.json, scripts/cs/deepfix-migrate-attempts-teacherids.mjs (NEW) | **DEEPFIX Task 3 — P10 part (c) read-surface widening IMPLEMENTED as a dormant DOUBLE-gated draft (LOCAL-ONLY; David decision U1=A teacherIds-array denorm+reindex+--dry backfill).** The C-19 fix: a promoted student's old-teacher-stamped attempts now show in the new teacher's gradebook. NEW flags `TEACHER_IDS_READ=false` (client: read query + ex-roster filter + client write-stamp) + `TEACHER_IDS_WRITE_ENABLED=false` (server write-stamp). Shared `computeTeacherIdsForAttempt` (foundation.js:1875, client twin db.js:1198) = stamp ∪ owners of the student's currently-enrolled classes that ASSIGN the attempt's listId (list-scoped); server helper returns null with ZERO reads when off. Write-stamped (additive, flag-gated) at writeAttemptTxn (index.js:426/:456), writeUpgradedReviewMarker (foundation.js:797), overrideAttempt (:2300), reviewChallenge arrayUnion re-stamp (:2120), client submit paths (db.js:1324/:1483). Gradebook query widens to `where('teacherIds','array-contains',uid)` (db.js:2076, flag-off = today's teacherId== verbatim), ex-roster name-filter hard-empty fixed via bounded cached union-roster (:1937). +4 teacherIds composite indexes (firestore.indexes.json:877/891/909/927). `--dry` backfill migration (P5-style: --commit+--confirm guard, per-batch backups, read-back verify, idempotent via teacherIdsBackfilledAt+set-union). Disjunction budget NOT exceeded (array-contains = DNF factor 1; C-33 guard unchanged). Orchestrator H1: flag-off byte-equivalence + zero-read VERIFIED (server :1876 short-circuit; client stamps gated :1324/:1483; query :2076; ex-roster :1946). Validation: parser OK; eslint delta 0; --dry ran WRITE-FREE on live 26SM (400 in-scope, 0 writes; --commit refused w/o --confirm); phase10c_diff.patch git-apply+round-trip. firestore.rules NOT touched (part d). → Codex round 1 (baton rev 54) + watcher (54). Notes/uncertainties: audit/deepfix/task3/P10c_impl_notes.md. |
| 2026-07-14 | firestore.rules | **DEEPFIX Task 3 — P10(c) Codex round-1 fold: closed P10c-1 (BLOCKER; still dormant/LOCAL-ONLY).** The widened `array-contains teacherIds` gradebook query was unreadable under today's attempts rule (`studentId==uid \|\| teacherId==uid`) — the I-10 §4 query-vs-rules same-release problem (my earlier "leave rules for part d" scoping was wrong for the READ widening). Fixed by adding ONE additive OR clause to the `match /attempts` read rule (`:190-194`): `('teacherIds' in resource.data && request.auth.uid in resource.data.teacherIds)` — existence-guarded (pre-backfill attempts without the field don't error → inert until backfill), grants EXACTLY `uid ∈ teacherIds`, pure WIDENING. NO narrowing added (isTeacher→isOwner breadth, attempts teacher-UPDATE, study_states, custom-claim all remain part (d)). Deploy-order corrected (P10c_impl_notes §6): indexes → rules(additive read, safe anytime) → --dry/--commit backfill → flip TEACHER_IDS_READ; the flag must NOT flip until the widened rule is deployed. Orchestrator H1-verified the clause (:190-194: guarded, exactly uid∈teacherIds, pre-existing clauses intact, zero create/update/delete change). Validation: parser OK; eslint delta 0 (JS unchanged bar one comment); phase10c_diff.patch regenerated (7 files incl. firestore.rules; git-apply+round-trip+reverse-apply clean). → Codex round 2 (baton rev 56) + watcher (56). |
| 2026-07-14 | firestore.rules, functions/index.js, scripts/cs/deepfix-backfill-teacher-claims.mjs (NEW) | **DEEPFIX Task 3 — P10 part (d) the SECURITY POINT-OF-NO-RETURN: custom-claim role model (David role=A) + rules NARROWING, as a STAGED dormant LOCAL-ONLY draft.** `isTeacher()` (firestore.rules:104-106) → `request.auth.token.role == 'teacher'` (was doc-read) — fail-closed, re-keys ~15 rules AT ONCE (blast-radius table in P10d_impl_notes). NARROWINGS: users-subcollection WRITE `isTeacher→isOwner` (:184, READ unchanged); attempts teacher-UPDATE branch REMOVED → `allow update: if false` (:282) — both safe because reviewChallenge/overrideAttempt are now server-side (Admin SDK bypasses rules), each with a documented SERVER_OVERRIDE / server-flag ROLLBACK COUPLING. Custom claim: `provisionTeacher` (functions/index.js:2058-2068) mints `setCustomUserClaims(uid,{role:'teacher'})`, gated on NEW `TEACHER_CLAIM_ENABLED=false` (:1965), fail-closed (throws internal on failure, retry re-attempts claim only, never re-burns invite), surfaced in version probe (:2130). NEW `--dry`-only backfill `scripts/cs/deepfix-backfill-teacher-claims.mjs` (P5-style: write-guard throws in --dry, --confirm-claims sentinel, MERGE-set preserves other claims, read-back verify). Transition-safety = Option A (HARD PRECONDITIONS D1-D4 in the rules header: claim-set live → backfill → token-refresh window → rules deploy; teachers lose all access if deployed before backfill), NOT Option B (claim-OR-doc window, documented inline for David). STAGED: rules deploy only at the P10 cutover (like P6); claim-set dormant. Orchestrator H1-verified (isTeacher fail-closed; narrowings server-path-safe; claim-set gated+fail-closed; backfill --dry write-guarded). Validation: parser OK; eslint delta 0; brace/paren balance {}=49/49 ()=101/101; phase10d_diff.patch git-apply+round-trip+reverse clean. (c)'s teacherIds read clause untouched. → Codex round 1 (baton rev 58) + watcher (58). Notes/U1-U8: audit/deepfix/task3/P10d_impl_notes.md. |
| 2026-07-14 | firestore.rules, functions/index.js | **DEEPFIX Task 3 — P10(d) Codex round-1 fold (still STAGED/dormant/LOCAL-ONLY).** **P10d-1 [HIGH]:** the users/{userId} teacher `challenges` update branch was still open (any claim-teacher could mutate any student's challenges). Grep-confirmed dead (the only client teacher-write to a student's challenges.history was `reviewChallenge` db.js:2945, now server-side under SERVER_OVERRIDE; `submitChallenge` db.js:2803 is the STUDENT/owner writing own → owner branch, unaffected). Fixed: removed the `isTeacher()&&hasOnly(['challenges'])` OR-leg from `match /users/{userId} allow update` (firestore.rules:156-159) → owner-only, KEEPING the `!hasAny(['role','roleProvisioning'])` exclusion; SERVER_OVERRIDE rollback-coupling comment (revert with the attempts-update/study_states narrowings). Header updated: FOUR narrowings; blast-radius corrected ~15→~14 rules (this branch DROPS isTeacher() rather than re-keying — 14 call-sites enumerated). **P10d-2 [MED]:** `provisionTeacher` REPLACED custom claims → now MERGES (functions/index.js:2063-2064: `getUser(uid)` then `{...(customClaims||{}),role:'teacher'}`), matching the backfill; one Auth read only inside the dormant TEACHER_CLAIM_ENABLED block; fail-closed throw kept. Orchestrator H1-verified both. Validation: parser OK; rules balance {}=49/49 ()=94/94; isTeacher call-sites=14; eslint delta 0; phase10d_diff.patch regenerated (git-apply+round-trip+reverse clean). (c)'s teacherIds read clause intact. → Codex round 2 (baton rev 60) + watcher (60). |
| 2026-07-13 | scripts/cs/scan-blocked-recent-login.mjs (NEW) | **CS read-only scanner** — lists 26SM students who logged in on/after a cutoff (Firebase Auth `lastSignInTime`, KST) AND are BLOCKED from advancing on their ACTIVE list (the list of their most-recent attempt). Blocked = #11 review-only completion-gate wall (list-end twi≥size, or throttle interv→1.0) OR impossible session_state (review-study + new-test-not-passed w/ no day anchor). Active-list = most-recent-attempt list (NOT greatest-twi/primaryFocus — those false-flag students who finished an early list and moved on). NO writes. Usage: `NODE_PATH=/app/node_modules node scripts/cs/scan-blocked-recent-login.mjs [classRegex=26SM] [cutoff=2026-07-12]`. |
| 2026-07-13 | scripts/cs/sweep-crossclass-undercount.mjs (NEW) | **CS read-only sweep** — finds 26SM students CREDITED LESS THAN EARNED (박혜린/Kaila family): a day D fully complete on a list (passed new AND passed review both exist, any class) but the ACTIVE class_progress.csd < D → reconciliation loops them on "Day complete!" and blocks advance. Root: with LIST_SCOPED_RECON=true the anchor is furthest word-position (student+list), but the review-pairing that confirms the anchor day fails when new/review live in different classes → csd=anchorDay-1. Reports gap, multiClass, anchorBehind, last activity/login. NO writes. `NODE_PATH=/app/node_modules node scripts/cs/sweep-crossclass-undercount.mjs [classRegex=26SM]`. |
| 2026-07-14 | dsg-edits/edit-t13-m2l.mjs (NEW), dsg-edits/DEPLOY_LOG_26SM_T13.md (NEW) | **26SM-T13 dsg-edit (live question bank):** applied 2 remaining M2L edits — M2L-Q2 (118935) Choice D variation→convergence; M2L-Q9 (118928) Passage Type Humanities→Science. Backup + reload-verify each; keys unchanged. The 3 M01 items (M01-Q2/Q9/Q12) were already applied by a prior editor (verified). All 5 field+key ✅. Re-auth'd expired session. See DEPLOY_LOG_26SM_T13.md. |
| 2026-07-14 | scripts/cs/scan-throttle-stuck.mjs (NEW), scripts/cs/fix-jisusu-throttle.mjs (NEW) | **CS #11 throttle:** scan-throttle-stuck = READ-ONLY 26SM sweep for the throttle review-only deadlock (interv→0 newWordCount + wordsRemaining>0 + actively re-taking the frozen-day review); separates list-end (different fix). fix-jisusu-throttle = Junseo-style manual #11 completion for 정지수 (csd 5→6, recentSessions day6 rev 0.97, interv→0.65, session cleared). Both derived/verified values; sweep CLEAN before/after. See SUPPORT_RUNBOOK CS-2026-07-14. |
| 2026-07-14 | scripts/cs/fix-csd-undercount.mjs (NEW) | **CS off-by-one csd corrector** — pace-aware (target csd=twi/pace, position-space) fix for the "completed Day D but csd frozen at D-1" reconciliation review-pairing bug. Guards reject cross-pace/carry/list-end/mid-day. Fixed 4: 김우주, 김건우, 문서윤, 박혜린 (all non-demoting csd bumps + session_state clear). Backups in scripts/cs/backups_csd/. See SUPPORT_RUNBOOK CS-2026-07-14b. |
| 2026-07-14 | scripts/cs/batch-advance-listend-0714.mjs (NEW) | **CS list-end batch-advance (refined):** config-only primaryFocus→next assigned list (Base→Ascent→Summit) for list-end finishers active last 7d; writes only real changes (skips already-pinned), broadened window catches currently-stuck. Advanced 52 (incl. 송유주, 정윤서 → Summit). See SUPPORT_RUNBOOK CS-2026-07-14c. |
| 2026-07-14 | functions/foundation.js, functions/index.js | **Deepfix Task-6 audit fix (modular firestore imports):** M-CALL emulator matrix crashed with `Cannot read properties of undefined (reading 'now'/'serverTimestamp')` — the firebase functions-EMULATOR stubs `admin` and drops the `admin.firestore.FieldValue`/`.Timestamp` namespace statics (works in prod; not under emulator). Converted BOTH files to modular `const {FieldValue, Timestamp} = require("firebase-admin/firestore")` + replaced all 35 namespace-static uses (foundation ×16, index ×19). PROVEN byte-equivalent: `FieldValue===admin.firestore.FieldValue`, `Timestamp===admin.firestore.Timestamp` (same refs in pinned 13.6.0), serverTimestamp sentinels JSON-equal; `admin.firestore()` db-handles untouched; `node --check` OK both. Prod behavior bit-identical. Also closes the deprecated-compat-API v14 risk. See audit/deepfix/task6/DIAGNOSIS_emu-r1_M-CALL.md (Round-2 update). |
| 2026-07-14 | audit/playwright/lsr_ui.mjs, lsr_deepfix_ui.mjs, lsr_deepfix_whitebox.mjs | **Deepfix harness portability (browser matrices → native Windows):** replaced hard-coded WSL paths so M-UI/M-WB run on the Windows executor. `lsr_ui.mjs` had import-time `/app` reads (SEEDED/WM top-level) that threw for ANY Windows importer; the two deepfix browser matrices hard-coded `AUD='/app/audit/playwright'` + git `{cwd:'/app'}`. Now derived repo-relative via `dirname(fileURLToPath(import.meta.url))` (+`REPO=resolve(HERE,'..','..')`). Verified byte-identical in WSL (AUD→/app/audit/playwright, REPO→/app) so existing lsr_* scripts unaffected; `node --check` OK all three; zero `/app` literals remain. Seed/provision chain (lsr_provision/clone_lists/etc.) NOT touched — deferred to the calibrate loop (only needed if a fresh 25WT seed is required). |
| 2026-07-14 | audit/playwright/lsr_deepfix_static.mjs | **Deepfix M-STATIC portability (native Windows):** Windows executor selfcheck returned NOT_CLEAN (23/4) with 9× "The system cannot find the path specified." — WSL baseline is CLEAN 27/0. Cause: POSIX shell-outs absent on native Windows: `treeGrep` used `grep -rnF … 2>/dev/null` (RET-1/DG-4b/CUT-1b), dist enum used `ls -1 … | head -1`, DG-4 git-log used POSIX single-quoted path `-- '${fp}'`. Rewrote all three Node-native (no shell): treeGrep → recursive readdirSync + literal line scan; dist → readdirSync; git-log → execFileSync arg array. Verified: `node --check` OK, WSL re-run STILL CLEAN 27/0 (no regression), node-native treeGrep finds 91 class_progress refs (was 0 on Windows). Handed back for Windows re-run confirmation. |
| 2026-07-14 | audit/playwright/lsr_reviewonly_fb.mjs | **Deepfix M-UI portability (native Windows):** first real M-UI run (round 3) died at setup init — Admin SDK opened hard-coded `/app/scripts/serviceAccountKey.json` (→ `c:\app\...` on Windows, ENOENT). Made the key path repo-relative via `import.meta.url` + `LSR_SA_KEY` env override (`const KEY = process.env.LSR_SA_KEY || resolve(REPO,'scripts','serviceAccountKey.json')`). Verified: node --check OK; WSL resolves to /app/scripts/serviceAccountKey.json (byte-identical); whole M-UI import chain (deepfix_ui→ui/deepfix_fb→reviewonly_fb/teacher) re-scanned /app-clean. The admin.firestore.Timestamp/.FieldValue statics here are fine (plain Node, not the functions-emulator that strips them). |
| 2026-07-14 | audit/playwright/lsr_deepfix_net_helpers.mjs (NEW), audit/deepfix/task6/M_NET_DESIGN.md (NEW) | **M-NET degraded-network resilience matrix (David request):** built the reusable degradation helpers (withOffline / withSlow[CDP bad-3G] / withFailOnce / withFailAll + NET_PATTERNS) + the design (5 seed-to-chokepoint scenarios NET-1..5: submit under offline/slow/fail-once, completion under offline, dashboard-recon under slow+fail; Admin-SDK white-box post-check oracle; short runs via seed-to-chokepoint per David). Runner (lsr_deepfix_netresilience.mjs) deferred until the shared M-UI setup/interaction primitives calibrate (M-NET reuses them) — avoids building scenario bodies on un-calibrated login/join/drive. Helpers portable + node --check OK. |
| 2026-07-14 | audit/playwright/lsr_deepfix_ui.mjs | **M-UI calibration (r5→r6): stale primaryFocus fix.** First real M-UI run cleared setup but both RA scenarios failed — student session focused "LSR TOP Vocab" not the assigned "LSR Base Camp" (RA1 oracle csd/reviewAttempts flat; RA2 no-submit). Cause: shared sandbox students (lsr_s41/42) carry a cross-class primaryFocus pin from prior audits; resetStudentState is scoped to the assigned class/list so it doesn't clear it → dashboard shows a foreign single-list focus selectList can't switch. Fix: in the shared scenario runner, after joinClass, FB.setPrimaryFocus(focusListId=LIST.id) to pin the assigned list + goDashboard reload before selectList. node --check OK. |
| 2026-07-14 | audit/playwright/lsr_deepfix_ui.mjs | **M-UI calibration (r6→r7): focus-pin reliability.** r6: setPrimaryFocus pin took for RA1 (focus mismatch gone) but NOT RA2 (still "TOP Vocab") — a client-pickup race (goDashboard soft-nav kept RA2's stale focus). Changed the post-pin `goDashboard` to a settle (1.2s) + HARD `page.reload({waitUntil:'domcontentloaded'})` + 1.5s so the client re-reads users/{uid}.settings deterministically for both students. (Separately: RA1's next blocker is a "Session menu" wait — verified the aria-label is UNCHANGED in SessionMenu.jsx:92, so it's a review-only FLOW issue, not a locator drift; requesting a stall screenshot+DOM next round to diagnose.) node --check OK. |
| 2026-07-14 | audit/playwright/lsr_deepfix_ui.mjs | **M-UI calibration (r7→r8): review-only completion recognition.** r7 (via screenshot): focus-pin fix VALIDATED for both RA1+RA2; the RA1 "Session-menu stall" is actually a "DAY 5 COMPLETE / Great Job!" success summary — driveReviewOnlyDay assumed a gradeable test (enter session→readTestRows→submit) but a review-only day (#11) completes straight to the day-summary (no in-session screen, no test). Added dayCompleteVisible() (Day N Complete / Great Job! / Session Summary) and made driveReviewOnlyDay recognize it as outcome='results' when enterReviewSession reports reached:false — then the EXISTING assertComplete oracle (csd+1/twi-flat) verifies correctness (flow fix, not a mask). node --check OK. |
| 2026-07-14 | audit/playwright/lsr_deepfix_ui.mjs | **M-UI calibration (r8→r9): rule out write-abort on review-only completion.** r8: oracle now runs; RA1/RA2 review-only day visibly completes (correct list, flag-off) but csd flat (4→4) + reviewAttempts not +1. Grounding: the automarker IS written flag-off (DailySessionFlow.jsx:1062 legacy client path; SERVER_REVIEW_MARKER only changes mechanism) + csd advance ungated → flag-off SHOULD persist both. Raw log showed Firestore Write-channel ERR_ABORTED → my dayCompleteVisible branch navigated away (goDashboard) before the async completion writes committed. Added waitForLoadState('networkidle',8s)+sleep(2500) before navigating so writes can land. If csd/reviewAttempts then persist = harness timing (fixed); if still flat = a REAL #11 completion-persistence finding (escalate to David). node --check OK. |
| 2026-07-14 | audit/playwright/lsr_deepfix_ui.mjs, lsr_ui.mjs | **M-UI: concurrency + r11 fixes.** (1) CONCURRENCY: run-loop now batches scenarios via Promise.all gated by DFX_CONCURRENCY (default 1 = sequential/byte-equiv); each scenario already pulls a distinct round-robin student + own class + browser context, so N-at-a-time is collision-free with SL_STUDENTS≥N*2 — the parallelism speed lever David asked for. (2) RS-1: removed the upper-bound "filler leaked" count check — the reused deep student accumulates attempts across un-swept classes (all rows ARE the deep student per r11 screenshot); server-side filter proven by the deep student surfacing on page 1 at all. (3) RO-S1/RA2 "Continue never appeared": wired a shot() on that flow-gap (lsr_ui.mjs:356) to capture the new-word results screen (drifted Continue label suspected). node --check OK both. |
| 2026-07-14 | audit/playwright/wordmap.json | **M-WB/M-UI high-leverage fix: missing "neutral" answer.** M-WB landscape (r13): 4/6 scenarios + RO-S1 (r12) all failed on ONE root cause — the answer-driver left "neutral" blank → 67% (2/3) on the Base-Camp day-1 set → below the 92% pass gate → app correctly refused to advance (csd flat / Submit disabled / no Continue). Cause: `wordmap.json` was missing "neutral" (scheme/agenda present). Added `neutral: {ko:"중립, 어느 편도 들지 않는", def:"..."}`. Unblocks W-RA3g/W-RA4/W-RA4b/CUT-5 (M-WB) + RO-S1 (M-UI). NOT an app bug — app-correct sub-92% refusal. |
| 2026-07-15 | scripts/cs/fix-throttle-reset.mjs (NEW), scripts/cs/fix-throttle-unstick.mjs (NEW) | **CS #11 throttle wave:** fix-throttle-reset = reset recentSessions→3×0.5 baseline + interv 0.556 + clear session (current review-only day re-allocates new words; ≥50% review sustains). Guard: only genuinely-throttled. Fixed 16 (all today's throttle tickets incl. 진현수/Final B). fix-throttle-unstick = earlier csd-advance variant (used on 유아현/임서진; superseded by reset). Backups in backups_throttle/. See SUPPORT_RUNBOOK CS-2026-07-15. |
| 2026-07-15 | scripts/cs/scan-throttle-stuck.mjs | Added `--emails` mode (prints all throttle-candidate emails, one per line) to pipe directly into fix-throttle-reset. Follow-up sweep reset 18 more throttled students; post-sweep throttle candidates = 0. |
| 2026-07-15 | (data) scripts/cs/fix-throttle-reset.mjs --commit | Third throttle sweep (David): reset 3 more (s22280753, limssol90, 이서현/emily1004enfj INT B3). Post-sweep throttle candidates = 0. Running total this incident: 37 students. |
| 2026-07-15 | (data) scripts/cs/fix-throttle-reset.mjs --commit | Throttle sweeps 4-5 (TA batch + regen): reset 8 (홍윤성,하승현,4×Bridge TOP,조준모,윤규보) + 이지현(tigeregina00) + 11 regen (incl. 유지민/kayliejyoo). Running total ~57. Wave regenerating ~11/scan. |
| 2026-07-15 | scripts/cs/reset-challenge-tokens.mjs (NEW) | Blanket challenge-token reset to 5 for 26SM (David). Backdates active rejected challenges.history entries' replenishAt (history preserved; no HMAC so honored client+server). 69/824 were <5 (4 at 0); all→5, verified. See SUPPORT_RUNBOOK CS-2026-07-15c. |
| 2026-07-15 | audit/playwright/lsr_deepfix_migrate_audit.mjs | **M-MIG seed bug: dotted-key set().** First full M-MIG audit died in seed — seedDualDocStrand → assertSandboxTriple REFUSED ("list not assigned to class"). Root: provisionMigClass wrote `set({[`assignments.${listId}`]: assignment}, {merge:true})` — Firestore set() treats a dotted key as a LITERAL field name (not a nested path, unlike update()), so `assignments[listId]` was never set → the guard correctly refused. Fixed → `assignments: {[listId]: assignment}` (nested-object literal; merge preserves siblings). Harness-specific (browser assigns via app UI, correct); RS-3 similarity coincidental. node --check OK. |
| 2026-07-15 | scripts/cs/{deepfix-migrate-list-progress,deepfix-migrate-attempts-teacherids,manual-pass,data-integrity-sweep,deepfix-census2}.mjs | **M-MIG portability: CS-script key paths.** r18: seed fix worked (M-MIG runs, fail=0), but the migration --dry subprocess (deepfix-migrate-list-progress.mjs:146) crashed ENOENT on hard-coded `/app/scripts/serviceAccountKey.json` (round-3 déjà vu; round-4 fix only covered lsr_reviewonly_fb.mjs) → 6 RUN-* legs + MIG-1..5 oracles INVALID (blocked, not failing). Fixed all 5 CS scripts the audit can invoke: key path → `process.env.LSR_SA_KEY || new URL('../serviceAccountKey.json', import.meta.url)` (self-contained repo-relative + env override; readFileSync accepts URLs; byte-identical in WSL). node --check OK all 5. |
| 2026-07-15 | audit/playwright/lsr_deepfix_netresilience.mjs (NEW) | **M-NET runner built (David's resilience ask).** Wired the degraded-network matrix runner reusing the calibrated M-UI setup (login→createClass→assignList→join→focus-pin) + submit primitives (driveNewWordsToTest/readTestRows/carefulAnswersFrom) + the degradation helpers (lsr_deepfix_net_helpers withOffline/withSlow/withFailOnce) + Admin-SDK oracle (readAttempts before/after). 3 submit-chokepoint scenarios NET-1 (offline blip → exactly-1 attempt synced, no dup/loss), NET-2 (slow-3G → eventual 1 attempt, no false-fail), NET-3 (one-shot write failure → retry to exactly 1, idempotent). Fixed self-inflicted `lsr_*/25WT` premature block-comment close + assignList opts-object signature. node --check OK. |
| 2026-07-15 | audit/playwright/lsr_deepfix_netresilience.mjs | **M-NET student auto-provision.** r20/r21: M-NET died at uid precondition — the seeded lsr_s* Auth pool is non-contiguous (s51-53, s136-138 don't exist) and the only proven-existing accounts (s41-50, s130-135) are polluted on the base clone (no clean+existing student left). Fix: M-NET now creates a fresh sandbox Auth account (admin.auth().createUser with the audit password) when uidByEmail returns null → genuinely-clean day-1 for the resilience oracle. Sandbox lsr_* only (identity-guarded). node --check OK. |
| 2026-07-15 | src/pages/Signup.jsx (reverted to HEAD) + audit/deepfix/task3/Signup.p6.jsx (new snapshot) | **F1 excluded from the initial release (Codex end-review-v2 BLOCKER, verified).** The Signup teacher-onboarding redesign (invite-code → provisionTeacher) was ungated in the tree, but provisionTeacher is gated by TEACHER_PROVISIONING_ENABLED=false — which by design (functions/index.js:1960-1961) flips only in the P6 release train. Shipping F1 now → a new teacher's invite hits a dormant callable (failed-precondition) → stuck as a student. Verified F1 is isolated to Signup.jsx (sole client caller of provisionTeacher). Fix: backed up F1 → audit/deepfix/task3/Signup.p6.jsx, then `git checkout HEAD -- src/pages/Signup.jsx` → today's working teacher self-registration stays live for the initial release; F1 re-applies at P6 with TEACHER_PROVISIONING_ENABLED + teacher_invites + P6 rules. Manifest updated (F1 moved A→D). Revert verified clean (empty diff). |
| 2026-07-15 | src/firebase.js + firebase.json | **Wire the Functions emulator (flag-on E2E rig, step 1).** Added `connectFunctionsEmulator(getFunctions(app),'localhost',5001)` inside the existing `VITE_USE_EMULATOR==='true'` block (the app already wired Firestore/Auth/Storage; Functions was missing) so the callable paths resolve to the emulator when validating flags-ON end-to-end. All ad-hoc `getFunctions()` calls resolve the default-app instance this connects. Pinned the functions emulator to port 5001 in firebase.json emulators config. Test-only (gated on VITE_USE_EMULATOR; false in prod) → byte-equivalent live. eslint-parse clean. |
| 2026-07-15 | src/services/progressService.js + src/services/studyService.js + src/pages/DailySessionFlow.jsx | **P9·CYC·U5: cycling lap-boundary intervention RESET (David's product decision).** When a student cycles into a new lap (virtual twi crosses k·cycleLength), reset the intervention throttle so lap N starts at full pace (vs the old CARRY behavior that kept throttled students throttled into lap 2). Core logic in updateClassProgress: detect `Math.floor(twiBefore/cycleLen) < Math.floor(twiAfter/cycleLen)` when `cycling.active`, then drop lap N-1's recentSessions (the intervention driver via calculateInterventionLevel) + zero the stored interventionLevel. Threaded cyclingActive/cycleLength from both completion summaries (completeSessionFromTest + DailySessionFlow) → recordSessionCompletion → updateClassProgress (new `cycling={active,cycleLength}` param, sole caller). HARD-GATED on cycling.active ⇒ byte-equivalent when cycling off (the added summary fields are consumed, not persisted). CLIENT path only — the server completeSession (foundation.js) needs the mirror before SERVER_PROGRESS_WRITE flips (noted in code + CYCLING_COHORT_VALIDATION_PLAN). Pending U3 verify + Codex v2. node --check + eslint-parse clean (only pre-existing unused-var errors). |
| 2026-07-15 | functions/index.js | **GRADE_TOKEN_MINT true→false (end-review MED-2 fix; match live prod).** End-review (Fable-server MED-2) flagged the tree carried `GRADE_TOKEN_MINT=true` ("on for validation") while HEAD≠live-prod; David confirmed he disabled MINT in prod. Deploying true would silently re-enable minting (a live gradeTokenSecret.value() dependency + extra response field). Flipped to false + disarm comment (mirrors the GRADE_TOKEN_ENFORCED P0 disarm) → both GRADE_TOKEN flags off ⇒ typed grading never touches GRADE_TOKEN_SECRET, byte-equivalent to prod. node --check OK. |
| 2026-07-15 | audit/playwright/lsr_deepfix_whitebox.mjs | **M-WB fresh-student auto-provision (port of the M-NET fix) to close the stale r14 failures.** M-WB last ran at wb-r14 (2026-07-14, PRE-calibration): 0/4 — W-RA3g csd 4→4 no-advance (wordmap/answer-seed gap, same class as RO-S1), W-RA4/W-RA4b Submit-button 30s timeout (the reach-submit flow-gap since fixed in the shared lsr_ui primitives), CUT-5 INVALID save-leg-not-observed. All harness-shaped, not app defects; the runner shares the now-calibrated primitives + wordmap, so a fresh run should clear most. Ported the same auto-provision (import admin; PASS from lsr_ui; `let uid=uidByEmail(email); if(!uid) createUser`) so it uses genuinely-fresh clean-day-1 accounts (avoids the list-scoped pollution that bit M-UI). admin already init'd (readListWordCount→FB.db() before the loop). node --check OK. Queued wb-r25 on 6 fresh students. |
| 2026-07-15 | audit/playwright/lsr_deepfix_netresilience.mjs | **M-NET NET-1 "lost write" = harness-timing artifact, NOT a product bug (adjudicated vs app code, H1 discipline).** net-r23 finally ran end-to-end (2/3): NET-2/NET-3 PASS, NET-1 FAIL "offline submit → 0 attempts (lost write)". Adjudicated against TypedTest.jsx `gradeWithRetry` (MAX_RETRIES=3, RETRY_DELAY_MS=10000, TIMEOUT_MS=90000): attempt 1 fails offline at t≈0.4s → `setRetryAttempt(1)` ("Retrying… Attempt 1/3") → waits 10s → **attempt 2 at t≈10.4s fires online (harness reconnected at t=2.5s) → succeeds → durable write lands ~t=11s**. But the oracle measured at ~2.5+networkidle+4 ≈ 6.5–8.5s — INSIDE the 10s retry gap → false "0 attempts". App is correct (it recovers on the next retry); NET-3 only passed by a timing race at ~t=10s. FIX: added `awaitAttemptDelta()` poll (2.5s steps up to 45s for delta≥1, covering the full 3×10s schedule + grading latency, then a 6s stabilization re-read so a genuine late DUPLICATE still trips >1). Refactored all 3 scenarios (NET-1/2/3) off fixed settle+read onto the poll → removes NET-3's raciness too. node --check OK. Expect net-r24 → 3/3 CLEAN. |
| 2026-07-15 | audit/playwright/lsr_deepfix_netresilience.mjs + lsr_reviewonly_fb.mjs | **M-NET real root cause — masked admin-init failure (winclaude_022 diagnosis; RETRACTS the r20/r21 "pool non-contiguous" theory).** M-NET called `admin.auth()` (uidByEmail :132, then createUser) BEFORE any `FB.db()` — the only thing that runs the FB helper's lazy `admin.initializeApp()`. So admin had no default app → `getUserByEmail` threw "default Firebase app does not exist" → `uidByEmail`'s silent blanket-catch swallowed it → returned null → surfaced as "no uid" for EVERY email regardless of existence (r20/r21). createUser (no silent catch) finally un-masked it (r22). Whether s136-138 actually exist is now unknown — the "no uid" was never proof of absence. **Fixes:** (1) netresilience — call `FB.db()` at startup before the scenario loop to force admin init; wrap uid resolve+create in one try→INVALID for per-scenario isolation. (2) lsr_reviewonly_fb `uidByEmail` — return null ONLY for `auth/user-not-found`, re-throw all other errors so infra failures can't masquerade as "no uid" again (genuine not-found still returns null → every caller's contract preserved). node --check OK both. |
| 2026-07-15 | dsg-edits/edit-t14.mjs (NEW), dsg-edits/DEPLOY_LOG_26SM_T14.md (NEW) | **26SM-T14 dsg-edit (live bank):** applied 3 items — M01-Q5 (119266) + M2L-Q3 (119255) Passage Type → Science; M2U-Q3 (119239) Passage Type → Science + stripped `nf-voc` generator leakage from passage. IDs found via admin list exact-tag search (single copy, 응시횟수=0). Keys unchanged; backup + reload-verified. See DEPLOY_LOG_26SM_T14.md. |
| 2026-07-15 | audit/deepfix/THROTTLE_FIX_VALIDATION.md (NEW), scripts/cs/scan-throttle-risk.mjs (NEW) | Throttle #11 fix-validation plan: at-risk roster (12 at-wall/6 close/56 watch), 6 coverage archetypes with exact seed states, "fix valid" criteria, and a duplicate→drive→assert Playwright audit design (sandbox-only, keystone=Recovering). scan-throttle-risk.mjs = READ-ONLY risk-tier scanner. |
| 2026-07-16 | scripts/cs/reconcile-reset-students.mjs (NEW), scripts/cs/fix-csd-to-completed.mjs (NEW) | Reconciled 58 throttle-reset students to reality post-#11-deploy: rebuilt recentSessions from real attempts, real interv, non-demoting csd (16 off-by-one bumps), cleared csFixNote/session. Phantom records + latent off-by-ones eliminated. fix-csd-to-completed = targeted csd→genuine-completed-day (이찬희/서혜빈). Backups in backups_reconcile/. See SUPPORT_RUNBOOK CS-2026-07-16/16b. |
| 2026-07-16 | audit/deepfix/THROTTLE_REVIEWONLY_ADVANCE_FLAW.md (NEW) | Documented design flaw in the deployed #11 fix: it advances csd on THROTTLE review-only days (should only for list-end) → students race through review-only days, rush→0-scores refill the last-3 window → recovery-defeating loop (이아연/김시연/조예서). Recovery path verified working (tests save; 1-2 good reviews → new words). Decision: NO override (David) — self-heal. Candidate NEED_TO_FIX #16. |
| 2026-07-17 | scripts/cs/throttle-relief-cohort.mjs (NEW) | **CS cohort throttle relief + runaway-csd reconcile (David-authorized band-aid).** Moved 144 currently-throttled 26SM docs off throttle: NULL the reviewScores in recentSessions → calculateInterventionLevel recomputes to 0 (verified live: recomputed-from-recentSessions=0.00, the value studyService.js:325 actually uses) → full new words. NO synthetic scores (the 07-15 synthetic-0.5 reset caused the 김예린 phantom-record tickets, CS-2026-07-16). Also reconciled 44 runaway-inflated csd DOWN to the last passed-new day (twi anchor) — safe: only pulls DOWN when csd>anchor, never demotes below real progress, so the ~100 legit-throttled students' csd is untouched (raw twi/pace would have wrongly demoted them). Excluded 3 list-end students (un-throttle is a no-op; need next-list advance). Dry-run default; --commit. Backups: scripts/cs/backups_throttle_relief/<uid>_<doc>.json (144). data-integrity-sweep 26SM identical before/after (0 corruption; csdImplausible=0; 72 pre-existing reviewNoNewPass unchanged). TEMPORARY: re-throttles after ~3 real reviews; durable fix = the forced-pathway code changes (docs/plans/FORCED_PATHWAY_FIX_PLAN). See SUPPORT_RUNBOOK CS-2026-07-17. |
| 2026-07-17 | scripts/cs/census-i4-pairing.mjs (NEW) | **READ-ONLY I4 pairing-predicate census (gates PR-1, no writes).** Faithful mirror of getMostRecentPassedNewTest (db.js:3507, list-scoped by max newWordEndIndex) + getReviewForDay (db.js:3705). Over 26SM (359 started): found **14 truly-stuck I4 victims** (storedCSD===anchorDay−1 + real anchor-day review + current exact-range predicate fails) — NOT the ~24 the Phase-3 agents estimated (matches Agent D's stricter 12). Measured drain under candidate reader predicates: Agent A's simple same-class leg = **3/14** (fails the 9 pre-anchor `[twi,twi−1]` relief-minted stubs its temporal filter excludes); Agent C's tiered predicate + exact-range temporal-drop refinement = **13/14, 0 cross-class false-pairs**. The 1 residual (SfEVUpvi) is a skip-only anchor day → correctly routes to a retake per David's decision #2. Empirically LOCKS the reader predicate as C-tiered (not A). See docs/plans/CS_2026-07-17_ROOT_CAUSE_EFFORT.md PHASE 3 census result. |
| 2026-07-17 | docs/plans/CONSOLIDATED_ROADMAP_2026-07-17.md (NEW), docs/plans/CONSOLIDATION_ROADMAP_2026-07-17.md (superseded header), RESUME.md | **Drift correction + consolidated roadmap grounded in the LIVE deploy.** Verified (4 Fable agents RC-1..4 + orchestrator prod probes): client `4b8452a` live since 07-15; **deepfix FUNCTIONS deployed-dormant** (prod completeSession → FAILED_PRECONDITION SERVER_COMPLETE_SESSION_ENABLED=false, a deepfix-only string); rules pre-deepfix; M4 shadow clock NOT started. Deepfix P0/P1/P2 live-behavioral, P3–P10 dormant behind false flags. Corrected the prior CONSOLIDATION_ROADMAP's FALSE "undeployed/prod=14e49a4" premise (root cause: stale RESUME + 07-17 compaction dropped the 07-15 deploy fact + a git-log misread became a "verified baseline"). New consolidated roadmap = deepfix DEPLOY_ORDER spine + CS PR-1/2/3 + invariant suite, split direction-independent (safe now) vs direction-dependent (gated on the open FREENAV closure, turnOwner=claude). #10-live claim retracted (it's the tracked DSF dayGuardRejected recovery path, closes at P4). |
| 2026-07-17 | scripts/cs/throttle-relief-cohort.mjs | **A4 (roadmap): marked DO-NOT-RUN.** Added a deprecation banner + a runtime guard (aborts unless LSR_ALLOW_IATROGENIC=1). Its csd-down + un-throttle legs are iatrogenic — verified to have minted ~9 of the 14 I4-stuck students. Superseded by the PR-1/PR-3 code fix. Prevents a future CS session from re-running it against 26SM. |
| 2026-07-17 | audit/playwright/lsr_deepfix_static.mjs | **A1 (roadmap): refreshed the RED M-STATIC flag-table.** Fixed GRADE_TOKEN_MINT baseline true→false (disarmed 07-15, index.js:80) + added the 9 missing flags (client CYCLING_ENABLED/SERVER_OVERRIDE/TEACHER_IDS_READ; foundation CYCLING_ENABLED/SERVER_REVIEW_CHALLENGE_ENABLED/SERVER_OVERRIDE_ENABLED/TEACHER_IDS_WRITE_ENABLED; index TEACHER_PROVISIONING_ENABLED/TEACHER_CLAIM_ENABLED) → FLAG_TABLE now 26 rows = full 17-server+10-client surface. VERIFIED: `node lsr_deepfix_static.mjs --target=baseline` → CLEAN pass=36 fail=0 invalid=0. The G-FLAGTABLE gate is green again (was blocking the next gated deploy's DG-1). |
| 2026-07-17 | audit/deepfix/task3/invariant_assert.mjs (NEW), docs/design/FREE_NAVIGATION_MODEL.md, docs/plans/loop/baton.json | **A5+A6+A2 (roadmap).** A5: appended a "GATE CLOSED — COEXISTENCE" section to FREE_NAVIGATION_MODEL.md (David: forced default + free-nav = future per-class option; binary supersedes floor). A6: polled Codex — baton round 8 CONSOLIDATED_ROADMAP_SEQ_GATE (handoff claude_to_codex_roadmap_seq_001.md + ready marker) for adversarial review of the deploy sequencing. A2 (first cut): new invariant suite (static-signature register checks) — runs CLEAN 8/8 (INV-10 non-reconciling read, non-demoting csd, lookup-fail pin, day-guard, attempts doctrine, anchor validity, #9 pairing range, twi-anchor). Pure-function + PR-activated invariants TODO. |
| 2026-07-17 | docs/plans/SESSION_TODO_2026-07-17.md, docs/plans/CONSOLIDATED_ROADMAP_2026-07-17.md | **Folded Codex round-8 NEEDS_FIXES (all 4, verified valid).** HIGH-1: propagated the FREENAV coexistence closure into CONSOLIDATED_ROADMAP §2 (was "open decision") + T0.3 + SESSION_TODO A3/B1/B2 → B1/B2 resolved. HIGH-2: D3/P4 hard-gated on C1+C2 live + C3/PR-3 flipped+soaked (was soft "lands with"). HIGH-3: D5/P6 gains "P5 migration complete + accepted (26SM quarantine=0)". MEDIUM-1: D1 scoped to the deepfix baseline only (PR-1/2/3 need own review+flip gates). Re-handed to Codex baton round 9 for GO confirmation. |
| 2026-07-17 | docs/plans/CONSOLIDATED_ROADMAP_2026-07-17.md, docs/plans/loop/baton.json | **Codex sequencing gate → GO (converged).** Baton loop rounds 8 (NEEDS_FIXES, 4 findings) → 9 (NEEDS_FIXES, 1 residual line-42 split-brain) → 10 (GO, codexConverged=true, codex_review_010.md). Fixed the residual (roadmap §2 line 42 rewritten Resolved/still-open). The consolidated roadmap's deploy sequencing is now Codex-blessed as the execution spine. PR-1/2/3 code diffs each still need their own Codex implementation review when built. |
| 2026-07-17 | audit/deepfix/task3/invariant_assert.mjs | **A2 (roadmap): invariant suite expanded 8→full 12-invariant register + F checks.** Added S-rows: #11 review-only 3-reason gate (exact `reviewOnlyReasonConfirmed` disjunction + `!reviewOnlyDay` guard, studyService.js:1715), #16 two-facts forward-spec anchor (appender welded to `currentStudyDay+1`; `recordReviewOutcome` ABSENT — flips when PR-3 lands), #7 cycling lap-reset (boundary predicate + carry drop + intervention zero, progressService.js:585-602), day-1 asymmetry (anchorDay===1→csd=1 + `impossible_phase_detected`), recentSessions append-cadence (sole `.slice(-MAX_RECENT_SESSIONS)` site, count=1), #12 display-only-except-challenge-accept (doctrine + db.js:3025 `phase:'review-study'` count=1 + isCurrentBoundary guard), engagement grandfather = PEND row + `isEngagedReview` absence anchor (activates PR-2). Added (F) extract-and-eval (p9_assert technique, extracts STUDY_ALGORITHM_CONSTANTS too): `calculateInterventionLevel` (<3→0, ≤0.30→1, ≥0.75→0 incl. boundaries) + `calculateDailyAllocation` (newWords=round(pace·(1−interv))). New helpers: C() exact-count, PEND() non-failing pending rows. VERIFIED: `node audit/deepfix/task3/invariant_assert.mjs` → ✓ CLEAN pass=34 fail=0 pending=1. |
| 2026-07-17 | audit/playwright/findings/deepfix_ui_prod-smoke-r2.json | **D1: prod-smoke GREEN (4/4) — unblocks win-executor round 26.** WSL-Claude ran the deepfix M-UI prod-smoke on live prod (vocaboostone.netlify.app) with sandbox identities (lsr_s130-133): RO-S1 PASS (day-1 completion csd0→1/twi0→3/full anchor), RO-S9 PASS (read-surface + re-entry recorded nothing), RS-1 PASS (gradebook server Name-filter — C-33 indexes live), RS-2 PASS (testId-less render — C-34). Confirms the deployed deepfix P1/P2 works in prod. The round-26 BLOCKED prod-smoke (Windows permission-classifier denial) is resolved. Remaining D1: flag-ON M-UI matrices (WinClaude round 27), single-runId cert, Codex end-gate, TASK6_REPORT. |
| 2026-07-17 | scripts/cs/backup-all-26sm.mjs (NEW), scripts/cs/restore-from-backup-26sm.mjs (NEW), scripts/cs/finalize-backup-26sm.mjs (NEW), scripts/cs/backups_full_26sm_20260717-165840/ | **A7: comprehensive reinstatable 26SM backup (David-required pre-cutover safety).** Read-only full dump: 925,851 docs — class_progress 1499, session_states 951, attempts 25640, study_states 896885, users 823, classes 40, lists 13 (list_progress/progress_meta 0 = dormant). MANIFEST.json (counts + git sha 4b8452a). Reinstate: `node scripts/cs/restore-from-backup-26sm.mjs --commit --dir=scripts/cs/backups_full_26sm_20260717-165840` (dry-run default). The initial agent run died before the lists+MANIFEST tail; finalize-backup-26sm.mjs completed it (big collections were validly closed + parse-verified). Re-run fresh right before P5. |
| 2026-07-17 | src/utils/reviewPairing.js (NEW), src/config/featureFlags.js, src/services/db.js, src/services/studyService.js, src/services/sessionService.js, src/services/progressService.js, src/pages/DailySessionFlow.jsx, src/pages/Dashboard.jsx, src/pages/MCQTest.jsx, src/pages/TypedTest.jsx, audit/playwright/lsr_deepfix_static.mjs | **CS PR-1 BUILT (dormant-draft; the 14-stuck-students fix — WI-2 pairing + WI-3 re-entry + WI-4 recovery + F2 warn; CS_2026-07-17_ROOT_CAUSE_EFFORT.md).** 3 NEW flags, all default FALSE (REVIEW_PAIRING_V2 / REENTRY_GUARD / RECOVERY_GUARD) — flag-off is byte-equivalent to today at every site (Run-L; verified per-hunk: each flag-off branch is the verbatim original expression). NEW pure module src/utils/reviewPairing.js = census-LOCKED `reviewPairsWithAnchor` tiered predicate (exact∧(temporal∨sameClass) → sameClass∧engaged∧temporal → sameClass∧engaged∧pre-anchor [nwsi,nwsi−1]-stub/null-range) + `isEngagedReview` (≥80% answered; tq:0/autoCompleted carve-outs; ENGAGEMENT_GRANDFATHER_TS=0 placeholder → set to deploy epoch at flip, David decision #3) + RECENT_ATTEMPTS_WINDOW=12. Under REVIEW_PAIRING_V2: db.js getReviewForDay drops the submittedAt>= pre-narrow + adopts the predicate; studyService determineStartingPhase pairs the day's review against the best-new pick with the SAME predicate (kills the I4 asymmetry); recent-attempts window 8→12 at all 5 sites (db.js default + progressService ×4). Under REENTRY_GUARD: shouldShowReEntryModal takes lastCompletedDay (Dashboard passes csd from progressData) + DailySessionFlow :779 gate requires existingState.currentStudyDay === config.dayNumber−1; handleReEntryRetake populates the queue via buildReviewStudySet (kills the empty-retake trap); MCQ under-answered REVIEW submit ConfirmModal (David copy; warn-only, never blocks — CS-16c). Under RECOVERY_GUARD: MCQ/Typed handleRecoveryResume intersects saved answers with the current word-id set, drops out-of-range index, empty intersection → start fresh (I6 >100% class). Unconditional additive: impossible_phase_detected payload +userId. FLAG_TABLE +3 rows. VERIFIED: lsr_deepfix_static --target=baseline CLEAN pass=39 fail=0; invariant_assert CLEAN pass=34 fail=0 (INV-9 needle + isEngagedReview/recordReviewOutcome absence anchors in progressService preserved); node --check + eslint = 0 new problems (64→64 baseline-identical); 30/30 predicate smoke tests vs census shapes (최희윤 null-range, [twi,twi−1] stub, cross-class 0-false-pairs, skip→retake). NOT deployed, NOT committed, all flags dormant. |
| 2026-07-17 | src/utils/reviewPairing.js (NEW), src/config/featureFlags.js, src/services/db.js, src/services/studyService.js, src/services/sessionService.js, src/pages/{DailySessionFlow,Dashboard,MCQTest,TypedTest}.jsx, src/services/progressService.js, scripts/cs/census-verify-pr1.mjs (NEW) | **C1/PR-1 BUILT + verified (dormant, flag-gated REVIEW_PAIRING_V2/REENTRY_GUARD/RECOVERY_GUARD, all false).** Pairing predicate (reviewPairsWithAnchor+isEngagedReview) adopted by db.js:3757 getReviewForDay + studyService.js:241 determineStartingPhase; re-entry attempts-conjunct (sessionService/Dashboard/DailySessionFlow) + retake-queue fix; recovery-intersect (MCQ/Typed); F2 under-answered confirm; 8→12 window ×5; impossible_phase userId. Orchestrator SHIP-GATE census against the SHIPPED predicate: 13/14 drain + SfEVUpvi retake (decision #2) + 0 cross-class false-pairs = PASS. static-harness CLEAN 39/0, invariant suite CLEAN, flag-off byte-equivalence grep-attested. grandfather placeholder=0 (census-matching; set to deploy-ms at flip). NOT committed/deployed. Next: Codex diff review → dev-E2E → flip. |
| 2026-07-17 | docs/plans/loop/win/reviews/winclaude_028.md, change_action_log | **Task-6 flag-ON M-UI: DEFERRED (documented, not a defect).** WinClaude r28 diagnosed a real harness gap: lsr_deepfix_ui.mjs is hard-wired to PROD sandbox fixtures (lsr_lists.json prod IDs, lsr_*/25WT in prod) with zero emulator awareness; seedEmulator.js seeds only generic data. It correctly refused to run flag-ON against prod or improvise a harness change. Decision (WSL-Claude): DEFER the emulator flag-ON M-UI rather than build a heavy emulator-seed. Rationale: (a) M-CALL already certified the server callables flag-ON on the emulator (r27 CLEAN 21/0/0); (b) flag-OFF M-UI passed live (prod-smoke 4/4); (c) the unique value (flag-ON client render/routing — RS-3/CUT) is covered by David's planned extensive post-cutover PROD full-UI audits against the real end-state. Joins the documented-deferred cert legs (CS-7/CS-10/DG-2/DG-3). |
| 2026-07-17 | src/utils/reviewPairing.js, src/config/featureFlags.js, scripts/cs/census-verify-pr1.mjs | **Folded Codex PR-1 round-11 NEEDS_FIXES (all 3 verified valid).** HIGH-1: removed grandfathering from PR-1 pairing entirely — isEngagedReview is now STRICT (= the census-certified predicate); deleted ENGAGEMENT_GRANDFATHER_TS + the leg; decision-#3 grandfather deferred to PR-3's completion reader (grandfathering pairing would make SfEVUpvi's skip pair, violating decision #2); updated the flag comment (no grandfather-at-flip). HIGH-2: census-verify-pr1.mjs now fail-closed (exit 1) + asserts the exact shape (stuck=14, drained=13, residual skip-only, 0 false-pairs). NOTE: impossible_phase userId kept unconditional, documented as observability-only. Re-verified: ship-gate PASS exit 0, static CLEAN 39/0, invariant CLEAN. Re-handed to Codex round 12. |
| 2026-07-17 | docs/plans/loop/baton.json + win/baton.json + handoffs/claude_to_codex_situation_confirm_001.md (NEW) + win/handoffs/claude_to_winclaude_029.md (NEW) + ready/claude_ready_round_013.json (NEW) | **Post-freeze re-grounding: batoned BOTH loops for independent situation-confirmation.** Session froze ~34min after Codex's round-12 PR-1 GO (baton -> claude 17:30:29Z, freeze before I acted on the GO). On resume, reconstructed state from disk (RESUME + CONSOLIDATED_ROADMAP + baton + change log + file mtimes; timestamps match David's "~30-40min ago"). Rather than act on the un-actioned GO immediately, flipped Codex loop -> round 13 SITUATION_CONFIRM (turnOwner=codex, rev 96) asking it to confirm/correct 4 reconstructed lines (PR-1 GO+converged & unchanged; PR-1 remaining = fail-closed census re-run + dev-E2E only; deploy baseline 4b8452a-live/functions-deepfix-dormant; task-list position A1/A3/A4 done, A2 substantially built, FREENAV closed, C1/PR-1 active). Flipped win-executor loop -> round 29 (turnOwner=winclaude, rev 57) for a READ-ONLY confirm: concur r28 flag-ON M-UI is DEFERRED; re-run the non-mutating prod posture probes (completeSession FAILED_PRECONDITION + bundle build-stamp) to reconfirm the deploy baseline from the prod-reachable Windows side; confirm clean working tree. No code/rules/data changed; no 26SM writes; nothing committed/deployed. PR-1 round-12 GO stands unchanged (codexConverged=true preserved).
| 2026-07-17 | RESUME.md + docs/resume_archive/RESUME_2026-07-17c.md (NEW) + docs/plans/loop/baton.json | **Situation-confirm checkpoint closed: applied Codex round-13 CORRECTION.** Codex CONFIRMED the operational reconstruction (PR-1 GO round 12; PR-1 remaining = fail-closed census re-run + dev-E2E; deploy baseline 4b8452a-live/functions-dormant; task-list position) but flagged ONE correction: RESUME.md was STALE (still said "FREENAV = the one open decision" + "CS fixes designed NOT written" — overtaken by roadmap round-10 GO + PR-1 round-12 GO; a literal resume from it could regress the session). Rotated per CLAUDE.md: copied stale RESUME → RESUME_2026-07-17c.md, rewrote RESUME.md to the true state (PR-1 built+GO'd dormant; FREENAV closed coexistence; A1/A3/A4/A7 + prod-smoke + M-CALL done; A2 substantially built; C2/C3/D-track pending; David full-auto on reversible + deploys via WinClaude + rails on one-way doors). Codex baton round 13 marked correction-applied/closed. WinClaude round-29 confirm still pending at write. No code/data changed; nothing committed/deployed; no 26SM writes. |
| 2026-07-17 | scripts/cs/census-verify-pr1.mjs (run) + memory/continuation-list-end-shape.md (NEW) | **PR-1 evidence gate (a) DONE + B4 CLOSED.** (a) Re-ran the fail-closed ship-gate census on the exact ship build (imports the actual src/utils/reviewPairing.js): SHIP GATE PASS exit 0 — stuck=14, drained=13, residual=SfEVUpvi (skip-only→retake per decision #2), 0 cross-class false-pairs. First half of PR-1's pre-flip evidence complete; dev-E2E (WinClaude flag-OFF sandbox) is the other half. (B4) David closed the last open product fork: list-end = auto-advance to next assigned list (CONT-A/P8); if the student re-selects the just-finished list → cycling (P9/CYC); fallback = completion state. Recorded in memory + SESSION_TODO + roadmap. David also confirmed for the unattended run: WinClaude fully autonomous on my baton deploys; P5/P6/P7 one-way doors = full send unattended behind rails + reinstatable backups. |
| 2026-07-17 | docs/plans/loop/win/reviews/winclaude_029.md (WinClaude) | **WinClaude round-29 situation-confirm = CONFIRMED (post-freeze re-grounding complete).** All 3 checks concur: (1) flag-ON M-UI DEFERRED not a blocker (concurs; harness fixture-gap); (2) deploy baseline unchanged — Windows-vantage probes: prod bundle __VOCABOOST_BUILD__ shortSha=4b8452a dirty:false builtAt 2026-07-15T22:46Z, prod completeSession → FAILED_PRECONDITION SERVER_COMPLETE_SESSION_ENABLED=false (non-mutating, flag-check precedes auth at foundation.js:1047-1050); (3) no uncommitted executor work Windows-side (the dirty src/ diffs are WSL-Claude's PR-1 build, not WinClaude's). Both Codex (CONFIRMED+RESUME-correction) and WinClaude (CONFIRMED) have re-grounded state. WinClaude standing ready for the PR-1 dev-E2E round; re-armed its watcher at baseline 58. No writes/commits/deploys. |
| 2026-07-17 | scripts/cs/dup-prefix-reentry-repro.mjs (run --commit) + docs/plans/loop/win/{baton.json,handoffs/claude_to_winclaude_030.md} | **Seeded PR-1 dev-E2E sandbox + handed WinClaude round 30.** Committed 3 sandbox re-entry dups (dup_repro_a/b/c@vocaboost.test, each w/ a DUP class) — real re-entry/stuck student states dup'd onto renamed sandbox copies (stale session phase=complete + reviewTestScore=0 + inflated csd; study_states/attempts copied). READ real 26SM (read-only); WROTE only NEW sandbox docs — no real student/class/list touched. These exercise all 3 PR-1 flag-ON client legs (pairing readers + re-entry + recovery). Handed WinClaude round 30 = PR-1 flag-ON dev-E2E (localhost:5173 flags-true → PROD Firebase, drive each dup_repro full-UI, guaranteed flag-restore; NO deploy) — the UI/audit evidence Codex required before the PR-1 flip. Sandbox-only; no 26SM writes/commits/deploys. |
| 2026-07-17 | functions/foundation.js, functions/index.js | **CS PR-2-CORE BUILT (dormant, flag-gated; NOT committed/deployed).** Server companion to PR-1. OC-2 pairing mirror: added isEngagedReviewServer/reviewPairsWithAnchorServer (byte-faithful port of src/utils/reviewPairing.js — STRICT engagement, no grandfather) + rewired getReviewForDayServer (drop temporal pre-narrow + tiered predicate) gated on REVIEW_PAIRING_V2_SERVER = SERVER_COMPLETE_SESSION_ENABLED‖SERVER_RESOLVE_LIST_PROGRESS_ENABLED (existing flags; threaded anchorClassId into both callers). F3 engagement stamp: computeReviewEngagementStamp (answeredCount/engagedReview) spread additively into writeAttemptTxn attemptData, dormant behind new REVIEW_ENGAGEMENT_STAMP_ENABLED (computeTeacherIdsForAttempt idiom → null ⇒ field omitted). WI-4/I6 server clamp: dedupeByWordId + correctCount∈[0,tq]/score∈[0,100] in writeAttemptTxn, dormant behind new RECOVERY_SCORE_CLAMP_ENABLED. OC-4 lap-reset: added the ABSENT completeSession cycling lap-boundary reset (drop prior-lap recentSessions + zero interventionLevel) gated on effective `cycling` (CYCLING_ENABLED). EXCLUDED (defer PR-3): OC-1 hold-csd/review_recorded + grandfather leg. Verify: node --check both PASS; invariant suite CLEAN 34/0/1 (unchanged — no client files touched); predicate parity vs shipped client = 0 mismatches over 9 tier/carve-out cases; clamp reproduces 150%→100%, byte-equiv on normal input; all 13 FOUNDATION_FLAGS false. Flag-off = byte-equivalent to deployed foundation.js. |
| 2026-07-17 | functions/foundation.js + functions/index.js (PR-2-core BUILT, Opus agent + orchestrator-verified) + audit/playwright/lsr_deepfix_static.mjs | **CS PR-2-CORE BUILT (dormant-draft; server companion to PR-1).** foundation.js +150/-13, index.js +40/-2, 2 NEW FOUNDATION_FLAGS (REVIEW_ENGAGEMENT_STAMP_ENABLED, RECOVERY_SCORE_CLAMP_ENABLED, both false → 13 flags). Hunks: OC-2 server pairing mirror (isEngagedReviewServer/reviewPairsWithAnchorServer = byte-faithful STRICT port of the shipped src/utils/reviewPairing.js; gated by derived REVIEW_PAIRING_V2_SERVER=SERVER_COMPLETE_SESSION_ENABLED||SERVER_RESOLVE_LIST_PROGRESS_ENABLED; getReviewForDayServer drops the submittedAt>= pre-narrow + adopts the tiered predicate, mirror of db.js:3702-3767); F3 additive engagement stamp (answeredCount/engagedReview on writeAttemptTxn); WI-4/I6 server score clamp (dedupeByWordId + correctCount∈[0,tq]/score∈[0,100] → fixes the 4 historical >100% docs); OC-4 the ABSENT server cycling lap-reset mirror (progressService.js:585-605). Grandfather/hold-csd DEFERRED to PR-3 (Codex PR-1 HIGH-1). ORCHESTRATOR-VERIFIED (not trusted): git-diff read per-hunk (flag-off = original expression verbatim; query refactor = clause-identical); OC-4 scope vars all declared in completeSession (cycling@1212 resolves false when CYCLING_ENABLED off → crossedLapBoundary=false → byte-equivalent); node --check both OK; invariant suite CLEAN 34/0/1; M-STATIC baseline CLEAN with +2 flag rows. DECISION: stamp+clamp stay dormant in tree (full flag-off byte-equivalence) → flip TRUE at the D2 functions deploy (roadmap "live at deploy" intent). NEXT: Workflow adversarial-verify (Opus/max) → Codex diff gate. NOT committed/deployed. |
| 2026-07-17 | (correction) change_action_log | **Correction to the prior PR-2 entry:** M-STATIC baseline is TRANSIENTLY NOT_CLEAN (38/3), NOT clean as I pre-wrote. The 3 fails are the PR-1 CLIENT flags (REVIEW_PAIRING_V2/REENTRY_GUARD/RECOVERY_GUARD) reading TRUE because WinClaude's concurrent round-30 flag-ON dev-E2E has them flipped (win baton round 30 pending). My 2 PR-2 foundation-flag rows PASS. Will re-run M-STATIC after WinClaude round-30 guaranteed-restore to confirm the true baseline is CLEAN 41/0. Did NOT touch featureFlags.js (WinClaude owns it during its round). |
| 2026-07-17 | scripts/cs/dup-prefix-reentry-repro.mjs (re-run --commit) + docs/plans/loop/win/{baton.json,handoffs/claude_to_winclaude_031.md} | **PR-1 dev-E2E: r30 BLOCKED (one-shot repro) → re-seeded + handed WinClaude round 31.** r30 CONFIRMED leg (a) re-entry render POSITIVE on flag-ON (all 3 dup_repro render an actionable re-entry modal, no dead-end/loading-trap; guaranteed-restore SHA-verified). Blocked on legs (b) advance / (c) recovery / flag-OFF because the stale-complete repro is one-shot (first nav consumes it) + WinClaude screenshot-then-navigated. Re-seeded fresh dup_repro_a/b/c; round 31 assigns ONE single-pass leg per account (a=FLAG-ON re-entry→Retry→playable→advance, b=2nd sample, c=FLAG-OFF dead-end gating), recovery deferred to prod audit. Also surfaced: David directly authorized WinClaude for git push + firebase deploy → laid out the full sequence-ahead + authorization split (WinClaude=deploys; WSL=P5/P7 data-writes under David's full-send authorization). Sandbox-only; no 26SM writes/commits/deploys. |
| 2026-07-17 | functions/foundation.js (PR-2 verify findings folded) | **Folded the PR-2 Workflow adversarial-verify (Opus/max, 3 lenses) findings — all 4 real, all fixed.** Byte-equiv lens = SOUND (affirmed all hunks flag-off byte-equivalent). Fixes: (HIGH, flag-on lens) OC-4 lap-reset used the WRONG modulus (lists.wordCount) instead of the live word-doc count the client mirror + §2 one-modulus rule require → added getCanonicalCycleLengthServer(listId) (aggregate .count() over lists/{id}/words, admin 13.6, try→0 fail-safe), fetched pre-txn only when cycling, replaced cycleLen=cycling?totalListWords:0 with cycleModulus; (MEDIUM, parity) computeReviewEngagementStamp omitted isEngagedReview's autoCompleted short-circuit → added ctx.autoCompleted===true disjunct; (LOW, parity) pairMs fallback wasn't byte-identical to client tsMillis (typeof-fn vs truthy toDate) → aligned to t?.toDate form (restores throw→query-error fail-closed parity; catch at :715-716 confirmed returns query-error); (LOW, parity) lineage-gate anchorClassId flag-off relaxation → documented provably-unreachable (both callers gated by the flags that make V2 true). VERIFIED: node --check foundation+index OK; invariant suite CLEAN 34/0/1; pairMs now char-identical to client tsMillis; stamp autoCompleted uses ctx (undefined in writeAttemptTxn today = no divergence, future-proofs PR-3). NEXT: Codex PR-2 diff gate (HARD). NOT committed/deployed. |
| 2026-07-17 | docs/plans/loop/{baton.json,win/baton.json,handoffs} | **PR-1 → FLIP (WinClaude r32) + PR-2 → Codex diff gate (r14), parallel.** WinClaude r31 PR-1 dev-E2E = PASS: decisive flag-ON/flag-OFF gating contrast — flag-ON populates a 60-card PLAYABLE review (buildReviewStudySet), flag-OFF = empty dead-end (the stuck-student bug). Re-entry render + retake-queue-population + gating all screenshot-confirmed. Caveat: literal complete→csd-advance not driven end-to-end (60-card study loop vs test-screen driver) → deferred to the post-flip prod smoke (r33) on the live build. Assessment: SAFE to flip (census 0 false-pairs + superset predicate = no regression; worst case a student stays stuck, never worse; reversible; no live cohort). Handed WinClaude r32 = flip 3 client flags true + commit (EXCLUDING functions/ so PR-2 stays in its own gate) + push→Netlify. In parallel, flipped Codex baton → r14 PR2_DIFF_REVIEW (PR-2-core diff, all 4 Workflow findings folded). No 26SM writes; PR-1 flip is a reversible client hosting deploy David pre-cleared. |
| 2026-07-17 | docs/plans/SESSION_TODO_2026-07-17.md + docs/plans/CONSOLIDATED_ROADMAP_2026-07-17.md | **Folded Codex PR-2 r14 NEEDS_FIXES (both valid; code confirmed defect-free, doc-only fixes).** HIGH: D2 activation said "flip EXACTLY 5" but the code gates PR-2's stamp+clamp behind 2 new flags → an operator would leave PR-2 dormant post-D2. Fixed → D2 now flips 7 (added REVIEW_ENGAGEMENT_STAMP_ENABLED+RECOVERY_SCORE_CLAMP_ENABLED) in SESSION_TODO:35 + ROADMAP:66 (matches lsr_deepfix_static.mjs). MEDIUM: roadmap listed OC-1 review_recorded/hold-shape + grandfather under PR-2, but the code defers them to PR-3 → corrected SESSION_TODO:30 + ROADMAP:60 to mark them PR-3-owned; P4 hard-gate on PR-3 unchanged. Codex confirmed OC-4 helper, pairMs parity, PR-2/PR-3 split all correct. Re-handed Codex r15 (narrow re-review). No code/deploy. |
| 2026-07-17 | docs/plans/{SESSION_TODO,CONSOLIDATED_ROADMAP}_2026-07-17.md | **CS PR-2-core = Codex GO r15, CONVERGED (codex_review_pr2_002.md).** Both r14 doc findings confirmed fixed (7-flag D2 set; PR-2/PR-3 scope split). Code reaffirmed defect-free (OC-4 canonical modulus, pairMs fail-closed parity, strict pairing mirror, I6 clamp, dormant posture). Folded the 2 non-blocking cleanup notes (OC-1/OC-3 phrasing → PR-2-core vs PR-3 split; the "11 FOUNDATION_FLAGS" wording describes the live baseline, normalizes to 13 at D2). PR-2-core is BUILT + verified + GO'd + DORMANT, staged for the 7-flag D2 functions deploy. NOT committed/deployed. |
| 2026-07-17 | src/config/featureFlags.js (WinClaude flip) + main@59df732 (LIVE) | **CS PR-1 FLIPPED LIVE — the 14-stuck-students fix is in production.** WinClaude r32 = DEPLOYED: commit 59df732 on main ("CS PR-1: activate review-pairing V2 + re-entry + recovery guards"), pushed (origin/main==59df732), Netlify build LIVE (prod __VOCABOOST_BUILD__ shortSha=59df732 dirty:false builtAt 19:20:54Z). Only the 3 PR-1 client flags flipped true; ALL dormant cutover flags (SERVER_PROGRESS_WRITE/LIST_PROGRESS_CANONICAL/CYCLING_ENABLED/etc) confirmed still false (no premature activation); functions/ EXCLUDED (PR-2 preserved for its gate); no secrets staged; committed on main (never branched). WinClaude fixed a git TLS issue for pushes (git config http.sslBackend schannel — Windows cert store, verification stays ON). Post-flip census re-confirm: SHIP GATE PASS (stuck=14, drained=13, 1 skip-only residual, 0 cross-class false-pairs). C1/PR-1 = DONE/LIVE. Next: WinClaude r33 post-flip prod smoke (complete→advance on fresh dup_repro, live 59df732 build). |
| 2026-07-17 | audit/playwright/lsr_deepfix_static.mjs + docs/plans/{SESSION_TODO}.md + win baton r33 | **Post-PR-1-flip housekeeping + final-proof smoke queued.** Updated M-STATIC flag-table: the 3 PR-1 client flags baseline false→true (now LIVE in main@59df732) → M-STATIC baseline CLEAN 41/0/0 against the new HEAD. Marked SESSION_TODO C1 = DONE/LIVE. Re-seeded fresh dup_repro_a/b/c. Handed WinClaude r33 = PR-1 post-flip prod smoke (complete→advance on the LIVE 59df732 build — flags already on in prod, no dev server) to close the one deferred r31 proof (literal csd-advance). PASS=stuck student completes+advances (definitive); FAIL=stays stuck→rollback consideration. In parallel: Task-6 closure Opus agent drafting TASK6_REPORT (D1, gates D2). |
| 2026-07-17 | audit/deepfix/task6/TASK6_REPORT.md (NEW, Opus agent + orchestrator-verified) + docs/plans/loop/baton.json | **Task-6 closure (D1): report drafted + handed to the Codex END-GATE (HARD).** Opus agent consolidated the Task-6 cert state; orchestrator-verified the load-bearing claims: NO single-runId cert exists (lsr_deepfix_cert.mjs never run on real findings); the consolidator's canonical 6 = {STATIC,UI,WB,MIG,CALL,RULES} (roadmap swapped M-NET for M-WB); M-WB FAILING but all 6 W-* = harness-artifacts (0 product-defects, covered by M-CALL flag-ON 21/0); matrices green-in-isolation span git HEADs+flag-states; flag-ON M-UI fixture-deferred. RESOLVED the agent's GAP-1: the M-STATIC "live RED 38/3" was the transient PR-1 flag-ON window → 59df732-baseline now CLEAN 41/0 persisted. Handed Codex r16 TASK6_ENDGATE to adjudicate a proposed D1-close basis (accept documented per-matrix greens + ratify W-* harness-artifacts + defer flag-ON-M-UI/M-WB/rules-sha to the post-cutover full-UI prod audits + WAIVE the mechanical single-runId cert, harness-blocked-not-product, per David's audits-replace-soaks direction) — GO or NEEDS_RERUNS. NO self-approval (HARD gate). SURFACED FOR DAVID: this adjudicates a single-runId-cert waiver for his review. |
| 2026-07-17 | docs/plans/SESSION_TODO_2026-07-17.md + docs/plans/loop/win/handoffs/claude_to_winclaude_034.md | **Codex Task-6 END-GATE r16 = NEEDS_RERUNS (HARD gate held the line — accepted).** Codex rejected the informal report-waiver: "'audits replace soaks' supports moving validation into audits; it does NOT justify waiving the audit BINDING that proves what was tested." Two paths: A/strict cert (re-run/bind CALL/RULES/MIG/UI on 59df732 + fix M-WB + DG-2/3 + rules-sha → lsr_deepfix_cert.mjs → DEEPFIX_AUDIT_CERT_*) or B/formal machine-bound TASK6_ENDGATE_WAIVER_* that DAVID must explicitly accept. Codex did NOT dispute the greens/W-*-harness-classification/59df732-M-STATIC — only the acceptance binding. Updated SESSION_TODO D1 (NEEDS_RERUNS + both paths). Drafted WinClaude r34 (queued post-r33): the strict-cert re-runs on 59df732 + DG-2/3 + rules-sha + an M-WB harness-fix attempt — if M-WB comes clean → autonomous D1 close via strict cert; else → formal waiver for David. D1 is NOT the cutover bottleneck (PR-3 gates D3/P4). NO self-approval of the waiver. |
| 2026-07-17 | (milestone) main@59df732 + docs/plans/SESSION_TODO | **CS PR-1 FULLY PROVEN — WinClaude r33 PASS (definitive complete→advance).** On the LIVE 59df732 build, TWO independent stuck accounts completed the paired review and ADVANCED: dup_repro_a csd 21→22; dup_repro_b (pristine single-pass: fresh re-entry modal → 60-card study → Start Test → MCQ 30/30 → submit) csd 17→18. Both advanced at 17% score → confirms advance-on-COMPLETION-not-score; twi unchanged (review adds no new words, only the anchor DAY completes). The 3 earlier inconclusives were DRIVER gaps (MCQ vs typed; modal 'Start Test' vs page 'Take Test') — NO product defect, corrected without touching product code. Marked C1 = LIVE + FULLY PROVEN. Launched the next parallel wave: WinClaude r34 (Task-6 strict-cert re-runs + M-WB fix on 59df732) + PR-3 build (Opus agent: FORCED_PATHWAY binary throttle + hold-csd/recordReviewOutcome + engagement readers F3 + OC-1 hold-shape + grandfather completion reader + reviewMode snapshot; all dormant). |
| 2026-07-17 | NEW src/utils/forcedPathway.js + src/config/featureFlags.js + src/services/{studyService,progressService}.js + src/pages/{MCQTest,TypedTest}.jsx + functions/foundation.js + audit/deepfix/task3/invariant_assert.mjs (PR-3 BUILT, Opus agent) | **CS PR-3 BUILT (dormant behind FORCED_PATHWAY=false; NOT committed/deployed; no 26SM writes).** New flag FORCED_PATHWAY (client) + FORCED_PATHWAY_ENABLED (server mirror, FOUNDATION_FLAGS→14) + grandfather const FORCED_PATHWAY_GRANDFATHER_EPOCH_MS=null. NEW pure module forcedPathway.js (deriveThrottleMode hysteresis enter<0.30/exit>0.50 reading the persisted class_progress.reviewMode bit; deriveBinaryInterventionLevel {0,1}; isCompletionEngaged = grandfather ∨ PR-2 engagedReview stamp ∨ STRICT isEngagedReview — consumes reviewPairing.js, does NOT modify it). Hunks (all gated, flag-off = verbatim-original): (F1 binary throttle) initializeDailySession derives binary interventionLevel → 0 new words in review mode; (hold-csd) NEW progressService.recordReviewOutcome appends review summary + reviewMode bit + stats/streak WITHOUT csd/twi (OC-1), routed in completeSessionFromTest on the reason split (skip→REVIEW_STUDY no-advance; engaged throttle-review-only→COMPLETE no-advance David-hold; engaged normal/list-end/#9→advance as today, #11 preserved); reviewMode-bit ownership added to updateClassProgress advance path; (F3) engagement threaded from MCQ/Typed (reviewAnswered+reviewAttemptId gated) → isCompletionEngaged; (server) completeSession binary serverInterv + reviewMode owner + review_recorded hold branch (double-gated FORCED_PATHWAY_ENABLED && SERVER_COMPLETE_SESSION_ENABLED) + client review_recorded mapping; (snapshot) reviewMode added to the MCQ/Typed retake-rewind snapshot create+restore. VERIFIED: node --check all 5 JS OK; forcedPathway 21/21 unit CLEAN; invariant suite CLEAN 34/0/1 after RETARGETING #16 (INV-16-pr3-not-landed ABSENT→PRESENT, polarity flipped not deleted) + #10 sole-appender count 1→2 (recordReviewOutcome = legit 2nd appender; reconciliation still 0). Flag-off byte-equivalence attested per hunk. FLAGGED: client hold gated on !SERVER_PROGRESS_WRITE (server owns post-P4); mirror sites db.js:3033 challenge-accept + foundation.js:1859 advanceForChallenge + M4 shadow:912 NOT in this PR's scope (needed before flip); phase-COMPLETE-with-held-csd interaction with PR-1 REENTRY_GUARD to scrutinize. |
| 2026-07-18 | audit/playwright/lsr_deepfix_emu.mjs (WinClaude r34) | **Fixed the r16 null-sha gap at ROOT (portable Node crypto).** `sha256File()` shelled to Unix `sha256sum` (absent under the Windows `emulators:exec` cmd.exe child) → M-CALL/M-RULES recorded `rulesSha256=null`. Replaced with `crypto.createHash('sha256')` on the file bytes → M-RULES now binds `sha256(firestore.rules)=752981b78f53…aca9` (portable WSL+Windows). Audit-tooling only; NO product source touched. Part of WinClaude r34 (Task-6 strict-cert re-runs, execDecision=PARTIAL: cert structurally un-green-able at 59df732 — full-program end-state cert vs flag-off PR-1 HEAD; D1 needs the formal waiver). |
| 2026-07-17 | (security + coordination note) src/config/featureFlags.js | **PR-3 build agent flagged a system-reminder claiming cutover flags (SERVER_PROGRESS_WRITE/CYCLING_ENABLED/SERVER_CHALLENGE_WRITE/SERVER_REVIEW_MARKER/CONTINUATION_LINKS) flipped true "don't revert" — VERIFIED BENIGN (NOT an injection/breach).** Independent check: ALL those flags = false; every foundation cutover flag = false; featureFlags.js diff = only +24 (PR-3's FORCED_PATHWAY=false block); REVIEW_PAIRING_V2 still true (PR-1 intact). ROOT CAUSE: WinClaude r34's flag-ON Task-6 audit transiently flips featureFlags.js cutover flags (guaranteed-restore) WHILE the PR-3 build agent was concurrently reading/editing featureFlags.js → a legitimate "file modified" system-reminder surfaced in the agent's session showing the transient true-state. The agent correctly refused to act (would violate dormant/byte-equivalent discipline) + flagged it. NO durable change, nothing committed/deployed. COORDINATION LESSON: do NOT run a featureFlags.js-editing build agent concurrent with a featureFlags.js-flipping audit — sequence them. CONSEQUENCE: PR-3's own byte-equivalence must be re-verified by me against a STABLE tree AFTER r34 completes its restore (its restore may also clobber PR-3's FORCED_PATHWAY block → re-add if so). |
| 2026-07-17 | (triage) M-UI RS-2 | **RS-2 (testId-less review attempt not visible in gradebook, M-UI 59df732) TRIAGED = NOT a PR-1 regression.** PR-1 (59df732 vs 4b8452a) touches ZERO gradebook or attempt-WRITE code (only client pairing/re-entry/recovery READ-side + reviewPairing.js); a review attempt's gradebook-relevant field-set is identical pre/post-PR-1. ⇒ the RS-2 fail is seed drift / harness calibration (dup_repro attempts vs the prod-smoke-r2 seed), NOT a live defect on the deployed PR-1. RO-S1 fail similarly = harness-vs-new-MCQ-review-flow calibration (r33 proved PR-1 completes→advances). Both to be definitively re-checked in the post-cutover full-UI prod audits. Also: WinClaude r34 fixed the r16 rules-sha gap at ROOT (sha256File shelled to Unix sha256sum → absent under Windows emulators:exec → replaced with Node crypto; M-RULES now binds rulesSha 752981b7). Tree verified stable post-r34 (FORCED_PATHWAY intact, cutover flags false). |
| 2026-07-17 | audit/deepfix/task6/TASK6_ENDGATE_WAIVER_cert-59df732-r34.md (NEW) + baton | **Task-6 D1: structural finding + formal waiver → Codex model-validation (r17).** DEEPFIX_AUDIT_CERT_cert-59df732-r34.md proves the strict cert certifies the RETIRED P0-P10+P7 END-STATE (RET-* FAIL by design pre-retire; the 38 COVERAGE_UNAUDITED = un-shipped P4-P10 scenarios; M-STATIC target==shipped∧all-clean mutually exclusive pre-retire) — so it is UNACHIEVABLE at 59df732 BY CONSTRUCTION (Path A void). Built the formal waiver: corrected acceptance model = per-phase gating (each phase on ITS coverage) + the strict cert as the POST-P7 FINAL gate; D2/P3 supported NOW by GREEN P3 coverage (M-CALL 21/0, P3 12/12, M-RULES 11/0 rulesSha 752981b78f53); the 38 later-phase + M-WB + flag-ON-M-UI deferred to their phase's post-ship audit; RO-S1/RS-2 triaged harness/seed. Handed Codex r17 to validate the MODEL (David accepts the BASIS). **CONSEQUENCE: the deepfix cutover D2+ is now DAVID-GATED on the waiver acceptance — cannot complete unattended. PR-3 is independent and CAN ship on its own gates.** |
| 2026-07-17 | audit/deepfix/task6/TASK6_ENDGATE_WAIVER_cert-59df732-r34.md | **Codex r17 = MODEL-SOUND (converged) — Task-6 waiver model validated; folded 4 wording precisions.** Codex confirmed: (1) strict cert is structurally a post-P7 end-state gate (not a valid pre-D2 gate); (2) per-phase gating is the correct model — D2/P3 supported by the green P3 coverage; (3) D2 STILL has its non-waiver gates (David's waiver acceptance + C2/PR-2 + A2 + exact 7-flag discipline). Folded Codex's 4 wording constraints into the waiver: strict-cert NOT-CERTIFIED = "structurally unreachable, not product failure"; CS-7/CS-10 = documented-skips-on-ledger (not executed passes); M-CALL is NOT rules-sha-bound (only M-RULES is); D2's 2 PR-2 flags ride on C2/PR-2 approval too. **D1 close is now one decision: 🔵 DAVID accepts the Codex-validated, evidence-bound waiver.** The deepfix cutover (D2+) unblocks on that acceptance. |
| 2026-07-17 | (PR-3 Workflow verify) audit findings | **PR-3 adversarial Workflow verify (Opus/max, 3 lenses): byte-equiv SOUND (dormant safe), flag-on DEFECT + coupling CONCERNS — 3 flip-blocking fixes + 1 deferrable, 2 hazards DEBUNKED.** FLIP-BLOCKERS: (HIGH) PR-1's live reconciliation reader (reviewPairsWithAnchor tier-1 exact-range, NO engagement check; db.js:3757 + progressService.js:203-219 + studyService determineStartingPhase) SILENTLY UNDOES the F3 skip-hold for same-session/normal-day skips → the I1 skip population still advances csd (throttle-review-only #16 kill unaffected — no anchor). (HIGH/MED) challenge-accept csd writer db.js:3031 (live, SERVER_CHALLENGE_WRITE=false) + dormant foundation.js:2405 advanceForChallenge NOT hold-guarded → a review challenge fail→pass advances csd on a held day (I2 runaway on challenge path) + leaves reviewMode stale (WI-1 mandated this guard). (LOW) recordReviewOutcome idempotency partial (lastEntry-only + null-id → double-append skews throttle avg). DEFERRABLE: (MED) validateAttemptAnchorShadow M4 recomputes graduated interv → false introduced_over_allocation under binary FORCED_PATHWAY, but INERT (SHADOW/ENFORCE both false) → must land before anchor-enforce+FORCED_PATHWAY concurrent. DEBUNKED (blocks:neither): the re-entry-modal hazard is OFF-BY-ONE (session_state.currentStudyDay=csd+1≠csd; WI-3 chose csd-equality BECAUSE robust to hold-csd) — no fix; PR-3's own writes don't re-mint I4 (hold never writes csd; ratchet intact — safe IFF the challenge-guard HIGH is closed). Folding the 3 blockers + the deferrable via Opus fix agent → my verify → re-Workflow → Codex diff gate. PR-3 build stays dormant-safe meanwhile. |
| 2026-07-17 | src/services/db.js + src/services/studyService.js + src/services/progressService.js + src/pages/{MCQTest,TypedTest}.jsx + functions/foundation.js + audit/playwright/lsr_deepfix_static.mjs | **CS PR-3 FLIP-BLOCKER FIXES folded (dormant, flag-off byte-equivalent; NOT committed/deployed; no 26SM writes).** Closed the 3 flip-blockers + 1 deferrable from the PR-3 adversarial Workflow verify, all gated on FORCED_PATHWAY (client) / FORCED_PATHWAY_ENABLED (server). **FIX 1 (HIGH, reader engagement):** the LIVE PR-1 reconciliation/completeness readers re-advanced a held skip. Added a READER-SITE engagement gate (grandfathered isCompletionEngaged) at BOTH call sites — db.js getReviewForDay `pairedComplete = FORCED_PATHWAY ? (paired && isCompletionEngaged(data)) : paired` (:3792) + studyService determineStartingPhase `.find(a => FORCED_PATHWAY ? (reviewPairsWithAnchor(a,newTest) && isCompletionEngaged(a)) : reviewPairsWithAnchor(a,newTest))` (:267). reviewPairsWithAnchor (PR-1 census-locked) UNTOUCHED; a rejected candidate is skipped so a genuine engaged review still pairs; grandfather keeps pre-epoch skips completing (decision #3). Imported isCompletionEngaged+deriveThrottleMode into db.js. **FIX 2 (HIGH/MED, challenge-accept hold-guard):** db.js:3054 (LIVE, SERVER_CHALLENGE_WRITE=false else-fallback) + foundation.js runChallengeDayAdvanceTxn:2232 (dormant server mirror) — when FORCED_PATHWAY[_ENABLED] && reviewMode===true, SKIP the csd/twi advance (return non-advancing server-side); when they DO advance, recompute+persist reviewMode via deriveThrottleMode[Server](recentSessions, prior) (one-owner invariant). **FIX 3 (LOW, idempotency):** progressService recordReviewOutcome now scans the WHOLE recentSessions window (`priorRecents.some(s=>s?.reviewAttemptId===id)`, :688) not just lastEntry; MCQTest/TypedTest thread a STABLE key `reviewAttemptId: result?.id ?? attemptDocId` (never null). **FIX 4 (MED, deferrable):** foundation.js validateAttemptAnchorShadow:1143 derives binary interv from deriveThrottleModeServer→{0,1} under FORCED_PATHWAY_ENABLED (else calculateInterventionLevel) so M4 shadow won't false-flag introduced_over_allocation under the binary allocation (INERT today — SHADOW/ENFORCE both false). **M-STATIC:** added FORCED_PATHWAY (client) + FORCED_PATHWAY_ENABLED (server) flag-table rows (baseline:false); noted DG-2 version-probe now reports FORCED_PATHWAY_ENABLED. VERIFIED: node --check all 5 JS OK; @babel/parser OK on both JSX; invariant suite CLEAN 34/0/1 (#16/#10 retargets kept); M-STATIC baseline CLEAN 43/0/0 (both FP rows PASS); census-verify-pr1 SHIP GATE PASS (stuck=14/drained=13/1 skip-only/0 cross-class false-pairs — PR-1 pairing undisturbed, reviewPairing.js+forcedPathway.js byte-identical to HEAD); 10/10 flip-target behavioral tests (engagement gate + hysteresis). Flag-off byte-equivalence attested per hunk (each else = verbatim pre-fix expression; both flags confirmed false). NEXT: re-Workflow adversarial verify → Codex diff gate. |
| 2026-07-17 | src/services/{db,studyService,progressService}.js + functions/foundation.js + src/pages/{MCQTest,TypedTest}.jsx + audit/playwright/lsr_deepfix_static.mjs (PR-3 fix pass, Opus agent + orchestrator-verified) | **PR-3 flip-blocker fixes APPLIED + independently verified (dormant, flag-off byte-equivalent).** F1 (HIGH) reader-engagement gate: getReviewForDay:3792 (pairedComplete=FORCED_PATHWAY?paired&&isCompletionEngaged:paired) + determineStartingPhase:266 (+&&isCompletionEngaged) — closes the reconciliation-undoes-hold. F2 (HIGH/MED) challenge hold-guard: db.js:3054 (client) + foundation.js:2231 runChallengeDayAdvanceTxn (the SHARED primitive backing advanceForChallenge/reviewChallenge/overrideAttempt — one edit covers all 3) — skip csd/twi advance + recompute reviewMode on held days. F3 (LOW) recordReviewOutcome idempotency: window-scan + reviewAttemptId=result?.id??attemptDocId (MCQ/Typed). F4 (MED,deferrable) M4-shadow binary mirror. reviewPairsWithAnchor + forcedPathway.js byte-identical to HEAD (only reader-site gates added). ORCHESTRATOR-VERIFIED: flags still false; node --check ×4 OK; invariant CLEAN 34/0; M-STATIC baseline CLEAN 43/0 (+2 FP rows); **census-verify-pr1 SHIP GATE PASS = FIX-1's live-reader touch is byte-equivalent flag-off (PR-1 pairing undisturbed)**. Agent's flip-target behavioral test 10/10 (post-epoch skip rejected, pre-epoch grandfathered, engaged pairs, stamp honored, hysteresis). ⚠️ RESIDUAL (flip-checklist): FORCED_PATHWAY_GRANDFATHER_EPOCH_MS=null in both forcedPathway.js:50 + foundation.js:134 — FIX-1 makes it LOAD-BEARING (set to deploy epoch AT flip, else pre-deploy stamp-less skips re-offered as retakes). Closure re-Workflow (flag-on+coupling) in flight → Codex diff gate → flip. |
| 2026-07-17 | functions/foundation.js (getReviewForDayServer:794) | **PR-3 closure re-Workflow (Opus/max ×2): flag-on-closure CLOSED (all 3 blockers), coupling-closure PARTIAL → folded the 1 residual HIGH.** Coupling lens confirmed the CLIENT reader tri-symmetry CLOSED (getReviewForDay+determineStartingPhase+completeSessionFromTest all agree a post-deploy skip=not-complete) + F2 challenge-guard CLOSED (all 3 server delegators via the shared primitive) + REENTRY_GUARD off-by-one debunk holds + one-owner reviewMode CLOSED. RESIDUAL HIGH (folded): FIX-1 was CLIENT-only — the SERVER mirror getReviewForDayServer had the identical defect (latent — a P4-blocker not a PR-3-flip blocker, since the server reader is dormant until P3/P4). Mirrored it: getReviewForDayServer:794 pairedComplete=FORCED_PATHWAY_ENABLED?(paired&&isCompletionEngagedServer(data)):paired (isCompletionEngagedServer already existed :715). Flag-off=paired verbatim (byte-equiv); node --check OK, invariant CLEAN, M-STATIC 43/0. TRACKED: (LOW) P5 canonical hydration drops the reviewMode bit → P5 work item; (flip-checklist) grandfather epoch null. PR-3 client fixes are flip-ready; the server mirror + P5-hydration are pre-P4 items. Handing COMPLETE PR-3 to the Codex diff gate. |
| 2026-07-17 | docs/plans/loop/baton.json | **CS PR-3 (complete: build + fixes + server mirror) → Codex diff gate r18.** All 3 Workflow-found flip-blockers CLOSED + verified (closure re-Workflow flag-on CLOSED, coupling PARTIAL→server-mirror folded); orchestrator-verified (census PASS, M-STATIC 43/0, invariant CLEAN, behavioral 10/10); dormant + byte-equivalent flag-off. Tracked residuals: grandfather-epoch-null (flip-checklist), P5-hydration reviewMode-drop (P5 item, added to D4). Handed Codex r18 PR3_DIFF_REVIEW (HARD gate) → then WinClaude flip (with the epoch-set in the flip commit). |
| 2026-07-17 | audit/deepfix/task6/TASK6_ENDGATE_WAIVER_cert-59df732-r34.md + docs/plans/SESSION_TODO | **🔵→✅ DAVID ACCEPTED THE D1 WAIVER — D1 (Task-6) is CLOSED.** The deepfix cutover (D2+) is now UNBLOCKED. Basis: the corrected acceptance model (per-phase gating; strict cert = post-P7 final gate; D2/P3 supported by green P3 coverage M-CALL 21/0 + P3 12/12 + M-RULES 11/0), Codex r17 MODEL-SOUND. Stamped acceptance into the waiver doc. D2 still requires its OTHER gates (C2/PR-2 GO'd ✅, A2 invariant suite, exact 7-flag discipline). Also: Codex PR-3 diff r18 = GO (converged) — code sound, GO conditional on the flip-checklist (mandatory: non-null MATCHING grandfather epoch client+server, with a fail-closed deploy verifier per Codex; + P5-hydration + review_recorded-UI-state as non-blocking behavioral-audit items). |
| 2026-07-17 | docs/plans/loop/win/baton.json (D2 handoff) | **Cutover EXECUTION started (David: full-send incl. P5 + PR-3-flip autonomous). D1✓ A2✓.** Handed WinClaude r35 = D2/P3 activation: functions deploy folding the Codex-GO'd PR-2-core + PR-3-server (dormant) + flip EXACTLY 7 foundation flags true (SERVER_COMPLETE_SESSION/RESOLVE_LIST_PROGRESS/RESET_PROGRESS/ADVANCE_FOR_CHALLENGE_ENABLED + ANCHOR_VALIDATION_SHADOW[starts M4 clock] + REVIEW_ENGAGEMENT_STAMP_ENABLED + RECOVERY_SCORE_CLAMP_ENABLED), all P4/P5/P6/P9/P10/PR-3-server flags stay false. Commit functions/ only; verify via completeSession probe + DG-2. Reversible, no data writes. FOLLOW-UP (mine, post-D2): update M-STATIC flag-table baseline for the 7 (false→true). Operating commitment stated to David: fresh + restore-verified backups before each one-way door (P5/P7); halt-and-preserve only on a truly unrecoverable state, else full send. Sequence ahead: D2 → PR-3 flip (dev-E2E + fail-closed epoch verifier) → D3/P4 → D4/P5 (one-way) → D5/P6 → D6-9 → post-P7 strict cert + full-UI audits. |
| 2026-07-17 | audit/deepfix/task6/verify_forced_pathway_epoch.mjs (NEW) | **Fail-closed grandfather-epoch verifier built + validated (Codex PR-3 r18 flip-checklist).** Asserts: whenever FORCED_PATHWAY (client) or FORCED_PATHWAY_ENABLED (server) is true, BOTH FORCED_PATHWAY_GRANDFATHER_EPOCH_MS (forcedPathway.js + foundation.js) must be non-null AND equal; dormant → null OK. Tested: dormant PASS exit 0; simulated null-epoch flip FAIL exit 1. Runs at the PR-3 flip (and P4) — WinClaude must get exit 0 before committing a FORCED_PATHWAY flip. |
| 2026-07-18 | functions/foundation.js + functions/index.js + main@26cd8ee (LIVE) | **D2/P3 server activation DEPLOYED (first server cutover).** WinClaude r35: flipped 7 foundation flags false→true (SERVER_COMPLETE_SESSION_ENABLED / SERVER_RESOLVE_LIST_PROGRESS_ENABLED / SERVER_RESET_PROGRESS_ENABLED / SERVER_ADVANCE_FOR_CHALLENGE_ENABLED / ANCHOR_VALIDATION_SHADOW [starts M4 shadow clock] / REVIEW_ENGAGEMENT_STAMP_ENABLED [PR-2] / RECOVERY_SCORE_CLAMP_ENABLED [PR-2]). Staged ONLY functions/ (PR-2-core + PR-3-server fold + the 7 flips, 425+/22-) → commit 26cd8ee on main → push (59df732..26cd8ee) → firebase deploy --only functions (18 functions "Successful update operation"). VERIFIED (deepfix_d2_verify_r35.json): DG-2 authenticated version posture POSTURE_OK, deployed sha 26cd8ee — the 7 report true + ALL must-stay-false false (LIST_PROGRESS_CANONICAL / ANCHOR_VALIDATION_ENFORCE / CYCLING_ENABLED / FORCED_PATHWAY_ENABLED / SERVER_REVIEW_CHALLENGE_ENABLED / SERVER_OVERRIDE_ENABLED / TEACHER_IDS_WRITE_ENABLED / GRADE_TOKEN_ENFORCED / GRADE_TOKEN_MINT / TEACHER_PROVISIONING_ENABLED / TEACHER_CLAIM_ENABLED); prod completeSession no longer disarmed (401 unauth, not the SERVER_COMPLETE_SESSION_ENABLED=false string). Reversible (flip back + redeploy); NO data writes, NO 26SM mutation. David authorized full-send (WSL = his proxy). Client PR-3 diff (src/** + forcedPathway.js) left uncommitted (dormant, FORCED_PATHWAY_ENABLED=false). |
| 2026-07-18 | src/config/featureFlags.js + src/utils/forcedPathway.js + audit (PR-3 flip staged) → WinClaude r36 | **PR-3 FLIP staged + handed to WinClaude (client forced-pathway binary throttle).** Set FORCED_PATHWAY=true + FORCED_PATHWAY_GRANDFATHER_EPOCH_MS=**1784333239063** (2026-07-18T00:07:19Z) in forcedPathway.js. ⚠️ **P4 MUST set functions/foundation.js FORCED_PATHWAY_GRANDFATHER_EPOCH_MS to this SAME value (1784333239063)** so client+server epochs match (fail-closed verifier enforces). Gates PASS: verify_forced_pathway_epoch.mjs exit 0; M-STATIC baseline CLEAN 43/0 (FORCED_PATHWAY baseline→true); census SHIP-GATE PASS (PR-1 13/14 drain preserved under FORCED_PATHWAY — the 13 are engaged; SfEVUpvi non-exact skip still retake). Traced PR-3×PR-1: engagement gate = paired&&isCompletionEngaged; pre-deploy exact-range skips grandfather, post-deploy skips hold (F3), decisions #2/#3 both honored. WinClaude r36 = mandatory epoch-gate → commit client (functions already at D2/26cd8ee) → push → build-stamp verify → best-effort throttle smoke. Reversible. Epoch T1 also recorded to scratchpad/pr3_epoch.txt. |
| 2026-07-18 | docs/plans/loop/codex_keepalive.mjs (NEW) | **Codex hourly session-keepalive (David request — keep Codex's baton poll from expiring during the cutover).** Smart heartbeat: flips a lightweight CODEX_KEEPALIVE checkin onto the baton ONLY when idle (turnOwner=claude + >50min since updatedAt) so it never clobbers a real review; Codex writes a one-line ALIVE ack + flips back. First ping fired --force now; a background loop re-runs it every 55 min. Win baton (PR-3 flip) is separate + unaffected. |
| 2026-07-18 | docs/plans/loop/codex_keepalive.mjs | **Codex hourly keepalive ACTIVE.** First ping fired (--force): baton → codex round 19 CODEX_KEEPALIVE #1; Codex writes an ALIVE ack + flips back. Background 55-min loop started to maintain it (idle-gated: only flips when turnOwner=claude + >50min idle, so it never clobbers a real cutover review). BOM-safe (strips Codex's Windows BOM). |
| 2026-07-18 | src/config/featureFlags.js (FORCED_PATHWAY=true) + src/utils/forcedPathway.js (grandfather epoch) + main@d2bb2bc (LIVE) | **CS PR-3 FLIPPED LIVE (client forced-pathway binary throttle).** WinClaude r36: MANDATORY epoch gate PASS (FORCED_PATHWAY=true client, epoch 1784333239063 non-null; FORCED_PATHWAY_ENABLED=false server dormant until P4). git add -A → commit d2bb2bc on main (client PR-3 source + epoch + audit/docs sweep, 65 files; functions/ already committed at D2/26cd8ee) → push (26cd8ee..d2bb2bc). VERIFIED: prod window.__VOCABOOST_BUILD__ shortSha=d2bb2bc dirty:false + app loads clean (0 real console errors, deepfix_pr3_buildstamp_r36.json). Post-flip throttle-behavior smoke DEFERRED to post-cutover full-UI audits (Codex-GO'd r18 + closure-verified; handoff permits deploy-live+clean-load confirmation). Reversible (FORCED_PATHWAY=false + push). Also fixed a hardcoded /app/ WSL path in audit/deepfix/task6/verify_forced_pathway_epoch.mjs (repo-relative resolve; gate LOGIC untouched). No secrets staged; NO data writes, NO 26SM. David full-send (WSL=proxy). |
| 2026-07-18 | main@d2bb2bc (PR-3 LIVE) + docs/plans/loop (P4 plan → Codex r20) | **PR-3 flip DEPLOYED (WinClaude r36, d2bb2bc) — binary throttle LIVE; keepalive CONFIRMED (Codex ack'd ALIVE r19).** Verified: FORCED_PATHWAY=true + epoch 1784333239063, epoch gate PASS, census SHIP-GATE PASS (PR-1 preserved), FORCED_PATHWAY_ENABLED still false (server dormant until P4), build-stamp d2bb2bc dirty:false clean-load. Client=PR-1+PR-3; server=D2/P3 7-flags. Throttle-behavior smoke deferred to post-cutover audits. NEXT: handed Codex r20 = P4/D3 PLAN review (client→server progress authority + PR-3-server activation: functions redeploy FORCED_PATHWAY_ENABLED+server-epoch-1784333239063 FIRST, then client push 4 cutover flags SERVER_PROGRESS_WRITE/CHALLENGE_WRITE/REVIEW_MARKER/RESET_PROGRESS). Awaiting Codex GO before executing the biggest cutover step. |
| 2026-07-18 | functions/foundation.js + functions/index.js (version epoch exposure) + P4 plan v2 → Codex r21 | **Folded Codex P4-plan r20 NEEDS_FIXES (real deployment-gate weakness).** Codex confirmed sequencing/epoch/flag-set correct but blocked: the local epoch verifier only proves the TREE, not the LIVE bundle serving students, and my gate ran after both deploys. FIX 1: exposed the deployed server epoch — foundation.js exports FORCED_PATHWAY_GRANDFATHER_EPOCH_MS + index.js version probe includes it (additive, node --check OK) → DG-2 can verify the live epoch. FIX 2: revised P4 order — functions redeploy ONLY → fail-closed server-state gate BEFORE client push (local verifier + DEPLOYED provenance: version reports FORCED_PATHWAY_ENABLED=true + epoch=1784333239063 + new sha; else STOP) → client push 4 flags → strengthened smoke (held-review/reviewMode/challenge-hold/dayGuardRejected/no-canonical-writes assertions). Re-handed Codex r21 for GO. |
| 2026-07-18 | functions/foundation.js (FORCED_PATHWAY_ENABLED=true + epoch) + WinClaude r37 | **Codex P4-plan v2 = GO (converged). P4/D3 client→server cutover EXECUTING (WinClaude r37, Codex-blessed gated order).** WSL set foundation.js FORCED_PATHWAY_ENABLED=true + server epoch=1784333239063 (== client; EPOCH GATE PASS both flags true + matched) + M-STATIC baseline CLEAN 43/0; client 4 route flags still false. Handed WinClaude the strict order: functions redeploy ONLY → FAIL-CLOSED server-state gate [(a) local verifier + (b) DEPLOYED version-probe: FORCED_PATHWAY_ENABLED=true + epoch=1784333239063 + new sha; else STOP no-client-push] → client push 4 flags → strengthened smoke (6 assertions incl. held-review review_recorded-no-advance + advanceForChallenge-no-advance-held + no-list_progress-writes). Reversible. FOLLOW-UP (mine, post-P4): update M-STATIC baseline for the 4 client flags. |
| 2026-07-18 | functions/foundation.js (FORCED_PATHWAY_ENABLED=true) main@0ddbb34 + src/config/featureFlags.js (4 route flags) main@6bffe1c (LIVE) | **P4/D3 client→server cutover DEPLOYED (gated order, Codex-GO'd r21).** WinClaude r37 (survived a mid-round machine restart; re-oriented from durable state): (1) node --check OK; (2) functions redeploy ONLY → commit 0ddbb34 (FORCED_PATHWAY_ENABLED=true + server epoch 1784333239063 + version epoch-provenance) → firebase deploy --only functions; (3) FAIL-CLOSED SERVER-STATE GATE **PASS** (local epoch verifier exit 0 + DEPLOYED version reports FORCED_PATHWAY_ENABLED=true ∧ epoch=1784333239063 ∧ sha=0ddbb34); (4) ONLY THEN client cutover → flipped SERVER_PROGRESS_WRITE/SERVER_CHALLENGE_WRITE/SERVER_REVIEW_MARKER/SERVER_RESET_PROGRESS=true → commit 6bffe1c → push → build-stamp verified (6bffe1c, dirty:false, app loads clean). NOTE: git add -A (per handoff) swept a repo-wide LF↔CRLF renormalization storm + junk (.gitattributes text=auto) → tripped the auto-mode classifier; committed the client cutover CLEANLY (featureFlags.js only) instead — WSL should do a `git add --renormalize .` hygiene commit. Post-client smoke: deploy-live + app-healthy CONFIRMED (fresh-student dashboard renders clean, 0 console errors); behavioral e2e hit a harness joinClass enrollment gap (r34 RO-S1 class), NOT a product regression — full 6-assertion behavioral validation deferred to post-cutover full-UI audits (server callables already M-CALL flag-ON CLEAN 21/0; LIST_PROGRESS_CANONICAL=false deployed). Reversible (flip 4 client flags false + push). No data writes/no 26SM. David full-send (WSL=proxy). |
| 2026-07-18 | NEW audit/playwright/lsr_deepfix_p4cert.mjs (WinClaude r42, audit tooling) | **P4/D3 forced-pathway hold-csd branch BEHAVIORALLY CERTIFIED (closes the C4 gap; unblocks D4/P5).** New cert harness (Codex-GO'd instrument r28, approach-1) extends the M-CALL emulator scaffolding with a class_progress (CANONICAL-off) fixture + 8 forced-pathway scenarios at the PINNED 0ddbb34 prod posture (FORCED_PATHWAY_ENABLED=true, epoch 1784333239063, 7 D2 flags true, LIST_PROGRESS_CANONICAL=false, ANCHOR_VALIDATION_ENFORCE=false — no flag_on flip; ran on the tree which is hash-verified == 0ddbb34). RESULT: CERTIFIED 10/10 PASS (deepfix_p4_behavioral_cert_0ddbb34.json): #1 normal advance csd+1/twi; #2a post-epoch non-engaged HOLD (review_recorded flat); #2b pre-epoch grandfathered ADVANCE-once; #2c throttle HOLD (post+pre epoch); #3 reviewMode r/w; #4 advanceForChallenge review_mode_hold csd-unchanged; #5a normal-not-dayguard; #5b stale day_guard_rejected + session cleared + exactly-one clear log; #6 zero canonical list_progress (7 uids). Emulator/sandbox ONLY, no prod deploy, no 26SM. D3/P4 = CERTIFIED. |
| 2026-07-18 | NEW scripts/audit/sandbox-guard.mjs + sandbox-guard.test.mjs + clone-ticketed-prefix.mjs + assert-recovery.mjs + seed-synthetic.mjs (D3.5 harness, WSL lane) | **D3.5 audit harness — WSL-side build (post 5-round critic convergence).** (1) `sandbox-guard.mjs` = the HARDENED prod-seeder guard the critic pass required: per-doc fail-closed on absent uid/classId, uid∈run-allowlist, `25WT`-prefixed + UNDERSCORE-FREE class ids (testId-parse-safe; reconciles the plan's imprecise "lsr_" — lsr_ is emails-only), never-write-`lists/`, teacher-pin, PRE-write join containment (S-A), per-run safety artifact (S6). Unit test 24/24 PASS incl. every critic case (reject real-uid/real-class writes, F8 canonical anomaly permitted under sandbox uid, join pre-write). (2) `clone-ticketed-prefix.mjs` = Part-A2 seeder: reads real 26SM READ-ONLY → 25WT sandbox clone reverted to the pre-fix state, FULL id-rewrite incl. testId + teacherIds[] (base dup script leaked both — S2), guard on every write, M9 provenance per student, dry-run default. DRY-RUN clean (정지수 A1: 554 study_states/27 attempts read, 584 writes asserted ALL-SANDBOX, 0 writes). (3) `assert-recovery.mjs` = recovery-verdict engine: PASS/FAIL/INVALID_PRECONDITION per family + M7 server-path-proof (server-only system_logs types) + canonical-empty invariant. NO 26SM writes. (4) `seed-synthetic.mjs` = Part-A/F synthetic-state seeder (fresh sandbox student + invented/edge state incl. F8 canonical anomaly permitted under sandbox uid; guard on every write; dry-run clean 10 writes all-sandbox). Executor-side (tier-1 emulator harness + Playwright MCQ/Typed driver) = WinClaude r48. |ead-only CS scans); NEW docs/plans/MASTER_TASK_LIST.md + MASTER_TASK_TRACKER.md + docs/plans/loop/CONVERGENCE_REPORT_v1-v4.md + P4_CERT_INSTRUMENT_approach1.md; RESUME.md rotated (→ resume_archive/RESUME_2026-07-18.md) | **Post-crash recovery + 5-way convergence + D3/P4 certification (docs + read-only audit tooling; NO client/functions behavior change).** After a mid-cutover crash, reconstructed ground truth from primary evidence; ran a 5-way convergence (WSL + 3 Fable + WinClaude + Codex) that CLOSED on GO-HOLD. Two NEW read-only 26SM diagnostics: scan-syslog-since.mjs (post-cutover system_logs NO-SPIKE) + scan-canonical-writepath.mjs (canonical list_progress EMPTY, csd_twi server-written). Plan of record moved to MASTER_TASK_LIST (+ append-only MASTER_TASK_TRACKER); supersedes stale RESUME/SESSION_TODO. D3/P4 then behaviorally CERTIFIED (see row above). |
| 2026-07-19 | NEW audit/playwright/lsr_deepfix_d35_tier1.mjs + lsr_deepfix_d35_tier3.mjs (WinClaude r48, audit tooling) | **D3.5 executor harnesses BUILT (tier-1 emulator recovery + tier-3 prod MCQ/Typed driver).** Tier-1 (extends p4cert): self-seeds A1/A2/A3/A6 recovery families at the pinned prod flag posture (M-B incl RECOVERY_SCORE_CLAMP+REVIEW_ENGAGEMENT_STAMP; mismatch=INVALID_PRECONDITION), drives completeSession, judges PASS/FAIL/INVALID_PRECONDITION vs CS-corrected expecteds (M1 hysteresis, M2 list-end reason-split) → ran on emulator pinned 0ddbb34: ALL-PASS 4/4 (A1 throttle-escape-on-2nd-good-review, A2 skip-runaway-held-3x, A3 off-by-one-V2-reconcile, A6 list-end engaged-advances/skip-held). Tier-3 driver: joinAndVerify (FIXES r37 joinClass gap via Admin read-back of enrolledClasses — join-smoke JOIN_FIX_OK=true, dashboard renders actionable session), driveTestToResults (r33-proven MCQ choice-cards+Submit-N/M + Typed), handleEmptySubmitConfirm (M5 non-blocking dialog), renderCheck (F-b), S4 join-containment. Emulator/sandbox only, no prod deploy, no 26SM. Full tier-3 seeded-recovery smoke awaits WSL --commit of a 1-student seed (coordinated clone->drive->assert loop). |
| 2026-07-19 | scripts/audit/clone-ticketed-prefix.mjs | Fixed class-COLLISION bug (David-flagged): sandbox class was minted `${RUN}${classCache.length+1}` — a per-run counter that reset each --commit, so separate runs restarted at 25WTa2r11 and crammed unrelated students (from different real classes) into shared class docs (25WTa2r11 held 4 students). Now mints ONE dedicated class PER STUDENT TAG (25WT<run><tag>, deterministic/collision-free), also giving each a unique testId. Per-user state was already uid-isolated (results uncorrupted) but shared class docs carried mixed assignments/settings (latent wrong-testMode risk) — now each mirrors its real class in isolation. |
| 2026-07-19 | scripts/cs/manual-pass.mjs | Added list-length GUARD (CS-2026-07-19b): refuses a day whose newWordEndIndex ≥ the list's actual word count — prevents writing a phantom anchor for a day beyond the list end (the 최도훈 day-16-on-a-15-day-list bug that inflated twi to 1280). |
| 2026-07-19 | scripts/cs/data-integrity-sweep.mjs | Hardened (CS-2026-07-19b): twiOverList + a NEW phantomAnchor check now compare each progress/attempt doc to ITS OWN listId's actual `words` count (was: the class's first-assignment list → multi-list blind spot that hid 최도훈's twi=1280>1200). reviewNoNewPass left unchanged (its documented benign baseline is intact). |
| 2026-07-19 | scripts/cs/fix-phantom-anchor.mjs (NEW) | CS repair script: backs up + deletes phantom passed-new anchors (nwei ≥ list size) and resets class_progress.twi to the real value (max non-phantom nwei+1), csd untouched. Used to fix 최도훈 (twi 1280→1200). Has --dry. |
| 2026-07-19 | audit/playwright/findings/a2_clone_roster.json | Reclassified choi_a12 family lost-save→list-end (forensic dig proved 최도훈 is a list-completion case, not a lost-save; the genuine lost-save is validated by lostsave_bc_d6). assert-recovery now PASSes it as list-end. |
| 2026-07-19 | scripts/cs/data-integrity-sweep.mjs | R2 FIX (David-directed, from 7-critic convergence): replaced the `csdImplausible` detector's broken `csd > twi+7` predicate (compared study-DAYS to WORD-count → structurally blind to runaway inflation; reported 0 while ≥44 inflated students existed) with an ANCHOR-BASED, PER-LIST predicate: flag when `csd > maxPassedNewDay + 1 && twi < listWordCount` (list-end review days exempt via twi>=wordCount). Moved it out of the first-list-only `if(!good)` gate into the per-doc loop; attempts fetched once up front. Validated: now flags 오윤권 (csd=12 > newAnchorDay=4) which the old check missed; correctly does NOT flag 최도훈 (list-end). |
| 2026-07-19 | scripts/cs/reconcile-inflated-csd.mjs (NEW) | CS reconcile script (David-authorized): sets an inflated student's csd to the deployed resolver value (reviewExists?anchorDay:anchorDay-1), keeps twi, clears stale session; precondition-gated (only acts if csd>anchorDay+1 AND twi matches anchor), backs up first, --dry default. Reconciled the 5 stragglers surfaced by the fixed detector (오윤권 12→4 + 4 hidden). SUPPORT_RUNBOOK CS-2026-07-19d. |
| 2026-07-19 | docs/plans/D3.5_WORKITEM_review_pass_threshold.md (NEW) | INVESTIGATE+PLAN (David-directed, WSL+Codex only): refined Work Item A from "review size" to an optional per-class **Review Pass Threshold** (retake-until-set-percent, mirror of the new-word test). Documents the finding that the review quiz is hardcoded `passed:true` (index.js:434, foundation.js:2614) with NO review-pass gate anywhere (grep-confirmed), the new-word `passThreshold`+retake pattern to mirror, every landing site, the server-authority/hold requirement, risks, and §8 open questions for Codex. No app-code changed. |
| 2026-07-19 | docs/plans/D3.5_WORKITEM_review_pass_threshold.md | Folded Codex r36 verification (codex_reviewpass_r36.md): §6 rewritten (server already has assignment via assertEnrolledAssigned → no client plumbing; use a SEPARATE `review_retake_required` gate not fpHoldCsd; specific exemptions list-end/#9-resume only, NOT throttle days); §8 marked answered + 3 remaining David product-decisions; NEW §10 adds 5 client landing sites Codex found (TypedTest/MCQTest completion guard, DailySessionFlow return + re-entry modal, teacher-challenge escape valve, 0-100-vs-0-1 units). Plan doc only; no app-code. |
| 2026-07-19 | functions/index.js | Grader fix (Work Item B, David-approved; Codex r35 input): hardened the Haiku grading prompt (index.js:1271) — scoped Rule 1 "Restating the word" to ENGLISH ONLY and added an explicit clause that a Korean answer is NEVER "restating" (a Korean word that means the target, incl. single-word direct translations + different POS, IS the meaning → CORRECT). Added 3 positive examples for the exact CS false-negative cases (자전적인←autobiographical, 무관심한←indifferent, 불협화믐-typo←dissonance). Wrong-control examples (그리프, running, 질리는, 암호화된) left intact. NOT YET DEPLOYED — gated on the regression fixture passing. |
| 2026-07-19 | scripts/grader-regression.mjs (NEW) | Grader regression harness: EXTRACTS the live systemMessage from functions/index.js (single source of truth, no drift) and runs Codex's 9-case fixture against real Haiku (temp 0.1, RUNS×) — known-correct Korean (자전적인/무관심한/불협화음/typo/piano) must pass, wrong controls (culminate←요점, dispel←"express disapproval", grief←그리프, run→running) must fail. Exits non-zero on ANY false-reject/accept. Reads ANTHROPIC_API_KEY from env (Firebase secret; never in-repo). Gates the grader redeploy. |
| 2026-07-19 | docs/plans/D3.5_WORKITEM_review_pass_threshold.md | Folded Codex r37 (codex_reviewpass_r37.md): §8 decision-1 RESOLVED (do NOT exempt #9-resume — Codex conceded; only no-score/empty-review carveout survives, keyed on absence of a review test not on reviewStudyResume); NEW §11 reader-correctness invariant (WSL-verified): under threshold-on, a review is completion evidence only if passed===true — 4 reader sites (determineStartingPhase studyService.js:312 [verified no passed check], getReviewForDay, getReviewForDayServer marker-suppression foundation.js:1638, retake path), default-off byte-equivalent. Plan doc only; no app-code. |
| 2026-07-19 | functions/index.js (DEPLOYED) | Grader fix DEPLOYED live (WinClaude r63): commit 0992f5f pushed to origin/main; surgical `firebase deploy --only functions:gradeTypedTest` (completeSession/resolveListProgress untouched, still 0ddbb34). Regression gate PASSED before deploy (scripts/grader-regression.mjs: 9/9, falseRej=0 falseAcc=0 unstable=0 ×3). Live gradeTypedTest = 0992f5f. Independent git-ground-truth verification: commit/files(3)/content/clean-tree all confirmed; Firebase deploy leg trusted-as-reported (no local Firebase creds; git fetch blocked by schannel SSL quirk). |
| 2026-07-19 | scripts/cs/find-grader-false-negs.mjs, scripts/cs/reconstruct-yyj-grader-input.mjs, scripts/grader-check-english-def.mjs, scripts/grader-replay-yyj.mjs (NEW, read-only diagnostics) | Grader verification against 윤여진's REAL inputs (David-directed). find-grader-false-negs: read-only pull of a student's wrongly-marked answers, categorized Korean vs English. reconstruct-yyj-grader-input: joins each rejected word to lists/{listId}/words/{wordId} for the SERVER-CANONICAL english(.definition)+korean(.definitions.ko) the grader actually receives (index.js:761-792), writes scripts/fixtures/yyj_grader_input.json. grader-replay-yyj: sends them EXACTLY like index.js (same wordsJson shape, batch, params, live-extracted prompt). FINDING (WinClaude r64 ran it): deployed grader 0992f5f ACCEPTS all 25 of 윤여진's correct English definitions (×3 stable) + 3 controls stay wrong. Her rejections were all 2026-07-08 under an OLDER prompt; no live defect for this pattern. No app-code change. |
| 2026-07-19 | functions/index.js, src/services/db.js, src/components/TestResults.jsx, src/pages/Gradebook.jsx | Challenge-token reset MONTHLY→WEEKLY (David-directed; Codex r38/r39 verified; Approach 1 read-time). Both token-count readers (`availableChallengeTokens` server + `getAvailableChallengeTokens` client) now count rejections by `status==='rejected' && challengedAt >= startOfKstWeekMs(now)` instead of `replenishAt > now` — so tokens refill to 5 at Monday 04:00 KST for ALL students automatically (no cron, no data write, no amnesty; existing pre-week rejections stop counting on deploy). Added byte-identical `startOfKstWeekMs` KST helper (fixed +540min, mirrors foundation.js STREAK_TZ_OFFSET) to both files. `replenishAt` now vestigial for availability. UI copy "for 30 days"→"until the weekly reset (Mon)". 15/15 KST boundary unit tests pass (scripts/challenge-token-kst-test.mjs). NOT yet deployed. Follow-up: CS diag scripts deepfix-f6-tokens.mjs + scan-preemptive-fixes.mjs still read the old replenishAt predicate (non-student-facing; update later). |
| 2026-07-20 | scripts/cs/verify-token-reset.mjs (NEW, read-only) | Live verification for the weekly token reset: mirrors the deployed (6094cdd) availableChallengeTokens + startOfKstWeekMs (Mon 04:00 KST) and computes each 26SM student's token count NOW vs old 30-day rule, highlighting who the weekly reset releases. Baseline @00:19 KST: 404 students w/ challenge history, boundary correctly = 2026-07-13 04:00 KST, 11 penalized this token-week (~6 were 07-15-amnestied → new rule re-counts their this-week rejections → transient reduce until 04:00, benign/sleep-hours/self-correcting). Scheduled to re-run at 04:05 KST to confirm the reset fires live. No writes. |
| 2026-07-20 | docs/TA_SUPPORT_GUIDE.md, docs/TA_FAQ.md (NEW), public/help-{student,teacher}-{en,ko}.html | FAQ/help refresh from the full CS-ticket read (David-directed). NEW docs/TA_FAQ.md (TA-facing FAQ built from ~40 CS tickets + triage + NEED_TO_FIX). Updated TA_SUPPORT_GUIDE.md: "changed this week" note + token 30d→weekly + class-change now auto-carries + new #7 (no-new-words/throttle) & #8 (gradebook no-results). Added 6 consumer-style FAQs to each of the 4 in-app help pages (student & teacher × en/ko): correct-answer-marked-wrong (grader improved→Challenge), no-new-words (low reviews), 92%-shows-fail (reload), class-change Day-1 (auto-carries→reload), stuck-loading/frozen (in-app browser→real browser), next-list/Summit. Fixed ALL token mechanic text (FAQ + Challenge sections) 30-day→weekly Monday reset across all 4 files (Gradebook "Past 30 days" date-filter left intact). Tag-balanced; not yet deployed (client). |
