# DEEPFIX2 WORK QUEUE — machine-read by `scripts/deepfix2/whats-next.mjs`

**SEQUENCED, NOT PARALLEL (David, 2026-08-03).** The server contract moved twice in one day, so
client work would be built against a shifting target. One item at a time; `after:` chains enforce it.

Format (one per line, parsed):  `- [S] <id> | <what> | blocker: <token>` — S: ` ` unstarted · `x` done · `>` IN FLIGHT (claimed, not ready, valid `after:` target) · `~` CARDED (parked, not ready, valid `after:` target); only an unblocked ` ` row ever makes the script exit 10.
Blocker tokens the script can RESOLVE by itself:
  `none`            — runnable right now
  `codex`           — the Codex baton is not back with claude
  `codex:YES`       — Codex has not returned a YES verdict (a returned baton is NOT approval)
  `win`             — the WinClaude baton is not back with claude
  `david:<what>`    — a decision only David can make (never auto-runnable)
  `after:<id>`      — another queue item must be done first

- [x] rules-artifact | Author + verify the merged rules artifact | blocker: none
- [x] rules-fix-r78 | Codex r78 blocker closed (agent-authored, I verified: 244/244 + pre-fix reproduction) | blocker: none
- [x] rules-receipt | Receipt/evidence INDEPENDENTLY RE-EXECUTED on a fresh harness (canonical 244/244 · full mutants regeneration byte-identical · pre-fix def5231f reproduced at 234/244, ten exact failures) · win 96 push verified by own ls-remote · r79 handed to Codex (flip follows the receipt commit) | blocker: none
- [x] rules-deploy-order | **DEPLOYED 2026-08-03** — order 97 executed, verdict DEPLOYED. Production ruleset `384c9c7a-b9ec-4f17-95ab-b72fff9c5fd1` (523 lines, sha16 f40f91fce3693b82) is BYTE-IDENTICAL to the certified artifact; the P10d trap did NOT ship. **Independently verified by me, not accepted from the report:** re-fetched production read-only (matched the executor's handback exactly) and re-ran the full matrix against THOSE fetched bytes — **262/262 green**. Rollback artifact preserved at live_baseline/firestore.live.PRE_R79_DEPLOY.rules; P10d draft preserved at audit/deepfix/task3/firestore.p10d.rules | blocker: none
- [x] engine-key-provenance-scan | CLOSED — full read-only cohort scan (scripts/deepfix2/engine-key-provenance-scan.mjs, NEW): **41,680 attempts, ZERO carrying any of the four engine keys, 0 quarantine candidates.** Codex r79's qualification is now measured rather than argued. Receipt: audit/deepfix/task3/live_baseline/engine-key-provenance-receipt.json. The artifact COMMENT repair is now DOWNGRADED: with the rules deployed, the claim is true on both legs, so the remaining nicety (cite this scan in the comment) BUNDLES with the next real rules change rather than forcing a comment-only production redeploy | blocker: none
- [x] typed-design | Engine typed-leg DESIGN (18_TYPED_LEG_DESIGN.md — reuse grading_jobs keyed on rv2_{presentationId}) | blocker: none
- [x] typed-fix-audit | CLOSED — cached-grade forgery closed at BOTH seams (usableCachedResults: engine provenance + presentation + answer-sheet hash), A3 completeDay wordId binding, A4 rv2_ replay provenance. Authoring delegated + independently audited (PASS WITH FINDINGS; the audit proved the already_graded sibling was code-true but evidence-free — now pinned by CASE TS + M-A1-SIBLING-CALL-SITE). Evidence re-executed by me: lap 395/395 · 7/7 typed mutants · rules 262/262 · 15/15 rules mutants. **NOT typed-leg readiness — see rv2-docid-collision and NEED_TO_FIX 21** | blocker: none
- [x] rv2-docid-collision | **FIXED 2026-08-03** — both derived global ids are now `rv2_{uid}_{presentationId}` from ONE shared `engineDocId()` (composer.js:117) called by callables.js:550 and typedGrading.js:260, so the legs cannot drift. presentationId UNCHANGED (already uid-scoped by path). NO migration: production had 0 rv2_ docs in attempts AND grading_jobs. The uid is a NAMESPACE, not a fence — the uid FIELD remains the fence. Fixtures: lap CASE RC + RC0 + the INVERTED TR(10) regression witness; mutant M-A1-UID-SCOPE-REVERT killed | blocker: none
- [x] rv2-refusal-status | **DECIDED 2026-08-03: a DISTINCT DATA status meaning *recompose, do not poll*** (not a discriminator field). One status cannot mean two opposite things; and the frozen RV2 list can be changed for free RIGHT NOW because the engine is dark and no client consumes these statuses yet. Recompose is also the ONLY student-side recovery from the card-19 denial. Implementation + both-leg fixtures ride with df2-51-client | blocker: none
- [x] rv2-collision-audit | DONE — independent audit returned PASS WITH FINDINGS (F1 the sharpest: 18_ §5.6 told readers a poisoned key is always self-inflicted, disproved by the fold's own fixtures). All findings folded at bd17873 | blocker: none

## THE UI BUILD (David 2026-08-04: build the UI now; Playwright/rehearsal work comes AFTER it exists)
- [x] rv2-refusal-status-impl | **SHIPPED 2026-08-04** — `grade_unusable` returned at the two PERMANENT sites (typedGrading.js:299 `return_cached`, :343 `already_graded`); the three TRANSIENT sites unchanged (:279 live lease, :350 no authority established, callables.js:655 concurrent submit). Frozen RV2 list + `isGradeUnusable()`, each status naming the other as its inverse. Lap 453/453, 11/11 mutants — both directions proven, which one mutant cannot do. CLIENT handling (recompose once, never loop) rides with df2-51b-submit | blocker: none
- [~] session-cutover (CONTAINER — not work; the four cutover-* folds below ARE the work) | **DECOMPOSED 2026-08-04 (delegator).** Surface measured: ~8,655 lines across DailySessionFlow.jsx (2,666), studyService.js (2,023), TypedTest.jsx (1,914), MCQTest.jsx (1,644), sessionService.js (408). The seam is `initializeDailySession` (5 call sites) + `completeSessionFromTest`. Today the CLIENT computes the review segment locally (wordIds/startIndex/endIndex) and grades; the engine does both server-side. TOO BIG FOR ONE FOLD — four, each behind REVIEW_V2_CLIENT with the legacy path intact as fallback, each with its own ledger | blocker: after:cutover-d-refusals
- [x] cutover-a-compose | **COMMITTED f9b423f 2026-08-04.** Review composition now comes from the engine behind REVIEW_V2_CLIENT (still false). Algorithm REPLACED (priority-bands-over-segment → cursor-chained rotation), so no fixture asserts the two agree. Lazy compose at review entry · presentedWordIds rendered verbatim · composeKey persisted. Opus audit PASS WITH FINDINGS — three undeclared student-visible changes folded (MCQ distractor pool narrowed → 3-option questions; dead range label; typed truncation guaranteeing an 83% cap) plus a silent fallback and a FALSIFIED receipt whose root cause is now structurally fixed. Evidence: pure 211/0 · emulator 89/0 · mutants 6/6 · lap 453/453. **VISUAL CHECK CLOSED** (win 98 PASS_WITH_GAP + win 99 CLEAN): review AND new-word phases both confirmed flag-off in a real browser | blocker: none
- [x] cutover-b-submit | **COMMITTED (local) + AUDITOR-VERIFIED GO 2026-08-04.** Submission routes through the engine `submitAttempt` behind REVIEW_V2_CLIENT (still false); adapter `reviewV2Submit.js` sends ONLY `{presentationId, answers}`; recompose-once on `grade_unusable` (persisted, fail-closed). Auditor (Opus, level-4): flag-off parity + payload law + recompose-once confirmed; pure 179/0, emulator 65/0 (passed WITH namespace guards live), mutants 2/2. **VISUAL CHECK OWED → cutover-b-visual** | blocker: none
- [x] cutover-b-visual | **CLOSED — win order 101 CLEAN 2026-08-04.** Typed submission authorized (David, ≤200); WinClaude drove the flag-OFF path END TO END: new-word study → typed test → SUBMIT → result card "100%, 20/20 correct" → phase ADVANCED to Review Study Day 5. 0 console errors. It verified the flag-gating itself (`rv2Handle` is null flag-off ⇒ the legacy path runs ⇒ the adapter is dead code, so what was exercised IS the legacy path) and read the stored attempt (score=100, matches the UI). The r098 gap is closed. **⚠ SURFACED AN OUT-OF-SCOPE LIVE-PATH FINDING → NEED_TO_FIX 26** (typed grader marked 20/20 garbage answers 100%) | blocker: none
- [x] cutover-c-complete | **COMMITTED + AUDITOR-VERIFIED GO 2026-08-04.** Test-driven day completion routes through engine `completeDay` behind REVIEW_V2_CLIENT (false); new adapter `reviewV2Complete.js`. Auditor (Opus, level-4): flag-off parity BYTE-IDENTICAL (empty diff on the 5 protected functions), V2 slot mapping correct, `already_completed`=terminal success (V3, mutant killed); pure 118/0 · emulator 40/0 (8 bypass rows) · mutants 2/2; cutover-b regression 179/0. **VISUAL CHECK OWED → cutover-c-visual** | blocker: none
- [x] cutover-c-visual | **CLOSED — win order 102 CLEAN 2026-08-04.** Full day driven to completion on 25WT MCQ: review test → submit → result (32%) → "Step 5 of 5 ✓ DAY 5 COMPLETE" + Session Summary → **Dashboard ADVANCED Day 5 → Day 6**. Legacy completion renders + advances unchanged; 0 console errors | blocker: none
- [x] cutover-d-refusals | **COMMITTED + AUDITOR-VERIFIED GO 2026-08-04.** (A1) fixed the grade_unusable recompose state-collision BUG the cutover-b audit missed (success → non-blocking banner, not the full-page interstitial; flag-gated) + (A2) closed the reuse_anchor_mismatch coverage gap + (A3) tokenized 6 raw-Tailwind refusal render sites (LIVE UI — 947 students' error banners). Auditor (Opus, level-4): A3 CSS-only + every token exists in index.css (no unstyled render), A1 sound; pure 90/0 · mutants 3/3; siblings unregressed. **VISUAL CHECK OWED (load-bearing, live UI) → cutover-d-visual.** Residual → NTF 27. RV2 refusal copy = separate register from df2-07 (E1/E2 carded) | blocker: none
- [x] cutover-d-visual | **CLOSED — win order 102 CLEAN 2026-08-04.** The refusal banner (forced via a network abort — NO AI spend) renders TOKENIZED + provably theme-aware in BOTH light AND dark (`bg-error` resolves per theme: light rgb(254,242,242) / dark rgb(127,29,29)); nothing unstyled, button + padding + readable text confirmed on screenshot. Narrower gaps flagged (non-blocking): the SUBMIT-specific banner wasn't isolated (same tokens ⇒ covered by construction); grade_unusable→recompose not reachable (as anticipated) | blocker: none
- [x] dashboard-streak-authority | **COMMITTED (local) + ORCHESTRATOR-VERIFIED (level-4 re-execution) 2026-08-04.** Reads the ACCOUNT-WIDE server `streak_credits` behind REVIEW_V2_CLIENT (client-only, direct read — rules allow; client calc stays flag-off). Verified: pure 32/0 + mutants 2/2 re-run; `calculateStreak` BYTE-IDENTICAL to HEAD; branch add-only; flag still false. New `streakAuthority.js#deriveAccountStreak` + `streakCredits.js#fetchAccountStreak` (`db` injected). Emulator 9/0 except the `orderBy(documentId(),'desc')` direction (local emulator can't run it — standard prod query, exercised at the rehearsal). **VISUAL CHECK OWED → dashboard-streak-authority-visual.** Cardinality changes per-list→account-wide flag-on (implements R2-21); `dashboard-df2-33` owns the display unification (E1). Ledger `_ledgers/dashboard-streak-authority-fold-ledger.md`. — **NEED_TO_FIX 25.** The engine writes `users/{uid}/streak_credits/{kstDate}` at day completion (completion.js:679, frozen R2-21 semantics); the dashboard computes its OWN streak client-side (Dashboard.jsx:37-123, consumed :1399) and **nothing in src/ reads the server credit**. Two authorities for one user-visible number, different inputs and different rules — and the student sees the client's. Read the server credit when the engine owns the day, keep the client calc as the flag-off path. Sequenced here because the credit only exists once completion runs server-side | blocker: none
- [x] dashboard-streak-authority-visual | **CLOSED — win order 103 TASK 1 CLEAN 2026-08-04.** Dashboard LOADS FULLY (hero + panels + tiles, no blank/interstitial) — the new streakCredits/streakAuthority imports don't break mount; streak renders normally ("1-day streak" pill + "1 days" tile, no NaN/undefined). 0 console errors. Flag-off parity proven in a real browser | blocker: none
- [x] dashboard-df2-33 | **LANDED 2026-08-04 — ORCHESTRATOR LEVEL-4 VERIFIED (fixtures 71/0 + mutants 3/3 re-run; diff read; flag false). ONE derivation `dayStatusAuthority.js` behind REVIEW_V2_CLIENT; the owed decision RESOLVED: Dashboard gets a READ-ONLY dashboard-local pure variant (zero Firestore verbs, data injected; deriveSessionState/DF2-20 absorbs it later); E1 settled (streak hero-only); rows add no new markers. VISUAL OWED → WinClaude (batched w/ df2-07a). Ledger `_ledgers/dashboard-df2-33-fold-ledger.md`.** Original card: **Dashboard one-affordance (DF2-33)** — hero + per-list on ONE derivation, closing the two-done-authorities class that `dashboard-streak-authority` is the first concrete instance of. Also owes an explicit decision the card names: whether Dashboard gets a READ-ONLY assembly variant, since the ordered-write pipeline must never run writes from a dashboard render | blocker: after:dashboard-streak-authority
- [x] df2-11-teacher-ui | **COMMITTED (local) + INDEPENDENT OPUS AUDITOR GO 2026-08-04.** Behind REVIEW_V2_CLIENT (still false), a review-settings group in BOTH teacher modals (AssignListModal + ClassDetail Edit) + BOTH writers (assignListToClass + updateAssignmentSettings) + the ClassDetail read card, writing reviewPassThreshold/reviewQueueSize/reviewTestSize/reviewGateEnabled(+reviewTestType) via a flag-scoped min/max→new-group SWAP; new pure validator `reviewSettingsAuthority.js` mirrors config.js:163-192. Auditor (Opus, level-4): re-ran pure 42/0 + mutants 6/6 killed; ALL 6 flag-off surfaces byte-identical; reviewGateEnabled default-TRUE on every path (no silent un-gating). D1 doc-repair done. Force-pass out (DF2-14); testSizeReview out (DF2-46); physical min/max delete rides the flip (DF2-02a, E1). Ledger `_ledgers/df2-11-teacher-review-settings-fold-ledger.md`. **VISUAL CHECK OWED → df2-11-teacher-ui-visual** | blocker: none
- [x] df2-11-teacher-ui-visual | **CLOSED — win order 103 TASK 2 CLEAN 2026-08-04.** BOTH teacher modals show today's Min/Max "Review Test Settings" (Min 30/Max 60, NOT the new group); ClassDetail Edit **SAVE EXERCISED** → "List settings updated successfully", 0 errors; read card shows NO review block. The new reviewPassThreshold/queueSize/testSize/gate group is provably ABSENT flag-off. WinClaude caught+discarded 3 false automated readings via the screenshots ("screenshots are authority for a render question") — no real finding | blocker: none
- [x] df2-07-messaging | **(a/d/e) LANDED 2026-08-04 (orchestrator-verified: grep-proofs re-executed, diffs read, cutover-b/c evidence re-certified 179/0+118/0 after the sha staling). VISUAL OWED → WinClaude (batched w/ df2-33). Remaining legs: (b) JOINS DF2-32 (see disposition), (c) small separate fold, unclaimed.** Original claim note: **(a/d/e) LEG CLAIMED 2026-08-04 (this session); (b/c) DISPOSITION RECORDED 2026-08-04: (b) is NOT a standalone fold — `06_MESSAGING_COPY.md` does not exist and the engine emits no reason field (r43 H4 confirmed: client-local discriminant), so a (b) design now would fork DF2-32's register machinery (reason⇔message table + per-reason oracle are DF2-32 rows). (b) JOINS the DF2-32 design when that card opens. (c) list-end message stays a small separate fold, unclaimed.** Messaging copy on existing screens. **SCOPED 2026-08-04 (read the sites — a real fold, not a swap):** (a) threshold copy — `SessionProgressSheet.jsx:82` hardcodes "95% required to pass" with NO `retakeThreshold` in scope ⇒ thread it through the 4 render sites; `HelpModal.jsx:209/212` "Must score 95%" has no class context ⇒ **genericize (drop the number)** [orchestrator decision — it's a class-less help modal], and NOTE :212 mislabels *Review* at 95% when review threshold is 92 (reviewPassThreshold) vs new-word 95 (passThreshold). (b) review-only reason banner = NET-NEW UI (client-local discriminant at two DSF boundaries, precedence, per-reason oracle) — the heavy leg, own design. (c) list-end message (mid-session, conditions on nextListId). (d) TZ-honest token copy (verify-only). (e) DELETE the 30% throttle passages from `public/help-student-{en,ko}.html` (en:1007/ko:998) + `help-teacher-{en,ko}.html` + TA_FAQ + TA guide (bounded doc deletion). **CONFIRMED 2026-08-04: (a)'s 4 render sites = DailySessionFlow:2161, MCQTest:1710, TypedTest:2030/2080 — retakeThreshold IS available in all 3 caller files, so threading is feasible.** **⚠ (e) DEPLOY-SEQUENCING: the allocation throttle is LIVE until the flip (DF2-10), so its help passages are ACCURATE today — the removal is committed-not-live (netlify off) and must reach PRODUCTION at/after the flip, NOT before, else students hit the live throttle with no explanation. Note this on the hosting-deploy order.** Suggested split: a bounded (a/d/e) increment + a separate (b/c) net-new-UI fold | blocker: none
- [>] df2-51-navui | **SCOPE RULED (David 2026-08-05): CORE + EXTRAS — the full R2-40 package. Design RATIFIED by the orchestrator (22_ §7): 7 decisions closed + 2 new ((h) the cap refusal must RENDER, (i) NTF-27's reload gap folded in), split into EIGHT folds with file ownership. IN FLIGHT 2026-08-05: 51-a (pure past-day model) + 51-b (client visit lifecycle) running in parallel — disjoint file sets. Folds c-h sequenced after.**  **DESIGN-DRAFT LEG IN FLIGHT 2026-08-04 (this session → `22_DF2-51_PASTDAY_NAV_DESIGN.md`, opus draft; orchestrator decision fold + plan-audit AFTER it returns; implementation additionally waits for df2-33 + df2-07-ade to land — Dashboard/DSF file overlap).** **THE ACTUAL DF2-51** — the past-day browser + within-day phase toggle, *"the universal model's only new nav UI"* (02_ DF2-51): past-days list → re-study flashcards + re-test (`type:'retest'`, non-advancing, gradebook-preserving, AI-metered), and review↔new freedom within a day. **Distinct from the session cutover above, which re-wires EXISTING screens and adds none.** I had borrowed this id for the cutover; corrected 2026-08-04. **ENGINE-SIDE RECON 2026-08-04: the server rerun leg ALREADY EXISTS in the committed engine — rerun compose (`callables.js:401-483`, modes `rerun-review`/`new-day` kind `rerun`), rerun submit txn (`:676` fingerprint kind `rerun`, `:739-741` REQUIRES `visitId`), `type:'retest'` stamped (`:761`), non-advancing consumption guards (`completion.js:323/:455`), rerun graduation tested-correct-only (`callables.js:797-836`, `rerun_graduation` ops event). ⇒ df2-51 is CLIENT-ONLY against the dark engine; flag-on behavior exercisable only at the rehearsal. Design owes: past-day browser entry point (Dashboard vs session flow), client visitId lifecycle, typed-retest AI-meter guard, within-day toggle UX on the legacy UI. R2-41 is the stamping law (12_ rows 72-73) | blocker: after:cutover-d-refusals

## PRE-FLIP (not pre-UI — nothing is activated, so these do not block the build)
- [x] namespace-reservation | **COMMITTED (local) + AUDITOR-VERIFIED GO 2026-08-04 (NTF 19+22).** The server-owned `rv2_` docId prefix reserved at all THREE client→global-collection mouths — G1 rules create/update/delete + G2 `submitVocabAttempt` + G3 `gradeTypedTest` — closing the permanent-denial squat (a classmate could plant an undeletable doc at another student's engine attempt/job id). Auditor (Opus, level-4): rules matrix 276/276 + 16/16 mutants incl M16; emulator 31/31; revert-mutants 2/2; deny-nothing-legit canary green; 3 deploy artifacts byte-identical sha16 4d8e511b. Bundled NTF-20's comment repair. **NOT deployed — rides functions-deploy-engine, Codex-gated.** Canonical: 20_RV2_NAMESPACE_RESERVATION.md · ledger _ledgers/namespace-reservation-fold-ledger.md | blocker: none
- [~] namespace-legacy-cache-23 | NEED_TO_FIX 23 — legacy `gradeTypedTest` cached-grade acceptance test (`index.js:1120`). **SHRUNK — namespace-reservation landed (9d73e98).** (a) **DONE 2026-08-04: the two live-flow sentences VERIFIED by re-reading source** (NTF-23 ✅ block) — cross-student pre-seed is fenced at three layers (`index.js:990-992` uid throw before `return_cached`; graded docs uid-stamped `:1001/:1010`; `grading_jobs` client-write-DENIED `firestore.rules:466-469`; `rv2_` refused `:1101-1102`); residual is same-uid Branch-2 (abandoned-attempt window) only. (b) CARDED consumer-side acceptance test (defense-in-depth on `:1120`, targets the Branch-2 residual) — **NOT a pre-flip blocker.** Remaining = ratify the shrink — **RE-ROUTED 2026-08-04: David says the codex baton MAY NOT RETURN ⇒ do not hold for a Codex checkpoint; ratify via an internal Opus auditor pass or David directly when this becomes live-relevant. Dispatch NO new codex rounds program-wide.** Then the carded test. Off the critical path | blocker: none (parked by choice, not codex)
- [x] ai-metering-build | **LANDED 2026-08-05 (d3dce7a) — TWO-ROUND independent opus audit GO; deploy NO-GO cleared. Counts every AI-grading call (frozen 15_ §6 shape, existing KST day law); caps ONLY practice retests; a live/required typed test is structurally unrefusable. Audit caught + fix verified for a contention blocker (global counter serialized all 947 students' live claims on one doc: 105ms→19,521ms at 80 concurrent; now out of the live txn, 183ms, counts exact). NTF-29 cards the LOW KST-straggler. NOT DEPLOYED — rides a later David-executed functions order (bundle with any next deploy).** Original: **IN FLIGHT 2026-08-05 (opus implementer, brief `_ledgers/ai-metering-build-BRIEF.md`; opus auditor after). CORE DESIGN FINDING: the metering point cannot today tell a retest from a live test — so the fold COUNTS everything (R2-20) but ENFORCES only on the rerun path, absence-of-discriminator ⇒ live ⇒ allowed. A naive cap would have outaged live typed tests for 947 students.** **DAVID-DECIDED 2026-08-05 (de-jargonized Q2): "Build the spending cap first, launch both" — typed retests do NOT ship MCQ-only; the frozen 15_ §6 ai_metering contract (aiCallCount · ai_metering/{uid} · ai_metering/_global; R2-20 binding; NTF-28) gets BUILT server-side BEFORE df2-51's typed-retest leg launches.** Server fold (functions/): enforcement at the typed-grading entry (shared path covers legacy + engine legs), per-student + global counters, refusal behavior when capped (needs a small design pass first — orchestrator writes the brief after reading 15_ §6 in full). Sequenced after ntf26-grader-fix (functions/index.js overlap). Rides a later functions deploy (bundle with the engine deploy ONLY if audited before David executes; do not hold the deploy for it). **FREEZE LIFTED 2026-08-05: order 105 DEPLOYED, so functions/ is editable again. This build's deploy is a SEPARATE later order (David executes) — it does NOT ride 105.** | blocker: none
- [x] df2-51-mockups | **DELIVERED 2026-08-05 (SendUserFile, committed a2109e2) — David's scope ruling PENDING his review of the two wireframes.** **DAVID-DECIDED 2026-08-05 (Q3): "Show me mockups first" — the df2-51 launch-scope ruling (core-only vs core+extras: resume sheet, 5-state TOC, pips, bookmarks, list-end screen) is DEFERRED until David sees wireframes.** Two self-contained HTML wireframes (core vs extended) → SendUserFile. IN FLIGHT this session | blocker: none
- [x] gradedis-sweep | **DONE 2026-08-05 (3882af7, orchestrator-verified): 974 students / 1,038,196 rows — 0 MCQ mismatches, 0 unexplained typed mismatches (the 23 are CS-2026-07-23b's own authorized fix), gradedIsCorrect exists on 51 rows (0.0049%). Evidence gradedis-correct-sweep.json.** **DAVID-DECIDED 2026-08-05 (Q4): "Scan first, then decide" — the gate-4 gradedIsCorrect policy waits on a READ-ONLY discrepancy scan.** New scripts/cs/ scanner (mirror data-integrity-sweep.mjs conventions): 26SM attempts, MCQ rows recomputed deterministically vs stored gradedIsCorrect; typed rows corroborated against server grading records where they exist; per-class/per-student discrepancy counts; ZERO writes. Result feeds gradedIsCorrect-decision. IN FLIGHT this session | blocker: none
- [x] ntf26-grader-fix | **LANDED 2026-08-05 (2dfdfa6; TWO-ROUND independent opus audit GO; softening round closed the audit's over-tightening finding; residual rejoinder-gloss false-reject measured PRE-EXISTING + ~absent from live data). Deploy rides order 105.** **DAVID-DIRECTED 2026-08-04 ("fix the prompt; assess first").** ASSESSMENT DONE (NEED_TO_FIX 26 + evidence ntf26-grader-leniency-{baseline,round2}.json): grader NOT generically lenient — the ONLY failure is ≥~10 identical "answer(-ish)" rows flipping to all-correct (schema confusion, 100% reproducible); everything else rejects clean. FIX IN FLIGHT (opus implementer, brief `_ledgers/ntf26-grader-fix-BRIEF.md`): (1) surgical prompt hardening (student field is literal, +1 worked rule-3 example), (2) harness BATCH mode incl. varied-filler + mixed + positive cases, (3) pre-AI ≥8-identical-rows heuristic. Opus auditor before trust. Committed-not-deployed; rides functions-deploy-engine | blocker: none
- [x] functions-deploy-engine | **DEPLOYED 2026-08-05 (order 105, David present). Ten targets updated, zero created/deleted, byte-verified against the 452/452 certification. ORCHESTRATOR-VERIFIED LIVE: dark state intact (enabled:false, rehearsalClassIds:[]) AND the NTF-26 exploit probe returns 0/20 correct in production (was 20/20).** Original: Deploy the engine (typed leg + typed-fix-audit guards + collision fix + the namespace-reservation blocker fixes). Production still runs b54c6e5. Needed BEFORE the rehearsal, which runs against the DEPLOYED backend. **BLOCKER CORRECTED 2026-08-04: namespace-reservation landed, but the deploy CONTENT depends on David's NTF-26 typed-grader decision (a/b/c) and DAVID/WinClaude executes the deploy. The old "Codex-gated" rider re-routes per the codex note above (baton may not return) — gating is David's alone now. Orchestrator can PREPARE the order (ordering-deploys) once NTF-26 is decided. **NTF-26 DECIDED 2026-08-04 (fix the prompt — fix in flight, `ntf26-grader-fix`); deploy content = engine + grader fix. DAVID-DECIDED 2026-08-05 (Q1): "Prepare now; I run it ASAP" — the orchestrator writes the FULL deploy order (ordering-deploys skill) the moment the grader fix passes its independent audit; David executes at his convenience. ai-metering-build bundles ONLY if audited in time; never holds this deploy** | blocker: david:deploy-exec-after-ntf26-fix-lands

## REHEARSAL (after the UI exists — the charter says the product rehearsal is 'impossible pre-build')
- [x] rehearsal-spec-data | **AUTHORED 2026-08-04 → docs/plans/deepfix2/19_REHEARSAL_SPEC.md.** Closes the DATA/CRITERIA half of 13_ O1-5 [HIGH], required by a panel and never written. Leads with the honest capability statement — 25WT is 13 students vs 26SM's 947, so it **structurally cannot rehearse a mass-wall event**, and a green rehearsal must not be read as 'the flip is safe'. Seven seeded strata derived from the B1 baseline (day-1/empty universe · healthy · underflow · recently-failed · list-end · post-reset · multi-student), plus the five exclusion classes B1 counts in the real cohort. Six explicit PASS criteria and named FAILs, written BEFORE the UI so the bar cannot be bent to fit it. Section 4 names every expected difference so a real bug cannot hide behind one | blocker: none
- [x] monitoring-abort-rule | **AUTHORED 2026-08-04 → docs/plans/deepfix2/21_DF2-14_FLIP_ABORT_CARD.md.** Closes 13_ O1-6 [HIGH]. Converts R2-18's 'at WSL discretion, numbers from B1' into a 7-signal abort rule (wall-rate · force-pass · label-write-failure · score-drop · composition_fallback · rerun-graduation · stuck-≥92), each with a source, a threshold, and an owner; action = flip enabled:false; fast signals (label-write >0.5%/hr · any stuck-≥92) + windowed (day-1 + rolling-3-day). Grounded from B1 (failed 15.6%, 947 students) + the H8 re-sim per-band wall table (lt50 hard-walls 100%, b50_70 crawls, top two sail — so the risk is the lt50+b50_70 POPULATION). Thresholds absolute/vs-B1 because the cohort flip leaves NO comparison class. Numbers derived from evidence, none typed | blocker: none
- [ ] flip-abort-thresholds-ratify | The `[RATIFY]` values in 21_ (S1 wall-rate %, S2 force-pass/day, S4 score-drop pts) encode risk tolerance = David's to set; WSL proposed candidates. ALSO the one owed number: the pre-flip PREDICTED cohort wall-rate needs the cohort binned by H8's last-answer metric (can't be faithfully re-derived from graduation-validity-26SM.json — word-level cells, not a per-student band); extend the H8 harness to emit per-band STUDENT COUNTS, publish to evidence/, set S1 against it. A REFINEMENT of S1 (which already fires on OBSERVED walls vs the B1 floor), not a blocker on the card | blocker: david:flip-thresholds
- [x] df2-34-regression-signal | **RE-DERIVED 2026-08-04 → 21_DF2-14_FLIP_ABORT_CARD.md §"Regression invariants".** Closes 13_ O1-5's third limb. The dissolved canary's signal was pilot-vs-rest; R2-5/R2-24 killed the pilot ⇒ no control class, so the re-derivation is TEMPORAL (cohort's own pre-flip B0 baseline vs post-flip) on 7 flip-INVARIANTS the redesign must not touch (R1 auth · R2 new-word submit · R3 attempt-write · R4 dashboard/JS-exceptions · R5 teacher gradebook · R6 non-review completion · R7 grader availability). R2/R3/R6 are the earliest spine-break warning (fire before any wall-rate accumulates). Same abort action/owner/soak as O1-6's signals — one card. The Playwright-scope half of O1-5 belongs with playwright-suite, noted there | blocker: none
- [ ] b0-baseline-capture | **RE-SCOPED 2026-08-05 by David's ruling + the derivability study (evidence/b0-derivability-study.md + orchestrator verification): NO WAITING WEEK — five invariants derive from 38-44 days of EXISTING history. R1 (auth) + R5 (teacher gradebook) are ACCEPTED AS MANUALLY WATCHED (David 2026-08-05: GCP console + a spot-check at the flip), NOT built. B0 = R2 · R3 · R4a · R6-volume · R7-typed. **THE WORK IS NOW A SCRIPT, NOT A WAIT:** write read-only `scripts/deepfix2/b0-baseline.mjs` taking an EXPLICIT window, computing each metric by a documented formula, writing a receipt — because the study's quoted rates did NOT reproduce (98.06% vs 99.70% on R2; window-definition gap larger than R2's own abort trigger). Also card 21_'s three scoping corrections (R6 is a volume not a rate; R4 splits; R7-MCQ vacuous) and its false 'the writers are emitting' sentence. | blocker: none **BLOCKED BY A GAP, NOT BY TIME — see NEED_TO_FIX 30 (2026-08-05).** Verified post-deploy: production `ops_metrics` is EMPTY (0 docs) and **no R1–R7 emitter exists anywhere in the tree** — the only emitters are engine-internal S-series signals, and they only fire when the engine runs (flag-gated). 21_'s dependency sentence ('the ops_metrics writers must be emitting R1–R7 in production, dark') is FALSE as written and must be corrected. NEXT STEP is a DESIGN PASS, not a wait: check whether R1–R7 can be DERIVED retrospectively from existing history (attempts · grading_jobs · system_logs) — if so the baseline needs no accumulation window at all and this stops being a schedule risk; whatever cannot be derived needs an emitter fold + a real baseline week. Orchestrator recommends deriving. | **PRE-FLIP GATE surfaced by the regression signal (21_ §regression).** A temporal regression signal needs a pre-flip baseline: the `ops_metrics` writers (DF2-10 dark build) must emit invariants R1–R7 in production, dark, for ≥ the baseline window (final pre-flip week) BEFORE the ACTIVATION go, frozen as B0 the day before the flip. Without B0 there is no regression signal. Gate, not a nicety | blocker: none
- [x] ui-visual-verification-route | **DECIDED + DOCUMENTED 2026-08-04.** Every UI fold ends with a WinClaude order, not a green test run — WSL structurally cannot run vite (win32 node_modules in a shared checkout). Written into CLAUDE.md as a standing convention with the order shape that worked for 98/99, the 25WT-only boundary (the dev build writes to REAL production Firebase), and two operational facts: typed tests cost real AI money, and a day-start account hits a first-run modal that silently hangs a driver | blocker: none
- [ ] playwright-suite | The Playwright suite + the spec's UI-matrix half. Genuinely impossible before the UI exists. Also must test id-squatting DELIBERATELY — the collision fix removed the accidental collisions that would have surfaced it. **BLOCKER CORRECTED 2026-08-04: the UI now exists (df2-11 ✓), but the suite RUNS against the DEPLOYED dark backend — flag-on paths are unexercisable until the engine ships. Authoring-before-runnable would publish untested specs; if David wants to parallelize authoring anyway, that is his call to make** | blocker: none
- [ ] rehearsal-25wt | 25WT rehearsal: localhost client → DEPLOYED dark backend, hosting NOT deployed, ON via rehearsalClassIds | blocker: after:playwright-suite

- [ ] shadow-audit-16 | Shadow audit | blocker: after:rehearsal-25wt
- [ ] gate4-backfill | THE 26SM BACKFILL | blocker: david:backfill-go
- [ ] gate5-flip | THE FLIP | blocker: david:flip-go
- [ ] gradedIsCorrect-decision | Decide how the backfill treats a client-writable preimage (NEED_TO_FIX). **DATA IN (sweep 2026-08-05): field exists on 51/1,038,196 rows; ZERO unexplained disagreement; recompute-only would REVERT the 23 CS-corrected rows. ORCHESTRATOR RECOMMENDATION for gate 4: backfill IGNORES gradedIsCorrect and derives from isCorrect + adjudication records, WITH a manualOverride/adjudication carve-out so CS corrections never revert. David decides at gate 4** | blocker: david:backfill-trust
- [ ] teacher-signup-decision | Remove the public Teacher radio, or scope the grant (NEED_TO_FIX) | blocker: david:teacher-registration
