# DEEPFIX2 WORK QUEUE — machine-read by `scripts/deepfix2/whats-next.mjs`

**SEQUENCED, NOT PARALLEL (David, 2026-08-03).** The server contract moved twice in one day, so
client work would be built against a shifting target. One item at a time; `after:` chains enforce it.

Format (one per line, parsed):  `- [S] <id> | <what> | blocker: <token>` — S: ` ` unstarted · `x` done · `>` IN FLIGHT (claimed, not ready, valid `after:` target) · `~` CARDED (parked, not ready, valid `after:` target); only an unblocked ` ` row ever makes the script exit 10.
Blocker tokens the script can RESOLVE by itself:
  `none`            — runnable right now
  `codex`           — the Codex baton is not back with claude
  `codex:YES`       — Codex has not returned a YES verdict (a returned baton is NOT approval)
  `win`             — the WinClaude baton is not back with claude
  `david:<what>`    — a decision only David can make (never auto-runnable)
  `after:<id>`      — another queue item must be done first

- [x] rules-artifact | Author + verify the merged rules artifact | blocker: none
- [x] rules-fix-r78 | Codex r78 blocker closed (agent-authored, I verified: 244/244 + pre-fix reproduction) | blocker: none
- [x] rules-receipt | Receipt/evidence INDEPENDENTLY RE-EXECUTED on a fresh harness (canonical 244/244 · full mutants regeneration byte-identical · pre-fix def5231f reproduced at 234/244, ten exact failures) · win 96 push verified by own ls-remote · r79 handed to Codex (flip follows the receipt commit) | blocker: none
- [x] rules-deploy-order | **DEPLOYED 2026-08-03** — order 97 executed, verdict DEPLOYED. Production ruleset `384c9c7a-b9ec-4f17-95ab-b72fff9c5fd1` (523 lines, sha16 f40f91fce3693b82) is BYTE-IDENTICAL to the certified artifact; the P10d trap did NOT ship. **Independently verified by me, not accepted from the report:** re-fetched production read-only (matched the executor's handback exactly) and re-ran the full matrix against THOSE fetched bytes — **262/262 green**. Rollback artifact preserved at live_baseline/firestore.live.PRE_R79_DEPLOY.rules; P10d draft preserved at audit/deepfix/task3/firestore.p10d.rules | blocker: none
- [x] engine-key-provenance-scan | CLOSED — full read-only cohort scan (scripts/deepfix2/engine-key-provenance-scan.mjs, NEW): **41,680 attempts, ZERO carrying any of the four engine keys, 0 quarantine candidates.** Codex r79's qualification is now measured rather than argued. Receipt: audit/deepfix/task3/live_baseline/engine-key-provenance-receipt.json. The artifact COMMENT repair is now DOWNGRADED: with the rules deployed, the claim is true on both legs, so the remaining nicety (cite this scan in the comment) BUNDLES with the next real rules change rather than forcing a comment-only production redeploy | blocker: none
- [x] typed-design | Engine typed-leg DESIGN (18_TYPED_LEG_DESIGN.md — reuse grading_jobs keyed on rv2_{presentationId}) | blocker: none
- [x] typed-fix-audit | CLOSED — cached-grade forgery closed at BOTH seams (usableCachedResults: engine provenance + presentation + answer-sheet hash), A3 completeDay wordId binding, A4 rv2_ replay provenance. Authoring delegated + independently audited (PASS WITH FINDINGS; the audit proved the already_graded sibling was code-true but evidence-free — now pinned by CASE TS + M-A1-SIBLING-CALL-SITE). Evidence re-executed by me: lap 395/395 · 7/7 typed mutants · rules 262/262 · 15/15 rules mutants. **NOT typed-leg readiness — see rv2-docid-collision and NEED_TO_FIX 21** | blocker: none
- [x] rv2-docid-collision | **FIXED 2026-08-03** — both derived global ids are now `rv2_{uid}_{presentationId}` from ONE shared `engineDocId()` (composer.js:117) called by callables.js:550 and typedGrading.js:260, so the legs cannot drift. presentationId UNCHANGED (already uid-scoped by path). NO migration: production had 0 rv2_ docs in attempts AND grading_jobs. The uid is a NAMESPACE, not a fence — the uid FIELD remains the fence. Fixtures: lap CASE RC + RC0 + the INVERTED TR(10) regression witness; mutant M-A1-UID-SCOPE-REVERT killed | blocker: none
- [x] rv2-refusal-status | **DECIDED 2026-08-03: a DISTINCT DATA status meaning *recompose, do not poll*** (not a discriminator field). One status cannot mean two opposite things; and the frozen RV2 list can be changed for free RIGHT NOW because the engine is dark and no client consumes these statuses yet. Recompose is also the ONLY student-side recovery from the card-19 denial. Implementation + both-leg fixtures ride with df2-51-client | blocker: none
- [x] rv2-collision-audit | DONE — independent audit returned PASS WITH FINDINGS (F1 the sharpest: 18_ §5.6 told readers a poisoned key is always self-inflicted, disproved by the fold's own fixtures). All findings folded at bd17873 | blocker: none

## THE UI BUILD (David 2026-08-04: build the UI now; Playwright/rehearsal work comes AFTER it exists)
- [x] rv2-refusal-status-impl | **SHIPPED 2026-08-04** — `grade_unusable` returned at the two PERMANENT sites (typedGrading.js:299 `return_cached`, :343 `already_graded`); the three TRANSIENT sites unchanged (:279 live lease, :350 no authority established, callables.js:655 concurrent submit). Frozen RV2 list + `isGradeUnusable()`, each status naming the other as its inverse. Lap 453/453, 11/11 mutants — both directions proven, which one mutant cannot do. CLIENT handling (recompose once, never loop) rides with df2-51b-submit | blocker: none
- [~] session-cutover (CONTAINER — not work; the four cutover-* folds below ARE the work) | **DECOMPOSED 2026-08-04 (delegator).** Surface measured: ~8,655 lines across DailySessionFlow.jsx (2,666), studyService.js (2,023), TypedTest.jsx (1,914), MCQTest.jsx (1,644), sessionService.js (408). The seam is `initializeDailySession` (5 call sites) + `completeSessionFromTest`. Today the CLIENT computes the review segment locally (wordIds/startIndex/endIndex) and grades; the engine does both server-side. TOO BIG FOR ONE FOLD — four, each behind REVIEW_V2_CLIENT with the legacy path intact as fallback, each with its own ledger | blocker: after:cutover-d-refusals
- [x] cutover-a-compose | **COMMITTED f9b423f 2026-08-04.** Review composition now comes from the engine behind REVIEW_V2_CLIENT (still false). Algorithm REPLACED (priority-bands-over-segment → cursor-chained rotation), so no fixture asserts the two agree. Lazy compose at review entry · presentedWordIds rendered verbatim · composeKey persisted. Opus audit PASS WITH FINDINGS — three undeclared student-visible changes folded (MCQ distractor pool narrowed → 3-option questions; dead range label; typed truncation guaranteeing an 83% cap) plus a silent fallback and a FALSIFIED receipt whose root cause is now structurally fixed. Evidence: pure 211/0 · emulator 89/0 · mutants 6/6 · lap 453/453. **VISUAL CHECK CLOSED** (win 98 PASS_WITH_GAP + win 99 CLEAN): review AND new-word phases both confirmed flag-off in a real browser | blocker: none
- [x] cutover-b-submit | **COMMITTED (local) + AUDITOR-VERIFIED GO 2026-08-04.** Submission routes through the engine `submitAttempt` behind REVIEW_V2_CLIENT (still false); adapter `reviewV2Submit.js` sends ONLY `{presentationId, answers}`; recompose-once on `grade_unusable` (persisted, fail-closed). Auditor (Opus, level-4): flag-off parity + payload law + recompose-once confirmed; pure 179/0, emulator 65/0 (passed WITH namespace guards live), mutants 2/2. **VISUAL CHECK OWED → cutover-b-visual** | blocker: none
- [x] cutover-b-visual | **CLOSED — win order 101 CLEAN 2026-08-04.** Typed submission authorized (David, ≤200); WinClaude drove the flag-OFF path END TO END: new-word study → typed test → SUBMIT → result card "100%, 20/20 correct" → phase ADVANCED to Review Study Day 5. 0 console errors. It verified the flag-gating itself (`rv2Handle` is null flag-off ⇒ the legacy path runs ⇒ the adapter is dead code, so what was exercised IS the legacy path) and read the stored attempt (score=100, matches the UI). The r098 gap is closed. **⚠ SURFACED AN OUT-OF-SCOPE LIVE-PATH FINDING → NEED_TO_FIX 26** (typed grader marked 20/20 garbage answers 100%) | blocker: none
- [ ] cutover-c-complete | Route day completion through `completeDay` (server advances + graduates + streaks) | blocker: after:cutover-b-submit
- [ ] cutover-d-refusals | Render a reason for EVERY refusal status — overlaps DF2-07's messaging leg; do them together or the copy drifts | blocker: after:cutover-c-complete
- [ ] dashboard-streak-authority | **NEED_TO_FIX 25 — the dashboard has NO queue row and was in no fold's scope.** The engine writes `users/{uid}/streak_credits/{kstDate}` at day completion (completion.js:679, frozen R2-21 semantics); the dashboard computes its OWN streak client-side (Dashboard.jsx:37-123, consumed :1399) and **nothing in src/ reads the server credit**. Two authorities for one user-visible number, different inputs and different rules — and the student sees the client's. Read the server credit when the engine owns the day, keep the client calc as the flag-off path. Sequenced here because the credit only exists once completion runs server-side | blocker: after:cutover-c-complete
- [ ] dashboard-df2-33 | **Dashboard one-affordance (DF2-33)** — hero + per-list on ONE derivation, closing the two-done-authorities class that `dashboard-streak-authority` is the first concrete instance of. Also owes an explicit decision the card names: whether Dashboard gets a READ-ONLY assembly variant, since the ordered-write pipeline must never run writes from a dashboard render | blocker: after:dashboard-streak-authority
- [ ] df2-11-teacher-ui | Teacher settings UI. Server target CONFIRMED EXISTS — config.js:157-187 already reads+validates reviewGateEnabled / reviewPassThreshold / reviewQueueSize / reviewTestSize as per-assignment overrides. **SHIPS PARTIAL: the force-pass row action has NO server resolver** (index.js exports 10 callables, none force-pass) — out of scope until DF2-10 leg 7 exists | blocker: after:cutover-d-refusals
- [ ] df2-07-messaging | Messaging copy on existing screens (threshold copy derives from real passThreshold; the 30% throttle passages come OUT of the help site) | blocker: after:cutover-d-refusals
- [ ] df2-51-navui | **THE ACTUAL DF2-51** — the past-day browser + within-day phase toggle, *"the universal model's only new nav UI"* (02_ DF2-51): past-days list → re-study flashcards + re-test (`type:'retest'`, non-advancing, gradebook-preserving, AI-metered), and review↔new freedom within a day. **Distinct from the session cutover above, which re-wires EXISTING screens and adds none.** I had borrowed this id for the cutover; corrected 2026-08-04 | blocker: after:cutover-d-refusals

## PRE-FLIP (not pre-UI — nothing is activated, so these do not block the build)
- [x] namespace-reservation | **COMMITTED (local) + AUDITOR-VERIFIED GO 2026-08-04 (NTF 19+22).** The server-owned `rv2_` docId prefix reserved at all THREE client→global-collection mouths — G1 rules create/update/delete + G2 `submitVocabAttempt` + G3 `gradeTypedTest` — closing the permanent-denial squat (a classmate could plant an undeletable doc at another student's engine attempt/job id). Auditor (Opus, level-4): rules matrix 276/276 + 16/16 mutants incl M16; emulator 31/31; revert-mutants 2/2; deny-nothing-legit canary green; 3 deploy artifacts byte-identical sha16 4d8e511b. Bundled NTF-20's comment repair. **NOT deployed — rides functions-deploy-engine, Codex-gated.** Canonical: 20_RV2_NAMESPACE_RESERVATION.md · ledger _ledgers/namespace-reservation-fold-ledger.md | blocker: none
- [ ] namespace-legacy-cache-23 | NEED_TO_FIX 23 — legacy `gradeTypedTest` cached-grade acceptance test (`index.js:1052`). **SHRINKS once namespace-reservation lands (canonical design Leg 3):** G3 kills the pre-seeding lever, so 23 = (a) confirm two sentences on the live flow (written into NTF-23 when the fold lands) + (b) a CARDED consumer-side acceptance test as defense-in-depth — **NOT a pre-flip blocker once the namespace is reserved.** Codex-checkpoint question: is the shrink accepted? | blocker: after:namespace-reservation
- [ ] functions-deploy-engine | Deploy the engine (typed leg + typed-fix-audit guards + collision fix + the namespace-reservation blocker fixes). Production still runs b54c6e5. Needed BEFORE the rehearsal, which runs against the DEPLOYED backend | blocker: after:namespace-reservation

## REHEARSAL (after the UI exists — the charter says the product rehearsal is 'impossible pre-build')
- [x] rehearsal-spec-data | **AUTHORED 2026-08-04 → docs/plans/deepfix2/19_REHEARSAL_SPEC.md.** Closes the DATA/CRITERIA half of 13_ O1-5 [HIGH], required by a panel and never written. Leads with the honest capability statement — 25WT is 13 students vs 26SM's 947, so it **structurally cannot rehearse a mass-wall event**, and a green rehearsal must not be read as 'the flip is safe'. Seven seeded strata derived from the B1 baseline (day-1/empty universe · healthy · underflow · recently-failed · list-end · post-reset · multi-student), plus the five exclusion classes B1 counts in the real cohort. Six explicit PASS criteria and named FAILs, written BEFORE the UI so the bar cannot be bent to fit it. Section 4 names every expected difference so a real bug cannot hide behind one | blocker: none
- [x] monitoring-abort-rule | **AUTHORED 2026-08-04 → docs/plans/deepfix2/21_DF2-14_FLIP_ABORT_CARD.md.** Closes 13_ O1-6 [HIGH]. Converts R2-18's 'at WSL discretion, numbers from B1' into a 7-signal abort rule (wall-rate · force-pass · label-write-failure · score-drop · composition_fallback · rerun-graduation · stuck-≥92), each with a source, a threshold, and an owner; action = flip enabled:false; fast signals (label-write >0.5%/hr · any stuck-≥92) + windowed (day-1 + rolling-3-day). Grounded from B1 (failed 15.6%, 947 students) + the H8 re-sim per-band wall table (lt50 hard-walls 100%, b50_70 crawls, top two sail — so the risk is the lt50+b50_70 POPULATION). Thresholds absolute/vs-B1 because the cohort flip leaves NO comparison class. Numbers derived from evidence, none typed | blocker: none
- [ ] flip-abort-thresholds-ratify | The `[RATIFY]` values in 21_ (S1 wall-rate %, S2 force-pass/day, S4 score-drop pts) encode risk tolerance = David's to set; WSL proposed candidates. ALSO the one owed number: the pre-flip PREDICTED cohort wall-rate needs the cohort binned by H8's last-answer metric (can't be faithfully re-derived from graduation-validity-26SM.json — word-level cells, not a per-student band); extend the H8 harness to emit per-band STUDENT COUNTS, publish to evidence/, set S1 against it. A REFINEMENT of S1 (which already fires on OBSERVED walls vs the B1 floor), not a blocker on the card | blocker: david:flip-thresholds
- [x] df2-34-regression-signal | **RE-DERIVED 2026-08-04 → 21_DF2-14_FLIP_ABORT_CARD.md §"Regression invariants".** Closes 13_ O1-5's third limb. The dissolved canary's signal was pilot-vs-rest; R2-5/R2-24 killed the pilot ⇒ no control class, so the re-derivation is TEMPORAL (cohort's own pre-flip B0 baseline vs post-flip) on 7 flip-INVARIANTS the redesign must not touch (R1 auth · R2 new-word submit · R3 attempt-write · R4 dashboard/JS-exceptions · R5 teacher gradebook · R6 non-review completion · R7 grader availability). R2/R3/R6 are the earliest spine-break warning (fire before any wall-rate accumulates). Same abort action/owner/soak as O1-6's signals — one card. The Playwright-scope half of O1-5 belongs with playwright-suite, noted there | blocker: none
- [ ] b0-baseline-capture | **PRE-FLIP GATE surfaced by the regression signal (21_ §regression).** A temporal regression signal needs a pre-flip baseline: the `ops_metrics` writers (DF2-10 dark build) must emit invariants R1–R7 in production, dark, for ≥ the baseline window (final pre-flip week) BEFORE the ACTIVATION go, frozen as B0 the day before the flip. Without B0 there is no regression signal. Gate, not a nicety | blocker: after:functions-deploy-engine
- [x] ui-visual-verification-route | **DECIDED + DOCUMENTED 2026-08-04.** Every UI fold ends with a WinClaude order, not a green test run — WSL structurally cannot run vite (win32 node_modules in a shared checkout). Written into CLAUDE.md as a standing convention with the order shape that worked for 98/99, the 25WT-only boundary (the dev build writes to REAL production Firebase), and two operational facts: typed tests cost real AI money, and a day-start account hits a first-run modal that silently hangs a driver | blocker: none
- [ ] playwright-suite | The Playwright suite + the spec's UI-matrix half. Genuinely impossible before the UI exists. Also must test id-squatting DELIBERATELY — the collision fix removed the accidental collisions that would have surfaced it | blocker: after:df2-11-teacher-ui
- [ ] rehearsal-25wt | 25WT rehearsal: localhost client → DEPLOYED dark backend, hosting NOT deployed, ON via rehearsalClassIds | blocker: after:playwright-suite

- [ ] shadow-audit-16 | Shadow audit | blocker: after:rehearsal-25wt
- [ ] gate4-backfill | THE 26SM BACKFILL | blocker: david:backfill-go
- [ ] gate5-flip | THE FLIP | blocker: david:flip-go
- [ ] gradedIsCorrect-decision | Decide how the backfill treats a client-writable preimage (NEED_TO_FIX) | blocker: david:backfill-trust
- [ ] teacher-signup-decision | Remove the public Teacher radio, or scope the grant (NEED_TO_FIX) | blocker: david:teacher-registration
