> **⛔ PARTIALLY SUPERSEDED (2026-08-02):** this report is the historical step-1 investigation INPUT. Where it
> recommends bilingual (ko/en) copy for new surfaces or a hover/long-press pip reveal, the LEDGER overrides:
> **R2-44 = ENGLISH-ONLY launch UI** (Korean is content only) · **R2-45 = static "×N" pip total, no reveal
> interaction**. Its component/token/pattern findings remain valid build input.

# C2 — UI calibration audit (Track A step-1 report, 2026-08-01 — investigation agent output, WSL-filed)

> Checkpoint-1 convergence target. Claims cite file:line; VERIFIED vs INFERRED per report text.

## Summary

C2 calibration verdict: every launch surface can compose from the existing token system (src/index.css) and a small set of proven components — ConfirmModal, ui/Button variants, the MCQ semantic result-card pattern, SessionProgressSheet's bottom-sheet shell, and CompletePhase's choice terminal — but five primitives genuinely do not exist (five-state day grid, 5-segment pips, a toggle/switch, a tooltip/long-press reveal, a client onSnapshot live-listener pattern). The single largest risk is pattern contamination: the surfaces DF2-12 extends (TypedTest's grading-outage modals, TypedTest.jsx:1810-1908) are entirely off-system (bg-white/gray/blue raw palette, dark-mode broken) and must be rebuilt, not extended. The teacher settings rebuild has two divergent hand-rolled implementations of the same form (AssignListModal vs ClassDetail settings modal) with conflicting validation clamps and label styles — DF2-11's consolidation should unify them onto ui/Modal + ui/Input/Select. Three convention forks need explicit design decisions: session-start decisions today are center ConfirmModals while the spec says resume "sheet"; the step model everywhere is 3-state linear (locked/current/completed) while the TOC needs 5 states plus non-linear jumping; and no teacher surface is bilingual today while DF2-11 mandates ko/en labels. Gradebook's default-OFF rerun hiding is not free: queryTeacherAttempts (db.js:2024-2076) has no attempt-type filter, so retests would appear by default without a query/UI change. The ko/en convention for student surfaces is verified: single hardcoded string, Korean first, English in parentheses. Dark mode is class-based (.dark on documentElement) with full token flipping; every raw-palette usage catalogued here is a dark-mode defect the new surfaces must not copy.

## 0. Scope, method, and baseline system

Audited per 11_ §4 C2 and the task brief: surfaces (a)-(e) from DF2-11/DF2-12/DF2-51/DF2-14 (docs/plans/deepfix2/02_TASK_LIST.md:80-81, :95, :141) and R2-40/R2-41/R2-13 (docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:35-36, :58). READ-ONLY; all claims VERIFIED by direct read unless marked INFERRED.

**Token system (VERIFIED, /app/src/index.css):** semantic bg (`bg-base/surface/muted/inset`, :11-14), borders (:17-19), text (`text-text-primary/secondary/muted/faint`, :22-26), full semantic-state families success/error/warning/info each with bg/subtle/border/ring/text/strong (:42-72) and dark-mode counterparts (:174-204), on-semantic-bg text tokens (`text-on-success/error/warning`, :78-84, aliases :427-433), solid button colors (:86-93), brand navy #1B3A94 / ember orange #F97316 (:311-313), radius tokens (:107-119), theme shadows (:121-127, dark :162-167). Dark theme = `.dark` class overrides (:131-229), applied by ThemeContext on documentElement (src/contexts/ThemeContext.jsx:78-84) — tokens auto-flip; raw Tailwind palette classes do NOT. Fonts: Plus Jakarta Sans headings / Pretendard body (Korean-capable, :233-308). One shared keyframe: `slideInFromRight` (:510-517, consumed by the Gradebook drawer via `animate-[slideInFromRight_0.3s_ease-out]`, src/pages/Gradebook.jsx:1242).

**Primitive inventory (VERIFIED):** ui/index.js exports Button/IconButton/NavButton/TabButton/LinkButton/CardButton/TagButton + Badge/Card/Input/Textarea/Select/Modal (src/components/ui/index.js:1-11). Button has 8 variants incl. primary (orange CTA), primary-blue, outline, danger, warning with hover-lift/active-scale transitions at duration-200 (src/components/ui/buttons/Button.jsx:23-96). Card has section/header/content/modal/inset/stat/alert-*/empty variants (src/components/ui/Card.jsx:37-118). ConfirmModal is the universal prompt (variant system mapping to Button variants, icon circle, Cancel-first order; src/components/ConfirmModal.jsx:17-121) — used at 5 DailySessionFlow sites + 3 sites in each test page (src/pages/DailySessionFlow.jsx:2038-2098, MCQTest.jsx:1565-1593, TypedTest.jsx:1770-1808). SessionHeader = sticky session chrome with clickable step pill (src/components/SessionHeader.jsx:27-111). SessionProgressSheet = the ONLY bottom sheet (src/components/SessionProgressSheet.jsx:131-143). ErrorDisplay (src/components/ErrorDisplay.jsx:20-63) and LoadingSpinner (raw `border-blue-600`, src/components/LoadingSpinner.jsx:11) round out state surfaces.

## 1. Surface (a) — teacher review-settings group (AssignListModal + ClassDetail)

**Compose from (VERIFIED):** the section-group pattern `border-t border-border-default pt-4 mt-4` + `h3 text-sm font-semibold` header ("Review Test Settings" exists in both: src/components/AssignListModal.jsx:166-167, src/pages/ClassDetail.jsx:1259-1260) — the DF2-11 pace/new-test + review two-group consolidation keeps this idiom. Field pattern: label block + `mt-1 w-full rounded-lg border border-border-default ... focus:ring-2` input + `text-xs text-text-muted` caption (ClassDetail.jsx:1144-1160, :1179). Footer: Cancel=outline / Save=primary-blue `flex-1` pair (ClassDetail.jsx:1386-1393; AssignListModal.jsx:219-232). `reviewGateEnabled` toggle: the ONLY checkbox-with-description precedent is the cyclingEnabled block (`h-4 w-4 rounded border-border-strong text-brand-primary` + title/sub-caption span, ClassDetail.jsx:1363-1382). Read surface: the assigned-list card meta row (`Pace: {n} words/day`, `Test Options: {n} choices`, `Next: {title}`, ClassDetail.jsx:799-820) is where review chips (threshold/queue/test + gate-off state) should join.

**Conflicts (VERIFIED):** (1) TWO divergent hand-rolled implementations of the same form — AssignListModal (create) and the ClassDetail settings modal (edit) — with conflicting styles: AssignListModal labels are raw `text-slate-700`/`text-slate-500` (AssignListModal.jsx:62, :70, :85 — dark-mode broken) while ClassDetail uses `text-text-secondary`/`text-text-muted` tokens (ClassDetail.jsx:1144, :1134); inputs are `bg-muted` in one (AssignListModal.jsx:75) vs `bg-base` in the other (ClassDetail.jsx:1158); validation clamps DIFFER for the same fields (review sizes min=10/max=100 at AssignListModal.jsx:189-207 vs min=1/max=500 at ClassDetail.jsx:1284-1309). DF2-11 should unify on ONE shared form component. (2) Neither modal uses ui/Modal, ui/Input, or ui/Select despite their existence (Modal.jsx:34-60 — note Modal uses `rounded-3xl` :42 vs the teacher modals' `rounded-2xl` AssignListModal.jsx:58/ClassDetail.jsx:1130); CLAUDE.md mandates composing existing ui components. (3) Deletion targets confirmed present: `reviewTestSizeMin/Max` state+fields (AssignListModal.jsx:14-15, :184-217; ClassDetail.jsx:177, :275-276, :416-417, :440-441, :1279-1317) and the now-FALSE caption "Review test size scales with intervention (min at 0%, max at 100%)" (AssignListModal.jsx:214-216, ClassDetail.jsx:1317) — dead copy under D-1 throttle removal; must not survive the rebuild. (4) The read-surface chips use raw `text-blue-600`/`text-emerald-600` (ClassDetail.jsx:804, :806) — dark-unaware; new review chips must use text tokens or semantic text-info/success tokens.

**Visible-defaults requirement (R2-15/DF2-11):** the current caption idiom (`placeholder` + helper text, ClassDetail.jsx:1233-1235) supports "visible defaults (60/30/92)" — recommend default-value display in the caption, e.g. the pattern at ClassDetail.jsx:1216.

## 2. Surface (b) — student restudy package (resume sheet, TOC, pips, list-end, continue)

**Resume sheet (R2-40a):** the bottom-sheet convention exists ONCE — SessionProgressSheet: fixed-bottom `rounded-t-2xl` panel, drag-handle bar, `max-h-[85vh]`, body-scroll lock, ESC close, component-LOCAL slide-up keyframes (src/components/SessionProgressSheet.jsx:131-143, :46-56, :286-298). CONFLICT: every session-start DECISION today is a centered ConfirmModal — Re-entry "Resume Day X?" (DailySessionFlow.jsx:2077-2086), test-recovery "Resume Previous Test?" (TypedTest.jsx:1799-1808) — so "sheet for a start-of-session decision" is a NEW convention; compose SessionProgressSheet's shell + Button pair rather than ConfirmModal, and hoist the slide-up keyframes next to `slideInFromRight` (index.css:510-517) instead of a second inline copy. Today-first highlight that later flips to the bookmark: the exact variant-swap mechanic exists in CompletePhase's choice terminal, where primary vs outline is computed per-condition (DailySessionFlow.jsx:2530, :2539).

**Five-state TOC day grid (R2-40b):** NO day-picker exists anywhere (grep NavigateHub/DayPicker/pastDay over /app/src = zero hits — VERIFIED). Nearest ancestors: SessionProgressSheet's THREE-state step model (`COMPLETED/CURRENT/LOCKED`, SessionProgressSheet.jsx:11-15) with status-circle rendering (completed=emerald w/ `dark:` variants :206-219, current=`bg-brand-primary text-white` :211, locked=`bg-muted border-2 border-border-default` :212); the Dashboard hero 2-step tracker (Dashboard.jsx:1860-1871) and DAY badge (:1836-1838); the 7-column weekly grid (Dashboard.jsx:1948-1961). The 5 states map: locked→SPS locked treatment; today/frontier→SPS current (brand-primary); completed→SPS completed (emerald); re-completed and last-visited/bookmark have NO existing treatment (gap — recommend success-strong/double-ring for re-completed and brand-accent ring for the bookmark, from the token families index.css:42-48/:174-180 and brand accent :312). CONFLICT: SPS's fixed linear `phaseOrder` (SessionProgressSheet.jsx:110-112) and the hero's fixed "New words › Review" order (Dashboard.jsx:1860-1871) encode forced linear flow — the TOC is free-jumping (R2-40f "TOC jumping stays free") and must not inherit index-comparison state logic (:118-123). PHANTOM-CHAPTER WARNING: the pervasive `displayDay = completedDays + 1` idiom (Dashboard.jsx:198-200, :1741) renders "DAY N+1" forever on a finished list — exactly the phantom chapter R2-40(b) forbids; the TOC must render N chapters + one persistent "Today's review" entry instead of deriving a chapter from csd+1.

**5-segment mastery pips (R2-40c):** net-new. The only segment-grid ancestor, MasterySquares (7-square grid, src/components/MasterySquares.jsx:16-33), is (i) DEAD CODE scheduled for deletion at DF2-02a (02_TASK_LIST.md:63) and (ii) raw-palette (`bg-emerald-500`/`bg-slate-200`, :26, :48 — dark-broken). Do NOT compose from it; build a token-based pip row (`bg-success-ring` fill / `bg-inset` empty). Exact-count on hover/long-press: no tooltip primitive exists — the convention is native `title=` (MasterySquares.jsx:28, ClassDetail.jsx:124), which has no mobile equivalent (gap, see §6). Pip-fill micro-animation: existing motion vocabulary is transition-all duration-200 (Button.jsx:93), duration-300 bar fills (SessionHeader.jsx:98, SessionProgressSheet.jsx:177), animate-pulse skeletons (Dashboard.jsx:1747) — a small new fill keyframe at 150-300ms fits CLAUDE.md's animation guidance.

**List-end screen (R2-39/R2-40f):** compose CompletePhase's list-complete terminal directly — it already has the celebration header (DailySessionFlow.jsx:2442-2464), "Advance to {nextListTitle} →" primary Button (:2519-2523) fed by the continuation resolver that validates `nextListId` against class lists (:912-943), a "Start over (review the list again)" slot (:2527-2536), and Back to Dashboard (:2537-2544). Mapping: [Start next list] = the existing advance affordance; [Read again from Day 1] = the start-over SLOT'S layout, BUT its wiring is P9 cycling (`cyclingCapabilityLive`/`onStartOver`, :2426-2429) which R2-39 RETIRED — the read-again button must be rewired to restudy-bookmark navigation (a non-progress pref write, R2-40e), never the cycling path (INFERRED mapping; layout claim VERIFIED). Keep the Dashboard finished-hero twin consistent ("Advance to {nextListTitle} →", Dashboard.jsx:1889-1897).

**End-of-restudied-day continue (R2-40f):** composes CompletePhase's day-complete variant (checkmark circle + "Day N Complete", DailySessionFlow.jsx:2465-2475) with [Continue to Day X+1 →] as primary Button; the streak-preserving "today's review is waiting" reminder fits the info-block idiom (`rounded-lg bg-info p-4` + `text-text-info-strong`, DailySessionFlow.jsx:2496-2503) or Card alert-info (Card.jsx:105-109).

## 3. Surface (c) — SUBMITTED–NOT-YET-GRADED + [Try grading again]/[Wait] + re-grade prompt

**Anti-pattern warning (VERIFIED, highest-priority conflict):** the three surfaces DF2-12 directly extends are OFF-SYSTEM — TypedTest's submission overlay (`bg-white`, `text-gray-800`, raw `border-blue-600` spinner, hardcoded `rgba(0,0,0,0.35)` backdrop, TypedTest.jsx:1810-1846), the write-failure modal (`text-yellow-700`, `bg-blue-600` buttons, :1848-1873), and the grading-error modal (`text-red-700`, :1875-1908). These are dark-mode broken and violate the token mandate. The new SUBMITTED-state UI must be REBUILT on ConfirmModal/Modal + Button + semantic tokens, not extended from these.

**Compose from (VERIFIED):** (1) The state screen itself — since "submitted, not yet graded" is a persistent phase state (student may leave for review and return), use the full-width semantic result-card pattern from MCQTest: `rounded-2xl p-8 text-center` card with semantic bg + ring + on-semantic text tokens (pass card `bg-success ring-ring-success text-text-on-success`, fail card `bg-error...`, MCQTest.jsx:1178-1250; 4-tier config :1260-1279). The natural tier for pending-grade is the WARNING family (`bg-warning ring-ring-warning` + `text-text-on-warning`, tier 'good' at :1272-1279; tokens index.css:58-64/:190-196). (2) Buttons: [Try grading again] = `primary-blue`, [Wait — try later] = `outline` — the exact pairing already used on the failed-result card (Try Again / Go to Dashboard, MCQTest.jsx:1226-1247). (3) Day-advance re-grade prompt ("your earlier test hasn't been graded — grade it now?") = ConfirmModal variant info, precedent = the Re-entry modal (DailySessionFlow.jsx:2077-2086). (4) Session-start pickup ("finish grading your earlier test") joins the DSF session-start prompt cluster (ConfirmModals at DailySessionFlow.jsx:2038-2098). (5) Persistence copy ("saved on this browser, survives closing it", R2-12a) has a precedent in the reassurance line "Your answers are safe — this only retries saving" (TypedTest.jsx:1861-1863); the retry-classification machinery (transient vs deterministic, `gradingErrorKind`, TypedTest.jsx:103-105, :1884-1904) carries over conceptually.

**Interaction with review-first flow (R2-13):** while pending, the student may proceed to review — so the state card needs a third affordance (proceed to review) beyond the R2-13 two-button pair; the SessionHeader step pill (SessionHeader.jsx:54-74) and SessionProgressSheet step list must be able to render a "submitted — grading pending" step state, which the current 3-state model cannot express (same gap as the TOC's 5 states; SessionProgressSheet.jsx:11-15). INFERRED: a fourth step status (pending/warning) is needed in whatever step component survives.

## 4. Surface (d) — teacher gradebook re-run toggle (default OFF)

**Home (VERIFIED):** the Gradebook filter bar — category buttons row (Gradebook.jsx:703-721), auto-add chip behavior for 'Test Type' (:752-783, :894-895), active-tag chips. The toggle is a new control in this bar. Gradebook is a SHARED component instantiated for teacher and student routes (props `queryFn = queryTeacherAttempts`, `showNameFilter`, `challengeMode` at Gradebook.jsx:27-31; App.jsx:119 teacher, :135 student) — the toggle must be prop-gated to the teacher instantiation only (R2-40g: teacher toggle, default OFF; students never see reruns via this surface).

**Not free (VERIFIED):** `queryTeacherAttempts` (src/services/db.js:2024-2076) parses ONLY Class/Name/List/Test Type(mcq|typed)/Date filters — there is NO attempt-`type` filter and no `'retest'` handling anywhere in vocaBoost client code (grep verified). Once `type:'retest'` attempts exist, they appear in the default gradebook view; the default-OFF behavior therefore requires a query-layer or post-query exclusion PLUS the toggle to re-include — a data-path change riding the UI card (INFERRED consequence of verified code).

**Display of revealed reruns (VERIFIED patterns):** Session-type chips (:1132-1144) need a 'retest'/'re-run' variant; the supplementary-chip precedent on the Score cell is "Pending Challenge" (`bg-amber-100 text-amber-700`, :1114-1118). CONFLICT: all these chips are raw palette (`bg-green-100/orange-100/purple-100/blue-100/gray-100`, :1123-1143) and the table body uses `divide-slate-200` (:1081) — dark-unaware; Badge.jsx has the same defect (raw `bg-blue-100/emerald-50/amber-100/red-50/purple-100`, src/components/ui/Badge.jsx:4-10). A rerun chip should use accent-bg tokens (`bg-accent-*`, index.css:33-38/:155-160) or the semantic families, not extend the raw set. The CSV export mapping (Type/Session labels, :608-609) must also learn the rerun vocabulary or exports will mislabel reruns (INFERRED from verified export code). Sort/thead conventions to keep: :1025-1080.

**No switch primitive exists** (grep `role="switch"`/peer-checked over /app/src = only an apBoost aria-checked, AnswerInput.jsx:70) — options: checkbox pattern (Gradebook.jsx:1028-1033) or a new switch primitive shared with (a)'s `reviewGateEnabled` (see §6).

## 5. Surface (e) — retake-wall screen with live-dissolve

**Current retake surfaces (VERIFIED):** (1) RetakePrompt (DailySessionFlow.jsx:2367-2415): score card + `bg-warning` notice ("Score below X%" — already threshold-derived, :2383-2386) + "Study Again & Retake" primary-blue — SCHEDULED FOR DELETION at DF2-02b (02_TASK_LIST.md:83), so it is NOT the base. (2) The MCQ failed-result card (`bg-error ring-2 ring-ring-error`, X icon, derived "Your score is below {threshold}%" copy, Try Again + Go to Dashboard, MCQTest.jsx:1178-1250) — this IS the visual ancestor for the wall. (3) The review 4-tier result system (:1254-1279+) incl. the critical tier tokens (`--color-error-critical-*`, index.css:74-76, :206-208).

**Wall requirements mapped to conventions:** the wall (`review_retake_required`) is a BLOCKING STATE, re-derived on every load, never seen-markered (DF2-32 rule, 02_TASK_LIST.md:115). Copy must state the frozen-queue rule (same pinned queue until pass, R2-26 Q5 — 10_REVIEW_GRADUATION_REDESIGN.md:42-43) and derive the threshold from the real class setting — live derivations already exist (MCQTest.jsx:1201; TypedTest equivalent per DF2-07a, 02_TASK_LIST.md:70). Primary action = restudy-and-retake (Button primary-blue, per RetakePrompt:2404 and MCQ:1228); secondary = Go to Dashboard outline (MCQ:1240-1246).

**Live-dissolve (A1(viii), 11_:66):** the wall holds a single-doc listener on its own attempt so a teacher force-pass dissolves it in real time. VERIFIED: there is NO `onSnapshot` anywhere in vocaBoost client code (grep over /app/src: the only hit is apBoost's APGradebook.jsx) — src/services/db.js patterns are one-shot reads with retry. The listener is a NEW client pattern; it should live in the services layer per CLAUDE.md's db.js-patterns rule (INFERRED placement; absence VERIFIED). Dissolve transition: no exit animation exists for state cards; the available motion vocabulary is opacity/transform transitions at 200-300ms (ConfirmModal backdrop transition-opacity SessionProgressSheet.jsx:134; Button.jsx:93) — a fade/collapse plus an explanatory interstitial ("your teacher passed this test") is a new micro-animation, small keyframe alongside index.css:510-517. The dissolve target routes forward through server truth (completeSession `already_completed` → discard local session), so the UI animation must gate on the listener event, never on local state (spec-anchored, 11_:66).

## 6. Gap inventory — primitives that do not exist

All absences VERIFIED by grep/read over /app/src:

1. **Reusable bottom sheet** — SessionProgressSheet is single-purpose with component-local keyframes (SessionProgressSheet.jsx:139, :286-298); the resume sheet needs the shell extracted or replicated (second consumer justifies extraction to /src/components/ui/).
2. **Five-state day-grid/TOC** — no ancestor beyond the 3-state step list (SessionProgressSheet.jsx:11-15); wholly new component; must also express 'bookmark' and 're-completed' states with no existing visual precedent.
3. **Segmented pips (5-segment, capped, exact-count reveal)** — MasterySquares is dying dead code (02_TASK_LIST.md:63) and raw-palette (MasterySquares.jsx:26, :48); build new on tokens.
4. **Toggle/switch** — none in vocaBoost (grep verified); needed TWICE (reviewGateEnabled in (a), rerun toggle in (d)) — a single new ui primitive amortizes; alternatively both compose the checkbox pattern (ClassDetail.jsx:1363-1373).
5. **Tooltip / long-press reveal** — only native `title=` attrs exist (MasterySquares.jsx:28, ClassDetail.jsx:124), desktop-only; the pips' exact-count on touch devices needs a designed affordance (tap-to-reveal or the sheet).
6. **Client Firestore live-listener pattern** — no onSnapshot in vocaBoost (grep verified; apBoost APGradebook.jsx is the only repo precedent and must not be imported per CLAUDE.md apBoost isolation).
7. **Micro-animation keyframes** for pip-fill and wall-dissolve — current shared keyframes are slideInFromRight only (index.css:510-517) plus one inline slide-up (SessionProgressSheet.jsx:286-298); add to index.css, 150-300ms per CLAUDE.md.
8. **Unified validated-number field** — numeric clamps are hand-rolled per field with DIFFERENT ranges for the same setting across the two teacher surfaces (AssignListModal.jsx:189-207 vs ClassDetail.jsx:1284-1309); DF2-11's validation leg should define one clamp set in one shared field component.
9. **A 4th step status** (pending/warning) for the session step model, required by the SUBMITTED-not-graded state (§3) — SessionProgressSheet renders at 4 sites (DailySessionFlow.jsx:1915, MCQTest.jsx:1376, TypedTest.jsx:1637/:1687) so one component change propagates.

## 7. ko/en copy conventions + theme (dark-mode) obligations

**ko/en (VERIFIED):** there is NO i18n infrastructure — contexts are AuthContext and ThemeContext only (ls /app/src/contexts). The bilingual convention lives in student-facing blocking/error strings as ONE hardcoded string, Korean FIRST, English in parentheses (or newline-separated in TypedTest): DailySessionFlow.jsx:899, :1554, :1615; MCQTest.jsx:637, :879, :890-891, :900, :920; TypedTest.jsx:893, :1135, :1147-1148, :1158, :1179. Example shape: '이 날을 완료하려면 먼저 새 단어 시험을 통과해야 합니다. (Day not complete — pass the new-word test first.)' (MCQTest.jsx:879). ALL UI chrome (buttons, headers, teacher surfaces, Dashboard hero, HelpModal) is English-only today. Consequences: (1) the new student blocking states — retake wall, SUBMITTED-not-graded, resume sheet, day-advance prompt — should follow the ko-first+(en) convention of their surface class; (2) DF2-11's "plain-language ko/en labels" (R2-15, 11_:31) has NO teacher-surface precedent — a NEW convention decision (inline pairs like the student strings, vs stacked bilingual labels) is required before build; (3) `06_MESSAGING_COPY.md`, the mandated single vocabulary source (02_TASK_LIST.md:70 M9, :115), DOES NOT EXIST yet (find verified) — the C2-audited surfaces must consume it by pointer when DF2-32 authors it; copy rules already decided: no "frontier/segment" jargon, TZ-honest wording (02_TASK_LIST.md:115). Fonts already support Korean everywhere (Pretendard body font, index.css:233-303).

**Dark mode (VERIFIED):** class-based `.dark` on documentElement (ThemeContext.jsx:78-84); every token flips (index.css:131-229); `dark:` Tailwind variants are the sanctioned escape hatch where no token exists (SessionProgressSheet.jsx:209, :216, :233, :255; Flashcard.jsx:97). OBLIGATIONS for all five surfaces: use on-semantic-bg text tokens on semantic fills exactly as MCQ result cards do (MCQTest.jsx:1188-1210; tokens index.css:427-433 — white in dark mode :210-216). The navy hero is deliberately theme-invariant (fixed white-on-brand-navy, Dashboard.jsx:1775, :1809-1810) — Dashboard-side entry points (streak display, resume affordance) must pick hero-style or token-surface style explicitly. **Raw-palette defects that must NOT be copied into new surfaces (all VERIFIED):** Badge variants (Badge.jsx:4-10) · Gradebook chips + divider (Gradebook.jsx:1115, :1123-1143, :1081) · AssignListModal slate labels (AssignListModal.jsx:62, :70) · ClassDetail read chips (ClassDetail.jsx:804, :806) · TypedTest grading modals (TypedTest.jsx:1813-1907) · LoadingSpinner (`border-t-blue-600`, LoadingSpinner.jsx:11) · DSF spinners (`border-blue-500`, DailySessionFlow.jsx:1809, :1964) · MasterySquares (MasterySquares.jsx:26, :48) · SessionProgressSheet's hardcoded "95% required to pass" (SessionProgressSheet.jsx:82) and HelpModal's "Must score 95% to continue" (HelpModal.jsx:212) are the DF2-07(a) threshold hardcodes — new surfaces derive from the real threshold (live precedents MCQTest.jsx:1201, RetakePrompt DailySessionFlow.jsx:2383-2386).

**Naming continuity:** "Day N" is the universal vocabulary (SessionProgressSheet.jsx:150, Dashboard.jsx:1837, ClassDetail.jsx:129, SessionHeader.jsx:88, Gradebook Day column :1146) — the TOC keeps Day numbers (R2-24b "teacher pacing surfaces read Day numbers exactly as today"); any "chapter" metaphor stays internal.

## Open questions

- Session-start precedence: when BOTH the R2-40(a) resume sheet and the DF2-12 pending-job pickup ('finish grading your earlier test') trigger at the same session init, which surfaces first? The spec defines each but not their ordering (11_:35 R2-40a; 02_TASK_LIST.md:81 session-start pickup).
- Teacher-surface bilingual layout: DF2-11 mandates ko/en labels but no teacher surface is bilingual today — inline 'ko (en)' pairs per the student-error convention, or a stacked/label-caption layout? Needs a David/design call before the AssignListModal rebuild.
- Shared switch primitive vs checkbox composition for the two new toggles (reviewGateEnabled, gradebook rerun reveal) — build one ui/Toggle or reuse the ClassDetail.jsx:1363-1373 checkbox idiom?
- Mobile affordance for the pips' exact-count reveal (R2-40c 'hover/long-press') — no tooltip/long-press primitive exists and title= is desktop-only; tap-to-reveal, or fold the count into the day sheet?
- TOC placement: inside DailySessionFlow (a phase/overlay composing SessionHeader) or a separate pre-session route — determines whether it inherits session chrome (SessionHeader.jsx) or Dashboard chrome (HeaderBar), and where the resume sheet anchors.
- Gradebook default-OFF mechanics: is rerun exclusion implemented in queryTeacherAttempts (server/query layer) or as a client-side filter over returned rows? db.js:2024-2076 currently has no type filter either way; the choice affects pagination correctness (a client filter breaks page sizes).
- Does the DF2-51 read-again affordance write the restudy bookmark to Day 1 and navigate, or open the TOC at Day 1? The CompletePhase 'Start over' slot it re-skins is currently wired to retired cycling semantics (DailySessionFlow.jsx:2527-2536) and must not inherit them.
