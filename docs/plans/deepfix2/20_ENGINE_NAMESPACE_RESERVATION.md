# 20 — ENGINE NAMESPACE RESERVATION — **SUPERSEDED, see `20_RV2_NAMESPACE_RESERVATION.md`**

This file was authored 2026-08-04 ~12:20 as a parallel design of the pre-flip namespace-reservation fold
(NTF 19+22), unaware that another session had authored `20_RV2_NAMESPACE_RESERVATION.md` ~12:11 covering
the same fold. The two converged on the SAME three client→global-collection "mouths" (rules attempts-create,
the `submitVocabAttempt` Admin-SDK bypass, and `gradeTypedTest`) and the SAME `rv2_`-prefix reservation —
independent convergence, which strengthens the finding.

**`20_RV2_NAMESPACE_RESERVATION.md` is the canonical union.** It is more complete (all-write-verbs rules
guard, the NTF-20 comment-repair bundle, a fuller safe-site census, the NTF-23 shrink analysis, and
alternatives-rejected), and this file's unique contributions (the already-existing retrospective receipt,
RE2 full-match detail, the deploy/Codex/David sequencing, the abort-card cross-ref) were folded into it.

Nothing here is authoritative — read `20_RV2_NAMESPACE_RESERVATION.md`. The working fold ledger is
`_ledgers/namespace-reservation-fold-ledger.md`.
