# DEEPFIX 2 — Redesign verification & fold plan (11_, 2026-07-26 — David's decision round 2)

> **§0 STATE (FINAL — decision phase COMPLETE 2026-07-26):** the round-2 walkthrough, two question rounds, the
> round-5 internal panel (13_), and Codex r46/r47/r48 are ALL COMPLETE. **The decision board is CLEAR: ledger
> R2-1..R2-47 below is the binding record — R2-38..41 ratified 2026-07-27, post-presentation** (per-exchange audit:
> `12_R2_DISCUSSION_TRACE.md` — one row per exchange, standing rule). The ONE
> deferred gate: R2-10 challenge-reversal (activates at A2's certification). **r48 verdict: both owner overrides
> ratified; MECHANICAL FOLD GO** — executing as one pass over the three checklists (13_ + r46 §fold-checklist +
> r47/r48 contracts), then DF2-14 authoring, Track A, Track B design, C2, bounded r49 fold-verification.

> **What this is:** the execution plan for David's 2026-07-26 second decision round on the review-graduation
> redesign (addendum 10_): his answers recorded verbatim-intent, the verification tracks he ordered, and the fold
> work. **Convergence method for every item marked 🔍6: 3 Fable + 2 Opus + 1 Codex (David's explicit spec —
> supersedes the reduced roster for these audits).** Actions log to MASTER_TASK_TRACKER under DF2-14/11_ refs.

## 1. DECISION RECORD (David, 2026-07-26 — R2-1..R2-47 — ALL NON-SUPERSEDED CLAUSES BINDING [r49-H1/r50-H2: inline ⟶ markers govern; where rows conflict, the LATER row wins]; the one deferred gate = R2-10/A2)

| # | Decision |
|---|---|
| R2-1 | **NO escape hatch — the review gate is HARD.** A failed retake never auto-completes the day. Relief = the TEACHER force-pass (below). r45-B2's exhausted-day disposition is DISSOLVED (no advance-without-pass exists; every advanced day has passed evidence, possibly teacher-authorized). |
| R2-1b | **Teacher force-pass ships as a plan item**: implemented as override-regrade of the failed review attempt **⟶ SUPERSEDED (r46-H4): today's `overrideAttempt` is a synthetic-attempt/manual-pass path, NOT reusable — a NEW exact-attempt grade-only resolver is the implementation (A1(vii)/DF2-10 wp-7)**; originally "(approach (i) — the existing surface)"; UI on the class panel next to the stuck-student row. **+ an informative read-only attempt label ("edited by teacher") so future readers never see the attempt's metrics as organic** (verify existing override metadata first — A1). |
| R2-2 | **Sticky (b)**: ever-failed words graduate only by direct proof in every cycle (needs the dedicated `fillEligible` predicate — r45-H7). |
| R2-3 | **Backfill = FULL history (a)**; students see only their teacher-set review count, so no visible-count shock. |
| R2-4 | **Teacher-set review numbers**: per-assignment `reviewQueueSize` + `reviewTestSize` (fixed numbers, no ranges). Shrink ONLY when the pool is smaller (test = whole remaining pool). **Small-sample gate harshness ACCEPTED — no minimum-count exemption** ("if they fail a 4-word test, they restudy 4 words — trivial"). |
| R2-5 | **All-at-once rollout** (no staged/per-class canary). Compensating controls REQUIRED: 25WT rehearsal + **very extensive, rigorous Playwright audits** + day-one monitoring with abort signals + the global kill switch (instant revert). |
| R2-6 | Enablement flags: **David flips them on ALL classes BEFORE deploy via firebase-admin SDK** — deploy choreography note: pre-set flags are inert to old code (additive unknown fields); activation happens at the deploy itself; §0's "no lever enableable mid-deploy" is satisfied because the LEVER-READING code and the lever arrive together, verified post-deploy. **⟶ SUPERSEDED by R2-31 (r48-sealed): dark-deploy `enabled:false` → verify the full set → backfill + bounded delta → ONE audited cohort-wide flip. The arrive-together rationale was incorrect (deploys are non-atomic across callables/artifacts) — retained as history only.** |
| R2-7/8 | Pass-to-advance exception + cross-class scope questions — DISSOLVED by R2-1 and R2-5 respectively. |
| R2-9 | **Backfill-then-launch**, with the FULL verification pipeline (Track B): investigation plan, investigation script, EXPECTED-result calculation (plan + script + results), backfill script, execution, post-verify vs expected — **each artifact 🔍6-converged**. |
| R2-12 | **Fragility residuals adjudicated (David 2026-07-26):** (a) grader-outage window — David's UX adopted: **"Retake OR Try again later"** (+ allow REVIEW-phase entry while new-test grading is pending — a deliberate forced-mode flow change; day still uncompletable until the grade lands); copy = "saved on this browser, survives closing it" (localStorage truth: survives close/reboot; dies on cleared site data / incognito / ~7-day-unvisited Safari / managed-profile wipes / device switch). **closure ⟶ ADOPTED (sealed by R2-13; built at DF2-12) — originally proposed: persist the submitted rows + grading history (`gradingAttemptedAt`/count/lastError/`succeededAt`) ON THE GRADING JOB at claim — never an ungraded attempt (attempt stays write-once at grading success; zero reader changes; David's can-of-worms correctly avoided) — plus pending-job pickup at session init ("finish grading your earlier test"), which closes the cross-device/wiped-profile residual**; (b) **NO graduation checker** — window is seconds-wide, harm = deferral, repair couldn't reproduce the random fill; the structural fix (graduation inside the server completion txn) arrives at consolidation; (c) legacy per-word status batch confirmed display-only post-redesign (behavioral truth = server-written labels), retired at DF2-46. |
| R2-14 | **VALUES + LAUNCH POSTURE (David 2026-07-26): queue 60 · test 30 · review-pass gate STARTS ON with DEFAULT ON at 92% for every class at flip** — reverses the old default-OFF posture (DF2-10/11 language rewritten in the C1 fold). Consequent LAUNCH BLOCKERS (same release): teacher force-pass UI in hands · teacher/TA comms for the wall + button · day-one monitoring sized for mass wall events; kill switch = the instant cohort-wide revert. **Config mechanism approved: ONE global doc `system_config/review_v2` {enabled:true, threshold:92, queueSize:60, testSize:30}** — David's fireadmin script writes it once ⟶ WRITE CHOREOGRAPHY SUPERSEDED by R2-31 (dark-deploy `enabled:false` → verify → backfill → ONE audited flip; the write moves AFTER deploy-verification); `enabled:false` doubles as the kill switch; per-assignment fields override where teachers later edit. |
| R2-15 | **Priority slots UNCAPPED (David): the test fills with as many flagged words as the day's queue offers (up to all 30) — maximum exposure to difficult words is the pedagogy.** Anti-repetition ⟶ SEALED (R2-26 Q13) *(clock field renamed `reviewLastTestedAt`, r53-B3)*: slot ordering key = **LEAST-RECENTLY-TESTED first** among flagged-in-queue (uses the existing per-word lastTestedAt) → consecutive tests rotate through the whole difficult backlog before repeating; ~~recovered words visibly drop out per retake~~ **(struck [FF+O2-14]: under the final two-predicate law nothing recovers on a failed attempt — flags persist across a day's retakes; drop-out follows a PASSING attempt)**; presentation shuffle retained. Honest boundary: backlog ≤ 30 ⇒ repeats across retakes are unavoidable (= the intended grind). Supersedes fixed-half-15 AND the earlier lastFailedAt-ASC ordering. **Menu simplification ADDED to the lever-UI card's scope [David #4]: consolidate AssignListModal into pace/new-test + review groups, delete the two dead review-size fields, plain-language labels w/ visible defaults (60/30/92), ko/en — audited by C2.** |
| R2-16 ⟶ *superseded by R2-29/R2-30 (the two-predicate law: correct-on-any-attempt clears PRIORITY; proof still pass-gated)* | **ASYMMETRIC LABEL STAMPING [proposed 2026-07-26, pending David's nod — closes the guess-spam laundering vector David's per-submit question surfaced]:** `lastFailedAt` stamps per wrong answer on ANY graded attempt; **`lastPassedAt` (recovery / unrecovered-clearing / fill-eligibility) stamps ONLY from the day's PASSING attempt** (+ accepted challenge; force-pass stamps nothing — consistent with R2-1b). Rationale: with symmetric stamping, random-guess spam recovers ~25% of flagged words per round (~94% per word over 10 rounds) — laundering the sticky/priority mechanism without learning; asymmetric stamping makes recovery un-guessable (requires a ≥92% passed test) and aligns labels with D-2 ("proof requires a passed test"). Blank spam stamps nothing (no answered rows); genuine wrong-answer flags are truthful and self-inflicted. Backfill/B1 calculation implements the SAME rule (historically trivial: pre-gate reviews all auto-passed). |
| R2-38 | **CROSS-CLASS EVIDENCE UNDER MIXED GATE POSTURES (David 2026-07-27, closing r51-B1): PROGRESS IS INDIVIDUAL — a valid attempt under the SOURCE class's effective posture is cross-class evidence for the shared logical day, INCLUDING an OFF-source auto-pass satisfying an ON target's day** (alternative (a), stated plainly as a deliberate product decision per Codex r51). Same family as R2-36's accepted threshold ambiguity. **Teacher escape hatch (named by David): a teacher wanting total control creates a NEW list assigned to their class only.** Labels stay class-blind: an OFF-source attempt mints NO proof (per-assignment freeze) while fail+correct stamp as always; an ON-source attempt on the shared word still mints proof; `reviewLastProvenAt` is globally frozen only under global OFF. CERT: the dual-class oracle gains mixed A-ON/B-OFF cases BOTH directions + concurrent submits + a mid-day assignment-disable edit (queue snapshot governs) [r51]. |
| R2-39 | **SAME-LIST CYCLING RETIRED (David 2026-07-27, closing D7/P9's F2):** the universal model already covers the goal ("students restudy each day") — the past-day browser + never-ending review + the LIST-END LAW: **a day with ZERO new words advances on the REVIEW TEST ALONE; the day counter, rotation, and streak continue (no phantom empty-day wall)** — now normative on DF2-10(2)/10_ §1. The lap-position question (F2) dies unanswered (nothing needs it). Evidence: `scripts/cs/cycling-usage-probe.mjs` 2026-07-27 — 302 classes, 0 `cyclingEnabled:true`, 0 `nextListId` (zero blast radius). Dormant cycling code (`cyclingEnabled` reads, `resolveEffectiveCycling`, G-CYCLE legs) removed at DF2-46. **KEPT: the NEXT-LIST feature — `nextListId` survives as the pointer for a list-finished END-SCREEN affordance ("start the next list"), UI at C2/DF2-51 scope.** CERT keeps list-end/continuation cases; lap cases drop. |
| R2-40 | **RESTUDY NAVIGATION + VISIBILITY PACKAGE (David 2026-07-27):** (a) session-start = a STATE-AWARE RESUME SHEET — [Continue re-reading: Day X (bookmark)] vs [Go to today: Day N]; today-first highlight until today's work is complete, then flips to the bookmark; no bookmark ⇒ no sheet. (b) Day picker = a TOC with FIVE states: locked (intake only) · today/frontier · last-visited (bookmark) · completed · re-completed; **post-intake the TOC never grows phantom chapters** — N chapters + one persistent "Today's review" entry (day counter still runs underneath, R2-39). (c) **RE-COMPLETION = BOTH rerun tests passed in that day's visit** — the day's passed NEW-WORD retest + a passed REVIEW rerun taken in that session (session-scoped pairing, mirroring a real day; each passing attempt counts toward at most ONE pip — no farming one half) **[amended to (ii), David 2026-07-27 — was: new-word retest only; rationale: the forward progression should feel like a real day]**; display = 5-segment MASTERY PIPS per day (capped at 5; ~~exact count on hover/long-press~~ ⟶ SUPERSEDED by R2-45: static "×N" total beside the pips, no hover/tap; pip-fill micro-animation); counter = SERVER-written, DISPLAY-ONLY, on a non-progress doc (rules-locked server-only; no progression logic ever reads it). (d) **Restudied days carry their OWN historical review set as label-neutral retests** (persisted-queue replay post-launch; deterministic day-offset reconstruction for pre-launch days); **the ONE live graded review exists only at the current day**. (e) Restudy BOOKMARK = a non-progress server pref (cross-device reading position — never progression; same class as the Dashboard focus). (f) End-of-restudied-day: [Continue to Day X+1 →] surfaces at RE-COMPLETION (both tests passed — the natural forward moment; TOC jumping stays free, no hard gate in restudy) + a streak-preserving "today's review is waiting" reminder; list-end screen = [Start next list (`nextListId`)] + [Read again from Day 1] + today's review. (g) **GRADEBOOK: original scores only by DEFAULT; retests remain stored VALID attempts (D-3) — a teacher TOGGLE (default OFF) reveals re-run attempts, read-only display; aggregation/accountability unchanged.** Build home: DF2-51's in-train leg (student UI) + the gradebook toggle on the teacher gradebook surface; C2-audited. **(d) is superseded in part by R2-41: rerun review = REGENERATED (pure-random), and reruns STAMP.** |
| R2-41 | **UNIFIED STAMPING + RERUN GRADUATION (David 2026-07-27 — supersedes R2-26 Q10's label-neutrality, r50's clock freeze, and R2-40(d)'s stamp-free regeneration):** (a) **EVERY graded test stamps** — live new · live review · rerun new · rerun review: wrong/BLANK = fail, correct = correct, on the presented set (matches the attempt-type-agnostic backfill law; **live NEW-WORD tests stamp too** — Q1 yes). (b) Proof = tested-correct on any PASSING test (unchanged; pass-gated). (c) **Graduation = review-type tests only**: live = the full R2-29 formula (correct + eligible fill); **RERUNS graduate TESTED-CORRECT ONLY, no fill** — a rerun graduates exactly what it proves. (d) Reruns stay NON-ADVANCING + gradebook-preserving (toggle reveals them); the recency clock (`lastTestedAt` ⟶ renamed `reviewLastTestedAt`, r53-B3) advances on review-type tests INCLUDING reruns. (e) **POOL-UNDERFLOW RULE (Q2): active pool < queueSize ⇒ top up from GRADUATED (resting) words, EARLIEST-GRADUATED FIRST**, up to queue size (supersedes bare "pool<size ⇒ whole pool"). A word failed while resting keeps its rest schedule (the fail is recorded; it arrives as priority when it returns or is pulled by underflow) — no early-return machinery. (f) ACCEPTED: introduced-range sabotage bound (Q3 — the mandatory queue stays fixed; self-harming only) · proven-metric dilution (Q4 — no stakes at the frontier). (g) **H8 must model rerun activity** (graduation throughput, return-slug sync, underflow dynamics); rerun-graduation volume = a monitoring signal; a config cap stays pocketed. (h) Rerun review composition = regenerated PURE-RANDOM draw over the FULL introduced range (incl. resting words), no priority slots, fresh shuffle per rerun (rows 70-71). |
| R2-48 | **THE DARK-WINDOW CUSTODY DECISION (David 2026-08-02, verbatim ratification of the r64/r65 proposal — "Yes, on the dark windows ratification"):** the six label fields are DORMANT until the first activation — live label writers stamp iff `system_config/review_v2.firstEnabledAt` is SET ∨ the class ∈ `rehearsalClassIds` (the rehearsal carve-out; battery-A ordering per 16_). **The FIRST flip is ONE audited txn writing `{enabled:true, firstEnabledAt:serverTimestamp}` TOGETHER** — `firstEnabledAt` write-IFF-absent, NEVER cleared; every later write (kill switch, re-enable) touches `enabled` only. **R2-32's OFF-stamping law is hereby SCOPED to post-activation kill-switch windows** (its ratified context; post-activation behavior unchanged). The dark window has zero live label writers outside rehearsal classes — B3 owns the fields exclusively; B3 is FORBIDDEN from the marker onward (durable guard, lap-proven). The pre-flip TAIL is classified + published per 14_ §4 (`preFlipTail`; bounded by the final pre-flip micro-lap; heals through live use). |
| R2-47 | **THE EXPOSURE-CLOCK PROMISE RETIRED (David 2026-08-02 — "Retire", after the provenance walkthrough: the numeric test-exposure guarantee protected nothing any mechanism consumes):** the written promise "every unproven word is tested within N days" is DELETED from the contract set. What replaces it: (1) the STRUCTURAL certification — the CURSOR-CHAINED rotation serves every word that remains in the ACTIVE pool across any TWO CONSECUTIVE LAPS (the lap-based guarantee — the day-offset/fixed-cycle claims were both FALSIFIED by the fixture [r58]; `rotation-cyclicity-fixture.mjs` v4, 2,688 checks measured from the current file [r62p — one number, remeasured at every fold], is the certified artifact); (2) the R2-42 deterministic selector + effectiveTestSize invariant + priority-prefix-preserving seeded fallback stay EXACTLY as built (correctness machinery, not promise machinery); (3) ALL exposure timings (gap percentiles, saturation days, plateau pools, return slugs) become MONITORING BASELINES — descriptive, nothing passes or fails by them; H8 = the baseline generator + the structural fixture. Supersedes the numeric-bound clauses of R2-42/R2-46 (their mechanism clauses stand); dissolves the r56 fractional-vs-binary oracle dispute (there is no oracle). Mechanism unchanged; zero student-visible effect. |
| R2-46 | **THE BOUNDED-EXCEPTION FAIRNESS CONTRACT (David 2026-08-02 — option (b); its NUMERIC-BOUND clauses ⟶ SUPERSEDED by R2-47 (promise retired; the effectiveTestSize invariant + fallback + saturation-monitoring clauses STAND)):** the exposure bound on unproven words is CONDITIONAL — it counts only days where the test had ≥1 remainder slot (`priorityCount < effectiveTestSize`); under PRIORITY SATURATION the test is 100% failed words BY DESIGN ("students will still be testing words they failed" — the pedagogy is the point) and unproven words defer, retaining STUDY exposure (queue membership is unconditional) + the RERUN path (no priority slots, stamps + graduates — the always-open proof route). Saturation is self-limiting for advancing students (a 92% pass clears ~27 priority words) and is a MONITORED signal (server-only sink). David rejected the testSize/queueSize config guardrail — every slot going to failed words first is correct under any config. **R2-42's invariant REPAIRED: `effectiveTestSize = min(testSize, |pinned queue|)`; the test = the top-min(priorityCount, effectiveTestSize) priority words by LRT, then remainder LRT; the invariant check asserts exactly-effectiveTestSize unique queue members INCLUDING that priority prefix; the fallback randomizes ONLY the remainder, preserves the priority prefix, and records a reproducible seed.** |
| R2-42 | **COMPOSITION LAW SEALED (David 2026-08-01, closing Q-D1 — proposal (b)+fallback accepted): the test's non-priority REMAINDER is selected DETERMINISTICALLY least-recently-tested** (`reviewLastTestedAt` asc, absent-first, tie wordIndex — the same key as the priority slots; presentation ORDER still shuffled). **THE GUARD: a post-compose INVARIANT CHECK** (⟶ repaired by R2-46: exactly `effectiveTestSize = min(testSize,|queue|)` unique queue members · the top-priority LRT prefix included · fallback randomizes only the remainder + records its seed) — **any failure ⇒ SEEDED-RANDOM FALLBACK from the same presentable set** (`compositionVersion:'fallback-random'` on the presentation record; `composition_fallback` ~~system_logs~~ ⟶ `ops_metrics` event [r56 — system_logs is client-creatable, never an authority signal]; a monitoring signal). Selection is not correctness-critical (grading/validation bind to the presentation record regardless), so the fallback is fail-open-safe by construction. H8's fairness role ⟶ per R2-47: baseline generator + the structural cyclicity fixture (no pass/fail bound). |
| R2-43 | **CHALLENGE-ON-RESTING GUARD (David 2026-08-01, closing Q-D2 — prohibit accepted): a challenge acceptance always applies the GRADE fix; the word-status write is SKIPPED while the word is resting** (`reviewRestingUntil` in the future) — rest schedules move only through graduation and return. Failure modes both degrade to pre-existing behaviors (skip-when-shouldn't ⇒ un-marked status, self-heals at next test; write-when-shouldn't ⇒ the old behavior) — intrinsically safe, no fallback needed. Satisfies R2-10 activation condition (ii). |
| R2-44 | **LAUNCH UI LANGUAGE = ENGLISH-ONLY (David 2026-08-01 — supersedes every ko/en label mandate on the NEW surfaces, incl. DF2-11's and the DF2-32/06_ messaging-register bilingual spec): Korean lives in CONTENT only (the vocabulary definitions — list data, a separate concern David confirmed); existing screens keep their current strings (byte-identity). A site-wide Korean/English toggle DOES NOT EXIST today and is NOT in this program — future feature, card only on David's ask.** |
| R2-45 | **PIP OVERFLOW = ICON + TOTAL NUMERAL (David 2026-08-01, closing C2-2 by convention — the Slack-reaction/Duolingo-crown pattern for repeated-action counts, vs "+N" which is for distinct-item lists): counts 1-5 ⇒ pips fill; counts >5 ⇒ five filled pips + a static "×N" total beside them.** No tooltip primitive, no tap interaction; the day sheet also shows the count. |
| R2-37 | **r48 FINAL RATIFICATION (Codex): BOTH OWNER OVERRIDES RATIFIED — FOLD GO.** Two error-prevention contracts carried into DF2-14 (implementing David's own bar, no new decisions): **(1) Q2 hygiene CORRECTED — impossible records are EXCLUDED, never clamped into proof** (WSL's clamp-to-100 was wrong: a stored 120% clamped to 100 passes 92 and converts known corruption into `reviewLastProvenAt`; validity filter = finite score 0-100, sane totals, numerator≤denominator, score↔rows agreement, no conflicting duplicates; malformed ⇒ omitted from ALL four labels + published exclusion counts). **(2) Q3 contracts:** queue identity = `{uid,classId,listId,logicalDay,resetEpoch,algorithmVersion,configVersion}` w/ snapshotted values (mid-day teacher edits affect the NEXT queue); cross-class evidence rule (an attempt validates only against ITS OWN class queue; a valid cross-class pass satisfies the shared logical day on uid/list/day/epoch match; consumed attempt + source config recorded in the audit); concurrency rule (exactly-one advance + one graduation per logical day, idempotent winner, loser gets `already_completed`, label mutations independently idempotent, epoch mismatch rejects, NO client focus/first-opened state in server authorization); the 10-case dual-class fixture list = the cert oracle. Q4 ratified as final law; Q7 ratified w/ honest wording (logical expiry EXACT at server-authored `expiresAt`; native TTL = async physical backstop; expired pickup transactionally marks + redacts). Bookkeeping: R2-15 stale open-item removed from the trace; **R2-10 carried as an explicit DEFERRED GATE (A2), not resolved**. Fold carries 8 contracts (incl. r46-B2 watermark, queue/presented identity, cached-client negotiation, epoch binding, kill-switch law, TTL wording). |
| R2-36 | **Q3 DECIDED (David — the LAST open decision): the shared-student ambiguity is ACCEPTED; the resolver options are rejected as overcomplication.** Per-class sessions run per-class settings over shared word truth (proofs cross classes exactly as MASTERED does); the student CONTROLS which class governs their day via the Dashboard focus — **premise CODE-CONFIRMED: `primaryFocusClassId/ListId` is an explicit persisted user setting (Dashboard.jsx:379-385 write, :1163 read)**. r46-B4 CLOSED by owner risk-acceptance. Binding conditions (David: "as long as it doesn't cause errors"): (i) the dual-class fixture is a CERT GATE — student on one list in two classes w/ different threshold/queue/test settings: no crash, no false server rejection, no data corruption (ambiguity permitted, ERRORS not); (ii) technical residue: the server-owned day-queue identity is CLASS-SCOPED (keyed class+list+day) so cross-class verification can't false-reject; (iii) label stamps stay class-blind (shared) by design; reconciliation/carry rules unchanged. **THE DECISION BOARD IS FULLY CLEAR — every R2/question-round/adjudication item sealed. The mechanical fold (panel + r46 + r47 checklists) is authorized to run.** |
| R2-35 | **Q2 DECIDED (David, overriding r47's provenance qualification as unnecessary overhead — "I doubt students cheated on MCQ tests to that extent"): UNQUALIFIED RECOMPUTE-AT-92** — all attempt history feeds all four labels symmetrically; a historical correct answer proves its word iff its attempt's stored score ≥ 92. Risk consciously accepted (pre-lockdown-era data quality); Track B hygiene **⟶ SUPERSEDED by R2-37/r48: impossible records are EXCLUDED from all four labels, never clamped into proof** (the clamp wording here was corrected at r48). **Q4 SEALED as amended** (fail+correct write while OFF; proven freezes; cold-start HOLD; config-version stamped through completion). **Q7 DECIDED: 12-HOUR recovery window** (David's preference; implementation confirmed EASY — exact enforcement = one `expiresAt` comparison on the pickup/retry path; physical cleanup = native Firestore TTL, lazy; UI shows the window + warns; expiry ⇒ rows hard-deleted, student takes a new test; reset cancels immediately). Practical note recorded: submit-evening → return-next-evening (>12h) = expired → redo the test (the day hadn't advanced anyway). **Q3 = the LAST open decision** (resolver pick pending David's read of the simple explanation). |
| R2-34 | **r47 OWNER-ANSWER ADJUDICATION received (Codex):** Q1/R2-31 SOUND — SEALED (closes r46-B1's activation half; B2 watermark cutover still open, Track B). **Q2 AMENDED: blanket score≥92 REJECTED** (pre-lockdown eras = client-authored grades/denominator bugs — unqualified trust just moves the laundering) → **provenance-qualified recompute**: Track B proves the earliest trustworthy attempt era; only trusted attempts feed ANY label (symmetric — the one-sided "historical failures, prospective proofs" variant FORBIDDEN); untrusted history diagnostic-only; fallback if no trustworthy boundary provable = ALL FOUR labels prospective (explicitly superseding R2-3). **Q3 REJECTED: "follow current mechanics" describes the collision, doesn't resolve it** (`reviewLastProvenAt` carries no threshold — a 70-class stamp becomes 92-class proof; queue identities collide when sizes differ; "which class opened first" must not define the contract) → required: ONE server-owned effective-config snapshot per student+list+logical-day `{threshold,queueSize,testSize,reviewTestType,sourceAssignments,configVersion}`; David picks the resolver (Codex safe default: agree→use it; conflict→STRICTEST-WINS deterministic + teacher-visible + logged; never first-opened/latest-edit/student-choice). **Q4 AMENDED**: OFF law = fail AND correct stamps keep writing, proven freezes, retests neutral; cold start with no cached posture ⇒ HOLD (unavailable; mint nothing) — never guess OFF; attempts keep stamped config-version through completion. **Q5 ACCEPTED + epoch contract**: reset deletes study_states (labels die naturally); "lifetime" = within reset epoch; resetEpoch stamped on grading jobs/queue records/completion requests, mismatch ⇒ reject/cancel; pending jobs cancelled at reset; LEDGER CORRECTION: `resetProgress` is student-self-service today (teacher/CS reset = separate surface if built). **Q6 SEALED**: delete the fallback; `actualMode = assignment.reviewTestType || 'mcq'` (matches teacher UI defaults); fixtures listed. **Q7 AMENDED**: WSL's "expiry recoverable via re-grade from stored rows" was CONTRADICTORY (deleted rows can't re-grade) → honest contract: 30d full recovery cross-device; at expiry rows hard-deleted + student takes a new test + UI warns; graded jobs follow shorter cache retention; reset cancels immediately. AWAITING DAVID: Q2 pick · Q3 resolver pick · Q4 ratify-as-amended · Q7 ratify honest-expiry. |
| R2-31 | **ACTIVATION CONTRACT CONFIRMED (David, r46-Q1 "Yes"):** dark-deploy every surface with `enabled:false` → verify the full set → backfill + bounded delta → **ONE audited cohort-wide config flip** (David's fireadmin write moves to AFTER deploy-verification). R2-6's literal pre-set-true mechanism is AMENDED; all-at-once EXPOSURE preserved. |
| R2-32 ⟶ **[SCOPED by R2-48 (David 2026-08-02): applies to POST-ACTIVATION kill-switch windows only; dark-window custody per 14_ §4 + 15_ §7 `firstEnabledAt`; post-activation behavior unchanged]** | **KILL-SWITCH LABEL LAW RATIFIED (David, r46-Q4):** while OFF — fail stamps keep writing (truthful, inert), proven stamps FREEZE (auto-passed OFF reviews never prove), graduation reverts to legacy, config-read failure ⇒ last-known posture. **David's inertness premise CONFIRMED: the labels have ZERO legacy readers (brand-new fields) — under revert nothing consumes them; accumulated OFF-window fail stamps are inert-then-truthful on re-enable.** |
| R2-33 | **r46 open trio + modality — WSL responses (Codex input requested via r47; David finalizes):** (Q2 historical proof) recommend **RECOMPUTE-AT-THRESHOLD** — proof = a historical correct answer on an attempt whose stored score ≥ 92 (one comparison per attempt during backfill; semantically uniform; slightly softens day-one fill-ineligibility with zero laundering risk); fails + corrects backfill as facts regardless; David's "start fresh" (prospective proofs) = the strictest defensible alternative. (Q3 shared students) recommend **FOLLOW CURRENT MECHANICS** — each class session runs at its own class's threshold/sizes; label stamps land on the shared per-student-per-list records whichever class produced them (exactly how MASTERED already crosses classes today; ~197 multi-class students; quirk: a low-threshold class mints proofs the 92-class honors — teacher-created, visible, bounded; strictest-wins resolver = the later upgrade if wanted). (Q5 reset) defined: the existing start-over operation (`resetProgress` callable + resetEpoch) teachers/CS use to wipe a student's list progress; recommend **RESET CLEARS THE FOUR LABELS** (reset means fresh start incl. difficulty memory; matches the resetEpoch-scoped backfill; metering counters unaffected). (Q6 modality) **CODE-VERIFIED**: `getReviewTestType` (sessionService:374) flips typed→MCQ after 3 attempts "to save API costs" — but ONLY as the legacy fallback when the teacher's `reviewTestType` is unset (DSF:1198 honors the setting first); recommend **DELETE the fallback flip** — teacher-set type governs always (matches David's model; no silent difficulty change under the wall; AI cost is governed by R2-20 metering instead). (Q7 job retention) proposed default: **30 days** then expiry w/ the attempt recoverable via re-grade from stored rows. |
| R2-30 | **PITR = the launch backup mechanism (David).** Enable AHEAD of launch week (window accrues only after enablement — a one-time project setting, David/WinClaude side); verification-of-active joins the launch blockers; quiet-window export optional supplement; script backup keeps its CS surgical-restore role. **Label naming finalized after David's collision concern (VERIFIED: lastCorrectAt/lastProvenAt exist NOWHERE today — brand-new fields, zero readers): the four labels are review-prefixed — `reviewFailCount` · `reviewLastFailedAt` · `reviewLastCorrectAt` · `reviewLastProvenAt`; legacy `lastTestedAt`/`lastTestResult`/`timesCorrectTotal` untouched.** |
| R2-28 | **Kill switch KEPT (David: "we'll deal with problems when they arrive") + FULL DB BACKUP before deploy.** Backup-fidelity guidance delivered (managed export = restorable but not transactionally consistent during live writes; PITR = consistent point-in-time, recommend enabling pre-launch; script backups = quiet-window only + must walk subcollections). **Backup tooling verification (coverage of users/*/study_states + attempts + grading_jobs; restore rehearsal) joins the launch blockers / Track B.** |
| R2-29 *(names as finalized by R2-30: review-prefixed)* | **TWO-PREDICATE STAMPING SEALED (David 2026-07-26 — schema walked: collision check resolved, no-current-mechanism confirmed, "Ok… ready to fold").** Word-record labels (study_states/{wordId}), final: `reviewFailCount` (lifetime, +1 per wrong-or-blank, any attempt) · `reviewLastFailedAt` (any attempt) · **`reviewLastCorrectAt` (NEW third label — any correct answer, clears PRIORITY)** · **`reviewLastProvenAt` (renamed from lastPassedAt — correct on a PASSING attempt or accepted challenge; gates FILL/graduation)**. Derived, never stored: `needsPriority = reviewLastFailedAt ∧ (¬reviewLastCorrectAt ∨ reviewLastFailedAt > reviewLastCorrectAt)`; `fillEligible = ¬(reviewLastFailedAt ∧ (¬reviewLastProvenAt ∨ reviewLastFailedAt > reviewLastProvenAt))`. ~~Retests stamp NOTHING (R2-26)~~ ⟶ SUPERSEDED by R2-41 (every graded test stamps; proof stays pass-gated); blank = fail (R2-17); backfill reconstructs all four from attempts history. |
| R2-27 | **Question-list round 2 (David 2026-07-26):** Q2 = **COEXISTENCE/per-class-mode architecture OBSOLETE, confirmed** (code removal ≈ NONE — the two-mode machinery was never built; the removal is PLAN surface: DF2-47/50/51/52/55 dissolve, DF2-53 rescopes to single-model, FREE_NAVIGATION_MODEL gets a superseded banner; the surviving NEW UI = a past-day browser + within-day phase toggle). Q4 = **G-DUE CANCELLED** (DF2-42d/DF2-42 dissolve; the fairness/no-starvation criterion transfers to the rotation spec; DF2-43's gates clean up). Q7 = **day-one wall rate ACCEPTED** ("if they fail they can just try again") — nothing more critical behind it; it's a launch-day support-volume/comms-sizing fact (concentrated first-attempt walls incl. strong band), covered by the comms + monitoring launch blockers. Q8 = **force-pass purpose NARROWED: ERROR-CORRECTION ONLY** (unfair blocks — grader/technical), NEVER weak-student relief; class ability is managed via the pace + reviewQueueSize/testSize teacher settings; the daily-toll question DISSOLVES (weak students grind by design). Q12 = **(A) PROSPECTIVE PACE CONFIRMED** (past days frozen by the anchor architecture; changes affect next-day slices only). Q13b = **CODE-VERIFIED ANSWER: day completion uses ANY-PASSING semantics, not latest-attempt** — `getDayNewPass` (foundation:819-836) fetches the day's attempts and FILTERS to `passed===true` (+ valid anchor); later failed attempts are filtered out and can never mask a pass; day reconciliation (resolveListProgress, server, pinned core) still exists and is non-demoting — David's retest premise fully confirmed. **R2-16 OVERRIDDEN → ⟶ RESOLVED at R2-29/30 (David picked the TWO-PREDICATE split; review-prefixed names): David wanted SYMMETRIC stamping** (recovery on ANY attempt, incl. failed ones — rationale: retakes must visibly shrink the flagged set; least-recently-tested alone is "a limited remedy"). WSL counter-proposal delivered (two-predicate split: correct-on-any-attempt clears PRIORITY; correct-on-passing-attempt required for FILL-ELIGIBILITY/graduation — gives the visible shrink while keeping sticky-(b)'s unproven-graduation protection); David picks symmetric vs two-predicate. Q9 kill-switch elaboration delivered — ⟶ KEPT (R2-31/32). |
| R2-26 | **Question-list round 1 (David 2026-07-26):** Q1 = **(i) every class flipped to free-nav at once**, where free-nav (David's Q11 definition) = **students move freely between review and new-word work within the day, plus backward re-study/re-test** — day-advance stays gated on both tests. COEXISTENCE-kill confirm ⟶ CONFIRMED at R2-27 Q2. Q3 = **gate is a teacher-changeable per-class-list setting; 92 = the default, written to all lists in all classes via fireadmin at deploy** (not non-disableable). Q5 = **walled student keeps the SAME queue until they pass** ("mandatory but manageable" — resolves O2-06: day-indexed rotation is fine). Q6 = **ONE LAUNCH: gate ON at the first deploy = the all-at-once flip — gate + redesign + backfill ship together** (no two-deploy interim; the DF2-10/DF2-14 split becomes internal build/cert staging, not two production exposures; deploy only when finalized). Q10 = **past-day re-tests are PURE REVIEW: ~~label-neutral (no fail stamps, no recovery stamps)~~ ⟶ SUPERSEDED by R2-41 (reruns STAMP like any graded test; still non-advancing, gradebook-preserving)** — David's premise CONFIRMED: passing logic is at-least-one-passing-attempt + non-demoting advance, so later failed attempts change nothing; AI-metering (R2-20) still counts them. Q11 = **review-first/within-day phase freedom RATIFIED**. Q13 = **R2-15 SEALED (least-recently-tested) + R2-16 SEALED** (asymmetric stamping — distinct from R2-17 blank=fail; see trace row 49 clarification). Force-pass (Q8 partial): **operates on GRADES only — new-word and review flippable separately or together** — never word records (extends R2-1b). ~~PENDING~~ ⟶ ALL RESOLVED at R2-27..R2-32 (Q2 confirmed · Q4 G-DUE cancelled · Q7 wall-rate accepted · Q8 daily-toll dissolved · Q9 = the R2-29/32 label/kill-switch laws · Q12 = (A) prospective). |
| R2-23 *(recast under the universal model, R2-24/26/27 — "free-mode" = the shipped model)* | **Challenges/tokens = IDENTICAL machinery (David; register item 12 closed).** David's triviality note accepted with one nuance: segment-test walls ARE gated situations (a wrongly-graded typed answer can block a pass), so challenges matter there exactly as in forced mode — volume expected low, design identical, nothing new to build. |
| R2-24 | **UNIFIED-MODEL REDEFINITION (David, verbatim-intent — ⚠️ ripple-flagged as the round-5 panel's PRIMARY interpretation check ⟶ RATIFIED by R2-26/27's explicit answers — O2-01 satisfied; rescoping executed):** (a) **free-nav ships to ALL students SIMULTANEOUSLY** — no pilot class (register item 10 closed; DF2-61's pilot ladder rescopes to 25WT rehearsal → cohort-wide flip + monitoring). (b) **Pacing = the existing per-class/list `dailyPace`; One Day = one pace increment** (80 words ⇒ Day 1 = words 1-80); teacher pacing surfaces read Day numbers exactly as today (register item 8 closed — "nothing changes for pacing"). (c) **"No progression of days whatsoever unless the new-word AND review tests have passed their thresholds"** — day-advance gates on BOTH tests. **⚠️ ROUND-5 PANEL RULING (O2-01/02/03, 2026-07-26) ⟶ RESOLVED (R2-26 Q1 + R2-27 Q2: David explicitly chose (i) — COEXISTENCE dead, ONE universal model; the question list was answered in full; rescoping executed — the prohibition below is LIFTED; historical ruling follows): WSL's "one universal model / COEXISTENCE superseded" reading is UNRATIFIED INFERENCE — David's words equally support (i) every class flipped to free-nav at once (COEXISTENCE dead) and (ii) the capability released to all classes with no pilot, teachers still enabling per class (COEXISTENCE intact). A David-locked architecture decision (COEXISTENCE, 2026-07-17) may only be reversed by an equally explicit statement. The R2-24 QUESTION LIST (round-5 report) is now the gate: no Wave-4/5/6 card rescoping, no mode-lever retirement, no DF2-47/50/51/52/53 changes until David answers. What IS decided regardless of reading: no pilot class (item 10), day-unit pacing (item 8 — its "moot under unified model" clause RETRACTED), both-tests day-advance wherever days advance.** PMv2 rail: NOT addressed — register item 9 stays open at its wave. |
| R2-25 | **DECIDE-0 = (b) SINGLE-TRAIN (David; register item 1 CLOSED):** "without the UI, the other changes can't ship together either — leaning very strongly one-shot." Container visibility waits for the one attested train (canary re-homes to DF2-60 per the existing (b) carding). Standing exceptions (DF2-01/07/36/CONT-A/35 — own gates, wave-independent) REMAIN as carded unless David revokes; panel confirms the (b) scope note still matches his intent. |
| R2-18 | **Monitoring posture (David):** signal set at WSL discretion (wall-rate, label-write failures, score-drop vs expected, force-pass usage); **CS tickets = David's primary response signal**; monitoring = context for fundamental fixes, numbers set with B1 baselines. |
| R2-19 | **Grader round-2 INTENT (David): semantic equivalence — accept ANY answer demonstrating the student knows the word's meaning** (paraphrase, synonym, informal phrasing — never exact-wording matching; reject wrong-meaning/unrelated/empty). Prompt round 2 + regression suite encode it (verbatim-English family = accepted). Deploy go = at implementation time per R2-5/6 rule ("ask when I say implementation goes"). |
| R2-20 | **Forced-mode backward affordance (David): re-STUDY past days = YES; re-TEST past days = YES** (register item 11 closed beyond rec (b) — semantics default: `type:'retest'`, non-advancing, original pass stays the accountability score). **CONDITION: every AI-grading call is METERED** (count per student + global — the grading job is the natural counting point) as the metric for a FUTURE global AI-grading limit gate. |
| R2-21 | **Streak (David): progression-based** — a streak day = the student PROGRESSED that day (day completed/advanced), not mere activity; **weekend absences never break the streak**. Register item 13 closed (supersedes the any-activity default). Free-mode mapping (what "progressed" means without forced days) resolves at DF2-50/51 — proposal: any completed study session counts. |
| R2-22 | **Starvation probe SKIPPED (David; register item 16 CLOSED):** BlindSpot hide is FINAL regardless; the redesign kills the slicer (rotation = bounded intervals by construction), the interim is bounded, and no decision hangs on the count. DF2-0P shelved (card marked; the A3 investigation gives partial visibility for free). |
| R2-17 | **BLANKED-WORD RULE — DECIDED (David 2026-07-26): BLANK = FAIL, on ANY graded attempt.** Every word presented on a test but left unanswered stamps `lastFailedAt` (and is thereby auto-excluded from fill — blank is just "wrong"). David's accepted bound: flags only ever stamp PRESENTED words, tests draw only from the day's pinned queue, so worst-case growth = today's ≤60 queue words (due for study anyway); recovery stays pass-gated. Supersedes the passing-only nuance. **Implementation requirement: the submission carries the PRESENTED word set (blanks have no answer rows) — same field the M10 composition-verification already needs.** Note: `reviewFailCount` can inflate under repeat blanks — analytics-only since R2-15 made ordering least-recently-tested; no mechanic reads the count. |
| R2-13 | **Grading-outage UX SPEC (David 2026-07-26) + job self-sufficiency requirement.** (a) Job doc must hold EVERYTHING needed to resume+complete WITHOUT the client re-supplying: at claim, store the submitted `rows` + the FULL `writeContext` (attemptDocId, classId, listId, testId, testType, sessionType, studyDay, totalQuestions, anchor fields, display fields) + top-level queryable `classId/listId/dayNumber` + `lastError` (job today holds only uid/status/lease/attemptCount/payload — VERIFIED insufficient to resume; the server already resolves the answer key itself from (listId, wordId), and pre-AI idempotency + the graded cache already exist, so enrichment completes the resume path). (b) Student-facing states/options: clear "SUBMITTED — NOT YET GRADED" state; options **[Try grading again] [Wait — try later]**; while waiting the student MAY PROCEED TO REVIEW study/test (forced-flow change; day completion still requires the graded+passed new test); on attempting day advance while blocked → PROMPT "your earlier test hasn't been graded — grade it now?" (re-grade from the job); + session-start pickup for cross-device/graded-but-unwritten recovery. Today's UX (verified TypedTest.jsx): auto-retry w/ transient-vs-deterministic classification + Retry-Save (write-only, never re-bills grading) + reload guidance — the "later/review-first/advance-prompt" surfaces are NEW build items (ride DF2-12). |
| R2-11 | **ENGAGEMENT RETIRED as a forced-mode completion criterion (David 2026-07-26: "gate ON → the score threshold subsumes it (unanswered counts as wrong); gate OFF → policy permits not caring about review").** Ripples: the engaged-paired-review completion reader legs (foundation:784-800, :624-625 zero-question quirk) retire inside DF2-10's already-rewritten reader set; the surviving non-engagement fpHoldCsd hold leg (foundation:1342-1347/:1462-1491) dies; **r44-H3's `review_not_engaged` holdReason is REVERSED (superseded)** — H4's vocabulary shrinks; messaging row 3 RETIRED; the before-the-fact engagement warn (MCQ:1592) is replaced by the generic unanswered-questions submit confirm (which stays — the accident guard); A1(vi) force-pass×engagement corner DISSOLVED; the PR-2 engagement stamp keeps being written as inert data until DF2-46 retires it. Honest residuals accepted — and further discounted (David 2026-07-26): in FREE-mode classes both residuals are structurally void (no day to burn; review is always-on and redoable); in FORCED classes a blank/lazy review costs only DEFERRAL, not loss — the unreviewed words stay in the pool and the rotation serves them again within ~pool/queueSize days, so the harm is one day's graduation progress on a class whose teacher opted out of review accountability anyway. Guard = the generic unanswered-questions confirm + CS recovery; the FREE-mode "what counts as a recorded review for the scheduler" question is UNTOUCHED (DF2-42d keeps it). |
| R2-10 | Challenge-reversal: David leans "whichever breaks least"; WSL recommendation = **UPDATE labels on accepted challenge** (today's code already stamps status PASSED on accept — foundation:2642-2647 — so NOT updating labels would create a novel three-way inconsistency; pool impact ≈ nil because day queues are pinned). **Confirm-pending: David's yes after A2's certified answer on pinned-pool immunity.** |

## 2. TRACK A — Code-truth verifications (answers David's questions)

| ID | Task | Method |
|---|---|---|
| A1 🔍6 | **Force-pass mutation-set + consumer audit (Q1)**: (i) read `overrideAttempt` end-to-end — exactly which attempt fields it mutates today (score? passed? answers untouched?), what override metadata it already stamps; (ii) enumerate EVERY reader of attempt data beyond `passed` (gradebook, credibility/retention fields, engagement stamp, review pairing, analytics, the validity probe, CS scripts) and classify each: does an override-passed attempt with unchanged per-word answers mislead it?; (iii) spec the "edited by teacher" read-only label (or confirm the existing metadata suffices) so no reader treats the attempt as organic/impossible; (iv) verify the invariant David asked about: **no functionality derives a grade independently of the attempt record** (session-local score uses at graduation time are the known exception — enumerate them); (v) decide + spec: force-pass does NOT retroactively graduate that day's words (they stay in the pool) — state it explicitly; **(vi) ⟶ DISSOLVED (R2-11 — engagement retired). [A1 2026-08-01 rationale CORRECTED: the engagement reader DOES exist in today's live code (getReviewForDayServer FIX-1 leg, foundation.js:794-799 — a non-engaged override-passed review is skipped by pairing until the build); the dissolution holds because DF2-10(2) REMOVES those legs in the same train that ships force-pass — never because no reader exists.]** *(historical text:) ENGAGEMENT × force-pass: an override-passed review that was NON-ENGAGED (<80% answered) may still fail the paired-ENGAGED-review completion reader (foundation:784-800) — the force-pass spec must define whether the override also satisfies/bypasses the engagement reader or requires an engaged attempt to exist; (vii) SCOPE [David 2026-07-26: BOTH test types]: force-pass covers review AND new-word attempts, behind a **fail-closed target RESOLVER** (David's framing: identify the one proper attempt to promote; everything else is inert by non-selection — nothing is deleted): server computes the EXPECTED anchor shape from the student's current csd/twi + open-day identity (class+list+day), selects the LATEST failed attempt matching it, refuses with a typed reason when none matches (null/partial anchor fields, stale-tab ranges, progression-already-moved → already_completed, wrong-class context) → refused cases route to CS. Reuses the anchor-validator coherence checks; promote-one-touch-nothing-else; an overridden-passed anchor must not trip `csd_anchor_invalid` (fixture); **(viii) CLIENT CONVERGENCE [David 2026-07-26 — "kill" = route the live browser to the proper session]: baseline invariant (certified) = every exit from the retake wall passes through server truth (retake-submit / re-entry both hit completeSession → `already_completed` → client discards the stale local session and routes forward — a stuck-forever state is structurally impossible); UX layer = the wall screen holds a single-doc live listener on its own attempt so a teacher override dissolves the wall in real time (rides DF2-10's retake-UX build); **(ix) OVERRIDE DURABILITY [David 2026-07-26 — stale state must never rewrite the forced pass]: fixtures = original-submission idempotent RETRY after override → server returns the existing doc, grade fields untouched (**CODE-VERIFIED 2026-07-26: index.js:474-478 existing-doc branch is `return normalizeExistingAttempt(existing)` — zero write ops; :539 fast path same**; fixture still ships) · stale retake-submit → NEW doc only, refused by the day guard (**[A1 2026-08-01] current code resolves this as `day_guard_rejected`, not `already_completed` — fixture asserts the day-guard refusal + no progress write**) · stale session autosave cannot demote the completed day (session_state = scratch, not truth) · override-then-real-pass → ONE advance + ZERO graduation [r54 — an override mints no proof, no graduation; the 02 DF2-10(7) law; client graduation runs NOTHING on `already_completed`] · "latest attempt" display surfaces respect day-status precedence + the teacher-edited label** |
| A2 🔍6 | **Pinned-pool immunity certification (Q10)**: prove/refute "a word-status change mid-day does not alter an in-progress review session." Verify: day-queue pinning (`segment.wordIds`), where status is re-read mid-session (the F01 still-retired filter at TEST build; `resolveSegmentWords` at graduation), dual-tab/second-class writes, crash-restore recompute-on-loss path (DSF:712-718). Deliverable: a certified statement WITH its boundary conditions — David gets "my word" only after this converges. **⟶ r55 STATE: conditions (i) ✅ (10_ §2.0 normative) · (ii) ✅ (R2-43) · (iii) ✅ (H6 §6b transactional guard) · (iv) OPEN — B1 must replay challenge adjudications at their adjudication timestamps under the R2-43 resting law (the r55 B1-v4 work); R2-10 activates only when (iv) closes.** |
| A3 🔍6 | **New-word test shortfall investigation (Concern 3)**: (i) was "fewer new words" normal intervention behavior — the interventionLevel scaling chain (`dailyPace × (1−interventionLevel)`, db.js:3061 + the 5-site allocation math) incl. LEGACY FRACTIONAL floats persisted from the pre-binary era; (ii) end-of-list behavior when ~10-20 words remain (allocation = remainder — expected); (iii) **retake-pool shrinkage**: David's scenario — 100 new words, test draws 30, student fails 3× — does the 4th test draw from only 10? Trace `selectTestWords` + queue tracking (`updateQueueTracking`, studyService:811-827) + new-test retake flow for an already-tested-words exclusion. Read-only; findings become fix-cards only after convergence. |

## 3. TRACK B — Backfill pipeline (R2-9; every stage 🔍6)

B1 **Expected-result calculation**: plan → script (per-student predicted distributions of ALL FOUR labels — `reviewFailCount`/`reviewLastFailedAt`/`reviewLastCorrectAt`/`reviewLastProvenAt` — from attempts history under the R2-35 recompute-at-92 law + the r48 impossible-record EXCLUSION filter w/ published counts; adjudication-aware per R2-10; resetEpoch-scoped) [FF1-01/FF3-09] → 🔍6 on plan+script → run → 🔍6 on results.
B2 **Database investigation**: plan → script (current `study_states` shapes/volumes/legacy keys/collisions, live-write patterns) → 🔍6 → run → 🔍6 on findings.
B3 **Backfill implementation script**: resumable, idempotent, delta-sweep at flip, pre-image backup, tamper-proof field set → 🔍6.
B4 **Execution**: backup → 25WT rehearsal (results vs B1 expected) → David go → 26SM run → post-verify vs B1 expected → 🔍6 on final results. Log: SUPPORT_RUNBOOK CS event + change log.

## 4. TRACK C — Plan + UI folds

C1 🔍6 **FULL plan update** (code + module/container design): fold into 02_TASK_LIST + addendum + the container architecture docs EVERY ledger decision — **R2-1..R2-47 (the COMPLETE record, incl. both question rounds + r46/r47/r48 contracts)** — original enumeration follows (historical): R2-1..R2-10, r45's surviving items (B1 queue-identity contract · B3 DF2-10/DF2-14 reconciliation · H5 staging gates (DF2-12/13, W3/D-3, DF2-44 rules clause, cached-client) · H7 `fillEligible` + reversal semantics · H8 simulator upgrade + SEEDED sensitivity runs at David's values · M9 evidence wording + testType stratification · M10 deterministic composition contract), the DISSOLVED items marked (B2 by R2-1, B4+dark-writes by R2-5/R2-9), the teacher force-pass card, the two new teacher levers + defaults, retake composition, Playwright-rigor requirement (R2-5), and the pre-set-flag choreography (R2-6). Every check performed as a first-and-only pre-implementation pass.
C2 🔍6 **UI-calibration audit**: every NEW/changed surface in the plan (retake wall, teacher force-pass + stuck-student row, review screens, messaging rows, container wireframe) audited against the EXISTING UI system — design tokens (`src/index.css`), `/src/components/ui/` inventory, live screen conventions — as a first-and-only pre-implementation pass.

## 5. Sequencing (reissued post-r49 — executed stages marked)

1. ✅ C1 fold + Track-A investigations scoped · 2. ✅ round-5 panel + Codex r46/r47/r48 (decision rounds complete;
ledger R2-1..R2-47) · 3. ✅ THE MECHANICAL FOLD (v5) · 4. ✅ round-6 verification (3-Fable panel + Codex r49,
simultaneous) · 5. ✅ THE CORRECTION FOLD (r49 packet + panel findings) · 6. ✅ round-7 BOUNDED convergence
(3-Fable Workflow panel + Codex r50, simultaneous — verdict: residue fold needed) · 6b. ✅ THE RESIDUE FOLD
(r50's 7 items + the panel's 27, deduped; batches RF-A..RF-D, 2026-07-26) · 7. the r51 CLOSURE verification →
the owner PRESENTATION · 8. Track A
execution + Track B design/scripts (🔍6) · 9. DF2-14 implementation authorization → dark build → 25WT product
rehearsal → backfill go → activation go (the r49-B4 split).


## 6. Values sheet — ALL DECIDED (tombstones; the ledger governs)

1. ~~reviewQueueSize default~~ **60** [R2-14] · 2. ~~reviewTestSize default~~ **30** [R2-14] ·
3. ~~slot rule~~ **UNCAPPED, least-recently-tested first** [R2-15/R2-26] · 4. ~~retake cap~~ dead (David's
retake model) · 5. ~~threshold posture~~ **ON@92 cohort-wide at the flip, teacher-tunable per class-list**
[R2-14/R2-26] · 6. ~~config artifact~~ **`system_config/review_v2`; activation per R2-31 (dark-deploy → one
audited flip); `enabled:false` = kill switch under the R2-32/r48 label law** · 7. ~~monitoring numbers~~ set with
Track-B1 baselines [R2-18; CS tickets = David's primary signal] · 8. ~~job retention~~ **12 hours logical expiry
(server `expiresAt`), async TTL physical backstop** [R2-35/r48] · 9. #10 challenge-reversal = DEFERRED GATE (A2).
