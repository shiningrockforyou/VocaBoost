# DEEPFIX 2 — Round-5 internal panel record (13_, 2026-07-26)

> Full findings of the round-5 convergence panel (3 Fable + 2 Opus, adversarial, read-only; Workflow run wf_02eb5596-ca1),
> filed to the repo so Codex r46 reviews the same material. Immediate WSL actions already taken: R2-24 demoted to
> unratified inference (since resolved by David question-rounds 1-2, ledger R2-26/R2-27); item-8 clause retracted.
> David subsequently answered the full question list (R2-26..R2-30) — Codex judges the findings AGAINST those answers.


## O2 — decision integrity / interpretation (Opus)
**Verdict:** R2-24's unified-model reading is unratified inference on its decisive legs; 12 questions + 9 ledger drifts must close before the C1 fold.

- **[BLOCKER] O2-01** — "Free-nav to ALL simultaneously" has two materially different readings and WSL silently picked the larger one. David answered a question about PILOT-CLASS SELECTION inside a per-class enablement ladder; his words equally support "the capability releases to all classes at once, teachers still opt in per class" (COEXISTENCE intact) as they do "every class is switched to free navigation at once" (COEXISTENCE dead).
  - *Evidence:* 12_R2_DISCUSSION_TRACE.md:60 (row 47) records David verbatim as "no pilot (simultaneous)"; the question it answers is 02_TASK_LIST.md:220 register item 10 "DF2-61: pilot class selection", whose card at 02_TASK_LIST.md:141 reads "25WT free class E2E → 🧭 pilot class → per-class enablement". 11_...md:42 R2-24(a) then generalizes to "free-nav ships to ALL students SIMULTANEOUSLY" and appends "likely SUPERSEDING the per-class navigationMode COEXISTENCE architecture" — an inference with no trace row behind it (12_:8-60, all 47 rows, never raise navigationMode).
  - *Correction:* Ask David one question: "(i) every class is flipped to free navigation at once, or (ii) free-nav is released to all classes at once with no pilot class, teachers still enabling it per class?" — (i) deletes DF2-52 (02_TASK_LIST.md:131), DF2-55 (:134), DF2-53's mixed-mode view (:132), and DF2-47's F1 leg (:116), and forces a ~907-student simultaneous mode flip; (ii) leaves the entire COEXISTENCE stack standing and deletes only DF2-61's pilot rung.
- **[BLOCKER] O2-02** — R2-24(c) ("no progression of days whatsoever unless the new-word AND review tests have passed") is recorded as binding, but (a) the plan still specifies the exact opposite for free classes, and (b) the review gate remains a teacher-editable per-assignment field, so any teacher can switch off the invariant David just declared unconditional.
  - *Evidence:* 11_...md:42 R2-24(c). Contradicted by 02_TASK_LIST.md:129 DF2-50: "wall is on the NEW-segment test only — review never gates in free mode, `reviewPassThreshold` inert for free classes [M2]", and by FREE_NAVIGATION_MODEL.md:248-249 binding requirement 4 "forced = the banked reviewPassThreshold gate; free = review informs, never gates (no review retake-wall)". Defeasibility: 11_...md:38 R2-14 "per-assignment fields override where teachers later edit" + 02_TASK_LIST.md:80 DF2-11 "review group = `reviewPassThreshold` (default 92) … as per-assignment overrides".
  - *Correction:* Ask: "does the review gate bind in EVERY class regardless of navigationMode, and may a teacher still set it off/0?" — binding-and-not-disableable requires deleting the OFF posture from DF2-11/R2-14 and rewriting DF2-50/M2 + FREE_NAV req 4; still-disableable means R2-24(c) is launch-posture prose, not an invariant, and must be reworded before any card cites it as one.
- **[HIGH] O2-03** — A previously David-locked governing architecture decision (COEXISTENCE, 2026-07-17) is being retired by WSL inference, and the plan has already begun acting on the inference before David has been asked.
  - *Evidence:* 00_ORIENTATION.md:54 lists "FREENAV direction | COEXISTENCE — per-class option, forced default | 2026-07-17 (B1)" among the CLOSED governing decisions, and :22-23 states program goal 3 as "Two modes, one system: `navigationMode: 'forced' | 'free'` per class". 11_...md:42 marks it "likely SUPERSEDING". Yet 02_TASK_LIST.md:218 already closes register item 8 with "Mixed-class view shape moot under the unified model" — a live DF2-53 deliverable (:132 "mixed forced+free class view") declared moot on the unratified reading.
  - *Correction:* Put the single question "do you want per-class `navigationMode` retired?" to David explicitly, and revert item 8's "moot under the unified model" clause to OPEN until he answers — a governing B1-class decision may only be reversed by an equally explicit statement.
- **[HIGH] O2-04** — Under the flip-everyone reading, a cohort-wide simultaneous mode flip is structurally incompatible with the plan's own boundary-only flip rule, and R2-24(a) inherits an all-at-once posture whose required compensating control (an instant kill switch) does not exist for free-nav.
  - *Evidence:* 02_TASK_LIST.md:116 DF2-47 requires "flip mappings BOTH directions … boundary-only flips, never mid-session [Opus-A R-I]"; :141 DF2-61 gates on "the DF2-47 flip ceremony (boundary-only, re-hydration step) + state-loss analysis doc". FREE_NAVIGATION_MODEL.md:123 records "1,273 live `session_states` (574 active in 4d) → mixed-bundle mid-day migration hazard". 11_...md:33 R2-5 makes "the global kill switch (instant revert)" a REQUIRED compensating control for all-at-once, but the only kill switch specified is `system_config/review_v2 {enabled:false}` (11_:38), which reverts the review redesign, not a deployed container/free-nav bundle.
  - *Correction:* Ask David (1) whether the flip may be staggered per student boundary rather than literally simultaneous, and (2) what the instant revert for the free-nav/container flip is; if the answer is "hosting redeploy", say so explicitly in R2-24(a) because R2-5's compensating-control set is then not satisfied.
- **[HIGH] O2-05** — Whether the G-DUE per-word scheduler survives is left completely undetermined by the reading, yet three cards and the whole Wave-5 entry gate depend on it. G-DUE existed only because a display-only csd freezes the day-indexed rotation; if R2-24 keeps days authoritative and 10_ §2.1's day-offset rotation selects review, DF2-42d/DF2-42 may be wholly unnecessary.
  - *Evidence:* FREE_NAVIGATION_MODEL.md:102-106: "the review scheduler *is* `currentStudyDay` … If csd goes display-only, free review returns the same slice forever. Free-nav needs a newly designed review scheduler". 10_REVIEW_GRADUATION_REDESIGN.md:23 defines the replacement selector as day-indexed: "start = ((studyDay−1) × queueSize) mod pool.length". Dependent surfaces: 02_TASK_LIST.md:71 (DF2-42d design), :120 (DF2-42 build), :121 (DF2-43 gates include "DF2-42's scheduler authority accepted"), :125 (Wave-5 entry gate "DF2-42d accepted").
  - *Correction:* Ask: "is review selection now the day-offset rotation for every class (G-DUE cancelled), or does a per-word due engine still ship alongside it?" — cancellation rescopes DF2-42d to a fairness/rotation spec, deletes DF2-42, and re-cuts DF2-43's and Wave-5's gates; retention requires stating which engine owns the daily queue when both exist.
- **[MEDIUM] O2-06** — If the day number is both the rotation index AND gated on passing both tests, a walled student's review queue never rotates — the pinned 60 words repeat indefinitely — so the addendum's "bounded sighting interval" property and free mode's "always-on review" both silently depend on the day advancing, which R2-24(c) forbids for exactly the students who most need rotation.
  - *Evidence:* 10_REVIEW_GRADUATION_REDESIGN.md:22-27 (queue selected by "the pure day-offset rotation" keyed on studyDay, "bounded sighting interval preserved"); 11_...md:42 R2-24(c) gates day advance on both tests; UNIFIED_SESSION_STATE_ARCHITECTURE.md:286 specifies free-mode review as an "always-on mode"; 11_...md:28 R2-1 makes the wall hard with no auto-exit.
  - *Correction:* Ask whether a walled (or deliberately non-advancing) student's review queue rotates; if it must, the rotation index cannot be the gated day number and 10_ §2.1 needs a separate monotonic review cursor — a change to the very mechanism R2-4/R2-15 were decided on.
- **[HIGH] O2-07** — R2-16's asymmetric stamping and R2-20's past-day re-test collide: a past-day retest stamps permanent failure flags on every missed word but can never stamp recovery, because `lastPassedAt` comes only from "the day's PASSING attempt" and a retest is explicitly non-advancing. The affordance David just granted is therefore strictly punitive and no student should rationally use it.
  - *Evidence:* 11_...md:40 R2-16: "`lastFailedAt` stamps per wrong answer on ANY graded attempt; `lastPassedAt` … stamps ONLY from the day's PASSING attempt"; 11_...md:49 R2-17 "BLANK = FAIL, on ANY graded attempt"; 11_...md:46 R2-20 grants re-TEST of past days as "`type:'retest'`, non-advancing, original pass stays the accountability score"; 11_...md:30 R2-2 sticky (b) makes ever-failed flags permanent absent direct proof.
  - *Correction:* Ask: "does a PASSING past-day retest stamp `lastPassedAt` (recovery), stamp nothing at all, or stamp only failures?" — recovery-yes reopens the laundering vector R2-16 exists to close (easy old days become recovery farms); stamp-nothing needs an explicit "retest attempts are label-inert" clause in 10_ §2.4 plus the M10 composition contract; the current text implies the punitive third option by accident.
- **[HIGH] O2-08** — R2-11's engagement-retirement rests on a residual discount whose main half R2-24 destroys: the discount claims free-mode residuals are "structurally void (no day to burn)", but under the unified model every class has gated days and a review wall.
  - *Evidence:* 11_...md:51 R2-11: "in FREE-mode classes both residuals are structurally void (no day to burn; review is always-on and redoable)" — recorded at trace exchange 7-8 (12_:16-17). 11_...md:42 R2-24(c), recorded 40 exchanges later (12_:60 row 47), makes day-advance gate on BOTH tests under one universal model.
  - *Correction:* Re-derive the engagement residual under the unified model before the C1 fold and show David the smaller discount: if every class has a gated day, only the forced-mode "deferral not loss" half survives, and the generic unanswered-questions confirm becomes the sole guard for a materially larger population.
- **[HIGH] O2-09** — The "AWAITING DAVID" list asks him to re-decide four values he has already decided, and carries a bracketed recommendation that directly contradicts his own binding decision.
  - *Evidence:* 11_...md:12-13 §0: "AWAITING DAVID … values sheet — ①②③⑤ + ⑥-approval only". But §6 item 1 (:84) and item 2 (:85) are fixed at 60/30 by R2-14 (:38); item 3 (:86) "Priority-slot rule: fixed fraction of test size [50%]" is dead under R2-15's UNCAPPED slots (:39); item 5 (:88) reads "`reviewPassThreshold` — stays a per-class teacher lever (default OFF). … [leave OFF]" while R2-14 (:38) reverses it to "STARTS ON with DEFAULT ON at 92% for every class at flip"; item 6 (:89) is answered by R2-14's `system_config/review_v2`.
  - *Correction:* Strike §6 items 1/2/3/5/6 and §0(i) entirely; the only genuinely open values are ⑦ (kill-switch doc values — itself contradicted, see O2-12) and ⑧ (monitoring thresholds, already deferred to B1 by R2-18).
- **[HIGH] O2-10** — R2-24(b)'s "One Day = one pace increment" has no stable definition, because `pace` is a teacher-editable per-assignment field: a mid-list pace change re-slices every past day's word range, so "nothing changes for pacing" is only true if past day boundaries are frozen — which nothing in the plan provides.
  - *Evidence:* src/components/AssignListModal.jsx:7 (`const [pace, setPace] = useState(20)`), :49 (pace passed to onAssign), :91 (bound input) — teacher-editable. 11_...md:42 R2-24(b) "One Day = one pace increment (80 words ⇒ Day 1 = words 1-80) … nothing changes for pacing". 02_TASK_LIST.md:116 DF2-47 F2 already owns exactly this problem: "stable segment identity under pace change; attempt-range→picker mapping". DF2-41 (:117) also has 129 divergent / 27 active twi records, so a twi-derived day label is not uniform across the cohort.
  - *Correction:* Ask: "when a teacher changes pace mid-list, do PAST day boundaries re-slice, or are past days frozen at their historical word ranges?" — re-slicing breaks R2-20's backward re-study/re-test targets and every attempt's `studyDay` label; freezing requires a new persisted per-day range record (today only the per-session `segment.wordIds` pin exists).
- **[MEDIUM] O2-11** — The ledger cites the wrong DF2-47 leg as superseded: it names F2, but F2 is the cycling/lap position problem that survives mode unification; the leg the unified model actually dissolves is F1 (same student+list in a forced class and a free class).
  - *Evidence:* 11_...md:42 lists "DF2-47's F2" among the superseded surfaces. 02_TASK_LIST.md:116 defines DF2-47 as "the design gate Codex F1 blocks on: same student+list in forced class A + free class B" and F2 as "physical free frontier vs forced cycling virtual position/lap (no naive `min(twi,N)`; stable segment identity under pace change; attempt-range→picker mapping)". Cycling remains a live parallel track keyed to F2: :147 and :160 "position model per DF2-47's F2 resolution".
  - *Correction:* Swap the citation (F1 dissolves, F2 survives) and state explicitly whether list cycling/laps survive the unified model — under the current text D7/P9 is orphaned, and 10_ §2.4's "per-cycle re-proof" language (sticky (b)) is ambiguous between the 21-day mastery cycle and a list lap.
- **[MEDIUM] O2-12** — Two different runtime-config documents are specified for the same gate, and their fail-directions conflict: one fails OPEN to gate-inactive on an unreadable doc, while the launch posture requires the gate ON — so a read failure silently disables the launch gate cohort-wide.
  - *Evidence:* 11_...md:38 R2-14: "ONE global doc `system_config/review_v2` {enabled:true, threshold:92, queueSize:60, testSize:30}". 02_TASK_LIST.md:79 DF2-10 H2: "a NEW server-read runtime-config document (e.g. `system_config/review_gate`) … missing/unreadable doc ⇒ gate-INACTIVE (fail-open-to-legacy) … default posture = gate-inactive". 11_...md:90 values item 7 still asks David for "`system_config/review_gate`: active=false at deploy".
  - *Correction:* Name one document and one field vocabulary in one place, and re-decide the fail-direction now that ON is the launch posture — fail-open was chosen when the gate defaulted OFF and is no longer the conservative direction.
- **[MEDIUM] O2-13** — Items the ledger marks "pending David's nod" are recorded as CLOSED decisions elsewhere in the plan of record, so the plan is being built on unratified proposals presented as David's.
  - *Evidence:* 11_...md:40 R2-16 is headed "[proposed 2026-07-26, pending David's nod]", but 02_TASK_LIST.md:350 lists "asymmetric stamping + blank=fail" under "David round 2 … The redesign's product decisions CLOSED". 11_...md:37 R2-12(a) says "PROPOSED closure [pending David's nod]" while 11_...md:14 §0 asserts "answers-on-the-job = approved (R2-13)" and 02_TASK_LIST.md:81 cards DF2-12 as "DESIGN CHOSEN 2026-07-26"; the trace records only a WSL recommendation (12_:36 row 27 "answers-on-the-job recommended").
  - *Correction:* Reconcile the pending markers across 11_/10_/02_ and put one short explicit nod list to David: R2-15 ordering, R2-16 asymmetry, R2-12(a) job persistence, R2-10 reversal — nothing may be carded as "chosen" until that list returns.
- **[MEDIUM] O2-14** — Both the ledger and the addendum still assert "recovered words drop out per retake", a claim the trace records as superseded and which asymmetric stamping mechanically forbids.
  - *Evidence:* 11_...md:39 R2-15: "recovered words visibly drop out per retake"; 10_REVIEW_GRADUATION_REDESIGN.md:31-32 repeats it. 12_R2_DISCUSSION_TRACE.md:56 (row 45): "pending David's confirm of the failed-attempt nuance: correct-on-a-FAILED-attempt words STAY flagged/priority — supersedes the earlier 'drop out per retake' claim". Mechanically forced by 11_...md:40 R2-16 (`lastPassedAt` only from a PASSING attempt).
  - *Correction:* Delete the drop-out sentence from both documents — the student-visible truth is that a retake re-presents the same flagged set until the whole test passes, and the teacher/TA comms leg (a R2-14 launch blocker) must describe that, not the reverse.
- **[MEDIUM] O2-15** — R2-22 records as an unconditional close a decision David stated conditionally; his re-open trigger was dropped and is now carded nowhere.
  - *Evidence:* 12_R2_DISCUSSION_TRACE.md:58 (row 46) records David's answer verbatim as "9 probe moot unless stuck-despite-92%". 11_...md:48 R2-22 records "Starvation probe SKIPPED … BlindSpot hide is FINAL regardless … no decision hangs on the count. DF2-0P shelved"; 02_TASK_LIST.md:287-288 closes register item 16 as "SKIPPED" with no trigger.
  - *Correction:* Card the condition David actually stated — "students stuck despite ≥92%" — as a named day-one monitoring signal under R2-18 that re-opens DF2-0P; otherwise the only stated re-entry path to the probe is lost.
- **[MEDIUM] O2-16** — Two register items contradict each other on re-test gradebook semantics: item 11 records them as closed by David, item 7 still lists the identical semantics as an open 🧭 decision — and the trace shows the semantics were a WSL default, not David's words.
  - *Evidence:* 02_TASK_LIST.md:216-217 item 7 (OPEN): "DF2-51: re-test gradebook semantics (default: `type:'retest'`, non-advancing, original pass = accountability score)"; :221-224 item 11 "CLOSED (David, R2-20)" carrying the same clause. 11_...md:46 labels it "semantics default" (a WSL proposal), and 12_:58 row 46 records only "10 re-study+re-test granted w/ AI-call metering".
  - *Correction:* State plainly that David granted the affordance and that the gradebook semantics remain open (item 7), or get his ratification of the `type:'retest'`/non-advancing default — currently the plan asserts both.
- **[MEDIUM] O2-17** — WSL's reading fixes only forward/backward movement and leaves four first-order product questions unaddressed, each of which decides whether an entire card is a build item or is deleted.
  - *Evidence:* 11_...md:42 describes the model solely as "the CURRENT day structure + free BACKWARD navigation (re-study/re-test past days per R2-20) + hard pass-gated FORWARD movement". Unaddressed against UNIFIED_SESSION_STATE_ARCHITECTURE.md §12.2: (a) the day/segment picker's existence and whether it may preview FORWARD/unreached days (:508-509 specs it as "all segments ≤ frontier"); (b) whether the student chooses the ORDER of work within a day (:533 "THREE actions per reached segment — {re-study, re-test, review-due}"); (c) whether day advance stays automatic-on-pass or becomes student-initiated (:510 "offered, never forced", ARCH:284); (d) whether review exists outside the day's pinned queue (ARCH:286 "always-on").
  - *Correction:* Put (a)-(d) to David as four yes/no questions; the answers decide whether DF2-51 NavigateHub (02_TASK_LIST.md:130) is built at all or folds into DF2-30's SessionStage enum, and whether ARCH §12.2's 15-item UX delta is a design backlog or dead.
- **[HIGH] O2-18** — R2-24(a) as read mandates exactly what DF2-60 forbids, and the program's own exit criteria become unmeetable — neither surface was reconciled when register item 10 was closed.
  - *Evidence:* 02_TASK_LIST.md:140 DF2-60: "full-cohort single-step exposure is FORBIDDEN either way [Fable-2 H2 / Opus-A R-D]"; :141 DF2-61 still carries "🧭 pilot class" and "Pilot success criteria (defined BEFORE pilot): 4 weeks · … ≥60% of actives advance ≥1 frontier segment via a PASSED test"; :306 §5 program exit criteria still require "≥1 real class through the defined pilot success criteria in free mode (pass-to-advance) on due-based review"; :220 item 10 closes the pilot without touching any of them.
  - *Correction:* Ask David which governs — "all at once" or the standing no-full-cohort-single-step rule — then rewrite DF2-60, DF2-61 and §5 together; if all-at-once governs, §5's exit criteria must be replaced (there will be no pilot class to measure) and the R2-5 compensating-control set must be restated for the container train.
- **[MEDIUM] O2-19** — R2-12(a)/R2-13 fold a forced-mode flow reordering (review before the new-word grade lands) into a durability decision; the trace shows David asked about retry UX and localStorage, not about resequencing the day.
  - *Evidence:* 12_R2_DISCUSSION_TRACE.md:36 (row 27) records the exchange as "Retake/Try-later UX; localStorage duration; attempt-first worms?". 11_...md:37 R2-12(a) then records "David's UX adopted: 'Retake OR Try again later' (+ allow REVIEW-phase entry while new-test grading is pending — a deliberate forced-mode flow change …)", and 11_...md:50 R2-13(b) hardens it into a build item ("the student MAY PROCEED TO REVIEW study/test"), carded at 02_TASK_LIST.md:81.
  - *Correction:* Separate the two and ask explicitly whether the new-word→review ordering may be broken while grading is pending — it changes the day's shape for every student who hits a grader delay and interacts with 10_ §2.1's per-day queue pinning and R2-16's stamping order.
- **[LOW] O2-20** — R2-21's free-mode streak mapping is both moot under the unified model and self-contradictory: the proposal it carries is precisely the "mere activity" definition the decision rejects.
  - *Evidence:* 11_...md:47 R2-21: "a streak day = the student PROGRESSED that day (day completed/advanced), not mere activity" followed by "Free-mode mapping (what 'progressed' means without forced days) resolves at DF2-50/51 — proposal: any completed study session counts"; repeated at 02_TASK_LIST.md:228. Under 11_...md:42 R2-24 every class has days, so the "without forced days" premise no longer exists.
  - *Correction:* Delete the free-mode mapping clause from R2-21 and register item 13: a streak day = a day advanced, in every class; the DF2-34 weekend-skip fixture is unaffected.
- **[LOW] O2-21** — R2-6 and R2-14 describe two different pre-deploy fireadmin artifacts, and the R2-6 deploy-choreography argument ("pre-set flags are inert to old code") was written for the per-class flags that R2-14 replaced.
  - *Evidence:* 11_...md:34 R2-6: "David flips them on ALL classes BEFORE deploy via firebase-admin SDK — deploy choreography note: pre-set flags are inert to old code (additive unknown fields)". 11_...md:38 R2-14: "ONE global doc `system_config/review_v2` … David's fireadmin script writes it once; per-assignment fields override where teachers later edit".
  - *Correction:* State which artifact the pre-deploy script actually writes (one global doc, or one doc plus N per-class overrides) and re-verify the inert-to-old-code claim for that artifact — the §0 "no lever enableable mid-deploy" argument depends on it.

## O1 — live-cohort safety (Opus)
**Verdict:** Launch posture unsafe: ~80% of 26SM walls day one on slot-loaded 92% tests, force-pass never clears flags, kill switch launders labels.

- **[BLOCKER] O1-1** — Compound day-one wall rate is quantified NOWHERE, and the probe data says the accumulated posture walls ~80% of the cohort on the first slot-loaded review test.
  - *Evidence:* 10_REVIEW_GRADUATION_REDESIGN.md:60-66 declares §3 STALE and the re-sim only "queued"; the sole existing sim (evidence/trajectory-pf.json meta: {rFail:0.32, test:30, slots:15}) uses fixed-half-15 slots and a cohort-average fail rate, both superseded by R2-15 and by 10_ §1's own per-band stratification. Recomputed from evidence/graduation-validity-26SM.json (907 students, 341,253 rows): afterWrong accuracy by student band = <50%: 0.213 (132 students) / 50-70%: 0.503 (61) / 70-85%: 0.689 (80) / 85%+: 0.882 (582). functions/index.js:429-434 gives score = round(correct/totalQuestions*100) and passed = score>=passThreshold, so 92% on a 30-item test requires >=28/30 (27/30 = 90 fails). P(>=28/30) per band = 0.000 / 0.000 / 0.0015 / 0.294, so expected passers of a fully flagged test (exactly what R2-15's "up to all 30" produces) = 172 of 855, i.e. 683 students (79.8%) wall, 273 of them with near-certainty (P<=0.15%). No deepfix2 doc contains any wall-rate, force-pass-load or backfill-population figure (grep for '613' returns 0 hits across 00_/01_/02_/10_/11_/12_; 815 of 907 students have >=1 wrong review answer ever, mean 97.6 wrong rows, 259 with >50, 131 with >200).
  - *Correction:* Run the R2-values seeded sim (r45-H8) and publish per-band wall-rate, flagged-population and daily force-pass-load numbers as a hard gating artifact BEFORE DF2-14 carding, not as a post-flip monitoring signal.
- **[BLOCKER] O1-2** — The only relief mechanism does not clear the cause, so a walled student walls again every subsequent day - force-pass is a permanent daily manual toll, not a release.
  - *Evidence:* R2-16 (11_:40) and 10_ §2.4:41-45: lastPassedAt "stamps ONLY from the day's PASSING attempt ... force-pass stamps nothing"; R2-2 + 10_ §2.4:46-48 sticky (b) "excludes ever-failed-unrecovered words from fill permanently"; R2-15 (11_:39) refills each test with "as many flagged words as the day's queue offers (up to all 30)"; R2-1 (11_:28) "NO escape hatch - A failed retake never auto-completes the day." A student who cannot reach 92% can never stamp lastPassedAt, so the flag backlog is strictly non-decreasing and reloads tomorrow's test identically. Relief is a teacher clicking a per-student row (R2-1b, 11_:29) across 40 classes / 907 students (class roster in graduation-validity-26SM.json), repeated daily and indefinitely.
  - *Correction:* Give the design a bounded, self-clearing recovery path (e.g. force-pass stamps recovery for that day's presented words, or attempt-count/decay-based recovery) and state the expected steady-state force-pass volume per class before the flip.
- **[BLOCKER] O1-3** — Flipping the kill switch launders the entire backfilled sticky-label population: under OFF the writer auto-passes every review, and asymmetric stamping treats a passing attempt as proof.
  - *Evidence:* 02_TASK_LIST.md:79 D-4: "under OFF the writer auto-passes reviews (`passed:true`), so the uniform reader is satisfied"; R2-16 (11_:40) / 10_ §2.4:42-43: lastPassedAt stamps from "the day's PASSING attempt". An auto-passed OFF review IS the day's passing attempt by that definition, so every presented flagged word (uncapped slots => up to 30/student/day per R2-15) recovers with zero proof. R2-16's own rationale rejects a mechanism that launders ~25% of flags per round; OFF launders 100%. Neither R2-14 (11_:38) nor 10_ §2.6:52-58 states whether the label WRITER is disabled by `enabled:false` - and the opposite reading (writer stops) leaves OFF-window wrong answers unrecorded with no re-enable delta backfill defined (11_ §3 B3 defines a delta-sweep only "at flip").
  - *Correction:* Specify in R2-14/10_ §2.6 that `enabled:false` disables the label writer (no lastPassedAt from auto-passed reviews) and define the re-enable delta backfill covering the OFF window, so the kill switch is state-preserving rather than state-destroying.
- **[HIGH] O1-4** — There are two kill-switch specs with different doc names and field vocabularies, and the frozen fail-OPEN direction now silently disables the gate cohort-wide instead of protecting it.
  - *Evidence:* DF2-10's FROZEN PRE-CODE CONTRACT (02_TASK_LIST.md:79, r43-H2/r44-M7) specifies `system_config/review_gate` with `{active, version, thresholdSource, readStatus}`, "missing/unreadable doc => gate-INACTIVE (fail-open-to-legacy)", "default posture = gate-inactive". R2-14 (11_:38) and 10_ §2.6:52-55 specify `system_config/review_v2` with `{enabled, threshold, queueSize, testSize}`. 11_ §6 item 7 (line 90) STILL solicits values for "`system_config/review_gate`: active=false at deploy", contradicting R2-14's enabled:true pre-set in the same file. Under ON-everywhere, a transient read failure fails open to auto-pass for 907 students, and 02:79 makes the stamped attempt posture IMMUTABLE ("completion interprets existing evidence from the attempt's STAMPED posture"), so those unearned passes become permanent evidence.
  - *Correction:* Collapse to ONE config doc and field set in the C1 fold, delete 11_ §6 item 7, and re-adjudicate the read-failure direction (fail to last-known posture, or fail-closed to a hold) now that ON is the default rather than gate-inactive.
- **[HIGH] O1-5** — The two compensating controls David ordered for the no-canary flip - 25WT rehearsal and "rigorous Playwright" - are unspecified, and 25WT is structurally incapable of rehearsing a 907-student mass-wall event.
  - *Evidence:* R2-5 (11_:33) makes them REQUIRED, but no card defines either. SUPPORT_RUNBOOK.md:9 defines 25WT as "audit sandbox" and :49 records validation done "via audit student in 25WT sandbox, cleaned up"; the 26SM probe roster shows the 25WT DUP classes holding 1-4 students each (13 total). C2 (11_ §72) is a design-token/UI-calibration audit, not functional E2E. The card that actually owns the E2E matrix, DF2-34 (02:106), still reads "canary/staged rollout (1 class -> N) w/ defined regression signal + rollback | 25WT -> staged" - its rollout half is dissolved by R2-5/R2-24 with no replacement regression signal authored.
  - *Correction:* Author a 25WT rehearsal spec (seeded flagged-label population reproducing 26SM's band mix, expected wall rates, explicit pass/fail criteria) and re-derive DF2-34's regression signal and Playwright scope for a no-canary flip, both as DF2-14 carding gates.
- **[HIGH] O1-6** — Monitoring for the highest-risk deploy in the program has no numbers, no card and no abort rule - and the cohort-wide flip destroys the only baseline the existing success criteria depended on.
  - *Evidence:* R2-18 (11_:44) sets the signal set "at WSL discretion" with "numbers set with B1 baselines", and B1 has not run (11_ §3). DF2-10 has a 7-day soak with named abort signals and a rollback pin (02:79), but DF2-14 has no row at all - 11_ §5 item 5 still lists "DF2-14 carding" as future work, so the riskier deploy inherits none of DF2-10's abort machinery. The only defined rollout success criteria in the repo are DF2-61's (02:141) - "pilot-class CS tickets <= its own forced baseline / >=60% of actives advance >=1 frontier segment via a PASSED test / no rollback triggered" - voided by R2-24(a) (02:220), which explicitly defers the replacement ("panel re-derives the ladder"); a simultaneous flip also removes the comparison class those criteria required.
  - *Correction:* Create the DF2-14 card with pre-flip numeric abort thresholds (wall rate, force-pass volume, label-write failure rate, score-drop vs B1 expected), a named owner and a rollback rule, and gate the flip on them rather than on CS tickets that only arrive after students are already blocked.
- **[HIGH] O1-7** — The score denominator is client-supplied; the gate turns it into a trivially exploitable pass lever on day one, and no plan doc names it.
  - *Evidence:* functions/index.js:429-434: `const totalQuestions = ctx.totalQuestions ?? attemptAnswers.length;` ... `scoreFraction = correctCount / totalQuestions` ... `passed = ctx.sessionType === "review" ? true : score >= passThreshold`. Today reviews auto-pass so the denominator is inert; from the flip it decides the 92% gate for 907 students. A client reporting totalQuestions=2 after answering only 2 known words scores 100 and, via R2-16's asymmetric stamping, also earns lastPassedAt recovery for those words. grep across all deepfix2 docs finds `totalQuestions` only in R2-13's writeContext list (11_:50), which PERSISTS the client value onto the grading job rather than deriving it; r45-M10 (codex_deepfix2_r45.md:206-221) and R2-17's presented-set requirement cover composition membership, not the denominator.
  - *Correction:* Derive totalQuestions server-side from the pinned day-queue/presented set inside the same M10 validation, and add an under-reported-denominator fixture to DF2-10's required tests.
- **[MEDIUM] O1-8** — r45-B4 (mixed posture over shared student+list state) is recorded as DISSOLVED, but DF2-11 reinstates exactly that condition at launch for 197 multi-class students.
  - *Evidence:* 11_:71 lists "B4+dark-writes by R2-5/R2-9" as DISSOLVED; but R2-14 (11_:38) retains "per-assignment fields override where teachers later edit" and DF2-11 (02:80) ships those overrides live from day one ("Ships WITH launch - the gate is ON at 92 from day one, so the lever edits a LIVE value"). r45:113-127 establishes that the behavioural state lives at users/{uid}/study_states/{wordId} and is student+list-wide, so a per-class threshold cannot be scoped to a class. graduation-validity-26SM.json shows 197 of 907 students enrolled in >=2 classes (170 in 2, 24 in 3, 3 in 4-5). A teacher lowering one class to e.g. 70 makes attempts there "passing", stamping lastPassedAt and clearing sticky flags consumed by the same student's 92% class.
  - *Correction:* Publish the deterministic effective-posture resolver for shared student+list state (r45-B4's actual ask) or forbid per-assignment threshold/size overrides for multi-class students until that resolver ships.
- **[MEDIUM] O1-9** — A previously-identified BLOCKER - "weak-stratum retake deadlock" - had its mitigation deleted while the posture got harsher, and was never re-adjudicated.
  - *Evidence:* 02_TASK_LIST.md:236-243 records the round-4 internal panel verdict "not buildable as-is / NO-GO as scoped" with 5 blockers including "weak-stratum retake deadlock", mitigated in addendum v2 by the escape valve `review_retake_exhausted`. R2-1 (11_:28) dissolves it outright ("r45-B2's exhausted-day disposition is DISSOLVED") while R2-14/R2-15/R2-17/R2-2 simultaneously add gate-ON-everywhere, uncapped flagged slots, blank=fail and stickiness. No document re-tests the deadlock finding at the resulting posture, and 12_ (the 47-row per-exchange trace) contains no exchange revisiting it.
  - *Correction:* Re-run the weak-stratum deadlock analysis at the R2 values and record the result adjacent to R2-1, since R2-1 removed the mitigation the original blocker was closed with.
- **[MEDIUM] O1-10** — The plan implies a 26SM write with no David-authorization gate (the flip-time delta sweep), and the flip itself has no row in the authorization matrix.
  - *Evidence:* 11_ §3 B3 requires the backfill script to include a "delta-sweep at flip"; B4's gate chain (11_:67) is "backup -> 25WT rehearsal -> David go -> 26SM run -> post-verify" - the go covers the 26SM RUN, while the delta sweep executes later, at deploy time, against live data. 00_ORIENTATION.md:65: "26SM ... read-only diagnosis; NO writes without explicit authorization." The task-scoped authorization matrix (02:294) lists under Gated on DAVID only "DF2-06 / DF2-0P / DF2-02a dead-lever branch / DF2-10 pin-move (item 15) / Wave-3 production exposure (DECIDE-0)" - DF2-14, the card that performs the cohort-wide flip, appears nowhere in it.
  - *Correction:* Add an explicit David-authorization gate for the flip-time delta sweep and a DF2-14 row to the task-scoped authorization matrix before any Track B execution.
- **[MEDIUM] O1-11** — Activation == deploy eliminates the last pre-exposure verification window, and DF2-10's OFF-parity falsifier can no longer be exercised in production at all.
  - *Evidence:* R2-6 (11_:34): "activation happens at the deploy itself; ... verified post-deploy"; 10_ §5:83-84: "config pre-set; activation = the deploy; kill switch = revert". DF2-10's cert requires "OFF byte-identical for the GATE + D-2 legs" (02:79), and that card's own R2 supersession block concedes the x3 posture fixtures "now certify the KILL-SWITCH path, not the launch state" - so the falsifier becomes fixture-only. With canary (R2-5), pilot (R2-24a) and staged rollout all dissolved, the first production execution of the gate, label writer, composition validator and graduation formula is simultaneously the first exposure of all 907 students.
  - *Correction:* Deploy with enabled:false, run the production parity/verification probe against live traffic, then flip the config doc as a separate David-authorized step - this preserves R2-5's all-at-once semantics while restoring a pre-exposure verification window.
- **[MEDIUM] O1-12** — Register item 15 - the OFF-posture graduation basis, i.e. what the kill switch actually reverts - is still OPEN, and its recommended answer references a mechanism R2-14 abolished.
  - *Evidence:* 02_TASK_LIST.md:284-286: "Sub-question (OFF-posture scoping): under gate-OFF every review auto-passes (D-4) - does the new basis apply there too ... or does OFF keep legacy graduation (RECOMMENDED - preserves the OFF-byte-identity falsifier; the new basis activates per-class with the gate)?" - "per-class with the gate" is dead under R2-14's one global doc. The item is marked "BLOCKS: DF2-10's pin-move (graduation leg) + DF2-42d mastery transitions". Nothing in 10_/11_ specifies what a mid-day flip does to a pinned queue or a partly-graduated day; the stamped-posture rule at 02:79 covers only the attempt pass verdict, not the graduation basis or the label writer.
  - *Correction:* Close register item 15 in the one-doc frame and specify mid-day kill-switch semantics for the graduation basis and the label writer, not just the pass verdict.
- **[MEDIUM] O1-13** — The retake wall silently switches test modality (and grading path) at attempt 4 - live behaviour no R2 decision or plan section accounts for, and which the AI-metering rationale ignores.
  - *Evidence:* src/services/sessionService.js:374-386 `getReviewTestType(attemptCount, teacherTestMode)`: returns 'typed' for attemptCount < 3, else 'mcq' - comment: "First 3 attempts use written, then MCQ to save API costs"; applied to review tests at src/pages/DailySessionFlow.jsx:1198-1199. Under R2-1's unbounded retakes the wall's difficulty profile and grading path change mid-wall while the 92% bar stays uniform across modalities, and 10_ §1:15-16 already withdraws the "chance floor" claim "until" testType stratification lands (r45-M9). R2-20 (11_:46) conditions past-day re-testing on metering "as the metric for a FUTURE global AI-grading limit gate" without citing this pre-existing 3-attempt damper as the only present-day bound on grading spend.
  - *Correction:* Name getReviewTestType in the plan, decide explicitly whether the 3-attempt modality switch survives the redesign, and stratify the 92% threshold evidence by testType before the flip.
- **[LOW] O1-14** — The plan's own values sheet and program exit criteria still assert the superseded launch posture, so the flip has more than one authoritative statement.
  - *Evidence:* 11_ §6 (lines 86-90) still solicits item 3 "Priority-slot rule: fixed fraction of test size [50%]" (superseded by R2-15's uncapped slots), item 4 retake composition cap (declared dead at 11_ §0:8), item 5 "`reviewPassThreshold` - stays a per-class teacher lever (default OFF) ... [leave OFF]" (reversed by R2-14 in §1 of the same file), and item 7's `system_config/review_gate` doc (superseded by review_v2); 11_ §0:12 still lists item 5 as "AWAITING DAVID". 02_TASK_LIST.md:306 program exit criteria still require ">=1 real class through the defined pilot success criteria in free mode", void per R2-24(a) at 02:220.
  - *Correction:* In the C1 fold, strike 11_ §6 items 3/4/5/7 and §0's item 5, and rewrite the 02 §5 exit-criteria pilot clause, so the launch posture is stated exactly once.

## F3 — cross-doc ecosystem (Fable)
**Verdict:** C1 ecosystem fold incomplete: R2-11/14/21/22/24/25 contradicted or unmarked across all six governing docs; no rule ranks the ledger above ★.

- **[HIGH] F3-01** — ARCH §10 still presents per-class COEXISTENCE (`navigationMode:'forced'|'free'`, csd as display-only `ceil(twi/segment)`, review always-on with no held/refused exit states, per-class dark enablement) with zero R2-24 supersession/ripple marker — the exact architecture R2-24 flags as likely superseded by the ONE universal model.
  - *Evidence:* /app/docs/plans/UNIFIED_SESSION_STATE_ARCHITECTURE.md:268-333 (coexistence premise :271-272; mode table :279-286 'derived label… display-only', 'no held/refused'; co-implementation directive :326-333) vs /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:42 (R2-24: day-unit pacing, both-tests day-advance, 'likely SUPERSEDING the per-class navigationMode COEXISTENCE architecture').
  - *Correction:* Add a §10 header banner: '⚠ R2-24 (2026-07-26, ledger 11_ §1) redefines the model — one universal day-structured model, free BACKWARD nav only, both-tests forward gate, no per-class mode; this section is under supersession review pending the round-5 interpretation check.'
- **[HIGH] F3-02** — FREE_NAVIGATION_MODEL's own protocol ('LAYERED DOC — the LAST decision governs') makes GATE CLOSED COEXISTENCE (2026-07-17) the governing layer because no R2-24 layer was ever appended — a reader following the doc's stated rule gets the superseded architecture as binding.
  - *Evidence:* /app/docs/design/FREE_NAVIGATION_MODEL.md:3 ('the LAST decision governs'), :5-10 (2026-07-24 banner asserting COEXISTENCE governs), :195-213 (GATE CLOSED layer, last decision layer in file); no 2026-07-26/R2 text anywhere in the file; vs 11_:42 (R2-24) and 12_:60 (row 47: R2-24 recorded, panel check launched).
  - *Correction:* Append a dated R2-24 top layer/banner: 'R2-24 (2026-07-26) supersedes the COEXISTENCE layer pending the round-5 interpretation check — one universal model; picker-never-forced and free-review-never-gates layers no longer govern.'
- **[HIGH] F3-03** — The 'review never gates in free mode' contract survives un-annotated in three docs, directly contradicting R2-24(c) ('no progression of days whatsoever unless the new-word AND review tests have passed') under the unified reading.
  - *Evidence:* /app/docs/design/FREE_NAVIGATION_MODEL.md:247-249 (binding req 4: 'free = review informs, never gates (no review retake-wall)'); /app/docs/plans/UNIFIED_SESSION_STATE_ARCHITECTURE.md:558 (row 15: 'the retake-wall message is free mode's ONLY blocking message'); /app/docs/plans/deepfix2/02_TASK_LIST.md:129 (DF2-50: 'review never gates in free mode, `reviewPassThreshold` inert for free classes [M2]') vs 11_:42 R2-24(c).
  - *Correction:* Stamp all three sites: 'CONTRADICTED BY R2-24(c) under the unified model — review test gates day advance for everyone; ambiguity flagged to David per the round-5 check.'
- **[HIGH] F3-04** — MAP (the 3×-audited ground truth) §10 glossary says free-mode pass-to-advance is OPEN while its own §12 says CLOSED YES 2026-07-25 — an internal contradiction — and §12's free-mode spec (day = display-only label, exit set with no held/refused/retake) carries no R2-24 marker.
  - *Evidence:* /app/docs/design/UNIFIED_SESSION_STATE_MAP.md:229 ('G-POLICY: pass-to-advance OPEN') vs :243 ('✅ CLOSED YES, David 2026-07-25'); :241-243 (§12: 'day = ceil(twi/segment) display-only… Exit set shrinks to recorded/frontier_advanced/already — no held/refused') vs 11_:42 R2-24(b)/(c).
  - *Correction:* Fix :229 to 'CLOSED YES 2026-07-25' and banner §12: 'FORWARD-LOOKING — superseded in part by R2-24 (day-unit pacing retained; both-tests forward gate); pending round-5 verification.'
- **[HIGH] F3-05** — Messaging row 3 (non-engaged review) is retired by R2-11 but still listed as live/ship-scope in four places — copy and a surface would be authored for a dead mechanic.
  - *Evidence:* /app/docs/plans/UNIFIED_SESSION_STATE_ARCHITECTURE.md:545 (row 3 live) and :561 ('New messages needed: #3-#5…'); /app/docs/plans/deepfix2/00_ORIENTATION.md:24-25 ('rows 3-14 + 16 = DF2-32'); /app/docs/plans/deepfix2/01_SOURCES.md:83-84 ('rows 3-16'); /app/docs/plans/deepfix2/02_TASK_LIST.md:104 (DF2-32 'rows 3-14') vs 11_:51 (R2-11: 'messaging row 3 RETIRED').
  - *Correction:* Strike row 3 in all four with 'RETIRED 2026-07-26 [R2-11 — engagement retired; the generic unanswered-questions confirm stays]' and change the needed-set to '#4-#5, #10-#11, #13-#16'.
- **[HIGH] F3-06** — Orientation's governing decisions table still records the review-pass gate as 'default-OFF lever' with 'open = register item 15' — reversed by R2-14 (gate STARTS ON at 92% cohort-wide via system_config/review_v2) and item 15 closed by the R2 round.
  - *Evidence:* /app/docs/plans/deepfix2/00_ORIENTATION.md:61 (§3 row) and :46-47 (§2 banked Item A framing) vs 11_:38 (R2-14) and /app/docs/plans/deepfix2/02_TASK_LIST.md:244-248 (item-15 ROUND-2 CLOSURE).
  - *Correction:* Rewrite the §3 row: 'Review-pass gate — STARTS ON at 92% for every class at flip via `system_config/review_v2` (R2-14 supersedes default-OFF); design closed by R2-1..R2-17; residual opens = R2-15 ordering nod + R2-10.'
- **[HIGH] F3-07** — Orientation goal 3 and §3's FREENAV row present per-class COEXISTENCE with 'offered-never-forced segments' as the governing direction, unflagged against R2-24's unified-model redefinition.
  - *Evidence:* /app/docs/plans/deepfix2/00_ORIENTATION.md:20-23 ('Two modes, one system… per class… offered-never-forced') and :54 ('FREENAV direction | COEXISTENCE — per-class option… 2026-07-17 (B1)') vs 11_:42 (R2-24, ripple-flagged as the panel's primary check).
  - *Correction:* Annotate both: '⚠ under supersession review per R2-24 (2026-07-26): one universal model, free BACKWARD nav + both-tests forward gate, shipped to ALL simultaneously — panel verifying.'
- **[HIGH] F3-08** — DF2-10's card head still specifies 'reviewPassThreshold per-class default-OFF' and an H4 vocabulary containing `review_not_engaged` bound to row 3, which its own tail R2-supersession block reverses — R2-14's promised 'DF2-10/11 language rewritten in the C1 fold' was only appended, not applied, leaving one build card asserting both postures.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:79 (head: 'pass-1 core (`reviewPassThreshold` per-class default-OFF…'; H4: '`review_not_engaged` added [r44 H3]… messaging row 3 binds to it'; pass-2 set includes `getDayReviewForEngagement`/fpHoldCsd legs) vs same line's tail 'R2 SUPERSESSIONS' block and 11_:38/:51.
  - *Correction:* Rewrite the card head to the R2-14/R2-11 posture (ON@92, engagement legs retired, `review_not_engaged` dropped) and leave the tail block as the change record, so the card reads one way.
- **[HIGH] F3-09** — The ecosystem's stated conflict-resolution rules point AWAY from the R2 ledger: 01_SOURCES says ★ sources win within their domain and orientation §5 says the audited source (MAP) wins — but ARCH/FNM/MAP are all ★/audited and all carry superseded coexistence content; no doc grants 11_ §1 supremacy over them (only 10_ and DF2-10 subordinate themselves to the ledger).
  - *Evidence:* /app/docs/plans/deepfix2/01_SOURCES.md:4 ('★ wins within its domain') and /app/docs/plans/deepfix2/00_ORIENTATION.md:89-90 ('the audited source wins') vs /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:24 (§1 'binding') and /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:3-4 (ledger-wins clause scoped to the addendum only).
  - *Correction:* Add one global precedence line to 01_SOURCES and orientation §5: 'The David decision ledger (deepfix2/11_ §1) outranks every ★/audited source where they conflict; ★/audited-wins applies only to code-truth claims, not decisions.'
- **[HIGH] F3-10** — Exposure-model contradiction left standing: DF2-60 declares 'full-cohort single-step exposure is FORBIDDEN either way' (and DF2-34 mandates 1-class→N canary), while R2-5/R2-14/R2-24(a) mandate cohort-wide single-step activation (gate ON at 92 for every class at flip; free-nav to ALL simultaneously) and R2-25 re-affirms the DF2-60 canary — the plan never states how a staged container canary coexists with the decided all-at-once flips on one train.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:140 (DF2-60 FORBIDDEN clause), :106 (DF2-34 canary) vs 11_:34 (R2-5 all-at-once), :38 (R2-14 ON at flip for every class), :42 (R2-24a no pilot, simultaneous), :43 (R2-25 canary re-homes to DF2-60).
  - *Correction:* Add an explicit reconciliation note (and flag to David): e.g. 'canary applies to CONTAINER rendering only (byte-identical forced UI); the gate/free-nav semantic flips are cohort-wide by decision — or the FORBIDDEN clause is superseded'; one sentence must win.
- **[MEDIUM] F3-11** — ARCH §12.1's supersession banner — added as the fix — is itself now stale: it says pre-Wave-6 exposure is 'the OPEN DECIDE-0 decision', and the layer below still stages per-class enablement, both superseded by R2-25 (single-train closed) and R2-24(a) (all-at-once, no per-class ladder).
  - *Evidence:* /app/docs/plans/UNIFIED_SESSION_STATE_ARCHITECTURE.md:493-496 (banner 'OPEN DECIDE-0') and :503-505 ('dark — no class has navigationMode:'free'' / 'Per-class enablement') vs 11_:43 (R2-25) and :42 (R2-24a).
  - *Correction:* Update the banner: 'DECIDE-0 CLOSED (b) SINGLE-TRAIN (R2-25, 2026-07-26); activation = cohort-wide flip per R2-24(a)/R2-5 with the DF2-60 canary question flagged (F3-10).'
- **[MEDIUM] F3-12** — Orientation's status header and ship-model rows lag the record: status says three rounds run (r41-r43) with Wave-1 waiting on 'the bounded r44 + register item 15', and goal 5/§3 show DECIDE-0 as 🧭 OPEN — r44/r45 have run, item 15 closed, DECIDE-0 closed (b), and round-5 is launched.
  - *Evidence:* /app/docs/plans/deepfix2/00_ORIENTATION.md:7-10 (status), :27-31 (goal 5 'DECIDE-0'), :57 (§3 'Ship model… 🧭 OPEN') vs /app/docs/plans/loop/codex_reviews/codex_deepfix2_r45.md:1-4, 11_:43 (R2-25), 12_:60 (round-5 launched).
  - *Correction:* Refresh the status line (rounds through r45 + the R2 decision round; next = round-5 3F+2O+Codex r46) and mark the ship-model rows 'CLOSED (b) SINGLE-TRAIN (R2-25)'.
- **[MEDIUM] F3-13** — ARCH §12.2 items 5-6 still design the 'Day N becomes a label' reframe and a new 'frontier-vs-expected' teacher pacing metric — reversed by R2-24(b) ('pacing = the existing dailyPace… teacher pacing surfaces read Day numbers exactly as today — nothing changes for pacing'; register item 8 closed).
  - *Evidence:* /app/docs/plans/UNIFIED_SESSION_STATE_ARCHITECTURE.md:513-516 (items 5-6) vs 11_:42 (R2-24b) and /app/docs/plans/deepfix2/02_TASK_LIST.md:218 (item 8 closure).
  - *Correction:* Annotate items 5-6: 'SUPERSEDED by R2-24(b) — Day stays the pacing unit and teacher surfaces are unchanged; mixed-mode metric question moot under the unified model (panel verifies).'
- **[MEDIUM] F3-14** — ARCH §12.2 item 10 instructs 'Streaks — keep as-is (activity-based, mode-agnostic)' — contradicted by R2-21 (streak = PROGRESSION-based, weekend-safe; 'supersedes the any-activity default').
  - *Evidence:* /app/docs/plans/UNIFIED_SESSION_STATE_ARCHITECTURE.md:520 vs 11_:47 (R2-21) and /app/docs/plans/deepfix2/02_TASK_LIST.md:226-228 (item 13 closure).
  - *Correction:* Replace with: 'Streaks — PROGRESSION-based per R2-21 (day completed/advanced counts; weekends never break); free-mode mapping resolves at DF2-50/51.'
- **[MEDIUM] F3-15** — The teacher-lever surface is described stale in four places: 'two FUTURE levers' with reviewPassThreshold default 'off' and no reviewQueueSize/reviewTestSize rows, dead min/max still 'fix-or-remove' — R2-4/R2-14/R2-15 add two levers with defaults 60/30, set threshold default 92 (ON), decide DELETE for min/max, and R2-24 puts the navigationMode lever itself in question.
  - *Evidence:* /app/docs/plans/UNIFIED_SESSION_STATE_ARCHITECTURE.md:384 ('two FUTURE levers'), :427-428 (lever table 'off'), :433-435 (§11.6-2 'Fix-or-remove'); /app/docs/design/UNIFIED_SESSION_STATE_MAP.md:195-196 (FUTURE rows); /app/docs/plans/deepfix2/01_SOURCES.md:61-63 ('gains navigationMode + reviewPassThreshold') vs 11_:32 (R2-4), :38 (R2-14), :39 (R2-15 menu simplification), :42 (R2-24).
  - *Correction:* Annotate all four: 'lever set per R2-14/15 = reviewPassThreshold(92, ON) + reviewQueueSize(60) + reviewTestSize(30) over `system_config/review_v2`; min/max DELETED (item 3 closed); navigationMode lever pending R2-24 resolution.'
- **[MEDIUM] F3-16** — FNM's GATE CLOSED layer (and its reading-order banner) still asserts the David-locked BINARY throttle as forced-mode policy with no D-1 marker — the throttle was removed entirely on 2026-07-26 (annotated in 01_SOURCES and ARCH §12.3 but never in FNM).
  - *Evidence:* /app/docs/design/FREE_NAVIGATION_MODEL.md:7-9 (banner: 'forced mode keeps the… binary throttle') and :201-203 ('The David-locked BINARY throttle… is its policy') vs /app/docs/plans/deepfix2/02_TASK_LIST.md:179-182 (D-1) and /app/docs/plans/deepfix2/01_SOURCES.md:27 (FORCED_PATHWAY superseded row).
  - *Correction:* Add to both spots: 'throttle SUPERSEDED by D-1 (2026-07-26) — removed entirely; the review-pass gate is the sole struggling-student mechanism.'
- **[MEDIUM] F3-17** — Two runtime-config doc names are live in the plan of record: DF2-10 H2 and the 11_ §6 values sheet spec `system_config/review_gate` with 'default posture = gate-inactive / active=false at deploy', while R2-14 and addendum v3 decided ONE doc `system_config/review_v2` {enabled:true…} pre-set before deploy — and H2's missing-doc⇒gate-INACTIVE fail direction is never restated for the launch-ON world.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:79 (H2 'e.g. `system_config/review_gate`… default posture = gate-inactive') and /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:90 (§6 item 7 'active=false at deploy') vs 11_:38 (R2-14 `system_config/review_v2` {enabled:true, threshold:92…}) and /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:52-56.
  - *Correction:* Normalize every cite to `system_config/review_v2` and add one sentence deciding missing/unreadable-doc behavior under the ON-launch (fail-open-to-legacy is now a silent cohort-wide gate-off — state whether that is still intended).
- **[MEDIUM] F3-18** — 11_'s own §6 values sheet and §0 awaiting-list were not reconciled after R2-14/R2-15: §6 item 5 still asks default-OFF-vs-preset ('[leave OFF]'), item 3 still proposes a fixed-50% slot fraction, item 7 still specs active=false, and §0 still lists ①②③⑤ as awaiting although queue 60/test 30/ON@92/uncapped slots are recorded three rows up.
  - *Evidence:* /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:12 (§0 '(i) values sheet — ①②③⑤ + ⑥-approval only'), :84-91 (§6 items 1-3,5,7) vs :38 (R2-14) and :39 (R2-15).
  - *Correction:* Strike §6 items 1/2/3/5/7 with 'DECIDED [R2-14/R2-15]' tombstones and trim §0's awaiting list to what the trace's open-items row actually shows (R2-15 ordering nod, R2-10).
- **[MEDIUM] F3-19** — 'Recovered words visibly drop out per retake' survives in R2-15 and addendum §2.2, but trace row 45 records that R2-15/16 were decided pending David's confirm of the OPPOSITE nuance (correct-on-a-FAILED-attempt words STAY flagged — under asymmetric stamping nothing recovers on a failed retake), and that pending confirm is absent from the trace's own open-items list.
  - *Evidence:* /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:39 (R2-15 'recovered words visibly drop out per retake') and /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:31-32 vs /app/docs/plans/deepfix2/12_R2_DISCUSSION_TRACE.md:56 (row 45 'supersedes the earlier drop-out-per-retake claim… pending David's confirm') and :64 (open items omit it).
  - *Correction:* Reword both sites to 'recovered words (from a prior PASSING attempt/accepted challenge) drop out; correct answers on a FAILED retake stay flagged [row-45 nuance, confirm pending]' and add the confirm to 12_'s open items.
- **[MEDIUM] F3-20** — R2-22 residue: the register closure claims 'DF2-0P shelved (card marked)' but the DF2-0P card bears no shelved marker and DF2-01 still recommends probe-first pending a register-16 green-light; likewise 'BlindSpot… survival undecided' persists in three docs though R2-22 makes the hide FINAL.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:65 (DF2-0P card unmarked), :62 (DF2-01 'probe-first is the RECOMMENDED order if item 16 closes promptly… if David green-lights (register item 16)'), :287-288 (closure claiming 'card marked'); /app/docs/plans/UNIFIED_SESSION_STATE_ARCHITECTURE.md:345, /app/docs/design/UNIFIED_SESSION_STATE_MAP.md:247, /app/docs/plans/deepfix2/00_ORIENTATION.md:60 ('survival undecided') vs 11_:48 (R2-22 'hide is FINAL… probe SKIPPED').
  - *Correction:* Mark DF2-0P 'SHELVED [R2-22]', strike DF2-01's probe-first sentences, and change the three BlindSpot cites to 'hide FINAL [R2-22]'.
- **[MEDIUM] F3-21** — R2-24(a)'s no-pilot decision is annotated only in the register: the DF2-61 card still reads '🧭 pilot class → per-class enablement' with 4-week pilot success criteria, §5 exit criteria still require '≥1 real class through the defined pilot success criteria', and the R2-24-affected cards (DF2-47 F2 frontier-vs-day, DF2-51/52/53/55) carry no ripple flag for the panel's primary check.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:141 (DF2-61 card), :305-306 (§5 pilot criterion), :116/:130-134 (unflagged cards) vs :220 (item 10 closure 'panel re-derives the ladder') and 11_:42 (R2-24a + the enumerated superseded surfaces).
  - *Correction:* Stamp DF2-61 and §5 'RESCOPED by R2-24(a): 25WT rehearsal → cohort-wide flip + monitoring; pilot criteria re-derived', and add a one-line '⚠ R2-24 interpretation pending' flag to DF2-47/51/52/53/55.
- **[MEDIUM] F3-22** — MASTER's banked Item A text ('0/unset = OFF = byte-identical to today', '⏸ BANKED', 'Awaiting David: 2 decisions (throttle-day gate y/n; retake-surface N) + go-to-build') carries no supersession pointer while 02 §0 grants deepfix1 sources 'original gate text governs' precedence — the un-annotated stale posture is a live mis-direction under that rule.
  - *Evidence:* /app/docs/plans/MASTER_TASK_LIST.md:173-174, :183, :188 vs /app/docs/plans/deepfix2/02_TASK_LIST.md:9-10 ('original gate text governs… the source wins') and 11_:38 (R2-14), 02:179-186 (D-1 dissolving the throttle-day question).
  - *Correction:* Add the same inline supersession treatment FORCED_PATHWAY received: 'Item A ACTIVATED as DF2-10 — posture superseded (R2-14 ON@92; throttle-day question dissolved by D-1; retake surface resolved on the card).'
- **[MEDIUM] F3-23** — 01_SOURCES omits the authoritative decision ledger and trace entirely (no rows for 11_/12_), describes 10_ by its superseded v1 content ('item-15 resolution candidate: pace-matched review queue'), and its Codex-loop rows stop at r43 ('+ r44 when raised') though r45 is filed and r46 queued.
  - *Evidence:* /app/docs/plans/deepfix2/01_SOURCES.md (sections A/C contain no 11_/12_ row), :14 (stale 10_ description), :37-38 (loop rows) vs /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:24, /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:1-7 (v3), /app/docs/plans/loop/codex_reviews/codex_deepfix2_r45.md:1.
  - *Correction:* Add ★ rows for 11_ (binding decision ledger) and 12_ (trace), rewrite the 10_ row to the v3 spec (teacher-set 60/30, ON@92, asymmetric labels), and extend the loop rows through r45/r46.
- **[MEDIUM] F3-24** — Codex r45's required fold item 8 ('replace the stale v1 item-15 synopsis') is unexecuted: register item 15's lead still describes the superseded v1 mechanics ('pace-matched daily review queue (rotation cursor)… failCount-DESC test-priority slots + failed-first retakes') ahead of the closure prose.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:232-236 vs /app/docs/plans/loop/codex_reviews/codex_deepfix2_r45.md:249 (required fold #8) and 11_:39 (R2-15 superseding both mechanics).
  - *Correction:* Replace the item-15 lead sentence with the v3 synopsis (teacher-set 60/30, uncapped least-recently-tested slots, asymmetric stamping) and keep the v1 text only inside the archived decision record.
- **[LOW] F3-25** — Cohort-size drift: the BINDING constraints in orientation, MASTER's goal, and item 15's sub-question all say ~824 students while the addendum's live probe counts 907 (and the panel brief says ~907).
  - *Evidence:* /app/docs/plans/deepfix2/00_ORIENTATION.md:65, /app/docs/plans/MASTER_TASK_LIST.md:23, /app/docs/plans/deepfix2/02_TASK_LIST.md:284 ('all 824') vs /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:11 ('26SM, 907 students').
  - *Correction:* Update the three cites to '~907 (per the CS-2026-07-26 probe)' or annotate 824 as the 07-18 snapshot.
- **[LOW] F3-26** — Two small residues: MAP §7 still says the banked reviewPassThreshold lever 'lands on the 3 pass-expression lines' (superseded by DF2-08's module boundary — expressions land in the module, never inline), and register item 7 still lists re-test gradebook semantics as open although R2-20/item-11 closed exactly that default.
  - *Evidence:* /app/docs/design/UNIFIED_SESSION_STATE_MAP.md:170 vs /app/docs/plans/deepfix2/02_TASK_LIST.md:78-79 (DF2-08 boundary + DF2-10 deviation note); 02:216-217 (item 7) vs :221-224 (item 11 closure) and 11_:46 (R2-20).
  - *Correction:* Annotate MAP §7 'lands in the DF2-08 module per the DF2-10 deviation note', and trim item 7 to the genuinely open half (hub layout/nudge strength).

## F2 — fresh defect hunt (Fable)
**Verdict:** Append-only R2 fold: DF2-10 cert/config and 10_ §2 composition contracts contradict the binding decisions; not buildable as written.

- **[BLOCKER] F2-01** — DF2-10's certification framework still certifies the default-OFF posture while the card now launches gate ON@92 cohort-wide, and the ON@92 launch (plus DF2-11 menu, force-pass UI, DF2-12's review-first flow change) is not enrolled in §0's TOTAL approved-delta mechanism that says 'every visible ship must be enrolled'.
  - *Evidence:* 02_TASK_LIST.md:44-47 Wave-1 delta list = throttle removal + scoped D-2 basis only; :79 deploy cert = 'extended behavioral cert at CURRENT prod posture' incl. 'OFF byte-identical for the GATE + D-2 legs', soak abort = 'any review_retake_required misfire' (undefined when mass walls are EXPECTED at ON@92); the R2 supersession block redirects the x3 fixtures to 'the KILL-SWITCH path, not the launch state' but no ON-posture cert matrix, wall-rate baseline, or delta enrollment replaces the OFF-centric set.
  - *Correction:* Re-derive DF2-10's cert matrix, soak signals (expected wall-rate baseline per R2-18/B1, misfire discriminator) and §0's Wave-1 delta list for the launch-ON posture, enrolling ON@92, DF2-11, force-pass UI, and the review-first flow as named deltas.
- **[BLOCKER] F2-02** — Addendum v3 §2's test-composition contract is unimplementable for recovered-ever-failed words: slots take only FLAGGED words and the remainder/fill both use 'clean (never-failed-unrecovered)', so either fill graduates ever-failed words without proof (violating sticky (b)/R2-2) or recovered ever-failed words are never presented again and can never re-prove ('per-cycle re-proof' impossible).
  - *Evidence:* 10_REVIEW_GRADUATION_REDESIGN.md:28-33 (§2.2 slots = FLAGGED; 'remainder random from the queue's clean words'), :34-36 (§2.3 'fill only from clean (never-failed-unrecovered), UNPRESENTED'), :46-48 (§2.4 fillEligible 'excludes ever-failed-unrecovered words from fill permanently (per-cycle re-proof)') — the compound 'never-failed-unrecovered' breaks one of the two rules under either parse.
  - *Correction:* Define three strata explicitly — flagged / recovered-ever-failed / never-failed — with recovered-ever-failed words eligible for the random remainder (presentation) but permanently excluded from fill (graduation), replacing the ambiguous 'clean' term.
- **[HIGH] F2-03** — DF2-10 carries two incompatible config contracts: the H2 block specifies system_config/review_gate, default gate-INACTIVE, missing-doc⇒inactive fail-open, shape {active,version,thresholdSource,readStatus}, while R2-14/the supersession block specify system_config/review_v2 {enabled,threshold,queueSize,testSize} pre-set enabled:true BEFORE deploy — and 11_'s values sheet still contradicts the binding R2-14 on both.
  - *Evidence:* 02_TASK_LIST.md:79 H2 ('e.g. system_config/review_gate ... missing/unreadable doc ⇒ gate-INACTIVE ... default posture = gate-inactive') vs same row R2 SUPERSESSIONS + 11_:38 R2-14 (review_v2, ON@92, pre-set); 11_:88 item 5 ('reviewPassThreshold — stays ... default OFF ... [leave OFF]') and 11_:90 item 7 ('system_config/review_gate: active=false at deploy') contradict R2-14; the fail-OPEN direction was chosen for an OFF-default gate and is never re-decided for a launch-ON gate where a config read error silently un-gates 907 students.
  - *Correction:* Rewrite H2 to the single review_v2 contract (one doc name, field names, three-layer precedence global→per-assignment→kill, an explicit fail-direction decision for the ON posture) and strike/annotate values-sheet items 5 and 7.
- **[HIGH] F2-04** — 10_ v3 §2.2's 'recovered words drop out visibly per retake' is impossible under its own §2.4 asymmetric stamping (recovery stamps only from the day's PASSING attempt, which ends the retake loop) and was explicitly superseded by trace row 45, whose pending-confirm nuance is missing from both the ledger text and 10_ §6's open list.
  - *Evidence:* 10_:32 ('recovered words drop out visibly per retake') vs 10_:43 ('lastPassedAt stamps ONLY from the day's PASSING attempt'); 12_R2_DISCUSSION_TRACE.md:56 row 45 ('correct-on-a-FAILED-attempt words STAY flagged/priority — supersedes the earlier "drop out per retake" claim', pending David's confirm); 11_:39 R2-15 still carries the superseded sentence; 10_:88-91 §6 omits the row-45 confirm.
  - *Correction:* Strike the drop-out-per-retake sentence from R2-15 and §2.2, state that within-day retakes keep prior flags, and add the row-45 failed-attempt-nuance confirm to §6's open items.
- **[HIGH] F2-05** — r45-B1 (queue identity) is claimed handled but the v3 text restates the rejected 'pure by day' contract — rotation start is mod pool.length over a mutating ACTIVE pool, 'pinned via the existing segment.wordIds mechanism' which r45 documented as client-built and not authoritatively persisted, with no identity record, no ordering/tie-break, and no snapshot of the now-teacher-editable reviewQueueSize — which also falsifies R2-22's probe-skip premise that 'rotation = bounded intervals by construction'.
  - *Evidence:* 10_:22-27 (§2.1 'start = ((studyDay−1) × queueSize) mod pool.length ... pinned per day via the existing segment.wordIds mechanism; re-derivable by every surface + the server's B1 deriveNoScoreEligibility') and 10_:85-86 (§5 'B1 queue identity via §2.1's pure derivation') vs codex_deepfix2_r45.md:39-69 (B1 BLOCKER: choose a server-created immutable day-queue record or immutable as-of inputs + transactional reject; stable ordering/tie-breaks required); 02:287-288 (R2-22 skip rationale); 02:80 (teacher-editable reviewQueueSize mid-day changes the derivation).
  - *Correction:* Adopt one of r45-B1's two identity options in 10_ §2.1 — including pool hash/resetEpoch/config-value snapshot and stable ordering/tie-break — before DF2-14 carding, and re-examine R2-22's bounded-interval premise against the mutating-pool rotation.
- **[HIGH] F2-06** — Blank=fail and slot-composition enforcement rest on a client-supplied presented-set while r45-M10's verification contract is unfrozen (no exact-set vs membership rule, no seeded/pinned randomness, no attempt-time snapshot, no label-drift rule), so a client can omit presented words and dodge blank=fail stamps — reopening the strategic-blanking vector R2-17 exists to close.
  - *Evidence:* 11_:49 R2-17 ('the submission carries the PRESENTED word set'); 10_:71-73 (§4 'validated server-side against the derived queue/labels [r45-M10]') vs r45:206-220 (M10's freeze list) — none of the four M10 items appear in 10_ v3 and §6's open list (10_:88-91) omits them; the random remainder is server-underivable without a seed, so the server cannot reconstruct what was presented.
  - *Correction:* Freeze M10's deterministic composition contract in 10_ §4 (server-pinned or seeded composition, exact priority-set verification, attempt-time queue/label snapshot, compose-to-submit label-drift rule) as a DF2-14 precondition.
- **[HIGH] F2-07** — DF2-14 still has no card, wave, deploy surface, cert, or rollback posture (r45-B3's requirement), and its stated staging gate on the DF2-44 rules clause places it behind Wave 4's 47→41→40→46 chain, making 'after DF2-10 settles' and 'the all-at-once flip at DF2-14's deploy' indeterminate — and the cached-client version-negotiation gate (r45-H5) has no design anywhere.
  - *Evidence:* 10_:79-86 (§5: DF2-12/13 + 'DF2-44 rules clause' + 'cached-client behavior' as gates; flip at DF2-14's deploy) vs 02:109 (Wave-4 execution order puts DF2-44 after migration/unification) and no DF2-14 row in any 02 wave table; r45:95-110 (B3: 'DF2-14 needs its own row, wave/dependencies, approved-delta entry, deploy surface, cert, soak, and rollback posture'); 12_:65 admits 'C1 residue (DF2-14 card authoring)'; r45:140-143 cached-client negotiation unanswered.
  - *Correction:* Author the DF2-14 card with an explicit wave position, a decision on whether the label-rules clause ships as a named pre-lineage artifact or DF2-14 truly waits for Wave 4, and a concrete cached-client version-negotiation design given the R2-5 all-at-once flip.
- **[HIGH] F2-08** — The DF2-10→DF2-14 interim regime — gate ON@92 for every class over TODAY's pool/5 slicer and random-30 tests with minimal D-2 (fail ⇒ zero graduation, pass ⇒ legacy formula) — has no dynamics/workload analysis anywhere, while the launch comms are scoped to 'new review mechanics' that only arrive at DF2-14.
  - *Evidence:* 10_:81-83 (DF2-10 ships ON@92; queue/test/slots/labels legs deferred to DF2-14); 10_:60-66 (§3 re-sim covers only the redesign values 'before DF2-14 carding'); 10_:11-14 (<50% band afterWrong 21.3% — repeated-wall exposure without priority composition or labels); 10_:56-58 (launch blockers' comms describe the wall + 'new review mechanics').
  - *Correction:* Add an interim-regime analysis (expected wall/repeat-fail rates at 92% on legacy random composition per band, zero-graduation pool effects) as a DF2-10 launch gate and scope the launch comms to what actually ships at DF2-10.
- **[HIGH] F2-09** — R2-24's unified model and the all-at-once decisions directly contradict standing plan text that remains binding on its face: DF2-50's 'review never gates in free mode', DF2-60's 'full-cohort single-step exposure is FORBIDDEN either way', DF2-61's pilot ladder, and the §5 exit criterion requiring a pilot class.
  - *Evidence:* 02:129 (DF2-50 'review never gates in free mode, reviewPassThreshold inert for free classes') vs 11_:42 R2-24(c) (day-advance gates on BOTH tests); 02:140 (DF2-60 forbidden rule + canary) and 02:106 (DF2-34 canary) vs R2-24(a) simultaneous-ALL + 11_:33 R2-5 (no staged/per-class canary); 02:141 (DF2-61 still '🧭 pilot class'/pilot criteria) and 02:305-306 ('≥1 real class through the defined pilot success criteria') vs item 10 closure 02:220; 00_ORIENTATION.md:54 still records COEXISTENCE as closed.
  - *Correction:* Disposition each collision explicitly in the round-5 fold — either David revokes the forbidden-rule/canary/pilot doctrine on the record or compensating controls replace them — and rewrite DF2-50/60/61 and §5 exit criteria to whichever survives.
- **[HIGH] F2-10** — R2-20's binding CONDITION — every AI-grading call metered per-student and globally at the grading job — has no owning card: the register points to DF2-30/51 (hosting/UI cards containing no metering text) while DF2-12, the only card that edits the grading_jobs schema at the named counting point, does not carry it.
  - *Evidence:* 02:221-224 (item 11: 'CONDITION: every AI-grading call METERED ... scope lands in DF2-30/51'); 02:102 (DF2-30) and 02:130 (DF2-51) contain no metering scope; 11_:46 (R2-20: 'the grading job is the natural counting point'); 02:81 (DF2-12's job-schema enrichment list has no counter fields).
  - *Correction:* Home the metering build on DF2-12's job-schema change (per-student + global counters + the future limit-gate metric) and make its existence a ship gate for DF2-51's re-test grant.
- **[MEDIUM] F2-11** — The operative authorization instruments still contradict the R2 ledger: the §4 matrix gates DF2-10 on item 15, DF2-02a's dead-lever branch on item 3, and DF2-0P on item 16 (all closed/shelved), the D-2 rider and item 15's tail still say item 15 'BLOCKS this pin-move', 11_ §0's 'AWAITING DAVID' list still shows values ①②③⑤ open though R2-14/15 decided them, and orientation still says default-OFF / DECIDE-0 OPEN.
  - *Evidence:* 02:294 (matrix rows 'DF2-10 pin-move (item 15)', 'DF2-02a dead-lever branch (item 3, rides 15)', 'DF2-0P (item 16)') vs closures at 02:209-210/244-248/287-288; 02:79 ('🧭 BLOCKS this pin-move') and 02:286; 11_:12-13 (§0 awaiting ①②③⑤) vs 11_:38-39; 00_ORIENTATION.md:57 and :61 (DECIDE-0 OPEN; 'default-OFF lever').
  - *Correction:* Sweep the §4 matrix, DF2-10's D-2 rider, item 15's tail, 11_ §0, and orientation §3/status to the post-R2 state in the C1 fold before any Wave-1 build starts.
- **[MEDIUM] F2-12** — R2-11's engagement retirement is declared only in DF2-10's supersession block while the same card's H4 body still adds review_not_engaged, the row-3 binding, the review_recorded-reservation question and the engaged field, DF2-32 still authors row 3, and §5's exit criteria still require 'rows 3-16 live'.
  - *Evidence:* 02:79 H4 ('review_not_engaged added [r44 H3] ... messaging row 3 binds to it'; 'engaged' required) vs same row's R2 SUPERSESSIONS ('review_not_engaged DROPPED ... messaging row 3 dead'); 02:104 (DF2-32 'rows 3-14 ... row 3 forward-framed'); 02:304 ('rows 3-16 live — rows 1-2 retired').
  - *Correction:* Rewrite H4's status/holdReason vocabulary and the engaged-field disposition post-R2-11 in place, retire row 3 in DF2-32 and the exit criteria alongside rows 1-2.
- **[MEDIUM] F2-13** — The least-recently-tested ordering key has no tie-break although live 26SM data shows batch-identical lastTestedAt values across words, and the key is a client-written field whose integrity is neither rules-protected before DF2-44 nor covered by Track B's expected-result calculation.
  - *Evidence:* 11_:39 (R2-15 'uses the existing per-word lastTestedAt' — field exists, src/types/studyTypes.js:26, written client-side in the per-word batch at src/services/studyService.js:770-786); scripts/cs/backups_full_26sm_20260717-165840/study_states.json rows share identical millis (e.g. 1780648103326 on multiple words); 10_:28-33 specifies no tie-break (r45:66-69 demanded one for exactly this reason); 11_:64 (B1 computes only reviewFailCount/lastFailedAt/lastPassedAt).
  - *Correction:* Freeze a stable tie-break (e.g. lastTestedAt then wordIndex/wordId) in 10_ §2.2 and add lastTestedAt provenance/verification to B1's expected-result scope.
- **[MEDIUM] F2-14** — R2-6's pre-armed lever (enabled:true written BEFORE deploy) makes the multi-callable partial-deploy window behaviorally live — mixed gate posture across the 10-callable set mid-deploy — which §0's step-1 choreography exists to prevent, and the recorded rationale ('the lever-reading code and the lever arrive together') is false for a non-atomic per-callable rollout.
  - *Evidence:* 11_:34 (R2-6 choreography note) vs 02:36-38 (§0 steps: '(1) no lever/threshold enableable while the deploy is incomplete → (2) deploy the complete target set → (3) verify EVERY target ... retry/roll back any partial'); M11's attempt-time posture stamping (02:79) covers per-attempt coherence, not cross-callable skew (attempt stamped under new posture while completeSession still runs old code).
  - *Correction:* Deploy with enabled:false, verify the full set, then flip enabled:true via fireadmin (still one doc, still before client-lever exposure) — or explicitly accept and fixture the live mixed-posture window as an off-peak bounded risk on DF2-10's card.
- **[MEDIUM] F2-15** — DF2-11's 'the lever edits a LIVE value (no dark period)' is false for two of its three fields: reviewQueueSize/reviewTestSize do nothing until DF2-14 ships the composition legs, recreating for teachers the exact dead-lever class DF2-02a is deleting.
  - *Evidence:* 02:80 (DF2-11 'Ships WITH launch — the gate is ON at 92 from day one, so the lever edits a LIVE value (no dark period)') vs 10_:81-83 (queue/test sizing legs deferred to DF2-14, composition legacy in the interim); 02:63 (DF2-02a deletes reviewTestSizeMin/Max precisely for being never-wired).
  - *Correction:* Either defer the size-field UI to DF2-14, or ship it visibly labeled as taking effect at the redesign launch, and correct DF2-11's no-dark-period claim to threshold-only.
- **[MEDIUM] F2-16** — Sticky (b) over FULL-history backfill permanently fill-excludes every word ever answered wrong across ~341k review rows — plausibly a large fraction of every pool given per-band afterWrong rates of 21-88% — yet §2.4 calls the backfill 'historically benign' and §3's queued re-sim does not name the backfilled initial label state as an input.
  - *Evidence:* 10_:49-50 ('pre-gate reviews all auto-passed ⇒ historically benign' — addresses only lastPassedAt availability); 10_:46-48 (permanent fill exclusion); 10_:11-14 (341k rows; afterWrong 21.3-88.2%); 10_:60-66 (re-sim inputs listed as sizes/slots/gate/backfill flags and per-band transition rates, not the reconstructed label distribution).
  - *Correction:* Require Track-B1's predicted label distributions as an explicit input to the H8 re-sim and replace 'historically benign' with the quantified ever-failed/unrecovered mass before DF2-14 carding.
- **[LOW] F2-17** — DF2-10's force-pass pointer cites A1(v)-(ix) including the (vi) engagement corner R2-11 dissolved, and the resolver's matching rule is anchor-shape-based (a new-attempt concept) with no stated matching criteria for the review-attempt leg it must also cover.
  - *Evidence:* 02:79 ('[spec = 11_ A1(v)-(ix)]') vs 11_:51 ('A1(vi) force-pass×engagement corner DISSOLVED'); 11_:58 A1(vii) ('server computes the EXPECTED anchor shape from the student's current csd/twi' — review attempts carry no newWordEndIndex anchor).
  - *Correction:* Re-point the card to A1(v)+(vii)-(ix) and have A1 spec the review-leg selection criteria (class/list/day identity + current-open-day match) explicitly.
- **[LOW] F2-18** — The plan now contains two successive review-composition replacements — DF2-14's rotation (R2-22: 'replaces the orbit-prone slicer by construction') and Wave-4's DF2-42 G-DUE scheduler — with no statement of whether G-DUE supersedes the rotation, coexists, or is itself superseded by R2-24's unified model.
  - *Evidence:* 02:287-288 (R2-22 rationale) vs 02:120 (DF2-42 build 'retires the shadow-orbit failure mode the probe (DF2-0P) quantified' — probe skipped) and 02:71 (DF2-42d design inputs still cite the item-15-era projections and pace-matched framing).
  - *Correction:* Add a reconciliation note to DF2-42d stating the rotation→G-DUE succession (or coexistence rule) as a post-R2 design input.
- **[LOW] F2-19** — R2-21's progression-based streak is a redefinition of the live activity-based implementation (streak derives from lastStudyDate) but no card owns the basis change — DF2-34 only verifies weekend-skip.
  - *Evidence:* 11_:47 (R2-21: streak day = day completed/advanced, weekends never break) vs src/services/progressService.js:494-604 (calculateUpdatedStreak keyed on lastStudyDate/activity); 02:106 (DF2-34 'streak weekend-skip verify' only); item 13 closure (02:226-228) assigns only the free-mode mapping to DF2-50/51.
  - *Correction:* Card the forced-mode streak-basis change (owner, wave, delta-list enrollment since student-visible streaks recalc) rather than leaving it verification-only.

## F1 — fold fidelity (Fable)
**Verdict:** R2 core folded (DF2-10/11/12/35, addendum v3); authorization matrix, §0 delta list, Waves 3-6, entry docs still enforce pre-R2 law.

- **[BLOCKER] F1-01** — The §4 task-scoped authorization matrix — which by its own rule supersedes any wave-wide gate sentence — still gates DF2-10's pin-move on closed item 15, DF2-0P on closed item 16, DF2-02a's dead-lever branch on closed item 3, and Wave-3 exposure on closed DECIDE-0, so the plan's authorization authority misgoverns in both directions (blocks unblocked work; leaves DF2-10's real post-R2 gates — round-5 verdicts, A1/A2, R2-14 launch blockers — unstated, so a literal read makes the pin-move David-cleared).
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:294 'Gated on DAVID | DF2-06 (item 4) · DF2-0P (item 16) · DF2-02a dead-lever branch (item 3, rides 15) · DF2-10 pin-move (item 15) · Wave-3 production exposure (DECIDE-0)' vs closures at :209 (item 3), :244-248 (item 15), :287-288 (item 16), :197 (DECIDE-0); DF2-02a card :63 says 'branch UNBLOCKED — item 3 CLOSED'; /app/docs/plans/loop/codex_reviews/codex_deepfix2_r45.md:104 already required this exact matrix reconciliation.
  - *Correction:* Reissue the matrix post-R2: strike the four closed gates, move DF2-02a's branch to may-proceed, and state DF2-10's actual gates (round-5 convergence verdicts + A1/A2 completion + the R2-14 launch blockers).
- **[HIGH] F1-02** — R2-24's unified-model redefinition (one universal model; day-advance gates on BOTH tests; likely superseding COEXISTENCE) is folded only into register items 8/10 — no Wave-4/5/6 card carries a supersession or ambiguity marker, and DF2-50 still mandates the opposite behavior ('review never gates in free mode, reviewPassThreshold inert for free classes').
  - *Evidence:* /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:42 (R2-24, ripple-flagged as the panel's primary check) vs /app/docs/plans/deepfix2/02_TASK_LIST.md:129 (DF2-50), :116 (DF2-47 dual-mode contract unflagged), :131 (DF2-52 navigationMode lever), :132 (DF2-53); 'BOTH tests' appears nowhere in 02; /app/docs/plans/deepfix2/00_ORIENTATION.md:54 still records 'FREENAV direction: COEXISTENCE' unannotated.
  - *Correction:* Stamp an R2-24 supersession/interpretation-pending banner on DF2-47/50/51/52/53 (and FREE_NAVIGATION_MODEL) and fold R2-24(c)'s both-tests day-advance rule into the plan text.
- **[HIGH] F1-03** — The cohort-wide retake wall from launching the gate ON at 92% — the largest visible Wave-1 change — is not enrolled in §0's TOTAL-delta mechanism ('every visible ship must be enrolled'; the Wave-1 list still names only the throttle removal plus an item-15-conditioned D-2 basis change), and §0's lever choreography ('no lever/threshold enableable while the deploy is incomplete... only then expose the client lever') was never reconciled with R2-6's pre-set-flags-before-deploy mechanism.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:42-47 (delta lists; :46 conditions on now-closed item 15) and :35-38 (choreography) vs /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:34 (R2-6 reconciliation note), :38 (R2-14), :71 (C1 explicitly lists 'the pre-set-flag choreography (R2-6)' as a fold item).
  - *Correction:* Enroll the ON@92 gate (wall + threshold + config-doc read path) as a named Wave-1 delta and add R2-6's pre-set-inert-fields clause to §0's choreography.
- **[HIGH] F1-04** — The fold split David's launch into two deploys — 10_ §5 turns the gate ON at 92% at DF2-10 on TODAY'S review composition (old slicer, no priority slots, no labels, minimal D-2), with sizes/slots/labels/backfill arriving later at DF2-14 — while the ledger reads as one event (R2-14 'STARTS ON... at flip'; R2-9 backfill-then-launch), so the interim harsh-wall regime and a config doc whose enabled:true spans two deploys with different live field subsets were never recorded as David-approved.
  - *Evidence:* /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:79-86 (§5 staging split) vs /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:36 (R2-9 'Backfill-then-launch'), :38 (R2-14 'at flip'); 11_ §5 sequences Track B (backfill) after round-5 while DF2-10 is Wave-1 in 02.
  - *Correction:* Put the explicit question to David — gate-ON at DF2-10 (pre-backfill, legacy composition) versus only at DF2-14's all-at-once flip — and record the answer in the ledger and on both cards.
- **[HIGH] F1-05** — DF2-10's body still states the superseded law that its own trailing R2 block reverses — headline 'reviewPassThreshold per-class default-OFF', H2's config doc named 'system_config/review_gate' with 'default posture = gate-inactive', H4's holdReason vocabulary containing review_not_engaged with row-3 binding, and the D-2 rider's 'item 15 (BLOCKS this pin-move)' — and DF2-08 still reasons about 'DF2-10's default-OFF release'; r45-B3 already ruled DF2-10 must WAIT until this contradiction pattern is removed, and it persists at round 5.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:79 (all four superseded statements and the reversing R2 block coexist on the DF2-10 card), :78 (DF2-08 'NEVER smuggled into DF2-10's default-OFF release'); /app/docs/plans/loop/codex_reviews/codex_deepfix2_r45.md:100-110 (B3).
  - *Correction:* Rewrite DF2-10's body to the R2 posture (single config-doc name system_config/review_v2, ON@92 launch, engagement-free vocabulary, minimal-D-2 graduation), leaving superseded text only in marked historical notes.
- **[HIGH] F1-06** — Pilot machinery contradicting R2-24(a)/R2-25 survives unmarked: DF2-61 still specifies the ladder ('pilot class → per-class enablement' plus before-pilot success criteria), DF2-60 and item 1's retained framing still declare 'full-cohort single-step exposure is FORBIDDEN either way' (the exact posture David chose for both the review flip and free-nav), and §5's exit criteria still require one real class through the pilot criteria.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:141 (DF2-61 ladder + pilot success criteria), :140 (DF2-60 forbid), :203-204 (item-1 forbid), :305-306 (§5 pilot criterion) vs /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:42 (R2-24a no pilot, simultaneous), :43 (R2-25), :33 (R2-5 no staged canary); item 10 closure at 02:220 admits the ladder needs re-derivation but the cards are untouched.
  - *Correction:* Rescope DF2-61 to 25WT rehearsal then cohort-wide flip + monitoring, delete or mark the forbidden-exposure sentences on DF2-60/item 1, and rewrite the §5 pilot exit criterion.
- **[HIGH] F1-07** — DF2-14 — the delivery vehicle for most binding R2 decisions (teacher-set 60/30, uncapped slots, labels, graduation formula, full-history backfill, the all-at-once flip) — has no card, wave placement, approved-delta entry, deploy surface, cert/soak, or rollback posture anywhere in 02, though r45's required bounded fold demanded exactly that, so round-5 convergence was launched against a plan of record missing its centerpiece.
  - *Evidence:* grep DF2-14 in /app/docs/plans/deepfix2/02_TASK_LIST.md hits only prose pointers (:245-248, :288, :350); /app/docs/plans/loop/codex_reviews/codex_deepfix2_r45.md:109-110 ('DF2-14 needs its own row, wave/dependencies, approved-delta entry, deploy surface, cert, soak, and rollback posture'); /app/docs/plans/deepfix2/12_R2_DISCUSSION_TRACE.md:65 admits 'DF2-14 card authoring' as un-done C1 residue while row 47 (:60) launched the convergence.
  - *Correction:* Author the DF2-14 card (wave slot after DF2-10, gates = Tracks A/B + convergence verdicts, named delta entry, deploy set, cert/soak/rollback) before any build authorization is issued.
- **[MEDIUM] F1-08** — Messaging row 3 was retired by R2-11 but is still shipped by the plan: DF2-32 builds 'rows 3-14' with an explicit 'row 3 forward-framed' copy rule, §5's exit criteria require 'rows 3-16 live', DF2-31 still renders a 'non-engaged review... server-discriminated' case, and orientation goal 4 still lists rows 3-14.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:104 (DF2-32), :304 (§5), :103 (DF2-31 parenthetical) vs /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:51 (R2-11: 'messaging row 3 RETIRED; r44-H3's review_not_engaged holdReason is REVERSED'); /app/docs/plans/deepfix2/00_ORIENTATION.md:24-25.
  - *Correction:* Tombstone row 3 on DF2-32 (numbering-stable, like rows 1-2), change §5 to rows 4-16, and strip DF2-31's engagement parenthetical.
- **[MEDIUM] F1-09** — Addendum §2.2 still asserts 'recovered words drop out visibly per retake', which trace row 45 explicitly superseded (correct-on-a-FAILED-attempt words STAY flagged/priority) and which contradicts 10_'s own §2.4 asymmetric rule (recovery stamps only from a passing attempt, so nothing can drop out between same-day failed retakes); the pending failed-attempt-nuance confirm from row 45 is also absent from both open-item lists.
  - *Evidence:* /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:31 vs /app/docs/plans/deepfix2/12_R2_DISCUSSION_TRACE.md:56 (row 45 'supersedes the earlier drop-out-per-retake claim') and 10_:41-45 (§2.4); 10_:90-91 and 12_:64 list only the ordering nod + R2-10.
  - *Correction:* Replace the §2.2 sentence with the row-45 semantics (flags persist across a day's retakes; drop-out only after a passing attempt) and add the failed-attempt-nuance confirm to the open-items lists.
- **[MEDIUM] F1-10** — R2-5's compensating control for the no-canary rollout — 'very extensive, rigorous Playwright audits' — appears nowhere in 02: DF2-10's launch-blocker list omits the suite and the word Playwright has zero occurrences in the task list, though C1 names 'Playwright-rigor requirement (R2-5)' as a mandatory fold item.
  - *Evidence:* grep Playwright in /app/docs/plans/deepfix2/02_TASK_LIST.md = zero hits; 02:79 R2 block launch blockers = 'force-pass UI + teacher/TA comms + day-one monitoring' only; /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:33 (R2-5), :71 (C1); only /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:58 mentions the suite.
  - *Correction:* Add the Playwright rehearsal suite (scope, owner, pass bar) to the DF2-10/DF2-14 launch-blocker lists in 02.
- **[MEDIUM] F1-11** — R2-22's assertion 'DF2-0P shelved (card marked)' is false in the plan of record: the DF2-0P card remains an active not-started row gated on 'register item 16', DF2-01 still recommends probe-first pending item 16, and DF2-42 still claims the probe 'quantified' the failure mode it will never run.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:65 (DF2-0P unmarked, 'David go = register item 16'), :62 (DF2-01 'probe-first is the RECOMMENDED order if item 16 closes promptly'), :120 (DF2-42 'the probe (DF2-0P) quantified') vs /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:48 (R2-22) and item 16 closure at 02:287-288.
  - *Correction:* Mark the DF2-0P card SHELVED, strip DF2-01's probe-first recommendation, and reword DF2-42 (exposure unquantified; A3 gives partial visibility).
- **[MEDIUM] F1-12** — R2-19's decided grader intent (semantic equivalence; verbatim-English family accepted) is not folded: DF2-06 still instructs 'Confirm verbatim-vs-paraphrase grading intent with David at go' and register item 4 still lists the intent question as open, when only the deploy-go remains per the ledger.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:69 (DF2-06), :211 (item 4) vs /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:45 (R2-19: intent decided, 'Deploy go = at implementation time').
  - *Correction:* Record the decided intent on DF2-06 and item 4, leaving only the ask-at-implementation deploy-go.
- **[MEDIUM] F1-13** — DECIDE-0-open language survives in normative wave text despite R2-25: Wave 3's preamble still frames production exposure as an open decision with two live options, item 1's retained framing still ends 'RECOMMENDED: (a)' (the option David rejected), and Wave 5's gate line still reads 'live-or-DECIDE-0-dark'.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:94-98 (preamble with open-decision marker), :204-205 ('RECOMMENDED: (a)'), :125 (Wave-5 gates) vs the item-1 closure at :197 and /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:43 (R2-25 single-train).
  - *Correction:* Rewrite the Wave-3 preamble and Wave-5 gate line to the decided (b) single-train posture and mark the (a) recommendation historical.
- **[MEDIUM] F1-14** — 02's round record and status text predate the actual state: §8 contains no Codex r45 row, the header still says 'next = bounded r44 fold-verification', and the closing status says 'NEXT: round-4 convergence... then Codex r45... After that fold + David's item-15 pick' — all events that already happened, contradicting §8's own David-round-2 row which says next is the round-5 convergence with Codex r46.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:6-7 (header), :354-358 (status block) vs :350 (David round-2 row); /app/docs/plans/loop/codex_reviews/codex_deepfix2_r45.md exists (reviewed 2026-07-26); /app/docs/plans/deepfix2/12_R2_DISCUSSION_TRACE.md:60 (row 47 launched round 5).
  - *Correction:* Add an r45 row to §8 and rewrite the header trajectory and status block to the round-5 state.
- **[MEDIUM] F1-15** — The ecosystem entry docs still teach pre-R2 law: orientation shows ship-model OPEN, the review-pass gate as 'default-OFF... open = register item 15', Wave-1 waiting on 'bounded r44 + register item 15', COEXISTENCE unflagged, and '~824 real students' (the cohort is ~907); 01_SOURCES still describes the addendum as the superseded 'pace-matched review queue' candidate and lists neither 11_ (the binding ledger) nor 12_ (the trace).
  - *Evidence:* /app/docs/plans/deepfix2/00_ORIENTATION.md:57, :61, :104, :54, :65; /app/docs/plans/deepfix2/01_SOURCES.md:14 (grep shows no 11_/12_ rows); /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:11 (907 students); admitted un-done C1 residue at /app/docs/plans/deepfix2/12_R2_DISCUSSION_TRACE.md:65 while the convergence already launched (:60).
  - *Correction:* Execute the orientation/SOURCES sweep (R2 posture, unified-model flag, 907 count, add 11_/12_ rows, update the 10_ description to v3) before the round-5 verdict is treated as covering the ecosystem.
- **[MEDIUM] F1-16** — The plan now contains two successive, un-reconciled review-selection replacements: item 16's closure says 'the DF2-14 rotation replaces the orbit-prone slicer by construction' while DF2-42/42d still build the due-based G-DUE engine as 'forced-compatible, free-primary' and §5's exit criteria demand free mode 'on due-based review' — with no statement of which selection feeds the 92% gate's queue after DF2-42, and DF2-42d still cites closed 'register item 15' as its graduation-basis input.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:288 vs :120 (DF2-42), :71 (DF2-42d 'basis per register item 15'), :306 (§5); /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:22-27 (§2.1 rotation is normative for DF2-14); no reconciliation clause exists anywhere.
  - *Correction:* Add a rotation-vs-G-DUE reconciliation clause to DF2-42d's design inputs (replacement, hybrid, or free-only) and repoint its item-15 citation to 10_ §2.
- **[LOW] F1-17** — R2-12(c)'s retirement of the legacy per-word status batch (display-only post-redesign, 'retired at DF2-46') is missing from DF2-46's retirement enumeration.
  - *Evidence:* /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:37 (R2-12(c)) vs /app/docs/plans/deepfix2/02_TASK_LIST.md:119 (DF2-46 lists twins, D-1 residuals, B1 STEP 2 only; grep 'display-only/batch' in 02 = no DF2-46 hit).
  - *Correction:* Add the legacy per-word status batch to DF2-46's retirement list.
- **[LOW] F1-18** — R2-18's monitoring posture (signal set at WSL discretion; CS tickets = David's primary response signal; thresholds set with B1 baselines) is represented in neither 02 nor 10_, which carry only generic 'day-one monitoring' phrases.
  - *Evidence:* /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:44 vs grep 'CS tickets' in 02_TASK_LIST.md and 10_REVIEW_GRADUATION_REDESIGN.md = zero hits; 10_:56-58 names only 'day-one monitoring sized for mass wall events'.
  - *Correction:* Attach R2-18's posture (signal set, CS-primary, B1-baseline numbers) to the day-one-monitoring launch blocker on DF2-10/DF2-14.
- **[LOW] F1-19** — R2-20's binding condition that every AI-grading call be metered (counting point = the grading job) lives only in register item 11 — DF2-12, the card that owns the grading job, and DF2-30/51, where item 11 says the scope lands, carry no metering requirement.
  - *Evidence:* /app/docs/plans/deepfix2/02_TASK_LIST.md:222-224 (item 11) vs :81 (DF2-12, no metering field in the job design), :102/:130 (DF2-30/51); /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:46 (R2-20 CONDITION).
  - *Correction:* Add per-student and global grading-call counters to DF2-12's job design and cite the metering condition from DF2-30/51.
- **[LOW] F1-20** — A cluster of stale pointers and markers survives: 10_'s and 12_'s headers scope the ledger as 'R2-1..R2-17' (it runs to R2-25); item 15's narrative keeps a live open-decision marker on the dead escape valve and points to 'David's addendum-§8 picks' though 10_ v3 has no §8; 11_ §2's A1(vi) is not marked DISSOLVED (R2-11 dissolved it) while DF2-10 cites 'A1(v)-(ix)'; 11_ §0/§6 still list values 1/2/3/5 as awaiting and recommend '[leave OFF]' and 'system_config/review_gate' against R2-14; and DF2-12 points a grading-job-cleanup check at A1, whose scope contains no such item.
  - *Evidence:* /app/docs/plans/deepfix2/10_REVIEW_GRADUATION_REDESIGN.md:3; /app/docs/plans/deepfix2/12_R2_DISCUSSION_TRACE.md:4; /app/docs/plans/deepfix2/02_TASK_LIST.md:241-243, :81; /app/docs/plans/deepfix2/11_REDESIGN_VERIFICATION_AND_FOLD_PLAN.md:58 vs :51 (A1(vi)), :12-13 (§0 awaiting), :88 and :90 (§6 items 5/7) vs :38 (R2-14).
  - *Correction:* Sweep the pointers: extend the ledger-range citations to R2-25, tombstone the escape-valve marker, fix the dead §8 pointer, mark A1(vi) dissolved, update 11_ §0/§6 to the recorded values, and re-home the job-cleanup check.
