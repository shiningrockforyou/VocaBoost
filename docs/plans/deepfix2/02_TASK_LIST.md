# DEEPFIX 2 — Consolidated task list (v5, 2026-07-26 — post R2-PROGRAM fold [rounds 4-6: r44-r49; the ledger 11_ §1 R2-1..R2-47 GOVERNS]; previously v4, post round-3 fold: Codex r43 + David D-1..D-4 + last-exchange findings; v3 archived at `_archive/`)

> **The one forward program**: deepfix remainder + unified container + ~~free-nav mode~~ the universal day-structured model (R2-24/26/27) + UX/messaging, sequenced as waves.
> **v2 folded round 1** (§6); **v3 folded round 2** (§7); **v4 folded round 3**; **v5 folds the R2 program** (§8 r44-r49; 11_ §1 governs) (§8 — Codex r43 fold-verification + David's
> D-1..D-4 decisions + the 07-26 last-exchange findings: pool projections, shadow-orbit starvation, attempt-write windows).
> Verdict trajectory: r41 UNSOUND → r42 SOUND-WITH-GAPS → r43 ACCEPTED+fixes → r44 fold-faithful → r45 addendum-fixes
> → **David's R2 rounds (the 36-decision record) → r46/r47/r48: overrides RATIFIED, FOLD GO → v5 = the complete fold.**
> Legend: ✅ done · 🔄 in progress · ⛔ not started · ⚠️ one-way door · 🔵 WinClaude deploy / David authorization ·
> 🧭 David decision needed · 🔍 convergence checkpoint. Deepfix1 IDs cited as `[D4/P5]` — **original gate text governs**
> (this file is a summary layer; where thinner than a gate-bearing source, the source wins — orientation §5).
> Actions log to `MASTER_TASK_TRACKER.md` under DF2-IDs.

## 0. Program invariants (apply to every item)

- 26SM read-only; 25WT sandbox; deploys via WinClaude 🔵; **commit on `main`, never branch**; targeted `git add` only;
  a passed `new` attempt is the CSD/TWI anchor (`twi = newWordEndIndex + 1`).
- **ALL attempts recorded [D-3, program invariant]:** every graded attempt — pass or fail, MCQ or typed, new-word or
  review — is durably written to Firestore. Verified already true today for graded attempts (`passed:false` recorded;
  only dormant practiceMode skips). Every cert matrix asserts it explicitly. Residual windows (STATE_BRIEF §6c
  numbering): **(i)** grade-OK → client write lost → abandon and **(iii)** crash + Start-Fresh discard close via
  **DF2-12/DF2-13**; **(ii)** grader outage mid-take (no grade exists to write — 조은서-class) is an acknowledged
  residual with monitoring, not silently claimed closed.
- **Pin discipline:** the CERTIFIED CORE (completeSession/resolveListProgress, `foundation.js`) is pinned `0ddbb34`
  (grader `gradeTypedTest`→`0992f5f` r63 and `submitChallenge`→`6094cdd` r65 already moved surgically). Any move of the
  core = deliberate, David-authorized, clean tree, behavioral re-cert on the new pin. **A `foundation.js` edit is NEVER
  surgical.** The deploy set is derived per-change by RULE, not copied as a constant [Opus-1 C3]: **= every deployed
  callable from which a CHANGED export is REACHABLE in the call graph — reachability, NOT mere bundle inclusion
  (index.js's top-level `require("./foundation")` puts foundation in all 17 exports' bundles; that alone enrolls
  nothing) [P3-Opus-2 M8]** (DF2-10's 10-callable set is the INSTANCE for the review-pass diff, not the template —
  the 7 exclusions (`submitChallenge, getGradingStatus, createSession, submitTest, pauseStaleSessions, renameStudent,
  provisionTeacher`) are justified by reachability per-diff at deploy time; and under B1's two-step retirement the set
  is RECOMPUTED at each step — a retired callable's DELETE is itself a deploy target that must deploy + verify).
  **The certified-pin constant is RE-STATED at each successful pin-move [P3-Opus-2 H5]: post-DF2-10 the rollback
  target for subsequent work is the DF2-10 pin — `0ddbb34` retires as a rollback destination.** "Atomic" means
  coordinated-with-verification [Codex S4]: (1) no lever/threshold enableable while the deploy is incomplete → (2) deploy
  the complete target set → (3) verify EVERY target's revision+behavior (`version` callable — which must itself redeploy to
  attest), retry/roll back any partial → (4) only then expose the client lever. **Rollback is itself a full-set redeploy of
  the rollback pin — NEVER surgical** [Opus-1 C2] (a surgical rollback re-creates the skew). Plus fail-closed flag-posture
  probe + `ANCHOR_VALIDATION_SHADOW`/M4-clock continuity assertion. Off-peak + pre/post live scans
  (`data-integrity-sweep` + `system_logs`) + a defined soak/rollback **for every deploy touching completeSession,
  rules, the ATTEMPT-WRITE PATH, or REVIEW COMPOSITION (DF2-10/12/13/**14**/44/45/46 — ~~42/43~~ dissolved [r50]) [P3-Opus-2 H6]**.
- **Byte-identity falsifier + the TOTAL delta mechanism** [Fable-2 H2/H3]: forced-mode behavior provably unchanged
  (differential fixtures) except via a **named per-wave approved-delta list — every visible ship must be enrolled**:
  **Wave-0 list** = DF2-01 (hide set) + DF2-07 (a-e); **THE LAUNCH delta set (DF2-14, one flip)** = throttle removal · universal navigation (past-day browser +
  within-day freedom — DF2-51 in-train on the legacy UI [FF2-01]) · the gate/wall @92 · labels/composition/graduation · teacher controls · force-pass · recovery
  UI + 12h window · progression streak · review-first flow — enrolled ON THE DF2-14 CARD as one named launch event
  [r48-ratified]; DF2-12/13 durability = in-train, outcome-identical;
  **Wave-3 list** = messaging rows, one-affordance Dashboard, chrome/derived copy, + DF2-36 (G-QUAR screen); standing
  exceptions that ship on their own gates regardless of ship model (DECIDE-0 CLOSED (b) single-train, R2-25) **and independent of wave placement [P3-Opus-2
  L23]** = DF2-01, DF2-07, DF2-36 (pre-P5 hard gate), CONT-A, DF2-35 (the hold-visibility PANEL half ONLY — the force-pass action is INSIDE DF2-14's train, FF2-10 [r50]).
- **One G-PASS predicate — transitional** [Codex N2]: canonical module AUTHORED at DF2-08 (no live reroute, no deploy);
  authoritative writer/readers ADOPT at DF2-10 (the first call-site adoption + deploy); consolidation COMPLETE at DF2-46
  (remaining twins retire). Every NEW consumer (review gate, SegmentTest, challenge/regrade) uses the module incl. the
  **server-read authoritative `passed:true` short-circuit — provenance-typed, never client-supplied** [Codex N4].
- Rules: named artifacts only; never bare-deploy; never the repo P10d draft; **no rules deploy after R3** (R3 = D8g's
  final rules artifact per MASTER D8 — glossed here because two other R-namespaces exist: D3.5's R1-R16, Opus-A's R-A..L).

## Wave 0 — Ready now (mostly parallel) 🔍 at wave end

| ID | Task | Notes / gates | Surface |
|---|---|---|---|
| DF2-01 ⛔ | **BlindSpot hide** — `BLINDSPOTS_UI=false`: Dashboard link (:2172), route (App:91→redirect), HelpModal copy (:252); help-site pages + **TA_FAQ/TA_SUPPORT_GUIDE blind-spot mentions** ride the same release. **Sequencing: unblocked — the probe is SHELVED (R2-22); the rotation (DF2-14) replaces the slicer the net covered** | Spec ARCH §11.1; code + 21-day data model KEPT (the 21-day rest SURVIVES the redesign; ~~G-DUE seed~~ — G-DUE CANCELLED, R2-27 Q4). **STARVATION NOTE [2026-07-26, sim3-verified]: BlindSpots was the accidental safety net for shadow-orbit-starved words (the daily-fifth slicer lets words dodge every slice indefinitely — see DF2-0P/DF2-42d). Hiding it removes that net **until DF2-14's rotation replaces the slicer (~~G-DUE/DF2-42~~ dissolved — the CURSOR-CHAINED rotation IS the fix [r58]: lap coverage proven by the fixture)**; help/TA copy must not promise blind-spot coverage; ~~DF2-0P~~ SHELVED (the flip retires the slicer)** | hosting 🔵 |
| DF2-02a ⛔ | **Safe dead-code deletions** — SessionSteps, SessionProgressBanner, BlindSpotsCard, MasteryBars, MasterySquares import, StudySelectionModal, **the dead 7-export sessionService transition API incl. `recordNewWordsTestResult` (sessionService.js:268, dead G-PASS copy)**; dead levers `reviewTestSizeMin/Max` (+ `assignment.testSizeReview` ghost-read): **branch UNBLOCKED — item 3 CLOSED (R2-4/R2-15): DELETE, replaced by DF2-11's teacher-set `reviewQueueSize`/`reviewTestSize` fields (deletion coordinates with DF2-11's menu rebuild — same modal, one release)** | MAP §13 + [E2] deletion leg · 11_ R2 | hosting 🔵 |
| DF2-0H ⛔ | **Deepfix1 housekeeping** (MASTER §4.3 a-d, now owned): (a) ✅ rotate RESUME.md → deepfix2 (done at the 07-26 save-state); (b) `git add --renormalize .` commit of the evidence pile (never `-A`); (c) SUPPORT_RUNBOOK cites for the 07-18 26SM scans; (d) cite sources for B2/B4 closures + PR-1's 2-account anecdote; + D3.5 report/FINDINGS honest reconciliation (with DF2-05) | Fable-2 L7 | docs |
| ~~DF2-0P~~ 🛑 **SHELVED (R2-22; register item 16 closed)** — the DF2-14 rotation kills the slicer class structurally; reopen trigger = the "stuck despite ≥92%" monitoring signal [r48]. Original card (historical): **Read-only shadow-orbit starvation probe on 26SM** (NEW — last-exchange (b); David go = register item 16): count, per active student, unmastered in-pool words with no review appearance for **N study-days, N swept ∈ {21, 35, 56}** (the tracer's observed extreme: 72+) despite eligibility (orbit signature per the `sim3.mjs` tracer: stable word index vs the rotating daily-fifth slice — live code slices identically in `computeUnmasteredSegmentIds`); **script = a DEFINED script `scripts/cs/orbit-starvation-probe.mjs`, added to the SUPPORT_RUNBOOK defined-scripts catalog (never scratch); read-bound: one `study_states` read + one bounded attempts query per student (≈2 reads/student, ~1.7k total, paginated) [P3-Opus-2 M13]**; output = per-N distribution + affected-student count + exemplars → `docs/plans/deepfix2/evidence/` (dir created at first write); read-only, off-peak; log as a CS diagnostic in SUPPORT_RUNBOOK. Feeds DF2-42d acceptance, DF2-01's exposure note, and any interim-mitigation decision | sim evidence: `docs/session_state/2026-07-26_4817fc5a/scratchpad/sim3.mjs` | scripts (read-only) |
| DF2-03 ⛔ | **Full state enumeration** (`03_STATE_ENUMERATION.md`) — ARCH §8 tiers 0-3 + A1-A5 + exit statuses + `carriedFrom` + ~~free-mode fields~~ universal-model fields (ONE model, R2-24) + event-seen markers + **`resetEpoch` tombstone** (ARCH §11.3); **+ audit whether any genuine allocation-zero day survives D-1's throttle removal (new-student edge, pace edge) — feeds DF2-07(b)'s reason set honesty. PRE-CODING GATE for Waves 2+ [restored, P3-Opus-1 F5]** | — | doc |
| DF2-04 ⛔ | **Golden/differential fixture harness** — initializeDailySession outputs across flag matrix + the 9 scenario fixtures + PDF/debug callers (A4); full-config compare; **PLUS: review-gate OFF-path with legacy `passed:undefined` AND new `passed:true` reviews byte-identical (Opus-A R-C) · grandfather-epoch × review-gate precedence (R-K) · `validateAttemptAnchorShadow` dependency-closure identity across pin-moves (M4-clock protection) · [NEW] the shadow-orbit starvation fixture (sim3-derived: a word in a stable orbit under the current slicer — a DOCUMENTATION fixture marking today's unfair rotation, so DF2-14's rotation fix (~~DF2-42~~ dissolved, R2-27 Q4) is provable and regressions can't hide) · [NEW, D-1] fixtures capture CURRENT (throttle-live) behavior; THE LAUNCH's delta list (DF2-14 — was DF2-10's named Wave-1 deltas) is where expectations fork — every forked expectation must cite the delta list** | accepted only after DF2-03 freezes the inventory | test infra |
| DF2-05 🔄 | **D3.5 risk remediation** (R1-R16, `D3.5_RISK_REMEDIATION.md` **v2 numbering**) — **Wave-4 HARD ENTRY GATES corrected [Codex N1]: R1 (anchor-minting — the standing NOW-authorization covers the AUDIT only; any resulting code/data fix is carded separately under its own David/pin gate [P3-Opus-2 M15]; implementation owner named at closure) + R2-class inflation confidence ONLY.** R7 (next-list carry) is CONT-A/[D6] ownership, NOT a P5 gate. R8 (verdict-engine needs a **functions-side `session_completed`/`day_advanced` success stamp**) → the stamp ships INSIDE DF2-10's already-authorized core pin-move **under DF2-10's M7 proof contract**; tooling hardens here afterward (requires the server-only stamp, fails closed on missing/mismatched window) | scripts/sandbox + DF2-10 |
| DF2-06 🧭 | **Grader prompt fix round 2** (verbatim-English family) + regression extension; surgical `--only functions:gradeTypedTest`. **Interleave rule: must deploy BEFORE DF2-10's server commit or inside DF2-10's set** (shared `writeAttemptTxn`; DF2-12 is a CLIENT flip sequenced separately — see its card [P3-Opus-2 M7]). **INTENT DECIDED [R2-19]: SEMANTIC EQUIVALENCE — accept anything demonstrating the student knows the meaning; regression suite encodes the verbatim-English family as accepted.** Deploy-go = ask-at-implementation | Fable-B corr.8 | functions 🔵 |
| DF2-07 ⛔ | **QUICK-WIN messaging on EXISTING screens** (Opus-B; no container dependency; a NAMED Wave-0 delta) — **(a) threshold copy derives from real `passThreshold`; hardcode inventory CORRECTED [r43 M5]: SessionProgressSheet.jsx:82 ("95% required to pass", threaded from its 4 render sites which already hold `retakeThreshold`) AND HelpModal.jsx:212 ("Must score 95% to continue" — derive it or genericize the class-less sentence); live result cards ALREADY derive (MCQTest.jsx:1201 / TypedTest.jsx:1461 — verify-only); the old RetakePrompt:2383 cite is DROPPED (dead branch per MAP §13)**; **(b) review-only reason banner — honestly NET-NEW minimal UI [Opus-2]; contract CORRECTED [r43 H3 — no entry-returned reasons object exists; the server `reviewOnlyReasons` is server-local (foundation.js:1390-1398), returned only on `completed` (:1677-1683), stripped on `review_recorded` (:1584-1590); studyService.js:1783 is a boolean]: build a CLIENT-LOCAL pure discriminant — `reviewOnlyReason: 'list_complete' | 'review_resume' | 'no_score' | null` — **at TWO honest boundaries [r44 H1: init cannot know `no_score`]: `list_complete`/`review_resume` derive once during `initializeDailySession` config assembly; `no_score` is SET only from the effective queue-build result (`buildReviewStudySet`, DSF:509-527) through one shared helper/state transition** (**post-D-1 set restored to the brief's three [P3-Opus-1 F3]: `no_score` = the genuinely-empty/all-mastered review day surfacing on the auto-complete path — it never enters H4's `holdReason`; its post-D-1 existence confirmed by DF2-03's audit; fixtures DISTINGUISH the terminal no-work branch (DSF:855-868 — deliberately does NOT complete/record) from the post-new-test empty branch (:1035-1055 — completes): "empty always completes" is FALSE [r44]**; precedence `list_complete > review_resume > no_score` applied exactly ONCE — **a NEW ordering decision OWNED here [P3-Opus-2 M10]: the server object (foundation.js:1390-1398) is a FLAT boolean set (only the dead throttle leg had ordering); DF2-10's H4 adopts the SAME ordering server-side so the two cannot drift**; never inferred from the coarse `reviewOnlyDay` flag; **reason-VOCABULARY single source = `06_MESSAGING_COPY.md` §0 — DF2-07/10/31/32/35 cite it by pointer [P3-Opus-2 M9]**); THROTTLE LEG DEAD [D-1]: `allocation_zero` is retired with the throttle — the 50% copy NEVER ships; while the throttle remains live (until DF2-10) allocation-zero days render NO banner (status-quo silence beats soon-dead copy); if DF2-03's audit finds a genuine post-D-1 allocation-zero day, card its honest copy then; build the derivation ONCE, shared with DF2-32's later re-home; fixtures = all three reasons + null + malformed/unknown; per-reason 25WT fixture oracle is the 26SM SHIP GATE [Opus-1 C1, Codex N9]**; **(c)** list-end message targets the MID-SESSION review-only/retake-wall surfaces only (the Complete-phase terminal already exists, DSF:2450) and conditions on `nextListId` absence (B4 auto-advance; coordinate with CONT-A) [Opus-2, Fable-2 M7]; **(d)** token copy — student surfaces already say Monday (verify-only); where touched, use TZ-honest copy ("weekly, Monday 4 AM Korea time" or the viewer-local instant — bare "every Monday" is FALSE for 미주) [Codex N8]; `06_MESSAGING_COPY.md` SUPERSEDES §12.3's draft strings [Fable-2 M5]; **(e) help-site/TA reconciliation RESCOPED [D-1]: the 30% throttle passages in TA_FAQ + TA guide + `public/help-student-{en,ko}.html` (en:1007 / ko:998 — the highest-harm surface [Opus-2 MUST-FIX at r42]) are REMOVED, not corrected to 50% — the mechanic dies at DF2-10; absence of an explanation beats a false or soon-false one; same-release with (a)-(d)** | | hosting + docs 🔵 |
| ~~DF2-42d~~ | **DISSOLVED — G-DUE CANCELLED (R2-27 Q4).** The rotation (DF2-14 §2.1 of the addendum) is the selector; the fairness/no-starvation criterion TRANSFERS to the rotation spec — **the PROOF = a named DF2-14 gate deliverable [r49-H5, FF1-15]: ~~the H8 re-sim demonstrating bounded sighting intervals~~ ⟶ [R2-47/r58] the rotation FIXTURE's lap-coverage + tiling properties, re-run against the extracted PRODUCTION composer at the launch gate (differential oracle) + H8's descriptive baselines; `reviewLastTestedAt` [renamed r53-B3] becomes SERVER-written in the accepted-attempt txn (a client-written clock is not authoritative)**; per-word due scheduling = a possible far-future revisit, out of program | R2-27 | — |
| ~~DESIGN TRACK~~ | **RETIRED [R2-24/27]: no modes ⇒ `08_MODE_RECORD_CONTRACT` + `09_FRONTIER_WRITER_SPEC` authoring is DEAD (DF2-47 dissolved; DF2-43's surviving contracts live in DF2-46).** *Historical:* they were pure design docs; Wave-4's entry gates (migration protections) apply to state-touching execution, not to authoring. DF2-03's free-mode-fields section + DF2-42d's mode sections stay PROVISIONAL until DF2-47's 🔍; **DF2-04's freeze covers FORCED-mode fields only** (free fields freeze post-47) | — | docs |

## Wave 1 — Policy module + review-pass gate + attempt-write durability [ARCH §8-G6: BEFORE any extraction] 🔍

| ID | Task | Notes / gates | Surface |
|---|---|---|---|
| DF2-08 ⛔ | **THE policy module: one G-PASS predicate + assignment-policy resolver** (absorbs [E3], gate-override vs its old "(after D5)" APPROVED here [Fable-2 M6]) — **BOUNDARY [Codex N2]: DF2-08 AUTHORS + equality-tests the pure module/generated copy ONLY; NO live call-site reroute, NO functions deploy — adoption is DF2-10's; the functions-side copy deploys ONLY inside DF2-10's set** [Opus-1 C4]; gated on DF2-04 green. Contract: units (0-1 vs 0-100), threshold source/fallback, test type (+ ~~navigationMode~~ — dead field [R2-27]; adapter maps legacy rows only), **provenance-typed `authoritativePassed`** (server-read persisted attempt / authorized override-regrade ONLY — client input never short-circuits; `submitVocabAttempt` still computes the initial verdict itself) [Codex N4], challenge/regrade/override behavior; **+ the review-test-size policy = FIXED teacher-set (default 30) — item 15 CLOSED [R2-13]; the adaptive variant is dead**. **Default-drift normalization (80/20, 95/92/0.95) is NOT byte-identical [Codex N3] → split**: (i) inventory every caller+fallback, (ii) **verdict-flip census** on records with missing/legacy fields, (iii) 🧭 David target-default decision, (iv) compat adapters preserved until that approved migration, (v) legacy/null/orphan shape tests in both runtimes — NEVER smuggled into DF2-14's train (~~default-OFF release~~ dead — one flip, R2-31) | Codex R2/R6/N2/N3/N4 | modules (no deploy) |
| DF2-10 ⛔ | **Core-server workpackage of THE LAUNCH (builds INSIDE DF2-14's train — NO separate production exposure).** The first CORE pin-move off `0ddbb34`: **(1) REVIEW-PASS GATE at the R2-14 posture** — threshold/sizes resolve `system_config/review_v2` → per-assignment overrides via ONE versioned helper `{enabled,threshold,queueSize,testSize,configVersion,readStatus}`; per-request snapshot; attempt-time posture+configVersion STAMPED and governing through completion; cold start w/o cached posture ⇒ HOLD, mint nothing [r48]; kill-switch label law = R2-32/r48 FINAL (fail+correct write while OFF, proven freezes, reruns follow the same OFF law [R2-41], `enabled:false` instant + state-preserving). **(2) UNIFORM EVIDENCE READER** — `passed===true` in EVERY posture at every reader site (determineStartingPhase · studyService:312 · getReviewForDay · getDayReviewForEngagement · `dayReviewExists` foundation:1649 · foundation:1638 · challenge/override · marker suppression); ANY-passing day semantics preserved (the foundation:819-836 filter pattern); **ENGAGEMENT LEGS REMOVED [R2-11]** (fpHoldCsd non-engagement hold, engaged-paired reader legs, `engaged` response field, zero-question quirk). **(3) B1 HARDENING** — shared `deriveNoScoreEligibility` server proof (effective composition from authoritative facts; fail-closed under gate-ON incl. the query-error leg; race test at the txn boundary); `markReviewComplete` HARDENED for cached bundles (genuine no-score served, all else rejected; DELETE at DF2-46 after drain). **(4) RESPONSE SCHEMA** (single source; DF2-31 by pointer): `completed | review_recorded | review_retake_required | no_evidence | day_guard_rejected | already_completed`; server-owned `holdReason` (`review_retake_required | list_complete | review_resume | no_evidence | day_guard`); per-status × field table FROZEN pre-code; `reviewMode` legacy passthrough until DF2-46. **(5) R8 SUCCESS STAMP** — atomic outbox in the txn or idempotent repair on `already_completed` retry; verdict tooling fails closed. **(6) D-1 THROTTLE REMOVAL** — server legs (reviewMode enter/exit, hold-csd, allocation scaling, float writes) + ALL client legs (studyService:364-368/:516 · forcedPathway:83-105 · db:3056-3061 · progressService:609 · MCQTest:820-838/:1016-1031 + TypedTest:1076-1094/:1294-1309 snapshot writers — same release); challenge-advance = `interventionLevel≡0` (unthrottled dailyPace); M4 shadow legs re-derived; ~27 held release organically (verified in cert; any data fix = David-authorized). **(7) FORCE-PASS server resolver [R2-10 (iv) note: challenge-adjudication replay in B1 is the last open activation condition]** — a NEW exact-attempt grade-only surface (today's `overrideAttempt` is a synthetic-attempt/manual-pass path, NOT reusable [r46-H4]): exact failed-attempt identity; review/new legs flippable separately or together [R2]; expected-anchor validation with typed refusals (null-anchor · stale-row · progression-moved ⇒ `already_completed` · stale-day submit ⇒ `day_guard_rejected` · wrong-context); **override-then-real-pass law [r53/A1]: ONE advance + ZERO graduation (an override mints no proof, no graduation; client graduation runs NOTHING on `already_completed` — CERT asserts it)**; `teacherEdited` label — **field set per A1 (2026-08-01): `teacherEdited:true / teacherEditedBy / teacherEditedAt / preOverride{score,passed}` — NOT `manualOverride` (that key is semantically claimed by the synthetic-anchor surfaces; CS scripts key on it)**; NO word-label/graduation mutation; race-safe; the wall's live listener dissolves on override [A1(v)-(ix)]. **Display leg [A1(ii)]: all four gradebook enrichment sites recompute correct-counts from rows while showing the stored score — an edited attempt renders "95% (12/30)" unless the gradebook renders the `teacherEdited` label; the label render is IN-SCOPE here.** **(8) GRADUATION** at completion under DF2-14's formula (three strata; NORMALIZED score `clamp(scorePercent,0,100)/100` [r49-B1] + the supply clamp; ZERO on fail; vectors 91/92/100 frozen; **+ the R2-41 RERUN branch: rerun review tests graduate tested-correct only, no fill; every graded test stamps incl. live new-word tests; the R2-41(e) underflow top-up in composition**). **(9) PROGRESSION-BASED STREAK — FROZEN SEMANTICS [R2-21, r51-H1]: calendar basis = the KST date (existing convention); AT MOST ONE streak credit per KST calendar date, regardless of same-date multi-advances/classes/lists/devices (credit keyed on (uid, KST-date) — idempotent by construction; concurrent completions cannot double-credit); Saturday/Sunday are SKIPPED in gap computation (Friday→Monday = consecutive); a WEEKDAY calendar-date gap with no completed day BREAKS it; EPOCH-SCOPED (resetProgress zeroes it); server-computed at the day-advance txn; Dashboard surface = DF2-51's leg.** CERT: DF2-14's matrix (10-case dual-class oracle · ×3 posture fixtures · OFF-parity on the kill path · D-3 all-attempts assertions · stamping truth-tables · anchor-shadow continuity · idempotent-retry-returns [code-verified index.js:474-478]). DEPLOY: inside DF2-14's dark train; set per §0 reachability; full-set rollback + §0 pin restatement | 11_ R2-1..R2-47 · r43-48, r51 · A1 | functions (in-train) 🔵⚠️ |
| DF2-11 ⛔ | **Review-settings teacher UI + menu simplification [RESCOPED R2-14/R2-15]** — AssignListModal consolidated into pace/new-test + review groups; review group = `reviewPassThreshold` (default 92) · `reviewQueueSize` (60) · `reviewTestSize` (30) as per-assignment overrides of `system_config/review_v2`; **DELETE the dead `reviewTestSizeMin/Max` fields** (register item 3 closed); plain-language ENGLISH-ONLY labels [R2-44 — supersedes ko/en] with visible defaults; ClassDetail read surface; validation. **Builds INSIDE DF2-14's train (one exposure; all three fields live from the flip — no dead-lever period)** ; C2-audited. **Per-class disable law [FF1-14]: a teacher MAY set a class-list's review gate OFF post-launch; class-OFF mirrors the global kill-switch label law (fail+correct still write; proven freezes for that assignment). FIELD CONTRACT [r50-H4]: per-assignment `reviewGateEnabled` (boolean, DEFAULT TRUE; missing/null ⇒ true) — resolver precedence: global `system_config/review_v2.enabled=false` ⇒ OFF everywhere; else assignment `reviewGateEnabled=false` ⇒ OFF for that assignment; NEVER overload the global `enabled`, NEVER threshold-0-means-off** | Opus-A R-A(2) · 11_ R2-14/15 | hosting 🔵 |
| DF2-12 ⛔ | **Typed attempt durable grade+write (D-3 closure, typed leg) — DESIGN CHOSEN 2026-07-26 [11_ R2-12/R2-13; instantiates r44-H4's preferred "cache the authoritative grade" option and EXTENDS it to the inputs].** Server reality [r44]: index.js:1167-1181 attempts `writeAttemptTxn`, :1182-1193 catches a write failure and returns a successful grade with `attemptWritten:false`; the job result persists only in the `!writeContext` branch. **THE DESIGN — ANSWERS-ON-THE-JOB (grading_jobs is LIVE in production — verified 2026-07-26, docs written same-day):** (1) at claim, the job stores the submitted `rows` + the FULL `writeContext` (attemptDocId/class/list/test ids, sessionType, studyDay, totals, anchor + display fields) + queryable top-level `classId/listId/dayNumber` + `lastError` — the job is then SELF-SUFFICIENT to re-grade (server resolves the answer key from (listId,wordId)) and to write the attempt, from ANY device; (2) one-call grade+write via writeContext; a failed write leg retries the WRITE ONLY from the cached grade (never re-bills the AI); the client never shows completion, never clears local recovery, never offers "Start Fresh" until the exact attempt doc is confirmed; (3) **student UX [R2-13]: a distinct "SUBMITTED — NOT YET GRADED" state with [Try grading again] / [Wait — try later]; the student may proceed to REVIEW study/test while grading is pending (deliberate forced-flow ordering change; day completion still requires the graded+passed new test); attempting day-advance while blocked → prompt "your earlier test hasn't been graded — grade it now?"; SESSION-START PICKUP queries the student's unfinished jobs (status≠graded → re-grade from stored rows; graded-but-attempt-absent → write the attempt from the cached payload — closing window (i) server-side) — needs the (uid,status) index; retention = the 12h LOGICAL-EXPIRY law below (~~delete/archive N days~~ superseded [R2-35/r48]; existence of any current cleanup still checked at A1)**; attempt stays WRITE-ONCE at grading success (no ungraded-attempt object states anywhere — 11_ R2-13's can-of-worms avoidance). Cert [r44 + R2-13]: crash before response · crash after grade cache · write failure → write-only retry · lease expiry/takeover · reload · cross-device pickup both branches · idempotent exact-doc recovery · attempt-record byte-identity vs the client-written path · challenge/regrade parity · review-first flow honors day-completion gating. **Builds INSIDE DF2-14's train (one exposure).** **GRADING-JOB OWNERSHIP [r56]: exact `job.uid === caller.uid`; missing/malformed uid ⇒ REFUSE + quarantine (status:'quarantined' + ops_metrics event; legacy rows scanned at the dark build). AI-GRADING METERING BUILDS HERE [R2-20 · F2-10]: per-student + global call counters on the grading-job path — the metric for the future global limit gate; a ship-gate for the past-day re-test grant.** Retention = 12h logical expiry (server `expiresAt`, transactional check + redact at pickup; async TTL physical backstop; reset cancels via epoch) [R2-35/r48]. §0's off-peak + pre/post scans + defined soak/rollback apply [P3-Opus-2 H6]. Residual now NARROWED: only "AI grading fails AND the student never triggers any retry surface" leaves an ungraded (but durably stored, server-recoverable) submission — monitored, never lost | D-3; STATE_BRIEF §6c · r44 H4 · 11_ R2-12/R2-13 | functions+hosting 🔵 |
| DF2-13 ⛔ | **MCQ durable grade+save (D-3 closure, MCQ leg)** — one server call computes the MCQ verdict AND writes the attempt (extend `submitVocabAttempt` or mirror the writeContext pattern), closing the client-grades-then-writes gap. Same cert pattern as DF2-12 (shape byte-identity, idempotency). Functions deploy — set re-derived per §0; `submitVocabAttempt` is in DF2-10's set → **builds INSIDE DF2-14's train (after core verification, one exposure)**; §0's off-peak + pre/post scans + defined soak/rollback apply (attempt-write path) [P3-Opus-2 H6] | D-3; STATE_BRIEF §6c | functions 🔵 |
| DF2-02b ⛔ | **RetakePrompt + REVIEW_TEST phase deletion** (moved from Wave 0 [Fable-2 M2]) — per spec (branch DSF:1952-1969 + def :2367; KEEP the live localStorage marker vocab); executes AFTER DF2-10 settles the retake surface; re-verified at this wave's 🔍 | Codex S5 | hosting 🔵 |
| DF2-35 ⛔ | **Teacher hold-visibility panel** (moved from Wave 3 [Opus-2] — no container dependency; a standing delta-list exception, wave-independent per §0) — **DATA CONTRACT REQUIRED FIRST [r43 M8]: "currently held + reason" must be provable from stored/server-read facts, never inferred ephemeral entry state for a whole class.** v1 scope = reasons provable from EXISTING class-scoped reads (class attempts + `studentProgressMap`): held-below-gate (post-DF2-10: latest review attempt `passed:false` on the current day) · list-end · review-resume; per-reason proof table + freshness semantics ("as of last completed session/attempt", timestamped) + unknown/error → "unknown", NEVER "held" + bounded-read/query-index strategy (no per-student N+1); quarantine leg activates only post-DF2-40 (canonical). **Post-D-1 the throttle-held reason does not exist** (panel ships with/after DF2-10 in this wave — never renders a dying mechanic). Shares DF2-07(b)'s reason-derivation VOCABULARY (single source `06_MESSAGING_COPY.md` §0 [P3-Opus-2 M9]; never its client-session state); + a **who-passed/frontier-advanced column** spec'd for DF2-53's follow-through; **SPLIT [FF2-10]: the hold-visibility PANEL = a standing exception on its own gates; the FORCE-PASS row action = INSIDE DF2-14's train (dark until the flip, live at it — a launch blocker)** | Opus-B decision 6 · r43 M8 · 11_ R2 | hosting 🔵 |


## THE LAUNCH — DF2-14: the review redesign, one all-at-once activation [AUTHORED post-r48; carries every launch contract] 🔍

> **What it is:** the single production event David decided (R2-26 Q6): gate ON@92 + the review redesign + the
> label backfill exposed to the whole cohort in ONE audited activation. Build/cert stages internally; ONE
> student-visible flip. The ledger (`11_` §1, R2-1..R2-47) governs; r48 + the r50-r56 closures + the post-presentation R2-38..47 ratifications.

| ID | Task | Notes / gates | Surface |
|---|---|---|---|
| DF2-14 ⛔⚠️ | **Review-graduation redesign + launch.** SCOPE: teacher-set `reviewQueueSize`(60)/`reviewTestSize`(30) rotation queue (**CURSOR-CHAINED [the fixture-driven fix 2026-08-02 — day-offset modulo skipped words under pool mutation; proven skip-free by `rotation-cyclicity-fixture.mjs`]**; pool<size ⇒ whole pool + the R2-41(e) UNDERFLOW TOP-UP: earliest-graduated resting words, up to size) · uncapped least-recently-tested priority slots AND the R2-42 DETERMINISTIC LRT REMAINDER (one clock, stable tie-break: `reviewLastTestedAt` [renamed r53-B3] then wordIndex; presentation shuffled; post-compose invariant check [R2-46: effectiveTestSize; priority prefix preserved; fallback randomizes the remainder only + records its seed] ⇒ `composition_fallback` to the SERVER-ONLY metrics sink + monitored) · the FOUR labels (`reviewFailCount`/`reviewLastFailedAt`/`reviewLastCorrectAt`/`reviewLastProvenAt`; two derived predicates needsPriority/fillEligible; THREE composition strata: priority / recovered-ever-failed (presentable; fill-ineligible until proven-after-fail [r49-H4]) / proven-or-never-failed (fill-eligible)) · stamping law (fail+correct any attempt incl. blank=fail via the presented set; proven ONLY on a passing attempt — or an accepted challenge **[R2-10 DEFERRED: built per r47; activation = the FOUR r53 conditions (11_ §2 A2) incl. David's Q-D2 — never A2's report alone]**; ~~retests stamp NOTHING~~ ⟶ R2-41: EVERY graded test stamps — live/rerun × new/review; live new-word tests included; reruns graduate tested-correct only, no fill) · graduation = min(floor(queueSize_effective × scoreFraction), |correct|+|eligibleFill|), scoreFraction = clamp(scorePercent,0,100)/100, queueSize_effective = the day's ACTUAL pinned queue length AFTER the R2-41(e) underflow top-up [r53 definition fix] [unit-normalized r49-B1 + r50-B2; default-92-threshold vectors 91/92/100], zero on fail, both terms logged · FULL-history backfill under R2-35's law (unqualified recompute-at-92 + r48's impossible-record EXCLUSION filter w/ published counts) · teacher menu simplification (DF2-11 scope) · force-pass UI + the NEW exact-attempt resolver (r46-H4: NOT today's overrideAttempt; A1-specced) · answers-on-the-job + 12h logical expiry (server `expiresAt`; async TTL backstop) · AI-grading metering (per-student + global) · modality-fallback DELETION (`actualMode = assignment.reviewTestType ∥ 'mcq'`) · review-first flow + past-day browser + retests (`type:'retest'`, non-advancing, gradebook-preserving, metered, STAMPING per R2-41; rerun review = regenerated pure-random over the introduced range) · **the R2-40 restudy package (resume sheet · TOC five-state day grid, no phantom chapters · mastery pips on a server-written display-only counter (a pip = BOTH rerun tests of the visit passed, R2-40c-ii; overflow >5 = five pips + a static "×N" total [R2-45]; all new UI English-only [R2-44]) · restudy bookmark pref · rerun review = REGENERATED pure-random over the introduced range [R2-41(h) — replay dropped], the day-advancing review only at the frontier · list-end screen w/ next-list + read-again · the teacher gradebook re-run TOGGLE, default OFF — **[C2 2026-08-01] `queryTeacherAttempts` (db.js:2024-2076) has NO attempt-type filter today, so default-OFF requires a QUERY-LAYER retest exclusion (client-side filtering breaks pagination sizes)** — build home DF2-51's leg + the gradebook surface; C2-audited)** · progression-based streak (build home DF2-10(9); display DF2-51's leg).<br>**THE 8 CARRIED CONTRACTS [r48]:** (1) impossible-record exclusion (never clamp into proof) · (2) Q3 error-fencing: queue identity `{uid,classId,listId,logicalDay,resetEpoch,algorithmVersion,configVersion}` + snapshotted values (mid-day edits ⇒ next queue) + cross-class evidence rule (attempt validates against ITS OWN queue; cross-class pass satisfies the shared day on `{uid,listId,logicalDay,resetEpoch,anchor/generation}` match [r48 verbatim, FF1-06]; audit records the consumed attempt + source config; **MIXED POSTURES [R2-38]: evidence validity follows the SOURCE class's effective posture — an OFF-source auto-pass IS cross-class evidence for an ON target (PROGRESS IS INDIVIDUAL, David 2026-07-27; teacher escape hatch = a class-only list)**) + exactly-once concurrency (one advance + one graduation per logical day; idempotent winner; loser `already_completed`; no client focus in server auth) · (3) r46-B2 backfill cutover: durable high-watermark + idempotent replay + late-write capture + activation barrier + post-activation reconciliation + **reset/challenge/override ORDERING + retry semantics [r46-B2, FF1-06]** · (4) authoritative queue/presented-set identity — TWO immutable records [r46-H2 + r50-B3]: the DAY-QUEUE record `{identity + anchor/generation, orderedQueueWordIds, poolHash, config snapshot}` (one per logical day) + the PER-ATTEMPT PRESENTATION record `{presentationId, queueRef + poolHash, presentedWordIds, compositionVersion, server claim}` (one per composed test — every retake composes a NEW presentation under the R2-15 rotation; each attempt validates against ITS OWN presentation) + server-derived `totalQuestions` (client denominator dies) + M10 exact-set verification + the compose-to-submit label-drift rule + **the rotation clock `reviewLastTestedAt` [RENAMED r53-B3 — legacy `lastTestedAt` stays client-written + unread until DF2-46] SERVER-WRITTEN in the accepted-attempt txn and advanced by review-type tests INCLUDING reruns [R2-41]** · (5) cached-client min-version negotiation (forced refresh or bounded adapter; old-bundle Playwright fixture; no silent validation weakening) · (6) resetEpoch binding on queues/attempts/jobs/completions (mismatch ⇒ reject/cancel; reset cancels pending jobs) · (7) the kill-switch label law (R2-32/r48 final: fail+correct write while OFF, proven freezes, reruns follow the same OFF law [R2-41], cold-start HOLD, stamped config-version governs through completion; the SAME law governs a per-assignment/class disable via `reviewGateEnabled` — default true; precedence global-then-assignment; field contract on DF2-11 [FF1-14/r50-H4]) · (8) 12h logical expiry vs async physical TTL, transactional expiry-marking + redaction.<br>**RULES LINEAGE [r49-B3]: the NAMED additive artifact `audit/deepfix/task3/firestore.review_v2.rules` — protected fields = the SIX `review*` labels [r56] + the queue-identity records + grading-job rows (server-only writes; owner-client DENIED); field-level immutability clauses; emulator matrix; enters the reviewed lineage BEFORE D8g's R3 (the DF2-44 final artifact re-baselines on top); deploys INSIDE the dark train ahead of activation.**<br>**DEPLOY (R2-31 choreography):** dark-deploy ALL surfaces `enabled:false` → verify the complete callable/rules/index/client set → backfill + bounded delta (Track B, David-authorized) → **ONE audited cohort-wide flip of `system_config/review_v2.enabled`** (David) → post-activation monitoring window.<br>**CERT:** the 10-case dual-class oracle + the R2-38 mixed-posture cases (A-ON/B-OFF both directions · concurrent submits · mid-day disable edit, snapshot governs) [r48/r51] · OFF-parity (kill-switch path) · ×3 posture fixtures · the R2-15/16/17/41 stamping truth-tables ×attempt-type (live/rerun × new/review; underflow top-up fixtures) · the R2-42 composition fixtures (LRT-remainder determinism · effectiveTestSize edges · invariant-check violation ⇒ prefix-preserving fallback + ops_metrics event) · **the R2-47 STRUCTURAL fixture (LAP coverage + per-day tiling [r58 — the fixed-cycle claim was falsified; lap-based is the certified property]; re-run against the PRODUCTION composer + a two-day cursor-continuity assertion in the shadow sweep)** · the R2-43 resting-guard fixture (challenge accept on a resting word: grade fixed, status untouched) · clamp/strata fixtures · force-pass resolver edges (A1 v-ix) · backfill expected-vs-actual at the watermark (Track B1) · LIST-END cases [R2-39, r53 residue fix]: no-new-words review-only day-advance (no phantom Day N+1) · the next-list END-SCREEN handoff (`listFinished ∧ nextListId` — ~~cyclingEnabled/cyclingSourceClassId~~ retired with cycling) · the LIST-END LAW case: zero-new-words day advances on the review test alone, counter/rotation/streak continue · streak fixtures: same-date multi-advance · Friday→Monday · weekday gap · weekend+weekday combined gap · concurrent-class · idempotent retry · reset [R2-21/r51-H1; build home DF2-10(9)] · the EXTENSIVE Playwright suite [R2-5: old-bundle, dual-class, wall/retake/restudy loops, force-pass live-dissolve, recovery/expiry, review-first] on 25WT.<br>**MONITORING [R2-18]:** soak window = 7 study-days post-flip; per-signal ABORT rule = flip `enabled:false` when any signal crosses its B1-derived threshold (owner: David, or WSL on standing instruction); restore-rehearsal targets RPO ≤ 1 min (PITR granularity) / RTO ≤ 1 h [FF1-05]. Signal set WSL-owned (wall-rate vs B1 expected · force-pass volume · label-write failures · score drop · RERUN-GRADUATION volume [R2-41(g)] · `composition_fallback` rate + PRIORITY-SATURATION days [R2-42/46 — both from the server-only `ops_metrics` sink, never client-writable system_logs] · "stuck despite ≥92%" as R2-22's reopen trigger); CS tickets = David's primary; numbers set from B1 baselines.<br>**ROLLBACK:** `enabled:false` (instant, state-preserving under the label law); PITR enabled pre-launch (verified + one restore rehearsal); full-set redeploy = the deep fallback.<br>**PRE-IMPLEMENTATION DELIVERABLES [r49-H6]: queue + PRESENTATION + completion doc locations/schemas (BOTH record levels [r50-B3]) + creation/claim txn + immutable-field list + presented-set hash format + retry responses + retention/reset cleanup + **the H8 BASELINE RE-SIM [recast R2-47 — no fairness oracle]: plateau pools · wall rates · exposure percentiles · saturation days · return slugs · underflow + R2-41 rerun scenarios, consuming Track B1's baseline; + the STRUCTURAL CYCLICITY fixture [r49-H5/R2-47]** (bounded authoring, owned here). GATES — SPLIT [r49-B4]: (1) IMPLEMENTATION AUTH = Track A complete · Track B design + read-only script rehearsal 🔍6-converged · the 8 contracts + H6 schemas (incl. the H8 fairness re-sim) + the rules artifact frozen → (2) DARK BUILD/DEPLOY (all surfaces inactive) → (3) POST-BUILD 25WT PRODUCT REHEARSAL (full UI/backend matrix + Playwright — impossible pre-build; ON-behavior via `rehearsalClassIds` [the sandbox-only config override, David 2026-08-02]; localhost client → deployed dark backend; hosting NOT deployed) → (4) David's 26SM backfill go → (5) David's ACTIVATION go (the flip).** | 11_ R2-1..R2-47 · 13_ · r46-52 | functions+hosting+rules+config 🔵⚠️ |

## Wave 2 — Unification increment 1 (byte-identical; DARK) [gates: DF2-03+04 done, DF2-10 certified] 🔍

| ID | Task | Notes / gates | Surface |
|---|---|---|---|
| DF2-20 ⛔ | **Client-only `initializeDailySession` core extraction** → `deriveSessionState` (entry) — constraints C1/C2/G4 (ordered assembly, double-call preserved)/G5/G7/A2/A3; anomalies returned (G3); ~~`navigationMode` constant `'forced'`~~ DROPPED from the output contract [r50: per R2-27 the mode machinery was never built — no such field exists in current code to extract]; differential CI green (flag matrix + both record shapes). Exit channel = DF2-31 (G0 owned there) | ARCH §8/§9 verbatim | hosting 🔵 (no visible change) |

## Wave 3 — Container + exit channel + messaging (BUILD + VALIDATE; CONTAINER production exposure = DF2-60/61's single container-train event [DECIDE-0 CLOSED (b), R2-25] — the review redesign exposes earlier at DF2-14's OWN flip; standing exceptions on their own gates) 🔍

> **Ship-model resolution (C1/C2):** this wave BUILDS and validates on 25WT + preview. What reaches production, when, is
> ~~🧭 DECIDE-0~~ **CLOSED: (b) SINGLE-TRAIN [R2-25]. Historical framing:** (a) *incremental line* — Wave-3 deltas go live per approved-delta list after DF2-34 (Fable-C position;
> ARCH §8/§9 shippable-increment doctrine; earlier CS relief), or (b) *strict single-train* — container stays dark behind an
> activation gate until DF2-60 (Codex position; matches "ship together" literally). Approved-delta list either way:
> messaging rows, one-affordance Dashboard, chrome/derived copy — each a NAMED exception to byte-identity (§0).

| ID | Task | Notes / gates | Surface |
|---|---|---|---|
| DF2-30 ⛔ | **One `<SessionStage>`** — enum fold, sub-views, collapse per MAP §15 **referencing the FULL MAP §3 chrome+modal inventory** (SessionMenu, Watermark, card-settings, drawer, Complete variants — nothing dropped untracked); chrome phase- + threshold-derived copy (~~mode-aware~~ — no modes, R2-27); `session_state.phase` DEMOTED; unify the inconsistent test-error exits (MCQ returnPath vs Typed navigate(−1), MAP §14.7); absorbs [E2-machine] (~~[E1]~~ moved → DF2-46 per the Codex fold note; §3 rows 175-176 govern) | | hosting 🔵 |
| DF2-31 ⛔ | **Exit channel** `deriveWriteOutcomeView` — explicit `advanced:false`+reason (A1); DSF `day_guard_rejected` gap fix. **Premise CORRECTED [Fable-2 M3]: the `review_recorded` return (foundation:1584-1590) DOES carry txn-time `reviewMode` (+progressDay) — consume it, never stale pre-submit state. The response fields it renders are EXACTLY DF2-10's H4 schema (single source — the old "also receives `reviewOnlyReasons`" claim is superseded [r43 H4]); post-D-1 `reviewMode` is legacy-compat passthrough only** | Codex N6 · Fable-2 M3 · r43 H4 | hosting 🔵 |
| DF2-32 ⛔ | **Messaging register rows 4-14 [rows 1-2 RETIRED by D-1; row 3 RETIRED by R2-11 (engagement dead); numbering stable]** (quick-win rows re-home from DF2-07; row 15 re-homes HERE — ~~DF2-50~~ dissolved [R2-24]; its retake variant stays a blocking state) + **row 16: the forced retake-wall (`review_retake_required`) message** [C8 — now standalone: the row-2 rolling-average disambiguation is moot]; **message ⇔ reason 1:1 binding table + per-reason fixture ORACLE** (a reason's copy never renders for another reason) [Opus-A R-E]; `06_MESSAGING_COPY.md` (~~ko/en~~ ENGLISH-ONLY [R2-44]) reviewed first w/ Opus-B copy rules (no "frontier/segment" jargon → "next 20 words unlocked"; quarantine reframed non-disciplinary; TZ-honest per N8) — **and it SUPERSEDES §12.3's draft strings** [Fable-2 M5]; `carriedFrom` attribution derives from resolve `sources` (pre-authorized inference); **event rows w/ seen-markers = 4/14/15-advance ONLY — row 16 and row 15's retake variant are BLOCKING STATES, re-derived visible on every load, never seen-markered** [Codex N7, Fable-2 L2] | G2 net-new UX | hosting 🔵 |
| DF2-33 ⛔ | **Dashboard one-affordance** — hero + per-list unified on the ONE derivation; two-done-authorities closed; **state whether Dashboard gets a READ-ONLY assembly variant** (the G4 ordered-write pipeline must not run writes from a dashboard render) | Fable-B claim-2 note | hosting 🔵 |
| DF2-34 ⛔ | **Validation matrix + 25WT rehearsal (canary DEAD, R2-5/24):** enumerated live-routed E2E matrix: every exit status × both test types × crash-recovery × every modal path × the 10-case dual-class oracle [r48]; the EXTENSIVE Playwright suite [R2-5]; streak weekend-skip + progression-basis verify [R2-21] | Opus-A R-D; entry fixtures are NOT sufficient for the UI swap | 25WT rehearsal (CONTAINER-train matrix — exposure at DF2-60/61's event; DF2-14's product rehearsal is its own stage 3) |
| DF2-36 ⛔ | **G-QUAR minimal blocking screen on the LEGACY UI** — build, deploy, behaviorally verify **BEFORE DF2-40** (fail-closed hard gate; the container absorbs it later). "DF2-32 or minimal" is not a gate | Codex S2 | hosting 🔵 |

## Wave 4 — Substrate [execution order post-R2: 41 → 40 → 46 → 44 → 45; ~~47/42/43~~ dissolved (universal model + G-DUE cancel)] 🔍
**Entry gates (state-touching execution only — design authoring started back in Wave 0-1 [Fable-2 M1]): DF2-05's
corrected P5-required set closed (R1 + inflation confidence; R8 stamp shipped via DF2-10; R7 is CONT-A's) · DF2-36 live ·
D3 certified + C1 live (inherited).**

| ID | Task | Notes / gates | Surface |
|---|---|---|---|
| ~~DF2-47~~ | **DISSOLVED — the per-class-mode/COEXISTENCE architecture is OBSOLETE (R2-27 Q2): ONE universal model** (day-structured; free within the day; backward re-study/re-test; both-tests day-advance). ~~The F2 cycling/lap position question SURVIVES and moves to [D7/P9]'s own track~~ **F2 DIED with cycling's retirement (R2-39)**; dual-enrolled same-list = R2-36's accepted ambiguity + r48's class-scoped queue/evidence/concurrency contracts (on DF2-14) | R2-24/26/27 · R2-36 · r48 | — |
| DF2-41 ⛔ | **Frontier adjudication census** (`07_FRONTIER_CENSUS_PLAN.md` — 🔍 its own convergence = hard gate): 129 divergent / 27 active dispositioned via the FULL CS ritual (read-only → 25WT rehearsal → named per-student ledger → David authorization → derived/verified per-student values, never blanket max → sweep before/after) | Opus-A R-H | scripts 🔵 |
| DF2-40 ⛔⚠️ | **[D4/P5] Canonical migration + flip** — original gate text governs (MASTER D4): --catchup MED-3/4 · toolchain retarget (**DF2-40 OWNS it, pre-flip hard gate — `data-integrity-sweep.mjs`, `census-i4-pairing.mjs`, `manual-pass.mjs`**; DF2-62 = universal-model doc/CS residuals only [FF2-13]) · demotee ledger · 25WT rehearsal · fresh census+backup before/after · **carry `reviewMode` into canonicalDoc at hydration [D-1 NOTE: post-throttle-removal this is FROZEN LEGACY METADATA — carry read-only or drop-with-note, decided at the 25WT rehearsal; no reader may depend on it] · apply the FIX-1 engagement gate to bestCsd** [C5] **(R2-11 DISPOSITION [r50-H1]: KEPT DELIBERATELY as a MIGRATION-TIME selection rule over LEGACY-era rows — their certified anchor semantics, mirroring the reviewMode frozen-legacy treatment — NOT a live engagement reader; forward evidence law = uniform `passed===true` (DF2-10(2)); no post-launch reader consumes engagement)** · off-peak David-authorized · **TOCTOU discipline: re-verify twi INSIDE the migration transaction + drift-diff vs pre-census that ABORTS on any change (the 27 keep studying)** [R-F] · **restore window: clean restore ONLY until the FIRST post-flip completion — named monitor owns that boundary** [R-G] · quarantine=0 acceptance · client-reader cutover per P5 plan | dual-purpose: single writer + frontier home | migration 🔵⚠️ |
| DF2-46 ⛔ | **Server unification + twin retirement** (the missing increment — Codex R1, Fable-A H1). **Shape [Codex N11]: share PURE POLICY PRIMITIVES (a named `deriveCompletionDecision` + the DF2-08 module) — the transactional writer is NOT routed through the entry-VM object**; `deriveSessionState` stays the entry VM, `deriveWriteOutcomeView` the exit renderer. Routes: forced `completeSession` policy + `getDayNewPass` + ~~engaged-paired-review reader~~ (retired R2-11 [r50-H1] — its call sites route to the uniform `passed===true` evidence reader; DF2-10(2) removes the legs, DF2-46 deletes the residual shell) + challenge/override policy + **the anchor-shadow validator's policy legs (rewire, not just test-continuity)** [Fable-1 residue]; **absorbs [E1] `isDayComplete → {complete, advances}`** [Codex fold note — moved from DF2-30]; consolidate the **5-site allocation math (studyAlgorithm:107 · foundation:1151 shadow leg · foundation:1378 completeSession recompute · foundation:2211 challenge-advance · db:3061)** [Fable-2 M4 — cites re-derived]; **retire the flag-suppressed client twins** (progressService:160/570/663) + legacy client progression writers + **the D-1 residuals: the legacy-compat `reviewMode` response passthrough and any remaining interventionLevel fields/readers (float fate RESOLVED by D-1 — the vocabulary dies with the throttle; challenge-advance sizing went FIXED back at DF2-10's rider)** + **B1 STEP 2: DELETE the hardened `markReviewComplete` route after a measured zero-call drain window (set re-derived)** + **the legacy per-word status batch (display-only post-redesign, R2-12c)** + **the DORMANT cycling surfaces (`cyclingEnabled` reads · `resolveEffectiveCycling` · G-CYCLE legs — R2-39, zero live usage per the 2026-07-27 probe; `nextListId` KEPT as the end-screen next-list pointer)** + **absorbs ~~DF2-43~~'s surviving contracts (challenge/override/manual-pass advancement + force-pass server home + canonical audit rules)**; equality-tested artifacts; full server re-cert (deploy set re-derived per §0 rule — WIDER than DF2-10's 10) | after DF2-40, before 43/44 | functions 🔵⚠️ |
| ~~DF2-42~~ | **DISSOLVED — G-DUE CANCELLED (R2-27 Q4)**; review selection = DF2-14's rotation | R2-27 | — |
| ~~DF2-43~~ | **DISSOLVED with the frontier concept (R2-24/27): day advancement stays the completeSession path.** Surviving content folds into DF2-46: the challenge/override/manual-pass ADVANCEMENT contracts + the force-pass resolver's server home + canonical-write audit rules | R2-27 | — (→DF2-46) |
| DF2-44 ⛔⚠️ | **Rules lineage** (`05_RULES_LINEAGE.md`): **44a = [D5/P6]** with the FULL choreography restored by pointer (MASTER D5 + roadmap Track-2 row 5: `TEACHER_PROVISIONING_ENABLED` functions-redeploy FIRST → named P6 artifact (`audit/deepfix/task3/firestore.p6.rules`) + matrix + bundle-grep → rules deploy → THEN flip `ANCHOR_VALIDATION_ENFORCE` → starts P7 clocks; F1 Signup-train re-apply; 26SM quarantine=0 acceptance; **≥14d M4 shadow at ≈0 false-rejects — the criterion, not just the clock** [Fable-2 L6]) · **44b = universal-model clauses [FF2-08/FF3-03]** — the `review*` label server-only locks + queue/grading-job protections (ABSORBS DF2-14's `firestore.review_v2.rules` lineage entry; the final artifact re-baselines it) · **the FINAL artifact re-baselines and SUPERSEDES the P10d draft; D8g's R3 is RE-POINTED at it; HARD interlock: no R3 deploy until this lineage is final** [Fable-B corr.2, Codex R5] · D8's own server-flag flips are foundation pin-moves — the "parallel" track SERIALIZES on the deploy artifact · off-peak | rules 🔵⚠️ |
| DF2-45 ⛔⚠️ | **[D9/P7] Retirement** — ≥14d post-rules + ≥7d zero `legacy_write_denied`; apply `phase7_retirement.patch`; delete `class_progress` (backups first); off-peak | verbatim | functions+data 🔵⚠️ |

## Wave 5 — Universal-model nav UI [post-R2: one surviving build (DF2-51 past-day browser + phase toggle) + teacher monitoring; single-train gates per R2-25] 🔍

| ID | Task | Notes / gates | Surface |
|---|---|---|---|
| ~~DF2-50~~ | **DISSOLVED — no free branch exists (R2-27 Q2)**; the universal model's derivations are DF2-20/30/31's single line; "review never gates in free mode" is DEAD (both tests gate everywhere, R2-24c/R2-26) | R2-24/26/27 | — |
| DF2-51 ⛔ | **RESCOPED: the PAST-DAY BROWSER + within-day phase toggle** (the universal model's only new nav UI, replacing NavigateHub/picker): past days list → re-study (flashcards) + re-test (`type:'retest'`, non-advancing, gradebook-preserving, AI-metered, STAMPING per R2-41 (rerun review regenerated pure-random; rerun graduation tested-correct-only) [R2-20/26/41]); within-day review↔new freedom [R2-26 Q11]; **builds INSIDE DF2-14's train on the LEGACY UI (detached from DF2-33/the container — the launch delivers it at the flip [FF2-01]; the container absorbs it later)**; C2-audited | R2-20/26 · FF2-01 | hosting (in-train) 🔵 |
| ~~DF2-52~~ | **DISSOLVED — no `navigationMode` lever exists (one model for everyone, R2-27 Q2)** | R2-27 | — |
| DF2-53 ⛔ | **Teacher monitoring (universal model)** — held/walled-students panel follow-through + Day-number pacing (unchanged semantics, R2-24b) + mastery + per-student label analytics (`reviewFailCount` etc.); single-model view (mode machinery dead) | Opus-B decisions 1/5 | hosting 🔵 |
| DF2-54 ⛔ | **PDF segment-based** + **PMv2 rail ruling 🧭** — PMv2 explicitly **non-progress / non-gradebook by default** (Codex caveat 4) or it recreates the fork; shared `practiceMode` rail | | hosting 🔵 |
| ~~DF2-55~~ | **DISSOLVED — no mode flips exist to explain (R2-27 Q2)** | R2-27 | — |

## Wave 6 — Train + activation 🔍 (final full-panel convergence)

| ID | Task | Notes / gates | Surface |
|---|---|---|---|
| DF2-60 ⛔ | **Container-train assembly (single-train per R2-25):** build-identity binding (golden/differential evidence ↔ deployed bundle ↔ callable set+config ↔ rules artifact), one attested identity for the CONTAINER line (the DF2-14 review launch is its own earlier train). ~~full-cohort single-step exposure FORBIDDEN~~ **SUPERSEDED (R2-24/25): cohort-wide single-flip IS the chosen posture — the compensating controls are DF2-61's rehearsal bars + monitoring + kill switch** | R2-25 · r48 | hosting 🔵 |
| DF2-61 ⛔ | **CONTAINER-line activation (Wave 6) — the review flip is NOT here: DF2-14 owns and executes its own flip, pre-Wave-2 [FF2-02].** This card = the CONTAINER train's exposure: 25WT container rehearsal green (explicit pass bars) → dark-train verification → the single-train container exposure event (R2-25) → its monitoring window | R2-24/25 · FF2-02 | container exposure 🔵⚠️ |
| DF2-62 ⛔ | **Docs + CS readiness residuals [rescoped FF2-13/FF3-03]** — SUPPORT_RUNBOOK + help-site + CS toolchain speak the UNIVERSAL MODEL (one model; past-day/retest semantics; the `review*` label vocabulary; canonical retarget lives in DF2-40), token-diag predicate; **CS-metrics instrumentation**: baseline = **the 4 weeks immediately PRECEDING DF2-07's Wave-0 ship, reconstructed from dated SUPPORT_RUNBOOK entries — never a post-DF2-07 window** [Opus-2] → ≥60% reduction target on the quick-win classes → in-app "still confused → contact" tap for direct deflection measurement | quick-win TA updates already shipped w/ DF2-07 | 🔵 |

## Parallel deepfix1 tracks (own gates; serialize on shared artifacts)

- **[D6/P8] CONT-A** — shippable early; **ship BEFORE Wave 3 or fold INTO DF2-30** (same DSF Complete surface — don't run concurrent).
- **[D7/P9] Cycling** — post-rules; **owns the F2 cycling/lap position question (inherited from dissolved DF2-47)**; universal-model day semantics apply. **RECONCILIATION CHECK REQUIRED [David 2026-07-27]: the cycling design (`docs/design/LIST_CYCLING_DESIGN.md`, 2026-07-08 + the built `cyclingEnabled`/`resolveEffectiveCycling`/G-CONT surfaces) PREDATES the universal model + the review redesign. Read-only reconciliation runs ALONGSIDE TRACK A against four named collision surfaces: (1) list-end day semantics — no-new-words days must day-advance on the review test alone (the phantom-Day-N+1 class; DF2-10's `deriveNoScoreEligibility`/`list_complete` legs must state it); (2) the lap position model — must not repeat `nwei` (anchor reconciliation selects greatest-nwei; design option B rejected for exactly this) AND must now define what a lap does to `logicalDay` in the queue identity; (3) cycling × the rotation — a lap-2 re-introduced word can be simultaneously new-intake and review-pool: composition/graduation collision needs a rule; labels are epoch-scoped and a lap is NOT an epoch bump (history persists — confirm intended); (4) `cyclingSourceClassId` cross-class unlock — apply the R2-36/R2-38 progress-is-individual treatment. Output: either "orthogonal — current behavior unchanged" or a short decision list for David; cert cases land in DF2-14 stage 3 EITHER WAY.** **⟶ RESOLVED (R2-39, David 2026-07-27): SAME-LIST CYCLING RETIRED — probe showed zero live usage (302 classes, 0 enabled); surfaces (1)/(4) collapse into the LIST-END LAW + the next-list end-screen affordance (`nextListId` kept as pointer); surfaces (2)/(3) die with the lap model; dormant cycling code removed at DF2-46.**
- **[D8] P10 chain** — R3 re-pointed at the DF2-44 final artifact (hard interlock); D8a/D8e flag flips = foundation pin-moves, serialized with the wave track's deploy discipline.
- **PMv2** — David-locked plan; rail ruling at DF2-54; non-progress/non-gradebook condition binding.
- **M4 shadow clock** (ends ~2026-08-01) — gates DF2-44a; continuity asserted at every pin-move (DF2-04); **throttle/allocation legs re-derived at DF2-10's D-1 rider**.
- **Grader thread** (DF2-06) — interleave rule with DF2-10 (shared `writeAttemptTxn`; DF2-12's client flip sequences separately per its card [P3-Opus-2 M7]).

## 3. Reconciliation table (v4 — reconstructed in full at v3 [Fable-2 H1]; round-3 rows added; the complete deepfix1 disposition record)

| Deepfix1 item | Disposition |
|---|---|
| D4/P5 migration | **ABSORBED → DF2-41 (census FIRST) + DF2-40** (hydration items, restore-window + monitor, TOCTOU verify-at-write, toolchain ownership all on the card; gates travel + v3 ADDS protections) |
| D5/P6 rules | **ABSORBED → DF2-44a** (full choreography by pointer incl. quarantine=0, ENFORCE-flip-last, P7-clock-start, ≈0-false-rejects criterion) |
| D6/P8 continuation | **PARALLEL** — shippable early (hosting-only, B4 closed); ship BEFORE Wave 3 or fold INTO DF2-30; DF2-07(c) copy conditions on `nextListId` |
| D7/P9 cycling | **RESOLVED — SAME-LIST CYCLING RETIRED (R2-39, 2026-07-27; zero live usage)**: the LIST-END LAW + next-list end-screen affordance replace it; F2 dies unanswered; dormant code removed at DF2-46; list-end cert cases ride DF2-14 stage 3 |
| D8 P10 chain | **PARALLEL** — R3 re-pointed at the DF2-44 final artifact (hard interlock); D8a/D8e flag flips = foundation pin-moves, serialized on the deploy artifact per §0 rule |
| D9/P7 retire | **ABSORBED → DF2-45** (verbatim: both clocks, `phase7_retirement.patch`, backups-first, irreversible) |
| PR-3 binary throttle (shipped 07-16, deepfix1 C) | **SUPERSEDED by D-1 (2026-07-26): the throttle is REMOVED entirely — retirement rides DF2-10's pin-move; the review-pass gate becomes the sole struggling-student mechanism; residual vocabulary retires at DF2-46** |
| E1 isDayComplete dispatch | **ABSORBED → DF2-46** (`deriveCompletionDecision` — moved from DF2-30 per Codex fold note; a UI container doesn't replace server completion policy) |
| E2 lifecycle machine + dead 7-export API | **ABSORBED → DF2-30 (machine) + DF2-02a (the API, incl. sessionService.js:268 dead G-PASS copy)** |
| E3 resolveAssignmentPolicy | **ABSORBED → DF2-08** (gate-override vs "(after D5)" APPROVED — the module must precede its first consumer DF2-10; drift-normalization split per N3) |
| E4 free-nav 🌟 | **→ THE UNIVERSAL MODEL (R2-24/26/27)** — delivered via DF2-14 (review-first + past-day browser, DF2-51 in-train) + the container line (Waves 4-6); ~~design track (47/42d/43-spec)~~ dissolved; container exposure = DF2-60/61's event [r50] |
| Banked Item A (review-pass gate) | **ACTIVATED → DF2-08/10/11** (Wave 1, per G6; deviation note on the card) |
| Banked Item D (unified container) | **ACTIVATED → DF2-20/30-34** (Waves 2-3; §8/§9 constraints verbatim) |
| D3.5 remediation R1-R16 | **CARRIED → DF2-05** (v2 numbering; Wave-4 entry subset corrected per N1; R8 stamp rides DF2-10 under the M7 contract) |
| Grader round 2 | **CARRIED → DF2-06** (interleave rule; verbatim-intent confirm at go) |
| Housekeeping §4.3 (a-d) | **OWNED → DF2-0H** (Wave 0) + D3.5 report/FINDINGS reconciliation with DF2-05 |
| Token-thread residual (diag-script predicate) | **CARRIED → DF2-62** |
| — NEW (no deepfix1 ancestor) | **DF2-46** (server unification/twin retirement) · ~~DF2-47~~ (mode-record contract — dissolved R2-24/27) · **DF2-07/32 messaging** · **DF2-35/53 teacher UX** (~~55~~ dissolved R2-24) · **DF2-36** (G-QUAR screen) · **DF2-12/13** (attempt-write durability, D-3) · **DF2-0P** (starvation probe, last-exchange (b)) |

## 4. Open decisions register 🧭 (David) — items 1/2/6/11 reframed per Opus-2 round-2

> **⚡ DAVID DECISIONS 2026-07-26 (D-1..D-4) — now FULLY FOLDED (at v4; ripple complete; r44 VERIFIED — §8):**
> **(D-1) REMOVE the review-score throttle entirely** — redundant under the review-pass gate. The binary
> reviewMode/interventionLevel machinery (enter <0.30 / exit >0.50, hold-csd throttle leg, allocation scaling) is
> RETIRED in forced mode; the review gate becomes the sole "struggling student" mechanism. Item 2 (throttle-day gate)
> DISSOLVED — no throttle days will exist. Item 6 (float fate) RESOLVED — the vocabulary dies with the throttle.
> Ripple (all landed): DF2-10's D-1 rider (removal rides the pin-move; ~27 released organically + CS comms; M4 legs
> re-derived; challenge-advance sizing fixed) · messaging rows 1-2 retired (DF2-32 + ARCH §12.3) · DF2-07(b)/(e)
> rescoped (50% copy never ships; help-site 30% text REMOVED) · review-test-size rule → item 15 · DF2-35 reason set ·
> DF2-46 residual retirement · DF2-40 C5 frozen-legacy note · §3 PR-3 supersession row.
> **(D-2) Graduation only on a PASSED review test** — a failed review graduates ZERO words (today's
> floor(size×score)-on-any-score formula dies). The within-pass basis + OFF-posture scoping = ~~item 15 (OPEN)~~ **CLOSED [R2-13/15/29]:
> fixed teacher-set 30; the R2-29 formula IS the within-pass basis; OFF = the kill-switch label law (~~DF2-42d~~ dissolved).**
> **(D-3) ALL attempts recorded, pass or fail, MCQ+typed, new+review** — PROGRAM INVARIANT (§0). Verified already true
> today for graded attempts; residual client-write windows close via DF2-12/13; grader-outage residual acknowledged.
> **(D-4) Gate-OFF semantics (closes r43-H2's amnesty half + N5):** OFF changes NOTHING retroactively — a past failed
> review never becomes completion evidence; the student retakes (which under OFF auto-passes) to advance. Implemented
> as a UNIFORM `passed===true` evidence reader (+ the DF2-10 read-only census precondition); re-enabling never
> re-blocks an advanced day. The kill-switch MECHANICS half of r43-H2 is spec'd on DF2-10's card.

1. ~~DECIDE-0 · Ship model~~ — **CLOSED (David R2-25, 2026-07-26): (b) SINGLE-TRAIN** ("without the UI, the other changes can't ship together either"); container visibility waits for DF2-60's attested train; canary re-homes there; standing exceptions (DF2-01/07/36/CONT-A/35) still ship on their own gates per the scope note below. Original framing kept for record — WHAT: when validated Wave-3 deltas (messaging rows, one-affordance Dashboard, chrome/derived
   copy) reach 26SM. **Scope note [Fable-2 H2]: the standing exceptions (DF2-01, DF2-07, DF2-36, CONT-A, DF2-35) ship on
   their own gates under the CLOSED (b) model — it means "no CONTAINER exposure before DF2-60," not "nothing visible ships."**
   OPTIONS: **(a) INCREMENTAL** — each delta live as it clears DF2-34. *Gain:* CS relief months earlier; each delta
   individually reversible. *Cost:* 26SM runs intermediate build states; more validation/rollback surfaces. **(b)
   SINGLE-TRAIN** — container visibility waits for DF2-60's one attested build identity. *Gain:* exactly one production
   identity to validate/roll back. *Cost:* every container-borne fix waits for Wave 6, and the canary re-homes to DF2-60
   (full-cohort single-step exposure is forbidden either way). RECOMMENDED: **(a)**, conditioned on the byte-identity
   falsifier holding for every non-delta path (Fable-C + Opus-B; round-1's back-loading critique weighs against (b)).
   BLOCKS: Wave-3 production exposure only — **it gates NOTHING in Waves 0-2** [r43].
2. ~~DF2-10 · Throttle-day review gate~~ — **DISSOLVED by D-1** (no throttle days will exist; the exemption list no longer
   carries the question). Tombstone kept for numbering stability.
3. ~~DF2-02a: dead levers~~ — **CLOSED (R2-4/R2-15, 2026-07-26): the never-wired `reviewTestSizeMin/Max` are
   DELETED and REPLACED by the teacher-set `reviewQueueSize`/`reviewTestSize` fields (DF2-11's menu simplification).**
4. DF2-06: grader round 2 — **intent DECIDED [R2-19]: SEMANTIC EQUIVALENCE** (verbatim-English family = accepted, regression-encoded); residual = deploy-go only (ask-at-implementation).
5. ~~DF2-42d G-ENGAGED question~~ — DEAD with G-DUE (R2-27) + engagement retirement (R2-11).
6. ~~DF2-46 · interventionLevel float fate~~ — **RESOLVED by D-1** (the vocabulary dies with the throttle: no new floats,
   readers retired at DF2-46, challenge-advance sizing goes FIXED at DF2-10's rider — the fixed value is named at
   implementation and shown to David pre-deploy). Tombstone kept for numbering stability.
7. ~~Re-test gradebook semantics~~ CLOSED (R2-20/26: `type:'retest'`, non-advancing, ~~label-neutral~~ ⟶ STAMPING per R2-41, original pass = the accountability score; teacher toggle reveals reruns [R2-40(g)]). Residual: past-day-browser layout polish (C2).
8. ~~DF2-53: free-mode pacing metric~~ — **CLOSED (David R2-24, 2026-07-26): pacing = the existing `dailyPace`; One Day = one pace increment; teacher surfaces read Day numbers exactly as today ("nothing changes for pacing"). Mixed-class view shape: **CLOSED — the universal model is RATIFIED (R2-24/26/27 explicit answers; O2-03 satisfied); the student steers via the persisted Dashboard focus (R2-36); teacher surfaces read Day numbers as today.**
9. DF2-54: PMv2 rail ruling — bound by the non-progress/non-gradebook default condition.
10. ~~DF2-61: pilot class selection~~ — **CLOSED (David R2-24, 2026-07-26): NO pilot — free-nav activates for ALL students simultaneously; DF2-61 rescopes to **CONTAINER exposure only [FF2-01] — DF2-14 owns the review flip; no pilot anywhere.**
11. ~~Forced-mode task selection~~ — **CLOSED (David, R2-20, 2026-07-26): (c)+ — past-day RE-STUDY and RE-TEST both
    granted** (`type:'retest'`, non-advancing, original pass = accountability score). **CONDITION: every AI-grading
    call METERED (per-student + global; counting point = the grading job) → the metric for a future global
    AI-grading limit gate.** Still a NAMED approved delta; scope lands in DF2-30/51.
12. ~~Free-mode challenge/tokens~~ — **CLOSED (David R2-23, 2026-07-26): identical machinery** (segment tests challengeable, same weekly token budget/reset).
13. ~~Streak definition~~ — **CLOSED (David, R2-21, 2026-07-26): PROGRESSION-based** — a streak day = the day was
    completed/advanced (not mere activity); **weekends never break it** (weekend-skip fixture stays at DF2-34).
    ~~Free-mode "progressed" mapping resolves at DF2-50/51~~ — **MOOT [R2-24/21]: one universal model; a streak day = the day completed/advanced, full stop** (~~DF2-50~~ dissolved).
14. **Scope note:** parents/parent-facing surfaces are OUT OF SCOPE for this program (no portal, no notifications) —
    re-raise only if ever wanted [Opus-2 restore].
15. **D-2 within-pass graduation basis + review-test size (resolves D-2's open clause + D-1's size rule).**
    **▶ RESOLVED — the addendum (10_) IS the design, final form: teacher-set 60/30 rotation queue · uncapped
    least-recently-tested slots · four review* labels + two predicates (three strata) · asymmetric stamping w/
    blank=fail · sticky via `reviewLastProvenAt` · recompute-at-92 backfill w/ the r48 exclusion filter · delivered
    by THE LAUNCH (DF2-14). [Historical v1 synopsis removed per r46 fold item / F3-24.] **Round-4 INTERNAL PANEL RAN 2026-07-26 (1 Fable + 2 Opus): "not
    buildable as-is / NO-GO as scoped / not decision-ready" — 5 blockers (cursor statelessness, posture-independent
    legs, inverted unrecovered predicate, pin-move bundling, weak-stratum retake deadlock) + stratified-evidence
    reversal (fill penalty +20.6pp for the <50% band; recovery 21.3% ≈ MCQ chance floor). ALL FOLDED → addendum v2
    (v1 archived): pure day-offset queue, ONE per-class enablement flag for every leg, fixed predicate, UNBUNDLED
    from DF2-10 (own future card DF2-14, staged + circuit breaker), escape valve `review_retake_exhausted` 🧭,
    lastFailedAt-ASC slots, explicit clamp, server-write-only labels bound to attempt creation, backfill options 🧭,
    corrected workload numbers + comms leg. ALL picks CLOSED in the ledger (R2-14/15/26/27); the addendum v4 FINAL is the spec.**
    Options (a)-(d) below retained as the decision record. **ROUND-2 CLOSURE (2026-07-26): David's R2-1..R2-17
    (ledger 11_ §1) resolve the design — teacher-set 60/30 · gate ON@92 all-at-once via `system_config/review_v2` ·
    uncapped least-recently-tested slots [R2-15 CONFIRMED] · asymmetric stamping + blank=fail · sticky (b) ·
    full backfill · hard gate + force-pass. Addendum v3 = the folded spec; DF2-14 carding follows Tracks A/B +
    the 🔍6 convergences.**
    Mechanics today: graduation = floor(STUDIED-segment-size × score) drawn from segment-minus-test-failed on ANY score
    (the studied slice, cap 60 — NOT the tested-30 size; `review-pool-sim.mjs:59` [P3-Opus-1 F4]); MASTERED =
    21-day parole, NEEDS_CHECK returns forever (no permanent exit); daily review = pool/5, capped 60 studied / 30 tested.
    **Steady-state ACTIVE-pool projections — REAL LISTS (v2 2026-07-26, per David; the earlier generic 600w@pace-20
    table is SUPERSEDED — `evidence/review-pool-trajectory.mjs`).** Lists: Base Camp 1,200w · Ascent 1,600w ·
    Summit 800w (TA guide + ACT-104 seed evidence); paces 80/day (INT) · 100/day (FINAL). Cell = plateau ACTIVE pool
    at 50·70·90·100% student avg (plateaus are PACE-INDEPENDENT; pace sets only ramp speed + peak timing):
    | list (intake ends) | today c0/(c) | (a) strict-30 | (a)+adaptive [REC] | (b) all-tested (REJECTED) |
    |---|---|---|---|---|
    | Base Camp 1,200w (d15@80 / d12@100) | 780·612·444·360 | 990·906·822·780 | ≈today: 780·612·444·360 **[REC pre-addendum — superseded as candidate by 10_ (P4-F-8)]** | 780 flat, score-blind |
    | Ascent 1,600w (d20@80 / d16@100) | 1180·1012·844·760 | 1390·1306·1222·1180 | ≈today: 1180·1012·844·760 | 1180 flat |
    | Summit 800w (d10@80 / d8@100) | 380·272·229·209 | 590·506·422·380 | 380·~300·287·290 (trigger-hover) | 380 flat |
    Insights (real scale): **(i) the 60-studied/30-tested caps BOUND graduation throughput** — at most ~900 words can
    rest at once (15 study-days × 60), so Ascent can never hold fewer than ~700 active words under ANY 60-test policy;
    the caps were sized for smaller pools (structural, feeds DF2-42d). **(ii) strict-30 (a) is the worst on every
    list** — Ascent@50% keeps 1,390 of 1,600 words circulating; fair-rotation sighting gap ≈ 23 study days, and the
    real slicer is NOT fair (shadow orbits, DF2-0P) so worst-case is longer. **(iii) (a)+adaptive's curve sits ON
    today's curve wherever the pool is big** — today's pool control, proven-words-only graduation, i.e. at real scale
    the recommendation costs nothing vs today; on Summit it hovers near its 300 trigger instead of following today
    lower (209→290 at 100%) — tune the trigger (e.g. 250 / hysteresis) at spec if picked. **(iv) (b) is no longer the
    floor at real scale (30/day < today's ~60×score) and stays REJECTED (score-blind: identical curve for a 50% and a
    100% student). (v) trajectory shape: ramp at pace to a peak at intake end, plateau ~20 study days later when the
    21-day return loop closes — the table = that plateau (d120 cross-checked).** Full curves (list × pace × avg ×
    policy, hover any day): chart artifact 📈 https://claude.ai/code/artifact/4a0f710a-3fc8-45a3-b18f-fc704141f9ee ·
    data `evidence/trajectory.json`. OPTIONS: **(a) strict** — graduate only correctly-answered words, fixed test 30 · **(a)+ADAPTIVE
    [REC pre-addendum — historical; the live candidate is the 10_ addendum (P4-F-8)]** — graduate tested-correct; test size 30 normally, up to 60 while the pool is oversized (>300 —
    matches TODAY's pool control on every real list, the adaptive curve sitting on today's wherever the pool is big,
    WITHOUT graduating unproven words; Summit trigger-hover → tune trigger/hysteresis at spec) · **(c) keep** floor(segSize×score)
    but applied ONLY on passed tests (D-2 floor) · **(d) PACE-MATCHED (David 2026-07-26)** — daily review queue =
    pace words (rotating position order); graduate floor(queue×score) = correct-tested + excluded-failed + random
    fill from that day's queue (test still 30). Plateaus: BC@80 640·416·192·80, BC@100 500·220·~50·0-100 wave;
    Ascent@80 1040·816·592·480, @100 900·620·340·200; Summit@80 240·~76·0-80 wave·0-80 wave, @100 100·~27·0-83
    wave·0. Caveats (material): with queue 80-100 and test 30, MOST graduates are untested (90% on an 80-queue:
    72 graduate, only ~27 proven — runs against D-2's proven-words direction); at ≥90% the pool periodically EMPTIES
    (review-less days, then synchronized 21-day return slugs — the 0→100→0 oscillation). **Sub-question (OFF-posture scoping) — CLOSED [R2-31/32]: the new basis activates cohort-wide (all 907) at DF2-14's ONE flip; OFF = the kill switch ⇒ legacy graduation while fail+correct labels keep writing and proven freezes.** ~~BLOCKS: DF2-10's pin-move + DF2-42d~~ — delivery = DF2-14; item 3 closed.
16. ~~Shadow-orbit starvation probe~~ — **CLOSED: SKIPPED (David, R2-22, 2026-07-26).** BlindSpot hide FINAL;
    the DF2-14 rotation replaces the orbit-prone slicer by construction; interim bounded; DF2-0P shelved.

### Task-scoped authorization matrix (REISSUED post-r48 — supersedes all prior instruments)

| May proceed NOW (no David, no further review) | Gated on DAVID (ask-at-implementation rule) | Gated on WORK completing |
|---|---|---|
| The folds (DONE 2026-07-26) · STEP 1 EXECUTING (2026-08-01): Track A reports FILED (`trackA/A1,A2,A3,C2`) · Track B plan (`14_`) + B2 probe RUN · DF2-0H · DF2-03 · DF2-47/43-spec-authoring residue REDIRECTED to the unified model (see dissolutions) · DF2-01 (own hosting gate) · DF2-02a (FULL scope — dead-lever branch unblocked, item 3 closed) · DF2-05 (R1 AUDIT only) · DF2-04 (after DF2-03; forced-fields scope) · DF2-08 authoring · Track A (A1/A2/A3) · ~~the D7/P9 cycling reconciliation~~ RESOLVED (R2-39: cycling retired; list-end law + next-list screen) · Track B DESIGN + scripts (non-production) · C2 UI audit | DF2-06 grader deploy-go (intent decided R2-19) · Track B 26SM backfill EXECUTION go · the DF2-14 ACTIVATION flip · any 26SM write | DF2-14 build (= the r49-B4 STAGE-2 dark build): stage-1 IMPLEMENTATION AUTH complete — Track A done · Track B design + read-only script rehearsal 🔍6-converged · the 8 contracts + H6 schemas + rules artifact + the H8 fairness re-sim FROZEN; the 25WT PRODUCT rehearsal is POST-build stage 3, never a build gate [r49-B4/r50-B1] · DF2-10/11/12/13 + DF2-51 + DF2-35's force-pass action: inside DF2-14's train (no separate exposure) [FF2-18/FF2-01/FF2-10] · #10 challenge-reversal: the FOUR r53 conditions — (iv) B1 adjudication replay OPEN · convergence state: r53-r56 + panels executed; r57 = the R2-47 freeze attempt |

DECIDE-0 is CLOSED (single-train, R2-25); the pilot ladder is dead (R2-24); production exposure = TWO named events [r50]: the review redesign at DF2-14's ONE flip (the earlier train) · container-borne work at DF2-60/61's single container event; standing exceptions ship on their own gates.

## 5. Program exit criteria (v5 — post-R2)

One release line live where: the UNIVERSAL MODEL runs for every class (day-structured; free within the day; backward
re-study/re-test; BOTH tests gate day-advance at the class threshold [default 92]); the four review labels + two
predicates are the only failure/proof memory (server-written, rules-locked, backfilled per R2-35/r48's law); every
hold/refusal renders a reason (rows 4-16 — rows 1-3 retired with throttle + engagement); the DF2-14 launch shipped
as ONE audited flip with its 8 contracts certified (incl. the 10-case dual-class oracle) and the monitoring window
closed without the kill switch firing; every G-* gate computed at exactly ONE site (twins retired at DF2-46; the
G-PASS module the only pass predicate); canonical record single-writer (P5) with rules lineage final (P10d
superseded, R3 last); `class_progress` retired; CS toolchain + docs speak the universal model; the CS
baseline→target metric met.


## 6. Round-1 convergence record (2026-07-25/26)

| Panelist | Verdict | Folded as |
|---|---|---|
| Codex r41 | UNSOUND-as-executable / direction SOUND | S1→DECIDE-0+Wave-3 reframe · S2→DF2-36 · S3→41-before-40 · S4→DF2-10 v2 · S5→DF2-02b · R1→DF2-46 · R2/R6→DF2-08 · R3/R4→DF2-40/44 restorations · R5→R3 interlock · F1/F2→DF2-47 · F3/F4→DF2-43 contract · F5/F6→DF2-42d |
| Fable-A | GAPS-FOUND | H1→DF2-46 · M1→DF2-03 · M2→DF2-42d/50 · M3→DF2-43 · M4→exemption fix · M5→reg.6 · M6→DF2-02a · M7→PMv2 track · L1-L6 swept |
| Fable-B | ordering GO w/ corrections | corr.1→DF2-10 · corr.2→DF2-44 · corr.3→DF2-43 gates · corr.4→Wave-4 order+gates · corr.5→DF2-08 · corr.6→Wave-5 gates · corr.7→DF2-42d · corr.8→DF2-06 · corr.9→DF2-31/32 seams · corr.10→DF2-02b |
| Fable-C | CONDITIONAL YES | C1/C2→DECIDE-0+§0 · C3→pin language · C4→exemptions · C5→DF2-40 · C6→§0 never-branch · C7→DF2-02a · C8→row 16 · C9→DF2-44a · N1-N12 swept |
| Opus-A | GO-WITH-CONDITIONS | R-A/B/C→DF2-10+DF2-04 · R-D→DF2-34 · R-E→DF2-32 oracle · R-F/G/H→DF2-40/41 · R-I→DF2-47/55/61 · R-J→§0 off-peak · R-K→DF2-04 · R-L→Wave-4 entry gates |
| Opus-B | delivered-but-back-loaded | quick-wins→DF2-07 · decisions 1-8→register/DF2-35/51/53/55 · copy rules→DF2-32/06_MESSAGING_COPY · metrics→DF2-61/62 · 50/30 fix→DF2-07(e) |

**Round-2 re-converge required before Wave-1 build authorization** (Codex's decision; panel re-runs on this v2).

## 7. Round-2 convergence record (2026-07-26) — folded into v3

| Panelist | Verdict | Folded as |
|---|---|---|
| Codex r42 | SOUND-WITH-GAPS, don't authorize Wave 1 yet; expected next verdict GO | N1→DF2-05/10 (R7 out, R8 stamp rides the pin-move) · N2→DF2-08 boundary + §0 transitional wording · N3→DF2-08 drift split (census + 🧭 default decision) · N4→provenance-typed authoritativePassed · N5→kill-switch rollback fixtures ×3 · N6→response fields on DF2-10 (supersedes client inference) · N7→row 16 = STATE · N8→TZ-honest copy · N9→DF2-07 per-reason oracle ship-gate · N10→DF2-43 gates += 46/42 · N11→DF2-46 pure-policy shape + E1 move · N12→DF2-42d post-47 recheck · fold notes→orientation §3 row, counts, set +resetProgress+version, coordinated choreography, DECIDE-0 non-blocking clarifications |
| R2 Fable-1 (fold fidelity) | Fold faithful; 4 residues | anchor-shadow rewire→DF2-46 · E1 rationale→DF2-46 (moved, per Codex) · 15→16 counts fixed · cosmetics swept |
| R2 Fable-2 (fresh hunt) | One more bounded structural pass | H1→§3 reconstructed + v2 archived (`_archive/`) · H2/H3→delta mechanism TOTAL + DECIDE-0(b) scoped + canary re-home · H4→DF2-10 deviation note · H5→set = 10 · M1/M2→design track + DF2-02b/35 moves · M3→DF2-31 premise corrected · M4→5-site allocation cites · M5→06_MESSAGING_COPY supersedes · M6→orientation §2 + E3 override note · M7→DF2-07(c) nextListId condition · L1-L7 swept (incl. DF2-0H housekeeping row, R3 gloss, L6 criterion) |
| R2 Fable-3 (cross-doc) | Coherent core, seams open | §12.1 supersession banner + §11.4/§12.3/MAP annotations (ecosystem edits) · orientation §3 row + counts · MASTER pointer amendment · task-list §3 reword · baton reconciled by Codex · r42 citation added |
| R2 Opus-1 (safety) | GO-WITH-CONDITIONS (Wave-0/1 build) | C1→DF2-07(b) binding + oracle ship-gate · C2→soak/abort/rollback-pin instantiated + full-set rollback rule · C3→§0 derivation rule · C4→DF2-08 deploy fence |
| R2 Opus-2 (product) | Product-ready pending 6 closures | (e) help-site 30→50 MUST-FIX → DF2-07(e) · DF2-35→Wave 1 · DF2-55 blocking interstitial · register 1/2/6/11 reframed + 14 added · DF2-61 X=60% · DF2-62 baseline pinned pre-DF2-07 |

## 8. Round-3 record (2026-07-26) — Codex r43 + David D-1..D-4 + last-exchange findings, folded into THIS v4

| Input | Verdict / decision | Folded as |
|---|---|---|
| Codex r43 (fold-verification + static code pass) | **Plan-of-record ACCEPTED · Wave-0 partial GO · Wave-1 needs fixes** (no further teardown; next review bounded to the amendments) | **B1** automarker bypass → DF2-10 (route retirement + server-proved no-score + 5 required tests + set recompute) · **H2** kill-switch mechanics → DF2-10 spec (semantics half closed by D-4) · **H3** nonexistent entry reasons object → DF2-07(b) client-local discriminant (post-D-1 set) · **H4** response-schema mismatch → DF2-10 = single source, DF2-31 by pointer · **M5** hardcode inventory → DF2-07(a) +HelpModal:212, live-card cites corrected, dead cite dropped · **M6** → §4 task-scoped authorization matrix replaces the wave-wide footer · **M7** → DF2-10 R8 stamp proof contract (+DF2-05 fail-closed tooling) · **M8** → DF2-35 data contract (provable-facts v1) · **L9** → orientation status/decisions split · authorization matrix adopted |
| David **D-1** (remove throttle) | BINDING | DF2-10 D-1 rider · register items 2 DISSOLVED / 6 RESOLVED · rows 1-2 retired (DF2-32 + ARCH §12.3) · DF2-07(b)/(e) rescope · DF2-35 reason set · DF2-46 residuals · DF2-40 C5 note · §0 Wave-1 delta list · §3 PR-3 row · item 15 (size rule) |
| David **D-2** (graduate only on pass) | BINDING; within-pass basis OPEN | DF2-10 D-2 rider · DF2-42d mastery-transition input · register item 15 (options + projections + OFF-scoping sub-question) |
| David **D-3** (all attempts recorded) | BINDING invariant | §0 invariant · DF2-12 (typed direct-write) · DF2-13 (MCQ durable) · cert-matrix assertions · honest grader-outage residual |
| David **D-4** (gate-OFF non-retroactive) | BINDING | DF2-10 OFF/rollback semantics + ×3 fixtures + uniform-reader census precondition |
| Last-exchange (a) review-pool projections (`review-pool-sim*.mjs`) | evidence | register item 15's table + insights + recommendation ((a)+adaptive) |
| Last-exchange (b) shadow-orbit starvation (tracer `sim3.mjs`; likely live) | discovery, probe OFFERED | DF2-0P probe card (item 16 go) · DF2-01 exposure note · DF2-04 starvation fixture · DF2-42d no-starvation acceptance criterion · DF2-42 build note |
| Last-exchange (c) attempt-write windows (writeContext path live, client unused) | verification of D-3 | DF2-12/13 cards + §0 invariant wording |
| **R3 Opus-1 (internal panel 07-26, fold fidelity)** | RESIDUES-FOUND (12 + judgment calls F13-F21 listed for r44 ratification) | F1→B1 two-step retirement (live caller DSF:1065) · F2→runtime-config kill switch (const-flag surface rejected) · F3→`no_score` restored to the reason set · F4→item 15 segSize mechanics · F5/F6/F7/F8/F11/F12→gate designations, deviation scope, schema-table obligation, evidence cites, provenance restored · F9→SOURCES/ARCH D-1 residues · F10→DF2-12 direct-write cite :1167-1184 |
| **R3 Opus-2 (internal panel 07-26, fresh eyes; all code cites re-verified)** | ISSUES-FOUND (6H/12M/7L) | H1→`dayReviewExists` joins pass-2 readers · H2→query-error fail-safe leg fails closed under gate-ON · H3→client throttle legs enumerated + skew window · H4→delta-scoped soak signals · H5→compound-rollback delta list + §0 pin restatement · H6→§0 protections extended to attempt-write/review-composition deploys (DF2-12/13/42 enrolled) · M7→DF2-12 client-flip sequencing · M8→§0 reachability rule + 7 exclusions · M9→vocabulary single source (06_MESSAGING_COPY §0) · M10→precedence owned client+server · M11→3 posture read sites + attempt-time stamping · M12→probe-first note · M13→DF2-0P bound (N sweep, defined script, read budget) · M14→OFF-falsifier scope qualified · M15→R1 audit-only authorization · M16→census era provenance · M17→challenge-advance = interventionLevel≡0 · M18→window (ii) named · L19-L25 swept |

| **Codex r44 (bounded fold-verification)** | **FOLD MOSTLY FAITHFUL · plan still ACCEPTED · Wave 1 still needs fixes** (6 gaps + 2 mediums; F13-F21 ALL ratified, F13/F16/F20 conditionally) | B1→`deriveNoScoreEligibility` shared contract + race boundary + harden-retain deploy wording · H1→DF2-07 `no_score` two-boundary derivation + terminal-vs-auto-complete fixtures · H2→ONE posture-independent pass-aware reader rule (D-4) across all six reader sites · H3→`review_not_engaged` holdReason + row-3 binding · H4→DF2-12 re-carded (fail-closed exact-attempt recovery; server reality index.js:1182-1193 `attemptWritten:false`; surface functions+hosting) · H5→stamp atomic-outbox-or-idempotent-repair · M6→retake snapshot writers (MCQ/Typed) added to the D-1 client inventory · M7→kill-switch frozen pre-code contract ({active,version,thresholdSource,readStatus}, stamped-posture immutability, no reclassification) — **ALL EIGHT folded into this v4 (2026-07-26)** |
| **David round 2 (2026-07-26 — ledger `11_` §1, R2-1..R2-17)** | **The redesign's product decisions CLOSED**: hard gate, no escape hatch + teacher force-pass (both types, fail-closed resolver, `teacherEdited` label) · engagement retired · teacher-set queue/test (60/30) · gate STARTS ON at 92% all-at-once (`system_config/review_v2`; enabled:false = kill switch) · uncapped recency-ordered slots · asymmetric stamping + blank=fail · sticky (b) · full backfill · answers-on-the-job grading recovery | Addendum **v3** (v2 archived) · DF2-10 R2-supersession block · DF2-11 rescoped (menu simplification, ships live) · register item 3 CLOSED · DF2-35 force-pass action · DF2-12 card updated inline · NEXT: Track A investigations + Track B pipeline + round-5 🔍6 convergence (3F+2O+Codex r46) |
| **David (post-r44 session)** | **Graduation-redesign DIRECTION** (pace-matched queue → failed-pool → label scheme) | **ADDENDUM `10_REVIEW_GRADUATION_REDESIGN.md`** = item-15 resolution candidate; validity probe (CS-2026-07-26) + failed-pool sim as evidence; item 15 annotated |
| **Round-4 internal panel (2026-07-26, Workflow: 1 Fable + 2 Opus, adversarial)** | **v1 addendum: not-buildable / NO-GO-as-scoped / not-decision-ready** — blockers F-1 (cursor statelessness), S-1 (posture-independent legs invert D-2 + break OFF byte-identity), S-2 (unrecovered predicate false for never-passed words), S-3 (10-legged pin-move uncertifiable), P-1 (weak-stratum retake deadlock), P-2/P-5 (Simpson-masked evidence; <50-band fill penalty +20.6pp, recovery 21.3% ≈ chance floor) + F-2/S-8 (false write-surface premise — processTestResults already writes per-answer), F-3/F-4/S-9 (adjudication hook; forgeable labels), S-4/S-5 (backfill not a write plan; day-1 shock), S-7/P-9 (failCount-DESC priority starves), F-10/S-15/P-7 (clamp), S-13/S-14/P-8/P-11 (workload numbers wrong both directions; missing comms leg), F-6 (resetEpoch-blind backfill), F-7 (M6 either/or), F-8 (dual REC markers) | **ALL folded → addendum v2** (pure queue, one enablement flag, fixed predicate, DF2-14 unbundle + circuit breaker, escape valve 🧭, lastFailedAt-ASC slots, clamp, server-write-only labels + attempt-bound mutation, backfill options 🧭, stratified §1, corrected workload + comms leg, committed pf sim [P-4]); task-list F-7/F-8 fixes applied |

| **Codex r45** | addendum v2 NEEDS FIXES pre-decision | B1 queue identity · B2 exhausted-day (→dissolved by R2-1) · B3 unbundle reconciliation · B4 shared-state (→closed R2-36/r48) · H5-H8 + M9/M10 → carried into DF2-14's contracts |
| **Round-5 internal panel (13_)** | not-buildable / NO-GO-as-scoped / not-decision-ready (pre-answer state) | ~90 findings repo-filed; adjudicated by r46 (survives/dissolved lists) |
| **David R2 rounds (11_ §1 R2-1..R2-47)** | THE DECISION RECORD — board closed | universal model · ONE launch · gate ON@92 · labels/strata sealed · all overrides ratified |
| **Codex r46/r47/r48** | decision-state check → owner-answer adjudication → **FINAL RATIFICATION: FOLD GO** | the 28-item fold checklist + the 8 launch contracts + the error-fencing — all carried on DF2-14 |

**Status: v5 = the COMPLETE fold (three checklists, one pass). The plan of record now matches the ledger
(11_ §1 R2-1..R2-47). Round-6 (r49) → CORRECTION FOLD → round-7 (r50) → RESIDUE FOLD → round-7b (r51 + panel;
R2-38 ratified by David 2026-07-27) → round-8 FINAL CLOSURE: r52 PRESENTABLE-YES + the 3-Fable panel (no blockers; bookkeeping folded) → **PRESENTED to the owner (2026-07-27); then Track A/B execution; build/backfill/activation at David's ask-at-implementation gates.**
