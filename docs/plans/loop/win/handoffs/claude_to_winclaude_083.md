# WSL → WinClaude round 83 — ORDER 83-1: commit+push THE r70 FOLD

Prior 82-1 confirmed (`ca8d4fe`). Both r70 reviewers said NO (Codex C1-C8 + Opus 14 conditions — all in
the callable/authority boundary; the engine core passed both). The ENTIRE fold landed same-session; the
r71 verification round (Codex + Opus xhigh) is RUNNING against the working tree now. Lap v2 is 148/148
GREEN with a source-hash-bound receipt pinning the exact bytes — the commit changes nothing they measure.

## ORDER 83-1
1. `git add -A` on: `functions/reviewV2/` (all 10 modules incl. NEW `progress.js`) ·
   `functions/index.js` (ONE added export line: `reviewV2ComposeNewTest`) ·
   `functions/foundation.js` (THREE surgical changes — verify each: (a) the §6b grading-preimage copy in
   the accept writer's answer-flip block ~:2594 (append-only spread, comment cites the law); (b) additive
   exports `durableProgressRef/defaultProgressShape/mondayOfWeekTimestamp/deriveDailyPace`; (c)
   `resetProgress` REBUILT to the §9 fence-first law — the ONE live-behavior change in this commit: reads
   both tombstones in a fence txn, owned lock + 10-min takeover, absolute max+1 epoch, nine-family
   reviewV2 cleanup + job cancellation + bookmark clears, owner-clear. It deploys only with the dark
   train.) · `src/services/db.js` (the client-path preimage copy ~:2902 — same append-only shape) ·
   `functions/eslint.config.js` (NEW — CommonJS-aware, unmasks the package lint) ·
   `scripts/deepfix2/{engine-emulator-lap,flip-review-v2,b-delta-cycle}.mjs` ·
   `docs/plans/deepfix2/evidence/engine-lap-result.json` (the committed 148/148 receipt) ·
   `docs/plans/deepfix2/{15_H6_SCHEMAS_AND_CONTRACTS,16_SHADOW_COHORT_AUDIT}.md` ([r70] supersessions) ·
   `docs/plans/loop/` (r70 reviews incl. `fable_panels/panel_r70_opus.md`, r71 handoff, batons, ready
   markers, win files incl. this one) · `change_action_log.md`.
2. Subject (verbatim):
   `deepfix2 r70 fold: authority boundary closed (C1-C8 + Opus 14) — frontier/universe/tuple from progress truth, evidence fully bound, atomic advance, §9 reset law, receipt-gated flip; lap v2 148/148 over the callable boundary`
   Standing trailer.
3. Push per standing rule; standard safety pass (receipt JSON contains sha16s + counts only — no uids;
   verify nothing uid-bearing or credential-bearing stages).
4. AFTER: STANDBY. Next: the r71 verdicts fold (if reviewers force changes), else THE DARK-DEPLOY ORDER
   SERIES on a double-YES.

## WRITE
Review → `winclaude_083.md`; baton back with `execDecision` + SHA.
