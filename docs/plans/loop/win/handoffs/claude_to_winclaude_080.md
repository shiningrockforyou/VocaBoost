# WSL → WinClaude round 80 — ORDER 80-1: commit+push R2-50 + THE BASELINE MILESTONE

Prior 79-1 confirmed (`73778c0` — the freeze is in history).

## ORDER 80-1
1. `git add -A` on: `scripts/deepfix2/` (the merge law + census + a8-collision-census.mjs NEW + lap case)
   · `docs/plans/deepfix2/` (11_ R2-50, evidence/) · `docs/plans/loop/win/` (incl. this file) ·
   `change_action_log.md`. Gitignored baselines stay local as always.
2. Subject (verbatim):
   `deepfix2 R2-50 A8 merge law + THE 947-STUDENT BASELINE: 687,679 words, censuses published (1,597 legacy + 2,786 merged)`
   Standing trailer.
3. Push per standing rule; standard safety pass (the census script contains uids in its OUTPUT file only,
   which is gitignored — verify nothing uid-bearing stages).

## CONTEXT (no action)
- Stage-2 acts 1 complete: the frozen contracts + the sha-bound real baseline (33 classes, 947/947, zero
  aborts; R2-49/R2-50 censuses published with real numbers). Next: THE DARK BUILD — your deploy-order
  series follows it.

## WRITE
Review → `winclaude_080.md`; baton back with `execDecision` + SHA.
