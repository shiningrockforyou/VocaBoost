# WSL → WinClaude round 84 — ORDER 84-1: commit+push THE r72 FOLD

Prior 83-1 confirmed (`c7abf0a` + your escalation — which the r71 Opus lane independently VINDICATED:
its BL-A blocker is exactly the reset rebuild's live-reader hazard you flagged). Both r71 verdicts were
NO (converged lists); the r72 fold closes everything. **Your escalation is ANSWERED IN CODE: the reset
rebuild is now gated `RESET_V2_ENABLED=false` (foundation.js) with the LEGACY reset law byte-preserved
as the false branch — the dark deploy is zero-delta again; flipping the const is David's named deploy
step.** LAP v3: 174/174 over the callable boundary; receipt binds 16 files incl. foundation/index/db.js.
The r72 verification round (Codex + Opus) is RUNNING against this tree.

## ORDER 84-1
1. `git add -A` on: `functions/reviewV2/` · `functions/foundation.js` (verify: the RESET_V2 gate const +
   pm-only pre-P5 fence [BL-A] + dual-lock reduction + `applyChallengeAdjudication` extraction — the
   accept writer now calls the exported transform, behavior-identical) · `functions/package.json` (lint
   mask dropped — the full package lints clean) · `scripts/deepfix2/{engine-emulator-lap,flip-review-v2,
   b-delta-cycle}.mjs` · `docs/plans/deepfix2/evidence/engine-lap-result.json` (174/174) ·
   `docs/plans/deepfix2/15_H6_SCHEMAS_AND_CONTRACTS.md` (§2/§3b/§9 supersessions — §3b carries the
   PROPOSED R2-51 view catch-up, David-ratification PENDING) · `SUPPORT_RUNBOOK.md` (the BL-A CS note) ·
   `docs/plans/loop/` (r71 reviews, r72 handoff, batons, markers, win files incl. this one) ·
   `change_action_log.md`.
2. Subject (verbatim):
   `deepfix2 r72 fold: reset gated zero-delta (BL-A fixed, RESET_V2 const), evidence fully bound, value-verified flip receipt, authz in every txn; lap v3 174/174`
   Standing trailer.
3. Push per standing rule; standard safety pass.
4. AFTER: STANDBY. On a double-YES at r72: THE DARK-DEPLOY ORDER SERIES — now honestly zero-delta
   (RESET_V2 false), with the deploy-order requirements collected in the r72 handoff's final section.

## WRITE
Review → `winclaude_084.md`; baton back with `execDecision` + SHA.
