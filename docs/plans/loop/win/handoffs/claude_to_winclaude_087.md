# WSL → WinClaude round 87 — ORDER 87-1: commit+push THE r75 FOLD

Prior 86-1 confirmed (`e9e8ac4`). r74 verdicts: **Opus YES (checkpoint closes on its side; carries in
this diff) · Codex NO (5 items)** — all five closed on Codex's terms, INCLUDING the full revert of my
r74 lock-window change (Codex was right: a crashed reset leaves a partially-deleted graph; engine
writers are fail-closed again, liveness = takeover only, THE SEQUENCE fixtured). **LAP: 211/211.**
Codex r75 is judging this tree now; on its YES the checkpoint CLOSES.

## ORDER 87-1
1. `git add -A` on: `functions/reviewV2/{composer,completion,config,callables}.js` (verify composer's
   diff REVERTS the takeover window — `resetLockActive` is two Boolean() lines again) ·
   `scripts/deepfix2/{engine-emulator-lap,list-position-sweep}.mjs` ·
   `docs/plans/deepfix2/evidence/{engine-lap-result,list-position-sweep-receipt}.json` (the sweep receipt
   is NEW: project/at/counts/scriptSha16, no uids) · `docs/plans/deepfix2/17_DEPLOY_ORDER_REQUIREMENTS.md`
   (§2b the restored lock law; §5 corrected mechanism) · `NEED_TO_FIX.md` (corrected card) ·
   `docs/plans/loop/` (r74 reviews/panels, r75 handoff, batons, markers, win files incl. this one) ·
   `change_action_log.md` · `RESUME.md`.
2. Subject (verbatim):
   `deepfix2 r75: reset fail-closed restored (takeover sequence fixtured), legacy evidence leg true in code, parent-container authority, sweep receipt committed; lap 211/211`
   Standing trailer.
3. Push per standing rule; standard safety pass.
4. AFTER: STANDBY. On Codex r75 YES: THE DARK-DEPLOY ORDER SERIES per 17_ — your next orders.

## WRITE
Review → `winclaude_087.md`; baton back with `execDecision` + SHA.
