# WSL → WinClaude round 69: PRESENT DAVID'S PERMISSION QUESTIONS + your shadow-audit duty roster

David ordered the SHADOW-COHORT AUDIT (read `docs/plans/deepfix2/16_SHADOW_COHORT_AUDIT.md` §1-§4 for what it
is) and directed that HIS PERMISSIONS BE ASKED THROUGH YOU — he will answer in YOUR session directly.

## TASK 1 — PRESENT THESE QUESTIONS TO DAVID (verbatim; record his answers)
Show him this list and wait for his answers (he is expecting it):
1. **Q1 — Clone consent:** Clone the 947 real students' study data into namespaced `shadow_` copies **with PII
   redaction** (names/emails replaced; recommended) — approve? [yes-redacted / yes-unredacted / no]
2. **Q2 — Auth users:** Create 947 shadow Firebase-Auth accounts (redacted emails, one audit password, deleted
   at cleanup) — approve? [yes / sample-only-30 / no]
3. **Q3 — Dark deploy CONFIRM (formal record):** functions + rules + indexes, all `enabled:false`, off-peak,
   backup branch exists — confirm the in-session grant? [confirm / hold]
4. **Q4 — AI-grading cap** for shadow typed tests: default 200 calls — set a number.
5. **Q5 — Shadow-data retention:** auto-delete immediately after the audit report (recommended) or hold until
   your review? [auto-delete / hold]
6. **Q6 — Hosting CONFIRM (formal record):** deploy hosting ONLY after OFF-parity + old-bundle checks pass —
   confirm the in-session conditional grant? [confirm / hold]
Record answers verbatim in your review file AND the baton note.
**NOTE (added after Codex r58): the convergence EXPANDED the permission set (isolation-infrastructure choice,
data-minimization detail, credential design, retention deadline, co-tenancy/rate consent, cleanup authority).
Present Q1-Q6 above AS-IS if David is present now; a consolidated FULL set arrives as r70 — tell him "a second,
expanded permission round follows the convergence" so nothing executes on partial consent.**

## TASK 2 — YOUR DUTY ROSTER for the audit night (for awareness now; numbered orders will follow)
1. (r68 if not done) push main + backup branch. 2. Emulator rules matrix when the merged artifact lands.
3. Dark deploy on my numbered order (after Q3 confirm + the freeze convergence passes). 4. Playwright batteries:
25WT flow rehearsal, then the shadow walkthroughs (audit §4-C/G) — stratified account lists + specs will be
provided. 5. Incremental commit+push milestones. FORBIDDEN list unchanged (NO hosting until Q6 conditions met +
my order; NO 26SM writes; NO config writes).

## WRITE
`docs/plans/loop/win/reviews/winclaude_069.md` (David's verbatim answers + env state); baton → turnOwner=claude,
execStatus=review-written, execDecision=ANSWERS_RECORDED, revision=137.
