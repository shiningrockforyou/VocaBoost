# WSL → WinClaude round 85 — ORDER 85-1: commit+push THE r73 FOLD

Prior 84-1 confirmed (`ce09792`). Both r72 verdicts folded; David ruled mid-round (both receipts in 15_
§3b): **R2-51 RATIFIED** (the dual-class view catch-up, his verbatim principle) and **RESET_V2 flip = the
sandbox-rehearsal phase**. LAP v3: **182/182**; r73 verification (Codex + Opus) RUNNING against this tree.

## ORDER 85-1
1. `git add -A` on: `functions/reviewV2/` (5 modules changed) · **`firestore.indexes.json` (ONE ADDITION —
   verify the diff is exactly the grading_jobs (uid ASC, status ASC) composite; it did not exist and the
   §9 job-cancel query needs it; deploys with the dark train, 17_ orders it before any RESET_V2 flip)** ·
   `docs/plans/deepfix2/17_DEPLOY_ORDER_REQUIREMENTS.md` (NEW — the durable deploy-card home incl. YOUR
   escalation's flip step with David's timing ruling) · `scripts/deepfix2/engine-emulator-lap.mjs` (v3) ·
   `docs/plans/deepfix2/evidence/engine-lap-result.json` (182/182) · `docs/plans/deepfix2/15_H6_*.md`
   (R2-51 ratification receipts) · `docs/plans/loop/` (r72 reviews/panels, r73 handoff, batons, markers,
   win files incl. this one) · `RESUME.md` + `docs/resume_archive/RESUME_2026-08-03.md` ·
   `change_action_log.md` · `SUPPORT_RUNBOOK.md`.
2. Subject (verbatim):
   `deepfix2 r73: R2-51 ratified (view catch-up live, completedTwi), override zero-grad, new-test fence, run-bound audits, grading_jobs index; lap v3 182/182`
   Standing trailer.
3. Push per standing rule; standard safety pass.
4. AFTER: STANDBY. On a double-YES at r73: THE DARK-DEPLOY ORDER SERIES per 17_ (zero-delta; RESET_V2
   false until the rehearsal phase per David).

## WRITE
Review → `winclaude_085.md`; baton back with `execDecision` + SHA.
