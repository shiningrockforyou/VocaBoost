# WINCLAUDE round 44 — D3.5 CRITIC PASS (executor-feasibility lens) — ⚠️ GAPS-FOUND

- **Executor:** Windows-native Claude Code @ `C:\Users\dmchw\vocaboost`. Verify + report only — no builds/deploys.
- **taskId:** `WINCLAUDE_D35_CRITIC_FEASIBILITY` · **execDecision:** `GAPS-FOUND` (the plan is *buildable*, but several specifics are wrong/underspecified — concrete gaps below, mostly fixable).

My lens: "can this be built + run on prod-Playwright?" — grounded in direct scar tissue (r33 MCQ saga, r34 RO-S1, r37 `joinClass` gap, M-UI prod-smoke, the p4cert build) + verifying the referenced artifacts against disk.

## Q1 — clone / seeders / harness / assertions: concrete gaps
- **GAP-1 (path/existence, HIGH):** the plan roots the clone at `scripts/audit/clone-ticketed-prefix.mjs` "extends `scripts/audit/dup-real-students-for-audit.mjs`" — **`scripts/audit/` does not exist**, and the base script is actually at **`scripts/cs/dup-real-students-for-audit.mjs`**. *Fix:* correct the paths (base = `scripts/cs/…`); `mkdir scripts/audit` or keep the new script under `scripts/cs/`.
- **GAP-2 (backup completeness, HIGH):** the per-family backups are **class_progress-ONLY and heterogeneous** — `backups_throttle/<uid>.json` = `{email,uid,cp,before}` (cp=class_progress); `backups_csd/<uid>.json` = `{…,before:{csd,twi}}` (**no `cp` at all, before-only**). **Neither holds `session_states` or `attempts`.** So "overlay the exact pre-fix state (dup class_progress, session_states, study_states, attempts)" is **not achievable from `backups_*/<uid>.json`** for families that depend on attempts/session_states (off-by-one A3, stale-session A10, list-end A6). *Fix:* pull `session_states`/`attempts` from the **aggregate collection dumps** in `backups_full_26sm_.../{session_states,attempts,class_progress}.json` (they exist — but as whole-collection arrays, index by uid), and write the overlay to handle the heterogeneous per-family schemas (`cp` vs `before` vs collection-dump). Spec the exact source per field.
- **GAP-3 (pre-fix vs post-fix provenance, HIGH):** `backups_full_26sm_20260717-165840/` is a **2026-07-17 16:58 snapshot** (class_progress.json = 1499 docs) — **dated AFTER most CS fixes ran (07-13…07-17)**, i.e. it is largely the *recovered/post-fix* state, NOT the broken pre-fix state. The **only true pre-fix snapshots are the per-family backups** (cp-only). ⇒ for families needing pre-fix attempts/session_states, the state must be **synthesized**, weakening the "authentic pre-fix data" claim to "class_progress-authentic + synthesized attempts." *Fix:* state this honestly; verify each target uid's `submittedAt`/fix-timestamp to confirm which backup is genuinely pre-fix; synthesize the rest from SUPPORT_RUNBOOK shapes (as Parts A/F already do).
- **GAP-4 (count drift, LOW):** plan says `backups_full` = 823; on disk it's **aggregate collection files** (5 JSONs), class_progress.json = 1499 docs — not per-uid, not 823. *Fix:* correct the description.
- **GAP-5 (underspecified seeds, MED):** a few Part-A/F signatures lack a reproducible recipe: **A5** ("score>100 via MCQ recovery-merge overflow") — the overflow is a *client* recovery-merge path; how to reproduce it server-side to exercise the clamp is unspecified. **A12** (lost-save corrupt session) — expected outcome is "observe whether it self-heals," i.e. no crisp PASS/FAIL. **F13/F18** (sub-second `completeSession` race / day-guard race) — the emulator/Firestore may **serialize** the txn so the race never actually races; asserting "exactly one advance" is sound but *proving the race happened* is non-deterministic. *Fix:* give A5 a concrete seed (or move to observe-only), define A12's pass line, and note F13/F18 prove the *invariant* (idempotency) not the *timing*.

## Q2 — is `joinClass` the ONLY live-UI blocker? NO — at least four
1. **`joinClass` enrollment** — confirmed (r37: the seeded student stayed unenrolled). Real blocker #1.
2. **MCQ vs Typed input automation (HIGH):** r33/r34 proved the review test is **MCQ** (a "Ready for the Test?" modal → "Start Test" → choice-card clicks → a "Submit Test N/30" counter), while the M-UI harness assumes the **typed** test (`input[placeholder*=definition]`). **r34 RO-S1 FAILED on exactly this.** Every Part-A/B/C/D UI scenario that submits a test needs BOTH drivers (MCQ + typed) + the modal handling. This is the single biggest tier-3 effort, bigger than `joinClass`.
3. **Seeded-state → client re-derivation (MED-HIGH):** post-cutover the client routes through `resolveListProgress`/`initializeDailySession`, which **re-derives** allocation/reviewMode from the seeded `class_progress`. Seeding server state ≠ the client *presenting* the intended broken screen — the resolver may reconcile the seed into a different session than the ticket described. Each Part-A UI scenario must verify the seed actually **renders as the intended state** before "driving the fix," or the assertion is meaningless.
4. **Multi-actor / challenge flows (MED):** B8/B9/B22 (challenge) need a **teacher browser session + a pending challenge in the UI** — a second-actor automation the harness doesn't have yet.
5. **NOT blockers (good news):** prod **App-Check is not enforced** (CF logs show `app:MISSING`, callables are `enforceAppCheck:false`); sandbox **login works on prod** (proven r33/r38). So auth/App-Check are fine.

## Q3 — Part B: automatable-on-prod-headless vs hard/impossible
- **Cleanly automatable:** B1 (double-click submit), B2 (spam start), B3 (empty submit — once MCQ/typed driving lands), B5 (reload mid-test), B7 (back/forward), B10 (retake passed), B13 (review-before-new), B14 (multi-day, but slow), B15 (logout/login), B16 (partial 79/81%), B17 (91/92/93 boundary), B18 (reset→restudy), B19 (join/leave/rejoin), B21 (throttle review spam — the headline A2).
- **Fragile / timing-racy headless (do at the CALLABLE level instead for the invariant, UI as best-effort):** **B4** (reload *before grade returns* — prod grade is sub-second; the window is nearly unhittable), **B6** (two-tab *simultaneous* — best-effort concurrency, not truly atomic), **B11** (network-drop mid-submit — Playwright `context.setOffline` works but timing the drop is racy), **B12** (slow/timeout grade — **can't induce a real slow grade on prod** without CDP network throttling; even then fragile).
- **Likely won't reproduce headless (document, don't fail — matches E5):** **B20** (dead-button freeze) — that was an **in-app-webview** env freeze (CS-06-29D); headless Chromium won't reproduce it.
- **Recommendation:** run the idempotency/race family (B1/B4/B6/B11/B13/B18/F13/F18) primarily as **concurrent callable** tests (deterministic, I proved this shape in p4cert) and treat their UI versions as best-effort fidelity, not pass/fail gates.

## Q4 — concurrency vs LIVE prod
- **Callables (5-8 concurrent):** trivially fine — the deployed functions run `maxInstanceCount:10 × maxInstanceRequestConcurrency:80`. Courteous. Could go **8-12** safely.
- **Browsers (3-4):** fine for *prod* (3-4 users' load = nothing), but the binding limit is **my box** (each headless Chromium + a prod session is heavy). Keep **≤3-4** — a box constraint, not a prod-courtesy one.
- **The real cost/rate limit is the AI grader:** `gradeTypedTest` calls the Anthropic API (`ANTHROPIC_API_KEY`) — real $ + rate limits. **Cap live-grader calls hard; prefer MCQ** (the grader itself is Part-E observe-only anyway). Don't drive hundreds of typed tests through the live grader.

## Q5 — 26SM-write / real-student side-effect risks to tighten
- **Already right (preserve it):** the base `dup-real-students-for-audit.mjs` rewrites cloned attempts' **`teacherId` → the sandbox teacher** (`lsr_teacher_02`) — so cloned attempts do NOT surface in a real teacher's gradebook. The extension MUST keep this.
- **RISK-1 (real `listId` reuse ⇄ the sandbox guard, HIGH):** the base script **reuses the REAL `listId` read-only** (doesn't clone the list). But the plan requires `clone-ticketed-prefix` to `assertSandboxTarget` (lsr-shaped ids) **fail-closed** — a real `listId` would **fail that guard**. These directly conflict. *Fix:* either (a) exempt read-only real `listId`s from the guard (keep the guard on the *write* targets — uid/classId — only), or (b) clone the list to an `lsr_`-shaped copy (copy `wordCount` + word docs read-only). Decide explicitly; today it's contradictory.
- **RISK-2 (NEVER write `lists/`, HIGH):** the shared real list is read by many real students — **no scenario may write to `lists/{realListId}`** (a stray write corrupts the list for the whole cohort). Add an explicit assert: writes to `lists/` are forbidden. (Callables write `class_progress`/`attempts`, not `lists/` — verify no path does.)
- **RISK-3 (source-uid write protection, MED):** clone must NEVER write to the **source 26SM uid's** docs. Assert on every write: `targetUid !== sourceUid && targetUid` is sandbox-shaped.
- **RISK-4 (prod-project pollution, MED, accept+note):** tiers 2/3 create sandbox auth users + emit `system_logs` (`review_recorded`, `day_guard_rejected`, …) on the **real project** (`vocaboost-879c2`) with sandbox uids. This is benign but real: (a) it grows the real project's user list, (b) it seeds sandbox `system_logs` that WSL's **26SM sweeps must exclude by uid** (else sandbox noise inflates the real counts). *Fix:* namespace all sandbox uids (`lsr_`) and make the sweeps filter them out; prefer the **emulator** (pinned `0ddbb34`) for the state-heavy Part-A/F cohort to keep prod clean, reserving prod for the representative-subset UI runs.

## Bottom line
Buildable and worth doing, but **not "just fix `joinClass`"**: the tier-3 long pole is really **MCQ+typed input automation + seeded-state-renders-as-intended + multi-actor challenge flows**, and several plan specifics (clone path, backup completeness/provenance, listId-vs-sandbox-guard) are wrong or contradictory as written. Fix the 5 Q1 gaps + the RISK-1/2 contradiction, scope the race-family to concurrent-callable tests, cap the live grader, and lean on the emulator for the full cohort — then it's feasible within the ~1–1.5-day estimate (tier 3 will run longer/flakier than 1.5–2.5h).

## Hand back
- Report: `docs/plans/loop/win/reviews/winclaude_044.md`.
- `baton.json` → `turnOwner="claude" round=44 execStatus="run-written" execDecision="GAPS-FOUND" updatedBy="winclaude" revision=88`.
- Watcher re-armed at baseline 88.
