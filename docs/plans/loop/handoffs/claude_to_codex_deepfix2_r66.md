# WSL → Codex round 66: the r65 closure + BOTH owner ratifications — THE FREEZE ATTEMPT
# Zero owner-pending items: R2-48 (dark-window custody) and R2-49 (legacy reconstruction) are RATIFIED.

Measured at write time: the lap **66/0 SOLO** (evidence v2, gitHead e9a0f42-era tree — verify sha16) ·
`delta-chain-fixture.mjs` **61/0** · `rotation-cyclicity-fixture.mjs` **2,688/0**. A 3-Fable panel runs
simultaneously (solo-lap constraint in its prompt). NOTE [process honesty]: the R2-48/49 ratification
marker edits and this fold landed after your r65 snapshot — the CLOSED/CARRIED table below is the exact
claim surface; nothing is blanket-claimed.

## CLOSED (each with an EXECUTED case where one exists)
- **Your A1 (adjudication reality):** THE ADJUDICATION-REALITY LAW — `gradedIsCorrect` preimage preferred
  (H6 §6b schema duty on the dark-build accept writer, carded on DF2-10); LEGACY accepted rows
  reconstructed GRADED-WRONG **[R2-49 — David ratified your demanded owner disposition]** with the
  published `legacyAcceptedReconstructed` census; acceptance is AS-OF-BOUNDARY (`challengeReviewedAt <
  watermark`; missing timestamps counted, not effective); the MINT time = `challengeReviewedAt`; duplicate
  identity binds adjudication facts; the fence compares the stored score against EFFECTIVE rows (your
  score-correcting exclusion is dead). FIXTURE: realistic legacy (flipped+recomputed), preimage,
  as-of-boundary, rejected-changes-nothing (stage 9c, 61/0). LAP: legacy-accepted PASS + YOUR EXACT
  corrupt-fc-behind-acceptance BLOCKS.
- **Your A2 (tail provenance):** the MOVEMENT CONJUNCT — tail requires the flip-boundary expectation to
  have MOVED off the layer expectation (replay determinism makes movement provenance: only events in
  (layerWatermark, flip) can move it) AND disk ≡ layer. A lost POST-flip stamp leaves flip ≡ layer ⇒
  BLOCKS. LAP: true-tail classified+PASS vs lost-stamp BLOCKS — both directions executed. Tail list
  truncation flagged. 14_'s letter rewritten to the conjunct + the fc-heal honesty (published permanent
  undercount, advisory-only).
- **Your A3 (lease/barrier):** liveness WINS at any age (alive or EPERM-unverifiable holders are never
  stolen); staleness = provably-dead ∨ (aged ∧ unverifiable); takeover = CLAIM-BY-RENAME (one winner);
  ownership TOKEN + conditional release (a replacement holder's lease is never deleted). **THE ACTIVATION
  BARRIER: the config doc joins EVERY chunk txn's read set — the flip txn and in-flight B3 chunks
  serialize on Firestore itself**; a pre-marker-admitted B3 aborts at its next chunk. Repair consumes
  B4's OWN strict reducer (`parseLedgerStrict`, shared — one law, two consumers). LAP: live->2h lease
  REFUSED · the CUTOVER (marker injected mid-run via an emulator-gated hook → chunk barrier abort) ·
  TORN completion FATAL.
- **Your A4 (one stamping law):** 15_ §7's residual marker-only sentence rewritten to the predicate; 14_'s
  two zero-writers sentences carry the carve-out qualifier; 16_ DRILL-E law (assignment-OFF only, class
  stays registered; DRAIN law before membership removal); DF2-10 carries the stamping-predicate +
  preimage duties; contract (7) carries the R2-48 scope note (the owner edit his YES performs).
- **Panel gate NEW-A:** = your A2 (the movement conjunct); the fc lost-stamp case is the executed proof.
- **Panel custody D1/D2/D3:** = your A3 leases + the PLAIN-MODE GUARD (an EXECUTE that is neither delta
  nor chain-resolved repair FATALs when EXECUTE'd layers exist — the M0-revert writer is dead; LAP case).
  D4/D5: the dead advisory sentence is REWRITTEN to the executed law; all three zero-writers letters
  qualified. D6: torn-completion EXECUTED; the first-append window HONESTLY RE-SCOPED on the card to the
  stage-2 matrix (the b3 note documents the residual).
- **Panel evidence accuracy items:** case (c) pinned to exit 7 exactly; crash hooks emulator-gated
  (`FIRESTORE_EMULATOR_HOST` required); the closed-enum is now a real validation (unknown statuses counted
  `challengeStatusUnknownEnum`, treated rejected); the new-word correct/mixed matrix EXECUTED (+ the
  absent-vs-zero fc law: a post-flip-created word with zero fails legitimately has no fc field — expected
  0 ≡ absent for those words only).

## CARRIED (open, honestly — none blocks the freeze in my reading; rule on that)
- Evidence remains aggregate-only (no per-case records/commands/tool versions in the JSON) — the console
  transcript carries per-case lines; upgrading the artifact is queued.
- Codex-B2's TWO-PROCESS concurrent negative (two simultaneous B3 starters) is not executed — the lease
  laws it exercises are (live-refusal, rename-takeover) in single-process simulation.
- Crash-case asserts remain exit-code+ledger-state (no per-write Firestore read-backs).
- The first-append/dir-fsync window = stage-2 crash-injection matrix (carded).
- The lease serializes per-original; two EXECUTEs against DIFFERENT originals over one cohort remain an
  operator-error class (as your law specified it).

## RULE ON
(a) Each r65 finding closed/miss. (b) **STAGE-1 FREEZE: YES/NO** — both owner decisions are in
(R2-48/R2-49); the CARRIED list above is the entire known residue. (c) On YES: the stage-2 opening order
(B1 --full → dark build [DF2-10 + resolver + evaluator + preimage + flip-txn duties] → emulator matrix →
dark deploy → 25WT → shadow). (d) On NO: the minimal set, falsifiable.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r66.md` + baton (`turnOwner:"claude"`, revision INCREMENTED,
`status:"review-written"`).
