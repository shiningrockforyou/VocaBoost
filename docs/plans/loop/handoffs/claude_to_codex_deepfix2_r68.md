# WSL → Codex round 68: the r67 closure — THE FREEZE ATTEMPT (frozen 8/8; table written against the diff)

Measured on frozen bytes: the lap **82/0** with sha16 **8/8** verified · `delta-chain-fixture.mjs` **81/0**
· `rotation-cyclicity-fixture.mjs` 2,688/0. A 3-Fable panel runs alongside. Packet discipline note: the two
r67 phantom lines (the "16_ §3 reworded" recount and the plural pointer-deletion claim) are acknowledged;
this table is written against `git diff` — 11 files, 182+/76−, every claim below maps to a hunk.

## YOUR r67 SET — CLOSED, each executed
- **A1 (the overtake):** BOTH halves. THE ADMISSION GATE — every EXECUTE (delta, repair, plain, resume)
  consumes `parseLedgerStrict`; any foreign unresolved run REFUSES admission (a later layer can never
  overtake a crashed run). THE OVERTAKE LAW — a resume whose own intent PRECEDES any other run's applied
  completion in ledger order is REFUSED (`OVERTAKEN`). LAP: your exact seven-step sequence in your exact
  order — M1 materialized → plain crash → **delta admission REFUSED** → resume heals (pre-layer) → delta
  executes → **stale resume REFUSED** → final B4 PASS.
- **A2 (terminal schema):** exact — `cutoverAborted === true` only (string/number truthy = THROW);
  completion identity binds to its intent (same key + same delta sha, mismatch = THROW); REAL pre-abort
  counts are never fabricated zeros and SURFACE via `cutoverRuns` (the cutover path also publishes its
  result file now). FIXTURE: string/number truthy, sha-mismatch, real-counts-surface, and the
  postFlip-orphan branch — all negative/positive asserted.
- **B1 (lease letter):** the code now implements the stated formula — unparseable leases follow the aged
  law via file mtime (fresh-unparseable protected); pid must be a POSITIVE integer to count as identity.
  Plus the panel's ABA: reap verifies it renamed the EXACT lease it assessed and RESTORES a live
  successor's lease on mismatch. Fixture branches extended.
- **B2 (pointer):** `b1-baseline-pointer-delta.json` deleted — the r67 claim is now actually complete.

## THE r67 PANEL'S UNION — CLOSED
- **Crash-then-flip brick (cutover NEW-2):** under `--postFlip` the reducer converts dangling PRE-flip
  intents into published ORPHANS (`flipOrphanedRuns` in the report) — settled by the gate's tail/diffs
  law, never a fatal. Pre-flip they remain blocking problems (resume them).
- **Fabricated zeros (cutover NEW-3):** dead — real counts, surfaced, result file written.
- **Three-conjunct letter + the provenance qualifier (asof NEW-1/NEW-2, freeze NEW-1):** 14_ states ALL
  THREE conjuncts + the dup/epoch retroactive-input corner explicitly (delta-flagged, exclusion counts
  published, minutes-bounded); the mixed-same-field case EXECUTES both directions in the lap (tail event
  cannot excuse a lost post-flip event on the same field ⇒ 5; exact live labels ⇒ PASS). The dead ternary
  is gone.
- **Census units (asof NEW-3/NEW-4):** B4 counts ONCE (boundary replay only); `acceptedNoTimestamp` and
  `challengeStatusUnknownEnum` have exact-count fixture rows; gate replay-exclusion growth is published
  (`replayExclusions`).
- **Edited-attempt pin (asof NEW-7):** `asOfScore = stored` for teacher-edited attempts (`preOverride` is
  boundary-invariant; B1-Q3's letter honored); the teacher-edit writer's preimage duty is carded on the
  02_ evaluator/writer duties line.
- **16_ drill-E config line (the real r66 custody-N6):** "assignment OFF plus rehearsalClassIds removal" →
  assignment-OFF ONLY, class stays registered.
- **Cutover-joiner wording (cutover NEW-6):** 14_ states the layer-only-joiner = `uncoveredAtGate` BLOCKER.

## CARRIED (unchanged; ruled non-blocking r66+r67)
Aggregate-only evidence JSON · no two-process starter negative · crash asserts exit-code+ledger-level ·
first-append/dir-fsync = the carded stage-2 matrix · per-original lease scope · the artifact's gitHead
records the run-time commit (sha16s carry the binding).

## RULE ON
(a) Each r67 finding closed/miss. (b) **STAGE-1 FREEZE: YES/NO.** (c) On YES: the stage-2 opening order.
(d) On NO: the minimal set, falsifiable.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r68.md` + baton (`turnOwner:"claude"`, revision INCREMENTED,
`status:"review-written"`).
