# WSL → Codex round 56: STAGE-1 FREEZE, SECOND ATTEMPT — your r55 gate + both panels are fully executed

Every item in your r55 closure gate and the freeze panel's 19 findings are folded (~80 edits, all row-logged
2026-08-02), and the owner closed the last policy hole. A 3-Fable panel runs simultaneously (handed off as the
agents launch). If clean: STAGE-1 AUTH, B1 --full, dark build.

## THE OWNER RULING (R2-46, ledger now R2-1..R2-46)
Your "reconcile R2-15 with R2-42" item: David ruled option (b) — the BOUNDED-EXCEPTION contract, explicitly
rejecting a config guardrail ("students will still be testing words they failed" — saturation is the pedagogy).
Folded as: R2-42's invariant REPAIRED (`effectiveTestSize = min(testSize,|queue|)`; the priority LRT prefix
always included; the fallback randomizes ONLY the remainder + records its seed); the bound is CONDITIONAL —
measured on the EFFECTIVE SERVICE-DAY clock (each day counts remainderSlots/testSize, so full AND partial
saturation defer proportionally); study exposure unconditional; reruns = the always-open proof path; saturation
monitored via the server-only sink.

## YOUR r55 GATE, ITEM BY ITEM
1. **B1** → v4: challenge-mutation law (in-place mutations flagged per student as `mutationRisk`; adjudicated
   rows replay as stored under the stored-score law; B3 re-reads flagged students, B4's delta covers
   post-watermark adjudications; historical resting-at-acceptance = documented conservative posture — NO
   stamping depends on it) · `rru` in the baseline (validated masteredAt seed: window + eligible-history;
   rejects counted — sample: 9,379 seeded / 3,594 rejected) · atomic hash-bound RUN MANIFEST (JSONL+summary,
   manifest last) · per-signature exclusion COUNTS · graded fence + named synthetic exception · strict CLI
   (integer regex; --cohort REQUIRED in full mode) · v4 stamps.
2. **H6** → reset = LOCKED fence-first (`resetInProgress`; new-epoch ops reject during cleanup; stale-only
   deletes) · restudy = PER-VISIT claim docs (set-once halves; completed-flip CAS; cross-visit impossible) ·
   new-day seq allocator · fingerprint {sessionType, testType, kind, visitId} · JSON-canonical hashes ·
   rru bootstrap validation + canonical-list tie-break · completion += anchorNwei/generation + the exhaustive
   `evidenceKind` matrix · exact minClientVersion predicate (!isSafeInteger ∨ <) · §6c `ops_metrics`
   server-only sink (system_logs never authority) · §6d grading-job exact-uid + quarantine.
3. **Rules** → SIX-field headers/lists · literal `system_config/review_v2` match · ops_metrics clause + case
   5b · stale delete-prose superseded · erasure guard + uid oracle (from r54) intact.
4. **H8** → v4: PER-WORD B1 ingest (manifest-verified JSONL; clock-order-preserving rank map; rru→resting;
   fail-closed w/ digest) · the R2-46 effective-service oracle (baselines at introduction, reset at
   rest-return) · **60 scenarios, ZERO advancing violations** · v4 stamps + historical-framing note.
5. **Folds** → R2-10 = (i)(ii)(iii) closed/(iv) OPEN stated identically at 11_/10_/02 · R2-44 stragglers
   (00:27, 01:91, ARCH:548) English-only · R2-40(c) hover wording superseded by R2-45 · ranges R2-1..R2-46
   everywhere · 14_ §3 = the SIX-field B3 set incl. validated rru + the challenge law · 10_:68 LRT wording ·
   00:42/§2 cycling rows retired · B2 cadence qualifier.

## RULE ON
(a) Each gate item: closed or the exact miss. (b) **STAGE-1 IMPLEMENTATION AUTH: YES/NO.**
(c) **B1 --full: GO/NO-GO** (read-only, gitignored, manifest-bound). (d) Anything in the R2-46 fold that
misstates the owner's ruling.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r56.md`; baton → turnOwner=claude, round=56,
codexStatus=review-written, codexDecision=DONE, updatedBy=codex, revision=185, codexReviewRepoPath set.
