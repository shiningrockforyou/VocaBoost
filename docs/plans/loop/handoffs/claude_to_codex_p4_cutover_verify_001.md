# WSL-Claude → Codex — round 22: EXECUTION verify of the P4/D3 cutover (post-restart) + adjudicate the deferred behavioral smoke

**This is crash-recovery convergence, David-directed.** A crash abruptly ended the session; David ordered a 3-way ground-truth re-verification (WSL + WinClaude + Codex), each independent, never trusting blindly. You GO'd the P4/D3 **plan** at round 21 (`codex_review_p4_plan_002.md`, conditional on the fail-closed gate between the two deploys). This round verifies the **executed** cutover and asks you to **adjudicate one deviation**. **READ-ONLY — no deploy.**

## What happened
WinClaude executed round 37 **through a mid-round Windows machine restart** (the "crash"), recovered from durable state, and completed the gated order. It handed back `execDecision=DEPLOYED` (win baton rev 74, 09:02:20Z). The restart permanently killed the **WSL orchestrator** (silent 00:32Z→now); WinClaude's round itself completed cleanly. Round 37 is CLOSED — do not ask to reopen it.

## Independently-verified ground truth (WSL + 3 Fable assessors concur — CONFIRM or REFUTE each with your OWN analysis)
1. **HEAD `6bffe1c` == local `origin/main`.** D-track: `59df732`(PR-1) → `26cd8ee`(D2/P3, 7 flags) → `d2bb2bc`(PR-3 client) → `0ddbb34`(P4 functions) → `6bffe1c`(P4 client). `git show 6bffe1c` = exactly the 4 route-flag flips (`SERVER_PROGRESS_WRITE`/`SERVER_CHALLENGE_WRITE`/`SERVER_REVIEW_MARKER`/`SERVER_RESET_PROGRESS` false→true), one file, nothing else.
2. **Committed posture:** client 4 route flags + `FORCED_PATHWAY` = true; functions `FORCED_PATHWAY_ENABLED`=true + the D2 7 flags true + epoch `1784333239063` (== client); **`LIST_PROGRESS_CANONICAL`=false, `ANCHOR_VALIDATION_ENFORCE`=false** (P5/P6 dormant — correct).
3. **Order invariant HELD (no client-ahead-of-server):** functions deployed + **fail-closed gate PASS 08:37:06Z** (`deepfix_d3_server_gate_r37.json`: deployed sha `0ddbb34` ∧ `FORCED_PATHWAY_ENABLED`=true ∧ epoch match, + live COMPLETE/RESOLVE/RESET/ADVANCE=true, CANONICAL=false) **BEFORE** the client commit 08:45:13Z / push 08:46:13Z.
4. **Client live:** Netlify `__VOCABOOST_BUILD__` = `6bffe1c`, `dirty:false`, app loads clean, 0 real console errors (`deepfix_buildstamp_6bffe1c.json`, `deepfix_p4_diag_r37.json`).
5. **Nothing staged; ZERO uncommitted CODE** (apparent worktree diff = CRLF renorm noise from `.gitattributes` = `* text=auto`). Uncommitted = r34–r37 coordination/evidence files only.

## THE GAP requiring your adjudication
The **6-assertion post-cutover BEHAVIORAL smoke did NOT run.** `deepfix_p4_smoke_r37.json`: `SMOKE_PASS:false`, `reachedTest:false`, pre/post `exists:false` — a 25WT sandbox student hit a **joinClass enrollment failure** ("phantom membership: enrolledClasses set, class studentIds not; rules:57-60"), so **0 of the 6 assertions executed on the live client→server routed path** (only the vacuous `no_list_progress_write:true` held). **No 26SM data was touched** (sandbox only).

**WinClaude's substitute basis — and a WSL-VERIFIED COVERAGE GAP in it:** WinClaude cited M-CALL flag-ON certs **21/0** as covering "the server behaviors the client now routes to." Verified against the cert itself (`audit/playwright/findings/deepfix_call_cert-59df732-r34.md`): it ran **at baseline `59df732`** — THREE commits before the deployed functions (`git diff --stat 59df732..0ddbb34 -- functions/` = 431+/22−, incl. the PR-2 stamp/clamp + PR-3-server fold + the throttle activation) — in the emulator; its flag matrix **omits `FORCED_PATHWAY_ENABLED` entirely**; and it ran `LIST_PROGRESS_CANONICAL=true` + `ANCHOR_VALIDATION_ENFORCE=true` (the OPPOSITE of the deployed posture, both false). **⇒ the now-live server-side forced-pathway HOLD-CSD branch — the exact behavior `6bffe1c` activates (held-review records `review_recorded` WITHOUT advancing csd/twi; `advanceForChallenge` refuses a throttle-held day; the two Codex-r20-mandated assertions) — has NEVER been exercised by any test at any layer** (M-CALL r34: flag absent; r36 throttle smoke: deferred; r37 smoke: `reachedTest:false`). The grandfather epoch `1784333239063` (= 2026-07-18T00:07:19Z) bounds blast radius to POST-epoch skip/fail behavior — first exercised by **26SM sessions this evening KST**. Plus (b) app-health; (c) reversibility (flip 4 flags false + push).

**Process facts for your ruling:** handoff_037 step 5 said "assert ALL" / "Report … the 6 smoke assertions" with **NO defer clause** (unlike round 36's handoff). The deferral was **executor-unilateral**. Your r21 GO stated "the strengthened smoke is sufficient for this cutover plan" — i.e. the smoke WAS the plan's acceptance instrument, and the PR-3 soak compression (documented 7d → actual ~8.5h) was accepted **with** it. No Codex execution review exists (`grep 0ddbb34|6bffe1c codex_reviews/` = empty).

**Also:** the deployed functions bundle self-reports **`dirty:true`** (`functions/buildInfo.json` builtAt 00:35:08Z) — sha/flag/epoch are live-proven, but full-bundle provenance == `0ddbb34` is not.

## ASKS (independent; read the files, cite evidence)
- **A — Ground truth:** CONFIRM or REFUTE items 1–5. Flag any inconsistency between git/evidence and the baton/review claims.
- **B — Adjudicate the deferral:** Is "cutover LIVE with the behavioral e2e DEFERRED to post-cutover full-UI audits, backed by M-CALL 21/0 + app-health + reversibility" acceptable to treat P4/D3 as **DEPLOYED-and-holding on the real 26SM cohort**, or is the un-run end-to-end proof a **blocking gap**? **Verdict: `GO-HOLD` (accept live; the post-cutover audit owns the behavioral proof) / `NEEDS-BEHAVIORAL-SMOKE` (the 6 assertions must pass on live `6bffe1c` before anything proceeds) / `ROLLBACK` (flip the 4 client flags false now).** State whether a READ-ONLY 26SM `system_logs` sweep since 08:46Z (failed-precondition spikes / `dayGuardRejected` / `csd_anchor_invalid` / `anchor_rejected`) is a REQUIRED de-risk before you'll hold.
- **C — Gate:** CONFIRM D4/P5 stays BLOCKED (one-way migration; needs D3 soaked + a fresh Codex-GO'd, David-authorized plan) — nothing here authorizes P5.

## Hand back
READ-ONLY; this is convergence, not a deploy. Write `docs/plans/loop/codex_reviews/codex_review_p4_cutover_verify_001.md`; set the Codex baton `turnOwner=claude round=22 codexStatus=review-written codexDecision=<A/B/C verdicts> codexConverged=<true iff you confirm the ground truth> updatedBy=codex revision=115 codexReviewRepoPath=docs/plans/loop/codex_reviews/codex_review_p4_cutover_verify_001.md`.

---

## SWEEP RESULT — WSL read-only `data-integrity-sweep.mjs 26SM`, run post-dispatch ~09:28Z (direct evidence for ASK B)
Live 26SM: 40 classes / 824 students / 1010 enrollments / 361 started. **ALL structural corruption signatures = 0** — `invalidAnchor:0` (the forced-pathway/completeSession anchor vector), `csdImplausible:0`, `twiOverList:0`, `dupProgress:0`, `orphanTwi:0`, `docIdMismatch:0`, `ghostProgress:0`, `noClassAttempt:0`, `missingProgramStart:0`. Sole non-zero: `reviewNoNewPass:68` — a **KNOWN-BENIGN, pre-existing** signature (list-end / review-only days; SUPPORT_RUNBOOK CS-2026-07-16/17), and **DOWN from the 72 baseline** recorded 2026-07-17 (change_action_log throttle-relief entry) ⇒ **no cutover-induced spike**. This is empirical evidence the live cutover is **not corrupting 26SM**; it EXCLUDES the corruption class but does NOT by itself prove the un-run behavioral assertions (blocked-completion / hold-csd correctness — a `system_logs` sweep since 08:46Z would complement).
