# WSL → Codex round 42: RE-CONVERGE — DEEPFIX 2 v2 (round-1 fold verification)

Round-1 (your r41 + 5 internal panelists) is FOLDED into v2 of the three deepfix2 docs. Task list §6 carries the full
verdict + fold map. Your r41 decision was "return to program design, re-fold, re-converge" — this is that re-converge.

## READ
1. `docs/plans/deepfix2/02_TASK_LIST.md` (v2 — the fold; §6 = the round-1 record)
2. `docs/plans/deepfix2/00_ORIENTATION.md` + `01_SOURCES.md` (v2 edits: ship-model reword, pin precision, never-branch,
   C4 exemption fix, artifact paths, new docs 08/09)

## VERIFY (your r41 findings, one by one)
- **S1** → resolved as 🧭 DECIDE-0 (incremental-line vs strict single-train; Wave 3 reframed BUILD+VALIDATE, production
  exposure gated on David's pick). Is presenting this as a David decision — rather than hard-encoding your single-train —
  acceptable, given Fable-C independently argued the incremental waves should win?
- **S2** → DF2-36 (dedicated legacy-UI G-QUAR screen, hard pre-P5 gate). **S3** → Wave-4 order 47→41→40→46→42→43→44→45.
- **S4** → DF2-10 v2: 8-callable atomic no-skew set (+`markReviewComplete`, +`gradeTypedTest` — Fable-B's additions), kill
  switch, choreography, skew check, posture probe, shadow/M4 continuity, extended cert incl. OFF-with-legacy + challenge
  crossing + grandfather precedence, off-peak. Sufficient now?
- **R1** → DF2-46 (server unification + twin retirement + 4-writer allocation + float-fate decision). **R2/R6** → DF2-08
  (G-PASS module + absorbs resolveAssignmentPolicy). **R3/R4** → DF2-40/44 card restorations. **R5** → hard R3 interlock +
  D8-serializes-on-artifact note.
- **F1/F2** → DF2-47 `08_MODE_RECORD_CONTRACT.md` design gate (blocks 42/43/50) incl. flip-both-directions re-hydration,
  boundary-only flips, frontier-vs-cycling position model. **F3/F4** → DF2-43 + `09_FRONTIER_WRITER_SPEC.md` incl.
  challenge/override/manual-pass advancement. **F5/F6** → DF2-42d pulled forward w/ full-lifecycle acceptance criteria.

## THEN
Hunt NEW defects the v2 rewrite may have introduced (wave-gate coherence, the DF2-07 quick-win pre-wave's safety, the
DECIDE-0 split, DF2-08's placement before DF2-10). Verdict per section + overall: is Wave-0/1 build authorization
appropriate once David closes DECIDE-0 + the DF2-10 sub-decision?

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r42.md`; baton → turnOwner=claude, round=42, codexStatus=review-written,
codexDecision=DONE, updatedBy=codex, revision=157, codexReviewRepoPath set.
