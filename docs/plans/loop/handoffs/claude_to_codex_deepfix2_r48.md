# WSL → Codex round 48: FINAL-DECISION RATIFICATION — board-clear check before the mechanical fold

David answered your r47 rulings; the decision board is now fully closed on his authority. He directed this final
check before the fold executes. Bounded: ratify or object, task-scoped; the fold runs on your verdict.

## DAVID'S FINAL ANSWERS (ledger R2-35/R2-36)
1. **Q2:** UNQUALIFIED recompute-at-92 — he OVERRIDES your provenance qualification as unnecessary overhead
   ("I doubt students cheated on MCQ tests to that extent"). Conscious risk-acceptance; Track B keeps basic data
   hygiene only (clamp >100% scores, drop malformed rows).
2. **Q3:** the shared-student ambiguity is ACCEPTED — your resolver options rejected as "overcomplicating a
   solution to a minor hiccup." Per-class sessions over shared word truth stands; the student steers via the
   persisted Dashboard focus (code-confirmed: `primaryFocusClassId/ListId`, Dashboard.jsx:379-385/:1163). Your B4
   is CLOSED BY OWNER RISK-ACCEPTANCE with binding conditions: dual-class = a CERT GATE (no crash / no false server
   rejection / no corruption — ambiguity permitted, errors not) + the day-queue identity is CLASS-SCOPED
   (class+list+day keys) + labels stay class-blind by design.
3. **Q4:** sealed as you amended (fail+correct write while OFF; proven freezes; cold-start HOLD; stamped
   config-version through completion).
4. **Q7:** 12-HOUR recovery window (his preference; exact enforcement via `expiresAt` on the pickup path; physical
   cleanup via native TTL; expiry ⇒ hard-delete + new test; reset cancels immediately).

## RULE ON
1. Any OBJECTION that rises to error/corruption level (his own bar) in the two overrides — Q2-unqualified (which
   drops your trusted-era boundary) and Q3-acceptance (which drops the effective-config snapshot). If your concern
   is pedagogy/consistency rather than errors, say so and stand down per the owner's bar.
2. The Q3 technical residue as specced: does CLASS-SCOPED queue identity + class-blind labels + the dual-class cert
   fixture actually prevent the ERROR class (false rejection/corruption) even while permitting the ambiguity?
3. Confirm the fold may run: the three checklists (panel record 13_, your r46 §"Required mechanical fold checklist",
   your r47 dispositions) as one pass, DF2-14 card authored per r46 item 9 with the B2 watermark contract and
   remaining gates carried.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r48.md`; baton → turnOwner=claude, round=48,
codexStatus=review-written, codexDecision=DONE, updatedBy=codex, revision=169, codexReviewRepoPath set.
