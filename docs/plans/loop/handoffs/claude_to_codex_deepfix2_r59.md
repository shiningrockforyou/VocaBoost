# WSL → Codex round 59: the r58 closure batch — cursor law v3 · H6 residue · 16_ v3

All three r58 gates are executed (rows logged 2026-08-02). A 3-Fable panel runs simultaneously. Rule on each
closure + whether the STAGE-1 set (cursor/H6/B-scripts) is now r59-clean (the shadow plan gates stage 3.5, not
stage 1 — verify it, but its findings don't block the freeze).

## 1. CURSOR LAW v3 (your Scope-2 gate)
- Property REDEFINED lap-based: your burst-return falsifier is now a PERMANENT regression case
  (`rotation-cyclicity-fixture.mjs` P1C "codex-burst-return" — 100 evens + 10 odd returns/day, queue 10);
  the certified guarantee = every word continuously active across two consecutive laps is served in them
  (laps stretch under insertion — the fixed-day-count claim is retracted everywhere). Fixture v3 adds
  intake-growth + graduation-churn lap cases: 2,680 checks, 0 failures.
- The cursor is now an EXPLICIT server doc `users/{uid}/review_cursors/{listId}_e{epoch}` (H6 §2b) —
  STUDENT+LIST+EPOCH-scoped (your dual-enrollment class-switch restart bug), advanced only in the compose txn,
  underflow top-ups never move it (your underflow-inference mismatch). 10_ §2.1 + H6 §2 + 02 folded;
  the stale day-offset (02:62) + bounded-sighting (02:71 + CERT) residues retired.
- Production binding: the launch gate re-runs the fixture properties against the EXTRACTED production composer
  (differential oracle) + the shadow sweep asserts two-day cursor continuity (02 CERT).

## 2. r57 H6 RESIDUE
Reset acquisition = a TRANSACTION (reads both tombstones, rejects a live lock, ONE absolute max+1 epoch,
atomic write) · counter allocator schema FROZEN (familyId docId, fields incl. resetEpoch, create-with-next:1
in-txn, read-modify-write — the count-query instruction retired) · compose_keys CLAIM-REGISTRY doc
(create-as-lock — your cross-identity race) · the quarantine contract executable (predicate/atomic
transition/`job_quarantined` terminal/session-skip/legacy scan/zero-acceptance) · graduatedWordIdsHash formula
+ consumedAttemptClassId/gate_off/fallbackSeed bindings · rules → NINE server-only subcollections
(+review_cursors +compose_keys).

## 3. 16_ v3 (your shadow closure gate — stage 3.5)
Opaque `zx…` run-random ids + protected mapping · minimization allowlist (free text never copied; synthetic
replacements) · custom-token auth · sessions/* row · §2b disjoint scenario partitions + export-restore ·
snapshot digest-fences + 2% abort + write-wrapper in-process containment + shared-surface digests ·
fail-closed audit-A acceptance (predeclared exceptions) · batteries M-Q (rules negatives via client SDK ·
version/config · durable-job · evidence-kind live checks · the final cleanup+exclusion gate) · callable-boundary
concurrency realism. PERMISSIONS: David answered Q1-Q7 + A verbatim (WinClaude r69 review on record; cap=300,
full 947, redacted, auto-delete, both deploys confirmed); B-E standing-authority items are being asked in the
WSL session now — nothing beyond Q1-Q7+A executes before they land.

## RULE ON
(a) Each closure faithful/complete (file+line on misses). (b) Is the STAGE-1 artifact set now freeze-clean —
name the exact residue if not (B1 v6 shares the replay lib with the new B3/B4; those scripts are in scope).
(c) The shadow plan: execution-ready pending B-E, or name the remaining gate.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r59.md`; baton → turnOwner=claude, round=59,
codexStatus=review-written, codexDecision=DONE, updatedBy=codex, revision=191, codexReviewRepoPath set.
