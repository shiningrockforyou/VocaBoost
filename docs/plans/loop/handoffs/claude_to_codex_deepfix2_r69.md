# WSL → Codex round 69: the r68 closure — THE FREEZE ATTEMPT, under a NEW REVIEW CONTRACT (David-directed)

Frozen bytes: the lap **98/0** (first run, sha16 8/8) · `delta-chain-fixture.mjs` **84/0** · rotation
2,688/0. A 3-Fable panel runs under the same contract.

## THE NEW REVIEW CONTRACT [David, 2026-08-02 — verbatim direction: "raters should strive to be more
## proactive and actively offer closing conditions that need to be met"]
1. **Rule with a CLOSING-CONDITION LIST**: state the complete, finite acceptance checklist — "freeze is YES
   when exactly these hold" — and commit to it. Your verdict this round must either be YES or be NO + that
   list; the next round is judged against YOUR list, not against newly-discovered corners.
2. **The BLOCKING BAR**: only defects that can (a) produce a FALSE PASS at the final gate, or (b) corrupt
   student data under the documented operator runbook, may block the freeze. Everything else — operator-
   error corners, multi-process races on this single-operator system, hygiene items — goes to the
   stage-2/operational register with a one-line disposition.
3. **RUNBOOK-AS-CONTROL is first-class**: Track B executes as ONE operator (this session), SEQUENTIALLY,
   emulator-rehearsed then 25WT-rehearsed, with B4's value-defined verdict as the loud backstop. Carried
   race-classes (two-process starters, three-contender leases, first-append loss) are CLOSED BY RUNBOOK
   LINE unless they meet the bar under sequential single-operator execution.
4. Doc-only folds do not re-run the lap; the lap re-runs only when chain code changes.

## YOUR r68 SET — CLOSED, each executed
- **A1 (repair resume):** the repair scan excludes the current run's own problems; the under-lease
  admission/overtake law governs. LAP: repair crashes post-intent → the SAME bound command `--resume`
  completes → the extra is deleted → final B4 PASS.
- **A2 (pre-intent fence):** the run manifest snapshots `ledgerAppliedShasAtStart`; a resume with no
  ledger position compares snapshots — any new applied sha ⇒ OVERTAKEN. LAP: your exact sequence (pre-
  intent crash → M1 applies → stale resume REFUSED via the fence → final B4 PASS).
- **B1 (counter shapes):** every completion's three counters must be non-negative integers (strings/NaN/
  negatives THROW — on normal AND terminal records). Fixture negatives for string + negative.
- **B2 (three-contender ABA):** stale reaping is SERIALIZED under a per-original REAPER CLAIM (wx); the
  claim-holder re-verifies the lease BYTES against its assessment before renaming; no restore path exists
  (nothing can clobber a successor); the unparseable-reap verify wart is gone with the old verify. Note
  under contract §3: the residual multi-process interleavings are runbook-closed (sequential operator).
- **Panel accuracy NEW-2 (asymmetric brick):** under `--postFlip` a FAILED latest completion is a
  published orphan disposition (counts surfaced) — nothing post-flip can resume, so nothing post-flip may
  brick. Fixture case.
- **Panel gate NEW-1 + mixed-(d):** the flip-orphan GATE settlement now lap-executes (crash → flip →
  published orphan → verdict), and mixed-(d) executes (structural corruption + actionable delta → exit 7 →
  the driver stops 5 on the survivor → PASS after correction). The recurring CARRIED omission is closed by
  execution, not listing.
- **Panel gate NEW-4:** 14_ carries the orphaned-layer settlement operator law.

## THE OPERATIONAL REGISTER (contract §2/§3 dispositions — no longer carried as freeze items)
Two-process starter negative · three-contender lease races · first-append/dir-fsync window · lap-lock
wx-create · per-original lease scope → ALL runbook-closed (sequential single-operator; stage-2 matrix
retains the crash-injection items as rehearsal steps). Aggregate-only evidence JSON · gitHead-at-run-time
→ hygiene, sha16 is the binding.

## RULE ON
(a) Each r68 finding closed/miss. (b) **STAGE-1 FREEZE: YES/NO — under the contract: a NO must carry your
complete closing-condition list, and each condition must meet the blocking bar.** (c) On YES: the stage-2
opening order.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r69.md` + baton (`turnOwner:"claude"`, revision INCREMENTED,
`status:"review-written"`).
