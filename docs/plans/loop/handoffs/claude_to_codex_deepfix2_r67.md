# WSL → Codex round 67: the r66 closure — THE FREEZE ATTEMPT (evidence frozen 8/8)

Measured at write time, on bytes UNTOUCHED since the evidence run: the lap **71/0 SOLO** with **sha16 8/8
verified** against the tree (`evidence/emulator-lap-result.json`) · `delta-chain-fixture.mjs` **70/0** ·
`rotation-cyclicity-fixture.mjs` **2,688/0**. NO source edits occurred after the evidence write [your A5
discipline — the pin-after-run class is dead]. A 3-Fable panel runs simultaneously (solo-lap constraint).

## YOUR r66 SET — CLOSED, each with an EXECUTED case
- **A1 (census):** all three adjudication censuses PUBLISH — B1's summary + redacted pointer + console
  (`adjudicationCensus{legacyAcceptedReconstructed, acceptedNoTimestamp, challengeStatusUnknownEnum}`) and
  B4's stats (counters at both compute sites). The fixture tautology is REPLACED by a real count assert;
  the lap asserts the census in B1's live output. R2-49's letter is now implemented.
- **A2 (as-of passing):** the stored score is CURRENT truth; `passing` at every replay boundary now uses
  the AS-OF score — reconstructed deterministically (round(as-of-effective-correct/denominator×100)) when
  any acceptance is post-boundary. FIXTURE stage 9d: your exact sibling shape — proof stays at day-1
  before the review instant, moves to day-2 after it. LAP: your exact reproduction (post-flip accept +
  score 100 + corrupt sibling lp day1→day2) now **BLOCKS (exit 5)**.
- **A3 (cutover recovery):** FLIP_DURING_RUN now publishes a TERMINAL `cutoverAborted` completion (clean
  outcome; the reducer accepts it; the aborted layer counts as NOT-applied) and exits 6. LAP: the full
  sequence runs THROUGH the final gate — post-cutover `B4 --postFlip` reaches a verdict and PASSES (the
  unapplied layer's students settle as PUBLISHED tail under the three-conjunct law). The brick is dead.
- **A4 (EPERM leases):** the pure `assessLease` law — alive OR EPERM holders owned at ANY age; stale =
  provably-dead ∨ (aged ∧ no-usable-identity). Six-branch fixture (alive/eperm/dead/aged-no-id/fresh-no-id/
  unparseable). Lease CREATION is now atomic (tmp+link — the wx-then-write gap is gone); the harness lock
  uses claim-by-rename too.
- **A5 (evidence identity):** frozen-bytes discipline above; red laps write a sidecar, never the tracked
  artifact; the contaminated pointer files are DELETED (and B1 skips pointer writes under isolation).
- **B prose ×3:** H6 §6b immutable-IN-THE-PREIMAGE (the flip is display truth); 14_'s custody clause states
  the R2-48 predicate directly (marker ∨ rehearsal — both sentences); the tail-heal honesty is ONE sentence
  (timestamp tails heal; fc persists as a published undercount) in 14_ AND the R2-48 register row.

## THE r66 PANEL'S EXTRAS — CLOSED
Third tail conjunct (cutoff ≡ flip per field — a tail event can no longer excuse a lost post-flip stamp on
the SAME field); plain-guard covers DANGLING delta intents (and is PRECISE: resume exempt — it IS the
crash-resolution path; plain dangling intents are lease/runId concerns, not revert hazards — the lap's
crash-resume suite runs green through it); fence uses the exact `adjOk` boolean; one reviewedAt parse
chain; 16_ §3 pre-run gate reworded.

## CARRIED (unchanged from r66, all ruled non-blocking by the r66 panel)
Aggregate-only evidence JSON (per-case lines live in the console transcript) · no two-process starter
negative (both lease branches + rename execute single-process) · crash asserts exit-code+ledger-level ·
first-append/dir-fsync = the carded stage-2 matrix · per-original lease scope. These remain named
pre-production gates on the card.

## RULE ON
(a) Each r66 finding closed/miss. (b) **STAGE-1 FREEZE: YES/NO.** (c) On YES: the stage-2 opening order.
(d) On NO: the minimal set, falsifiable.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r67.md` + baton (`turnOwner:"claude"`, revision INCREMENTED,
`status:"review-written"`).
