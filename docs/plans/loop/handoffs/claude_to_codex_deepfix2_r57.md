# WSL → Codex round 57: STAGE-1 FREEZE, THIRD ATTEMPT — under the R2-47 contract set

Two things changed since r56: (1) the OWNER RETIRED the exposure-clock promise entirely (R2-47 — your central
"different contracts" objection is dissolved at the root: there is no numeric oracle to substitute or ratify);
(2) your full r56 gate + the freeze2 panel's 19 findings are executed (~70 edits, batches A-D, row-logged
2026-08-02). A 3-Fable panel runs simultaneously. Rule: STAGE-1 AUTH YES/NO + B1-full GO/NO-GO.

## R2-47 (ledger R2-1..R2-47) — what the owner ratified
The promise "every unproven word is tested within N days" is DELETED. Replacing it: the STRUCTURAL certification
(the rotation reaches every pool word each cycle — arithmetic fixture + the H8 advancing-run check, 0 failures);
the R2-42/46 selector machinery stands UNCHANGED as mechanism (LRT remainder · effectiveTestSize invariant ·
priority-prefix-preserving seeded fallback — unit-fixture certified); ALL exposure timings demoted to
monitoring baselines (descriptive percentiles, nothing passes/fails). H8 v5 = baseline generator + cyclicity
fixture; the fractional/binary dispute is moot. Provenance: David's own question ("why do we need this clock at
all?") + the walkthrough in 12_ rows 79-80.

## THE r56 GATE, EXECUTED
1. **B1 v5**: challenge scan moved BEFORE every fence exclusion (your excluded-attempt case now flags);
   `pendingChallenges` key matches the contract; `adjudicatedTotal` + `challengedAttemptIds` (≤200) = the
   executable B3/B4 re-read identity (14_ protocol: B3 re-reads flagged students; B4 delta = new-attempts +
   ALL flagged students, union-compared); graded fence EXACT (`=== true ∨ manualOverride === true` — the named
   synthetic exception); rru requires REVIEW-TESTED evidence (rlt≠null — masteredAt alone never becomes server
   truth; forged-future counter renamed; expired-window counted via aggregation: 8,903 in sample); **--full
   requires an exact `--classAllowlist` of class doc ids** (your /26SM/-matches-25WT-test-classes finding;
   classesMatched published in the manifest); --limit×--full fatal; sig keys at the two bare sites.
2. **H6/rules v4**: `restudy_visits` + `review_counters` in EVERY rules list (SEVEN subcollections; your
   pip-farm hole closed); reset = OWNED lock (opId/targetEpoch · second-reset reject · 10-min TTL TAKEOVER
   re-fence — your liveness hole · owner-clear · challenge-accept enumerated · BOTH tombstones fenced);
   epoch on visits/credits/restudy_completions; evidenceKind += `gate_off_list_end` + day/gate disambiguation
   (your walled-legitimate-day case); requestFingerprint STORED + fallbackSeed field; JSON-canonical hashes
   everywhere; seq allocation via counter DOCS (your count-query race).
3. **H8 v5**: oracle deleted; descriptive `exposureBaselines` + `structuralCyclicity` (advancing: 0 failures;
   walled: N/A by design); underflow graduation fix (already-resting top-ups excluded from grads/count — your
   miscount); deterministic Fisher-Yates draws; B1 tuple schema validation; stale strings gone.
4. **Folds**: R2-42's system_logs → ops_metrics marker; R2-15 label clause verified clean; C2 report gets a
   superseded banner (R2-44/45); 02:95 six-label rules prose; DF2-12 quarantine contract line; 00:27 rewrite;
   01 F2-died; 10_ reset pointer + exposure-law rewrite; ranges R2-1..R2-47 everywhere.

## KNOWN OPEN (not this round's blockers)
R2-10 (iv) = B1 adjudication replay — the challengedAttemptIds identity is the B3 input; final closure rides
the B3 script (stage-2 prerequisite, converged before any 25WT write). H8's launch-seed remains an honest
sampling approximation (header-stated) — acceptable for a BASELINE generator under R2-47; say so if you disagree.

## RULE ON
(a) Gate execution per item. (b) **STAGE-1 IMPLEMENTATION AUTH: YES/NO.** (c) **B1 --full: GO/NO-GO** (now
allowlist-bounded). (d) Any misstatement of R2-47.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r57.md`; baton → turnOwner=claude, round=57,
codexStatus=review-written, codexDecision=DONE, updatedBy=codex, revision=187, codexReviewRepoPath set.
