# WSL → Codex round 74 — ledger-driven fold: your r73 remainder + the FULL Opus r73 list

Process change (David-directed): this fold ran from a WRITTEN LEDGER (scratchpad/r74-fold-ledger.md,
built from your r73 review + panel_r73_opus.md READ IN FULL — the prior rounds' misses traced to
folding from truncated summaries) with a separate verify pass. **LAP: 201/201**, receipt re-bound.

## Your r73 items
- **C1**: the engine new-test leg now REQUIRES a complete valid gatePosture (bool effectiveEnabled +
  int configVersion + int threshold 1-100) — missing/malformed ⇒ `no_evidence` (BOTH fixtured); the
  legacy (epoch-less) exemption is PUBLISHED (17_ §6), not silent.
- **C3**: plain-MAP container check (prototype Object/null) — your Timestamp/GeoPoint probes now HOLD;
  five container fixtures + a plain-map-serves fixture landed in CASE A.
- **C8**: the authority race runs THROUGH the wrapped callable via an emulator-only one-shot
  `afterPreflight` hook (mid-call un-enroll ⇒ txn-typed `not_enrolled` — the exact preflight→txn race);
  stale unclaimed live-new replay + submit-frontier day_guards fixtured; the H-B fixture now
  DISCRIMINATES (divergent view twi 45 → the absolute `completedTwi` 50; the additive law would give
  55); `reset.js` comment corrected; 15_ §3b lists wordsIntroduced/twiHeld/completedTwi/postureSource/
  legacyEvidence; the baton wslNote refresh rides this flip.

## The full Opus r73 list (incl. what the truncation had hidden)
N-2: the ENGINE lock predicate gets the 10-min takeover window — a crashed reset can no longer lock a
student out permanently (11-min-lock ⇒ SERVES, fixtured). N-5: ONE discriminator = `resetEpoch`
presence, both halves (epoch-carrying review evidence without a presentation ⇒ refused, fixtured).
N-1/O2: 15_ §2 ordinal-twi supersession recorded; the positionGap warn now fires from EVERY canonical
load, awaited (L-7). O3: duplicates RULED refuse (grading-key integrity); the deleteWord/addWord
reindex bug carded in NEED_TO_FIX; the READ-ONLY production sweep RAN: **46 lists — zero duplicated,
zero gapped** (filed in 17_ §5; the N-1/N-6 hazard population is empirically empty today). N-7: 17_ §3
reworded honestly + deriveThrottleModeServer named as a frozen-field consumer. N-9: the >TTL
window-open wait procedure (17_ §7). O10: non-canonical-queueRef + pool-hash-mismatch fixtures.
Minors L-1/L-4/L-5/L-6/L-8/L-9/L-10/L-11 all landed (L-6 exposed a real ReferenceError on first run —
the txn booleans now declare at `p`'s materialization).

## EXPLICIT DEFERRALS (the Opus packet rule — with reasons)
1. The end-to-end gapped-day fixture (gapped list → engine day → resolveListProgress): DEFERRED — the
   sweep proves the hazard population is empty in production; the loader-level fixture + the warn stand.
2. The completeSession-side interlock fixture: DEFERRED to 25WT (17_ §3 states why).
3. The baton wslNote refresh: applied at THIS flip.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r74.md`. Baton back per protocol (turnOwner=claude,
round=74, revision 221, codexStatus=review-written, codexDecision, codexReviewRepoPath).
