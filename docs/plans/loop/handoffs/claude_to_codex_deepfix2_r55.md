# WSL → Codex round 55: STAGE-1 FREEZE VERIFICATION — the r54 residue + David's four rulings are folded

Everything you named in r54 is executed, and the owner answered the load-bearing questions. Round 55 = the
final stage-1 closure check. A 3-Fable panel runs simultaneously (handed off as the agents launch).
If clean, STAGE-1 IMPLEMENTATION AUTH is declared and the dark build (stage 2) is next.

## THE OWNER RULINGS (2026-08-01, ratified R2-42..45 — ledger now R2-1..R2-45)
- **R2-42 (your Q-D1)**: deterministic LRT remainder (same clock/tie-break as the slots; display shuffled) +
  post-compose invariant check + SEEDED-RANDOM FALLBACK (`compositionVersion:'fallback-random'`, logged,
  monitored) — the owner's own fallback condition; fail-open-safe because selection is not correctness-critical.
  H8 reruns the deterministic selector; fairnessLaw RESOLVED; a fairnessBoundNote records the measured
  launch-transition backlog peak (48 vs bound 40, one seed, priority-flood pedagogy — monitoring note).
- **R2-43 (your Q-D2)**: challenge-on-resting guard — grade fix always, status write skipped while resting;
  H6 §6b; R2-10 conditions (i)(ii)(iii) now closed ((i) = 10_ §2.0's normative bounded-pinning theorem;
  (iv) rides B1).
- **R2-44**: launch UI ENGLISH-ONLY (Korean = definitions/content only; no site language toggle exists — future
  card only on the owner's ask; supersedes ko/en on DF2-11 AND the DF2-32/06_ messaging register).
- **R2-45**: pip overflow = five pips + a static "×N" total (icon+total convention); no tooltip primitive.

## THE R54 EXECUTABLE CLOSURES (all row-logged 2026-08-01)
1. B1 v3: per-attempt watermark fence (`postWatermark` bucket — composite-index-free) · strict CLI · sessionType
   whitelist + answers-must-be-array · teacher-edit law (preOverride governs; `editedNoOrganicScore`) ·
   dupWordIdInRows fixed · content hash += tq + order-insensitive · per-signature rows · explicit epoch-0 ·
   atomic tmp+rename · synthetic-anchor blank carve-out · jointMix emitted (matches your independent numbers
   exactly: 83.06/2.62/6.78/7.54) · **output MOVED to gitignored `audit/deepfix/trackB_baselines/`** (repo keeps
   a uid-free pointer) · snapshot limitation documented. B2 v2: caps merged, write-cadence histogram, version 2.
2. H6: exact pairing law (opposite-type + same-visit; same-type/cross-visit REPLACE) · §3b nullability laws ·
   composeKey request-FINGERPRINT (`compose_key_reused`) · presentations for ALL FOUR graded types ·
   fence-first reset (epoch bump → deletes → reconciliation) · minClientVersion frozen (integer, forced-refresh
   chosen) · **`reviewRestingUntil` — the sixth server field: pool/resting/underflow derivation reads ONLY
   server truth; client `status`/`masteredAt` are NEVER behavioral inputs** (§10 rewritten; backfill seeds it
   from legacy masteredAt once).
3. Rules: six-field deny lists · label-ERASURE delete guard · grading_jobs `uid` oracle fix · :270-322 cite ·
   case-1 split + case-10 pool-forgery inertness.
4. H8 v3: fail-closed B1 joint-mix ingest w/ source digest · cumulative disjoint seeding (dead branch gone) ·
   first-sighting + censor-at-exit accounting · null-checks · 3 seeds on specials · deterministic selector.
5. Fold residue: 11_:66 zero-graduation · R2-15/41d rename annotations · 10_/02 R2-10 four-conditions wording ·
   00:8 single range · 00:59 cycling row · 02:127 F2-died · §4 D7/P9 RESOLVED · ranges r50-r54/R2-38..41.

## RULE ON
(a) Closure of each r54 item (file+line on any miss). (b) The R2-42..45 folds: faithful + contradiction-free.
(c) **STAGE-1 IMPLEMENTATION AUTH: YES or the exact residue.** (d) B1 --full (read-only, gitignored): now GO?

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r55.md`; baton → turnOwner=claude, round=55,
codexStatus=review-written, codexDecision=DONE, updatedBy=codex, revision=183, codexReviewRepoPath set.
