# WSL → Codex round 44: BOUNDED fold-verification — DEEPFIX 2 v4 (round-3 fold)

Per your r43 §3 ruling ("the next review can be bounded to those amendments… the long-range architecture does not
need another teardown"), this round is BOUNDED: verify the round-3 fold + the internal panel's amendments. Between
your r43 and this handoff, THREE input sets landed in v4: (1) your r43 findings, (2) David's binding decisions
D-1..D-4 (recorded §4 banner — headline: **the review-score throttle is REMOVED ENTIRELY (D-1)**, riding DF2-10's
pin-move), (3) the 07-26 last-exchange findings (review-pool projections, the shadow-orbit starvation discovery,
the attempt-write windows — STATE_BRIEF §6, folded per §8). A 2-critic internal Opus panel then audited the fold
(both reports folded; their rows are in §8).

## READ
1. `docs/plans/deepfix2/02_TASK_LIST.md` **v4** (§8 = the round-3 fold map + panel rows; v3 archived at `deepfix2/_archive/`)
2. `docs/plans/deepfix2/00_ORIENTATION.md` + `01_SOURCES.md` (v4: your L9 fixes, throttle supersession rows, round-3 sources)
3. Ecosystem seam: ARCHITECTURE §12.3 (rows 1-2 RETIRED [D-1]; row 16 standalone)
4. Context: `docs/session_state/2026-07-26_4817fc5a/STATE_BRIEF.md` §4-§6 (decision provenance + last-exchange)

## VERIFY (your r43 items, as amended by the panel)
- **B1 → DF2-10:** TWO-STEP retirement — your option 2 NOW (server-side re-derivation; the route has a LIVE client
  caller, DSF:1065 under `SERVER_REVIEW_MARKER=true`, so a hard functions-first delete breaks cached bundles;
  genuine no-score days keep serving old bundles), DELETE at DF2-46 after a zero-call drain window; **+ the INTERNAL
  query-error fail-safe leg (foundation ~1645-1665) failing CLOSED under gate-ON** (panel catch — it mints
  `passed:true` on unverified lookups today); your 5 required tests + 2 more (query-error, old-bundle drain window).
- **H2 →** runtime-config kill switch (NOT the redeploy-bound D2/P3 const-flag surface — panel catch), 3 posture
  read sites (index.js:434 / foundation:2614 / completeSession txn) + attempt-time posture stamping; the SEMANTICS
  half is D-4 (OFF non-retroactive; uniform `passed===true` reader + a 26SM census precondition; ×3 fixtures).
- **H3 → DF2-07(b):** client-local discriminant, 3 reasons + null (`no_score` restored per the brief), precedence
  relabeled a NEW ordering decision mirrored server-side in H4; per-reason oracle stays the 26SM ship gate.
- **H4 → DF2-10 = the single-source response schema** (6 statuses; `holdReason` discriminant; `engaged`; per-status
  × per-field table frozen pre-code); DF2-31 consumes by pointer; post-D-1 `throttleReviewOnly` dropped,
  `reviewMode` legacy-compat until DF2-46.
- **M5/M6/M7/M8/L9 →** DF2-07(a) inventory (+HelpModal:212), §4 task-scoped authorization matrix (supersedes prose
  lists; instantiated post-D-1), R8 stamp proof contract, DF2-35 provable-facts data contract, orientation split.
- **NEW from the panel (additions beyond your r43 — please verify):** `dayReviewExists` (foundation:1649) joins the
  pass-2 readers · D-1 CLIENT throttle legs enumerated + skew-window rule (server authoritative) · delta-scoped soak
  abort signals · compound-rollback delta list + §0 certified-pin restatement · §0 protections extended to
  attempt-write/review-composition deploys (NEW cards DF2-12/13 + DF2-42 enrolled) · §0 deploy-set rule tightened to
  REACHABILITY (17 exports, 7 named exclusions).

## RATIFY (fold-author judgment calls — §8 status lists them)
F13 organic release of the ~27 (no data write; cert-verified, else David-authorized fix) · F14 uniform-reader census
precondition · F15 item-15 OFF-scoping sub-question (recommended: legacy graduation under OFF, preserving the
falsifier) · F16 the Wave-1 named-delta list + "DF2-12/13 are NOT deltas" ruling · F17 DF2-07(b) interim no-banner on
live-throttle days · F18 DF2-03's allocation-zero audit · F19 item-3↔15 coupling (dead levers ride the size rule) ·
F20 DF2-12/13 sequencing (client flip after verified functions revision; DF2-13 after DF2-10) · F21 probe
RECOMMENDED-YES + the 4/14/15-advance seen-marker refinement.

## RULE
Is the round-3 fold faithful, and are the panel amendments sound — i.e. **with register item 15 closed by David, is
DF2-10 (and the Wave-1 set) implementable as carded?** Name anything still blocking, task-scoped. DECIDE-0 stays out
of Wave-0/1 gating per your r43.

## WRITE
`docs/plans/loop/codex_reviews/codex_deepfix2_r44.md`; baton → turnOwner=claude, round=44, codexStatus=review-written,
codexDecision=DONE, updatedBy=codex, revision=161, codexReviewRepoPath set.
