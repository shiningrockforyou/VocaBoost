# 3-Fable panel — DEEPFIX2 r65 (2026-08-02)


---

## LENS: gate

A sibling reviewer's solo lap is live (pid 61642 holds the wx lock) — the lock mechanism is working and I correctly stayed off the lap. My audit is complete: static against the sha16-verified tree plus the offline fixtures. Composing the verdict.

# DEEPFIX2 r65 — Panel review. LENS: the post-flip gate end-state (B4 `--postFlip` as one machine)

**Reviewer basis (measured on head `4fbe2e1`, working tree clean for `scripts/deepfix2/`):** per-script sha16 of all eight chain scripts **matches `evidence/emulator-lap-result.json` exactly** (verified byte-for-byte against the tree — the 54/0 evidence is bound to this source) · `delta-chain-fixture.mjs` **57/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` clean ×11 · read in full: b4-verify, b1-replay-lib, b-baseline, b3-backfill-writer, b-emulator-lap, b-delta-cycle, B1's publish path, 14\_ §4, 15\_ §7, 02\_ DF2-14 card + deploy line, 11\_ R2-32 row, 16\_ §3. **Lap constraint honored:** at review time a sibling reviewer's solo lap was live (pid 61642 holding `audit/deepfix/emulator-lap.lock`) — I did not run the lap; the wx lock is observed working in anger. My execution evidence is the sha16-bound artifact + the two offline fixtures.

## (1) r64 findings — CLOSED / MISS

| Finding | Verdict | Evidence (file:line) |
|---|---|---|
| Codex A1 forged `--postFlip` boundary | **CLOSED** | `b4-verify.mjs:103-114`: marker read, `toMillis` type-check, **exact equality** or FATAL 2; `marker` {value, configUpdateTime, enabled} in the report (`:113,:294`). Lap executes forged-boundary REFUSED + wrong-value-at-true-boundary DIFFS (`b-emulator-lap.mjs:256-261`). |
| Codex A2 rejected-challenge whole-word skip | **CLOSED** (one letter-residue) | Census deleted (`b1-replay-lib.mjs:44-47`; zero `adjudicatedRecent` hits in B4). Adjudication law: `rows.push({ok:isCorrect, okEff:isCorrect\|\|accepted})` (`:89`), fc/lf from `ok`, lc/lp from `okEff` (`:127-128`). Corrupt checked before anything (`b4:217`). Lap: rejected+corrupt-fc = DIFFS, accepted = PASS (`lap:263-283`); fixture 9c (`delta-chain-fixture.mjs:351-354`). Residue: no malformed-status *validation* bump — any unknown string silently behaves as rejected. Fail-closed (can only under-mint, producing red not green), but Codex's "validate a closed enum" letter is met in effect, not in shape. |
| Codex A3 `!src.row` re-enrollment skip | **CLOSED (strict direction)** | Skip deleted; uncovered uids counted + listed + **blocking** (`b4:192`, `uncoveredAtGate` in report `:294`). Lap: pre-flip joiner BLOCKS + listed (`lap:292-301`). But see NEW-D: 14\_ §4 still carries the dead advisory sentence. |
| Codex A4 valid post-flip new-word false-red | **CLOSED** | Universe rebuilt from the through-cutoff replay; fc = cutoff value, timestamps = flip value or null (`b4:191-199`); per-field exemption for ≥-flip stamps (`:220`, `b-baseline.mjs:38-47`). Lap: valid post-flip new-word failure PASSES (`lap:284-291`). |
| Codex A5 owner authority | **HONORED** | Everything marked PROPOSED–PENDING DAVID: `14_:109`, `15_:206`, 02\_ deploy line, `11_:48` (R2-32 ⟶ annotation present). No freeze ruled this round; correct posture. |
| Codex A6 lap scope/isolation | **CLOSED for isolation; PARTIAL for scope/evidence** | `DEEPFIX_AUDIT_ROOT` honored chain-wide (`b-baseline.mjs:11-15`, `b3:205,297`, `b4:57,278`, B1 evidence-pointer gated `b1:213`); wx lap lock with pid-liveness (`lap:43-57`) — observed refusing-capable live this round; adversarial matrix + adjudication case executed. Still short of the 02\_ card's letter: see NEW-C/NEW-E. |
| Codex B1 shadow §3 contradiction | **CLOSED** | `16_:117-122` one-sentence consumption law + reworded pre-run gate. |
| Codex B2 repair/delta custody race | **CLOSED** | Exclusive per-original execution lease, wx + pid-liveness + 2h takeover, released after completion publication (`b3:235-257`); reality scan re-runs **under** the lease (`:256`) and refuses dangling intents (`:177`); repair absent-ledger now FATAL (`:166`). |
| Panel: B1 A8 exit 3 (lap NEW-3 / lawclose N1) | **CLOSED** | `b1-expected-labels.mjs:177` exits 8; driver relays (`b-delta-cycle.mjs:65`); exit table `14_:95`. |
| Panel: harness custody (lap NEW-1 / custody N1 / lawclose N2) | **CLOSED** | Isolated root + lock + gated evidence-pointer as above; `wipeArtifacts` confined to `emulator-lap-root` (`lap:64-66`). |
| Panel: plain-mode ledger guard (lap NEW-2) | **MISS — see NEW-B.** The only ledger reads in B3 are inside `repairRealityScan` (`b3:162-179`), gated on `extrasRepair`. Plain `--execute` still consults no ledger. |
| Panel: crash-state read-backs (lap NEW-4) | **MISS (accepted residue?)** | Crash asserts remain exit-code-only (`lap:184-216`); `B3_CRASH_AT` still not gated on `FIRESTORE_EMULATOR_HOST` (`b3:57`). Not claimed closed by the handoff; carrying residue. |
| Panel: evidence identity (lap NEW-5 / custody N5) | **PARTIAL** | v2 artifact is source-bound (git head, per-script sha16, node) — verified against the tree; still aggregate-only (no per-case records/commands/artifact hashes per Codex A6's letter), and the lap unconditionally overwrites the tracked file on every run incl. red ones (`lap:351`). |
| Panel: custody N2/N3/N4 + lawclose N3/N4 (doc contradictions, rehearsal, tail) | **N2/N3 CLOSED as PROPOSED text; N4 PARTIAL** | Two-field txn + write-iff-absent + post-flip asserts + micro-lap carded in 02\_'s deploy line; 15\_ §7 rewritten (enabled-ONLY sentence gone) with the stamping predicate; battery-A ordering `16_:55`; windows-never-span-flip `16_:55` + 02\_ assert. The tail disposition exists — but its fc implementation is defective (NEW-A). |
| Panel: lawclose N5/N6/N8, custody N6/N7 | **CLOSED** | Repair absent-ledger FATAL (`b3:166`); fresh EXECUTEs covered by the execution lease (`b3:239`); 16\_ §3 sentence fixed; window/flip overlap asserted; dir-fsync honesty note real this time (`b3:291-296`); `--uids` comment reworded (`b1:55,67`); DA reader strict (`b1:98-99`). |

## (2) NEW defects

**NEW-A (HIGH — the lens target): the fc `preFlipTail` classifier waves through REAL post-flip corruption; the code does not implement 14\_ §4's own predicate.**
`14_:108` defines tail as "disk ≡ the layer expectation **while the flip-boundary expectation moved**", and both the doc and the handoff assert "mixed tail+post-flip fc divergence still BLOCKS". The code (`b4-verify.mjs:226-229`) classifies on *any* compared-expectation mismatch with disk ≡ layer — and for fc the compared expectation is the **cutoff** replay, never the flip replay. The flip-boundary fc (`flipRows[k].fc`, in scope at `:194`) is never consulted. Constructed corruption satisfying the predicate: student covered by layer L (fc=N, disk=N, byte-correct; micro-lap ran, tail genuinely empty). Post-flip the student fails word w in a graded attempt; the live writer's stamp is lost (crashed txn, or a stamping-predicate regression). Gate: cutoff fc=N+1, disk=N → mismatch → `:229` compares disk to **layer** fc: N===N → `preFlipTail`, informational, `continue` — the retry fence (`:231-237`), which would catch the persistent deficit, is never reached. Verdict **PASS, exit 0**. Under the doc's own predicate this is not tail (flip-fc never moved off N). Consequences: (i) the r64-demanded negative "disk fc one short + fresh lf ⇒ must DIFF" now passes green (lf self-exempts at `:220`, fc classifies as tail); (ii) a cohort-wide live-writer death — the one failure the single post-flip reconciliation could catch — classifies every deficit as tail and the gate publishes PASS; (iii) the doc's "heal through live use" is false for fc — a lost increment is a permanent base deficit (future increments carry it; `needsPriority` undercounts forever). "Mixed still BLOCKS" is true only when at least one post-flip increment landed (disk ≠ layer). Bounded honesty: `preFlipTail` counts + rows are published (`:294`), so a reader of the report can notice nonzero tail after a micro-lap — but the runbook and driver consume the exit code. Fix is one conjunct: tail-classify fc only when the cutoff expectation equals the flip expectation (all fc divergence pre-flip in origin — carry `flipFc` per key from `:194`); genuine pure-tail stays informational, lost post-flip increments block, and the doc sentence becomes true. Timestamps are sound as-is (their compared expectation IS the flip value, so mismatch + disk≡layer implies the flip expectation moved).

**NEW-B (MEDIUM): the handoff's "every finding from your r64 review and the r64 panel's three lenses is executed" is false — panel lap-lens NEW-2 (plain-mode ledger guard) is unclosed.**
Plain-mode B3 `--execute` (no `--deltaDir`/`--repairExtras`/`--appliedDelta` — bare shell-history flags) still reads no ledger anywhere (`b3-backfill-writer.mjs`: ledger access only at `:162-179` under `extrasRepair`, and `:297,:389,:444` appends). After M1..Mn EXECUTEs with no roster churn (churn triggers the `:188` scope FATAL; adjudication/new-attempt-driven layers don't), a plain EXECUTE resolves everyone against M0 at the M0 watermark and reverts M1-correct labels with a clean exit-0 completion; only the next full-chain B4 catches it (exit 5). The execution lease serializes concurrency, not wrong-mode sequencing. This was item (2) of the r64 lap-lens minimal closure set; no r65 change-log row claims it; the handoff's blanket closure claim and the "Panel residue closed" list silently omit it. Fix is the one the panel named: ledgered EXECUTE'd layers for this original ⇒ plain-mode FATAL (the loop at `:167-175` made unconditional).

**NEW-C (MEDIUM, evidence): the new tail law has ZERO execution anywhere — and the fc-deficit BLOCK direction still has none either.**
`preFlipTail` appears only in b4-verify and 14\_ (repo grep); no lap case creates a tail-shaped divergence, no fixture stage exercises `:226-229`, and no case anywhere plants an fc deficit for `--postFlip` to catch (the r64 lap-lens residue, verbatim, still true — fixture 9b is replay-arithmetic on `computeStudentLabels`, not B4's branch). A verdict-affecting law landed this round with no negative or positive execution — and NEW-A is exactly the class of bug that execution would have surfaced (a deficit case would PASS instead of DIFF).

**NEW-D (MEDIUM-LOW, doc-vs-code): 14\_ §4 still teaches the r64 semantics the r65 code deleted.**
`14_:106-107`: "post-flip-ADJUDICATED words … and post-flip RE-ENROLLED students (the advisory case below) are classified informational, never PASS-blocking" — the census and the `!src.row` skip were both deleted this round (uncovered uids BLOCK, `b4:192`); the "advisory case below" (`14_:118-120`, post-flip re-enrollment "converges through live use") describes a disposition the gate no longer grants. A stage-2 operator following 14\_ §4 will expect an uncovered re-enrollee to be advisory and instead get a blocking exit 5. One-sentence fold each.

**NEW-E (LOW, card-letter + packet accuracy):** (i) 02\_'s REQUIRED CASES still demand "(d) a **mixed** unrelated structural diff" and "during completion publication — incl. the first-append window"; the executed lap's (d) has no concurrent actionable delta (`lap:148-162`) and no torn-append/lost-file-creation case exists — B3's new honesty note (`b3:291-296`) defers first-append to stage-2, but the card text still requires it of the lap: execute or re-scope the sentence. Case (c) still tolerates `6 || 7` (`lap:143`). (ii) The handoff's "the first-append window (pre/post-intent pair)" repeats the exact mapping the r64 panel ruled wrong (pre/post-intent is crash-around-append, not lost-file-creation). (iii) The lap rewrites the tracked evidence file unconditionally even on failing runs (`lap:351`).

**Residue notes (no action forced):** a post-flip epoch reset drops flip-known keys from the cutoff universe → the stale docs land in extras → blocking red (fail-closed, correct posture, but an operator should know a reset-during-gate reads as structural); ≥-flip timestamps are exempt-not-verified by design (sanity caps `b-baseline.mjs:45-46` bound it); a post-flip live stamp on a replay-fence-excluded attempt (e.g., >2pp score/rows disagreement) creates an unexcusable fc-bearing extra → false-red risk, minutes-scale window; the okEff law change vs r63 alters no existing baseline (challengeDigest inputs unchanged — `R|` rows hash `ok=isCorrect` in both eras; no production baseline exists yet).

## (3) FREEZE-READY-EXCEPT-DAVID: **NO**

A freeze-YES would not follow immediately from David's ratification. The four Codex counterexamples are genuinely dead in code and in execution, custody/serialization is real, and the doc set is internally consistent under the PROPOSED framing — that is major, verified progress. But: NEW-A is a verdict-affecting defect in the exact machine this fold round certifies, contradicting the packet's own 14\_ §4 letter and the handoff's "mixed fc still BLOCKS" claim; NEW-B falsifies the handoff's blanket closure claim on a finding the r64 panel put in its minimal closure set; NEW-C means the round's one genuinely new law ships unexecuted; NEW-D leaves the governing choreography doc teaching deleted semantics. Minimal set to flip me to YES-except-David: (1) the fc tail conjunct (cutoff-fc ≡ flip-fc) + one executed lap/fixture case each way (pure-tail informational; lost-post-flip-increment BLOCKS); (2) the unconditional plain-mode ledger guard + a case; (3) the 14\_ §4 stale-sentence fold (NEW-D) and the card-letter re-scope or execution (NEW-E-i); (4) handoff/change-log accuracy corrections (NEW-B, NEW-E-ii). All bounded — days, not a redesign; nothing reopens the r64 closure set.

## (4) The two NEW laws

**Tail disposition (14\_ §4 + b4): right law, wrong predicate in code.** The disposition itself is the correct resolution of custody-N4 — the tail *is* writer-less by construction, publishing beats silent absorption, the micro-lap bounds it, and blocking pure tail would make the one gate structurally red on any live cohort. The doc's predicate ("disk ≡ layer **while the flip-boundary expectation moved**") is exactly right. The implementation drops the second conjunct for the one field where it matters (fc — the only field whose compared expectation is cutoff, not flip), which converts the gate's only cumulative-exactness check into an exemption for lost post-flip writes (NEW-A). Fix the conjunct and this law is freezable; as shipped it is not. Also fold: "heal through live use" should say timestamps heal by last-write-wins, fc deficits do not heal and are why the mixed case must block.

**Stamping predicate rehearsal carve-out (15\_ §7): sound, coherent, correctly weighted.** `stamp iff firstEnabledAt set ∨ class ∈ rehearsalClassIds` is the minimal predicate making the dark-window custody law coexist with the 25WT/shadow rehearsal that must certify stamping itself; the dangerous edges are each pinned: write-iff-absent kills era-boundary movement, "26SM ids NEVER in the list" + flip-choreography empty-assert kills scope leakage, battery-A-before-registration (`16_:55`) preserves B3's exclusive custody during the B3-on-shadow battery, kill-switch OFF-window stamping stays exactly R2-32's ratified letter (marker set → predicate true regardless of `enabled`). Two honest limits, neither blocking: the predicate is doc-law for a stage-2 dark-build writer — nothing in today's code enforces it (only B3's marker guard exists), so it must appear verbatim in the DF2-10 workpackage before the dark build; and B3's config guard cannot see `rehearsalClassIds`, so rehearsal-era exclusivity rests on the pinned ordering, not code — acceptable for a sandbox. PROPOSED–PENDING-DAVID is the right weight: the predicate rides the same three-line ask as the two-field txn, and nothing in it forecloses either answer David could give.

**Fold-verification bottom line:** the r64 closure is real and execution-backed (sha16-bound 54/0 + 57/0 + 2,688/0 all reproduce/verify); the round's one new gate law contains one precisely-fixable false-informational hole, one panel finding was skipped while claimed closed, and the governing doc lags the code in two sentences. Close those and FREEZE-READY-EXCEPT-DAVID flips to YES on my lens.

---

## LENS: custody

All verification complete. Assembling the verdict.

---

# DEEPFIX2 r65 — Fold-verification review. LENS: custody + serialization end-state

**Basis (measured on this checkout, HEAD `4fbe2e1`, tree clean over the eight scripts):** `node --check` clean on all eight chain scripts · `delta-chain-fixture.mjs` **57/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** (both match the handoff). **Emulator lap: deliberately NOT re-run** — no live lap process existed (`pgrep` clean), but three reviewers run simultaneously with no coordination channel, and "at most ONE may run" is unsatisfiable from inside my seat; re-running would be the exact custody violation my lens exists to catch. Instead I executed the sanctioned fallback: **the source-bound evidence artifact trust-checks CLEAN** — all eight `scriptSha16` values in `docs/plans/deepfix2/evidence/emulator-lap-result.json` equal sha256-first-16 of the tree's bytes, `gitHead` equals current HEAD, node version matches the box, and my static count of the harness's `ok()` sites is exactly **54** (3+2+2+2+3+6+2+4+6+6+7+5+6), arithmetically matching the artifact's 54/0. The 54/0 claim is bound to precisely the bytes I audited.

## (1) r64 findings — CLOSED / MISS

| Finding | Verdict | Evidence |
|---|---|---|
| Codex A1 forged `--postFlip` boundary | **CLOSED** | `b4-verify.mjs:103-113`: marker read + type-check + EXACT equality, missing/malformed/mismatch = FATAL 2; marker + `configUpdateTimeMs` in the report (`:113,:294`). Lap executes both directions (`b-emulator-lap.mjs:256-261`: `FLIP-2` refused; wrong pre-flip value at the true boundary = 5). |
| Codex A2 rejected-challenge masking | **CLOSED** | The word-level census is DELETED (`b1-replay-lib.mjs:44-47`); fc/lf from grading-time `ok`, lc/lp from `okEff` (`:89,:127-128`); B4 has no whole-word skip — corrupt is checked before anything (`b4-verify.mjs:216-217`). Fixture stage 9c (`delta-chain-fixture.mjs:340-354`) asserts the law directly; lap executes rejected+corrupt-fc = 5 and accepted = PASS (`b-emulator-lap.mjs:263-283`). Nit: "closed status enum" is implemented as default-inert (any status ≠ `accepted` mints nothing), not as validation — fail-safe, but not the letter of "validate a closed enum". |
| Codex A3 `!src.row` re-enrollment skip | **CLOSED in code; 14_ prose contradicts it — see D4** | Skip deleted; uncovered uids LISTED (`b4-verify.mjs:192`, `uncoveredAtGate` in report `:294`) and their diffs BLOCK (no bypass in the compare loop; the tail rule `:226` requires `src.row`). Lap: pre-flip joiner = 5 + listed (`b-emulator-lap.mjs:292-301`). |
| Codex A4 valid new-word false-red | **CLOSED** | Universe rebuilt through the captured cutoff (`b4-verify.mjs:38,:193-199`): fc = cutoff value, timestamps = flip value or null; post-flip-fresh timestamps live-exempt per field (`b-baseline.mjs:38-47`). Lap: valid post-flip new-word failure PASSES (`b-emulator-lap.mjs:284-291`). |
| Codex A5 owner authority | **HONORED** | Nothing ratified on David's behalf: 11_:48 R2-32 annotation, 02_:95 deploy clause, 14_:109, 15_:206 all carry **PROPOSED — PENDING DAVID**; this round's handoff parks the freeze. Internal-consistency residue remains — D4/D5. |
| Codex A6 lap scope + isolation | **CLOSED with bounded residue** | `DEEPFIX_AUDIT_ROOT` (`b-baseline.mjs:11-15`) honored by B1 (`b1-expected-labels.mjs:136`, pointer write gated `:213`), B3 (`:205,:297`), B4 (`:57,:278`); harness passes it on every spawn (`b-emulator-lap.mjs:74`), wipes only inside `emulator-lap-root` (`:64-66`), wx lap lock with pid-liveness (`:44-57`), source-bound evidence v2 (`:347-355`, verified above). Adversarial matrix + valid-repair + stale-report all execute. Residue: two carded crash points still not genuinely executed (D6). |
| Codex B1 shadow §3 contradiction | **CLOSED** | 16_ §3 consumption is one sentence (whole-class allowlists = reducedSet ∩ mirror membership; student-granular batteries consume the frozen partition manifest; "no battery equates a class with a partition") and the PRE-RUN GATE is reworded to the same law (16_ §3, "[reworded r65p]"). |
| Codex B2 repair/delta serialization | **CLOSED as designed; the lease's RECOVERY path is defective — D1/D2** | One exclusive per-original EXECUTE lease, wx-created (`b3-backfill-writer.mjs:240-241`), taken BEFORE phase-1 reads (`:239` precedes `:310`), released AFTER completion publication (`:449` after `:444`); repair reality scan re-runs UNDER the lease (`:256`), refuses dangling intents (`:177`); absent-ledger FATAL in repair (`:166`) — B4 symmetry (`b4-verify.mjs:60-62`) closed (panel N5). **EPERM answer:** `process.kill(pid,0)` catch sets stale only on `ESRCH` (`:249`) — EPERM (recycled pid owned by another user) ⇒ holder treated alive ⇒ refuse. Fail-closed, correct; worst case a bounded wait. |
| Panel: B1 A8 exit 3→8 | **CLOSED** | `b1-expected-labels.mjs:177` exits 8; driver relays B1 codes (`b-delta-cycle.mjs:65`) and documents 8 as the A8 hazard (`:16`). |
| Panel: DA reader strictness / stale `--uids` comment / dir-fsync note | **CLOSED** | DA version+uniqueness (`b1-expected-labels.mjs:98-99`); `--uids` comments replaced (`:55,:67`); the honest first-append boundary note now exists and replaces the old durability overclaim (`b3-backfill-writer.mjs:291-296`). |
| Panel custody N2/N3/N4/N6 (flip writer carded / rehearsal carve-out / tail / window-spans-flip) | **CLOSED as PROPOSED text, with letter residue D4/D5** | 02_:95 cards the two-field flip txn + write-iff-absent + post-flip marker assert + `rehearsalClassIds`-empty assert + no-window assert + pre-flip micro-lap; 15_:206 stamping predicate + write-iff-absent; 14_:108 tail disposition; 16_:55 battery-A ordering + windows-never-span-flip. |
| Panel lap-lens NEW-2 (plain-mode ledger guard) | **MISS** | No plain-mode guard exists: the reality scan runs only `if (extrasRepair)` (`b3-backfill-writer.mjs:162,:179`); plain mode consults the ledger nowhere (grep: only comments at `:75,:181,:294`). The M0-revert writer survives — D3. The handoff's blanket "Every finding from … the r64 panel's three lenses is executed" is falsified by this item. |
| Panel lap-lens NEW-4 (crash-state read-backs, case-c pin, `B3_CRASH_AT` gating) | **MISS (evidence-strength)** | Crash cases remain exit-code-only (`b-emulator-lap.mjs:184-216`, no Firestore/journal read-backs); case (c) still tolerates `6 || 7` (`:143`); `B3_CRASH_AT` not gated on `FIRESTORE_EMULATOR_HOST` (`b3-backfill-writer.mjs:57`). |
| Panel lawclose N6 (fresh-run lease) | **CLOSED for EXECUTE** | The execution lease serializes all fresh EXECUTEs per original; concurrent DRY runs can still interleave tmp files (evidence-only, no state risk). |

## (2) NEW defects

**D1 — HIGH (code, deterministic): the 2h staleness cap OVERRIDES pid-liveness — a live slow EXECUTE gets its lease stolen.** `b3-backfill-writer.mjs:248-249`: `stale = Date.now() - L.at > 2h;` then `if (!stale && L.pid) { …kill(pid,0)… }` — the liveness probe runs ONLY when the lease is younger than 2h. A lease older than 2h is stale *regardless of the holder being alive*. Concrete scenario: the real 26SM full EXECUTE (947 students, phase-1 reads + rate-controlled chunked txns per 14_ §3) runs past 2h; any second B3 invocation (driver's next cycle, an operator probe) reads the >2h lease, skips the liveness check, `rmSync`s the LIVE holder's lease (`:253`), wx-creates its own, and runs phase 2 concurrently — two writers transacting against possibly different chains, both publishing clean completions: verbatim Codex-B2, reintroduced by the lease's own recovery clause, with no race timing required. Fix: `stale = dead(ESRCH) || (age>2h && pid unverifiable)`, or a holder heartbeat that refreshes `at`.

**D2 — MEDIUM (code, racy): the stale-takeover is an unlink TOCTOU — two post-crash starters can both win.** `b3-backfill-writer.mjs:252-254`: takeover = `rmSync` + `wx`. Interleaving A.rm → A.wx → B.rm (deletes A's FRESH lease; `force:true` is silent) → B.wx: both hold "the lease" and EXECUTE concurrently. The `catch → "takeover raced"` only covers the other ordering. Concrete scenario is the panel-proven one: a crashed holder (dead pid ⇒ stale immediately, `:249`) followed by an automation retry and a human operator starting within the same second — exactly how two agents launched laps simultaneously in r64. Same pattern in the harness lap lock (`b-emulator-lap.mjs:53-54`). Fix: claim-by-rename (rename the specific stale lease to a unique reaped name — one renamer wins, the loser's ENOENT is the race signal), then wx. (Also microscopically: the wx-then-write gap `:241` lets a concurrent reader parse an empty lease as malformed ⇒ stale ⇒ rm a live lease — same family, same fix.)

**D3 — MEDIUM (code): plain-mode B3 still has no ledger guard — the M0-revert regression writer survives (panel NEW-2, demanded, not delivered).** After M1..Mn are EXECUTE'd, `--execute` with no `--deltaDir`/`--appliedDelta` (plain shell-history replay) resolves every student against M0 (`b3-backfill-writer.mjs:75,:312-319` — post-watermark attempts change neither digest nor epoch, so `expected = src.row.words` = M0), reverts chain-correct labels under the lease (serialization does not help — it is the *resolver* that is wrong), publishes a clean completion, and is loud only at the next B4. The harness itself calls a delta-less re-run "illegal" (`b-emulator-lap.mjs:157`) yet the CLI accepts it. Fix is the one the panel wrote: ledgered EXECUTE'd layers for this original ⇒ plain-mode FATAL (make the `:162` scan unconditional for EXECUTE), plus a case.

**D4 — MEDIUM (doc-vs-code, inside the clause David reads): 14_ §4 still promises the r64 behavior the r65 code deliberately removed.** `14_TRACK_B_BACKFILL_PIPELINE.md:106-107`: "post-flip-ADJUDICATED words … and post-flip RE-ENROLLED students (the advisory case below) are classified informational, never PASS-blocking". In the executed law there is NO adjudicated-word classification (census deleted; a rejected challenge + corrupt fc BLOCKS — the lap asserts exit 5) and NO re-enrolled advisory path (uncovered uids are listed AND blocking; a departed-at-B3, re-enrolled-post-flip student whose labels were never written produces blocking diffs). The sentence is the *old false-green described as law*, in the same section as the tail disposition David is being asked to ratify. One-sentence deletion/rewrite before the ask is answered.

**D5 — LOW-MEDIUM (letter consistency ×3 + the ask): the rehearsal carve-out lives in one doc and remotely amends the other two.** 15_:206's STAMPING PREDICATE (marker ∨ `rehearsalClassIds`) says "the 14_ §4 'zero live writers' law *therefore reads* 'zero live writers OUTSIDE rehearsalClassIds'" — but 14_:110-112 and the binding 11_:48 R2-32 annotation still state the UNQUALIFIED "ZERO live label writers / stamp ONLY when firstEnabledAt is SET", and 15_ §7 itself retains "live label writers stamp ONLY when it is set" two sentences after its own predicate (the exact two-sentences-apart shape r64's panel N4 flagged). Trip scenarios: a builder implementing from 14_ (or the retained 15_ sentence) builds marker-only stamping and stage-3 cannot rehearse stamping (panel N3 verbatim); the David-ask as panel-drafted ("labels dormant until your first flip") has him ratify a letter the rehearsal he separately granted violates on day one — line 1 of the ask must carry "outside the rehearsal classes you granted 2026-08-02". Three one-sentence edits + one ask qualifier. (Contract (7) on 02_ remaining unqualified is CORRECT procedure pre-ratification — R2-24 says requalifying ratified card text is the owner edit David's YES performs; it is not packet-inconsistency, but it must be on the David-YES fold list, together with the stamping-predicate duty on DF2-10, which today carries no `firstEnabledAt` text at all.)

**D6 — LOW-MEDIUM (card letter vs execution, carried): two of the card's REQUIRED crash cases are still not executed and the card was not re-scoped.** 02_:95 requires "during completion publication" (a torn completion append — B4's malformed-line FATAL `b4-verify.mjs:72` fires in no execution anywhere) and "incl. the first-append window" — r65 repeats the pre/post-intent mapping the r64 panel explicitly ruled wrong (the hazard is intent appended+fsynced but file CREATION lost; `b3-backfill-writer.mjs:291-296` now honestly documents it and defers to "the stage-2 crash-injection matrix", i.e., the code re-scoped what the card still requires). Plus case (c)'s `6 || 7` tolerance (`b-emulator-lap.mjs:143`) and case (d) still lacking the concurrent-delta "mixed" shape. The r64-lap-lens freeze ground (i) — "satisfy the expansion or re-scope it honestly" — remains half-open: honesty landed in a code comment, not on the card.

**D7 — Notes (no action forced):** `reportTail` caps at 500 rows without any partial-list flag (`b4-verify.mjs:229` — count stays exact, list silently truncates; informational); the lap unconditionally overwrites the tracked evidence artifact even on a red run (`b-emulator-lap.mjs:351` — now self-identifying via `failures`+`gitHead`, and git-recoverable, so honest, but the panel asked for an explicit flag); the lease is per-original-manifest — two EXECUTEs against *different* originals over one cohort remain unserialized (operator-error class, per the law as Codex specified it); `audit/deepfix/emulator-lap-root/` is untracked-not-gitignored (the win-r74 order already flags it for the commit exclusion).

## (3) FREEZE-READY-EXCEPT-DAVID: **NO**

A David-YES would not cure the residue: **D1/D2** are defects in the execution lease's own recovery path — the packet's headline serialization law admits a second writer deterministically (>2h live holder) and racily (post-crash double-takeover, the panel-proven two-simultaneous-starters shape); **D3** is an explicitly demanded panel closure the handoff's blanket line claims executed and is not; **D4** puts a false sentence (the removed false-green described as law) inside the very clause the three-line ask ratifies; **D5** means the ask's own line 1, as drafted, contradicts the carve-out. Minimal set to reach FREEZE-READY-EXCEPT-DAVID, all bounded (hours, no law substance reopens): (1) lease staleness = dead ∨ (aged ∧ unverifiable) + claim-by-rename takeover (both lease and lap lock); (2) unconditional EXECUTE ledger guard (plain-mode FATAL on ledgered layers) + one case; (3) delete/rewrite 14_:106-107; (4) the three zero-writers letter qualifiers + the ask-line-1 qualifier; (5) either execute torn-completion + a real first-append case or move those two items out of the card's lap-REQUIRED list onto the stage-2 matrix where `b3:294` already points. Everything else I attacked held: marker binding, adjudication law, uncovered-joiner blocking, through-cutoff universe, absent-ledger symmetry, scan-under-lease, release-after-publication, harness isolation, and the 54/0 evidence chain are real and verified against the exact tree bytes.

## (4) The two NEW laws

**Tail disposition (`preFlipTail`, `b4-verify.mjs:221-230` + 14_ §4): SOUND — the predicate is the right one and cannot mask corruption.** Classification requires `actual ≡ the resolved layer expectation` — byte-equality with what B3 was supposed to have written — so the informational carve-out is unsatisfiable by any wrong write (one byte off the layer value blocks); tail-introduced words (layer null, disk null) classify correctly; and I traced the mixed tail+post-flip fc arithmetic: live increments from the stale base give disk n+1 vs cutoff n+2 vs layer n ⇒ not tail, retry-fence fails ⇒ BLOCKS — which is correct, because that divergence is a genuine lost update, and 14_:108's letter matches the code exactly. Caveats: the disposition's operational bound (tail = minutes) rests entirely on the PROPOSED pre-flip micro-lap, which B4 cannot itself verify — a stale last layer yields a large published-but-green tail, so the micro-lap must stay a hard choreography step, not advice; the 500-row list cap is unflagged (D7); and the stale 14_ sentence one line above it (D4) must go before David reads the section.

**Stamping-predicate rehearsal carve-out (15_ §7): the right law, correctly ordered, one letter-fold short.** It resolves the panel-custody dilemma the only coherent way — without it the shadow audit cannot certify the stamping it exists to certify; with it, battery A's B3-exclusivity is protected by the 16_:55 ordering pin (shadow ids enter `rehearsalClassIds` only AFTER battery A completes, so B3-on-shadow always runs under zero-writers and never returns after registration), and the 26SM verify scope never intersects the carve-out (26SM ids never listed; the flip asserts the list empty), so B3/B4's zero-writers assumption holds over everything B3/B4 touch. It is consistent with battery-A ordering and with the *intent* of the 14_ zero-writers sentence — but not yet with its *letter*: the carve-out amends 14_ and 11_ remotely while both retain the unqualified sentence, and 15_ §7 itself still carries the unqualified "stamp ONLY when it is set" after its own predicate (D5). Ratifiable the moment those three sentences and the ask's line 1 say what the predicate says.

---

## LENS: evidence

All verification complete — zero footprint, scripts byte-identical across both commits, packet artifact restored byte-exact. Composing the verdict.

# DEEPFIX2 r65 — panel review. LENS: evidence integrity + the lap

**Reviewer basis (executed, not just read):** independently recomputed all eight `scriptSha16` values (sha256/16) — **8/8 MATCH** the evidence artifact; `gitHead 4fbe2e1` = the reviewed tree (and `git diff 4fbe2e1..e9a0f42 -- scripts/deepfix2/` is empty — HEAD moved mid-panel but the eight scripts are byte-identical, so the binding survives) · `delta-chain-fixture.mjs` **57/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` 11/11 · full read of all eight chain scripts + harness + 02_/11_/14_/15_/16_ + change log + both r64 verdict docs · **I was the panel's ONE lap re-runner: my exclusive solo run reproduced 54 checks / 0 failures, exit 0** (transcript `/tmp/claude-1000/-app/87eba36e-8e66-4638-bae9-6cd6f923fff6/scratchpad/r65-solo-lap4.log`).

**Runner incident, disclosed:** my first run was destroyed by my own tooling, not the chain — a flaky pgrep-based watch declared the lap dead at 22/22-green; I killed "orphaned" processes that included its live emulator. Two byproducts are themselves evidence: (a) my relaunch was **REFUSED by the wx lap lock while the wounded holder lived** ("FATAL: another emulator lap is RUNNING"), and after I terminated it the next launch **took over the stale lock via pid-liveness** — both lock branches live-fired and behaved exactly as designed (the r64 concurrent-contamination fix is real); (b) the tracked evidence artifact was backed up before my runs and **restored byte-exact** (sha `129f750325f16e67…`), lock cleaned by the harness's own exit handler, `git status` footprint zero. Siblings attempting the lap during my window were correctly refused per protocol.

## (1) r64 findings — CLOSED / MISS with file:line

| Finding | Verdict | Evidence |
|---|---|---|
| **Codex A1** forged `--postFlip` boundary | **CLOSED + EXECUTED** | `b4-verify.mjs:106-113`: marker read; missing/malformed FATAL 2 (`:111`); exact-equality FATAL (`:112`); marker + `configUpdateTimeMs` in the report (`:113,:294`). Lap matrix reproduces Codex's exact 4-step scenario both ways: `--postFlip=FLIP-2` REFUSED exit 2 + "marker" (`b-emulator-lap.mjs:256-257`), wrong pre-flip value at the TRUE boundary → 5 (`:259-261`). Green in my solo run. |
| **Codex A2** rejected-challenge masking | **CLOSED + EXECUTED** | Census DELETED (`b1-replay-lib.mjs:44-47`); `okEff = isCorrect ∨ accepted` (`:89`); fc/lf from grading-time `ok`, lc/lp from `okEff` (`:127-128`); B4 has no word skip (compare loop `b4-verify.mjs:210-246`). Fixture 9c asserts the law directly (`delta-chain-fixture.mjs:340-354`). Lap matrix: rejected+corrupt-fc → 5 (Codex's exact repro, harness `:263-273`); accepted → PASS with history kept (`:274-283`). Nit: "closed status enum" is effect-closure, not validation — no FATAL on unknown statuses; I probed for an exemption hole and found none (unknown ⇒ inert; a live mint ≥ flip is per-field exempt regardless). |
| **Codex A3** uncovered joiner | **CLOSED + EXECUTED** | The `!src.row` skip is gone — uncovered uids counted + LISTED (`b4-verify.mjs:191-192`, report `uncoveredAtGate` `:294`) and their diffs fall through to normal comparison ⇒ BLOCK. Lap matrix: pre-flip joiner emE → 5 + listed (harness `:292-301`). Codex's alternative branch ("remove the exemption") taken; the genuine post-flip-joiner PASS side is unexecuted (emerges via per-field exemption — acceptable). |
| **Codex A4** valid new-word false-red | **CLOSED + EXECUTED** | Universe = through-cutoff replay keys: fc = cutoff value, timestamps = flip value or null (`b4-verify.mjs:193-199`) — replay-known words never route through extras. Lap matrix: valid post-flip new-word failure → PASS (harness `:284-291`). Vanished-key direction fails closed (drops to extras ⇒ blocks). |
| **Codex A5** owner authority | **CORRECTLY PARKED** | 14_ §4, 15_ §7, 02_:95 flip clause, 11_:48 all stamped PROPOSED-PENDING-DAVID; no freeze ruled this round. |
| **Codex A6** lap execution/isolation/evidence | **SUBSTANTIALLY CLOSED; two residues** | Isolation real: `auditRoot()` honors `DEEPFIX_AUDIT_ROOT` (`b-baseline.mjs:11-15`), consumed by B3 (`:205,:297`), B4 (`:57,:278`), B1 (`:136`); B1 pointer write gated off under isolation (`b1-expected-labels.mjs:213`); harness confines wipes to `emulator-lap-root` (`:64-66`); wx lap lock + pid-liveness (`:43-57`, both branches live-fired). Post-flip adjudication matrix + all four adversarial cases EXECUTE. Evidence v2 source-bound and **independently verified** (8/8 sha16, gitHead, 54 = my static `ok()` count). Residues: no per-case records/artifact hashes (A6's letter); the card's first-append + torn-completion cases still unexecuted (below). |
| **Codex B1** shadow §3 contradiction | **CLOSED** | 16_ §3 one-sentence CONSUMPTION law + PRE-RUN GATE reworded (`16_:113-122`). |
| **Codex B2** repair custody | **CLOSED in code; one NEW defect; negative unexecuted** | Per-original wx exec lease, pid-liveness, takeover-race fail-closed, released after completion (`b3-backfill-writer.mjs:238-257,:449`); reality scan re-runs UNDER the lease (`:256`); repair absent-ledger FATAL (`:166`). But the dangling-intent scan is over-strict (NEW-2 below), and Codex's demanded two-process emulator negative exists nowhere (my live lock observations covered the LAP lock, not the B3 lease). |
| **Panel lap NEW-1 / custody N1 / lawclose N2** harness custody | **CLOSED** (isolation+lock+provenance above) — residues NEW-3/NEW-4 below | |
| **Panel lap NEW-2** plain-mode ledger guard | **MISS — nothing landed** | grep-verified: B3 consults the ledger ONLY in repair mode (`b3:162-179`); no plain-mode guard anywhere; not re-scoped in 14_/02_/the handoff (zero mentions). The harness's own case-(d) comment concedes the scenario ("delta-less B3 re-run is illegal", harness `:157`) — commented, unenforced. The panel's minimal-closure item (2) verbatim, still open. |
| **Panel lap NEW-3 / lawclose N1** B1 A8 exit | **CLOSED** | `b1-expected-labels.mjs:177` exits 8; B1 has no exit-3 path left; driver passthrough safe (`b-delta-cycle.mjs:13-18,:65`); change log corrected (row 1446 reworded driver-only; row 1455 records the B1 leg). |
| **Panel lap NEW-4** crash-state read-backs + env-gate | **MISS** | Crash asserts remain exit-code-only (harness `:190-216`); `B3_CRASH_AT` still ungated on emulator host (`b3:57`). |
| **Panel lap NEW-5 / custody N5** evidence identity | **SUBSTANTIALLY CLOSED** | v2 binds gitHead/node/per-script sha16 (verified). Per-check records still absent — partial vs the letter. |
| **Panel lap NEW-6 / custody N3** rehearsal carve-out | **CLOSED** | 15_ §7 stamping predicate (marker ∨ `rehearsalClassIds`); battery-A ordering pinned (`16_:55`). Ruling in §(4). |
| **Custody N2 / lawclose N3** carded flip writer + 11_ marker | **CLOSED** | 02_:95 cards the two-field txn, write-IFF-absent, post-flip marker/list/window asserts, pre-flip micro-lap; 11_:48 carries the R2-32 ⟶ PROPOSED annotation. |
| **Lawclose N4** 15_ §7 self-contradiction | **CLOSED** | §7 rewritten: ONE txn writes both fields; later writes touch `enabled` only. |
| **Custody N4** pre-flip tail orphan | **CLOSED as law+code; ZERO execution** | `b4-verify.mjs:221-230` + 14_:108. `preFlipTail` appears nowhere else in the repo — no lap/fixture case ever fires the branch (lap tails are empty by construction). Ruling in §(4). |
| **Custody N6** window/flip overlap | **CLOSED** | `16_:55` WINDOWS NEVER SPAN THE FLIP + the 02_ flip-clause assert. |
| **Lawclose N5/N6, N7; custody N7/N8** | **CLOSED** | Repair absent-ledger FATAL (`b3:166`); fresh-run serialization via the exec lease; dir-fsync honesty note now real (`b3:291-296`); `--uids` comments purged (`b1:55,:67`); DA reader validates version+uniqueness (`b1:98-99`); 16_ §3 reworded. |

## (2a) Lap audit — case by case (assertion strength; 54 = exact static `ok()` count, arithmetically honest)

- **0/a/b — faithful**, exit-code asserts; (a)'s delta attempt genuinely post-watermark.
- **(c) — executed, still tolerant**: `7 || 6` (harness `:143`) — the card's "WITH diffs" half is not required by the assertion (panel demanded pinning; not done). My transcripts pin actual = 7.
- **(d) — pure structural only**: no concurrent actionable delta is seeded; the card's word "mixed" (corruption + pending delta, B4 must emit 7 and the corruption must survive to a final 5) remains unexecuted; driver-stop proven by exit code only.
- **(e) — faithful** incl. `departedSkipped:1` read-back; setup exit codes unasserted (`:168`, also `:252,:327`).
- **Crash — 4 hook names fire; states never read back**: post-intent/after-first-chunk/pre-complete remain observationally identical; **"during completion publication" (torn append → `b4:72` parser FATAL) and "incl. the first-append window" are still unexecuted** — and both are still in the card's REQUIRED CASES letter, unre-scoped, third round running.
- **postFlip lap — strong**: both guard windows executed (`enabled:true` refused; kill-switch `enabled:false`+marker refused, `:242-245`); per-field exemption asserted via `liveExemptFields>0`.
- **Adversarial matrix — faithful to Codex's four exact scenarios** (verified construction line-by-line against the r64 repro steps) + the accepted-adjudication positive + a report-file read-back for `uncoveredAtGate`.
- **Stale-report — executed**, `A3-reality` refusal pre-write.
- **VALID repair — the round's best case, and the byte-equal IS probative**: repair phase-1 plans writes for EVERY uid via the chain resolver (`b3:158` `deltaLayers = chainLayers`, planning loop `:310-349`) — an M0-resolution regression would have planned lc/lp/rlt reverts on emB/w3 (M0 values differ from M1's on three fields) and `before ≡ after` would fail. Real Firestore read-backs (before/after doc, extra-gone). Nit: the `gone` check is falsiness-based (fc=0 would false-pass; seeded fc=9, so sound here).
- **Never executed anywhere in-repo**: the `preFlipTail` branch; the plain-mode guard (doesn't exist); repair-after-crash-history (would false-FATAL — NEW-2); torn-completion FATAL (`b4:72`); missing-layer FATAL (`b4:94-95`); `--ignoreLedger` stamp/refusal; driver exits 3/4/9; the B3 exec-lease two-process refusal.

## (2b) Evidence integrity + packet accuracy (my lens headline)

**The evidence artifact is genuine.** All eight sha16 values match my independent computation against the tree; gitHead matches the reviewed commit (scripts byte-identical across the mid-panel `e9a0f42` commit); 54 equals the static check count; the lap timestamp postdates the commit; **and the whole number reproduces 54/0 on my exclusive re-run**. The r64 identity-less-artifact failure mode is dead. Residues: no per-case records; the harness still unconditionally overwrites the tracked artifact on every completed run including red laps (`b-emulator-lap.mjs:351` runs before `exit(failures?1:0)`) — detectable now, not prevented.

**Packet accuracy — the r64 false-claim class recurs, twice:**
1. **Headline "Every finding from your r64 review and the r64 panel's three lenses is executed" — FALSE.** Panel-lap NEW-2 (plain-mode guard) has no code, no doc re-scope, no handoff mention; panel-lap NEW-4 (crash-state read-backs, env-gating) untouched; case-(c) pinning untouched; torn-completion/first-append cases absent; Codex B2's two-process negative absent.
2. **"the first-append window (pre/post-intent pair)" EXECUTES — FALSE, second offense.** The r64 panel explicitly ruled this exact mapping wrong (the hazard is intent appended + fd-fsynced, FILE CREATION lost — neither pre- nor post-intent crash tests it), and B3's own new honesty note (`b3:291-296`) defers the window to the *stage-2* matrix — the packet claims executed what its own code comment defers.
3. Minor overstatement: "closed status enum" (no validation; effect-closure only — no hole found).

Everything else I checked is true: 54/0 solo ✓ (reproduced) · 57/0 + 2,688/0 ✓ · isolation/lock/lease/evidence-binding claims ✓ · B1 A8 + change-log correction ✓ · dir-fsync note ✓ · DA reader ✓ · 11_/02_/15_/16_ folds ✓ · "the panel PROVED concurrent contamination" ✓ per the receipt.

## (2c) NEW defects

- **NEW-1 (MEDIUM, code — carried-open panel NEW-2, now silently claimed closed):** plain-mode B3 still has no ledger guard. Fresh-runId plain `--execute` after EXECUTE'd M1..Mn (the exact shell-history slip the driver's own exit-3/4 recovery text invites when `--deltaDir` is forgotten) resolves every student against M0 and writes reverts of layer-correct labels, exits 0 with a clean completion; the exec lease serializes, it does not forbid. Fix is the one the panel specified: ledgered EXECUTE'd layers for this original ⇒ plain-mode FATAL, + a case.
- **NEW-2 (MEDIUM, code/ops — repair livelock):** the repair reality scan treats **superseded** attempts as dangling. `b3:173-177` keys intents by `runId#attempt` and deletes only the exact key; a crashed attempt-0 healed by clean resume-1 leaves `intent#0` permanently unmatched in the append-only ledger ⇒ **every future `--repairExtras` for that original FATALs "dangling intent(s)" forever** (no path can ever complete attempt 0). Contradicts B4's own latest-attempt pairing law (`b4-verify.mjs:86-91`). Fail-closed, but it bricks the exact stage-2 sequence crash → resume → later repair. Unexecuted anywhere (lap repair cases run on crash-free ledgers). Fix: dangling = intent without completion **only when it is the latest attempt for its runId** (mirror B4), + a case.
- **NEW-3 (LOW, tracked-evidence contamination):** `docs/plans/deepfix2/evidence/b1-baseline-pointer-full.json` (+ `-delta`) at HEAD still contain 3-student emulator data presented as full/delta-mode pointers (`students: 3`). The r65 gate stops future contamination; the committed record still misrepresents. One restore/annotate commit.
- **NEW-4 (LOW, evidence):** red laps overwrite the freeze-cited artifact in place (above). Gate the write on green, or write red results beside it.
- **NEW-5 (LOW, lap strength):** case-(c) tolerance; setup exits unasserted (`:168,:252,:327`); crash-state assertions exit-code-only; `preFlipTail` branch has zero execution evidence.
- **NEW-6 (LOW, doc-lag):** `14_:107` still says post-flip-ADJUDICATED words and post-flip RE-ENROLLED students are "classified informational, never PASS-blocking" — r65 deleted both classifications; uncovered joiners now BLOCK (the lap's own A3 case asserts exit 5) and no adjudicated-word classification exists. One stale sentence beside an otherwise faithful §4.

## (3) FREEZE-READY-EXCEPT-DAVID: **NO**

The chain substance is in the best shape it has ever been — Codex A1–A4 closed with faithful executed evidence, isolation/lock/lease real, the evidence artifact verified and reproduced 54/0 by an independent runner. But a freeze-YES would **not** follow from David's ratification alone; residue outside the David question would ride in: (i) the packet again contains false claims of the exact class that helped sink r64 (§2b — and r64 precedent treats packet accuracy as freeze material); (ii) the 02_ card's REQUIRED CASES letter still demands first-append + torn-completion + mixed-(d), unexecuted and unre-scoped — the lap-gate sentence cannot back a freeze while its own letter is unmet (the r64 panel's rule, unmet a third round); (iii) NEW-1 — an explicitly-demanded MEDIUM-HIGH writer-regression guard from the panel's minimal closure set, absent while claimed closed; (iv) NEW-2 sits on the stage-2 crash→resume→repair path. **Minimal set (bounded, days, no law reopened):** plain-mode guard + case · repair-scan latest-attempt fix + case · execute or honestly re-scope first-append/torn-completion/mixed-(d) and pin case (c) · correct the two packet claims · restore/annotate the two pointer files · (hygiene) green-gated evidence write, per-case records, one `preFlipTail` case.

## (4) The two NEW laws

**(a) Tail disposition (`preFlipTail`, `b4-verify.mjs:221-230` + 14_ §4): SOUND — the right minimal classifier; ratifiable as written, with two caveats.** The predicate (disk ≡ resolved-layer expectation while the flip/cutoff expectation moved) cannot absorb novel corruption: any value ≠ the certified layer value still blocks; a revert-to-certified-value is indistinguishable from never-written, which IS the tail semantics, and is published (counts + rows), never swallowed. Uncovered uids are excluded (`src.row` required) so A3 stays closed. I traced "mixed tail+post-flip fc still BLOCKS" arithmetically: disk = layer+post-increments matches neither the layer value nor the cutoff replay ⇒ falls through the tail check to the fc fence ⇒ blocks (fixture 9b covers this arithmetic at replay level). Caveats: (i) 14_'s "they heal through live use" **overstates for fc** — lf/lc/rlt genuinely re-stamp, but a pure-tail fc deficit persists permanently as a published undercount (advisory-only impact; say it plainly in David's three-line ask — the disposition is his to ratify); (ii) **zero execution evidence** — no case anywhere fires the branch; a one-line regression in it would keep every suite green. One lap case each way (tail ⇒ classified+PASS; tail+post-flip fc ⇒ BLOCKS) before the freeze.

**(b) Stamping predicate (15_ §7 marker ∨ `rehearsalClassIds` + 16_:55 ordering): SOUND — it makes the custody law implementable and is correctly parked on David.** It resolves the r64 unimplementability exactly (batteries C/E/G/P can rehearse stamping while globally dark); battery-A's B3 exclusivity is protected by the pinned registration ordering; the flip choreography asserts the list empty AND no window doc (02_ + 16_ — closing the quarantine/soak overlap in the same stroke); 15_ §7's parenthetical explicitly requalifies 14_'s "zero live writers" as "outside `rehearsalClassIds`". Correctly PROPOSED-PENDING-DAVID since it widens the writer gate he personally ratifies. Nits: 14_ §4 still states the absolute "ZERO live label writers" with the carve-out living only in 15_ — ensure David's ask contains the carve-out sentence so he ratifies the predicate he'll actually get; live-writer-side enforcement is build-contract only (correctly a stage-2 deliverable, named in DF2-10's text).

**Protocol notes for the orchestrator:** the ONE lap-runner slot was consumed by this lens (sibling refusals during 03:47–04:10Z are the lock working, not defects); the packet's cited evidence artifact was restored byte-exact after my reproduction (`129f750325f16e67…`), lock cleaned, `git status` footprint zero; HEAD moved to `e9a0f42` mid-panel — script bytes unchanged (diff empty), but committing during a live panel is the same N9 process rule the r64 panel flagged.