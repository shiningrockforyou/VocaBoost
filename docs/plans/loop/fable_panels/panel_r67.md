# 3-Fable panel — DEEPFIX2 r67 (2026-08-02)


---

## LENS: asof

All verification complete. Composing the verdict.

# DEEPFIX2 r67 — panel review. LENS: the as-of-boundary law + census

**Basis (measured on this checkout, HEAD `83fafa7`, working tree clean over `scripts/deepfix2/` + `docs/plans/deepfix2/`):** solo-lap constraint honored — `pgrep -f b-emulator-lap` matched only my own probe shell and I did NOT run the lap; my execution evidence is the two offline fixtures + independent sha16/mtime verification. `delta-chain-fixture.mjs` **70/0** reproduced · `rotation-cyclicity-fixture.mjs` **2,688/0** reproduced · `node --check` clean 11/11 · **evidence sha16 recomputed: 8/8 MATCH** against the tree (`b-emulator-lap.mjs = 02f8038db89c719a` included) · **frozen-bytes discipline verified**: latest source mtime 06:03:49Z < evidence write 06:19:13Z; the r67 commit (06:21:39Z) is byte-identical to the evidence-run tree · lap `ok()` site count = **71 ≡ the artifact's 71 checks**. Full read: b1-replay-lib, b1-expected-labels, b4-verify, b3-backfill-writer, b3-txn-core, b-baseline, b-emulator-lap, fixture stages 9c/9d/9e, 11_ rows R2-48/49, 14_ §4, 15_ §6b, 16_ items 8/11, 02_ DF2-14 card, production writers (`functions/foundation.js:2592-2627`, `functions/index.js:690-754`, `src/services/db.js:1275-1500, 2895-2947`), r67 diff `ce7b548..83fafa7`, baton (round 67, `turnOwner:codex`, ready marker present).

One process note: the evidence artifact's `gitHead` records `ce7b548` because the run predates the r67 commit; the sha16 byte-binding (the load-bearing control) verifies 8/8 against `83fafa7`'s tree, so the identity claim holds.

## (1) r66 findings — CLOSED / MISS

### Codex A1-A5 + B

| Finding | Verdict | Evidence (file:line) |
|---|---|---|
| **A1 — R2-49 census discarded** | **CLOSED** (two nits, NEW-3/NEW-4 below) | Generic note sink + aggregation: `b1-expected-labels.mjs:144,149-151`. Published on all three demanded surfaces: full summary `:197`, redacted pointer `:221`, operator console `:224`. B4 stats carry all three counters (`b4-verify.mjs:123`), counted at both compute sites (`:124,:143,:172`), published in report `:291` + stdout `:316`, both modes (shared stats object). The tautology is dead: fixture 9c now runs a real counters callback and asserts `legacyAcceptedReconstructed >= 1` (`delta-chain-fixture.mjs:358-362`; e1 has exactly one legacy row). Lap asserts the census in B1's live output (`b-emulator-lap.mjs:409`). Name strings verified consistent lib↔labels↔B4 (`b1-replay-lib.mjs:96,100,104` ↔ `labels:197` ↔ `b4:123`). |
| **A2 — proof uses the current corrected score** | **CLOSED** — my lens core; attacked, held | The two-concept split is exactly as demanded: fence = current effective (`b1-replay-lib.mjs:111-112`, now exact `adjOk === true`), proof = as-of (`:142-146`): `hasPostBoundaryAccept = adjOk ∧ ¬adjEff` → `asOfScore = Math.round(effAsOf/tq*100)`, else stored. **I verified the reconstruction against the writers byte-for-byte**: both accept writers compute `Math.round((correctCount/denom)*100)` with `denom = attempt.totalQuestions` (`foundation.js:2603-2605`, `db.js:2920-2922`); both organic writers persist `score = Math.round(correct/totalQuestions*100)` with the same denominator they persist (`db.js:1288/1312/1316` and `:1438/1483/1487` where `totalQuestions = words.length`); the AI-recovery path matches (`functions/index.js:432-433`). Blanks-as-wrong under `/tq` matches the organic formula, so blank-row attempts (which skip the ±2pp fence) reconstruct exactly too. Missing-timestamp accepts are permanently non-effective + census-counted — fail-closed and B3/B4-consistent. Fixture 9d is the exact sibling shape with boundary-time lp asserted both sides of the review instant (`delta-chain-fixture.mjs:371-386`). Lap runs Codex's exact reproduction — post-flip accept + score 100 + corrupt sibling `w1.lp` day1→day2 — and demands **exit 5** (`b-emulator-lap.mjs:403-421`); I traced the arithmetic: as-of 50 → flip lp(w1)=day1=layer → not moved → not tail → diff. Residues: NEW-1/NEW-2 below (neither reopens the closed case). |
| **A3 — terminal cutover intent** | **CLOSED** | FLIP_DURING_RUN publishes a terminal `b3-applied` completion with `outcome.cutoverAborted:true`, releases the lease, exits 6 (`b3-backfill-writer.mjs:456-467`). The shared reducer accepts it as terminal and excludes the layer from `appliedLayerShas` (`b-baseline.mjs:202-207`) — post-cutover B4 has no dangling problem and no missing-layer fatal. More B3 writes stay refused (admission guard `b3:122-127` covers resume). Lap executes the full sequence THROUGH the final gate: barrier abort exit 6 (`lap:387-388`) then `B4 --postFlip` over the chain reaching PASS with the unapplied layer's students settled as published tail (`lap:391-394`). Committed-chunk variants settle by value or tail per the resolver — no brick path found. |
| **A4 — EPERM lease steal** | **CLOSED** (one wording nit) | Pure law `assessLease` (`b-baseline.mjs:214-221`): EPERM returns `stale:false` at any age (`:220`); stale = ESRCH ∨ (aged ∧ no-usable-identity). Six-branch fixture 9e (`delta-chain-fixture.mjs:387-396`). B3 consumes it (`b3:266-268`) with claim-by-rename takeover (`:269-272`); lease creation is atomic tmp+`linkSync` (`:251-256`) — the wx-then-write empty-file window is structurally gone, and I traced the rename-gap race to a single winner (loser fatals at re-create, `:272`). Nit: an unparseable lease is stale at ANY age (`b-baseline.mjs:215`), which the handoff's formula ("aged ∧ no-usable-identity") doesn't state — now race-unreachable, but the sentence and the branch diverge. |
| **A5 — evidence not source-bound** | **CLOSED** | 8/8 sha16 recomputed by me ≡ tree ≡ HEAD; all script mtimes precede the evidence write (max 06:03:49Z vs 06:19:13Z) — the pin-after-run class is dead this round. Red laps write a sidecar, never the tracked artifact (`b-emulator-lap.mjs:452-454`); B1 skips pointer writes under isolation (`b1-expected-labels.mjs:217`). `b1-baseline-pointer-full/-sample` deleted in `83fafa7`. **Residue: `b1-baseline-pointer-delta.json` survives — see NEW-5.** |
| **B prose ×3** | **CLOSED** | H6 §6b: "immutable IN THE PREIMAGE [r67 — reconciled with the (1) duty below]" (`15_H6_SCHEMAS_AND_CONTRACTS.md:175-176`) — the self-contradiction is gone. 14_ custody: the predicate stated directly with the rehearsal disjunct at BOTH sentences (`14_TRACK_B_BACKFILL_PIPELINE.md:113,116`). Tail-heal honesty one sentence in `14_:111` AND the R2-48 register row (`11_:38` "[r67 honesty]"). |

### r66 panel NEW-1..7 (gate-lens numbering; law/custody-lens equivalents noted)

| Finding | Verdict | Evidence |
|---|---|---|
| NEW-1 (third tail conjunct) | **CLOSED in code; letter + execution gaps — NEW-2 below** | `b4-verify.mjs:212-221`: tail now requires MOVED ∧ QUIET (`cutVal ≡ flipVal` per field) ∧ disk ≡ layer. I traced the r66 counterexample (layer fc=1, tail→flip 2, lost post-flip→cutoff 3): quiet fails (3≠2) → falls to the fc fence → BLOCKS. Same for lost timestamps behind a tail event. |
| NEW-2 (census void) / law NEW-2 | **CLOSED** (= Codex A1) | Above. |
| NEW-3 (evidence binding) / law NEW-1 | **CLOSED** | 8/8 on frozen bytes, verified independently. |
| NEW-4 (plain-guard misses dangling delta intents) / custody N1 | **CLOSED** | Guard consumes `parseLedgerStrict` and FATALs on applied layers ∨ dangling intents carrying `deltaManifestSha256` (`b3:279-295`); RESUME exempt — which also kills custody N1's forever-wedge (a crashed plain M0 can be resumed after M1 delta history); the lap's crash-resume suite runs through it green. |
| NEW-5 (fixture tautology) / law NEW-3 | **CLOSED** | Real count assert `delta-chain-fixture.mjs:358-362`. |
| NEW-6 (`14_:113` marker-only) / custody N5 | **CLOSED** | `14_:113` now states `firstEnabledAt SET ∨ class ∈ rehearsalClassIds`. |
| NEW-7 (wx-then-write lease creation) / custody N3 | **CLOSED for B3** (tmp+link). The HARNESS lap-lock still creates via wx-then-write (`lap:47`) though takeover is now rename-claim (`lap:53-56`) — test-infra residue, still unlisted. |
| law NEW-5 nits (i)(ii) | **CLOSED** | Exact boolean `lib:111`; one parse chain `lib:102` (≡ `:41`). (iv) red-lap sidecar CLOSED; (v) pointers PARTIAL (NEW-5 below). |
| custody N2 (EPERM claim false) | **CLOSED** | Code now matches the claim (EPERM owned forever) rather than the claim being corrected down. |
| custody N4 (provenance absolute overstated) | **PARTIAL — NEW-2** | The as-of fix kills the score-driven variant; the absolute survives verbatim in `14_:111` and adjudication/epoch-driven movement still falsifies it (NEW-1 below). |
| custody N6 (`16_` item 8 vs DRILL-E LAW) | **MISS** | `16_SHADOW_COHORT_AUDIT.md` item 8 still says drill E uses "`reviewGateEnabled` + rehearsalClassIds removal only", contradicting the DRILL-E LAW in item 11 ("the shadow class REMAINS registered throughout; removal is a separate DRAIN-gated teardown"). **r67 contains no 16_ diff at all** (`git diff ce7b548 83fafa7` — 16_ absent), yet the handoff claims "16_ §3 pre-run gate reworded" — see NEW-6. |
| custody N7 (CARRIED completeness) | **PARTIAL** | Red-lap overwrite and two pointer files cured; `b1-baseline-pointer-delta.json`, the lap-lock creation gap, and the card's mixed-(d) case (still executed as pure structural, `lap:150-163`) remain open and remain absent from the CARRIED list. |

## (2) NEW defects

**NEW-1 (MEDIUM-LOW, the as-of law's remaining pinhole): duplicate identity and the epoch snapshot are CURRENT-truth inside a boundary replay — a post-flip event can still move the flip-boundary expectation and classify as `preFlipTail`.** The content hash binds `adjState = status|reviewedAt` unconditionally (`b1-replay-lib.mjs:105,114`) and `epochByList` is read live (`:19-28`), with no as-of scoping. Concrete scenario: a pre-flip attempt exists as an identical doc-level duplicate (a known production class — the dup law exists because of it). Post-flip, a teacher accepts a row on ONE copy: the group's contents diverge → `dupConflictGroup` → the whole group is excluded from the FLIP replay too → flip fc for its words drops below the layer value. Cutoff replay excludes identically → `quiet` holds; disk ≡ layer (B3's correct write) → all three conjuncts satisfied → `preFlipTail`, informational PASS — although the movement's provenance is a post-flip adjudication, not a tail-window event. Same door for a post-flip `resetAt` (preEpoch exclusions move both replays together). Bounded honestly: the student is simultaneously flagged `adjudicationChanged`/`epochDrift`, tail rows are listed, independent disk corruption still blocks on the third conjunct, and the window is the minutes-scale flip→gate lap. But `14_:111`'s parenthetical "movement can only arise from events in the tail window — provenance by construction" remains false in this corner (the surviving custody-N4 kernel), and B4's replay drops exclusion bumps silently (`b4:124` `bump: () => {}`), so `dupConflictGroup` growth at the gate is invisible in stats. Fix: one qualifying sentence in 14_ §4 (custody N4's exact ask), optionally publish B4-replay exclusion counts.

**NEW-2 (MEDIUM-LOW, letter vs code + execution): the frozen 14_ §4 letter still states the TWO-conjunct tail law that r66 falsified, and the new third conjunct's blocking direction has zero executed cases.** `14_:111` says "`preFlipTail` REQUIRES BOTH [r66]: … MOVED … AND disk ≡ the layer expectation" — the code requires THREE (`b4-verify.mjs:212-221`). The letter's own asserted property ("mixed tail+post-flip fc divergence still BLOCKS") is only delivered by the third conjunct the letter doesn't mention; a builder re-implementing from the frozen letter ships the r66 hole verbatim. Execution: the quiet=true path runs in the lap (true-tail and cutover-tail cases pass THROUGH the new clause), but no test anywhere makes `quiet` FAIL (tail event + lost post-flip event on the SAME field ⇒ 5) — the r66 gate lens asked for exactly "one mixed-same-field lap case each way". A one-sentence letter fix + one lap case. (Cosmetic: the `quiet` ternary at `b4:220` has identical branches — the fc/timestamp distinction already lives in `:218`.)

**NEW-3 (LOW, census semantics): B4's published `adjudicationCensus` double-counts every row under `--postFlip`.** Both the flip replay (`b4:143`) and the cutoff replay (`b4:172`) share `censusCounters`, so each legacy accepted row notes twice per postFlip gate run, while pre-flip B4 counts once and the fc-retry replay (`b4:229`) passes `{}` and counts zero — three different units behind one published field, none documented. Fail-safe direction (over-report of an honesty counter), but the number an operator reads at the final gate is ~2x the row population. Fix: count in one replay only, or name the unit in the report.

**NEW-4 (LOW, execution coverage): two of the three ratified censuses have no executed assertion anywhere.** `acceptedNoTimestamp` and `challengeStatusUnknownEnum` appear in no fixture and no lap assert (grep-verified); the fixture asserts only `legacyAcceptedReconstructed >= 1` and the lap asserts key presence over a zero-valued seed (`lap:409`). Codex A1's closure letter said "assert exact nonzero/zero values in fixture and emulator cases." The publication path is shared code, so the risk is name-string drift only — but one fixture row with a missing timestamp and one with a rogue status is minutes of work.

**NEW-5 (LOW, packet accuracy): "the contaminated pointer files are DELETED" is incomplete — `docs/plans/deepfix2/evidence/b1-baseline-pointer-delta.json` survives tracked**, and it is exactly the r65 contamination class: `students: 0, cohortTotal: 2`, the lap's all-departed case-e shape presented as a real delta-mode pointer (last touched at r64, `4fbe2e1`). The change log's "stale pointers purged" overclaims the same way.

**NEW-6 (LOW, packet accuracy): the handoff claims "16_ §3 pre-run gate reworded" — no r67 change to 16_ exists** (`git diff ce7b548 83fafa7` lists no 16_), the phrase matches no r66 panel finding, and the one 16_ defect the r66 panel did name (custody N6, item 8's "rehearsalClassIds removal only") stands unchanged. A false closure claim of the exact class that sank the r64-r66 packets — small, but on the freeze attempt itself.

**NEW-7 (note, dormant): the as-of reconstruction bypasses the preOverride law for teacher-edited attempts.** For an edited attempt, `a.stored = preOverride.score` is boundary-invariant by construction (accept recomputes write `a.score`, never `preOverride`), yet `hasPostBoundaryAccept` substitutes `round(effAsOf/tq*100)` computed from CURRENT (teacher-edited) rows — up to ±2pp of silent proof-flip for full attempts, unbounded for blank-row edited attempts (no fence). Dormant today: `teacherEdited`/`preOverride` have zero production writers (grep of db.js/functions — every current edited attempt fails closed via `editedNoOrganicScore`), and post-DF2-10 the row preimages make the reconstruction exact — provided the dark-build teacher-edit writer carries the same per-row `gradedIsCorrect` duty as the accept writer. Card that duty; optionally pin `asOfScore = stored` when `edited`.

## (3) STAGE-1 FREEZE: **NO — by the narrowest margin this workstream has seen; the set is one sentence, one clause, one lap case, and packet corrections.**

**What I attacked and could not break (the r66 blocking set is genuinely dead):** the as-of passing law is exact against every production score writer I could find, including rounding, denominator, blanks, pre/post-boundary accept composition, and missing-timestamp accepts; the sibling false green blocks in an executed lap negative; the census publishes on every demanded surface with a real fixture assert; the cutover brick is dead through an executed final gate; EPERM leases are owned forever under a pure, fixture-tested law with atomic creation; the evidence is bound 8/8 to frozen bytes that verifiably predate the artifact. The five CARRIED items remain honestly scoped and non-blocking. My lens's r66 headline items (census void, as-of substitution, fence boolean, parse chain) are all closed with evidence I reproduced myself.

**What blocks:** (a) **NEW-2** — freezing 14_ §4 as written freezes the two-conjunct tail letter r66 falsified, inside the section the ratified R2-48 row cites; the code is right and the letter is not, which is precisely the contradiction class Codex ruled a freeze cannot preserve. (b) **custody N6 + NEW-6** — the 16_ drill-E contradiction from the r66 minimal set is untouched while the packet claims a 16_ rewording that does not exist; with NEW-5 that is two false packet claims on the freeze attempt itself, the r64/r65/r66 recurring class (much smaller now — every load-bearing claim verified true — but this bar was set by three consecutive rounds and the packet still fails it). (c) The new third conjunct's blocking direction is unexecuted (NEW-2's second half).

**Minimal falsifiable set to YES:** (1) rewrite `14_:111` to the three-conjunct law and qualify the provenance absolute per NEW-1 (two sentences); (2) strike "rehearsalClassIds removal only" from 16_ item 8 (one clause); (3) add the mixed-same-field lap negative (tail fail + lost post-flip fail on ONE word ⇒ exit 5) and re-run the solo lap on the frozen tree; (4) delete or annotate `b1-baseline-pointer-delta.json` and issue the r68 packet with the two claims corrected; (5) cheap, non-blocking: one fixture row each for the two unasserted censuses, B4 census unit note, `asOfScore = stored` for `edited` (or card the teacher-edit preimage duty). Nothing in the set touches ratified substance, the replay law, or any chain CLI's behavior except one added test.

On YES thereafter, I concur with the handoff's stage-2 opening order (B1 --full → dark build with the DF2-10 duties → emulator matrix → dark deploy → 25WT → shadow), with the 26SM B1 --full landing after the set so the real baseline publishes its reconstruction census under a letter that matches the machine.

---

## LENS: cutover

All verification complete. Composing the verdict.

# DEEPFIX2 r67 — panel review. LENS: cutover + lease end-state

**Basis (measured on this tree, HEAD `83fafa7`, clean over `scripts/deepfix2/`):** `pgrep -f b-emulator-lap` EMPTY at start; with three uncoordinated reviewers I took the **verification seat — lap NOT re-run**. Executed: `delta-chain-fixture.mjs` **70/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` clean on all 11 scripts · **sha16 recomputed myself: 8/8 MATCH** `evidence/emulator-lap-result.json` (71/0, written 06:19:13Z, after every script mtime; artifact `gitHead` = `ce7b548` because the run pre-dated the r67 commit — byte-level binding holds, the pin-after-run class is dead). Offline probes (scratchpad, repo untouched): `assessLease` degenerate-pid battery; `parseLedgerStrict` on a synthetic cutoverAborted-with-real-failures ledger. Full read: b3-backfill-writer, b3-txn-core, b-baseline, b4-verify, b-emulator-lap, b1-replay-lib (adjudication region), b1-expected-labels (census region), fixture stages 9c/9d/9e, 14_/15_/16_/02_/11_ folds, r66 review + panel, r67 handoff.

## (1) r66 findings — CLOSED / MISS

| r66 finding | Verdict | Evidence (file:line) |
|---|---|---|
| **Codex A1 (census discarded)** | **CLOSED** | Generic note sink aggregates all three counters (`b1-expected-labels.mjs:144-149`); published in the full summary (`:197`), redacted pointer (`:221`), console (`:224`); B4 stats carry `adjudicationCensus` with counters at both compute sites (`b4-verify.mjs:123-124,:143,:172`). Tautology REPLACED by a real count assert (`delta-chain-fixture.mjs:358-362`); lap asserts the census in B1's live output (`b-emulator-lap.mjs:409`). Nits: the fc concurrent-attempt fence replay passes `{}` (`b4:229`, uncounted), and B4's post-flip census double-counts per student (flip + cutoff replays both note) — B1's ratified census is single-count clean. |
| **Codex A2 (as-of passing)** | **CLOSED** | `asOfScore` reconstructed deterministically when any acceptance is post-boundary (`b1-replay-lib.mjs:142-146`, `round(as-of-effective/tq×100)`); fixture 9d executes BOTH boundary directions (`delta-chain-fixture.mjs:371-386`); lap runs Codex's exact reproduction and it **BLOCKS exit 5** (`lap:404-421`). |
| **Codex A3 (cutover brick)** | **SUBSTANTIALLY CLOSED; the claim "the brick is dead" is overbroad (NEW-2, NEW-3, NEW-6)** | Terminal `cutoverAborted` completion published in the FLIP_DURING_RUN catch + lease released + exit 6 (`b3-backfill-writer.mjs:456-467`); the shared reducer accepts it as terminal (`b-baseline.mjs:202-205`) and excludes the aborted layer from `appliedLayerShas` (`:207`); marker guard forbids any second B3 and any resume of the aborted run (`b3:122-127`, before all mode logic). Lap executes the FULL sequence through the final gate: inject-marker-pre-phase2 → exit 6 → `B4 --postFlip` reaches a verdict and PASSES with the unapplied layer's students settling as tail (`lap:387-394`). I verified the settle mechanics statically beyond the lap: unwritten students → moved ∧ quiet ∧ disk≡layer → published tail; committed-chunk students → disk ≡ cutoff expectation → clean PASS. Residuals in §2. |
| **Codex A4 (EPERM lease)** | **CLOSED; three edges (NEW-4, NEW-5)** | Pure `assessLease` law: EPERM owned FOREVER, alive at any age, stale = ESRCH ∨ (aged ∧ no-usable-identity) (`b-baseline.mjs:214-221`); six-branch fixture (`delta-chain-fixture.mjs:388-396`); B3 wiring maps non-ESRCH probe errors to "eperm" (`b3:266-268`). Creation is ATOMIC tmp+fsync+link (`b3:251-256`) — the wx-then-write gap is dead; on a no-hardlink mount `linkSync` FATALs loudly (fail-closed, never a silent non-atomic fallback), and **this box's `/app` is v9fs — the green 71/0 lap itself is the empirical proof link works here** (every EXECUTE exercised it). Harness lap-lock got claim-by-rename (`lap:53-56`). |
| **Codex A5 (evidence identity)** | **CLOSED core; one claim false (pointer)** | 8/8 sha16 verified by me against the tree; no source mtime post-dates the evidence write; red laps write a sidecar, never the tracked artifact (`lap:452-454`); B1 skips pointer writes under isolation (`b1-expected-labels.mjs:217`). **MISS: "the contaminated pointer files are DELETED" is half-true — `evidence/b1-baseline-pointer-full.json` is gone, but `b1-baseline-pointer-delta.json` remains tracked at HEAD (emulator-shaped: `students: 0, cohortTotal: 2`), the exact r65 evidence-NEW-3 file.** |
| **Codex B prose ×3** | **CLOSED** | 15_ §6b "immutable IN THE PREIMAGE [r67 — reconciled]" (`15_:175-176`); 14_ custody predicate in both sentences (`14_:113,:116` — marker ∨ rehearsal); tail-heal honesty ONE sentence in 14_ §4 (`14_:111`) AND the R2-48 register row (`11_:38`). |
| **Panel: evidence binding (law NEW-1 / gate NEW-3 / custody §0)** | **CLOSED** | 8/8 above. |
| **Panel: census void + fixture tautology (law NEW-2/3, gate NEW-2/5)** | **CLOSED** | As A1. |
| **Panel gate NEW-1 (third tail conjunct)** | **CODE CLOSED; letter + execution MISS** | The quiet conjunct is real: tail requires moved ∧ (cutoff ≡ flip per field) ∧ disk ≡ layer (`b4-verify.mjs:212-221`; the `:220` ternary is vestigial — both branches identical). **But 14_ §4 still states the TWO-conjunct law ("REQUIRES BOTH [r66]") and keeps "provenance by construction" verbatim (`14_:111`) — the letter-alignment the gate lens prescribed is absent — and the prescribed mixed-SAME-field lap case (tail + lost post-flip stamp on ONE word ⇒ 5) was never added: no executed case makes the quiet conjunct load-bearing in the blocking direction** (lap tailA/lostB are disjoint words, `lap:335-343`). Custody N4's in-place-mutation qualifier also unaddressed. |
| **Panel gate NEW-4 / custody N1 (plain-guard delta blindness)** | **PARTIAL — fresh-plain closed; the wedge was traded for a revert (NEW-1)** | Fresh plain EXECUTE now FATALs on applied layers OR dangling DELTA intents via `parseLedgerStrict` (`b3:284-294`), lap-executed for the applied case (`lap:376-377`; the dangling-delta-intent negative is unexecuted). Custody N1(b)'s forever-wedge is resolved by exempting RESUME (`b3:279` `!RESUME`) — which opens the M0-revert my lens brief names (NEW-1). Custody N1's second ask (delta EXECUTE refuses over `red.problems`) is unimplemented — delta mode still consults NO ledger. |
| **Custody N2 (EPERM claim false)** | **CLOSED** | Code now matches the sentence: EPERM owned at ANY age (`b-baseline.mjs:220`; refusal message `b3:268`). |
| **Custody N3 / gate NEW-7 (empty-lease instant steal)** | **SUBSTANTIALLY CLOSED** | Atomic creation kills the in-law window; residual letter nit: `assessLease(null)` is stale at ANY age (`b-baseline.mjs:215`, fixture `:395`) — the handoff formula "aged ∧ no-usable-identity" doesn't cover it; reachable only via out-of-law corruption, and the ENOENT race path fail-closes ("takeover raced", `b3:270`). |
| **Custody N5 / gate NEW-6 (14_:113 marker-only)** | **CLOSED** (`14_:113`). |
| **Custody N6 (16_:72-73 vs DRILL-E LAW)** | **MISS — unchanged** | `16_:72-73` still says "…+ rehearsalClassIds removal only", contradicting the DRILL-E LAW at `16_:88-94` one screen below. |
| **Law-lens roster leftover (14_ post-flip re-enrollment)** | **MISS — unchanged** (`14_:122-123`), LOW. |
| **Law NEW-5 nits** | (i) fence exact boolean **CLOSED** (`b1-replay-lib.mjs:111` `r.adjOk === true`); (ii) one reviewedAt parse chain **CLOSED** (`:102`); (iii) lap-lock rename **CLOSED** (`lap:53-56`; unconditional exit-`rmSync` at `:59` remains, test-infra); (iv) red-lap sidecar **CLOSED**; (v) pointer files **PARTIAL** (delta pointer remains). |
| **Custody N7 (CARRIED completeness) + mixed-(d)** | **PARTIAL** | Red-lap overwrite and lap-lock fixed; the -delta pointer and the card's mixed-(d) REQUIRED case (still executed as pure structural, `lap:150-163`) remain open and remain absent from the r67 CARRIED list. |

## (2) NEW defects

**NEW-1 (MEDIUM-HIGH — the lens headline, the prompt's named attack CONFIRMED): the plain-guard's blanket RESUME exemption revives a plain run AFTER delta layers landed — a tool-instructed, clean-exit M0 revert.**
`b3-backfill-writer.mjs:279` guards only `!args.deltaDir && !extrasRepair && !RESUME`. Concrete scenario: (1) plain M0 EXECUTE `R0` crashes post-intent (dangling plain intent). (2) A delta M1 EXECUTE is admitted — delta mode consults NO ledger — applies M1, releases the lease. (3) Any B4 now FATALs `"R0 attempt 0: intent without completion (crash mid-run? **resume it**)"` (`b-baseline.mjs:200`) — the ledger itself orders the destructive step. (4) The operator resumes R0: marker absent, manifest binding passes (mode/baseline/`deltaLayer:null` all match, `b3:229-241`), plain-guard SKIPPED, lease free. Resume regenerates plans from live disk against M0 expectations — `resolveExpectedSource(uid, original, [])` at the M0 watermark; M1's attempts are post-watermark so the digest check CANNOT rescue (`b3:339-346`) — and phase 2's re-diff dutifully writes M0 values over every M1-updated, non-journaled student (`b3-txn-core.mjs:51-63`). Clean completion, **exit 0**. Loud only at the next B4 (structural exit 5) — precisely the deficiency class the plain-guard was built to kill (`b3:275-278`'s own comment). The handoff's precision claim ("resume exempt — it IS the crash-resolution path; …not revert hazards") is wrong exactly here, and its cited evidence — "the lap's crash-resume suite runs green through it" — proves nothing: crash2/3/4 resume with ZERO delta history, where the guard passes with or without the exemption. Fix (small): plain RESUME refuses when `red.appliedLayerShas.size` or foreign delta-bearing dangling intents exist (message: "superseded by delta history — re-execute the delta chain"); delta EXECUTE refuses over `red.problems` (custody N1's unimplemented half, which also removes step 2); one lap case: crashed-plain → delta lap → resume REFUSED.

**NEW-2 (MEDIUM): the terminal-completion fix covers only the in-process abort path — a crash-then-flip still bricks the gate permanently, with self-contradictory instructions.**
The cutoverAborted record is appended by the LIVE process inside the catch (`b3:460-464`). If B3 dies without it — host crash mid-phase-2 with the flip landing before anyone resumes, or `ledgerAppend` itself throwing inside the FLIP_DURING_RUN handler (disk full) — the intent dangles, the marker stands, and: B4 FATALs forever (`b4:69-72`, problems fatal under `--postFlip` too) telling the operator to "resume it", while B3's marker guard refuses every invocation including `--resume` (`b3:122-127`). No in-law resolution exists (B4 is read-only; `--ignoreLedger` is forensic-only and B3 refuses such reports). Mitigation is purely procedural — the DF2-14 choreography's final pre-flip micro-lap (`02_:95`) would catch a dangling intent before the flip — so the reachable window is a B3 crash between the final green B4 and the flip, or choreography violation. The handoff's "The brick is dead" must be scoped: the deliberate interleaving is dead; the crash-window brick is not, and it has no carded disposition. Fix: card it with an operator law (or implement Codex's other prong — the flip txn checks a B3 execution lease).

**NEW-3 (MEDIUM-LOW): the cutoverAborted completion FABRICATES a zero outcome and skips result publication — pre-cutover failures vanish from every durable record.**
`b3:463` hard-codes `outcome: { txnFailures: 0, skippedResetLocked: 0, skippedEpochDrift: 0, cutoverAborted: true }` while `stats` may hold real accumulated txnFailures/skips from students processed before the flip (`b3:469-477`); `process.exit(6)` also skips the `result.json` write (`b3:485-490`), and skipped (reset-locked/epoch-drift) students get no journal line by design (`b3:472-474`). Scenario: 30 students fail chunk txns (network flap), then the flip lands — the ledger's terminal record claims a zero-failure run; reset-locked students are recorded NOWHERE durable. Value-safe (failed txns are atomic no-ops; B4 re-derives state and settles them as tail/diffs) and my probe confirms the reducer ignores the outcome numbers under `cutoverAborted` (`b-baseline.mjs:204` short-circuits before the `:205` check — problems `[]` even with `txnFailures:30` folded in), so writing the TRUE stats costs nothing. A forensic ledger must not contain fabricated zeros. Fix: publish `...stats` fields + write the result file (or document its absence) on the exit-6 path.

**NEW-4 (MEDIUM-LOW): lease-takeover ABA — the claim-by-rename can reap a LIVE successor's lease; "exactly one renamer wins" is not a two-claimant safety argument.**
`b3:264-272`: read holder → assess → `renameSync` → re-create. Two starters A and B both read a genuinely stale lease and assess stale. A renames (wins) and re-creates its own live lease. B's later `renameSync` targets the same path — which now holds A's LIVE lease — and SUCCEEDS (path rename verifies nothing about content); B re-creates and both EXECUTE concurrently: the exact two-writer hazard the lease exists to prevent. The loser's-ENOENT signal only fires in the microsecond gap between A's rename and A's re-create. Preconditions: a dead lease + two near-simultaneous starters — the class the CARRIED "two-process negative" defers, but that item frames a TEST gap ("both lease branches + rename execute single-process"); this is a DESIGN gap. Fix: after renaming, read the reaped file and verify token/pid ≡ the assessed holder; on mismatch rename it back and FATAL. Same pattern applies to the lap lock (test-infra; its exit handler's unconditional `rmSync` at `lap:59` compounds it there).

**NEW-5 (LOW): `assessLease` treats pid ≤ 0 as a probeable identity — a malformed-pid lease is permanently unreapable.**
Probed live on this box: `{pid: 0}` → `process.kill(0,0)` signals our own process group → `{"stale":false,"reason":"alive"}` at 10h age; `{pid: -1}` → "alive" likewise. A corrupted lease with pid 0/-1 can never be reaped by the law (manual `rm` only) — and the "six-branch" fixture has no such branch; the law's own intent ("malformed pid" = no-usable-identity) says these should fall to the aged rule. In-law writers always record a real positive pid, so this needs out-of-law content — but the fix is two tokens (`Number.isInteger(holder.pid) && holder.pid > 0`). Related letter nit: unparseable → stale at ANY age (fixture `:395`) diverges from the handoff's "aged ∧ no-usable-identity" formula.

**NEW-6 (LOW — packet accuracy): "the unapplied layer's students settle as PUBLISHED tail" is overbroad — a layer-only JOINER of a cutover-aborted layer settles as an `uncoveredAtGate` BLOCKER, not tail.**
The aborted layer is excluded from `appliedLayerShas` (`b-baseline.mjs:207`), so B4's chain legally omits it; a student covered ONLY by that layer resolves `src.row = null` → `uncoveredAtGate` + diffs BLOCK (`b4:171,:210` — the tail law requires `src.row`). Fail-closed and listed, so nothing is masked — but post-marker no B3 can ever cover them, so the gate blocks until manual disposition; the handoff sentence and the lap (whose aborted layer contains only M0-covered emB) don't cover this shape. Also unexecuted: the partial-commit cutover (marker landing after chunk k) — statically it settles correctly (committed students match the cutoff expectation; unwritten students are tail), but the executed case pins only the zero-chunks variant.

**Nits:** B4's post-flip census double-counts (both replays note per student) and the fc fence replay is uncounted (`b4:229`) — B1's published census, the ratified artifact, is clean. `quiet`'s ternary at `b4:220` is dead weight. `16_:72-73`, `14_:122-123`, the mixed-(d) card case, and the -delta pointer are open, unlisted residue (§1).

## (3) STAGE-1 FREEZE: **NO** — narrowly; the closure work is real and the evidence binding finally holds

What held against my attacks: the evidence identity (8/8, frozen bytes — the r64/r65/r66 class is genuinely dead); the terminal cutover completion's core mechanics (reducer terminality, aborted-layer exclusion, no-second-B3, no-resume-of-aborted, lease release, executed end-to-end through a green final gate); the settle semantics for both written and unwritten students of an aborted layer; the EPERM-forever lease law and its atomic creation (empirically green on this v9fs mount); the census publication end-to-end; the as-of law with its executed negative.

What blocks: **(a) NEW-1** — a constructible, tool-instructed M0-revert with clean exit 0 inside a headline r66 closure (the plain-guard), introduced by this round's own "PRECISE" exemption and certified by evidence that never exercises it. **(b)** The packet again contains false/overbroad claims of the class that sank r64-r66: "the brick is dead" (NEW-2's crash window has no disposition), "resume … not revert hazards" (NEW-1), "pointer files are DELETED" (the -delta file is tracked at HEAD), "settle as published tail" (NEW-6), plus the CARRIED list again omitting known residue (mixed-(d), -delta pointer). **(c)** The third tail conjunct — an r67 headline extra — has no executed case in its blocking direction and 14_ §4 still states the two-conjunct letter; freezing now freezes a governing sentence the code has already outgrown.

**Minimal falsifiable set to YES:**
1. Scope the plain-guard RESUME exemption (refuse plain resume over applied layers / foreign dangling-delta intents; delta EXECUTE refuses over `red.problems`) + one lap negative (crashed-plain → delta → resume REFUSED).
2. Cutover truthfulness: real `stats` in the cutoverAborted outcome (+ result-file disposition); scope the "brick is dead" claim and card the crash-then-flip window with an operator law (or add the flip-txn lease check).
3. Packet/letter corrections: the pointer-file sentence (delete or annotate `b1-baseline-pointer-delta.json`); the tail-settle joiner qualifier; 14_ §4 aligned to the three-conjunct law; the `16_:72-73` fold; CARRIED completed (mixed-(d), pointer).
4. One mixed-same-field tail lap case each way (the gate lens's prescribed executions for the quiet conjunct).
5. Cheap hygiene, non-blocking alone: `pid > 0` in `assessLease`; verify-after-rename on lease takeover.

Items 1-2 are hours; 3-5 are minutes-to-hours; none reopens law substance or touches David. On completion, this lens flips to YES, and the handoff's stage-2 opening order stands (B1 --full → dark build → emulator matrix → dark deploy → 25WT → shadow) — with item 1 landing BEFORE any real 26SM B3 EXECUTE, since crash-resume is precisely the flow real operations will exercise.

---

## LENS: freeze

All verification complete. Composing the verdict.

# DEEPFIX2 r67 — panel review. LENS: freeze-readiness holistically

**Basis (measured on this tree; HEAD moved ce7b548 → 83fafa7 mid-review as the orchestrator committed the r67 fold — I re-verified against BOTH):** evidence sha16 recomputed by me for all eight scripts vs `docs/plans/deepfix2/evidence/emulator-lap-result.json` — **8/8 MATCH against the worktree AND against committed HEAD 83fafa7** · mtime timeline proves frozen bytes: last source edit 06:03:49Z (b3-backfill-writer) < evidence write 06:19:13Z < handoff 06:20:06Z < ready 06:20:25Z — **zero post-run source edits** · `delta-chain-fixture.mjs` **70/0** (executed) · `rotation-cyclicity-fixture.mjs` **2,688/0** (executed) · `node --check` clean 11/11 · index ≡ worktree ≡ commit on every bound script · lap NOT re-run (pgrep showed only my own probe; three uncoordinated reviewers — I took the static seat my lens prescribes) · lap `ok()` sites counted: exactly **71** ≡ the artifact's 71/0 · full read: b1-replay-lib, b1-expected-labels, b4-verify, b3-backfill-writer (lease/guard/cutover regions), b-baseline (reducer + assessLease), b-emulator-lap, fixture stages 9c/9d/9e, 14\_/15\_/16\_/11\_/02\_ card, baton (round 67, owner codex, rev 206, ready marker consistent), change-log rows.

## (1) r66 findings — CLOSED / MISS

### Codex A1–A5 + B

| Finding | Verdict | Evidence |
|---|---|---|
| **A1 census discarded** | **CLOSED** | Generic note sink `b1-expected-labels.mjs:144,149-151`; published in the full summary `:197`, redacted pointer `:221`, operator console `:224` — all three counters, zero-defaulted. B4 counts at both compute sites (`b4-verify.mjs:123-124` stats init + counter callback; fed at `:143` and `:172`). The tautology is dead: `delta-chain-fixture.mjs:358-361` is a real callback-counter assert (`legacyAcceptedReconstructed >= 1`). Lap asserts the census in B1's live output (`b-emulator-lap.mjs:409`). R2-49's "COUNTED and PUBLISHED" is now true of the implementation. |
| **A2 as-of passing** | **CLOSED** | `b1-replay-lib.mjs:142-146`: `hasPostBoundaryAccept` (adjOk ∧ ¬adjEff) ⇒ `asOfScore = round(as-of-effective-correct/tq×100)`, `passing = asOfScore >= 92`. Fixture 9d is Codex's exact sibling shape both directions (`delta-chain-fixture.mjs:381-385` — lp stays day-1 before the review instant, moves to day-2 after). Lap executes the exact reproduction (post-flip accept + score 100 + corrupt sibling lp day1→day2) ⇒ **BLOCKS exit 5** (`b-emulator-lap.mjs:403-421`). |
| **A3 cutover brick** | **CLOSED** | FLIP_DURING_RUN publishes a terminal completion `outcome.cutoverAborted:true` + conditional lease release + exit 6 (`b3-backfill-writer.mjs:456-467`). The shared reducer accepts it as terminal (`b-baseline.mjs:202-204`) and excludes the aborted layer from `appliedLayerShas` (`:207` — NOT-applied). Lap runs THROUGH the final gate: post-cutover `B4 --postFlip` never ledger-FATALs and **PASSES** (`b-emulator-lap.mjs:387-394`, two asserts). |
| **A4 EPERM steal** | **CLOSED** | Pure `assessLease` (`b-baseline.mjs:214-221`): EPERM ⇒ `stale:false` "eperm-owned" at ANY age; dead(ESRCH) stale immediately; aged-no-identity stale; fresh-no-identity protected. Six-branch fixture `delta-chain-fixture.mjs:387-396`. Consumed at `b3:266-268` with a three-way probe. Creation is atomic tmp+`linkSync` (`b3:251-255`) — the wx-then-write empty-lease window (custody N3's root cause) is dead at the source. |
| **A5 evidence identity** | **CLOSED** | 8/8 sha16 verified by me twice (worktree + HEAD 83fafa7); frozen-bytes timeline above; red laps write a sidecar, never the tracked artifact (`b-emulator-lap.mjs:452-454`); both contaminated pointer files DELETED in 83fafa7; B1 skips pointer writes under isolation (`b1-expected-labels.mjs:217`). |
| **B prose ×3** | **CLOSED** | §6b: "grading history is immutable IN THE PREIMAGE [r67 — reconciled with the (1) duty below … the flip is display truth]" (`15_:175-177`). 14\_ custody clause states the predicate in BOTH sentences ("per THE R2-48 PREDICATE — `firstEnabledAt` SET ∨ class ∈ `rehearsalClassIds` [r67: the one predicate, stated here too]" + "ZERO … OUTSIDE `rehearsalClassIds` [the carve-out]", `14_:112-117`). Heal honesty is ONE sentence in `14_:111` AND the R2-48 register row now carries the fc qualifier (`11_:38` "timestamp tails heal through live use — a pure-tail fc deficit persists as a PUBLISHED undercount [r67 honesty]"). |

### Panel r66 extras (across lenses)

| Finding | Verdict | Evidence |
|---|---|---|
| law NEW-1 / gate NEW-3 / custody §0 (evidence binding) | **CLOSED** | 8/8, frozen bytes — verified independently above. |
| law NEW-2 / gate NEW-2 (census void) | **CLOSED** | = A1. |
| law NEW-3 / gate NEW-5 (fixture tautology + §6b self-contradiction) | **CLOSED** | Real assert `:358-361`; §6b reconciled `15_:175-177`. |
| law NEW-4 (as-of residue) | **CLOSED** | = A2. |
| law NEW-5 nits (i)-(v) | **CLOSED** | (i) fence uses the exact boolean `r.adjOk === true` (`b1-replay-lib.mjs:111`); (ii) one reviewedAt parse chain (`:102` now identical to `:41`); (iii) lap lock claim-by-rename (`lap:53-56`); (iv) red-lap sidecar; (v) pointers deleted. Residual: the lap lock's CREATE is still wx-then-write (`lap:47,:56`) — test-infra, nit. |
| **gate NEW-1 (third tail conjunct)** | **CLOSED IN CODE; letter + executed case lag (see NEW-1 below)** | `b4-verify.mjs:212-225`: tail requires ALL THREE — MOVED (flip ≠ layer) ∧ QUIET (cutoff ≡ flip for THIS field; fc uses the through-cutoff `exp`, timestamps use `cutoffFieldRef`) ∧ disk ≡ layer. I traced the gate lens's exact scenario (layer fc=1, tail→flip 2, lost post-flip→cutoff 3, disk 1): quiet fails (3≠2) ⇒ falls to the fc fence `:227-233` ⇒ BLOCKS. Timestamp variant likewise blocks. |
| gate NEW-4 / custody N1 (plain-guard bypass + wedge) | **CLOSED** | Guard consumes `parseLedgerStrict`; FATALs on applied layers OR dangling DELTA intents (`b3:279-295`); N1(a) bypass dead. RESUME exempt ⇒ N1(b)'s permanent wedge dead (the crash-resume lap suite passes through it). Residual corner in NEW-2 below. |
| gate NEW-6 / custody N5 (`14_:113` marker-only) | **CLOSED** | Verified above; repo-wide grep finds NO surviving unqualified "stamp ONLY when firstEnabledAt" sentence. |
| gate NEW-7 / custody N3 (lease-creation gap) | **CLOSED at the root** | tmp+link creation. Residual: unparseable ⇒ stale at ANY age (NEW-3 below). |
| custody N2 (EPERM packet claim false) | **CLOSED** | Cured in the direction custody offered: the code now MATCHES the claim (EPERM owned forever). |
| **custody N4 (provenance absolute)** | **MISS** | `14_:111` still says "movement can only arise from events in the tail window — provenance by construction" unqualified. A post-flip in-place mutation of a pre-flip attempt moves flip AND cutoff expectations equally ⇒ QUIET holds ⇒ still classified tail (published as `adjudicationChanged`, never silent). Survives the third conjunct by construction. LOW; not in CARRIED. |
| **custody N6 (`16_:72-73`)** | **MISS** | Byte-identical: "drill E uses shadow-class-scoped `reviewGateEnabled` + rehearsalClassIds removal only" — still contradicts the DRILL-E LAW at `16_:88-94` (class REMAINS registered; removal = DRAIN-gated teardown) one screen below. 16\_ has ZERO r67 edits (mtime 04:17Z, absent from commit 83fafa7). Not in CARRIED. |
| custody N7 (CARRIED completeness) | **PARTIAL** | Three of the four named omissions closed the right way (lap-lock rename; red-lap sidecar; pointers deleted). The fourth — the card's mixed-(d) case — is still neither executed as mixed nor re-scoped nor CARRIED (see below). |

## (2) NEW defects

**NEW-1 (MEDIUM-LOW, letter vs code — the freeze's own coherence standard): the three-conjunct tail law exists ONLY in code; every governing letter still states the two-conjunct law, and the conjunct's distinguishing case is unexecuted.**
`14_:111` reads "`preFlipTail` REQUIRES BOTH [r66] … MOVED … AND disk ≡ the layer expectation; a lost POST-flip stamp leaves flip ≡ layer ⇒ NOT tail ⇒ BLOCKS" — the exact two-conjunct letter whose hole gate NEW-1 proved (the pure lost-stamp claim is true only when the tail is empty on that field). Grep across 14\_/15\_/16\_/11\_/02\_: the QUIET clause appears NOWHERE. Concrete failure: a stage-2 implementer or re-verifier building/checking from the frozen letter reproduces the r66 false green (tail event + lost post-flip stamp on the SAME field ⇒ excused). Mitigations: the code is sha16-frozen alongside, and the letter UNDERSTATES the implemented protection (fail-safe direction — opposite of r66's blocking prose class). Also: the lap's mixed case is disjoint-words only (`tailA` w3 + `lostB` w1, `lap:335-343`); the same-field mixed case — the ONLY case that distinguishes conjunct 3 — is executed nowhere (the conjunct lives in the B4 CLI, unreachable by the offline fixture). My closure of gate NEW-1's code half is static-verified, not execution-verified. Fix: one sentence in `14_:111` (BOTH → ALL THREE + the quiet clause + custody N4's "absent in-place mutation" qualifier), one same-field mixed lap case each way (fits the carded stage-2 matrix).

**NEW-2 (LOW-MEDIUM, code corner opened by the r67 exemption): a `--resume` of a crashed PLAIN run after an interleaved delta EXECUTE reverts M1 → M0 under a clean exit 0.**
Sequence: plain M0 EXECUTE crashes mid-phase-2 (proven crash points) → operator hand-runs a delta EXECUTE from a leftover materialized layer dir (delta mode consults NO ledger — custody N1's prescribed "delta refuses over `red.problems`" half was not implemented; B4 would refuse to mint a NEW layer over the dangling intent, but a pre-crash dir suffices) → M0 `--resume` is now ADMITTED (`b3:279` exempts RESUME), regenerates plans from live state against M0 expected, and reverts the delta students it hadn't journaled — loud only at the next B4 (exit 5). In r66 this sequence was impossible (the guard bricked the resume instead — the N1(b) wedge); r67 traded the brick for this narrower corner and the packet states the trade's rationale ("plain dangling intents are lease/runId concerns") without naming the interleaved-delta case. Requires two operator errors outside the driver's sequencing; ends loud; same class as the CARRIED two-process negative. Fix when touched: resume re-checks the ledger for layers applied since its own intent, or land custody's delta-mode-refuses-over-problems half.

**NEW-3 (LOW, packet formula vs code): `assessLease` treats an unparseable lease as stale at ANY age — the handoff's own formula says "stale = provably-dead ∨ (aged ∧ no-usable-identity)".**
`b-baseline.mjs:215` returns `{stale:true}` for a null/non-object holder with no age gate (the fixture states it plainly: "unparseable stale"). A FRESH corrupted lease of a live holder is stolen instantly ⇒ two writers. Reachable only via out-of-band corruption now (tmp+link creation means no legitimately-created lease is ever partial), and the conditional release keeps the aftermath sane — but the handoff sentence and the code diverge on the branch, which is the r64-r66 packet-accuracy class in miniature. Fix: age-gate the unparseable branch (mtime), or state the formula as implemented.

**NEW-4 (NITS):** (i) `b4-verify.mjs:220` — the `quiet` ternary has byte-identical branches (dead conditional). (ii) B4's census double-counts each legacy row under `--postFlip` (both the boundary and cutoff replays feed `censusCounters`; the `:229` fc-retry passes `{}` — inconsistent counting policy; B1's census, the ratified one, is single-count and correct). (iii) The artifact's `gitHead` records ce7b548 (true at run time) while its bytes now live in 83fafa7 — sha16s carry the binding; a future gitHead cross-check will look one commit off. (iv) The as-of reconstruction also fires for teacher-edited attempts whose proof comparator (`preOverride.score`) is boundary-invariant by construction — a row-recompute substitution the B1-Q3 letter forbids; self-consistent across all replays (invisible to the gate), edited+post-boundary-accept corner only. (v) The handoff's panel-extras line "16\_ §3 pre-run gate reworded" is a phantom: no r66 finding requested it (grep of the panel and Codex review: zero hits), 16\_ has zero r67 edits — it appears to recount the r65p rewording already committed in r66. Benign direction (claims unneeded work rather than concealing needed work), but a false packet line by the letter of this workstream's standard.

## (3) The lens-specific verdicts

**Evidence 8/8: VERIFIED.** Computed myself against worktree and commit; frozen-bytes discipline proven by mtimes. The r64/r65/r66 evidence-identity class is dead this round.

**02\_ card REQUIRED CASES, letter-by-letter vs the lap:** (a) ✓ `lap:115-121` (exit 6 → driver PASS) · (b) exit-7 ✓ `:123-134` · (c) pinned exit 7 ✓ `:136-148` · **(d) "a MIXED unrelated structural diff" — the lap still executes pure-structural-only** (`:150-164`, B4 exit 5 "structural DIFFS-no-delta", no concurrent actionable delta; the custody-r66 reading of "mixed" remains unexecuted, un-rescoped, and absent from CARRIED — the sole surviving card-letter gap) · (e) ✓ `:166-184` (empty excused layer, departed COUNTED) · crash ×4 + torn ✓ (`:187-217`, `:397-400` — torn "[executed: malformed-line FATAL]" matches the card annotation; first-append re-scope stands) · --postFlip incl. post-flip adjudication ✓ (`:220-310`) · resume lap ✓ (crash2/3/4) · A3 stale-report negative ✓ (`:312-328` "predates reality"). **8.5 of 9 letters.**

**Packet accuracy line by line:** every load-bearing claim verified TRUE — 71/0 (71 `ok()` sites ≡ artifact), 70/0 and 2,688/0 (reproduced), 8/8 (computed), frozen bytes (mtimes), all A1-A5 mechanism claims, all three prose claims, "the lap runs THROUGH the final gate and PASSES", "resume exempt … crash-suite green". The three inaccuracies found are small and named above: the phantom "16\_ §3" line (NEW-4v), the EPERM-formula/unparseable divergence (NEW-3), and the unstated resume-after-delta trade (NEW-2). No false claim conceals a safety gap this round — a first for three rounds.

**CARRIED completeness:** the five listed items are honest and non-blocking (I concur with the r66 panel's rulings on each). The list is again not the entire known residue: open-but-unlisted are `16_:72-73` (N6), the `14_:111` two-conjunct letter + provenance absolute (NEW-1/N4), and mixed-(d). All letter-level; none freeze-heavy alone.

**One-law coherence sweep:** greps for marker-only (dead — no surviving unqualified sentence anywhere), heal (coherent — 14\_:111 one-sentence honesty, 11\_:38 qualified; the roster-law "converges through live use" sentence is defensible as scoped to advisory labels), immutability (§6b reconciled). The single surviving intra-doc contradiction is `16_:72-73` vs `16_:88-94`; the single letter-vs-code divergence is the tail conjunct count.

## (4) STAGE-1 FREEZE: **YES** — with a mandatory nit list

The r66 blocking set is closed in substance AND in execution: census published end-to-end with real asserts; as-of passing with Codex's exact reproduction blocking in the lap; the cutover un-bricked through a PASSING final gate; EPERM owned forever under a pure, fixture-covered law; and the evidence pillar — the item that sank three consecutive rounds — is 8/8 on provably frozen bytes, verified independently twice. Both ratified rows (R2-48, R2-49) now match their implementations; R2-49's census clause, the one ratified letter that overclaimed, is implemented as minted. What remains is exclusively letter-lag in the fail-safe direction, unexecuted corners of the class already carried, and cosmetics. Per my charter I do not manufacture blockers from these.

**The nit list (fold with the freeze; none reopens law substance):**
1. `14_:111` — one sentence: BOTH → ALL THREE + the QUIET clause (+ N4's "absent in-place mutation of pre-flip attempts, which publishes as adjudicationChanged" qualifier). The frozen letter must state the implemented law.
2. `16_:72-73` — one sentence: align item 8 to the DRILL-E LAW (assignment-gate OFF only; removal = DRAIN-gated teardown).
3. Card: execute mixed-(d) (concurrent actionable delta + unrelated structural ⇒ FINAL failure after the delta lap) or re-scope its letter honestly; add the same-field mixed tail case both ways — both fit the already-carded stage-2 emulator matrix.
4. Record NEW-2 and NEW-3 as named corners (or land the two tiny fixes: resume ledger re-check / delta-refuses-over-problems; age-gated unparseable branch).
5. Cosmetics at leisure: `b4:220` dead ternary; B4 postFlip census double-count policy; the artifact gitHead note; the phantom handoff line; lap-lock wx-create.

**Stage-2 opening order on YES:** the handoff's standing order remains right — B1 --full (the census now lands in the 26SM baseline's first real run) → dark build with the DF2-10 duties (stamping predicate + §6b preimage verbatim from the card) → emulator matrix (inherits first-append + torn + two-process + the nit-3 cases) → dark deploy → 25WT rehearsal → shadow audit.

**Bottom line:** the strongest packet this workstream has produced — the first round where every headline claim survived independent verification. Freeze it, fold the two sentences and the named nits into the freeze record, and open stage 2.