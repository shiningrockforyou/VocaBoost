# 3-Fable panel — DEEPFIX2 r64 (2026-08-02)

NOTE: two panel agents re-ran the emulator lap CONCURRENTLY in the shared tree — collision artifacts contaminated the in-flight r65 lap run and the evidence artifact (restored by the lap-lens agent). Findings below.

---

## LENS: lap

# DEEPFIX2 r64 — Panel review. LENS: the emulator lap itself (b-emulator-lap.mjs vs the 02_ card)

**Reviewer basis (executed, not just read):** `node --check` clean on all 10 `scripts/deepfix2/*.mjs` · `delta-chain-fixture.mjs` **55/0** and `rotation-cyclicity-fixture.mjs` **2,688/0** (both match the handoff) · read every script in the review surface (harness 257 lines, B1/B3/B4/driver/baseline/replay-lib/txn-core in full) · **and I RE-RAN THE EMULATOR LAP MYSELF on this box** (Temurin-21 + firebase-tools present; transcript at `/tmp/claude-1000/-app/87eba36e-8e66-4638-bae9-6cd6f923fff6/scratchpad/solo-lap.log`): **41 checks / 0 failures, exit 0** — the headline claim REPRODUCES. My transcript additionally pins what the artifact cannot: case (c) actually exited **7**, case (e) exited **6**.

**Incident disclosed up front:** the other two panel re-runs executed CONCURRENTLY with each other (pids observed live, 02:42 and 02:49 starts). The harness shares one set of REAL artifact paths with no lock, so each run's `resetCase()` wiped the other's mid-case state: the packet's evidence artifact `docs/plans/deepfix2/evidence/emulator-lap-result.json` was overwritten to **41/2 (02:51:56)** and then **41/9 (02:58:14)** during its own review round. Both red tallies are interference artifacts, not chain defects — my quiet-box solo run restored 41/0 deterministically. I backed up the author's original artifact (02:38:46, 41/0, untracked — it would otherwise have been unrecoverable) and **restored it** after my run; `26sm-census.json`/`26sm-class-allowlist.json` verified byte-intact. This live accident is itself finding NEW-1.

## (1) r63 findings — CLOSED / MISS with evidence

| Item | Verdict | Evidence |
|---|---|---|
| Codex A1 fc cumulative | **CLOSED (code+fixture); lap executes the green side only** | `b-baseline.mjs:31` fc never timestamp-exempt; `b4-verify.mjs:38` captured cutoff, `:183` through-cutoff replay, `:201` fc expectation swap, `:207-213` concurrent-attempt re-read fence. The 3-step counterexample is fixture stage 9b (`delta-chain-fixture.mjs:324-338`, deficit 2≠3 caught) + `:357`. The lap's postFlip case executes the through-cutoff branch on a MATCHING value (hand-written fc=2 ≡ replay 2) — the catch path never fires in any CLI execution (see lap audit). |
| Codex A2 repair resolution | **CLOSED in code; the demanded positive fixture is ABSENT** | Modes exclusive `b3-backfill-writer.mjs:76-77`; chain loads and **becomes the resolver** `:146-158` (`deltaLayers = chainLayers` at `:158`); delta-scope collapse gated to delta mode `:188`; ledger reality audit `:162-173`. Lap executes the REFUSAL (`A3-reality`, exit 2 observed). But Codex demanded "add a fixture where M1 changes U, an unrelated extra is repaired, and U remains byte-equal": `grep repairExtras delta-chain-fixture.mjs` = **zero hits**, and the lap never runs a VALID repair. The mode-law resolver swap has never executed anywhere. |
| Codex A3 ledger pairing | **CLOSED in code; the dangerous direction unexecuted** | Keyed `(runId, attempt)` `b4-verify.mjs:77`; version/attempt/outcome strictness `:74,:75,:80`; latest-attempt-clean law `:86-91`; absent-ledger FATAL with `--appliedDelta` `:60-62`. Lap executes dangling-intent FATAL twice + resume supersede (benign direction). Codex's exact demanded test — clean attempt then a CRASHED LATER resume whose dangling intent must FATAL past the older completion — executed nowhere (fixture has no ledger coverage at all: `grep ledger delta-chain-fixture.mjs` = zero hits; no repo fixture spawns the CLIs). |
| Codex A4 resume custody | **CLOSED** | `wx` lease loop `b3:243-247`; per-attempt `resume-N.plans.jsonl` + `wx` preimage `b3:248-253`. Lap executes three resume heals (single-process; the concurrency claim rests on `wx`, which is fine). |
| Codex A5 governance | **PARTIAL — the lap ran pre-freeze (verified by my re-run), but the executed scope < the r64-expanded card's own REQUIRED CASES** | Cards repaired: `02_TASK_LIST.md:95` names `b-delta-cycle.mjs` + full scope; `14_:92-95` likewise; only the driver's own historical header still mentions the `.sh`. The gap is section (2)/lap-audit: the card (expanded THIS round) requires mixed-(d), the post-flip-adjudication lap case, the first-append window, and "during completion publication" — none of which the executed lap contains. The freeze claim "the card's lap-gate is satisfied" fails on the card's letter. |
| Codex B1 knownUids | **CLOSED + lap-executed** | `b4-verify.mjs:125` auth-uids unioned; lap case (e) `departedSkipped: 1` asserted and observed. |
| Codex B2 eager index | **CLOSED** | `b-baseline.mjs:79-84` envelope validated during index construction. |
| Codex B3 / panel §2.1-2.2 driver recovery | **MISS on one leg — a handoff claim is FALSE** | Driver nonce `b-delta-cycle.mjs:48`, chain print `:50`, recovery commands `:69-70`, exits 8/9 `:56,:76` all real. **But `b1-expected-labels.mjs:174` still `process.exit(3)` on the A8 collision.** The handoff says "8 = A8 hazard [remapped from 3 in B1/B3/B4]" and change-log row 1446 claims it landed in B1's file — B3:280 and B4:154 exit 8; B1 was missed. Driver `:65` passes B1's 3 through, colliding with its documented exit-3 "B3 skipped students — resume when quiet" semantics: the r63-panel exit-collision defect survives via B1 (bites on the runbook's standalone `B1 --full` first run over a collision cohort, and on a collision arising between a lap's B4 and B1 steps). |
| Panel N2/N3 custody | **CLOSED + lap-executed** | `14_:109-116`, `15_` §7, guard `b3:120-127`; lap asserts FATAL under `enabled:true` AND under `enabled:false`+`firstEnabledAt` (both observed). Ratification read = section (4). |
| Panel N1 postFlip false-red | **CLOSED in code; the carded lap case is MISSING** | `b4-verify.mjs:190,197` adjudicated-word diffs informational (census `b1-replay-lib.mjs:47`, capped 500); `:191-192` re-enrolled skip; fixture stage 9c. The lap's postFlip case contains NO adjudication and NO re-enrollment — despite the card's "one --postFlip lap incl. the post-flip-adjudication case" added this same round. Zero execution evidence for either informational path. |
| Panel N4 ignoreLedger | **CLOSED in code, never executed** | Stamped `b4-verify.mjs:269`; refused `b3:91`. No lap case, no fixture. |
| Panel N5 / N6 | **CLOSED** | N5 = A4 row above. N6: `.sh` gone from 02_/14_ (grep). |
| Shadowlaw N3-N6 | Text-folded into `02_:95` (evaluator duties verbatim: window artifact, missing/invalid/≠-generation quarantine, published `quarantinedRowCount`, all consumers) — substance deferred to the shadowlaw-lens reviewer. |

## Lap audit — case by case against the 02_ card

The card's gate sentence (02_:95): required cases (a)-(e), the FOUR crash points "(before first write / after a committed chunk / after final commit / during completion publication — incl. the first-append window)", one `--postFlip` lap "incl. the post-flip-adjudication case", one resume lap ("clean completion supersedes"), the A3 stale-report negative.

- **Check accounting:** the harness contains exactly 41 `ok()` executions when every branch runs (3+2+2+2+3+6+2+4+6+6+5) — consistent with the artifact and with my observed transcript. The artifact's 41 is arithmetically honest.
- **(a) zero-diff lap — FAITHFUL.** Hard-asserts exit 6, driver 0. PASS is value-defined, so the driver's 0 transitively proves B3 wrote what B1 predicted (a missing-label bug would surface as exit 5 next cycle). Boundary honestly stated: B1/B3/B4 all derive expectations from the same `computeStudentLabels`; a shared replay-law bug yields consistent-wrong-everywhere PASS. The lap proves CLI-glue composition, not label-law truth — that division of labor matches the card's intent ("the stratum no fixture covers"), but no case anywhere does a direct Firestore read-back of a single label value.
- **(b) roster-added — FAITHFUL.** Hard exit 7, driver 0, chain passed from the ledger.
- **(c) adjudication — WEAKER THAN CARDED.** The card says "WITH diffs" (= exit 7). The harness accepts `6 || 7` (`b-emulator-lap.mjs:131`). A B4 regression that wrongly exempts the adjudicated fc/lf reversal would exit 6 and the lap stays green — the tolerance masks exactly the "adjudication produces diffs" half of the case. My transcript shows 7 actually occurred, but the aggregate-only artifact could never show that, and the assertion doesn't require it.
- **(d) structural — PROVES LESS THAN CLAIMED, in two ways.** (i) The handoff calls it "mixed structural diff"; as executed there is NO concurrent actionable delta (no new attempt is seeded beside the corruption) — B4's verdict at that moment is plain `DIFFS` → 5. The genuinely adversarial carded scenario — corruption AND a pending delta simultaneously, where B4 must emit 7, the driver laps, and the corruption must SURVIVE to a final exit-5 (or, if the corrupt doc belongs to the delta student, B3's convergent write silently repairs it and PASS swallows the "stop and investigate" posture entirely) — never executes. (ii) "driver STOPS" is proven only by exit code: nothing asserts the ledger gained no EXECUTE during the stop. The driver source (`:56`) makes the stop obvious, so (ii) is minor; (i) is a real scope miss against the card's own word "mixed".
- **(e) all-departed — FAITHFUL with nits.** Empty excused layer, B3 no-op, `departedSkipped: 1` all executed. Nits: the case's own baseline `b1full(); b3exec("lapE0")` codes are unasserted (`:156`); B3's "no-op" isn't pinned to `"docsWritten": 0`; the pre-delta B4 accepts `6 || 7`.
- **Crash injection — hooks sound and inert; the lap cannot distinguish three of the four states.** Hooks: `b3:57-58` env-gated (`B3_CRASH_AT`, inert unset), reachable only inside the EXECUTE path (`:354,:357,:381,:409`), loud `[TEST]` banner, exit 99, always between durable operations — fail-closed even if accidentally set in production (a killed run resumes). Placement verified correct in source (`after-first-chunk` after the awaited `runTransaction`, `:378-381`). **But the lap's assertions are exit-code-only:** post-intent, after-first-chunk, and pre-complete produce IDENTICAL observable assertion patterns (99 → B4-FATAL → resume-0 → PASS; pre-complete skips even the B4-FATAL probe). No case reads Firestore or the journal to verify the crash STATE (post-intent ⇒ zero study_states docs; after-first-chunk ⇒ exactly student 1's docs; pre-complete ⇒ all docs). A refactor that moves the after-first-chunk hook above the commit would keep the lap 41/0 while the partial-write recovery case silently stops existing. The "four points executed" evidence is really "pre-intent (distinguished by ledger absence) + a crash somewhere in phase 2, three times."
- **Card-vs-harness crash mapping — two carded points missing.** Card: before-first-write ✓ (post-intent), after-committed-chunk ✓, after-final-commit ✓ (pre-complete), **"during completion publication" ✗** (a torn/partial completion append — the strict parser's malformed-line FATAL, `b4:72`, fires nowhere in any execution; a 5-line case corrupting the ledger file would cover it), and **"incl. the first-append window" ✗** — the handoff maps it to pre-intent, which is wrong: pre-intent is crash-before-append (no file semantics tested); the first-append hazard is intent appended + fd-fsynced but the FILE CREATION lost (no dir fsync, panel r63 §2.3) — simulated by deleting the ledger after a post-intent crash and asserting B4's behavior. Never run.
- **postFlip lap — executes the per-field law and BOTH guard windows for real; the "live writer" is the harness's own hand.** `b-emulator-lap.mjs:217-218` writes `fc:2, lf, rlt` directly — the correct through-cutoff value computed by the author. So the case proves: real B4 exempts lf/rlt per-field (`liveExemptFields>0` observed) and the fc through-cutoff branch accepts a CORRECT value. It does not beg the question entirely (a wrong hand-written fc would have failed), but it executes only the green side — no in-lap deficit negative (disk fc one short + fresh lf ⇒ must DIFF), and its fidelity to the eventual live writer is exactly the fidelity of the author's hand-instantiation of 15_ §1; the real closure is the dark-build writer against B4 in the stage-2 emulator matrix. The carded post-flip-adjudication and re-enrollment informational cases are absent. The durable-marker guard pair (`enabled:true` refused; `enabled:false`+`firstEnabledAt` refused) is the lap's strongest new execution evidence.
- **Resume lap** — benign supersede direction only (see A3 row). **Stale-report negative** — well-constructed and the round's best case: report↔invocation equality (`[]≡[]`) deliberately PASSES and the LEDGER reality audit catches it (exit 2 + `A3-reality` observed); "pre-write" is argued from code placement (`b3:162-173` before any plan/write), not from a state snapshot — acceptable.
- **Never executed anywhere in-repo:** B4's missing-layer FATAL (`b4:94-95`), malformed/unknown-probe/version/outcome ledger FATALs, `--ignoreLedger` refusal, positive-path `--repairExtras`, driver exits 3/4/9, B1's departed-note path aside from case (e).

## (2) NEW defects

**NEW-1 (HIGH, demonstrated live this round): the harness operates on the REAL artifact tree, wipes it, and unconditionally rewrites the tracked evidence — no lock, no sandbox, no provenance.** `b-emulator-lap.mjs:40-53`: the "scratch" dir holds only `allowlist.json`; the ledger (`audit/deepfix/trackB_baselines/applied-layers.jsonl`), `b3-runs/`, `b4-runs/`, and every `b1-*` file are the REAL paths — necessarily, because B3/B4 hardcode them via `import.meta.url` (`b3:199,263`; `b4:57,254`; only B1 has `--outDir`, `b1:134`). The header's "redirected to a scratch sandbox via cwd-relative paths" is FALSE twice over (not redirected; the CLI paths aren't cwd-relative). Failure scenarios: (i) OBSERVED — the standard 3-reviewer protocol re-ran the lap concurrently; mutual `wipeArtifacts()` produced bogus 41/2 and 41/9 results and destroyed the packet's untracked 41/0 evidence artifact twice (recovered only from my pre-emptive backup, now restored); (ii) FUTURE ops box — after the real 26SM run exists, one lap invocation deletes the real applied-layers ledger (the sole custody chain), every real `b1-*` baseline (`b1-manifest-full.json` collides by name), and the `b3-runs` pre-image backups (the restore path). The real `26sm-*.json` files survive today by prefix luck only. Also: every lap B1 run clobbers `docs/plans/deepfix2/evidence/b1-baseline-pointer-full.json` with unmarked emulator provenance (`b1:210` writes unconditionally — the tracked evidence dir currently presents a 3-student emulator run as a `full`-mode baseline pointer). Fix: artifact-root env override in B3/B4 + true sandboxing; a `wx` lockfile (the pattern already exists at `b3:243`); refuse to wipe when non-lap artifacts are present; evidence write only behind an explicit flag; stamp emulator provenance into everything the lap emits.

**NEW-2 (MEDIUM-HIGH): plain-mode B3 has no ledger guard — the M0-revert regression writer survives one mode over from Codex A2.** The reality audit `b3:162-173` runs only `if (extrasRepair)`. After layers M1..Mn are EXECUTE'd, a plain `--execute` with a fresh runId (no `--deltaDir`, no `--appliedDelta` — the flags you get by re-running shell history) loads nothing but M0 (`b3:75`), resolves every student against it, and writes reverts of layer-correct labels; exit 0 with a clean completion; only the next B4 catches it (Codex's A2 wording verbatim: "the writer has already accepted and executed authority from the wrong expected state"). The harness knows: case (d)'s comment calls a delta-less re-run "illegal" and hand-reverts the corruption instead of running one. Fix is the existing loop made unconditional: ledgered EXECUTE'd layers for this original ⇒ plain mode FATAL.

**NEW-3 (MEDIUM): `b1-expected-labels.mjs:174` still exits 3 on A8** — falsifies the handoff's "remapped from 3 in B1/B3/B4" and change-log row 1446; driver passthrough (`b-delta-cycle.mjs:65`) re-creates the r63-panel exit-collision (3 = "resume when quiet" advice against a structural data hazard). One-line fix.

**NEW-4 (MEDIUM, evidence-strength): crash-state assertions are exit-code-only** (detail above) — the lap pins hook NAMES, not crash STATES; add per-state Firestore/journal read-backs and gate `B3_CRASH_AT` on `FIRESTORE_EMULATOR_HOST` being set (the handoff invited this hardening; take it).

**NEW-5 (LOW-MED): the evidence artifact is an unauditable aggregate.** Six lines: no per-case exits, no environment, no commit/hash binding. Bring-up edits to the harness are admitted in the handoff, so nothing ties 41/0 to the current harness bytes; tolerant assertions (6||7) make "exit 7 → PASS" in the handoff unverifiable from the artifact. My solo transcript closes this for r64; the harness should emit per-check records + env + a source hash.

**NEW-6 (LOW): rehearsal-vs-era-boundary drafting gap inside the new custody law** — see (4).

## (3) STAGE-1 FREEZE: **NO**

The chain itself held up under my independent execution — 41/0 reproduced, and every red seen this round was harness-custody interference, which is precisely the point: **the gate artifact is currently a footgun that corrupted its own evidence during its own review.** Grounds for NO, all bounded: (i) the r64-expanded card's REQUIRED CASES are not all executed — mixed-(d), the post-flip-adjudication lap case, the first-append window, torn-completion — so "the card's lap-gate is satisfied" is false by the card's own letter (the round that expanded the card must satisfy the expansion or re-scope it honestly); (ii) two handoff claims are falsified in source (B1 exit-3, `NEW-3`; the sandbox-redirect header claim, `NEW-1`); (iii) NEW-1 and NEW-2 are real hazards on the exact machine and mode the stage-2 runbook will use. **Minimal falsifiable closure set:** (1) harness custody: sandbox/lock/provenance + wipe-guard (NEW-1); (2) unconditional plain-mode ledger guard + a lap or fixture case for it (NEW-2); (3) `b1:174` → exit 8 (NEW-3); (4) add the missing carded cases — mixed-(d) with a concurrent delta, postFlip adjudication + re-enrollment, first-append deletion, torn completion append, clean-then-crashed-resume — and pin case (c) to exit 7 with per-state crash read-backs (NEW-4); (5) per-check evidence records (NEW-5). Nothing here reopens the r63 closure set; this is days, not a redesign. On YES-after-closure I have no objection to the proposed stage-2 opening order.

## (4) Dark-window custody clause: **faithful scope refinement — does not require David now, with one drafting fix and one condition**

R2-32's ratified context was the KILL SWITCH (r46-Q4: revert after launch). Read literally into the dark window, it collides with two equally ratified decisions — R2-31's choreography (dark-deploy → verify → backfill → one flip) and R2-9's verify-against-expected pipeline — because dark-window live stamping makes B3/B4's expected-vs-actual unverifiable and forces B3 to overwrite live truth (panel N2's exact mechanism). Some interpretation was mandatory; `firstEnabledAt` (15_ §7, 14_:109-116) is the minimal one that leaves every post-activation behavior David decided byte-identical: after the first flip the marker is permanently set, so kill-switch OFF windows stamp fail+correct exactly per R2-32's letter, proven freezes, and B3 stays locked out (lap-executed both ways). Student-visible delta: zero. That is a scope clarification of an ambiguity between David's own rows, not a new decision — Codex ratification plus a surfacing line on the stage-2/dark-build authorization David already gates is the right governance weight; a dedicated David round-trip now would gate law on a question whose only coherent answer is already written. **Two caveats:** (a) drafting gap (NEW-6) — 15_ §7's `rehearsalClassIds` makes sandbox classes gate-ON "even while globally dark"; if gate-ON includes label stamping (it must, or stage-3 cannot rehearse stamping), the clause's "the dark window has ZERO live label writers" is violated in letter — benign only because 25WT is outside the 26SM verify scope, which is exactly why the era-boundary sentence needs the explicit carve-out ("…or the class is in rehearsalClassIds; rehearsal classes never enter B3/B4 scope"); (b) the clause's enforcement on the LIVE-writer side is pure stage-2 build contract — only the B3-side guard exists in code today; the writers' `firstEnabledAt` gate must appear in the DF2-10 workpackage text before the dark build, or the clause is half-implemented law.

---

## LENS: lawclose

All verification complete. The 03:17 artifact (before my restore) even showed the orchestrator's in-flight r65 harness already adds script fingerprints and an `isolatedRoot` — independently corroborating my harness-isolation finding. Compiling the verdict.

# Panel review r64 — Lens: the r63-residue law closures (fc/repair/ledger/custody/driver)

**Reviewer basis (all measured on the r64 handoff surface — the working tree over `2f80e6d` as read 02:40–02:58Z; file:line cites refer to that tree):** read every script + 02_/14_/15_/16_/11_ + the r63 review/panel; ran `delta-chain-fixture.mjs` = **55/0** (matches), `rotation-cyclicity-fixture.mjs` = **2,688/0** (matches); `node --check` clean ×11. **I re-executed the emulator lap** (JRE + firebase-tools + cached emulator jar were present): my first run measured **41/9**, forensically traced to a **concurrent sibling-reviewer lap sharing the canonical artifact tree** (receipt: my case-c FATAL names `srLap-msb7edc8-c1`, whose nonce decodes to 02:51:11Z — mid-my-run; a sibling's strictly-solo run then passed **41/0**, green on every case mine failed). My second attempt was invalidated by **live r65 code edits landing mid-panel** (all chain scripts mtime 03:02–03:08Z; a new 48-check harness vs half-edited B3/B4 → meaningless 48/21). Union across the panel window: **every one of the 41 checks passed in at least one execution of the r64-era code.** The 41/0 evidence artifact is corroborated: my static count of the harness's `ok()` sites is exactly 41, the harness spawns the REAL CLIs against the emulator, and the enumerated cases are all genuinely executed.

## (1) r63 findings — CLOSED / MISS

| Finding | Verdict | Evidence |
|---|---|---|
| **Codex A1** fc cumulative | **CLOSED** | `b-baseline.mjs:31` fc returns false unconditionally from `isFieldLiveExempt`; fixture `:356-357` asserts both the unrelated-stamp AND the fresh-lf negatives. B4 `--postFlip`: cutoff captured once (`b4-verify.mjs:38`), per-student through-cutoff replay (`:183`), **fc's expected is REPLACED by the cutoff value before comparison** (`:201`) — the coincidental flip-boundary match never enters the fc comparison — then one fresh-replay+re-read retry (`:207-213`). Fixture stage 9b (`delta-chain-fixture.mjs:324-339`) reproduces Codex's exact 3-step sequence with the real replay lib: flip=2≡disk 2 (documented false green), cutoff 3≠2 caught. **Concurrent attempts:** every residual race (attempt landing between retry-replay and re-read; attempt-txn straddle) degrades to false-RED, never green. **Blank rows:** replay counts fc from `answers` rows only, so exactness holds ONLY under 15_ §1's COMPLETE-ROWS law (blank = explicit row) — that law is now frozen in the schema row the attempt writer builds from; a writer violating it produces a loud diff (disk > replay), fail-closed. The ±2pp score↔rows fence stays consistent because every score-mutating surface preserves agreement by law (challenge accept fixes score+row together; teacher edits replay `preOverride.score`). Residue (notes): `cw ? cw.fc : exp` falls back silently to the flip value if the key vanishes from the cutoff replay (constructible only via a forged duplicate-signature attempt); the CATCH direction is fixture-arithmetic only — no lap case plants an fc deficit for `--postFlip` to catch. |
| **Codex A2** repair resolution | **CLOSED** | Modes exclusive (`b3-backfill-writer.mjs:76-77`); repair chain loads, order-checks, sha-equals the report, then **`deltaLayers = chainLayers` (`:158`)**. I traced every consumer after that assignment: scope check is chain-aware (`:178-186`), the delta-scope narrowing is gated on `args.deltaDir` (`:188` — repair can never collapse to a first layer), phase-1 resolves every uid via `resolveExpectedSource(uid, original, deltaLayers)` (`:278`), and the run manifest merely records (`:336`). **No path resolves against M0 when a chain is supplied**; Codex's U-regression is dead (U resolves to M1, plans a no-op). Empty-chain repair with roster churn FATALs (fail-closed). PLUS the reality audit (`:162-173`): a report whose `appliedDeltas` misses any LEDGERED EXECUTE for this original is refused pre-write — **executed in the lap** (stale-report negative, `A3-reality`, green in all three runs). Residue → new defect N5 below. |
| **Codex A3** ledger pairing / absence | **CLOSED** | Keys `${runId}#${attempt}` (`b4-verify.mjs:77`), integer-attempt + version + object-outcome strictness (`:74-80`), latest-attempt-per-runId must have a clean completion (`:83-92`) — Codex's exact scenario (clean attempt 0 + crashed resume-1 intent) now FATALs; a clean later attempt legitimately supersedes. Absent ledger + `--appliedDelta` ⇒ FATAL (`:60-61`); `--ignoreLedger` stamped in reports (`:269`) and refused by B3 (`b3:91`). Lap-executed: dangling-intent FATAL + resume-heals ×3 crash points. Residue: the specific (runId,attempt) key case is not executably tested in-repo (the lap's crashes all dangle attempt 0); attempt-number reuse requires deleting persistent lease files (forensic-only). |
| **Codex A4 + panel N5** resume custody | **CLOSED** | `wx` lease = exclusive attempt reservation (`b3:243-247`, fsync'd, never deleted); per-attempt immutable `resume-N.preimage` (wx stream, `:249`) and **`resume-N.plans.jsonl`** (`:252`) — the shared-plans overwrite is dead; each resume's result records its own plansFile+hash (`:335-341,401-406`). Residue → N6 (fresh runs have no lease). |
| **Codex A5** governance / lap-before-freeze | **CLOSED** | The lap **ran** (41/0, see basis) — the card's own "must RUN before any freeze claim cites it" is satisfied without any gate-order amendment. 02_ DF2-14 and 14_ §4 now name `b-delta-cycle.mjs` (zero `.sh` hits in either) and the card enumerates the FULL required scope ((a)–(e), 4 crash points, postFlip, resume, stale-report). 14_'s post-flip prose = the per-field/cutoff law verbatim. Caveat → N1 (the 14_/driver exit table is wrong for one path). |
| **Codex B1** knownUids | **CLOSED** | Union adds every layer's `auth.uids` (`b4:122-126`); lap case (e) executed: empty excused layer → B4 PASS with `departedSkipped: 1`. |
| **Codex B2** eager index | **CLOSED** | Envelope validated for every row during index construction, transient parse (`b-baseline.mjs:79-84`) + dup-uid check. Standing residue (unchanged from r63's letter): inner word-payload values are not schema-checked — B3 trusts B1's inner values hash-blind (single point of truth = B1). |
| **Codex B3** driver recovery | **PARTIAL — one MISS** | Nonce runIds (`b-delta-cycle.mjs:48`) kill the re-invocation trap; APPLIED CHAIN printed at every stop (`:50,69-71`) with exact resume commands; exits 9 (exhausted) and 8 (A8, with a stop-and-investigate message, `:56`) are disjoint in B3/B4 (`b3:280`, `b4:154`). **MISS: `b1-expected-labels.mjs:174` still exits 3 on the A8 collision** → new defect N1. |
| **Panel N1** postFlip false-red | **CLOSED** | Word-level `adjudicatedRecentWordIds` census in the replay lib (`b1-replay-lib.mjs:43-48`, fixture stage 9c) consumed by the verdict (`b4:190,197`); post-flip re-enrolled students informational (`b4:191-192`, counted). Note: census caps at 500 words — pathological false-red beyond. |
| **Panel N2/N3** dark-window custody | **CLOSED in 14_/15_/code; ledger + build-card fold GAP** | 14_:109-116 + 15_ §7 (`firstEnabledAt` written in the first-flip txn, never cleared); B3's guard checks the durable marker (`b3:122-127`) — **lap-proven in a kill-switch `enabled:false` window** (green in all three runs). Gap → N3. My custody ruling in §4. |
| **Panel N4/N6** | **CLOSED** | ignoreLedger stamped+refused (above); `.sh` purged (grep = 0 in 02_ and 14_). |
| **Shadowlaw N3–N6** | **Present as claimed** (presence + coherence spot-check; depth = the shadow lens) | 16_:89 window artifact + missing/invalid/future⇒quarantine + published `quarantinedRowCount` + all-consumers rule; partition-vs-mirror resolved (mirrors 1:1, partitions driver-side); fidelity gate rewritten — the pinned attempts allowlist cross-checks **exactly** against every field `b1-replay-lib.mjs` reads (verified field-by-field); `--uids` deleted from B1's arg set (`b1:54`). Residue → N8. |
| **Protocol residue** | **2 of 3 delivered** | Sanity caps ✓ (`b-baseline.mjs:36`, fixture-asserted); `B4 v4` line ✓ (`b4:128`). **The claimed "ledger dir-fsync boundary honestly noted in B3" does not exist** — grep finds no such note, and `b3:261-262` actually overclaims ("a host crash cannot leave applied writes without ledger evidence") when the first-append dir-entry window is precisely the case it can → N7. |

## (2) NEW defects

**N1 — MEDIUM (code): the A8 exit remap missed B1 — the r63-panel exit-3 trap survives on the B1 leg.** `b1-expected-labels.mjs:174` exits **3** on the cross-list wordId collision; the driver relays B1 failures verbatim (`b-delta-cycle.mjs:65`), and both the driver contract (`:13-14`) and 14_:95 define exit 3 = "B3 skipped students — rerun with `--resume` when quiet." Scenario: a collision is born between B4's clean pass and B1 `--deltaAuth` (a new attempt mints the divergent second list-key inside the lap) → B1 FATALs 3 → driver 3 → stage-2 automation/operator follows the frozen advice and reruns forever against a structural data hazard. The handoff's "remapped from 3 in B1/B3/B4" and the change-log row are inaccurate for B1. Two-line fix (B1 → exit 8, or driver-side remap of the B1 leg).

**N2 — MEDIUM (harness/ops, EXECUTION-PROVEN): the lap harness is not isolated and has no run exclusivity.** Its header claims artifacts are "redirected to a scratch sandbox"; in fact it runs in and destructively wipes the **canonical** `audit/deepfix/trackB_baselines/` tree — the applied-layers ledger, `b3-runs/` (pre-image rollback backups), and every `b1-*` baseline (`b-emulator-lap.mjs:45-54`) — and B1's evidence-pointer write escapes into tracked `docs/plans/deepfix2/evidence/` with emulator data (`b1-expected-labels.mjs:210`; the checked-in pointer files contain emA/emB/emC). Proven consequence: two panel laps running concurrently cross-contaminated through the shared tree (my 41/9 with a foreign `srLap` ledger record vs the same code solo = 41/0). A stage-2 rerun after any REAL EXECUTE would delete the mandatory ledger and the only rollback pre-images — fail-loud afterward (absent-ledger law), but irreversible evidence destruction. Fix: an env-overridable artifact root + refuse to wipe when the ledger contains non-emulator records + a lock file. *(The 03:17Z r65 in-flight artifact already shows an `isolatedRoot` — the fix is being built; it must also ride the freeze packet.)*

**N3 — MEDIUM (doc-law): the custody refinement is folded everywhere EXCEPT the binding record and the build card.** 11_ §1 is the binding ledger with the rule "inline ⟶ markers govern"; its R2-32 row carries **no marker** (grep: zero `firstEnabledAt` in 11_), so the governing record still commands the unscoped "while OFF — fail stamps keep writing" letter that 14_/15_ now forbid in the dark window — the contradiction this round resolves is still frozen in the top-of-hierarchy doc. And 02_ has **zero** `firstEnabledAt` mentions: the deploy choreography line still reads "ONE audited cohort-wide flip of `system_config/review_v2.enabled`" with no marker-write duty — a builder working from the card builds a flip that never sets the marker (see N4 for the blast radius). Fix: one ⟶ annotation on R2-32 (citing the ratifying round) + one clause in 02_'s deploy line.

**N4 — LOW/MEDIUM (doc): 15_ §7 contradicts itself two sentences apart, and the losing reading is catastrophic-silent.** "David's fireadmin write flips `enabled` ONLY" vs "`firstEnabledAt` … written in the SAME audited txn as the first `enabled:true`." A flip script implementing the first sentence never sets the marker → live label writers (now gated on the marker) **never stamp** → the entire label system is silently dead post-flip while B3 is also forbidden. One-sentence fix ("flips `enabled` + stamps `firstEnabledAt` in one txn, first flip only").

**N5 — LOW (code): B3 repair-mode's reality audit is fail-open on an absent ledger** (`b3:165` `existsSync` gate silently skips), contradicting the handoff's letter "an ABSENT ledger is FATAL whenever `--appliedDelta` is given" (true only in B4, `b4:60`). Exploit needs ledger deletion after report production (same-checkout tampering), so narrow — but the symmetry is one `else FATAL` away.

**N6 — LOW (code): fresh (non-resume) runs have no lease.** Two concurrent same-runId fresh EXECUTEs pass the `existsSync` single-use check together, interleave the same `.preimage.jsonl.tmp`/`.plans.jsonl.tmp`, and double-append attempt-0 ledger records. State stays safe (txn re-diff); evidence hashes become unverifiable. Driver nonces make this manual-invocation-only.

**N7 — LOW (packet accuracy ×2):** (i) the claimed dir-fsync honesty note in B3 does not exist, and the existing comment overclaims durability (`b3:261-262`); (ii) the change-log/handoff "A8 exits 3→8" claim is false for B1 (N1).

**N8 — LOW (doc residue): 16_ §3:120's PRE-RUN GATE still asserts each shadow class's `studentIds` "equals its partition"** — the exact equality §2.11's r64 resolution (studentIds = reduced ∩ mirror membership; partitions are driver-side lists) makes false. The gate as written would fail on every legitimately-configured class. One sentence lag from the same fold.

**N9 — PROCESS (flag to the orchestrator): the review surface mutated DURING the live panel.** Every chain script was edited 03:02–03:08Z ("r65 [Codex r64 A1]" folds) while this 3-Fable panel was still measuring — invalidating one of my lap executions outright and tainting any sibling measurement after 03:02. On a shared checkout, folds must wait for panel completion (turn separation), or reviewers get a moving target and evidence artifacts get overwritten mid-review (the evidence file changed hands four times in 40 minutes: 41/0 → my 41/9 → sibling 41/0 → r65 48/22 → my restore of the handoff original).

Notes: `fcRetriedClean` re-replays the full student per mismatched word (cost only); stale `--uids` comment survives at `b1:67`; the r64 evidence artifact was count-only (no case list/fingerprint — already being enriched in r65).

## (3) STAGE-1 FREEZE: **YES — conditional on a micro-fold riding the freeze packet**

The r63 blockers are genuinely closed **with execution evidence**: the lap ran (41/0, corroborated by artifact + exact static check-count match + a solo panel-window re-execution + union-of-green across three runs), the card's own lap-before-freeze law is satisfied, both fixtures match the handoff, and I could not construct a false-PASS or a brick anywhere in my lens — every residual I found degrades to a loud red or is evidence-custody, never green. What must ride the freeze (all bounded, none reopens law substance): **(a)** N1 — B1's A8 exit 3→8 + the 14_ exit-table note (freezing a wrong exit contract is freezing a defect); **(b)** N3 — the 11_ R2-32 ⟶ annotation + the 02_ flip-card `firstEnabledAt` duty (the binding record must not contradict the frozen law); **(c)** N4 — the 15_ §7 one-sentence flip-txn fix (silent-death reading); **(d)** N2 — the harness isolation/exclusivity guard (in flight per the r65 artifact; must land before the lap is a standing stage-2 gate). If the program treats these as unable to ride the freeze: **NO** with exactly that four-item set and nothing larger.

## (4) Dark-window custody ruling (14_ §4 + 15_ §7 `firstEnabledAt`)

**Faithful scope refinement — Codex ratification suffices; it does not need David as a blocking gate.** Grounds: (i) R2-32's ratified context was the kill switch (r46-Q4); the dark window did not exist as a concept until R2-31 landed in the same round — the clause fills genuine silence rather than reversing an answered question; (ii) the literal dark-window reading is incompatible with the ratified pipeline itself — concurrent dark writers would make R2-9's mandated expected-vs-actual verification unachievable and would have B3 force-overwriting live stamps (the panel-N2 proof), so ONE of the two ratified clauses must yield, and this refinement is the unique reading keeping both operable in their own eras; (iii) zero post-activation behavior change — every window R2-32 now governs behaves exactly as David ratified, and the inert-then-truthful property he certified still holds; (iv) zero student-visible effect (the labels have no readers while OFF — R2-32's own confirmed premise). The R2-24 precedent ("a David-locked decision reverses only by an equally explicit statement") is the reason this must be **logged, not just prose**: the ratification lands as the 11_ R2-32 ⟶ marker (N3) plus a one-line FYI in the next David presentation. Ratify, annotate, notify — do not block on him.

---

## LENS: custody

All verification complete. Composing the verdict.

# Panel review r64 — lens: dark-window custody + shadow closures

**Basis:** the r64 tree (handoff mtime 02:40Z; code mtimes ≤ 02:39Z). Independently measured: `delta-chain-fixture.mjs` **55/0** (matches), `rotation-cyclicity-fixture.mjs` **2,688/0** (matches), `node --check` clean on all 11 scripts. **Emulator lap: I re-ran the real runbook command.** My first run returned **41 checks / 2 failures** — forensically traced to a *concurrent sibling panelist's lap* sharing the same artifact paths (their run returned 41/9 for the mirror-image reason, and overwrote `evidence/emulator-lap-result.json` with `failures: 9` mid-panel). A subsequent **exclusive** run (03:07Z, full transcript audited: 41 green, 0 red, all 11 case groups present) reproduced **41/0**. The headline execution claim is GENUINE; the harness that produces it is not safe (finding N1). **Caveat:** an r65 fold began landing at 03:02Z during this review (Codex r64 already filed: freeze NO, clause NOT RATIFIED); all file:line refs below are to the r64 versions I reviewed.

## (1) r63 findings — CLOSED / MISS

| Item | Verdict | Evidence |
|---|---|---|
| Codex A1 fc cumulative | **CLOSED (flip-boundary words)** | `b-baseline.mjs:29-31` fc never timestamp-exempt; `b4-verify.mjs:182-183,199-213` through-cutoff second replay + one re-read retry; fixture stage 9b (`delta-chain-fixture.mjs:324-338`) is Codex's exact 3-step counterexample — coincidental flip-boundary match documented, through-cutoff catches 2≠3; `15_` §1 COMPLETE-ROWS row present. Residues: post-boundary-created words outside the comparison universe (Codex r64 A4 reproduced it), and the tail orphan (my N5 below). |
| Codex A2 repair resolution | **CLOSED** | `b3-backfill-writer.mjs:76-77` modes exclusive; `:146-158` loaded chain ≡ report.appliedDeltas AND `deltaLayers = chainLayers` (the resolver); `:159-173` A3-reality ledger audit (pre-write refusal — executed in the lap, `A3-reality` confirmed in my runs); `:188-196` delta-scope filter gated to delta mode. Residue: the usage header (`:31-33`) still omits `--appliedDelta` — the exact wart Codex named in r63 A2 step 3. |
| Codex A3 ledger pairing/absence | **CLOSED** | `b4-verify.mjs:60-62` absent ledger FATAL when layers claimed; `:74-75` version + integer attempt strict; `:77-91` `(runId,attempt)` keys, latest-attempt-must-be-clean; `:80` outcome required. |
| Codex A4 resume custody | **CLOSED** | `b3:243-247` `wx` lease = exclusive attempt reservation; `:248-253` per-attempt immutable preimage + `resume-N.plans.jsonl`; `:404` per-attempt result. |
| Codex A5 lap-before-freeze | **EXECUTED — but PARTIAL vs its own card** | The lap ran and reproduces 41/0 solo (verified above); 02_/14_ repaired to `b-delta-cycle.mjs` + the expanded scope. MISS inside the scope: the card requires "one --postFlip lap **incl. the post-flip-adjudication case**" — `b-emulator-lap.mjs:206-234` contains no adjudication sub-case (convergent with Codex r64 A5). The gate-order dispute itself is dead — the lap preceded this freeze claim. |
| Codex B1 knownUids union | **CLOSED** | `b4:122-127` union includes every layer's `auth.uids`; lap case (e) `departedSkipped: 1` executed (confirmed in my transcripts). |
| Codex B2 eager index validation | **CLOSED** | `b-baseline.mjs:79-84` `validateRowShape` during index construction. (Inner epochByList/word-payload values still container-checked only — r63's stated nit stands.) |
| Codex B3 driver recovery | **CLOSED with one MISS** | Nonce runIds (`b-delta-cycle.mjs:48,68`), exact recovery commands + APPLIED CHAIN (`:50,69-70`), exit 9 exhausted (`:76`), B3/B4 A8 → exit 8 (`b3:280`, `b4:154`). **MISS: `b1-expected-labels.mjs:174` still exits 3 on A8**; the driver passes B1 codes through (`:65`) and documents driver-exit 3 as "B3 skipped students — rerun with --resume when quiet" (`:13-14`) — the r63-panel §2.1 hazard survives on the B1 leg, and the handoff's "remapped from 3 in B1/B3/B4" is false for B1. |
| Panel N1 postFlip false-red | **FIXED AS ASKED — but the mechanism over-reaches into false-GREENS** | `b4:190,197` word census consulted; `b4:191-192` re-enrolled informational. The census skip is whole-word (`continue` before any field compare) — a corrupt/stale unrelated field on an adjudicated word rides through; and `!src.row` treats a pre-flip-late joiner (post-last-layer, pre-flip, with history) as advisory too. I flagged both shapes; Codex r64 reproduced both as PASS-when-DIFFS on the emulator. Net: the false-red died, two false-greens were born. |
| Panel N2/N3 dark-window custody | **MECHANISM CLOSED; LAW NOT FREEZABLE** | B3 guard reads the durable marker (`b3:120-127`: FATAL on `enabled===true \|\| firstEnabledAt`), lap-proven including the kill-switch `enabled:false` window (both my runs). The clause set around it is incomplete/contradictory — findings N2-N4 below; ratification NO (§4). |
| Panel N4 ignoreLedger | **CLOSED** | Stamped `b4:269`; refused `b3:91`. |
| Panel N6 `.sh` refs | **CLOSED** | Repo grep: only the historical comment `b-delta-cycle.mjs:1`. |
| Shadowlaw N3 quarantine | **CLOSED as law** | 16_ §2.11: `shadow_registry/window` artifact defines the window + registered generation; missing/non-integer/</> all quarantine (deploy-skew hole closed); `quarantinedRowCount` published + alertable; rule binds BOTH evaluators AND §2.5 baselines. Residue below (N6). |
| Shadowlaw N4-doc evaluator fold | **CLOSED** | 02_ DF2-14 "THE EVALUATOR API [duties EXPANDED r64 = 16_'s law verbatim]" — I diffed the duty text against 16_: writer stamping, window artifact, all-consumers quarantine, published count, scope predicates — equivalent (02_'s "≠ the window generation" ≡ 16_'s "< OR >"). |
| Shadowlaw N5 fidelity gate | **CLOSED — executable now** | 16_ §3 rewritten: per-uid `words`/`digest`/`epochByList` under the uid map; `challengeDigest`/`challengedAttemptIds` EXCLUDED (no id-bearing comparison remains — I checked `digest` hashes only `wordsOut`, id-free `listId\|wordId` keys, `b1-replay-lib.mjs:140-145`); real side filtered to the reduced set; roster-drift joiners LISTED+EXCLUDED with counts; pinned attempts allowlist verified field-by-field ⊇ the replay lib's actual read set (incl. `preOverride.score`, `dayNumber/studyDay`, `answers[challengeStatus/challengeReviewedAt]`; epochs come from cloned `progress_meta`/`list_progress`). One textual residue: §3 still says "every script (B1/B3/B4, **every battery**) consumes it through generated CLASS allowlists," contradicting §2.11's "student-granular batteries consume the partition manifest directly" (Codex r64 convergent; r65 log row claims a one-sentence fix in flight). |
| Shadowlaw N6 partition-vs-mirror | **CLOSED** | 16_ §2.11 [r64]: strict 1:1 mirrors; `studentIds` = reduced ∩ mirror membership; partitions = driver-side lists, never class structure; `scope-<runId>.json` = whole-class scoping only — exactly the resolution r63 recommended. |

## (2) NEW defects

**N1 — [PROVEN LIVE, twice] The lap harness runs in the production artifact namespace with no exclusivity — and destroys the forensic chain it certifies.** `b-emulator-lap.mjs:40-53`: `ledgerPath` is the REAL `audit/deepfix/trackB_baselines/applied-layers.jsonl`; `wipeArtifacts()` deletes it plus `b3-runs/`, `b4-runs/`, and every `b1-*` at every `resetCase()`. The header's "redirected to a scratch sandbox" (lines 11-13) is false. Demonstrated failure scenarios this session: (a) two concurrent laps (different emulator ports, same files) corrupted each other into 41/2 and 41/9 from checks that are all green solo; (b) the un-demonstrated-only-by-luck case: on the ops checkout, one lap invocation permanently erases a real 26SM EXECUTE's mandatory A4 ledger and every b3-run custody artifact — after which a plain B4 (no `--appliedDelta`) runs with **no ledger audit at all** (`b4:60` gates the absence-FATAL on `appliedDelta.length`). The stage-2 opening order runs the lap on the same checkout as the real chain. Fix: an env-selectable artifact root for the entire chain + a hard refusal when the existing ledger contains records for a non-lap baseline + an exclusivity lock. (Codex r64 A5 found the deletion; the live concurrency proof is mine. r65 log rows claim `DEEPFIX_AUDIT_ROOT` + full isolation in flight.)

**N2 — `firstEnabledAt` has no carded writer, and 15_ §7 contradicts itself about who writes it.** `firstEnabledAt` appears **zero** times in 02_TASK_LIST.md; DF2-14's deploy choreography still reads "ONE audited cohort-wide flip of `system_config/review_v2.enabled` (David)", and 15_ §7's own opening sentence still says "David's fireadmin write flips `enabled` ONLY" one line above the same-txn marker clause. Failure scenario: the flip script is built from the card → writes `enabled` only → the marker never exists → live label writers (gated on the marker per 15_ §7) never stamp for the entire launch — zero fail/correct/proven stamps cohort-wide, rotation runs blind on priority, and the postFlip reconciliation is all-red; no flip-choreography assertion checks the marker exists post-flip. Also unpinned: write-iff-absent (a re-enable that rewrites `firstEnabledAt` with a fresh serverTimestamp silently moves the era boundary — "never cleared" does not forbid overwrite). Rules protection for the field itself is fine (clause 3 of `firestore.review_v2.rules` denies all client writes on the doc).

**N3 — The marker law contradicts the rehearsal/shadow design it must coexist with.** 15_ §7: live label writers stamp **ONLY** when `firstEnabledAt` is SET — no rehearsal carve-out — while the same section's `rehearsalClassIds` exists precisely so 25WT/shadow classes "rehearse ON-behavior while globally dark," and 16_ battery E (line 176) asserts "labels-keep-writing law observed" and battery C walks wall→pass→graduation — all PRE-first-flip. As frozen, a faithful dark build either (a) obeys the marker → batteries C/E/G/P observe zero label writes and the shadow audit cannot certify the stamping law it exists to certify, or (b) carves out rehearsal classes → violates 14_ §4's "the dark window has ZERO live label writers — B3 owns the fields exclusively," with battery A's B3-on-shadow exclusivity then resting on unstated sequencing (shadow ids must enter `rehearsalClassIds` only after battery A). One sentence fixes it (stamping predicate = marker set ∨ class ∈ rehearsalClassIds; registration ordering pinned) — but as written the clause set is unimplementable, which alone makes it unfreezable.

**N4 — The pre-flip tail is custody-orphaned and makes the final gate structurally red.** Events between the last applied layer's watermark and the flip get labels from nobody: the dark window has zero live writers, B3 is forbidden from the marker on, and live writers stamp only events ≥ flip. The postFlip B4 recomputes expected at boundary=flip — which INCLUDES the tail — so every tail fail/correct is a permanent non-exempt diff (its timestamps are < flipTs; fc through-cutoff mismatches forever). 14_ §4 itself says "students keep submitting — the tail is expected," so on a live cohort the one published reconciliation verdict is structurally DIFFS with no disposition clause; behaviorally, tail fails never set `lf`, so needsPriority misses them (bounded by flip latency — which nothing bounds in prose). The lap's postFlip case cannot see this: its FLIP (BASE+50d) predates the baseline watermark, so the tail is empty by construction (`b-emulator-lap.mjs:213`). Needs a written disposition (final micro-lap immediately pre-flip + tail-window classification, or accept-and-publish).

**N5 — [PROVEN LIVE] The freeze's headline evidence artifact is identity-less and silently overwritable.** `evidence/emulator-lap-result.json` = `{probe, at, checks, failures}` — no git rev, no script hashes, no case list, no run identity. During this panel's window it read 41/0 (02:38), then **41/9** (02:58, a contaminated sibling run), then 41/0 (03:07). Any freeze text citing the file cites whatever ran last. (r65 log row claims source-bound evidence in flight.)

**N6 — Quarantine law: the window/post-flip overlap is unpinned.** The law quarantines real (`shadow:false`) rows during any open audit window. No clause forbids a window doc existing after the first flip (cleanup is the only deleter, and a crashed teardown leaves it standing) — a stale `shadow_registry/window` during the 7-day soak would quarantine stamp-less REAL rows out of the R2-18 abort evaluation exactly when it matters, with only the quarantine counter as the tell. One sentence (flip choreography asserts no window doc exists; windows may never span the flip) closes it. With that, quarantining real rows during genuine pre-flip windows does NOT conflict with R2-18: pre-flip production signal volume is ~nil (gate dark) and the blindness is published+alertable by design.

**N7 — Accuracy nits.** (a) Handoff: "ledger dir-fsync boundary honestly noted in B3" — no such note exists (grep: zero hits in `b3-backfill-writer.mjs`); the pre-intent lap case also does not cover the lost-first-append-with-committed-chunks crash (file creation lost ⇒ writes exist, no ledger, plain B4 green), so the card's "incl. the first-append window" is satisfied by neither hooks nor note. (b) `b1-expected-labels.mjs:66-67` still documents the deleted `--uids` mode. (c) B1's `--deltaAuth` reader (`b1:96-101`) still skips DA version/uid-uniqueness (r63 residue, still open; fail-closed downstream). (d) `B4 v4` log line fixed (`b4:128`) as claimed.

## (3) STAGE-1 FREEZE: **NO**

The r63 mechanical set is genuinely closed and the lap execution claim is real — 41/0 reproduces on an exclusive run, and the gate-order controversy is dead. But: the dark-window clause — the packet's own named ratification item — is internally contradictory at three points (N2/N3/N4) and has no carded writer for its load-bearing field; the N1-repair mechanism introduced reproduced false-GREENS at the one post-flip authority gate (whole-word census skip, `!src.row` skip, unbound `--postFlip` boundary — Codex r64 confirmed all on the emulator; convergent with my static findings); the lap harness live-destroys and races the forensic namespace it certifies (proven twice this session); and the evidence artifact cannot survive citation. Codex r64, filed during this panel, independently ruled freeze-NO with the clause NOT RATIFIED. Minimal set: the two-field flip txn carded + write-iff-absent + post-flip assert (N2) · the rehearsal carve-out sentence + battery-A ordering (N3) · a tail disposition clause (N4) · lap artifact-root isolation + exclusivity + source-bound evidence (N1/N5) · the postFlip false-green trio (Codex r64 A1-A3; r65 in flight) · B1 A8 exit 8 + the adjudication lap case + the §3 allowlist sentence.

## (4) Dark-window custody clause — my ruling

**As a text reading: faithful. As a freeze: needs David.** R2-32 is by title, origin (r46-Q4), and language ("re-enable," "accumulated OFF-window fail stamps... on re-enable") the KILL-SWITCH law — every behavior it ratifies presupposes a prior activation, and the r64 clause changes none of it post-activation. The dark window had no explicit label-writer law; the r63 contradiction was between inferences. So scoping R2-32 to post-activation windows is a scope *refinement*, not a reversal — Codex ratification would suffice **if that were all it did**. It is not: the clause adds a new duty to the activation write David personally executes (R2-14/R2-31 name his fireadmin script; the two-field txn changes it), and it requalifies contract (7) on the frozen 02_ card, which still reads "fail+correct write while OFF" unqualified — under this program's own R2-24 precedent, requalifying frozen card text is an owner-level edit. And per N2/N3/N4 the clause is not yet internally consistent enough to ratify in any forum. Disposition: fix N2/N3/N4, then a three-line David ask (labels dormant until your first flip; your flip transaction writes `enabled:true` + `firstEnabledAt` together, once; kill-switch behavior unchanged). Codex r64 ruled identically (NOT RATIFIED; owner decision required), and the in-flight r65 already marks the clauses PROPOSED–PENDING DAVID — I concur with that framing.