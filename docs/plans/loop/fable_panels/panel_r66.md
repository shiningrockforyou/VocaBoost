# 3-Fable panel — DEEPFIX2 r66 (2026-08-02)


---

## LENS: law

All verification complete. Composing the verdict.

# DEEPFIX2 r66 — panel review. LENS: the adjudication-reality law end-to-end

**Reviewer basis (executed):** solo-lap constraint honored — at my start `pgrep -f b-emulator-lap` matched only my own probe shell (no live runner), but with three uncoordinated reviewers I took the STATIC seat and did not run the lap; my execution evidence is the two offline fixtures + the sha16 trust-check. Measured on this working tree (HEAD `e9a0f42`, r66 fold UNCOMMITTED in the tree): `delta-chain-fixture.mjs` **61/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` clean ×10 · sha16 recomputed for all eight chain scripts vs `evidence/emulator-lap-result.json` — **7/8 MATCH, 1/8 MISMATCH (see NEW-1)** · full read: b1-replay-lib, b4-verify, b3-backfill-writer, b3-txn-core, b-baseline, b1-expected-labels, b-emulator-lap, fixture stages 9b/9c/10, 11\_ rows R2-32/43/48/49, 15\_ §6b/§7, 14\_ §4, 02\_ DF2-14 card, 16\_ drill-E, production accept writers (`functions/foundation.js:2592-2627`, `src/services/db.js:2900-2947`), change-log rows 1449-1463, baton (round 66, `turnOwner:codex`).

## (1) r65 findings — CLOSED / MISS

| r65 finding | Verdict | Evidence (file:line) |
|---|---|---|
| **Codex A1 — adjudication law (my lens core)** | **SUBSTANTIALLY CLOSED; one minted clause NOT implemented (NEW-2); one as-of residue (NEW-4)** | Preimage preferred: `b1-replay-lib.mjs:99` (§6b duty (1) + the DF2-14 card's "GRADING-PREIMAGE schema" sentence; `gradedIsCorrect` written nowhere today — grep clean — so it is correctly a dark-build duty). Legacy reconstruction GRADED-WRONG: `:100` (R2-49 RATIFIED, 11\_:37, dual-attested receipt). Closed enum with unknown-treated-rejected: `:94-97`. As-of-boundary acceptance: `:102-103` (`rvAt < watermark`), missing-timestamp not-effective `:104`. Mint time = `challengeReviewedAt`: `:105,:146` (lc AND lp via `mintT`). Duplicate identity binds adjudication facts: `:114` (status+reviewedAt in the content hash ⇒ divergent-adjudication retries ⇒ `dupConflictGroup`, fail-closed). Effective fence: `:111-112` (Codex's score-correcting whole-attempt exclusion dead — the accept writers' `Math.round` recompute sits well inside the 2pp fence). Production reality verified: both writers flip `isCorrect=true` + recompute score/passed (`foundation.js:2601,2605-2607`; `db.js:2910-2923`) — the fixture/lap shapes now model this exactly. Fixture 9c executes legacy-flipped+recomputed / preimage / as-of-boundary / rejected-inert / mint-time / lp-at-review-time (`delta-chain-fixture.mjs:340-365`). Lap (tree bytes): A2-reality legacy-accepted (flipped + `score:100`) PASS + **Codex's exact corrupt-fc-behind-acceptance BLOCKS** (`b-emulator-lap.mjs:274-289`). Codex's remaining asks: accepted-resting fixture = N/A at replay level (rru is live-only; R2-43 governs the live writer — see §4) and carded on DF2-14 CERT; duplicate-divergent-adjudication fixture absent (mechanism covered by the generic dup-conflict stages; handoff does not overclaim it). |
| **Codex A2 — tail provenance** | **CLOSED (both directions executed); the "provenance by construction" absolute has a pinhole (NEW-4)** | Movement conjunct: `b4-verify.mjs:207-219` — tail requires flip-expectation MOVED off layer (`:213-214`) AND disk ≡ layer (`:215`), all fields including fc (`flipRowsRef` carried from `:172`); lost post-flip fc stamp ⇒ flip ≡ layer ⇒ falls to the fc fence `:221-228` ⇒ BLOCKS. Truncation flag now real (`:217` `preFlipTailTruncated`). 14\_ §4 letter rewritten to the conjunct + the fc-heal honesty ("a pure-tail fc deficit persists as a PUBLISHED permanent undercount — advisory-priority impact only"). Lap executes BOTH directions: true tail classified+PASS and lost-stamp BLOCKS (`b-emulator-lap.mjs:329-342`), and the same run holds a live tail (tailA) while lostB's deficit blocks = the mixed case. Gate NEW-A dead. |
| **Codex A3 — lease / barrier / repair reducer** | **CLOSED; two-process negative honestly CARRIED** | Staleness = `dead ∨ (aged ∧ unverifiable)` — liveness wins at ANY age, EPERM protected (`b3-backfill-writer.mjs:259-265`); takeover = claim-by-rename, loser's ENOENT is the race signal (`:266-269`); ownership TOKEN + conditional release (`:250,:475-478`, release after completion publication `:470`). THE ACTIVATION BARRIER: config doc read in EVERY chunk txn (`b3-txn-core.mjs:18-21`), `FLIP_DURING_RUN` FATAL (`b3:444`); cutover EXECUTED via the emulator-gated hook (`b3:403-406`, lap `:376-388`). Repair consumes B4's own strict reducer (`parseLedgerStrict`, `b-baseline.mjs:179-206`; consumers `b4:66`, `b3:169`) — and its latest-attempt law (`:198-203`) also kills the r65 evidence-lens NEW-2 repair livelock (superseded crashed attempts no longer dangle forever). Live >2h lease REFUSED executed (lap `:361-368`). Panel D3/plain guard: `b3:276-283` FATAL + executed (lap `:371-375`). Residue: the HARNESS lap-lock still uses the rm+wx TOCTOU the panel named alongside D2 (`b-emulator-lap.mjs:53-54`) — test-infra only, unlisted (see CARRIED note). |
| **Codex A4 — one stamping law** | **CLOSED; one NEW doc self-contradiction introduced (NEW-3)** | 15\_ §7's residual sentence now reads "per THE STAMPING PREDICATE below (marker ∨ rehearsal…)"; 14\_'s zero-writers sentences carry "OUTSIDE `rehearsalClassIds` [the R2-48 carve-out]"; 11\_:38 R2-48 row carries the predicate verbatim; 11\_:50 R2-32 scoped; 16\_:88-92 DRILL-E LAW (assignment-OFF only, class stays registered; DRAIN law before membership removal); DF2-14 card carries THE STAMPING-PREDICATE DUTY + the §6b preimage duty for DF2-10's writers; contract (7) carries the R2-48 scope note. |
| **Codex B-evidence 1-5** | 1 (score uncorrected in accepted cases) **CLOSED** (lap `:280` `score:100`; fixture e1 recomputed shape) · 2 (tail cases) **CLOSED** (r66 lap) · 3 (lease/cutover) **CLOSED** (r66 lap) · 4 (new-word matrix) **CLOSED as claimed** — correct + mixed executed (lap `:343-356`) + the absent-vs-zero fc law (`b4:197`); a genuine blank-row post-flip case still absent (per-word unattributable by design; handoff does not claim it) · 5 (aggregate-only JSON) **CARRIED, honest** | |
| **r65 panel gate NEW-A / NEW-B / NEW-C / NEW-D / NEW-E** | NEW-A **CLOSED** (= A2 above). NEW-B plain guard **CLOSED** (`b3:276-283` + lap case). NEW-C tail-law-zero-execution **CLOSED** (both directions + the fc-deficit BLOCK now execute). NEW-D 14\_ stale advisory sentence **CLOSED in the gate law** ("no auto-advisory re-enrollment path [r66]") — but the neighboring ROSTER LAW paragraph still says "a POST-flip re-enrollment converges through live use", a soft leftover for the flip→gate window (LOW, note 7). NEW-E case-(c) pinned to exit 7 (`lap:143`); torn-completion EXECUTED (`lap:390-392`); first-append RE-SCOPED on the card to the stage-2 matrix (02\_ card text verified). | |
| **r65 panel custody D1-D7 / evidence NEW-1..6** | D1/D2 lease **CLOSED** (above; harness-lock half of D2 remains). D3 **CLOSED**. D4 **CLOSED** (gate paragraph). D5 **CLOSED** (all three letters + 11\_ row). D6 **CLOSED-or-rescoped** (torn executed; first-append re-scoped; case-(c) pinned). Evidence NEW-1 **CLOSED**, NEW-2 **CLOSED** (latest-attempt reducer), NEW-3 pointer files **MISS — untouched** (`b1-baseline-pointer-full.json` still `students: 3` emulator data presented as a full-mode pointer), NEW-4 red-lap overwrite **MISS** (`lap:424-428` still unconditional), NEW-5 partially (case-(c) pinned; crash read-backs still exit-code-only — CARRIED), NEW-6 **CLOSED**. | |

## (2) NEW defects

**NEW-1 (HIGH, evidence identity — the freeze's own standard): the 66/0 lap artifact does NOT verify against the tree — the harness was edited 27 seconds AFTER the evidence was written.** The artifact (written 05:06:10.230Z) records `b-emulator-lap.mjs → bd41b58903c243a8`; the tree's harness hashes `2c344171622cc0e2` (mtime 05:06:37.598). The other seven scripts — the law code — match byte-for-byte, and my static `ok()` count of the tree harness is exactly 66, but the bytes that produced the freeze-attempt's headline execution evidence (movement-conjunct both directions, cutover barrier, lease liveness, plain guard, torn ledger, A2-reality) exist nowhere recoverable (not in HEAD `ea0b7165…`, not in the index, not in stash). The handoff itself instructs "verify sha16" — it fails on the harness. This is the exact r64/r65 evidence-identity class this workstream ruled freeze-material twice. Fix is one solo lap re-run on the frozen tree (the artifact then rebinds to `2c34…`); nothing suggests the edit was substantive, but "suggests" is what binding exists to eliminate.

**NEW-2 (MEDIUM-HIGH, minted-law gap — my lens headline): the ratified R2-49 census clause is UNIMPLEMENTED — `legacyAcceptedReconstructed` (and both sibling counters) publish NOWHERE.** R2-49 as minted (11\_:37): "Every reconstruction is COUNTED and PUBLISHED (`legacyAcceptedReconstructed` census)"; 15\_ §6b(2): "with a PUBLISHED census"; handoff: "missing timestamps counted", "unknown statuses counted `challengeStatusUnknownEnum`". Reality: the three r66 `note()` calls (`b1-replay-lib.mjs:96,:100,:104`) are dropped by every consumer — B1's note handler recognizes ONLY `attemptsSeen`/`teacherEditedSeen` (`b1-expected-labels.mjs:148`; nothing lands in the summary, manifest, or redacted pointer), B3 passes `{}` (`b3:328`), B4 passes `{}` at all three call sites (`b4:141,:171,:223`). Concrete scenario: the real 26SM B1 --full runs over a cohort with hundreds of legacy accepted rows — the reconstruction silently reshapes fc/lf for every one of them and the published baseline carries zero trace; the census David's ratification explicitly rides on (the honesty mechanism for the ~rare fc over-count) does not exist. Repo-wide grep confirms the three names appear only in the lib, two docs, and the handoff. The detection and fail-safe treatment are real; the census is a void. Fix: ~5 lines in B1's note handler + summary/pointer fields, real counters in B4's stats, and replace the fixture's fake assert (next item).

**NEW-3 (MEDIUM, evidence integrity): `delta-chain-fixture.mjs:359` is a TAUTOLOGY — `ok((ePre.mutationRisk ? 1 : 1) === 1 && true, "census present")` cannot fail under any input.** `x ? 1 : 1` is 1 always. The one check in the round's headline adjudication stage that names the census is vacuous — it inflates 61/0 by a green that asserts nothing, and it is NEW-2 manifesting inside the fixture (there is no census to assert). Also in the same family, LOW: §6b now contradicts itself two sentences apart — the retained r65 sentence "adjudication NEVER rewrites the row's `isCorrect` in place — grading history is immutable" sits directly above the r66 duty "(1) …copies the pre-accept value into `gradedIsCorrect` BEFORE flipping `isCorrect`". The dark-build implementer of DF2-10's accept writer reads two opposite letters in one section; the r65 sentence needed the r66 rewrite ("immutable in the preimage").

**NEW-4 (LOW-MEDIUM, law letter vs code): the flip-boundary replay is not fully as-of-boundary — `passing = a.stored >= 92` (`b1-replay-lib.mjs:138`) and the fence read the CURRENT stored score, so a post-flip accept can move the flip-boundary expectation, and the movement conjunct then classifies a genuinely-lost post-flip stamp as `preFlipTail`.** Concrete scenario: pre-flip attempt, organic 85 (<92), lp=null in every layer and at the true flip boundary. Post-flip a teacher accepts one wrong row; the writer recomputes stored to 92+; the live lp stamp is LOST (crash after the attempt update, before the study_states mint). The gate's flip replay now reads stored=92 ⇒ `passing=true` ⇒ flip-lp = attempt time for the other correct rows; layer lp = null = disk ⇒ moved ∧ disk≡layer ⇒ `preFlipTail`, informational — a lost POST-flip stamp excused, the exact class the conjunct exists to block. Variants through the same door: a teacher-edited attempt whose accept pushes `correctEff` >2pp off `preOverride.score` (inclusion flips ⇒ whole-attempt fields excused as tail, including an fc drop), and a duplicate group diverging on adjudication post-flip. Bounded honestly: it needs a post-flip adjudication inside the minutes-scale flip→gate window plus a lost stamp; the tail rows are PUBLISHED (counted + listed), never silent; fc's own flip value is immune for plain accepts (reconstruction makes it deterministic). But the absolute "movement can ONLY come from events in (layerWatermark, flip) — provenance by construction" (`b4:208-212`, 14\_ §4, handoff A2) is falsified in this corner. Fix: reconstruct as-of passing (organic score when the acceptance is not boundary-effective), or qualify the letter + add the gate-runbook duty to reconcile nonzero tail rows against adjudication events.

**NEW-5 (LOW, nits):** (i) the fence counts `(r.adjState ?? "").startsWith("accepted")` (`lib:111`) — an unknown status literally prefixed "accepted…" would count effective-correct in the fence while being treated rejected for minting; fail-closed (worst case a counted spurious exclusion), fix `===` on a carried boolean. (ii) The two census scans parse `challengeReviewedAt` differently — string timestamps parse via `Date.parse` at `:41` but are NaN at `:102` (not-effective; cosmetic divergence, fail-closed). (iii) The harness lap-lock keeps the rm+wx TOCTOU (`lap:53-54`) after the exec lease got claim-by-rename. (iv) Red laps still overwrite the tracked artifact (`lap:424-428`). (v) r65 evidence NEW-3 pointer files still contaminated (`students: 3`/`0`).

## (3) STAGE-1 FREEZE: **NO** — narrowly, with a small falsifiable set

**The CARRIED list does not block** — I rule on each: aggregate-only evidence (hygiene; sha16 binding is the load-bearing control — which is exactly why NEW-1 must be cured); two-process negative (both lease branches + rename semantics executed single-process; documented residual); exit-code-only crash asserts and the first-append window (honestly re-scoped to the carded stage-2 matrix, card letter verified updated); per-original lease scope (operator-error class as the law specified). The list is however not quite "the entire known residue": the r65 pointer-file contamination and the harness-lock TOCTOU are open, unlisted (both LOW).

**What blocks:** (a) **NEW-1** — the freeze attempt's execution pillar is bound to harness bytes that no longer exist; by this packet's own thrice-enforced standard the 66/0 artifact cannot back a freeze until one clean solo re-run on the frozen tree rebinds it. (b) **NEW-2 (+NEW-3)** — David ratified R2-49 WITH the census clause; freezing now would freeze a minted law whose publication mechanism is a no-op, certified by a fixture check that cannot fail. Both fixes are hours, not days, and reopen zero law substance. NEW-4 should be recorded (letter qualification or the small as-of fix) but I would not block on it alone: the misclassification is published, the window is micro-lap-bounded minutes, and the preconditions are compound.

**Minimal set to YES:** (1) re-run the solo lap on the frozen tree → 8/8 sha16 verify; (2) publish the three censuses end-to-end (B1 aggregation → summary/pointer; B4 stats) + replace `fixture:359` with a real census assert (a legacy-accepted stage asserting count ≥ 1); (3) the two one-sentence doc fixes (§6b self-contradiction; 14\_ roster-law leftover); (4) qualify or fix NEW-4's provenance absolute. On YES thereafter, the handoff's stage-2 opening order (B1 --full → dark build with DF2-10 duties → emulator matrix → dark deploy → 25WT → shadow) is the right sequence — with the census (2) landing BEFORE the real B1 --full so the 26SM baseline publishes its reconstruction count.

## (4) The two ratified laws as MINTED vs the implementations

**R2-48 (dark-window custody, 11\_:38): MINTED FAITHFULLY.** Every operative clause traces to implementation or carded duty: stamping predicate (15\_ §7 + DF2-14 duty; correctly a dark-build contract — no live writer exists yet), two-field flip txn write-iff-absent + post-flip asserts (02\_ deploy clause), R2-32 scoped (11\_:50 + contract (7)), B3 forbidden from the marker onward incl. kill-switch windows (b3:122-127, lap-proven `:242-245`), zero-writers-outside-rehearsal qualifiers present in 14\_/15\_, tail classified + published (b4:207-219 + report `:284`). One cosmetic note: the row's unqualified "heals through live use" defers to 14\_ §4, which now carries the fc-doesn't-heal honesty — the governing text is honest; the row's shorthand is not.

**R2-49 (legacy reconstruction, 11\_:37): MINTED WITH ONE UNIMPLEMENTED CLAUSE.** Reconstruction GRADED-WRONG ✓ (`lib:100`, fixture 9c, lap A2-reality, executed against the true production-writer shape I verified in foundation.js/db.js); preimage forward-duty ✓ (§6b + DF2-14 card; `gradedIsCorrect` correctly absent from today's writers); the fc over-count direction and advisory-only impact stated accurately. "COUNTED and PUBLISHED (`legacyAcceptedReconstructed` census)" ✗ — no consumer aggregates or publishes it (NEW-2). The ratified letter and the chain diverge on precisely the clause that makes the reconstruction auditable; close it before the freeze claims R2-49 implemented-as-ratified.

**Bottom line:** the adjudication-reality law itself is the strongest piece of this round — grading truth recovered exactly as production destroys it, as-of-boundary minting, adjudication-bound duplicate identity, the effective fence, and the movement conjunct all verified in code and (modulo NEW-1's binding break) in execution; 61/0 and 2,688/0 reproduce on this tree. The freeze fails today on evidence identity and on one ratified-but-void census clause — both small, both falsifiable, neither touching law substance.

---

## LENS: gate

All verification complete. Composing the verdict.

# DEEPFIX2 r66 — Panel review. LENS: the post-flip gate + tail (movement conjunct, absent-vs-zero, marker, uncovered, cutover barrier)

**Basis (measured on this checkout, HEAD `e9a0f42`, working tree = the claim surface):** `node --check` clean 11/11 · `delta-chain-fixture.mjs` **61/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** (both match the handoff) · full read of b4-verify, b3-txn-core, b3-backfill-writer, b1-replay-lib, b-baseline, b-emulator-lap, b1-expected-labels (aggregation region), 14_ §4, 15_ §6b/§7, 16_ drill-E, 02_ DF2-10/DF2-14/contract-(7), 11_ R2-48/R2-49/R2-32 rows. **Lap constraint honored: I did NOT run the lap** (`pgrep` clean at start; my hit was my own shell; three uncoordinated reviewers — I took the static seat). **Evidence sha16 verification: 7/8 MATCH, 1/8 FAIL** — see NEW-3.

## (1) r65 findings — CLOSED / MISS with file:line

| r65 finding | Verdict | Evidence |
|---|---|---|
| Codex A1 (adjudication: preimage / legacy / as-of-boundary / mint time / dup identity / fence) | **CLOSED in substance; ONE subclaim false** | `gradedIsCorrect` preferred (`b1-replay-lib.mjs:99`); legacy accepted → GRADED-WRONG (`:100`, R2-49-ratified `11_:37`); as-of-boundary `challengeReviewedAt < watermark`, missing-ts not effective (`:102-104`); mint = reviewedAt (`:105,:146`); dup identity binds `adjState` (`:114`); fence vs EFFECTIVE rows (`:111-112`). Fixture 9c executes realistic flipped+recomputed legacy (score 50 = the recompute), preimage, as-of-boundary, rejected-changes-nothing, passing-accept proof mint (`delta-chain-fixture.mjs:343-366`). Lap has the reality-shape accept + Codex's exact corrupt-fc-behind-acceptance BLOCK (`b-emulator-lap.mjs:274-289`). **FALSE subclaim: the "published `legacyAcceptedReconstructed` census" does not exist — NEW-2.** |
| Codex A2 (tail = value coincidence, reproduced false green) | **PARTIAL — the named counterexamples are dead; the class is one clause short** | Movement conjunct implemented (`b4-verify.mjs:207-219`): tail requires flip-expectation ≠ layer-expectation AND disk ≡ layer. Codex's accepted-score/corrupt-fc case now BLOCKS (executed, `lap:284-288`); lost-stamp-with-empty-tail BLOCKS (executed, `lap:339-341`); true tail classified + PASS with the branch provably fired (`stats.preFlipTail` reaches stdout only when incremented, `b4:216,:310`); truncation flag exists (`b4:217`). **But the conjunct checks only (layer→flip) movement, never (flip→cutoff) — a lost POST-flip stamp still rides free behind any genuine tail event on the same field: NEW-1.** |
| Codex A3 (lease steal / no activation barrier / weak repair scan) | **CLOSED; two small residuals** | Liveness wins at any age: `stale = dead(ESRCH) ∨ (aged ∧ unverifiable)` (`b3-backfill-writer.mjs:259-265`); claim-by-rename takeover, one winner (`:266-269`); ownership token + conditional release (`:250,:475-477`). ACTIVATION BARRIER: config doc in EVERY chunk txn's read set (`b3-txn-core.mjs:18-21`), wired unconditionally into phase 2 (`b3:427`) incl. resume and extras plans; admission guard (`b3:122-127`) + barrier close the admission-to-phase-2 gap; FLIP_DURING_RUN FATALs (`b3:444`). Repair consumes `parseLedgerStrict` (`b3:169`, `b-baseline.mjs:179-206`) — the r65 superseded-attempt livelock is dead (latest-attempt law `:198-203`). Executed: live 3h lease REFUSED, cutover mid-run abort via barrier, torn completion FATAL (`lap:360-392`). Residuals: NEW-4 (plain-guard intent hole), NEW-7 (empty-lease read window). |
| Codex A4 (one stamping law across docs) | **CLOSED except one sentence** | 15_ §7 predicate `marker ∨ rehearsalClassIds` with R2-48 RATIFIED stamp (`15_:218-221`, residual marker-only sentence rewritten); 16_ DRILL-E = assignment-OFF only, class stays registered, DRAIN before membership removal (`16_:88-94`); DF2-10 carries the STAMPING-PREDICATE DUTY + `gradedIsCorrect` preimage duty (`02_:95`); contract (7) carries the R2-48 post-activation scope note (`02_:95`); 11_ R2-32 row scoped (`11_:50`). **MISS: `14_:113` still says "label writers stamp ONLY when firstEnabledAt is SET" with no rehearsal disjunct — NEW-6; the handoff's "14_'s two zero-writers sentences carry the carve-out qualifier" is half-true (only `14_:115` carries it).** |
| Panel gate NEW-A (fc tail classifier) | **PARTIAL = A2 above.** The r65 panel's prescribed conjunct was "tail-classify fc only when the cutoff expectation equals the flip expectation"; r66 shipped a different conjunct (flip ≠ layer), which kills the pure lost-stamp case but not the mixed one (NEW-1). |
| Panel custody D1 (live lease steal) | **CLOSED + EXECUTED** (`b3:259-265`; `lap:361-368`). |
| Panel custody D2 (takeover TOCTOU) | **CLOSED (rename)**; microsecond empty-lease residual — NEW-7. |
| Panel custody D3 / evidence NEW-1 (plain-mode ledger guard) | **CLOSED + EXECUTED; one edge** | Guard at `b3:276-283`, FATAL "plain-guard"; lap case `r66plain` (`lap:374-375`). Edge: the scan matches only `b3-applied` records — NEW-4. |
| Panel custody D4 / evidence NEW-6 (14_ stale advisory sentence) | **CLOSED** | 14_ §4 now: adjudication via the reality law, "NO word-level exemption exists"; "UNCOVERED students are LISTED and their diffs BLOCK — there is no auto-advisory re-enrollment path" (`14_:102-110`). |
| Panel custody D5 (three zero-writers letters + ask qualifier) | **CLOSED except `14_:113`** (NEW-6); 11_ R2-48 row itself states the predicate verbatim. |
| Panel custody D6 / evidence packet-accuracy (first-append, torn, case-c pin) | **CLOSED** | Torn completion EXECUTED (`lap:390-392` → `parseLedgerStrict` malformed-line FATAL); first-append RE-SCOPED on the card to the stage-2 matrix (`02_:95` "RE-SCOPED [r66, honestly]"); case (c) pinned to exit 7 exactly (`lap:143`). |
| Panel evidence NEW-2 (repair livelock on superseded attempts) | **CLOSED** — latest-attempt-per-runId law in the ONE shared reducer (`b-baseline.mjs:198-203`), consumed by both B4 (`b4:66`) and repair (`b3:169`). |
| Panel evidence NEW-3/NEW-4 (pointer files, red-lap overwrite) | **NOT RE-CHECKED in depth; evidence write still unconditional (`lap:424-428`)** — carried. |
| Panel evidence: crash hooks env-gate | **CLOSED** — `B3_CRASH_AT` and `B3_TEST_SET_MARKER` both require `FIRESTORE_EMULATOR_HOST` (`b3:57,:403`). |
| Panel: closed-enum as real validation | **HALF-CLOSED** — unknown statuses are treated rejected and `note("challengeStatusUnknownEnum")` fires (`lib:96`), but nothing aggregates or publishes the count — NEW-2. |
| Panel: new-word correct/mixed matrix + absent-vs-zero | **CLOSED + EXECUTED** | Matrix case (`lap:343-356`); absent-vs-zero fc law (`b4:197`) is correctly scoped: `exp===0 ∧ act===null ∧ word ∉ flip universe` — I attacked it and found no hole (pre-flip-created words get explicit `fc:0` from B3 via `b3:353`, so absence there still diffs; a post-flip-created word with expected fc ≥ 1 and no fc field falls through to the fence and BLOCKS; flip universe ⊆ cutoff universe so no word can dodge the guard). |

## (2) NEW defects

**NEW-1 (HIGH — the lens target): the movement conjunct is half of provenance — a genuine tail event on a field EXCUSES a lost post-flip stamp (or revert-to-layer corruption) on the same field.**
`b4-verify.mjs:213-215` classifies tail when `flipVal ≠ lay ∧ act ≡ lay`. The conjunct proves the tail is nonempty; it never checks that nothing moved AFTER the flip. Concrete scenario (deterministic): emB/w3, layer fc=1 (byte-correct). Tail-window fail → flip fc=2. Post-flip fail whose live stamp is LOST (crashed txn / stamping regression) → disk stays 1, cutoff fc=3. Gate: exp=3 vs act=1 → mismatch; tail check: `lay=1, flipVal=2, moved ✓, act≡lay ✓` → `preFlipTail`, `continue` — the fc fence (`:221-227`), which would catch the deficit (exp2=3 ≠ act2=1), is never reached → **PASS exit 0**. The same shape false-greens for timestamps and for the tail-created-word variant (lay=null, flipVal=0, post-flip fail lost → tail). This directly falsifies both freshly rewritten letters: 14_:111's "a lost POST-flip stamp leaves flip ≡ layer ⇒ NOT tail ⇒ BLOCKS" (only true when the tail is empty for that field) and "mixed tail+post-flip fc divergence still BLOCKS" (only true when the post-flip increment LANDED — disk off layer). The executed lap cases prove exactly the disjoint-word shapes (tail on w3, lost stamp on w1) and cannot see this. It is also the surviving kernel of Codex r65 A2's "ordinary corruption that happens to equal the old layer": alive on every tail-active field. Fix is the conjunct the r65 panel actually prescribed: tail-classify only when additionally `cutoffVal ≡ flipVal` for that field (all divergence pre-flip in origin) — `flipRowsRef` and `c.wordsOut` are both already in scope at `:213`; the pure tail stays informational (the R2-48-ratified disposition), landed-mixed still blocks, lost-mixed starts blocking. One clause + one mixed-SAME-field lap case each way.

**NEW-2 (MEDIUM — ratified-letter vs code): the three adjudication censuses are claimed "COUNTED and PUBLISHED" in a David-ratified row and two doc laws, and no publisher exists.**
`legacyAcceptedReconstructed`, `challengeStatusUnknownEnum`, `acceptedNoTimestamp` are emitted only as `note()` calls (`b1-replay-lib.mjs:96,:100,:104`). B1's aggregator counts ONLY `attemptsSeen`/`teacherEditedSeen` (`b1-expected-labels.mjs:148`); B3/B4 pass `{}` (no-op). Repo grep: zero aggregation, zero summary field, zero report field. Yet `11_:37` (the RATIFIED R2-49 row): "Every reconstruction is COUNTED and PUBLISHED (`legacyAcceptedReconstructed` census)"; `15_:190`: "with a PUBLISHED census"; `15_:182`: "unknown strings are counted"; the handoff: "the published `legacyAcceptedReconstructed` census". The census is R2-49's own accountability mechanism (it bounds the over-count David accepted). Fix: extend B1's note handler + summary (three fields), or strike the claim from the letters before freezing — the ratified row must not overclaim at freeze.

**NEW-3 (MEDIUM — evidence binding): the 66/0 artifact fails its own "verify sha16" instruction on the harness itself; the executed assertion bytes are unrecoverable.**
`docs/plans/deepfix2/evidence/emulator-lap-result.json` records `b-emulator-lap.mjs: bd41b58903c243a8`; the tree's file hashes `2c344171622cc0e2` (committed e9a0f42 version: `ea0b7165d75b66f8` — neither). Timeline is exact: evidence written 05:06:10.230, harness mtime 05:06:37.598 — a post-run edit 27 seconds after the artifact, 37 seconds before the handoff (05:07:14). The seven SUT scripts all match, and the current harness's static assertion-site count is exactly 66, so the drift is almost certainly a small post-run tweak — but "almost certainly" is what source-binding exists to eliminate, the exact bytes that produced 66/0 exist nowhere (no transcript found; scratchpad logs show only 41/0 and 48/21 intermediates), and this is the third consecutive round with a packet-accuracy defect of this class. Fix: one solo lap re-run on the frozen tree re-binds the artifact.

**NEW-4 (MEDIUM-LOW — barrier-adjacent): the plain-guard matches only `b3-applied` records, so a crashed delta EXECUTE (intent, no completion) does not trip it.**
`b3-backfill-writer.mjs:279` requires `e.probe === "b3-applied" && e.deltaManifestSha256`. Sequence: delta EXECUTE crashes after committing chunks (intent carries the delta sha, `b3:410-411`; no completion) → operator runs plain `--execute` (the exact forgotten-`--deltaDir` slip the guard exists for) → guard sees no applied record → admitted → reverts the committed M1 chunks to M0 with a clean exit 0. Bounded: the next B4 FATALs on the dangling intent (`parseLedgerStrict:200`) and resume regenerates — loud, healable — but the guard's own letter ("EXECUTE'd delta layer(s) exist ⇒ FATAL") is bypassed in precisely the crash window where the mistake is most likely. Fix: also match `b3-intent` records carrying `deltaManifestSha256` (or run `parseLedgerStrict` and refuse on problems + any delta sha).

**NEW-5 (LOW — evidence hygiene): fixture check 9c-"census present" is a tautology.**
`delta-chain-fixture.mjs:359`: `ok((ePre.mutationRisk ? 1 : 1) === 1 && true, "census present")` — true under every possible behavior; 61/0 is effectively 60 real checks plus one that cannot fail, and the thing it names (census plumbing) is exactly what NEW-2 shows is missing. Replace with a real counters-callback assertion (which would have caught NEW-2).

**NEW-6 (LOW-MEDIUM — doc letter): `14_:113` still states the unqualified marker-only stamping law** ("label writers stamp ONLY when `firstEnabledAt` is SET") two lines above the qualified zero-writers sentence (`:115`). Under the now-RATIFIED R2-48 predicate this sentence is false for rehearsal classes — the exact builder-implements-marker-only trip r65 D5 named, surviving in one sentence. The handoff's claim that both 14_ sentences carry the qualifier is inaccurate.

**NEW-7 (LOW — note): the wx-then-write lease-creation gap survives.** `takeLease` (`b3:251`) opens `wx` then writes; a concurrent starter reading the empty file gets `holder=null` → `unverifiable=true`, `aged=true` (`b3:263` defaults null-holder to aged) → stale → renames a microseconds-old LIVE lease away. Claim-by-rename means one winner of the takeover, but the original creator also proceeds (its fd survives the rename) → two writers. Microsecond window, needs simultaneous starts; the conditional release keeps the aftermath sane. Fix when touched next: create the lease via write-tmp-then-`rename` into place, or treat unparseable-but-fresh-mtime as alive.

## (3) STAGE-1 FREEZE: **NO**

**The CARRIED list itself does not block** — I agree with the handoff's reading on all five items (aggregate-only evidence, the unexecuted two-process negative, exit-code crash asserts, first-append on the stage-2 matrix, per-original lease scope): each is honestly scoped, fail-safe in direction, or carded where it belongs. The r65 blocking set is genuinely closed in code with real executions — the adjudication-reality law, the marker binding, uncovered blocking, the activation barrier, liveness-wins leasing, the shared strict reducer, and the plain guard all held against my attacks.

What blocks is outside the CARRIED list: **NEW-1** is a deterministic false green in the exact machine this freeze certifies, contradicting the two sentences of 14_ §4 rewritten this round to describe it — freezing now freezes a letter the code does not implement, inside the section R2-48's ratification points at. **NEW-2** freezes a ratified row that overclaims its own accountability mechanism. **NEW-3** means the freeze-cited 66/0 is bound to harness bytes that no longer exist. Minimal falsifiable set (hours, no law substance reopens, nothing touches David):
1. Add the second conjunct at `b4:213-215` (tail requires `cutoffVal ≡ flipVal` per field) + one mixed-same-field lap case each way (pure fc-tail ⇒ classified+PASS; tail-fail + lost post-flip fail on ONE word ⇒ 5). Align 14_:111's two sentences to the completed conjunct.
2. Publish the three censuses in B1's summary (extend `b1-expected-labels.mjs:148`) — or amend 11_:37/15_:182,190 to stop claiming publication; replace the tautological fixture check (`delta-chain-fixture.mjs:359`) with a real counters assertion.
3. One solo lap re-run on the frozen tree so the evidence artifact's sha16 verifies 8/8; no further edits after the run.
4. Plain-guard: match intent records carrying a delta sha (`b3:279`); add the rehearsal disjunct to `14_:113`.

On YES (after the set closes): the handoff's stage-2 opening order is the right one — B1 --full → dark build (DF2-10 + resolver + evaluator + preimage + flip-txn duties, now carrying the stamping-predicate text verbatim) → emulator matrix (which inherits first-append + torn + two-process) → dark deploy → 25WT → shadow.

## (4) The two ratified laws as MINTED

**R2-48 (dark-window custody) — MINTED: YES, with one letter defect in its reference chain.** The 11_:38 row states the full ratified content: predicate (marker ∨ rehearsal), one-txn two-field flip, write-iff-absent, never-cleared, R2-32 scoping, B3 forbidden from the marker, tail per 14_ §4 — each element traces to an implementation or a carded stage-2 duty: B3's durable guard (`b3:122-127`, lap-proven incl. kill-switch window), the activation barrier (`b3-txn-core:18-21`, executed), the flip-txn card + asserts (`02_:95`), the predicate in 15_ §7 (RATIFIED-stamped) and the DF2-10 duty, R2-32 row annotation (`11_:50`). Dual-attested receipt present. The defects: the row's tail reference lands on 14_ §4 whose conjunct sentence is currently false as implemented (NEW-1), and `14_:113` still states the pre-carve-out absolute (NEW-6). The ratified substance is sound; the referenced letter needs the two fixes before it is frozen as his.

**R2-49 (legacy reconstruction) — MINTED: PARTIAL.** The core is real and executed: reconstruction GRADED-WRONG (`b1-replay-lib.mjs:100`), preimage-preferred forward path (`:99` + H6 §6b schema duty + DF2-10 card), the over-count direction David accepted is exactly what the code produces, fixture 9c + the lap's reality-shape case execute it. Dual-attested receipt present. But the row's sentence "Every reconstruction is COUNTED and PUBLISHED (`legacyAcceptedReconstructed` census)" is not true of the implementation (NEW-2): the counter fires into a no-op in every consumer. Either the one-line aggregation lands or the row's letter is corrected — as written, the mint overstates.

**Bottom line:** the r65 closure is real — every reproduced r65 counterexample is dead in code and in execution, and the CARRIED residue is honestly declared and non-blocking. The freeze fails on one precisely-fixable gate clause (the conjunct's missing flip→cutoff half), one ratified-row overclaim (the unpublished census), and one broken evidence binding (the post-run harness edit). Close those four bounded items and this lens flips to YES.

---

## LENS: custody

# DEEPFIX2 r66 — Panel review. LENS: leases + ledger + packet accuracy

**Basis (measured on HEAD `ce7b548`, working tree clean over `scripts/deepfix2/`; 11_ carries uncommitted receipt-line edits):** `delta-chain-fixture.mjs` **61/0** reproduced · `rotation-cyclicity-fixture.mjs` **2,688/0** reproduced · `node --check` clean on all scripts · full read of b3-backfill-writer / b3-txn-core / b4-verify / b1-replay-lib / b-baseline / b-emulator-lap + 14_/15_/16_/02_/11_ folds + baton (round 66, owner codex, consistent). **Lap NOT re-run:** at audit start `pgrep -f b-emulator-lap` showed a live pid (68253) — a sibling held the solo slot; later the lock and pid were gone but the tracked artifact was never rewritten, and my prompt forbids editing repo files (the harness unconditionally rewrites the tracked evidence artifact). I audited statically and verified the sha16 binding against the tree — which produced my headline finding.

## (0) HEADLINE — the evidence binding FAILS on 1 of 8 scripts

`evidence/emulator-lap-result.json` (66/0, gitHead `e9a0f42`, at 05:06Z) binds sha16 for eight scripts. Independently recomputed against the tree: **7/8 match exactly** (b-baseline `416abae6…`, b1-replay-lib `8ea31d7c…`, b1-expected-labels `0141d0aa…`, b3-backfill-writer `4dffb7d7…`, b3-txn-core `3a0fd64d…`, b4-verify `9fc77a22…`, b-delta-cycle `4b8a8b97…`). **`b-emulator-lap.mjs` does NOT: evidence says `bd41b58903c243a8`, the tree's file is `2c344171622cc0e2`, and e9a0f42's committed harness is `ea0b7165d75b66f8`** — the 66/0 run used an intermediate, never-committed harness, then the harness was edited again before the `ce7b548` commit. The seven CLIs under test ARE bound; the file that defines what "66/0" asserts is not. Mitigating analysis: the current harness has exactly 66 `ok()` sites (54 r65 + 12 added, matching the artifact); the evidence run must have contained the new `"predates reality"` assertion string (the old string would have failed against r66's B3), so the run was substantially the current case set — but "substantially" is precisely what the sha16 law the packet itself instituted exists to eliminate. This is the r64/r65 evidence-identity class recurring on the freeze attempt's own headline number. Fix is minutes: one solo lap on the committed tree, republish.

## (1) r65 findings — CLOSED / MISS

| r65 finding | Verdict | Evidence |
|---|---|---|
| Codex A1 — adjudication law (preimage / legacy / as-of-boundary / mint time / duplicate identity / fence) | **CLOSED, executed** | `b1-replay-lib.mjs:99` gradedIsCorrect preferred; `:100` legacy accepted → GRADED-WRONG + `legacyAcceptedReconstructed` census (R2-49); `:103-104` acceptedEffective = known `challengeReviewedAt < watermark`, missing ts counted not effective; `:105,:146` mint = challengeReviewedAt (lc/lp via max — the day-1-fail/day-10-accept ordering inversion is dead); `:114` adjState in the duplicate content hash; `:111-112` fence vs EFFECTIVE rows (score-correcting exclusion dead). Fixture stage 9c is the realistic shape — isCorrect flipped + score recomputed, as-of-boundary at 100d vs 105d accept, rejected-changes-nothing, lp minted at review time (`delta-chain-fixture.mjs:344-365`, 61/0). Lap: legacy-accepted flipped+recomputed PASS + Codex's exact corrupt-fc-behind-acceptance BLOCKS (`b-emulator-lap.mjs:274-288`). H6 §6b preimage duty in 15_:186; writer duty carded (02_:95 STAMPING-PREDICATE DUTY names the accept writer + §6b). |
| Codex A2 — tail provenance | **CLOSED, executed; one qualifier overclaimed (N4 below)** | Movement conjunct: `b4-verify.mjs:213-215` — tail iff flip-expectation moved off layer AND disk ≡ layer; fc's flip value carried via `flipRowsRef` (`:172`); lost post-flip stamp ⇒ flip ≡ layer ⇒ falls to the fc fence (`:221-227`) ⇒ BLOCKS. Executed BOTH directions (`b-emulator-lap.mjs:329-341`). Tail-list truncation flagged (`b4:217` `preFlipTailTruncated`). 14_:111 rewritten to the conjunct + the fc-heal honesty ("pure-tail fc deficit persists as a PUBLISHED permanent undercount"). |
| Codex A3 — lease/barrier/repair-reducer | **CLOSED in substance; EPERM claim false + two narrow admissions (N2, N3)** | Liveness wins: `b3:259-265` — stale = ESRCH ∨ (aged ∧ unverifiable); live >2h holder REFUSED, lap-executed (`lap:361-368`). Claim-by-rename takeover `b3:266-269`; token + conditional release `b3:250,:475-477`. Activation barrier: config doc in EVERY chunk txn's read set (`b3-txn-core:18-20`, wired `b3:427`), FLIP_DURING_RUN FATAL (`b3:444`); cutover lap case executes guard-read→flip→write (`lap:376-387`). Repair consumes the shared `parseLedgerStrict` (`b3:169`; B4 at `b4:66`) with latest-attempt pairing — the r65 crash→resume→repair livelock is dead (lap crash2/crash3 resume sequences exercise the superseded-attempt law). Codex's heartbeat letter unimplemented (consequence-free: a live holder refuses at any age); two-process negative honestly CARRIED. |
| Codex A4 — one stamping law across docs | **MOSTLY CLOSED; two stale sentences survive (N5, N6)** | 15_ §7: predicate `marker ∨ rehearsalClassIds` + explicit 14_-requalification, no unqualified "stamp ONLY" remains in 15_ (`15_:218-221`). 11_ R2-32 row annotated "[SCOPED by R2-48]" (`11_:50`). 16_ DRILL-E LAW real: assignment-OFF only, class stays registered, DRAIN law before removal (`16_:88-94`). Contract (7) carries the R2-48 scope note (`02_:95`). DF2-10 writer-eligibility + per-field-posture + §6b preimage duties carded (`02_:95`). BUT: 14_:113 still says "label writers stamp ONLY when firstEnabledAt is SET" unqualified, and 16_:72-73 still says "drill E uses … rehearsalClassIds removal only" — both contradict the new law one breath away. |
| Panel gate NEW-A (fc tail conjunct) | **CLOSED, executed** | Same as A2 — the fc branch now consults the flip replay; the lost-stamp lap case is the executed negative. |
| Panel NEW-B / custody D3 / evidence NEW-1 (plain-mode guard) | **CLOSED as claimed; bypass found (N1)** | Guard exists `b3:276-283`, runs under the lease, applies to resume and `--allowSampleExecute`; lap case executes (`lap:372-375`). But it hand-rolls a WEAKER scan than `parseLedgerStrict` — see N1. |
| Panel NEW-C (tail zero execution) | **CLOSED** | Both tail directions executed (above). |
| Panel NEW-D / custody D4 / evidence NEW-6 (14_ §4 stale advisory sentence) | **CLOSED** | 14_:106-110 rewritten: adjudication-reality law + uncovered students LISTED and BLOCK, "no auto-advisory re-enrollment path [r66]". The surviving roster-law sentence (`14_:122` post-flip re-enrollment "converges through live use") is now mechanically TRUE via per-field exemption + through-cutoff fc — no false promise. |
| Panel NEW-E / custody D6 (card letter) | **PARTIAL** | Case (c) pinned to exit 7 (`lap:143`); torn completion EXECUTED (`lap:390-392`) + card updated "[executed: malformed-line FATAL]"; first-append HONESTLY RE-SCOPED to the stage-2 matrix on the card (02_:95) with `b3:203-208` documenting the residual. **MISS: "(d) a mixed unrelated structural diff" is still in the card's REQUIRED CASES, still executed as pure structural only (`lap:148-162` — no concurrent actionable delta), neither executed nor re-scoped — and absent from CARRIED.** |
| Custody D1/D2 (lease steal + TOCTOU) | **D1 CLOSED (executed); D2 HALF-CLOSED** | B3 lease: rename-claim + token release ✓. **The lap lock (`b-emulator-lap.mjs:45-56`) still has the exact rm+wx TOCTOU D2 named ("same pattern in the harness lap lock") — no rename-claim, no token; two post-crash lap starters can interleave A.rm→A.wx→B.rm→B.wx and both run.** Not in CARRIED. |
| Evidence NEW-2 (repair dangling-intent livelock) | **CLOSED** | `parseLedgerStrict` latest-attempt law (`b-baseline.mjs:198-203`) shared by B4 + repair. |
| Evidence NEW-3 (pointer-file contamination) | **MISS** | `evidence/b1-baseline-pointer-full.json` at HEAD still shows `students: 3` emulator data as a full-mode pointer; unrestored, unannotated, not in CARRIED. |
| Evidence NEW-4 (red laps overwrite the tracked artifact) | **MISS** | `lap:424` still writes unconditionally before `exit(failures?1:0)`. Not in CARRIED. |
| Evidence NEW-5 (setup exits unasserted etc.) | **PARTIAL** | Case (c) pinned; bare `b1full(); b3exec(...)` setup calls remain unasserted (`lap:168,:252,:332,:360,:399`). Minor, unclaimed. |

## (2) NEW defects

**N1 — MEDIUM (code): the plain-guard scans a weaker law than the reducer sitting one screen above it — a crashed delta EXECUTE bypasses it, and the guard+delta-blindness combination creates an unrecoverable wedge.**
The guard (`b3:276-283`) hand-parses the ledger and FATALs only on **`b3-applied`** records with a `deltaManifestSha256`. (a) **Bypass:** M1 delta EXECUTE crashes after `after-first-chunk` (a proven crash point) — the ledger holds only `b3-intent` with M1's sha; a plain EXECUTE is ADMITTED, reverts M1's committed chunks to M0 under a clean exit-0 completion, and is loud only at the next B4 (via the still-dangling M1 intent) — exactly the "loud only at the next B4" deficiency the guard was built to kill; the handoff's "the M0-revert writer is dead" is overbroad. (b) **Wedge:** plain M0 EXECUTE crashes → operator runs an M1 delta EXECUTE (delta mode consults NO ledger — `repairRealityScan` is repair-only, the plain-guard is skipped when `--deltaDir` is set) → any later `--resume` of the M0 run now FATALs on the plain-guard → M0's intent dangles FOREVER → every future B4 FATALs "intent without completion (crash mid-run? resume it)" while the resume it instructs is forbidden. Fail-closed both ways, but a permanent gate brick with no sanctioned recovery on a reachable stage-2 sequence. Fix: the guard consumes `parseLedgerStrict` (FATAL when `appliedLayerShas` is nonempty OR any dangling intent carries a delta sha — one law, THREE consumers), and delta-mode EXECUTE refuses over `red.problems` (forcing resume-first, which kills the wedge); one lap case each way.

**N2 — MEDIUM (packet accuracy + narrow admission): the handoff's EPERM sentence is false against its own formula.**
Handoff A3: "alive **or EPERM-unverifiable holders are never stolen**" — but the implemented staleness (`b3:261-264`) sets `unverifiable` on EPERM, and `aged ∧ unverifiable ⇒ stale ⇒ rename-claimed`. An aged (>2h) live holder running as a different user (EPERM on `kill(pid,0)`) IS stolen — a constructible second-writer admission (cross-user ops, e.g. one run under sudo), and the r65 custody review had explicitly praised the old EPERM-refuses behavior. The code implements the r65 panel's own suggested formula, so the behavior is defensible — the CLAIM is not. Correct the sentence to the formula (or refuse EPERM at any age and make the sentence true).

**N3 — LOW-MEDIUM (code, racy): an unreadable lease is INSTANTLY stale — the 2h age gate defaults open.**
`b3:260,:263`: parse failure ⇒ `holder=null` ⇒ `unverifiable=true` AND `aged=true` (the `holder ? … : true` default) ⇒ stale with **no age wait**. A reader that hits the microsecond window between a legit holder's `openSync(wx)` and `writeSync` (`b3:251`) sees an empty file and immediately rename-claims a LIVE just-created lease; both then EXECUTE. This is the exact "wx-then-write gap … same family" case r65 custody D2's parenthetical named; rename-claim fixed the two-claimant race but not this. Fix: derive age from `statSync(...).mtimeMs` when the lease is unparseable (fresh-but-unreadable ⇒ refuse; old-and-unreadable ⇒ stale, which keeps crash recovery).

**N4 — LOW-MEDIUM (doc qualifier): "movement can ONLY come from events in (layerWatermark, flip)" is overstated.**
Handoff A2 and `14_:111` claim provenance-by-construction. A **post-flip in-place mutation of a pre-flip attempt** (raw score/rows edit without the preOverride law — a corruption class, not a legit flow) also moves the flip expectation while disk ≡ layer ⇒ classified `preFlipTail`, informational, PASS. Bounded: the student is simultaneously flagged `adjudicationChanged`/digest-moved and the tail rows are published — signal exists, verdict doesn't block. The determinism argument holds only over an append-only attempt corpus; one sentence should say so ("absent in-place mutation of pre-flip attempts, which publishes as adjudicationChanged").

**N5 — LOW-MEDIUM (letter, inside ratified text): `14_:113` still states the marker-ONLY stamping law.**
"The live label writers stamp ONLY when … firstEnabledAt is SET" — unqualified, two lines before the qualified "ZERO live label writers OUTSIDE rehearsalClassIds" (`14_:115`), inside the very R2-48 block David ratified. Under the ratified predicate a rehearsal class stamps with the marker ABSENT. This is the surviving half of r65 D5 (which named this exact sentence); the handoff's "14_'s two zero-writers sentences carry the carve-out qualifier" is inaccurate for it. One-sentence fold.

**N6 — LOW-MEDIUM (letter): `16_:72-73` contradicts the new DRILL-E law.**
Item 8 still says "drill E uses shadow-class-scoped reviewGateEnabled **+ rehearsalClassIds removal** only", while the r66 DRILL-E LAW (`16_:88-94`) says the class REMAINS registered throughout and removal is a separate DRAIN-gated teardown. An operator following item 8 disables the writers the drill certifies — Codex A4's exact trip, surviving one screen above its fix.

**N7 — packet accuracy (the CARRIED list is not "the entire known residue").**
Four known-open r65 items are absent from CARRIED: the card's mixed-(d) REQUIRED case (neither executed nor re-scoped); the lap-lock TOCTOU (custody D2's named second half); the red-lap unconditional overwrite of the tracked evidence artifact (evidence NEW-4); the contaminated `b1-baseline-pointer-full/-delta` files (evidence NEW-3). None is freeze-heavy alone; the framing "the CARRIED list above is the entire known residue" is what fails. Additionally minor: "the closed-enum is now a real validation" — it is a counted census + inert-as-rejected treatment (`b1-replay-lib:95-96`), not a validation that rejects; the parenthetical states the truth, the label oversells. Fixture stage 9c line 359 (`(ePre.mutationRisk ? 1 : 1) === 1 && true`) is a tautological check — one of the 61 can never fail.

## (3) STAGE-1 FREEZE: **NO** — narrowly, and cheaply curable

What I attacked and could NOT break: the adjudication-reality law end to end (including the production-shaped legacy fixture), the movement conjunct both directions, the live-lease refusal at any age, claim-by-rename, token-conditional release, the Firestore activation barrier, the shared strict reducer in B4 + repair, the durable-marker B3 guard, and the R2-48/R2-49 minting. The r65 closure is real, and the CARRIED items genuinely don't block (aggregate evidence, two-process negative, crash read-backs, first-append→stage-2, per-original lease scope — all honest, all bounded).

What blocks: **(i)** the freeze-cited 66/0 artifact does not satisfy the packet's OWN evidence-binding law — the harness bytes are unbound (§0), on a round whose r64/r65 precedent made evidence identity freeze material; **(ii)** N1 — the plain-guard, a headline r66 closure, has a constructible bypass and creates an unrecoverable B4 wedge on a reachable crash sequence; **(iii)** N2/N7 — the handoff again contains false claims of the exact class that sank r64/r65 (EPERM sentence, "M0-revert writer is dead", CARRIED completeness); **(iv)** N5/N6 — two stale letters inside ratified/consumed text. Minimal falsifiable set: (1) one solo lap on the committed tree, republished with 8/8 sha16 ≡ tree; (2) plain-guard + delta-mode consume `parseLedgerStrict` (fatal on applied layers ∨ delta-bearing dangling intents; delta refuses over problems) + one case each way; (3) the two one-sentence folds (14_:113, 16_:72-73); (4) packet corrections (EPERM law as implemented; guard claim scoped; CARRIED completed with the four omissions); (5) cheap hygiene: statSync-mtime age for unparseable leases, rename-claim on the lap lock, mixed-(d) executed or re-scoped. Hours-to-days; nothing reopens law substance. On completion, freeze-YES follows on my lens.

## (4) The two ratified laws as MINTED

**R2-49 (legacy reconstruction) — MINTED CLEAN.** The 11_ row (David's words, 2026-08-02, option 1) matches the implementation exactly: preimage preferred, legacy accepted reconstructed GRADED-WRONG, over-count direction stated honestly, census published (`b1-replay-lib:99-101` `legacyAcceptedReconstructed`), forward-path §6b preimage duty in 15_:186 + carded. Fixture stage 9c + the lap's flipped+recomputed case execute exactly Codex's demanded disposition. No mismatch found.

**R2-48 (dark-window custody) — MINTED VALID; two letter nits ride on it.** The row's operative clauses all correspond to real artifacts: predicate = 15_ §7 = 02_ flip clause; one-txn `{enabled:true, firstEnabledAt}` write-IFF-absent carded with the post-flip asserts (marker present, `rehearsalClassIds` empty, no window doc, micro-lap); R2-32 scoped in its own 11_ row; "B3 FORBIDDEN from the marker onward (durable guard, lap-proven)" is true (`b3:122-127`; kill-switch-window case `lap:242-245`). Nits: (a) the row's tail parenthetical "heals through live use" carries no fc qualifier while the 14_ §4 it cites now says pure-tail fc deficits do NOT heal (published permanent undercount) — the ratified parenthetical preserves the pre-honesty letter; since it touches ratified text, fold it as an annotation with David's nod rather than a silent edit; (b) `14_:113`'s unqualified sentence (N5) sits inside the R2-48 block in 14_. Neither undermines the mint — the row itself contains the carve-out and the correct predicate. Process note: both rows carry UNCOMMITTED "dual-attested" receipt additions in the working tree (content benign, receipts only) — the review ran against a dirty ratification doc, the same N9-family process residue the r65 panel flagged about mid-panel commits.