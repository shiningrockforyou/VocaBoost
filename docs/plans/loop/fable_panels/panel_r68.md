# 3-Fable panel — DEEPFIX2 r68 (2026-08-02)


---

## LENS: custody

# DEEPFIX2 r68 — panel review. LENS: the admission gate + overtake law as one machine

**Basis (measured on this tree; HEAD moved 83fafa7 → 15fe0f8 mid-review as the orchestrator committed the r68 fold — all bytes re-verified against BOTH):** solo-lap constraint honored — `pgrep -f b-emulator-lap` matched only my own probe shell; I took the verification seat and did NOT re-run the lap. Executed: `delta-chain-fixture.mjs` **81/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` clean 10/10 · **evidence sha16 recomputed myself: 8/8 MATCH** (worktree AND committed 15fe0f8) · frozen-bytes discipline proven: max bound-source mtime 07:19:19Z < artifact write 07:36:50Z; artifact `checks:82` ≡ 82 lap `ok()` sites (71 + 9 overtake + 2 mixed) · **the handoff's diff-arithmetic claim verified EXACT**: `git diff 83fafa7..15fe0f8` over scripts+docs/deepfix2 = 11 files, +182 −76 · offline probes (scratchpad only, repo untouched): reducer counter-shape battery, completion-without-intent, prefix-runId filter semantics, overtake-anchor positions. Full read: b3-backfill-writer (gate/lease/cutover/manifest-order regions), b-baseline (reducer v3 + assessLease), b4-verify, b1-replay-lib, lap, fixture stages 9f/9g/9h, 14_/16_/02_ folds, r67 Codex review + full r67 panel, baton (round 68, rev 209). Codex's already-landed r68 review was NOT read — independence preserved.

## (1) r67 findings — CLOSED / MISS

| Finding | Verdict | Evidence (file:line) |
|---|---|---|
| **Codex A1 — stale plain resume overwrites a later delta** | **CLOSED — both halves, lap-executed in Codex's exact order** | THE ADMISSION GATE: every EXECUTE (plain/delta/repair/resume — the gate sits inside `if (EXECUTE …)` with no mode condition, under the lease) consumes `parseLedgerStrict`; any foreign unresolved latest attempt ⇒ exit 2 (`b3-backfill-writer.mjs:291-296`). THE OVERTAKE LAW: a RESUME with its own first intent preceding any other run's non-cutover applied ⇒ exit 2 OVERTAKEN (`:297-302`). The seven-step lap sequence matches Codex's demanded asserts verbatim: M1 admission refused while ovtP dangles (`b-emulator-lap.mjs:418`), heal-then-execute, stale resume refused (`:424`), final B4 PASS (`:426`). Anchoring on the FIRST intent (findIndex) is the conservative correct choice — I could construct no false-refusal that isn't a harmless refusal of a redundant/superseded resume. Foreign-problems filtering is SAFE: runId charset `[A-Za-z0-9._-]{4,64}` (`b3:55`) forbids spaces, the filter appends `" "`, probe confirms `run1` vs `run10` stays foreign; runId REUSE is blocked pre-gate by single-use (`b3:221-228`). No EXECUTE path bypasses the gate (b3 has no `ignoreLedger`; non-EXECUTE writes nothing; B4's orphan relaxation exists only under `--postFlip` and B3's gate parses WITHOUT it). Residue: NEW-1 below (pre-intent anchor gap) — a corner of the implemented proof, not a reopening of either executed half. |
| **Codex A2 — truthiness-accepted `cutoverAborted`** | **CLOSED, with one honest deviation** | Exact schema: `"cutoverAborted" in o && o.cutoverAborted !== true` THROWS; a cutover completion must have its intent (same key) and matching delta sha (`b-baseline.mjs:198-204`); applied-set exclusion is `=== true`-exact (`:233`). Fixture 9f: string dies, number dies, sha-mismatch dies, real-counts-surface, postFlip orphan (`delta-chain-fixture.mjs:397-410`). Deviation: Codex demanded a ZERO-counter terminal contract; the r67 panel (cutover NEW-3) demanded the opposite — REAL counts, never fabricated zeros. The two conflicted; r68 sided with the panel (counts surface via `cutoverRuns`, `b-baseline.mjs:224-229`) and the handoff says so plainly. Right call — a forensic ledger must not contain fabricated zeros — but recorded here as a deviation, not silent compliance. Residual shape hole in the NON-cutover counters: NEW-3. |
| **Codex B1 — lease letter ≠ code** | **CLOSED at the law level; one wiring defect (NEW-2)** | `assessLease`: unparseable follows the aged law via file mtime, fresh-unparseable protected (`b-baseline.mjs:245-248`); pid must be a POSITIVE integer (`:249`, also closes cutover NEW-5 — pid 0/-1 no longer probe as alive-forever). b3 passes the lease mtime (`b3:270-271`). Fixture 9h: fresh-unparseable protected / aged-unparseable stale / pid 0 fresh-protected / aged pid -1 stale (`delta-chain-fixture.mjs:414-419`). The ABA reap-verify (panel cutover NEW-4) restores a live successor's lease on token mismatch (`b3:275-280`). BUT the verify re-parses the reaped bytes — see NEW-2: the aged-UNPARSEABLE class it just legalized always fatals its first takeover. |
| **Codex B2 — contaminated delta pointer** | **CLOSED** | `docs/plans/deepfix2/evidence/b1-baseline-pointer-delta.json` deleted in 15fe0f8; `evidence/` now contains no pointer files of the r65 class (verified by listing). |
| **Panel: crash-then-flip brick (cutover NEW-2)** | **CLOSED** | Reducer: under `postFlip`, a dangling intent = published ORPHAN, never a problem; pre-flip it stays blocking (`b-baseline.mjs:214-221`). B4 stashes and publishes `flipOrphanedRuns` (`b4-verify.mjs:70-71,127`). Fixture 9f asserts the orphan branch. B3's gate parses without `postFlip`, so orphans can never grease an admission. Nit: no end-to-end postFlip-orphan LAP case (fixture + shared-settle-machinery coverage only; the settle path is the identical code the r67 cutover lap executed). |
| **Panel: fabricated zeros (cutover NEW-3)** | **CLOSED** | Real `stats` in the terminal outcome (`b3:486-489`); the exit-6 path now writes the result file (tmp+rename, `:491-494`) carrying `resetLockedList`/`epochDriftSkippedList` — skipped students are durably recorded. |
| **Panel: three-conjunct letter + provenance qualifier (asof NEW-1/NEW-2, freeze NEW-1)** | **CLOSED** | `14_:111` states ALL THREE conjuncts, the QUIET same-field clause, and the dup/epoch retroactive-input PROVENANCE QUALIFIER (delta-flagged, exclusion counts published, minutes-bounded). Dead ternary gone (`b4:225` one expression). The mixed-same-field case executes BOTH directions: tail fail + lost post-flip fail on ONE word ⇒ exit 5 (`lap:429-437`); exact live labels ⇒ PASS (`lap:443`). |
| **Panel: census units (asof NEW-3/NEW-4)** | **CLOSED** | B4 counts ONCE — the cutoff replay gets `{}` (`b4:177`), boundary replay keeps the counters; `replayExclusions` published (`b4:126,129` — dupConflictGroup growth at the gate is now visible). Fixture 9g: `acceptedNoTimestamp` exactly 1, `challengeStatusUnknownEnum` exactly 1 (`delta-chain-fixture.mjs:405-412` region). `computeStudentLabels` tolerates `{}` (`b1-replay-lib.mjs:15-16`). |
| **Panel: edited-attempt pin (asof NEW-7)** | **CLOSED** | `asOfScore = stored` for edited attempts — `(hasPostBoundaryAccept && !a.edited)` (`b1-replay-lib.mjs:146`; `edited` defined `:68`, rides the group at `:119`). The teacher-edit writer's per-row preimage duty is carded on the 02_ DF2-14 stamping-duty line. |
| **Panel: 16_ drill-E (custody N6)** | **CLOSED** | `16_:73`: "reviewGateEnabled ONLY — the class REMAINS in `rehearsalClassIds` per the DRILL-E LAW below [r68]". |
| **Panel: cutover-joiner wording (cutover NEW-6)** | **CLOSED** | `14_:111` adds the layer-only-joiner = `uncoveredAtGate` BLOCKER sentence. |
| **Panel: packet accuracy (asof NEW-6, freeze NEW-4v)** | **CLOSED** | The two r67 phantom lines are acknowledged in the handoff, and its "written against git diff — 11 files, 182+/76−" claim verified EXACT (+182 −76 computed). First round in this workstream where the packet's arithmetic self-claim checked out to the digit. |
| **CARRIED completeness** | **PARTIAL — nit** | The six listed items are honest. The mixed-(d) card case (named open-and-unlisted by two r67 lenses) is STILL neither executed as mixed nor re-scoped nor listed. Recurring omission, letter-level. |

## (2) NEW defects

**NEW-1 (MEDIUM-LOW — the lens headline): the overtake proof anchors on the run's ledger intent; a run that crashed in the manifest→intent window has NO anchor, and its resume skips the proof — the M1→M0 revert survives through that window.**
The run manifest publishes at `b3:431-432`; the intent appends at `:448` (`crashPoint("pre-intent")` `:447` — the lap's crash1 executes this point and asserts "no ledger record before intent — rerun is legal", `lap:191-194`, without resuming it). A death in that statement window (host crash, or `ledgerAppend` itself throwing on ENOSPC) leaves a run with a published manifest but zero ledger presence. Sequence: plain pX crashes pre-intent → M1 delta is ADMITTED (pX is invisible — no foreign problem; probed: `ownIntentIdx = -1`) → M1 applies → `--runId=pX --resume` passes the marker guard, single-use (RESUME-exempt), manifest binding, and foreignProblems; `findIndex` returns −1 so the overtake check is SKIPPED (`b3:298`); the plain guard is RESUME-exempt → phase 1 re-plans live vs M0 → phase 2 reverts every M1 student, exit 0. Loud only at the next B4 (exit 5). Mitigations, honestly weighed: the window is milliseconds (phase 1, the long part, precedes the manifest); unlike r67-A1 the destructive step is NOT tool-instructed — the ledger holds nothing and B4 never says "resume it", so no runbook path leads there; a pre-intent run wrote NOTHING, so refusing costs nothing. Fix is two lines inside the gate's RESUME branch: `ownIntentIdx === -1` ∧ any foreign applied (or `red.appliedLayerShas.size`) ⇒ REFUSE ("no custody anchor — superseded; start fresh under a new runId"), + one lap case (pre-intent crash → delta → resume REFUSED). Fail-closed by the gate's own principle.

**NEW-2 (LOW): the ABA reap-verify makes every first takeover of an aged-UNPARSEABLE lease FATAL — the exact class B1's fix just legalized.**
`b3:275-280`: after the rename, the verify does `JSON.parse(readFileSync(reaped))`; for garbage bytes the parse throws SyntaxError (no `.code`) → the catch's `e.code !== "ENOENT"` arm exits 2 — without restore, without `takeLease`, with the garbage lease left renamed away. Net: the takeover the law now permits always fatals once (confusing message, orphaned `.reaped-*` file), then a retry succeeds against the cleared path. Fail-closed, self-healing, no two-writer hazard — but the handoff's "unparseable leases follow the aged law" is true only of `assessLease`; the wiring contradicts it, and fixture 9h covers only the pure law. Also: (i) the ENOENT branch proceeds-without-verification — unreachable in-law (the reaped name is pid+timestamp-unique and nothing touches it), but the right response to "cannot verify what I reaped" is stand-down; (ii) if the ABA-mismatch restore rename itself fails (EPERM), the live successor's lease stays stolen while we exit — exotic. Fix: when the assessed holder was unparseable, verify by raw-byte equality instead of token; stand down on ENOENT.

**NEW-3 (LOW-MEDIUM by this round's own A2 principle): reducer v3's counter fields remain JS-coerced — a completion with non-numeric counters reads as CLEAN.**
`b-baseline.mjs:230`: `(o.txnFailures || 0) + …`. Probed: `txnFailures: []`, `{}`, `"abc"`, `null`, `false` each yield `problems=[]` — the same failed→clean conversion Codex blocked for `cutoverAborted`, one field over ("the reducer is the corruption/schema boundary; it cannot delegate correctness to its producer"). Reachability is out-of-law bytes only (producers write numbers; torn lines fail `JSON.parse` and THROW) — the same reachability as A2's reproduction. Narrower than A2 (counters feed only the failure check; nothing branches into the applied set on them). Fix: integer-validate the three counters when present (throw on non-integer/negative), one fixture row.

**NEW-4 (NIT, defense-in-depth): the overtake predicate's `!r.cutoverAborted` exclusion is wrong-if-reachable.** A cutover-aborted run's COMMITTED chunks are real pre-flip writes; a resume of an older run past it would revert them. Unreachable in-law: `cutoverAborted` ⇒ `firstEnabledAt` set ⇒ the durable marker guard (`b3:124-127` — checks the marker, not the resettable flag) refuses every B3 including that resume, before the gate. Drop the exclusion (it can never save a legitimate resume) or comment the marker-shadowing.

**NEW-5 (NIT, pre-existing): a completion-without-intent is accepted as an applied layer.** Probed: a single `b3-applied` line with no intent → `problems=[]`, `appliedLayerShas=['L1']`. Not an r68 regression — the new intent-binding covers cutover completions only; in-law appends are always intent-first. Fold `intents.has(key)` for every completion when NEW-3 lands.

## (3) STAGE-1 FREEZE: **YES — with a mandatory fold list**

**What I attacked and could not break:** the admission gate covers all four EXECUTE shapes under the lease with no bypass flag; the foreign-problems filter is collision-proof (charset + trailing space + single-use runIds — the lens's named attack is dead); first-intent anchoring is conservative-correct; the seven-step overtake sequence is lap-executed to Codex's letter (82/0 on bytes I hash-verified twice, before and after the mid-review commit, with the mtime timeline proving frozen bytes); truthy terminal shapes die with fixture negatives; the orphan settle cannot leak into B3 admission; the lease law matches its letter under fixture; the packet's own claims — including its diff arithmetic — verified true to the digit, a first for this workstream.

**Why the residue does not block:** NEW-1 is a genuine hole but materially unlike r67-A1: reachable only through a millisecond, zero-writes crash window; never tool-instructed (the ledger is empty of the run — nothing ever says "resume it"); ends loud at the next B4; fixed by a two-line fail-closed refusal that costs nothing. NEW-2 is fail-closed and self-healing. NEW-3 requires hand-forged ledger bytes and feeds only the failure check. NEW-4/NEW-5 are marker-shadowed/pre-existing. None reopens a ratified law, an executed case, or any letter this round rewrote. Per the charter I do not manufacture blockers from these.

**The mandatory fold list (order matters; items 1–3 change b3/b-baseline bytes ⇒ one solo lap re-run + republish 8/8 from the folded bytes):**
1. **NEW-1**: the no-anchor resume refusal (2 lines in the RESUME branch) + one lap case (pre-intent crash → delta applied → resume REFUSED). Must land BEFORE any real-cohort B3 EXECUTE — resume-after-crash is precisely what live operations will exercise.
2. **NEW-3**: integer-validate the reducer's three counters + one fixture negative.
3. **NEW-2**: byte-equality verify for unparseable reaped leases; stand down on ENOENT.
4. Nits at leisure: drop/comment the `!cutoverAborted` overtake exclusion; intent-required-for-every-completion; a postFlip-orphan lap case and mixed-(d) into the carded stage-2 emulator matrix; add mixed-(d) to CARRIED until then.

**Stage-2 opening order on YES:** the handoff's standing order holds — B1 --full (read-only; safe ahead of the folds) → dark build with the DF2-10 duties (now incl. the teacher-edit preimage duty) → emulator matrix (inherits first-append/torn/two-process + the nit-4 cases) → dark deploy → 25WT rehearsal → shadow audit — with fold item 1 landed before the 26SM B3 EXECUTE leg.

**Bottom line:** the r67 blocking set is closed in substance and in execution, the evidence identity held under independent recomputation through a mid-review commit, and the packet told the truth about itself. Freeze stage 1, fold the five items into the freeze record, re-lap once on the folded bytes, and open stage 2.

---

## LENS: gate

All verification complete. Composing the verdict.

# DEEPFIX2 r68 — panel review. LENS: the post-flip gate final form

**Basis (measured on this working tree over HEAD `83fafa7`; the r68 delta is uncommitted):** `git diff` scoped to `docs/plans/deepfix2/ + scripts/deepfix2/` = **11 files, 182+/76−, exactly as the handoff states**, and every hunk maps to a handoff claim (no smuggled changes; the extra 4 files in the full stat are batons/settings/change-log). Executed: `delta-chain-fixture.mjs` **81/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` clean on all deepfix2 scripts. **Lap seat: NOT re-run** — my `pgrep -f b-emulator-lap` hits were my own probe shells (ps cross-check found no real runner), but with three uncoordinated reviewers I took the verification seat: **evidence sha16 recomputed by me — 8/8 MATCH** against current bytes (`b-baseline 96e1cd170ba6155c`, `b3 a22366b450a6a7d2`, `b4 77ee0c7abbbb5923`, `lap ea5a2d0228c65d44`, others unchanged-and-matching); every bound source mtime (latest `b4-verify.mjs` 07:19:19Z) precedes the artifact write 07:36:50Z; artifact checks=82 ≡ 71 prior ok() sites + 11 new (9 overtake + 2 mixed) counted in the diff. Full read: b4-verify, b-baseline (reducer+lease), b3-backfill-writer (lease/admission/cutover regions), b1-replay-lib, lap new blocks + helpers + seed, fixture 9e-9h, 14_/16_/02_ diffs, r67 Codex review + full panel.

## (1) r67 findings — CLOSED / MISS

| Finding | Verdict | Evidence (file:line) |
|---|---|---|
| **Codex A1 (overtake/admission)** | **CLOSED — both halves, executed** | Admission gate is mode-unconditional under the lease: every EXECUTE consumes `parseLedgerStrict` and refuses foreign unresolved runs (`b3-backfill-writer.mjs:291-296`, after lease acquisition `:257-280` — no scan-to-lease gap). Overtake law: a RESUME whose own intent precedes any foreign non-cutover applied completion in ledger order is REFUSED (`:297-303`, using the new `ordered` custody array `b-baseline.mjs:181,206`). Lap runs Codex's exact seven-step in his exact order (`b-emulator-lap.mjs:405-427`): M0 clean → layer materialized+built → plain crash post-intent → **delta admission REFUSED** (asserted on "unresolved runs") → resume heals pre-layer → delta executes → **stale resume REFUSED** (asserted on "OVERTAKEN") → final B4 PASS. I verified `freshManifest()` is one fixed path (`lap:98`) so all runs share the original sha — the refusals are genuine, not per-original filtering artifacts. No bypass exists: B3 has no `--ignoreLedger` (`b3:44`), crash hooks are emulator-only (`b3:57`). |
| **Codex A2 (terminal schema)** | **CLOSED** | Exact boolean: `"cutoverAborted" in o && o.cutoverAborted !== true` THROWS (`b-baseline.mjs:198`); completion binds to its own intent — same runId#attempt key + same delta sha, mismatch/missing-intent THROWS (`:199-203`). Fixture 9f: string truthy, numeric truthy, sha-mismatch all die; real pre-abort counts surface via `cutoverRuns` (`:222-227`, fixture asserts `txnFailures===2` surfaces); postFlip-orphan branch asserted (`delta-chain-fixture.mjs:397-411`). Codex's "require zero counters" clause was superseded by panel NEW-3's real-counts law — the correct reconciliation; the fail-open truthiness (the defect) is dead. B3 now writes REAL stats into the terminal outcome (`b3:490`) and publishes the result file on exit 6 (`b3:493-496`). |
| **Codex B1 (lease letter)** | **CLOSED** | Unparseable leases follow the aged law from file mtime (`b-baseline.mjs:241-243`; B3 plumbs `statSync().mtimeMs` at `b3:267`); fresh-unparseable protected, aged-unparseable stale, no-mtime conservative-stale — all fixture-asserted (9h, `fixture:427-433`). pid must be a positive integer (`b-baseline.mjs:246`; pid 0/-1 branches in 9h). ABA verify-after-rename with restore on token mismatch (`b3:272-277`) — the r67 cutover NEW-4 two-claimant race is closed. Residues below (nits 4a/4b). |
| **Codex B2 (delta pointer)** | **CLOSED** | `docs/plans/deepfix2/evidence/b1-baseline-pointer-delta.json` deleted in the working tree (git status `deleted:`; absent from the evidence dir). The r67 plural claim is now actually true. |
| **Codex B3 (census units)** | **CLOSED** | B4 counts census ONCE — boundary replay only (`b4-verify.mjs:148`); the cutoff replay passes `{}` (`:177`) and the fc-retry passes `{}` (`:234`); the lib tolerates `{}` via noop fallbacks (`b1-replay-lib.mjs:15-16`), which the green postFlip lap cases execute. |
| **Panel: crash-then-flip brick (cutover NEW-2)** | **CLOSED** | Under `{postFlip:true}` the reducer converts dangling intents to published ORPHANS, never problems (`b-baseline.mjs:214-218`); pre-flip they stay blocking. B4 threads the opt (`b4:67`), NOTEs and publishes `stats.flipOrphanedRuns` (`:74,:127`) into report + stdout. Settlement traced (my lens core — see below): fail-closed in every invocation shape. Execution is fixture-level (reducer) only — nit 2. |
| **Panel: fabricated zeros (cutover NEW-3)** | **CLOSED** | Real accumulated `stats.txnFailures/skipped*` in the terminal outcome (`b3:490`), `cutoverRuns` surfaces them (`b-baseline.mjs:225`), result file written pre-exit-6 (`b3:493-496`). Fixture 9f asserts surfacing. |
| **Panel: three-conjunct letter + provenance qualifier (asof NEW-1/NEW-2, freeze NEW-1)** | **CLOSED** | `14_:111` now states ALL THREE conjuncts (MOVED ∧ QUIET-same-field ∧ disk≡layer) + the PROVENANCE QUALIFIER naming the dup-divergence and post-flip-reset retroactive-input doors, delta-flagged + exclusion-published + minutes-bounded. Code unchanged-correct (`b4:222-229`); dead ternary removed (`:225` one expression). **The blocking direction now EXECUTES**: lap mixed-same-field case (`lap:429-443`) — tail fail (flip fc 1→2) + lost post-flip fail (cutoff 3) on ONE word w3 ⇒ exit 5, then exact live labels ⇒ PASS. I verified load-bearing-ness: under the two-conjunct law this case is excused (moved ∧ disk(1)≡layer(1)) → false green; only quiet(3≠2) blocks it. Seed arithmetic checked (`lap:94-95`: b2 gives layer fc=1; `attAbsT` writes attempts only, never labels — disk stays at B3's write). |
| **Panel: census units/execution (asof NEW-3/NEW-4)** | **CLOSED** | Double-count dead (count-once above). `acceptedNoTimestamp` and `challengeStatusUnknownEnum` have exact-count fixture rows (9g, `fixture:413-424` — one timestamp-less accepted row, one "banana" status ⇒ exactly 1 each; I traced both notes fire before any exclusion bump). `replayExclusions` published from the boundary replay's real bump (`b4:126,:129`) — all 13 EXCL_KEYS reasons funnel through it, so the dup/epoch doors instrument as `dupConflictGroup`/`preEpoch` growth. |
| **Panel: edited-attempt pin (asof NEW-7)** | **CLOSED** | `asOfScore = stored` when `a.edited` (`b1-replay-lib.mjs:146`, `edited = a.teacherEdited === true` rides at `:68,:119`); `stored` = `preOverride.score` for edited attempts (`:69`) — boundary-invariant, B1-Q3 honored. The teacher-edit writer's per-row preimage duty is on the 02_ DF2-14 card (STAMPING-PREDICATE DUTY sentence, with the interim pin stated). I checked for a NEW retroactive door here: none — the pin substitutes a boundary-invariant value both directions of the edit time. |
| **16_ drill-E (custody N6, the real r66 item)** | **CLOSED** | `16_:73`: "the class REMAINS in `rehearsalClassIds` per the DRILL-E LAW below" — now consistent with the law at `16_:88-94`. |
| **Cutover-joiner wording (cutover NEW-6)** | **CLOSED** | `14_:111`: "a LAYER-ONLY JOINER of a cutover-aborted layer surfaces as an `uncoveredAtGate` BLOCKER, not tail [r68]" — matches the mechanics (`b-baseline.mjs:230` excludes the layer; `b4:176` lists + diffs block). |
| **freeze NEW-4 nits (i)-(v)** | **CLOSED/CARRIED** | Ternary ✓, double-count ✓, gitHead honestly CARRIED (new artifact records `83fafa7` while the run used the dirty tree — sha16s carry the binding, verified), edited pin ✓, phantom-line class acknowledged in the handoff's own packet-discipline note. |
| **Mixed-(d) card case (custody N7 → all three r67 lenses)** | **MISS — the one unfolded union item** | Lap case (d) is still pure-structural with no concurrent actionable delta (`lap:150-164` unchanged); the 02_ card letter still says "a MIXED unrelated structural diff"; not re-scoped, and absent from the r68 CARRIED list. Thrice-ruled non-blocking-alone; stays a nit. |

## (2) NEW defects — my lens attacks, with concrete scenarios

**Traced and HELD (no blocker found):** the orphan settlement final form. Partially-committed flip-orphaned delta M1, layer omitted from `--appliedDelta` (the ledger's own instruction — no completion ⇒ not in `appliedLayerShas`): written students value-match the flip replay (clean); unwritten students classify `preFlipTail` via lay=M0 (informational). Layer passed anyway (legal — `b4:71` checks only ledgered-not-passed): unwritten students fail conjunct 1 or 3 ⇒ blocking DIFFS. Partial-completion variant (failed attempt 0 + dangling resume intent): layer sha IS ledgered (`b-baseline.mjs:230` iterates all applieds) ⇒ chain required ⇒ unwritten remainder blocks. Every shape is fail-closed; no false green exists. Orphan/cutoverRuns lists are bounded by run count, not students — no cap needed.

**NEW-1 (LOW, execution coverage — my lens's publication surfaces): the two r68 gate-publication paths have no executed case.** (a) B4's orphan wiring — the `{postFlip}` opt thread (`b4:67`), the NOTE (`:74`), `stats.flipOrphanedRuns` (`:127`) — is executed nowhere; fixture 9f stops at the reducer. The lap's cutover case exercises the cutoverAborted TERMINAL path, never a dangling-intent-at-the-gate. (b) `replayExclusions` (`b4:126,129`) is published but asserted nowhere (no fixture/lap hit). Both fail SAFE if broken (the gate would fatal as in r67 = loud; an honesty counter would sit empty), which is why this is a nit, not a blocker. One lap case (crash post-intent → flip → `B4 --postFlip` asserts the NOTE + settlement) fits the carded stage-2 matrix.

**NEW-2 (LOW, overtake blind spot bounded by a CARRIED item):** `b3:298-299` skips the overtake check when the resume's own intent is absent (`ownIntentIdx >= 0` guard). Scenario: the first-append/dir-fsync loss window (the CARRIED stage-2 item) eats the ledger file after P's intent; M1 executes on the re-created ledger; P `--resume` — manifest exists, foreign problems none, own intent absent ⇒ overtake check no-ops ⇒ the M0-revert returns. Precondition is exactly the carried crash-loss window, so it inherits that boundary; the two-token hardening (refuse RESUME when own intent is missing but foreign applieds exist) belongs in the stage-2 fold.

**NEW-3 (NIT, ABA residue — same class as the CARRIED two-process negative):** (a) an aged-UNPARSEABLE lease — the exact case r68 legalized for reaping — FATALs at the verify step every time (`b3:275` `JSON.parse` throws, no `.code` ⇒ `:277` exit 2, no restore, no takeLease): the reap completes only on the NEXT invocation (lease path left empty — fail-closed, self-healing, but "aged-unparseable is reapable" executes as a two-invocation flow). (b) The mismatch-restore `renameSync(reaped, execLeaseUrl)` (`:276`) clobbers a THIRD claimant's fresh lease if one took the empty path in the rename-to-restore window ⇒ two live writers. Needs three near-simultaneous claimants over a stale lease — the multi-process race class the CARRIED item explicitly defers.

**NEW-4 (NIT, doc):** the partial-commit orphan asymmetry above (written student + tail event + layer omitted ⇒ blocking DIFF; same student with the orphaned layer passed ⇒ informational tail) is undocumented — `14_:111` documents only the joiner corner. One operator-law sentence ("an orphaned layer MAY be passed to --appliedDelta for settlement") removes the surprise. Fail-closed both ways today.

**NEW-5 (COSMETIC):** `foreignProblems` filters by string prefix `${RUNID} ` (`b3:295`) with no runId charset validation — structured problem objects would be cleaner. Lap-lock creation is still wx-then-write (`lap:46-47`; takeover is rename-claim). `14_:122-123` roster leftover stands (ruled LOW r67). The r68 CARRIED list omits mixed-(d) (the §1 MISS).

## (3) STAGE-1 FREEZE: **YES — with the nit list**

**What I attacked and could not break:** the orphan settlement is fail-closed in every invocation shape I could construct (written/unwritten × layer passed/omitted × partial-completion variant); the admission+overtake pair closes the M0-revert twice over and the lap executes Codex's sequence verbatim; the terminal schema is exact, intent-bound, and negative-tested; the quiet conjunct's blocking direction is finally load-bearing in an executed case whose arithmetic I traced against the seed; the census is single-count with both remaining counters exact-asserted; every retroactive-input door I could find (dup divergence, post-flip reset, `editedNoOrganicScore`, in-place corruption) funnels through a published `replayExclusions` bump AND a delta flag — the 14_ qualifier's generic clause covers even the unnamed instances; the edited pin introduces no new door. Evidence: 8/8 computed by me on bytes that predate the artifact; 82 = 71 + exactly the 11 new asserts; 81/2,688 reproduced. **Packet accuracy: every load-bearing handoff claim verified true against the diff — zero false claims for the first time in the workstream's r64-r67 run — including the self-acknowledged phantom-line correction.**

**Why YES despite the residue:** the surviving items are one unfolded thrice-ruled card nit (mixed-(d)), unexecuted fail-safe publication wiring, a blind spot gated behind an already-CARRIED crash window, ABA corners of the already-CARRIED multi-process class, and cosmetics. None is a false green, an authority hole, or a letter/code contradiction — the three classes that justified NO in r64-r67. Manufacturing a blocker from this set would violate the charter.

**The nit list (fold into the freeze record; none reopens law substance):**
1. Mixed-(d): execute as truly-mixed, re-scope the card letter, or add to CARRIED — the sole card-letter gap (8.5/9).
2. One crash-then-flip lap case asserting `flipOrphanedRuns` + a `replayExclusions` assert (stage-2 emulator matrix).
3. Overtake hardening: refuse RESUME when own intent is absent but foreign applieds exist (`b3:299`).
4. ABA: byte-compare fallback for unparseable reaped content; link-not-rename (or occupancy check) on restore.
5. Doc: the orphaned-layer `--appliedDelta` operator sentence; `14_:122-123`; CARRIED completeness; lap-lock wx-create.

**Stage-2 opening order (on YES):** the handoff's standing order holds — B1 `--full` (the census + exclusion surfaces land in the first real 26SM baseline) → dark build with the DF2-10 duties (stamping predicate + §6b preimage + the r68 teacher-edit duty verbatim from the card) → emulator matrix (inherits first-append, torn, two-process, and nits 1-4) → dark deploy → 25WT rehearsal → shadow audit. The cutover lens's precondition (admission law before any real 26SM B3) is already satisfied pre-freeze.

---

## LENS: accuracy

# DEEPFIX2 r68 — panel review. LENS: packet + evidence end-to-end

**Basis (measured on this tree; HEAD moved 83fafa7 → 15fe0f8 mid-review as the orchestrator committed the r68 fold — verified against both; worktree ≡ commit over `scripts/deepfix2/` + `docs/plans/deepfix2/`):** `pgrep -f b-emulator-lap` matched only my own probe shell (verified dead) — I took the verification seat, lap NOT re-run. Executed: `delta-chain-fixture.mjs` **81/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` clean on all 11 scripts · **sha16 recomputed myself: 8/8 MATCH** the artifact (`b-baseline 96e1cd170ba6155c` … `b-emulator-lap ea5a2d0228c65d44`) · frozen-bytes timeline holds: last source mtime 07:19:19Z (b4-verify) < evidence write 07:36:50Z; artifact `gitHead` 83fafa7 = the run-time HEAD (the CARRIED item, honest; sha16s carry the binding into 15fe0f8) · lap `ok()` call sites counted: **exactly 82 ≡ the artifact's 82/0** · fixture delta arithmetic checks: 70+11 new asserts (9f:5, 9g:2, 9h:4) = 81; lap 71+11 (overtake:9, mixed:2) = 82 · four offline reducer probes run from scratchpad (repo untouched).

**The diff-mapping claim, tested literally:** `git diff 83fafa7 15fe0f8 -- docs/plans/deepfix2 scripts/deepfix2` = **exactly 11 files, 182+/76−** (the four out-of-scope files — settings, change log line, two batons — account for the rest). I mapped **every** handoff claim to a hunk; **zero phantom lines this round** — a first for this workstream. The two r67 phantom classes are acknowledged in the handoff header, as the packet claims.

## (1) r67 findings — CLOSED / MISS

### Codex A1/A2/B1/B2

| Finding | Verdict | Evidence (file:line) |
|---|---|---|
| **A1 — overtake** | **CLOSED for the ledgered class; one probe-confirmed residue (NEW-1)** | Admission gate: EVERY EXECUTE (plain/delta/repair/resume) consumes `parseLedgerStrict`; foreign unresolved runs refuse admission (`b3-backfill-writer.mjs:291-296`, exit 2). Overtake law: resume with own intent preceding a foreign non-aborted applied completion is REFUSED (`:297-303`, `ordered` positions from `b-baseline.mjs:181,206`). Lap runs Codex's seven steps **in his exact order** (`b-emulator-lap.mjs:404-427`): M0 clean → layer materialized → delta B1 → plain crash **post-intent** → delta admission REFUSED ("unresolved runs", `:418`) → resume heals → delta executes → stale resume REFUSED ("OVERTAKEN", `:424`) → final B4 PASS. All required asserts present and green in the 82/0 artifact. **Residue:** the law is blind to a run that crashed BEFORE its intent append — see NEW-1. |
| **A2 — terminal schema** | **CLOSED (one deliberate deviation, judged correct)** | Exact boolean: `"cutoverAborted" in o && o.cutoverAborted !== true` THROWS (`b-baseline.mjs:198`; my probe 4 reproduced the r67 string exploit dying). Identity binding: completion requires its intent + delta-sha ≡ (`:199-203`). Real counts surface via `cutoverRuns` (`:222-226`); B3 writes real stats into the outcome (`b3:490`) and now publishes the result file on exit 6 (`b3:493-496`). Fixture 9f (`delta-chain-fixture.mjs:398-410`): string/number truthy negatives, sha-mismatch negative, real-counts positive, postFlip-orphan positive. **Deviation:** Codex's literal "zero failures/skips terminal counter contract" was NOT adopted — it was mutually exclusive with the panel's cutover NEW-3 (fabricated zeros must become real counts). r68 chose truthfulness + exact boolean + identity binding, and the handoff states it openly. Right call. |
| **B1 — lease letter** | **CLOSED** | Unparseable lease follows the aged law via file mtime; fresh-unparseable protected (`b-baseline.mjs:241-244`; `mtimeMs=null` degrades conservatively to stale). pid must be a positive integer (`:246`). B3 wires the mtime (`b3:267-268`). Panel ABA: verify-what-we-reaped + restore a live successor on token mismatch (`b3:272-277`) — exactly the prescribed fix. Fixture 9h (`:425-431`): fresh/aged unparseable, pid 0 protected, aged pid −1 stale. Nit below on the unparseable-reap verification path. |
| **B2 — pointer** | **CLOSED** | `b1-baseline-pointer-delta.json` deleted (diff 0/42); `git ls-files docs/plans/deepfix2/evidence/` shows zero pointer files remain. |
| B3 (non-blocking, census units) | **CLOSED** | Cutoff replay now passes `{}` (`b4-verify.mjs:177`); census counts once, at the boundary replay only. |

### r67 panel union

| Finding | Verdict | Evidence |
|---|---|---|
| cutover NEW-1 (= A1) | CLOSED (NEW-1 residue) | Above. |
| cutover NEW-2 (crash-then-flip brick) | **CLOSED for its letter; a sibling brick survives (NEW-2)** | Under `--postFlip` the reducer converts dangling pre-flip intents to published ORPHANS (`b-baseline.mjs:216-218`), B4 consumes and publishes `flipOrphanedRuns` (`b4-verify.mjs:71-75,127`); probe 3 reproduced it. Pre-flip they stay blocking problems. This exceeds the panel's ask (in-law, not just carded). **But** a failures/skips COMPLETION post-flip is still a permanent fatal — see NEW-2. |
| cutover NEW-3 (fabricated zeros) | **CLOSED** | Real stats in the outcome (`b3:490`), surfaced (`cutoverRuns`), result file written (`b3:493-496`). |
| cutover NEW-4 (lease ABA) | **CLOSED** | `b3:272-277`, as prescribed. |
| cutover NEW-5 (pid ≤ 0) | **CLOSED** | `b-baseline.mjs:246` + fixture 9h rows. |
| cutover NEW-6 (joiner wording) | **CLOSED** | `14_:111` "(a LAYER-ONLY JOINER of a cutover-aborted layer surfaces as an `uncoveredAtGate` BLOCKER, not tail [r68])". |
| asof NEW-1 (provenance qualifier + exclusions) | **CLOSED** | `14_:111` PROVENANCE QUALIFIER sentence (dup/epoch retroactive-input corner, delta-flagged, minutes-bounded); gate replay exclusions published (`b4:126,129` `replayExclusions`). |
| asof NEW-2 / freeze NEW-1 (three-conjunct letter + executions + ternary) | **CLOSED** | `14_:111` "REQUIRES ALL THREE [r66/r67/r68]" with the quiet clause verbatim; mixed-same-field lap case BOTH directions (`lap:429-443`: tail fail + lost post-flip fail on w3 ⇒ exit 5; exact live labels ⇒ PASS); dead ternary gone (`b4:225` one expression). |
| asof NEW-3 (census double-count) | **CLOSED** | Count once (`b4:177`). |
| asof NEW-4 (unasserted censuses) | **CLOSED** | Fixture 9g (`:412-423`): `acceptedNoTimestamp` exactly 1, `challengeStatusUnknownEnum` exactly 1. |
| asof NEW-5 (delta pointer) | **CLOSED** (= B2). |
| asof NEW-6 / custody N6 (16_ drill-E) | **CLOSED** | `16_:73`: "`reviewGateEnabled` ONLY — the class REMAINS in `rehearsalClassIds` per the DRILL-E LAW below [r68]". The phantom-claim class is acknowledged in the handoff header. |
| asof NEW-7 / freeze NEW-4(iv) (edited-attempt pin) | **CLOSED** | `asOfScore = a.stored` when edited (`b1-replay-lib.mjs:146`; `edited` defined `:68`, rides the group row `:119`); teacher-edit writer preimage duty carded on the 02_ STAMPING-PREDICATE DUTY line (`02_:95`). |
| freeze NEW-2 (resume-after-delta corner) | **CLOSED for the ledgered class** (the admission gate + overtake law kill both prongs); NEW-1 caveat. |
| freeze NEW-3 (unparseable age) | **CLOSED** (= B1). |
| freeze nits 1/2/4/5 | **CLOSED** | 14_/16_ sentences; NEW-2/NEW-3 corners landed as law (better than "record"); ternary/census/gitHead(→CARRIED)/phantom-line all addressed. |
| freeze nit 3 — mixed-(d) execute-or-rescope | **MISS — untouched and again unlisted** | Lap CASE (d) is still pure structural (`lap:150-164`: corrupt fc 77, no concurrent actionable delta); the 02_ card letter still says "a MIXED unrelated structural diff". 8.5/9 card letters persists. Not in the r68 CARRIED list. |

### CARRIED completeness (my lens duty)

The six listed items are genuine and honestly scoped — I verified each is real (aggregate-only artifact; no two-process starter negative in the lap; crash asserts are exit-code+ledger-level; the first-append window documented at `b3:204-208`; per-original lease name `exec-<sha16>`; gitHead = run-time commit, which I confirmed). **The list is again not the entire known residue:** open-but-unlisted from r66/r67 reviews are (a) **mixed-(d)** — named by all three r67 lenses, the freeze lens's MANDATORY nit 3; (b) the lap-lock wx-then-write creation (`lap:47`, test-infra); (c) `14_:122-123` post-flip re-enrollment roster leftover (LOW, cutover lens). Two are LOW/test-infra; (a) is the recurring packet-discipline class in miniature.

## (2) NEW defects

**NEW-1 (MEDIUM, probe-confirmed): the overtake law is blind to a run that crashed BEFORE its intent append — and B3 itself instructs the destructive resume.**
The overtake check keys on the resume's own intent position (`b3:298-299`); when the crashed run never reached `ledgerAppend` (`b3:448`), `ownIntentIdx === -1` and the check is **skipped**. The crash window is real: the run manifest publishes at `b3:430-433`, the intent appends at `:448` — a host crash/OOM between them, or `ledgerAppend` itself throwing (disk full/EIO) at the intent, leaves manifest+plans on disk and **zero ledger presence, zero Firestore writes**. Sequence: (1) plain M0 crashes pre-intent — invisible to B4 and to the admission gate (nothing ledgered; the lap's own crash1 case asserts exactly this invisibility, `lap:193` "no ledger record before intent — rerun is legal"). (2) Delta M1 EXECUTEs — rightly admitted, ledger is clean. (3) The operator retries the crashed runId fresh; `b3:226` FATALs with "**use --resume to continue an interrupted EXECUTE**" — the tool orders the resume. (4) The resume is ADMITTED (my probe 1, run against the real reducer: `foreignProblems:[]`, `ownIntentIdx:-1`, `ADMITTED:true`), binds cleanly (`rm.deltaLayer` null ≡ null), regenerates plans from live M1 state against its M0 resolver, and phase 2 reverts every M1 student — clean exit 0, loud only at the next B4. This falsifies the handoff's law-statement "a later layer can never overtake a crashed run" and Codex's required "a resume must prove its authority was not overtaken" — the proof is skippable exactly when it cannot be given. Fix is one clause: on RESUME with no own intent in the ledger, refuse if any foreign non-aborted applied completion exists (nothing is lost — zero work was committed; the chain re-executes fresh), plus a fixture row (probe 1's shape) and, ideally, one lap case; also soften `b3:226`'s unconditional "use --resume" instruction.

**NEW-2 (LOW-MEDIUM, probe-confirmed): the crash-then-flip closure is asymmetric — a failures/skips COMPLETION still bricks the post-flip gate permanently.**
r68 orphans only intent-without-completion (`b-baseline.mjs:216-218`). A completion whose outcome carries `txnFailures`/`skippedResetLocked`/`skippedEpochDrift` > 0 (reset-locked skips are ROUTINE — a student resetting mid-run) remains a problem under `postFlip` (probe 2: `problems:["F1 attempt 0: latest completion has failures/skips — resume to a clean completion"]`, `orphans:0`) → B4 FATALs forever (`b4:76-78`) with an instruction that is impossible post-flip (the marker guard refuses every B3 including `--resume`, `b3:122-127`). Same permanence, adjacent trigger, same self-contradictory-instruction shape as cutover NEW-2's letter; reachable only in the final-B4→flip window or by choreography violation (the pre-flip micro-lap catches it before the flip). Not a miss of the r67 letter (which named dangling intents), but the sibling should have died with it. Fix: under `postFlip`, surface failed/skip completions as a published terminal disposition (e.g., `flipStrandedRuns`) settled by the gate's existing value laws — those students' writes never happened, so everything downstream is fail-closed; one reducer clause + one fixture row.

**NEW-3 (NITS):** (i) Reaping an aged-UNPARSEABLE lease FATALs at the ABA verification (JSON parse throw lacks `.code === "ENOENT"`, `b3:277`) with the lease left renamed away — takeover needs a second invocation; fail-closed, clunky. (ii) The gate-side orphan path (`flipOrphanedRuns` stash → publication → settlement) has no executed lap case — the reducer branch is fixture-executed only; same execution-lag class the panel previously dinged, smaller. (iii) The handoff's "a later layer can never overtake a crashed run" is overbroad in the NEW-1 corner (its only inaccuracy — and the corner is a new discovery, not a concealment). (iv) CARRIED omissions per above (mixed-(d), lap-lock wx-create, `14_:122-123`).

## (3) STAGE-1 FREEZE: **NO — one clause short; the narrowest NO available**

**What I verified true (the packet/evidence pillar is finally clean):** every cited count is the measured one (82/0 ≡ 82 sites ≡ artifact; 81/0 and 2,688/0 reproduced; 8/8 sha16 computed by me; frozen-bytes mtime timeline holds); the diff claim is exact (11 files, 182+/76−); every closure claim maps to a real hunk; both r67 phantom classes are acknowledged and no new phantom exists; all four Codex findings and the entire panel union are genuinely executed-closed at the cited lines, with one openly-stated, correctly-judged deviation (real counts over zero-counters). This is the first round where the packet survived a literal line-by-line test with zero false claims of the load-bearing kind.

**What blocks:** NEW-1 alone. It is not manufactured — I reproduced the admission decision against the shipped reducer offline, the revert mechanics are the ones Codex r67 already established, and the destructive step is tool-instructed (`b3:226`). Freezing now would certify the round's headline law ("a later layer can never overtake a crashed run") with a demonstrated bypass in it — the exact certificate-vs-machine mismatch class this workstream refuses to freeze.

**Minimal falsifiable set to YES:** (1) the no-intent resume refusal clause + fixture row (probe-1 shape; a lap case crashed-pre-intent → delta → resume REFUSED completes it) + fix `b3:226`'s instruction; (2) fold NEW-2's `flipStrandedRuns` clause + fixture row in the same commit (minutes, kills the last brick); (3) complete CARRIED (mixed-(d) execute-or-rescope, lap-lock wx-create, `14_:122-123`) or list them; (4) re-run the solo lap on the final bytes, publish 8/8. Nothing touches ratified substance. On completion I flip to YES, and the standing stage-2 opening order (B1 --full → dark build with DF2-10 duties → emulator matrix → dark deploy → 25WT → shadow) stands — with item (1) landing before any real 26SM B3 EXECUTE, since crash-retry is precisely what real operations will do first.