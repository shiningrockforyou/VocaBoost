# 3-Fable panel — DEEPFIX2 r62 freeze attempt (2026-08-02)

Verdicts: chain NO · writer NO · law NO. Convergent blockers: b-delta-cycle.sh layer-discovery glob (one dir off b4-runs/), rosterAdded permanent non-PASS loop; plus future-watermark false PASS, post-flip endgame vs B4 rru law, epoch-gap in the phase-2 fence, rru residue ×4, count drift (2,688 measured).


---

## LENS: chain

All evidence gathered. Composing the review.

# DEEPFIX2 r62 — Delta-chain lens review (independent reviewer, adversarial)

**Lens:** b-baseline.mjs + b1-expected-labels.mjs (--deltaAuth) + b3-backfill-writer.mjs + b4-verify.mjs + b-delta-cycle.sh + delta-chain-fixture.mjs as ONE protocol.
**Executed:** `delta-chain-fixture.mjs` → 20 checks / 0 failures (exit 0). `rotation-cyclicity-fixture.mjs` → **2,688** checks / 0 failures (ran twice, deterministic). Cycle-script layer-discovery glob reproduced empirically against B4's real output layout → **empty result**. B1/B3/B4 CLIs reviewed statically (credentials required).

## Verdict summary

**STAGE-1 FREEZE: NO.** Two structural blockers sit inside the exact artifact this round was named for — the convergent endgame. The cycle driver cannot complete its first delta lap, and any roster addition makes the 14_ §4 "FINAL B4 = PASS" activation gate permanently unreachable. Both live outside the fixture's honest coverage boundary, which is exactly where r61 predicted the risk would hide.

## (1) r61 closure items — CLOSED / MISS

### r61 #1 — Atomic runnable chain + nonempty fixture + Windows: **MISS (BLOCKER component)**
What closed: B1 `--deltaAuth` direct consumption with uid extraction, `--full --classAllowlist` requirement, parent-hash + departedUids stamping (`b1-expected-labels.mjs:93-106,211`); `loadDeltaLayer` verifies BOTH parent hashes (`b-baseline.mjs:88-91`); B4 materializes a canonical layer dir atomically (`b4-verify.mjs:158-168`); the fixture is real, nonempty-delta, drives the REAL `computeStudentLabels`/loaders/`resolveExpectedSource`, and runs green (20/0) with a 7-case negative battery whose fake-db surface faithfully matches `b1-replay-lib.mjs`'s queries.

What misses:
- **The one runnable cycle cannot run.** `b-delta-cycle.sh:17,33` discovers the freshly materialized layer via `ls -1dt "audit/deepfix/trackB_baselines"/*-delta/` — but B4 materializes into `audit/deepfix/trackB_baselines/`**`b4-runs/`**`{runId}-delta/` (`b4-verify.mjs:145,160`). The glob does not recurse; I reproduced it: empty result → line 35 `FATAL: B4 exited 6 but no materialized delta dir found` → exit 2 **on the first delta lap, every time**. The "loops to PASS" driver has structurally never completed a lap. Secondary hazard: if any stale `*-delta/` dir ever exists directly under `trackB_baselines/`, the mtime glob picks it forever (B1 writing into it keeps it newest), and B1 overwriting an already-EXECUTE'd layer's `b1-manifest-delta.json` permanently poisons the ledger (the ledgered sha then exists nowhere; every future B4 FATALs at `b4-verify.mjs:49` unless `--ignoreLedger`).
- **B4's own printed next-step mints a dead layer.** `b4-verify.mjs:168` (and header `:7`, comment `:158`) still instruct `b1 --uids=<dir>/uids.json ...`. The `--uids` path stamps NO parent hashes (`deltaAuthMeta` is set only for `--deltaAuth`, `b1-expected-labels.mjs:93-106`), so `loadDeltaLayer` unconditionally FATALs on the result (`b-baseline.mjs:89`). An operator following the tool's own guidance produces an unloadable layer — the r60/r61 interface break reproduced in the hint.
- **Windows: partial.** Loaders normalize `\`→`/` (`b-baseline.mjs:14,59,80`), but B1's `--outDir` URL construction (`b1-expected-labels.mjs:142`) is byte-identical to the construction r61-A6 proved resolves `C:/...` to protocol `c:`. The claim's wording ("both loaders + loadDeltaLayer") quietly excludes the one site r61 cited by line number.

### r61 #2 — Fail-closed exits + true transaction fence: **PARTIAL**
Closed: B4 exits 0 only on PASS, 5 = DIFFS, 6 = delta outstanding (`b4-verify.mjs:171-173`); B3 exits 4 on txn failures, 5 on reset-locked (`b3-backfill-writer.mjs:295-296`); phase 2 is genuinely chunked transactions with ALL reads (both tombstone collections + chunk targets) before ANY write, then a re-diff against txn-read state (`b3-backfill-writer.mjs:239-269`) — serializable isolation makes the lock fence real, and the r61 stale-plan re-force is gone. Reset-locked students get NO journal line (`:275`) so resume retries. The honest residual (chunks committed pre-reset) is documented in the header and in a real 15_ §9 clause (`15_H6_SCHEMAS_AND_CONTRACTS.md:230-236`).
Misses: (a) the txn checks `resetInProgress` only (`:243`) — it never re-checks **epoch** against the plan's snapshot, though §9(2)'s letter is "each writer's FINAL txn re-reads the epoch + lock". A reset that acquires AND releases between phase 1 and a student's chunk txn re-creates pre-reset labels on a freshly wiped student; healed only by the NEXT B4 lap (epochDrift → delta), never a false PASS, but a one-lap stale-write window §9 does not admit. (b) r61-A3 explicitly demanded "a deterministic race fixture must acquire the reset lock after phase 1 and prove that zero stale writes commit" — no such fixture exists; the delta-chain fixture contains no transaction machinery at all.

### r61 #3 — Resume binds everything: **CLOSED** (one narrow residual)
Mode, original sha, delta-layer sha, extras sha all bound (`b3-backfill-writer.mjs:126-130`); DRY→EXECUTE rejected; journal trusts only `ok:true` (`:132`), and `ok:true` is written only when every chunk committed (`:275-279`); backup is complete-or-absent by tmp→rename (`:205-208`). Residual: on resume the plan is freshly recomputed (`plans.resume.jsonl`, `:141`) while `preStream` is null — a mid-run mutation of pre-watermark facts (e.g., a CS deletion of one conflicting duplicate un-excluding a group, `b1-replay-lib.mjs:98-105`) can introduce target docs with no pre-image in the published backup. "Covers every uid" is true; "covers every written doc" is not.

### r61 #4 — Exact validation + bound extras: **PARTIAL**
Closed: duplicate-uid rows THROW in both loaders (`b-baseline.mjs:36,73`); delta watermark must strictly EXCEED the original (`:97`, fixture-negative covered); extras report sha-bound to the original and scope-checked (`b3-backfill-writer.mjs:71,103-104`).
Still open — items r61-A6 named by symptom: `classesMatched` is compared **only when truthy** in both scripts (`b3-backfill-writer.mjs:92`, `b4-verify.mjs:60`) — the handoff claims "classesMatched ≡ allowlist (both scripts)" without the absent-field guard r61 asked for; the ORIGINAL manifest's watermark is never validated (absent/non-finite ⇒ `t >= undefined` is false ⇒ the newAttempts detector silently dies in a no-layer B4 run); B4 never requires a `full` original (`b4-verify.mjs:30`, no `requireMode`); version remains `>= 6` rather than one understood schema; B3 accepts a truncated extras report (`:69-74` never checks `diffsTruncated`) — convergent over repeated laps, but r61-A5 said refuse.

### r61 #5 — Bounded memory + backpressure: **CLOSED** (evidence honestly deferred)
Offset-index baseline consumed by both B3 (`:59`) and B4 (`:30`) (`b-baseline.mjs:13-56`); the write plan streams to disk with an incremental hash and phase 2 consumes it line-by-line (`b3-backfill-writer.mjs:141-146,226-232`); both streams await drain (`:145` used at `:171,:189,:197,:200`). Large-cohort RSS/wall-clock evidence is explicitly deferred to the stage-2 B1 --full run — honest, but "bounded" remains asserted, not measured.

### r61 #6 — Wording law / counts: **PARTIAL — the count defect is REPEATED**
14_ now states the FIVE-field law with rru retired (`14_TRACK_B_BACKFILL_PIPELINE.md:62,82`); the H8 header states the two-consecutive-lap property. But 11_ R2-47 states "`rotation-cyclicity-fixture.mjs` v4, **2,692 checks**" (`11_...md:37`) and the change log recorded 2,692 — the current file deterministically produces **2,688** (executed twice). r61-C1's exact instruction was "record the actual fixture count generated by the current file"; the fold re-wrote the claimed number instead of measuring. Evidence-hygiene MISS.

### r61 #7 — Shadow: **NOT ASSESSED (outside this lens)**
Noted only: the "reduced-set scope through the EXISTING --classAllowlist surface" mechanism at least corresponds to a real shared surface in B1/B3/B4. Deep verdict belongs to the shadow-lens reviewer.

### Panel item — --deltaAuth direct: **CLOSED** (with the D5 hint blemish above).
### Panel item — Roster churn never a brick: **MISS (BLOCKER)** — see D2.
### Panel item — Retry re-force eliminated: **CLOSED** (`b3-backfill-writer.mjs:249-267`; failed txns journal `ok:false` + exit 4; no path replays phase-1 values).
### Panel item — Applied-layers ledger: **CLOSED** (minor edges)
Every EXECUTE appends (`b3-backfill-writer.mjs:290-293`); B4 FATALs on a missing EXECUTE'd layer (`b4-verify.mjs:39-51`). Edges: an all-txns-failed EXECUTE still ledgers its layer (consistent — resume finishes it); crash between commit and append is benign (every delta category re-flags against the original, so no false PASS is constructible through that hole); the overwritten-layer-file poisoning described under #1 has no recovery story short of `--ignoreLedger`.

## (2) NEW defects

**D1 (BLOCKER) — The cycle driver cannot find the layer B4 just made.** `b-delta-cycle.sh:33` vs `b4-verify.mjs:145,160`. Failure scenario: any real run reaching B4 exit 6 → glob empty → exit 2. Reproduced offline. The r62 headline "the one runnable cycle" is not runnable; nothing in the fixture covers the driver.

**D2 (BLOCKER) — rosterAdded is a permanent non-PASS loop; the activation gate is unreachable.** `b4-verify.mjs:65,71` computes `rosterAdded` against the ORIGINAL baseline only, and `:91` re-flags unconditionally — applied delta layers are never consulted. Failure scenario: one student joins any 26SM class after the FULL B1. Lap 1: B4 flags `rosterAdded`, materializes a layer; B1 --deltaAuth baselines them; B3 EXECUTEs; disk now correct. Lap 2: the uid is still absent from the ORIGINAL rows ⇒ `rosterAdded` again ⇒ deltaList nonempty ⇒ verdict ZERO-DIFF-BUT-DELTA-OUTSTANDING (exit 6) ⇒ another layer containing the same uid ⇒ forever. `b-delta-cycle.sh` exhausts MAX and exits 7 every attempt; 14_ §4's "the FINAL B4 = PASS" is unreachable without a full-chain restart (which the delta chain exists to avoid, and during which more churn can occur). The handoff's "never a brick" is false for additions; the r61-panel receipt row ("ROSTER CHURN BRICKS THE PROTOCOL") is only half-closed (departed side is genuinely fixed: `b-baseline.mjs:92-96`, `b3:105-112`, `b4:147`). Fix shape: `rosterAdded = live uids covered by neither the original nor any applied layer`, plus a fixture case proving a roster-added student can reach an empty deltaSet.

**D3 (HIGH) — A future watermark yields a false PASS that hides unbaselined attempts.** `b1-expected-labels.mjs:67` accepts any ≥10-digit epoch-ms for `--watermark`, including the future; `:123` uses it raw; no loader anywhere validates watermark ≤ now (`b-baseline.mjs` checks only delta > original at `:97`). Failure scenario: operator runs B1 `--deltaAuth --watermark=1786000000000` (paste/typo; header even advertises `--watermark` for fidelity replays) → layer manifest watermark ≈ 2026-08-06+; all hash checks pass; B3 writes the rows (computed from attempts existing at run time); students submit attempts the next day; final B4: for those uids `t >= src.watermark` is false ⇒ no `newAttempts` flag, expected ≡ disk ⇒ **PASS while on-disk labels exclude real attempts**. The hash chain cannot catch it because the wrong watermark is hash-consistent. Same hole exists for a full-mode B1. One guard line (`watermark <= Date.now()` at B1, mirrored at layer load) closes it. Related low-severity: the delta manifest's watermark is entirely self-asserted — a hand edit before the FIRST EXECUTE defeats even the ledger (the fixture's negative battery covers auth/rows tampering but not manifest-watermark tampering).

**D4 (MEDIUM) — Phase-2 txn omits the epoch check and the demanded race fixture does not exist** (detailed under #2 above).

**D5 (MEDIUM) — B4's operator hint directs to the dead `--uids` path** (detailed under #1).

**D6 (LOW-MEDIUM) — Departed-with-failed-writes is invisible under PASS.** A student whose B3 txns failed (journal `ok:false`) and who then departs leaves live scope: resume's uid set is re-derived from live enrollment (`b3:88-96`), so they are silently dropped; nothing checks journal completeness against the run's original uid set; B4 verify-skips departed (`b4:72,147`). Their partial labels persist unverified under a final PASS, and a post-flip re-enrollment resurrects them with wrong historical labels that nothing reconciles.

**D7 (LOW) — Resume pre-image doc-level gap** (under #3). **D8 (LOW) — truncated extras accepted** (under #4). **D9 — 2,692 vs 2,688** (under #6).

**Fixture scope honesty (cross-cutting):** the fixture certifies the shared law modules and it does that well — but B4's verdict/rosterAdded law, B3's transaction law, the ledger audit, and the cycle driver are all CLI-side code with zero fixture coverage. Both blockers (D1, D2) and D3 live precisely in that uncovered band. The negative battery also lacks: duplicate-uid row, future/tampered manifest watermark, multi-layer resolution ordering.

## (3) STAGE-1 FREEZE: **NO**

Minimal remaining set (each falsifiable):
1. Fix `b-delta-cycle.sh` layer discovery to consume B4's actual materialized path (parse the runId/report, not an mtime glob); demonstrate one full mock/emulator lap reaching PASS.
2. Redefine `rosterAdded` as not-in-original AND not-in-any-applied-layer; fixture case: roster-added → layer → apply → re-verify ⇒ empty deltaSet (currently impossible).
3. Reject `--watermark > Date.now()` in B1; validate finite-positive-past watermarks at both loaders.
4. Add the epoch re-check to the phase-2 txn (or amend 15_ §9 to admit the one-lap heal) and ship the deterministic reset-race fixture r61-A3 required.
5. Fix B4's `--uids` hint/header to the `--deltaAuth` path; drop the `classesMatched` truthy-guard; require a full original in B4; refuse truncated extras reports; fix B1 `--outDir` for Windows absolute paths.
6. Write the measured rotation-fixture count (2,688 today) into 11_ R2-47.

Genuinely sound and worth keeping: the --deltaAuth composition with dual parent hashes, the per-uid watermark resolution, the txn re-diff (re-force eliminated), resume hash-binding, the ledger concept, the offset-index/streamed-plan memory work, and the fixture's loader-law coverage. The protocol's failure modes are now almost all fail-closed or self-healing through the next B4 lap — the two blockers are convergence bricks, not silent corruption — but a contract freeze over a chain that cannot complete one lap and cannot reach its own PASS gate is not a freeze.

---

## LENS: writer

All verification complete. Here is my review.

---

# DEEPFIX2 r62 adversarial review — Reviewer lens: B3 writer under concurrency + failure

**Independent checks executed:** read all five scripts + driver + fixture in full; ran `delta-chain-fixture.mjs` (20 checks / 0 failures, exit 0); ran `rotation-cyclicity-fixture.mjs` (**2,688** checks / 0 failures — verified the `ok()/fail()` assertion pattern is genuine, not vacuous); simulated the cycle driver's layer-discovery glob against B4's actual output path (empty result — see NEW-1); grepped `functions/` for `resetInProgress` (zero hits — live reset writes only `resetEpoch`/`resetAt`, foundation.js:2113); verified 14_/15_/16_/02_/11_/10_ doc claims line-by-line.

## (1) The eleven r61 closure items

| # | Item | Verdict | Evidence |
|---|---|---|---|
| C1 | Atomic runnable chain + nonempty fixture + Windows | **PARTIAL — driver MISS** | Fixture is real and green: `delta-chain-fixture.mjs` drives REAL `computeStudentLabels` + REAL loaders through full→mutate→churn→delta-auth→delta layer→resolve→apply→zero-diff (stages 1-6) + a genuine 7-case negative battery (lines 183-223); honest scope line at :12-14. **But the shipped "one runnable cycle" is broken**: `b-delta-cycle.sh:33` globs `$BASE_DIR/*-delta/` while B4 materializes the layer under `b4-runs/` (`b4-verify.mjs:145,160` → `trackB_baselines/b4-runs/<runId>-delta/`). Simulated: glob returns empty → driver exits 2 ("no materialized delta dir found") on its FIRST outstanding delta. This is exactly the r61-A1 defect class (chain cannot run as written), relocated to the seam the fixture honestly declares uncovered. Windows: loaders normalize `\`→`/` (`b-baseline.mjs:14,59,80`) but B1's `--outDir` URL construction (`b1-expected-labels.mjs:142`) is unchanged — r61-A6's absolute-drive-path repro still fails (mitigated only by the WSL env + the driver passing relative paths). |
| C2 | Fail-closed exits + TRUE txn fence | **PARTIAL** | Exits CLOSED: B3 exit 4 on `txnFailures`, 5 on `skippedResetLocked` (b3:295-296); B4 exit 0 ONLY on PASS (5=DIFFS, 6=delta-outstanding, 2/3 fatal; b4:171-172; truncation forces non-PASS since extras count into `totalDiffs`). Txn law CLOSED in structure: chunked `runTransaction` per student, tombstone queries + `txn.getAll` targets ALL before writes (b3:239-268), 300 writes < 500 limit, re-diff against txn-read state (the r61 stale-plan re-force is dead — the plan is a hint), RESET_LOCKED → no journal line → resume retries (b3:275), residual documented in the header AND 15_ §9:230-236. **But the fence checks ONLY `resetInProgress`, never the epoch it has already read** (b3:243 inspects one field of the tombstone docs in hand) — see NEW-2 counterexample. And r61-A3's explicitly demanded deterministic race fixture does not exist: the fake db has no transactions; the phase-2 txn law is exercised by NOTHING. |
| C3 | Resume binds everything | **CLOSED (one narrow unproven residual)** | Resume FATALs on mode, original sha, delta sha, extras sha (b3:126-130); allowlist transitively bound via classesMatched≡allowlist against the sha-bound manifest; journal skip = `ok:true` lines only (b3:132); journal records `ok:false` with failure list on any chunk failure (b3:276-279) — the r61 "failed UID journaled as committed" is dead. Pre-image complete-or-absent holds by tmp→rename (b3:138-139,205-208; crash mid-phase-1 leaves no backup, `--resume` FATALs without the manifest which is written after the rename). Residual: resumed plans are REGENERATED from live state with `preStream=null`; a doc newly entering expected between run and resume (in-place mutation flipping an old attempt's eligibility, e.g. one of a `dupConflictGroup` pair deleted by CS cleanup) is written with NO pre-image, and no check enforces resumed-plan ⊆ backup. r61's "proves every resumed write has an original pre-image" is asserted, not proven. Also the resume overwrites `{runId}.result.json` (b3:286-287), erasing the original run's audit record. |
| C4 | Exact validation | **PARTIAL** | Duplicate-uid THROW in both loaders (b-baseline:36,73); delta watermark must EXCEED original, fatal (b-baseline:97); extras sha+scope bound (b3:71,103-104). **But `classesMatched ≡ allowlist` is still presence-gated** — `if (original.manifest.classesMatched)` in BOTH b3:92 and b4:60-63; a manifest lacking the field silently skips the check (the exact r61-A6 bullet), and the handoff's "(both scripts)" claims it unconditionally. Version still open-ended `>= 6` (b-baseline:18,63); the ORIGINAL manifest's watermark is never validated finite/positive; B4 loads the original without `requireMode:"full"` (b4:30). These degrade to delta-outstanding/diffs rather than false PASS, but "exact validation" overstates. |
| C5 | Bounded memory + backpressure | **CLOSED** | `loadVerifiedBaselineIndexed` is a real `{uid→[offset,len]}` byte index, hash-once, buffer released, per-uid `readSync` (b-baseline:27-55); B3 and B4 both consume it (b3:59, b4:30); B3 phase 1 streams plans (b3:141-146,189) and phase 2 consumes line-by-line via readline (b3:226-227); `swrite` awaits drain on BOTH streams (b3:145). Large-cohort evidence honestly deferred to the stage-2 B1 `--full` run. Nit: stream `error` events are unhandled — fail-closed by crash, but a disk-full during a drain wait can hang. |
| C6 | Wording law (rru purge · one cursor law · counts · differing-size) | **PARTIAL — two MISSes** | Cursor law CLOSED: "highest" is gone from 15_; §2 defers to §2b; 10_ carries the two-consecutive-lap wording (10_:38-45). Differing-size CLOSED: 15_:55 (`snapshot.queueSize` = \|reused queue\|, `configQueueSize` audit-only, CERT both orders) + 02_ DF2-14 CERT amended. **rru residue MISS**: 14_ is purged, but r61-B3's other cited locations remain verbatim — B1 header still describes the rru seed AND a `--seedRru` flag that no longer exists in B3 (b1-expected-labels:7,12-13), B1's PUBLISHED summary `law` string still says "rru validated masteredAt seed (r55)" (:193 — an artifact string, emitted every run), replay-lib header still states the masteredAt-window law (b1-replay-lib:5-6), and **15_ §10:256 still says `reviewRestingUntil` is "backfill-seeded from legacy masteredAt once"** — directly contradicting §1:21's "the backfill NEVER writes it" in the same governing contract. **Count MISS**: the fixture actually outputs **2,688**; 11_ R2-47 claims **2,692** (the handoff's "real fixture count" is false); 10_:41 still says **2,671** (the exact r61-C1 complaint, untouched). Three different numbers now stand across the certified-artifact record. |
| C7 | Shadow executable | **PARTIAL** | Battery I two named invocations with disjoint predicates + cross-contamination negatives CLOSED (16_:158-165). Teacher invisibility structural all-teacher proof CLOSED (16_:49-54: full ownership enumeration + rules denial + direct-doc AND query negatives per surface). Registry consent fix + GENERATION/CACHE law CLOSED at design level (16_:84-89 — moved off `system_config/*` to a new Admin-only `shadow_registry/{n}` under the granted B/E authority; generation counter + ≤60s TTL + membership-stable windows + driver visibility assert). **Reduced-set scope PARTIAL**: `scope-<runId>.json` via `--classAllowlist` (16_:89) is CLASS-granular, but the destructive partitions are STUDENT-granular (~30/~100/~20, 16_:101-103) — the mechanism cannot express those subsets unless partitions are materialized as classes, which is nowhere stated; B3/B4 still have no uid-scope input, and full-cohort reduced-set consistency rests on the clone enrolling only reduced uids (implied at 16_:113-115, not stated as the sole scope authority). **Carried item MISS**: the handoff says the authority-union force-pass/stamp-leg negatives are "CARRIED (stage-2/3 build matrix, named)" — grep across all deepfix2 docs finds NO such named item (only the trackA A1 audit's descriptive note); 16_:119 still reduces containment to `ownerTeacherId === caller`, the exact shorthand r61-D3 warned against. |
| P1 | `--deltaAuth` direct | **CLOSED** | b1-expected-labels:93-105 consumes the B4 artifact itself, requires `--full --classAllowlist` (:71), extracts+scopes uids, counts departed, stamps `parentOriginalManifestSha256`+`parentDeltaAuthSha256`+`departedUids` (:99-104,211); `loadDeltaLayer` verifies BOTH parent hashes (b-baseline:88-91) — fixture negative n2 proves post-stamp auth edits die. |
| P2 | Roster churn counted, never a brick | **CLOSED** | B4: `rosterAdded` → delta reason (b4:71,91), `departedSkipped` counted+listed (b4:72,147,154); B3: `deltaUidsDropped` + list (b3:105-112); loader law (auth − departed) ≡ rows (b-baseline:92-96), fixture cases n3/n4 prove departed is the ONLY excusal. |
| P3 | Retry re-force eliminated | **CLOSED** | No precondition/retry path remains; phase 2 recomputes the diff inside the txn against txn-read state (b3:249-266); a stale phase-1 value that already matches current state produces no write. |
| P4 | Applied-layers ledger | **CLOSED** | Every EXECUTE appends (b3:290-293, including failed runs — correct, writes happened); B4 FATALs on any EXECUTE'd layer for its original missing from `--appliedDelta` (b4:39-51), `--ignoreLedger` forensics-only. Crash between result-write and ledger-append degrades to DIFFS on the final B4, never false PASS. |

## (2) NEW defects (my lens)

**NEW-1 (blocker): the delta cycle driver cannot find the layer it just ordered built.** `b-delta-cycle.sh:17,33` — `BASE_DIR=audit/deepfix/trackB_baselines`, glob `$BASE_DIR/*-delta/`; B4 writes `.../trackB_baselines/b4-runs/<runId>-delta/` (b4:145,160). Failure scenario: first live run, B4 exits 6, materializes the layer, driver's `LAYER` is empty, driver exits 2 with "B4 exited 6 but no materialized delta dir found". The convergent endgame (14_ §4) has no working instrument. (Also cosmetic: B3's exit 4/5 propagates through `set -e` as the driver's exit, colliding with the driver's documented "5 = DIFFS".)

**NEW-2 (medium): a COMPLETED reset between phase 1 and phase 2 slips the fence and resurrects pre-reset labels with a clean exit.** Interleaving: T0 phase 1 plans student S (epoch e, expected L_e). T1 S runs `resetProgress` to completion — even the H6 §9 reset sets `resetInProgress`, wipes, bumps epoch to e+1, then **owner-clears the lock** (15_ §9 step 5). T2 phase-2 chunk txn: tombstone read shows no live lock (cleared), `resetEpoch` e+1 is in the read data but b3:243 inspects only `resetInProgress`; targets read as wiped (`cur=null`); re-diff sets everything → `txn.set` recreates full pre-reset label docs on a freshly-reset student; journal `ok:true`, `skippedResetLocked=0`, exit 0. Not silent-to-PASS — the next B4's `epochDrift` delta forces re-baseline and the endgame deletes the stale labels — but the run reports clean, the r61-A3 letter ("checks epoch/lock immediately before the label writes") is unmet, and the fix is one comparison against the phase-1 `epochByList` already carried per student. Compounding: TODAY's deployed reset never writes `resetInProgress` at all (zero hits in `functions/`; foundation.js:2113 writes only the epoch), so before the dark build ships §9's reset the lock leg of the fence can never fire — B3 verifies nothing about its counterpart's existence.

**NEW-3 (minor): phase-2 stats mutate inside the transaction callback** (b3:264-267): contention-driven SDK retries re-execute the callback and inflate `docsWritten`/`fieldSets`/`fieldDeletes`/`docsVerifiedEqual` in the published result — an audited artifact over-reporting writes.

**NEW-4 (minor): torn-journal tolerance is correct but silent** — a partial `appendFileSync` line is swallowed by the parse-catch (b3:132) and the student retried; convergent, acceptable, worth a counter.

No interleaving I could construct double-writes divergent values (all writes re-diff to the same expected inside the txn), and no failure is recorded as committed (`ok:false` on any chunk failure; no line on locked). The corruption class that survives (NEW-2) is stale-but-committed, structurally absorbed only by the mandatory B4 endgame — whose driver is broken (NEW-1).

## (3) STAGE-1 FREEZE: **NO**

The field narrowed substantially — exits, txn re-diff, resume binding, journal truth, bounded memory, the delta-auth chain, churn law, and the ledger are real closures, and the fixture is a genuine artifact. But three of the eleven fail verification on their letter, one with a fresh counterexample of the exact r61-A1 "cannot run as written" class.

Minimal remaining set (each falsifiable):
1. Fix `b-delta-cycle.sh` layer discovery (glob `b4-runs/`, or B4 prints the layer path and the driver consumes it); prove by one driver run against a mock/emulator or a path-level unit check.
2. Phase-2 txn compares txn-read `resetEpoch`/`resetAt` against the phase-1 snapshot (abort → `skippedResetLocked` or recompute) — plus the r61-A3 deterministic race fixture (fake-txn or emulator) proving zero stale writes commit across a phase-boundary reset, lock held OR already cleared.
3. Purge the four remaining rru locations (B1 header + summary `law` string, replay-lib header, 15_ §10:256).
4. One true fixture count everywhere: re-run, record 2,688 (or current) in 11_ AND 10_; delete 2,671/2,692.
5. Make `classesMatched` mandatory in both scripts (absent ⇒ FATAL); validate the original watermark finite-positive.
6. Name the carried authority-union negatives in an actual doc row (02_ build matrix or 16_), and state the reduced-set/partition granularity mechanism (partition-as-class, or a uid-scope input for B3/B4).

Item (c) stage-2 opening order: not confirmed — blocked on the above. Items 1, 2, and 4 block the dark build directly: the endgame instrument doesn't run, the claimed H6 §9 compliance is narrower than written, and the certification record cites evidence numbers the artifact does not produce.

---

## LENS: law

# Fable panel review — DEEPFIX2 r62 (STAGE-1 FREEZE attempt) — lens: doc-law coherence + shadow audit

**Independent execution performed:** `rotation-cyclicity-fixture.mjs` run twice → **2,688 checks / 0 failures** (deterministic, seed 424243). `delta-chain-fixture.mjs` run → **20 checks / 0 failures**. All five Track-B scripts + b-delta-cycle.sh read in full; 10_/11_/14_/15_/16_/02_ + H8 header + winclaude_069 authority record read against the claims.

**STAGE-1 FREEZE: NO.**

---

## 1. The eleven closure items

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| 1 | Atomic runnable chain + nonempty fixture + Windows | **MISS** | Fixture leg real and green: `delta-chain-fixture.mjs` drives real `computeStudentLabels` + real loaders, 7-case negative battery (n1 tamper, mispaired sha, post-stamp auth edit, watermark ≤, rogue uid, unexcused missing uid, mode law), honest scope header (:12-14). B4 materializes canonical `delta-auth.json` (b4-verify.mjs:157-168). **But the "one runnable cycle" is broken by its own path bug** (new defect N1 below), B4's printed hint still instructs the superseded `--uids` path (N5), and Windows coverage is loaders-only — `b1-expected-labels.mjs:142` still builds `outDir` from `"file://" + process.cwd()`, the exact r61 `c:`-protocol construction; no Windows path tests exist. |
| 2 | Fail-closed exits + TRUE txn fence | **CLOSED (code) / PARTIAL (proof)** | b3-backfill-writer.mjs:239-269: chunked `runTransaction`, both tombstone collections + chunk targets read before any write, re-diff in-txn; RESET_LOCKED ⇒ `skippedResetLocked`, no journal line (:275), exit 5 (:296); txnFailures ⇒ exit 4 (:295). B4 exits 0 only on PASS (b4-verify.mjs:152-153, 171-173; truncation cannot be PASS). Matches H6 §9 [r62] exactly. **Missing:** r61-A3's required deterministic race fixture (reset lock acquired after phase 1 ⇒ zero stale writes) does not exist anywhere; and "the txn recomputes" is overstated — it re-DIFFS against phase-1 *expected values*, never recomputes them (consequence at N3). |
| 3 | Resume binds everything | **CLOSED** | b3:121-133 binds mode + original sha + delta sha + extras sha; resume skips only `ok:true` journal lines (:131-132); pre-image complete-or-absent via tmp→rename (:138-139, 205-208). Nits: the claim "journal = ok:true lines only" is literally false (`ok:false` lines are written, :279 — behavior safe); a resumed run's `plans.resume.jsonl` is never manifest-bound (`writeplanSha256` stays the original's, :215-217). |
| 4 | Exact validation | **PARTIAL** | Closed: dup-uid THROW both loaders (b-baseline.mjs:36, 73); delta watermark strictly > original (:97) + fixture negative; extras sha+scope-bound (b3:71, 103-104). **Not closed (all r61-A6-named):** `classesMatched` check is still conditional on presence (b3:92, b4:60 — absent field ⇒ zero validation, the exact bypass r61 named, so the handoff's "classesMatched ≡ allowlist (both scripts)" is inaccurate); original watermark never validated positive-finite — an absent `watermark` passes all hashes while `t >= undefined` is always false (b4:104-105), silently killing the new-attempt delta detector: the precise false-green r61 described; version still `>= 6` not exact (:18, 63); delta-auth `version` unvalidated; **B3 does not refuse a truncated b4-report**, and B4's header still claims "enumerated FULLY, spill-safe" while `extrasList` caps at 5,000 (b4:4-5 vs :137). |
| 5 | Bounded memory + backpressure | **CLOSED (honest deferral)** | `loadVerifiedBaselineIndexed` offset index is real (b-baseline:13-56, buffer released :41); both streams await drain (b3:145); phase 2 consumes the plan file line-by-line (:226); B4 uses the indexed loader (b4:30). Large-cohort RSS evidence deferred to stage-2 B1 --full, declared honestly. |
| 6 | Wording law (rru / one cursor law + count / differing-size C3) | **MISS** | **rru:** 14_ is clean, but four of r61-B3's five named residues remain: b1-expected-labels.mjs:7 + :12-13 (seed header + a `--seedRru` flag that no longer exists in B3), **:193 — the summary still publishes "rru validated masteredAt seed" in its `law:` artifact string**; b1-replay-lib.mjs:3-6; **15_ §10 line 257 still says rru is "backfill-seeded from legacy masteredAt once" — directly contradicting §1's r59-A9 FINAL LAW in the same frozen document.** **Cursor law:** genuinely one now — H6 §2 defers to §2b (:38), "highest" gone; 10_:46-48, 11_ R2-47, and the H8 header all carry the two-consecutive-lap wording, which matches the fixture's actual assertion (fixture:141-142, 164-168). **Count: false.** Actual = **2,688**; 11_ R2-47 says 2,692 (the handoff calls 2,692 "the real fixture count" — it is not); 10_:41 still says 2,671; change_action_log:1427 records 2,692. r61-C1's exact demand — record the count the current file generates — failed, and the wrong number now sits inside the binding ledger row. **C3 differing-size:** substance closed in H6 §2b (:55 — content truth + `configQueueSize` + both-orders CERT landed in 02_ DF2-14), but the snapshot-truth change reached exactly one place: H6 §2's own canonical field list (:33) has no `configQueueSize`; **10_ §2 (normative) still says "the day's effective values snapshotted" with a snapshot field list (:52-53) and carries no same-day-reuse law at all — it now contradicts H6 §2b for the receiving class**; and H6 §3b's `sourceConfig.queueSize` provenance (configured vs content) is now ambiguous for the cross-class audit record R2-37/38 contract (2) requires. |
| 7 | Shadow executability | **PARTIAL** | Closed: two-mode monitoring predicates disjoint + named + cross-contamination negatives (16_ §4-I); all-teacher STRUCTURAL isolation with full ownership enumeration + rules denial + per-surface direct-doc AND query negatives (§2.2); registry authority defensibly relocated (below); generation/cache law present and sound BY SCHEDULE (TTL-expiry forces every instance to re-read before batteries). **Gaps a driver cannot bridge as written:** (a) `evaluateThresholds({scope,dryRun})` exists nowhere in code and is **not named as a dark-build deliverable** in 02_/DF2-14 — unlike the rehearsalClassIds resolver, which is; battery I invokes an API no build item promises. (b) "asserts generation visibility" names no probe surface (what does the driver read?). (c) Reduced-set scope: `--classAllowlist` is a real surface on all three CLIs, but the reduced set is **uid**-granular and B3/B4 derive uid scope from class `studentIds` with FATAL on drift — the clone must build shadow-class `studentIds` from the reduced set (prune flagged-skips) or every full-shadow B3/B4 run dies on scope drift; 16_ asserts "the reduced set IS … B3/B4 scope" but never states that mechanism. (d) **The D3 carry is unanchored:** "authority-union"/"target-bound" appear ONLY in the r62 handoff — in no build matrix, not in 16_, not in 02_ — while 16_ §3's containment triad still contains the exact `ownerTeacherId === caller` reduction r61-D3 warned against. "CARRIED … named" is false; the requirement will be lost at freeze. |

**Panel's four:** --deltaAuth direct — **CLOSED** (b1:93-106; loader verifies both parent hashes, b-baseline:88-91; fixture stage 4/5). Roster churn counted — **CLOSED** (b4:66-72, 147; b3:105-112; loader :92-96 + fixture n3/n4). Retry re-force — **CLOSED** for the r61 mechanism (no precondition-retry path survives), with the N3 caveat. Applied-layers ledger — **CLOSED** (b3:288-293 appends every EXECUTE; b4:39-51 FATALs on a missing layer; `--ignoreLedger` forensics-only).

**Registry authority (r61-D1):** the move off `system_config/*` to a new top-level `shadow_registry/{n}` respects narrowed-A's letter (winclaude_069.md:107-109: only *system_config* paths are forbidden). B ("standing execution authority", :118-122) + David's completion mandate (:152-153) + Q3's rules-deploy grant plausibly cover an Admin-written, rules-denied, cleanup-deleted audit collection — creating shadow docs inside REAL collections is already granted, and a new isolated collection is strictly less invasive. Defensible; Codex should ratify explicitly since the §2 isolation laws were amended after the E-grant.

## 2. New defects

**N1 — BLOCKER: b-delta-cycle.sh cannot complete its first delta iteration.** B4 materializes layers under `audit/deepfix/trackB_baselines/b4-runs/<runId>-delta/` (b4-verify.mjs:145, 160), but the cycle script globs `"$BASE_DIR"/*-delta/` with `BASE_DIR="audit/deepfix/trackB_baselines"` (b-delta-cycle.sh:17, 33) — one directory level short, non-recursive. Failure scenario: any nonempty delta → B4 exits 6 → glob returns nothing → `FATAL: B4 exited 6 but no materialized delta dir found` → exit 2. The headline "one runnable cycle" claim fails on the exact CLI/glue seam the fixture honestly declares uncovered. (Also: the stated exit contract 0/5/7/2 omits a propagated B3 exit 4 under `set -e`.)

**N2 — HIGH (doc-law): the post-flip endgame is structurally incompatible with B4's rru law.** 14_ §4 requires the FINAL B4 = PASS *after* the flip; B4 unconditionally asserts `reviewRestingUntil === null` on every expected word (b4:127-128) and counts any rru-bearing doc as an extra (SIX includes rru, :76, 131-133). Post-flip, the first live graduation legitimately writes rru (H6 §1: set in the graduation txn). rru is never in any baseline (live-only, r59-A9), so **no delta iteration can ever absorb the diff**. Scenario: flip 08:00; one student passes review 08:05, graduates 27 words; final B4 reports 27 rru diffs ⇒ exit 5, permanently — and an operator running `--repairExtras` off a post-flip report would delete legitimate live labels+rru. The 15_ §9 [r62] honest-residual clause leans on this very reconciliation pass. Fence B4's rru assertion pre-flip (or graduation-aware via `day_completions.graduatedWordIds`) and forbid post-flip `--repairExtras` before freezing.

**N3 — MEDIUM: post-flip B3 value re-force.** Phase 2 writes phase-1 expected values after re-diff — safe pre-flip (no other writers of the five new fields exist), but the 14_ §4 reconciliation B3 can overwrite labels the live server stamped between its phase 1 and phase 2. 14_ §4's "the flip itself is the write-freeze" is backwards for labels — the flip *starts* live label writes; the actual quiescence/ordering rule is unstated.

**N4 — the binding ledger row R2-47 embeds a false evidence count** (2,692 vs measured 2,688; 10_ says 2,671). Three counts in circulation for one deterministic artifact.

**N5 — MEDIUM-LOW: superseded-path traps.** b4-verify.mjs:168 still prints `next: b1 --uids=…` — the --uids path emits a manifest without parent hashes, which `loadDeltaLayer` now rejects; b-baseline.mjs:2 header still describes the `--uids` chain. An operator following B4's own instruction bricks at B3.

## 3. STAGE-1 FREEZE: **NO**

Genuinely strong round — the fixture, the txn fence, resume binding, the ledger, --deltaAuth, roster churn, and the shadow predicate/isolation rewrites are real closures. But the freeze bar is "nothing REMAINING blocks the dark build," and the minimal remaining set is:

1. Fix the b-delta-cycle.sh layer glob (`b4-runs/`) + purge the --uids hints (N1/N5).
2. Write the post-flip rru/reconciliation law and fence B4's rru assertion + `--repairExtras`; correct 14_ §4's "write-freeze" sentence (N2/N3).
3. Purge the four remaining rru-seed residues: b1 header (:7, :12-13), b1 summary `law:` string (:193), b1-replay-lib header (:3-6), **H6 §10:257** (item 6).
4. One fixture count everywhere = the number the current file emits (2,688, or re-land the fixture): 11_ R2-47, 10_:41, change log (N4).
5. Loader exactness: require `classesMatched` unconditionally (at least for mode `full`), positive-finite original watermark, exact version; B3 refuses truncated b4-reports; fix or delete B4's "enumerated FULLY" header claim (item 4).
6. Mirror the differing-size snapshot truth into H6 §2's field list + 10_ §2, and one sentence fixing §3b `sourceConfig` provenance (item 6-C3).
7. Shadow: name the two-mode `evaluateThresholds` API as a dark-build deliverable; state the studentIds-from-reduced-set clone rule; give the authority-union/stamp-leg negatives a durable home (amend 16_ §3's `ownerTeacherId === caller` reduction) (item 7).
8. Stage-2 carry (not stage-1-blocking but must be recorded): the reset-race fixture r61-A3 demanded, Windows tests + B1 `--outDir` pathToFileURL, large-cohort RSS evidence.

Items 1-7 are hours of doc/script work, not redesign. Nothing found contradicts the R2 ledger's decisions themselves (R2-38..47 substance is faithfully carried); the failures are evidence-accuracy and one-law-everywhere discipline — exactly what a contract freeze exists to guarantee.