# 3-Fable panel — DEEPFIX2 r63 freeze attempt (2026-08-02)

Verdicts: protocol CONDITIONAL-YES (micro-fold) · postflip NO · shadowlaw NO. Ran SIMULTANEOUSLY with Codex r63 (also NO). Convergent core: all r62 blockers genuinely closed; residue = fc cumulative law (Codex A1 = postflip-adjacent), repair-chain resolution (Codex A2), ledger attempt-pairing/absence/dir-fsync, plans-file custody, dark-window custody contradiction (postflip N2/N3 — law-level), postFlip false-RED on adjudication+re-enrollment (postflip N1), quarantine underspecification + fidelity-gate unrunnable + partition contradiction (shadowlaw N3-N6), smoke-lap cards name the deleted .sh at sub-required scope (all three + Codex A5).


---

## LENS: protocol

All verification complete. Compiling the verdict.

---

# Panel review r63 — Lens: the convergence-protocol end-state (A1/A4/A6 as one machine)

**Reviewer basis:** read every changed artifact; ran `delta-chain-fixture.mjs` (**47/0**, matches handoff) and `rotation-cyclicity-fixture.mjs` (**2,688/0**, matches); `node --check` clean on all 10 scripts; `b-delta-cycle.sh` confirmed deleted (commit 2f80e6d). Additionally built an offline harness (scratchpad, not repo) that **executed** B4's strict ledger gate (it runs before credential load) against 8 planted-ledger scenarios, and **executed** the real `b-delta-cycle.mjs` against stub B4/B1/B3 across 12 exit/parse scenarios. The b1/b3/b4 CLIs against live Firestore: static review only (credentials).

## (1) r62 blockers

| Blocker | Verdict | Evidence |
|---|---|---|
| **A1** driver rejects actionable delta | **CLOSED** | `b4-verify.mjs:217-221,252-255`: `DIFFS-WITH-ACTIONABLE-DELTA`→exit 7; exit 5 now provably diffs-with-NO-delta only; unmapped verdict→exit 2 (:255). Driver `b-delta-cycle.mjs:48-53` continues only on 6/7 AND the exact `MATERIALIZED_DELTA_DIR` line AND `delta-auth.json` existing — verified live: exit 7+line+auth → full lap → chain passthrough in order → PASS; 7-without-line→2; line-to-dir-without-auth→2; exit 5→5; exhaustion→7. CRLF + trailing-slash line parsing verified (regex `$` before `\r` + `.trim()` + slash-strip, :49-50). Named args, argv[4] wart gone, bash driver deleted. Emulator laps (a)-(e): **not run — honestly carded**, see §3. |
| **A2** doc-wide isLiveDoc | **CLOSED** | `b-baseline.mjs:26-34` per-field law; `b4-verify.mjs:167` corrupt short-circuits **before** the exemption (never exempt); fixture case :329 is exactly Codex's counterexample (fresh unrelated stamp does NOT exempt fc). Extras excusal requires EVERY present owned field live (b4:185-198). |
| **A3** repairExtras chain custody | **CLOSED** | `b3-backfill-writer.mjs:130-142` FATAL unless ordered `--appliedDelta` sha sequence ≡ `report.appliedDeltas` exactly; `:75` requires `extrasDeletionLaw:"all-six-present"` (B4 stamps it, b4:224); `:77-85` exact/unique tuples, fields ⊆ six. Stale-M0-report + M1-word now dies pre-write. |
| **A4** durable fail-closed ledger | **CLOSED at law level** | Intent before first write `b3:302`, fsync'd append `b3:212-216`, completion with outcome `b3:354-358`. B4 strict parse `b4:55-80` **verified by execution**: malformed line→FATAL 2; unknown probe→FATAL (even for another original); intent-without-completion→FATAL; completion with txnFailures>0→FATAL; EXECUTE'd layer missing from `--appliedDelta`→FATAL; clean pair→gate passes. Residues in §2 (items 3-5) — none reaches false-PASS. |
| **A5** per-attempt resume identity | **CLOSED** | `b3:194-199` per-attempt `resume-N.preimage.jsonl`/`resume-N.result.json`, no appends; the attempt counter probes BOTH filenames so a crashed attempt's partial preimage is left immutable and the next attempt gets N+1 (:195). Orphan rule `b3:158-167` un-bricks the backup-without-manifest dead end. Residue: `plans.resume.jsonl` is overwritten across attempts (§2 item 6). |
| **A6** exactness | **CLOSED** | Strict chain order `b-baseline.mjs:163-167`, enforced B3 (:136) + B4 (:51) — the `>=` resolver tie unreachable; row shape both loaders incl. per-get (:39-44,:92,:118); DA version/uniqueness/nonempty in the shared loader (:135-139); departed strings/unique/⊆-auth (:147-151); **union** departures b4:104-106; all-departed → auditable EMPTY layer is real B1 code (`b1:107`, writes zero-row artifacts that load clean) + fixture n9. Residue: B1's own `--deltaAuth` reader (b1:98-100) still skips version/uniqueness — fail-closed at consumption, so immaterial. |
| **B1** one reduced-scope law | **CLOSED (doc)** | `16_:114` "--uids interface is DELETED from this plan"; `16_:89` clone-time `studentIds` partitions + generated-manifest gate through the existing `--classAllowlist` surface. Note: B1's `--uids` **code** path and B4's `uids.json` emission (b4:239-240) survive as dead bait — every route into the chain fails closed (no parent hashes ⇒ `loadDeltaLayer` rejects). |
| **B2** generation probe | **CLOSED (doc)** | `16_:89` quarantine-by-construction (registryGeneration stamped per row; stale generation never production-classified; injection test specified; probe demoted to diagnostic). |

## (2) NEW defects (adversarial construction results)

I tried to construct: a strand (driver stops when it should continue), a wrong-CONTINUE that corrupts, a wrong-PASS, and a brick. **No false-PASS and no brick is constructible** — the load-bearing reason is that PASS is *value-defined* (actual ≡ chain-resolved expected, ledger⊆chain enforced), so every ledger/driver gap I found degrades to a loud red or misdiagnosed stop, never green. What remains:

1. **MEDIUM — exit-code namespace collision, confirmed live.** B4:133, B3:228, B1:186 all `process.exit(3)` on the A8 cross-list-wordId collision (stop-and-investigate). The driver passes 3 through (`b-delta-cycle.mjs:48,57,63`), and its own contract (:13-14) defines exit 3 = "B3 skipped students — rerun with --resume when quiet." Scenario: A8 fires → driver exits 3 → operator/stage-2 automation follows the documented advice and re-runs forever against a structural data hazard. The frozen exit contract is wrong for a code the machine emits. Two-line fix (remap pass-through 3→2, or document 3 as ambiguous).
2. **MEDIUM — driver re-invocation trap.** runId = `prefix-c{cycle}`, cycle restarts at 1 every invocation. After any completed cycle-1 EXECUTE (exhaustion exit 7, crash, B1 failure), the natural re-run `--prefix=p --appliedDelta=…` reaches B3 `--runId=p-c1` → FATAL "runIds are single-use (use --resume…)" (b3:166) → following THAT hint FATALs again ("resume with a DIFFERENT delta layer", b3:177). Fail-closed at every step, but two consecutive misleading recovery hints, a fresh orphaned layer dir per attempt, and the exhaustion path never prints the accumulated applied chain the re-run needs (the ledger FATAL names runIds, not layer dirs). Fix: nonce in runId or a one-line usage law ("fresh --prefix per invocation; re-pass all applied layers").
3. **LOW/MEDIUM — the ledger is same-checkout, fail-open on absence, and its creation isn't dir-durable.** `b4:57` skips the entire audit when the file doesn't exist; `ledgerAppend` fsyncs the fd but never the parent directory, so the first-ever append's *file creation* can be lost in a host crash — the precise A4 crash the intent record exists for can erase the intent along with the file, silently disabling the mandatory audit. Also invisible: EXECUTEs from any other checkout/machine. Backstopped by the value diff (committed chunks either match declared-chain expected or show as diffs), but the "mandatory audit authority" sentence should carry this boundary, and stage-2 crash-injection must include the first-append window.
4. **LOW — intent/completion matched at runId, not (runId, attempt) — verified by execution (T7/T8).** A crashed resume attempt's dangling intent is invisible whenever any clean completion for that runId exists; a runId-reusing later run's applied record shadows the earlier layer's under latest-wins. I could not convert either into divergence (a clean completion ⇒ journal-complete ⇒ a subsequent resume has nothing to write; resume binds delta/extras hashes; shadowed-layer states surface as diffs), but this is coarser than r62's "duplicate records ⇒ fail" letter and it needs the b3-runs artifacts intact to stay benign.
5. **LOW — legacy-shape tolerance in the strict parse.** `b4:73` `e.outcome || {}` treats an outcome-less `b3-applied` as clean, and `version` on known probes is unchecked — a pre-r63-format or future-format line passes the "any malformed line ⇒ FATAL" law. No such lines exist today (no EXECUTE has ever run; audit dir has no ledger).
6. **LOW — A5 custody residue.** Each resume attempt's `writeplanSha256` is recorded in its immutable result, but the file it hashes (`plans.resume.jsonl`) is clobbered by the next attempt — the sha becomes unverifiable for all but the last attempt.
7. **DOC — the frozen law cites the deleted driver.** `14_:92` and the 02_ DF2-14 smoke-lap card both still name `b-delta-cycle.sh`. The smoke-lap card that the freeze would freeze points at a file that no longer exists.
8. **Cosmetic / packet accuracy:** the handoff's "NODE_PATH derived from the repo root" is mechanically inert — these are ESM modules and ESM ignores NODE_PATH; resolution works via the upward node_modules walk (behavior right, claimed mechanism wrong). `--maxCycles=3.7` silently truncates to 3. `b1:122` dead `mode2` var. Driver exit 7 (exhausted) shares a number with B4 exit 7 (actionable) — documented, and "rerun" happens to be the right response to both.

## (3) STAGE-1 FREEZE: **YES — conditional on a micro-fold; the emulator-lap set may be the stage-2 gate**

- **The machine is law-sound.** The A1/A4/A6 closures compose: exit 7's continue condition is machine-checked and fail-closed in every mutation I threw at it; continuation is convergent by construction (B3 writes only toward layer-expected values, watermarks strictly ascend, churn bounds at maxCycles with a loud non-green stop); the ledger law is strict where it exists and every gap is backstopped by the value-defined PASS. Mixed structural+delta drains to a correct exit-5 stop; all-departed drains to an auditable empty layer, not a brick. I could not reach a wrong PASS or a bricked convergence.
- **Conditions that should ride the freeze packet (text/two-line-code level, not laps):** (a) fix or re-document the exit-3 collision (§2.1) — freezing a wrong exit contract is freezing a defect; (b) correct the two stale `b-delta-cycle.sh` references (§2.7) — the freeze must not cite a deleted artifact; (c) one sentence bounding the ledger's authority to same-checkout-with-file-present (§2.3).
- **Emulator laps: stage-2 gate is acceptable — barely, and only as carded.** Both r62 blockers lived in CLI glue no repo fixture executes, and r63 *rewrote that same glue* (new driver, new verdict law, new ledger parse); the repo's fixture still executes none of it — my stub-harness execution is review evidence, not repo evidence. Note 02_'s own card language ("the chain must RUN … before any future freeze/gate claim cites it"): the r63 packet honors this by not claiming execution, so **freeze-YES-with-the-lap-as-stage-2-gate** is consistent — provided the lap set (r62's (a) new-attempt/zero-diff, (b) roster-added+diffs→exit 7, (c) in-place adjudication+diffs→exit 7, (d) mixed structural stays terminal exit 5, (e) all-departed empty layer, + crash injection now including the first-ledger-append window and a driver re-invocation lap per §2.2) remains the FIRST stage-2 act, blocking 25WT and everything after. If the micro-fold cannot ride the freeze, then NO with exactly that three-item set — nothing larger is warranted.

---

## LENS: postflip

All evidence gathered. Composing the verdict.

---

# Fable panel r63 — reviewer lens: post-flip reconciliation + writer custody (A2/A3/A5)

**Independent measurements:** `delta-chain-fixture.mjs` **47/0**, `rotation-cyclicity-fixture.mjs` **2,688/0** (both match the handoff — the r62 packet-accuracy failure is not repeated). `node --check` clean on all 8 `.mjs`. `b-delta-cycle.sh` confirmed deleted; commit surface `2f80e6d` matches the handoff's enumeration.

## (1) r62 blockers

| # | Verdict | Evidence |
|---|---|---|
| A1 | **CLOSED (static)** | `b4-verify.mjs:217-221,252-255` — `DIFFS-WITH-ACTIONABLE-DELTA`→exit 7, exit 5 = structural only, unmapped verdict→exit 2. `b-delta-cycle.mjs:43-64` — Node/cross-platform, named args, repo-root NODE_PATH (`:31-32`), continues on 6 AND 7 only under the machine-checked `MATERIALIZED_DELTA_DIR=` line + `delta-auth.json` existence (`:49-53`), B3 exits mapped 4→4, 5→3 (`:61-62`). CLI/emulator laps (a)–(e): not run — see freeze ruling. |
| A2 | **CLOSED as specified — but the same gate has new law-level holes (N1/N2)** | Doc-wide `isLiveDoc` deleted; `isFieldLiveExempt` per-field, fc rides only on `reviewLastFailedAt` (`b-baseline.mjs:26-34`); corrupt-typed bypasses exemption (`b4-verify.mjs:167`, `:176`); extras exempt only when EVERY present owned field is live (`:185-199`). The r62 counterexample is fixture case `delta-chain-fixture.mjs:329`. The exemption side is sound; the **recomputed-expected side** is where the gate now breaks (below). |
| A3 | **CLOSED as specified — residue N3/N4** | Ordered `--appliedDelta` chain must equal `report.appliedDeltas` exactly (`b3-backfill-writer.mjs:127-142`); postFlip/truncated reports refused (`:73-74`); `extrasDeletionLaw:"all-six-present"` required (`:75`) and stamped (`b4-verify.mjs:224`); tuples validated unique/nonempty/⊆six (`:76-85`). |
| A4 | **CLOSED (static; crash-injection = emulator scope)** | fsync'd intent before first write (`b3-backfill-writer.mjs:209-216,302-303`), completion-with-outcome after (`:354-358`); B4 strict parse: malformed line/unknown probe/intent-without-completion/failures-or-skips all FATAL (`b4-verify.mjs:58-79`). |
| A5 | **CLOSED as specified — residue N5** | Per-attempt immutable `resume-N.preimage/result` (`b3-backfill-writer.mjs:194-199,349-351`); orphan backup-without-manifest set aside, fresh start legal (`:158-166`) — and the ordering is right: the manifest publishes after the preimage rename (`:279-293`), so no-manifest genuinely proves zero writes. |
| A6 | **CLOSED** | `assertLayerChainOrder` strict (`b-baseline.mjs:163-167`, enforced `b4-verify.mjs:51`, `b3:136`); row shape both loaders incl. indexed per-get (`b-baseline.mjs:39-44,92,119`); DA version-2/unique/nonempty (`:135-139`); departed law (`:147-151`); departures from union (`b4-verify.mjs:104-106`); all-departed → auditable empty layer (`b1-expected-labels.mjs:107`, fixture n9). Minor: a joiner covered only by delta-**auth** (never any layer's rows) who departs still vanishes from `departedSkipped` — audit-count only. |
| B1 | **CLOSED at plan level** | `16_ :114-120` — `--uids` deleted from the plan, clone-time `studentIds` ≡ partition, generated-manifest pre-run gate. Note: the `--uids` code path survives in `b1-expected-labels.mjs:109-117` — chain-dead (no parent hashes ⇒ `loadDeltaLayer` rejects) but it can clobber the default-outDir delta artifacts; footgun, not authority. |
| B2 | **CLOSED at plan level** | `16_ :89` — probe pretense dropped; `registryGeneration` stamped per row, stale-generation quarantine both evaluators, stale-cache injection test specified, `getShadowRegistryGeneration` demoted to diagnostic. |

## (2) NEW defects (lens findings, concrete counterexamples)

**N1 — The ONE post-flip reconciliation is structurally false-RED on two legitimate post-flip events, with no remediation path in the law.**
Under `--postFlip`, expected recomputes from `computeStudentLabels` at boundary=flipTs (`b4-verify.mjs:131-137`), which replays pre-flip attempts using their **current** rows (`b1-replay-lib.mjs:111-121`).
- (a) *Post-flip challenge acceptance of a pre-flip attempt* (14_ §2's own in-place-mutation law): the accept fixes `isCorrect`/score in place → expected flips to fc=0/lf=null/lc=t₁, while the actual doc holds the B3-written pre-adjudication truth fc=1/lf=t₁ (< flipTs) — `isFieldLiveExempt` correctly refuses (values are pre-flip) → non-exempt diffs on lf, fc, lc. B3 is FORBIDDEN post-flip (`b3:106-109`), so nothing can ever converge it: the final launch verdict is permanently `DIFFS`. The machinery to exempt it exists and is ignored — `mutationRisk.adjudicatedAtOrAfterWatermark` / `challengeReviewedAt ≥ flipTs` (`b1-replay-lib.mjs:40-43`) is computed and never consulted by the verdict. Any challenge pending at the flip and adjudicated before the reconciliation run triggers this.
- (b) *Post-flip re-enrollment with pre-flip history*: 14_ §4:108 rules them "converges through live use — advisory"; the code counts their absent labels vs recomputed expectations as PASS-blocking diffs (`src.row` null → recompute; absent fields never exempt). The law and the verifier contradict each other on 14_'s own named case.

**N2 — The dark-window writer-custody contradiction is frozen into the contract set.**
02_ DF2-14 contract (7) + R2-32/R2-38 (11_ §1): "fail+correct write while OFF." R2-31: the dark deploy sets `enabled:false` on **every surface**. 14_ §4:98: "the flip is NOT a write-freeze for labels; it's the moment LIVE label writes BEGIN." Both cannot hold: if the dark-deployed client/backend stamps under the OFF law during the backfill window, then (i) B3's txn core **force-overwrites** those live stamps with replay values (`b3-txn-core.mjs:46-58` writes expected wherever txn-state differs — the precise harm 14_:99-100 forbids post-flip exists all through the window), and (ii) the final B4 can never reproduce OFF-window **blank=fail** stamps (the replay iterates `a.answers` only — `b1-replay-lib.mjs:113`; blanks have no rows; the live writer stamps them from `presentedWordIds` per 15_ §1:16) → non-exempt pre-flip-timestamp diffs at the final gate for whatever B3 didn't already erase. If instead the writers are gated on `enabled===true`, R2-32's ratified kill-switch law ("accumulated OFF-window fail stamps are inert-then-truthful") is unimplementable — same flag, same code. No clause anywhere pins client-exposure timing vs the OFF-stamping law, and 15_ §7's schema has no field that could distinguish dark-pre-first-flip from killed-post-flip.

**N3 — "B3 NEVER runs post-flip" is enforced by a flag the kill switch resets.**
The guard is `enabled === true` (`b3-backfill-writer.mjs:106-109`). A post-flip kill switch (R2-32) restores `enabled:false` → B3 and `--repairExtras` (against a still-pre-flip-stamped report) run "legally" during exactly the incident window in which, per contract (7), the live server is still writing fail/correct stamps — B3 then reverts truthful OFF-window stamps to watermark-era values. Needs a durable flipped-once marker (e.g. `firstEnabledAt`, never cleared — grep confirms none exists in any schema or script).

**N4 — repairExtras chain custody trusts the report's self-declared chain; `--ignoreLedger` provenance is unrecorded.**
B4 run with `--ignoreLedger` (offered for forensics, `b4-verify.mjs:54`) while an EXECUTE'd layer M1 exists emits a report with `appliedDeltas:[]` — indistinguishable from a clean report (the report schema records no `ignoreLedger`, `:222-226`). B3's equality gate (`[] ≡ []`) passes (`b3:137-141`); B3 never reads the ledger itself → the stale-M0-deletes-M1-word scenario r62-A3 named survives one level up, behind one operator flag. Fix: stamp `ignoreLedger` into the report and refuse it in B3, or run the ledger audit in B3.

**N5 — Resume-attempt custody: attempt-number race + a shared mutable plans file.**
Two concurrent `--resume` processes scan the same directory (`b3:195`) and can both select attempt N; `createWriteStream` default `'w'` truncates — interleaved writes make `resumePreimageSha256` verify nothing (fix: `flags:'wx'` + a run lock). Independently, **every** resume writes `{runId}.plans.resume.jsonl` (`b3:200`) — resume N+1 clobbers resume N's plan file, so `resume-N.result.json.writeplanSha256` references a destroyed artifact: the exact r62-A5 shape ("recorded hash can't be checked against the final sidecar") reproduced one artifact over. Preimage/result immutability itself: real.

**N6 — The frozen law names the deleted driver.**
14_ §4:92,95 and the 02_ DF2-14 smoke-lap card (the David-ratified stage-2 opening act) both instruct `b-delta-cycle.sh`, deleted this round. The smoke-lap card as frozen is unexecutable as written.

Minor: `isFieldLiveExempt` has no upper sanity bound (a rogue future-dated stamp on the five event fields self-exempts; server-only rules make this remote); departed-joiner audit undercount (A6 note); driver exit 7 overloads B4's exit 7 (cycles-exhausted vs actionable) — cosmetic.

## (3) STAGE-1 FREEZE: **NO**

The r62 blockers are genuinely closed to their letter — this is real closure, honestly measured. But the freeze would seal a contract that is **internally inconsistent about who owns the six fields around the flip**: N2 (two ratified clauses contradict over the dark window), N1 (the one post-flip gate is unavoidably false-red on two events the law itself names as legitimate, with no disposition clause), N3 (the custody guard does not implement its own law under kill-switch revert). These are law-level defects; **no emulator lap can surface N2 or N3** (a seeded-fixture lap has no dark-window live traffic and no kill-switch timeline), and the carded lap set contains no post-flip-adjudication case for N1.

**On the emulator-lap question specifically:** yes — the lap set may remain the stage-2 opening act rather than a freeze precondition. It is execution evidence for already-specified behavior (A1's CLI composition, A4's crash injection), not contract content; holding the freeze for it would gate law on evidence the law doesn't need. Two caveats: (i) the 02_ card's own ratified wording — "the chain must RUN … before any future freeze/gate claim cites it" — means the freeze text must not claim chain-execution closure (r63's A1 claim is static-only and should say so); (ii) the lap must gain the N1(a) post-flip-adjudication case once N1 closes.

**Minimal falsifiable closure set:** (1) one clause pinning dark-window custody — client-exposure timing vs the OFF-stamp law, with either a no-stamp-before-first-flip marker or B3/B4 merge-not-overwrite semantics for live-stamped fields (N2); (2) `--postFlip` exemption for post-flip-adjudicated attempts (the `mutationRisk`/`challengeReviewedAt ≥ flipTs` machinery already computes it) + informational classification for post-flip re-enrollment diffs, each with a fixture counterexample proving the false-red dies while the r62 false-green stays dead (N1); (3) a durable flipped-once marker gating B3 (N3); (4) `ignoreLedger` stamped in the b4-report + refused by B3 (N4); (5) `'wx'` resume streams + per-attempt plans files (N5); (6) purge `b-delta-cycle.sh` from 14_/02_ (N6). All six are bounded edits; none reopens the r62 set.

---

## LENS: shadowlaw

All evidence gathered. Composing the verdict.

# DEEPFIX2 r63 — Reviewer verdict (lens: doc-law coherence + shadow executability)

**Independently executed:** `node --check` all 10 `scripts/deepfix2/*.mjs` — pass. `delta-chain-fixture.mjs` = **47 checks / 0 failures**; `rotation-cyclicity-fixture.mjs` = **2,688 / 0** — both match the handoff (packet-accuracy item C: clean). `b-delta-cycle.sh` confirmed deleted; `b-delta-cycle.mjs` present. Grep for `--uids` across docs/scripts executed. B1/B3/B4 CLIs need credentials — static review as instructed.

## 1. r62 blockers — CLOSED / MISS

| Blocker | Verdict | Evidence |
|---|---|---|
| A1 actionable-delta outcome + cross-platform driver | **CLOSED at law level** | `b4-verify.mjs:217-221` verdict law; `:252` exit 7 = `DIFFS-WITH-ACTIONABLE-DELTA`; `:253` exit 5 only for delta-less DIFFS; `:255` unmapped verdict → exit 2. `b-delta-cycle.mjs:48` continues on 6 AND 7; `:49-53` machine-checked condition (exact `MATERIALIZED_DELTA_DIR=` line + `delta-auth.json` exists); `:31-32` NODE_PATH from repo root; named args; `:61-62` B3 exit mapping (4 stops, 5→3 stops). Bash driver deleted. **But the closure is static-review-only** — no fixture executes B4's verdict branch or the driver, and the carded emulator laps do NOT cover Codex's (a)-(e) (see §2-N1). |
| A2 per-field post-flip exactness | **CLOSED** | `b-baseline.mjs:26-34` `isFieldLiveExempt`: exemption only via the field's OWN timestamp ≥ flipTs; `reviewFailCount` only via `reviewLastFailedAt` (`:28-31`). `b4-verify.mjs:167` corrupt-typed counted as diff BEFORE any exemption; `:170` per-field exemption; `:185-198` extras docs exempt only when EVERY present owned field is live-exempt. The r62 counterexample is the fixture case: `delta-chain-fixture.mjs:329` (fresh unrelated stamp does NOT exempt fc) — passes failing-closed. |
| A3 repairExtras chain custody | **PARTIAL — the r62 counterexample itself still passes pre-write** | Delivered: `b3-backfill-writer.mjs:75` `extrasDeletionLaw:"all-six-present"` required (B4 stamps it, `b4-verify.mjs:224`); `:76-85` exact unique tuples, fields ⊆ six; `:130-141` ordered `--appliedDelta` chain must sha-equal `report.appliedDeltas`. **Miss:** the check binds report ↔ *invocation*, not report ↔ *reality*. B3 `--repairExtras` never reads the applied-layers ledger. Concrete replay of Codex's exact scenario: B4 report R0 produced before any layer (`appliedDeltas:[]`, legitimately — B4's ledger audit passes on an empty ledger) → B3 EXECUTEs layer M1 (word X becomes expected) → `B3 --repairExtras=R0` with no `--appliedDelta`: `mine=[] === theirs=[]` at `:137-139` → accepted → deletes X's six fields → exit 0. Codex demanded "a pre-write rejection" for precisely this; it does not exist. Mitigation (real but not what was demanded): PASS requires zero extras (`b4-verify.mjs:204,218-219`), so a post-repair B4 lap is structurally forced before any PASS and catches the damage. Fix is one check: repairExtras must FATAL unless `report.appliedDeltas` covers every ledgered EXECUTE for this original (B4 already has that audit at `b4-verify.mjs:71-78`; B3 lacks it). |
| A4 durable fail-closed ledger | **CLOSED (code); crash-injection evidence deferred and NOT carded** | `b3-backfill-writer.mjs:212-216` write+fsync+close per append; `:302-303` intent before the first write; `:354-358` completion with outcome. `b4-verify.mjs:58-78` STRICT parse: malformed line FATAL (`:64`), unknown probe FATAL (`:67`), intent-without-completion FATAL (`:70`), latest completion with failures/skips FATAL (`:74`), EXECUTE'd layer missing from `--appliedDelta` FATAL (`:75-78`). The handoff's "Crash-injection runs = emulator-lap scope (**carded with the smoke lap**)" is false — no crash-injection text exists on the 02_/14_ cards (grep; the only "crash" hits are DF2-12/DF2-34 client-side cases). |
| A5 per-attempt resume identity | **CLOSED** | `b3-backfill-writer.mjs:194-199` immutable `{runId}.resume-N.preimage.jsonl` per attempt (hash over exactly that file, `:347`); `:349-351` per-attempt `resume-N.result.json`; `:158-167` manifest-less backup = orphaned `.orphan-<ts>` + legal fresh start. |
| A6 exactness | **CLOSED** | `b-baseline.mjs:163-167` strictly-increasing chain (enforced `b4-verify.mjs:51`, B3 repair chain `b3:136`); `:39-44` row shape validated in eager (`:119`) AND indexed per-get (`:92`); DA v2 unique nonempty uids (`:135-139`); departedUids strings/unique/⊆auth (`:146-152`); B4 union departures (`b4-verify.mjs:102-106`); all-departed empty layer loads clean (fixture `:364-377`, B1 emits it `b1-expected-labels.mjs:107`). |
| B1 one reduced-scope law | **CLOSED in 16_** (residue notes) | Both cited residues deleted (r63 diff to `16_:110-137`): §3 executable law now clone-time class membership + generated `scope-<runId>.json`; the fidelity gate now `--full --classAllowlist --watermark`; PRE-RUN generated-manifest gate added (`16_:118-120`). Grep: the only `--uids` in 16_ is the deletion tombstone. Residue: B1's `--uids` CLI mode is still live code (`b1-expected-labels.mjs:70,109-117`) — isolated only by downstream loader rejection (no parent hashes), and its artifacts share mode-keyed filenames with `--deltaAuth` outputs (an overwrite footgun in a shared `--outDir`); `b-baseline.mjs:127` still describes delta layers as "the fresh-watermark B1 --uids artifacts", contradicting line 3 of the same file. |
| B2 generation probe | **REPLACED — the direction Codex demanded is closed by construction on paper, but the law as written is underspecified and not folded into its build card** (see §2-N3/N4) | `16_:89` — writer stamps `registryGeneration`; production evaluator quarantines stale-generation rows during audit windows; audit evaluator consumes current-generation shadow rows; injection test specified; `getShadowRegistryGeneration` demoted to diagnostic. |

## 2. NEW defects (concrete failure scenarios)

**N1 — The smoke-lap cards mandate a DELETED artifact, and the carded scope ≠ the claimed scope. BLOCKER for the freeze-as-asked.**
The same commit (2f80e6d) that deleted `b-delta-cycle.sh` *added* smoke-lap cards naming it: `02_TASK_LIST.md` DF2-14 ("one delta cycle via **b-delta-cycle.sh**"), `14_:92` ("one **b-delta-cycle.sh** lap"), and `14_:95` (endgame law: "the driver = `b-delta-cycle.sh`") was left stale. An operator executing 14_ §4 verbatim runs a nonexistent file. Worse for the ruling being requested: the handoff equates "your (a)-(e)" with "the David-ratified smoke lap already carded" — but the card text is **one lap of the happy chain**; it enumerates none of (a) new-attempt/zero-diff, (b) roster-added+diffs, (c) in-place adjudication+diffs, (d) mixed structural diff must remain a final failure, (e) all-departed — and no crash injection (A4's four points), no A2 postFlip lap, no A5 resume lap, no A3 stale-report negative. If freeze-YES-with-lap-as-gate were granted on this card, the gate that gets frozen is the *thin* lap, not the closure set Codex demanded.

**N2 — The packet's own ratified law forbids the freeze reading it requests.**
The DF2-14 card (David-ratified 2026-08-02, added this round): "the chain must RUN, not merely be read, **before any future freeze/gate claim cites it** (both r62 blockers lived in CLI glue no fixture executes)." The r63 freeze claims for A1/A3/A4/A5 live exactly in CLI glue no fixture executes (B4 verdict/exit branch, driver, ledger strict parse, repairExtras custody — the fixture honestly disclaims CLI coverage, `delta-chain-fixture.mjs:12-14`). By the program's own card, those closures are not citable as freeze grounds until the lap has run.

**N3 — The quarantine law can mask a REAL production signal during an audit window, with zero required visibility.**
`16_:89` as written: during an audit window the production evaluator quarantines *any* row with `generation < window generation` — including **real** rows (shadow:false) written by instances inside the ≤60s TTL staleness window after each registry bump; "never alert-feeding" makes them permanently invisible to the R2-18/DF2-14 abort evaluation, silently. Bounded in the intended case — but the law requires no quarantine-count publication, so the failure mode is unbounded under a stamping bug: a writer that stamps generation 0/null on every row blackholes ALL production rows for the entire multi-day audit window while battery I's stated assertions (production sees zero shadow rows; the planted G−1 row lands in quarantine) still pass — the audit certifies "monitoring works" during the exact window production monitoring was structurally blind. Additionally: (i) "an audit window" / "the window's registered generation" has no defined artifact (registry-doc existence? a window record? — unstated, and the registry is deleted at cleanup); (ii) rows **missing** the stamp escape quarantine (`undefined < G` is false) — during dark-deploy revision skew, an old-revision writer emits unstamped rows, so a stale instance CAN emit a production-classified shadow-mis-tagged metric: the "BY CONSTRUCTION" claim has a deploy-skew hole; the law needs "no/invalid generation during a window ⇒ quarantine, and quarantine volume is itself a published signal"; (iii) the law binds only the two evaluator invocations — §2.5's baseline computations filter by the `shadow` tag alone, so a stale-cache mis-tagged shadow row (shadow:false, gen G−1) is quarantined by the evaluator yet **consumed by the monitoring baselines**.

**N4 — Build-card drift on the same law.** 02_ DF2-14's evaluator card (the named build home) still specifies only "production scope consumes only `shadow !== true` rows" + the diagnostic callable — no `registryGeneration` stamping duty for ops_metrics writers, no quarantine duty for the evaluator. A builder working from the card builds no quarantine; 16_ and 02_ now give different answers to "what must the dark build do". The r63 16_ edit was not folded into its build home.

**N5 — The clone-fidelity gate is not executable as written.**
`16_:134-137`: shadow B1 "must equal the real baseline modulo the id map (mapped-digest byte-diff)". But `challengeDigest` is a sha256 whose preimage embeds **attempt docIds and classIds** (`b1-replay-lib.mjs:51` `${d.id}|...`, `:91` `R|${d.id}|${sig}|...` — sig embeds classId — `:136` hash), and every eligible attempt contributes an R| row. The clone remaps attempt ids and classIds, so shadow `challengeDigest` ≠ real for essentially **every student with any attempt**; you cannot map ids inside a hash, and the preimage is not in the artifact. As written the gate fails 100% spuriously. (The per-student `digest` at `:138` hashes only `wordsOut` — id-free `listId|wordId` keys with numeric values, lists shared un-remapped — so it IS directly comparable; the law must say: compare per-uid `words`/`digest` + `epochByList` under the uid map, challengeDigest and `challengedAttemptIds` excluded or mapped-recomputed.) Three more executability gaps in the same clause: (a) the real baseline has 947 rows, the shadow run 947−k (flagged-skip pruned) — byte-diff needs the real side filtered to the manifest's reduced set, unstated; (b) roster drift between the real baseline's run time and clone time (26SM is active; joiners are why `rosterAdded` exists) produces row-set mismatches the gate has no reconciliation clause for; (c) the gate silently assumes the attempts clone allowlist ⊇ the replay-law field set (`preOverride.score`, `teacherEdited`, `manualOverride`, `challengeStatus/ReviewedAt`, `score/scorePercent`, `totalQuestions`, `dayNumber/studyDay`, …) — §1 enumerates only the rewritten fields; one omitted field (e.g. `preOverride`) shifts exclusions and fails the gate.

**N6 — §1 vs §2.11 class-structure contradiction.**
§1: shadow classes are the 33 allowlist classes 1:1 (`docId = shdw{origId}`, assignments verbatim, composite `{classId}_{listId}` ids rebuilt from *the* mapped classId). §2.11: "each shadow class's `studentIds` = exactly the reduced uid set for that partition … A partition = its own shadow class(es); **partitions never share a shadow class**", where "partition" is §2b's defined term (C ~30 stratified across bands · D ~100 · E ~20 · F ~3 · G dual-enrollment …). Counterexample: real class X holds s1 (dual-enrolled → G) and s2 (concurrency → D); mirror law forces both into `shdwX` → G and D share a shadow class → §2.11 violated; satisfying §2.11 requires splitting X → violates `shdw{origId}` 1:1, makes the `{classId}_{listId}` remap and attempts' classId re-derivation ambiguous, and breaks assignments-verbatim. Cross-class strata (~30 stratified accounts over 33 classes averaging ~29 students) make partition-aligned whole classes unsatisfiable. One reading must be chosen and written (cleanest: partitions are driver-side account lists; `scope-<runId>.json` class allowlists exist only for whole-class B1/B3/B4 scopes; delete or rescope the "never share a shadow class" sentence) — as frozen, the two clauses cannot both hold, and N5's gate depends on which one wins.

**N7 — minor:** `b4-verify.mjs:107` logs "B4 v3" (version is 4); driver exit 7 (cycles exhausted) collides numerically with B4's exit 7 (documented, but a trap for wrapper scripts).

## 3. STAGE-1 FREEZE: **NO**

The Track-B code closures are largely real (A2/A4/A5/A6 verified in code; A1's outcome model correct as read), fixture counts verify, and 16_'s reduced-scope law is now genuinely ONE law. But: A3's demanded pre-write rejection is absent (the r62 counterexample still passes B3 pre-write); the 16_ generation/quarantine law is underspecified at freeze grade, carries a production-signal-masking hazard, and contradicts its own build card; the clone-fidelity gate is unrunnable as written; §1/§2.11 contradict; and the smoke-lap cards mandate a deleted driver at sub-required scope.

**On the emulator-lap question specifically: the laps must PRECEDE freeze — freeze-NO-until-laps-run.** This is not reviewer preference; it is the packet's own David-ratified card: "the chain must RUN … before any future freeze/gate claim cites it." The r63 freeze claims cite the chain, and every one of this round's A1/A3/A4/A5 closures again lives in CLI glue no fixture executes — the exact stratum that produced the r61→r62 blockers. A freeze re-scoped to exclude chain-soundness claims would exclude the packet's core content. The laps additionally cannot be "the carded gate" until the cards are repaired to the required scope (N1).

**Minimal falsifiable closure set:**
1. Doc repairs (hours): fix the three `b-delta-cycle.sh` references (02_ ×1, 14_ ×2) to `b-delta-cycle.mjs`; extend the smoke-lap cards to enumerate (a)-(e), the four A4 crash-injection points, the A2 postFlip lap, the A5 resume lap, and the A3 stale-report negative.
2. B3 `--repairExtras`: add the ledger audit (report.appliedDeltas must cover every EXECUTE'd layer for this original) — pre-write rejection of the stale-report counterexample; fixture/lap it.
3. 16_ §2.11: define the audit-window artifact + registered generation; missing/invalid stamp during a window ⇒ quarantine; quarantined-row counts published and alertable; extend the quarantine rule to all ops_metrics consumers (baselines); fold the writer/evaluator duties into 02_'s evaluator card.
4. 16_ §3: rewrite the fidelity-gate comparison law (per-uid `words`/`digest` + `epochByList` under the id map; `challengeDigest` excluded; real side filtered to the reduced set; roster-drift reconciliation with published counts; attempts allowlist pinned ⊇ the replay-law field set).
5. Resolve the §1/§2.11 partition-vs-mirror contradiction with one explicit sentence.
6. Run the emulator lap set (per the card's own law) — then re-attempt freeze.