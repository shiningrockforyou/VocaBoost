# 3-Fable panel — DEEPFIX2 r69 (2026-08-02, under the closing-condition contract)


---

## LENS: authority

All verification complete. Composing the verdict.

# DEEPFIX2 r69 — panel review. LENS: the recovery/overtake authority machine final form

**Basis (measured on this tree; HEAD `15fe0f8`, the r69 fold uncommitted in the worktree):** solo-lap constraint honored — `pgrep -f b-emulator-lap` matched only my own probe shell; I took the verification seat and did NOT re-run the lap. Executed myself: `delta-chain-fixture.mjs` **84/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` clean **11/11** · **evidence sha16 recomputed: 8/8 MATCH** (`b-baseline 64ff0087faf34f7c`, `b3 088000b61c445c11`, `lap d298ec3aff5d0d37`, `b4 77ee0c7abbbb5923` unchanged-from-r68, others unchanged-and-matching) · frozen-bytes timeline holds: max bound-source mtime 07:57:42Z (`b-emulator-lap.mjs`) < artifact write 08:19:29Z · lap `ok(` call sites counted: **exactly 98 ≡ the artifact's 98/0**, and the +16 arithmetic is exact (repair-resume 6 + pre-intent 5 + mixed-(d) 3 + orphan-gate 2); fixture +3 (2 counter throws + 1 postFlip-failed ok) = 84 ✓ · diff read hunk-by-hunk (`git diff 15fe0f8`: b3 +49/−..., b-baseline +10, lap +65, fixture +14, 14_ one sentence — every hunk maps to a handoff claim, no smuggled changes) · full read of the b3 lease/reaper/gate/manifest regions, reducer v3, plain guard, marker guard, repair scan (one shared function, both call sites `b3:176,:301`) · 12-shape offline reducer probe battery run from scratchpad (repo untouched). Codex's r69 review not read — independence preserved.

## (1) r68 findings — CLOSED / MISS (judged against the r68 closing lists, per contract §1)

| r68 item | Verdict | Evidence (file:line) |
|---|---|---|
| **Codex A1 — repair-crash resume brick** | **CLOSED — executed** | The self-exclusion filter sits inside the ONE shared `repairRealityScan` (`b3:172`), so BOTH call sites (pre-lease `:176`, under-lease `:301`) now pass a run's own problems to the generic admission/overtake law (`:315-328`), which filters own problems and runs the overtake check on the own intent. The report-vs-reality check (`:173`) still fatals if a layer applied since the report — a repair-resume overtaken by a new layer is refused fail-closed BEFORE the overtake law even fires. Lap executes Codex's demanded sequence: valid repair report → repair crashes post-intent (99) → the SAME bound command `--resume` exits 0 → the extra is deleted → final B4 PASS (`lap:431-444`). Letter deviation, non-material: no intermediate "B4 FATAL" assert between crash and resume (that exact behavior is asserted by the standing crash2 case). |
| **Codex A2 — pre-intent fence** | **CLOSED — executed, Codex's exact sequence** | The run manifest (v4) snapshots `ledgerAppliedShasAtStart` at manifest build, under the lease, sorted (`b3:448-452`); an anchorless resume (`ownIntentIdx === -1`) compares the snapshot to the live applied set — any NEW sha ⇒ FATAL OVERTAKEN (`b3:322-327`). The gate reads the CRASHED run's on-disk manifest (`:325`) before any rewrite — no self-overwrite hole; program order is lease → gate → phases. Fail-closed on every degenerate shape I attacked: missing/legacy/unparse-at-snapshot all yield `[]` ⇒ over-refusal, never false admission; a legitimate no-history-moved pre-intent resume still completes. Lap: pre-intent crash → M1 applies (asserted admitted — the dangling-invisibility that made this hole) → stale resume REFUSED on the fence message → final M1 B4 PASS (`lap:449-462`). Closes panel custody NEW-1 and accuracy NEW-1 identically. The `!r.cutoverAborted` blind spot remains marker-shadowed (cutover-abort ⇒ durable marker refuses every B3 at `b3:124-126` pre-gate) — register. |
| **Codex B1 — counter shapes** | **CLOSED — stricter than the letter** | Every completion (normal AND terminal — the check at `b-baseline.mjs:195-197` precedes the cutover branch) requires all three counters present, integer, non-negative, else THROW. My probe battery: string / negative / **missing** / float / null / bool / terminal-string — **all 7 THREW**; string-`cutoverAborted` regression intact. Fixture negatives for string + negative (`fixture:405-406`). NaN is JSON-unreachable; the reachable classes are covered. Closes custody NEW-3; NaN-evasion of the arithmetic check is dead. |
| **Codex B2 — three-contender ABA** | **CLOSED IN DESIGN; residual interleavings register per contract §3** | The protocol is REPLACED, not patched: stale reaping serializes under a per-original wx REAPER CLAIM (`b3:275-281`); identity is verified by re-reading the LIVE pathname BEFORE the rename (`:283-292`); **no restore path exists anywhere** — the C-clobber vector is structurally gone, and the authoritative pathname is never absent before identity is proven. The unparseable-reap FATAL wart is gone (holder===null ⇒ unchanged ⇔ still-unparseable ⇒ reap proceeds, `:290`). The dead-pid reap class is LAP-EXECUTED by every crash→resume case (the crashed b3 dies holding its lease; the resume reaps it under the claim — many green executions inside 98/0). Codex's "real multi-process starter negative" is runbook-closed under contract §3 (register: stage-2 matrix rehearsal step). |
| **Panel accuracy NEW-2 — asymmetric post-flip brick** | **CLOSED** | Under `postFlip` a failed/skips latest completion becomes a PUBLISHED orphan with its counts (`b-baseline.mjs:231-235`); pre-flip it stays a blocking problem. My probe confirms the load-bearing safety property: the failed completion's layer sha REMAINS in `appliedLayerShas` — the chain law still binds, unwritten students still settle by value/diff at the gate ⇒ **no false-green door opened by the leniency**. B3's admission parses WITHOUT postFlip, so no admission grease. Fixture case asserts problems=0/orphans=1/counts surfaced (`fixture:407-410`). |
| **Panel gate NEW-1(a) + mixed-(d)** | **CLOSED — both lap-executed** | Flip-orphan gate settlement: crash post-intent → flip → `B4 --postFlip` exit 0 + "flip-orphaned" in output (`lap:481-491`). Mixed-(d): structural corruption + actionable delta ⇒ exit 7 ⇒ driver stops 5 on the survivor ⇒ PASS after correction (`lap:464-479`) — the thrice-carried card case finally executes as truly-mixed. **NEW-1(b) miss:** `replayExclusions` is still published-but-unasserted (grep: only `b4:126,129`) — fail-safe if broken; register. |
| **Panel gate NEW-4 — operator law** | **CLOSED** | `14_:111`: "an ORPHANED/ABORTED layer MAY be passed to `--appliedDelta` at the final gate so its PARTIALLY-written students settle by value instead of blocking [r69 — operator law]". |
| **Register honesty** | **PARTIAL — one unlisted leftover** | The handoff's register block is genuine, but `14_:122-123` (roster leftover, accuracy's "complete or LIST them" sub-item) is neither fixed nor listed. Letter-level; one line adds it. |

## (2) NEW items — bar test applied (false-PASS-at-gate / data-corruption-under-runbook)

**NONE meets the blocking bar.** Register entries with dispositions:

- **R1 — leaked reaper claim (the one real find):** a crash inside the claim window (openSync `:277` → finally `:297`, milliseconds, itself reachable only during recovery-of-a-crash) leaks `exec-*.reaping`; every later reap FATALs EEXIST "stand down and retry" — and retry never succeeds; recovery is a manual `rm`, currently undocumented. Bar: loud refusal, no false PASS, no corruption ⇒ register. Disposition: pid/age-check the claim (it already records `{pid, at}`) or one runbook line; add the crash-injection to the stage-2 matrix.
- **R2 —** the unparseable-holder reap branch (`:290`, holder===null) is unexecuted (dead-pid class is lap-executed; unparseable is fixture-law only) — fail-closed by construction; stage-2 matrix case.
- **R3 —** handoff wording nit: the claim-holder verifies parse-identity (JSON.stringify-equal) for parseable leases and parseability-CLASS for unparseable ones, not raw bytes; adequate in-law (no live writer writes unparseable bytes) — freeze-record note.
- **R4 —** `b3:226`'s unconditional "use --resume" instruction is now safe-by-hardening (the gate refuses every stale resume) — accuracy's soften-ask is moot; no action.
- **R5 —** completion-without-intent still enters `appliedLayerShas` (custody NEW-5, pre-existing, out-of-law bytes only) — fold `intents.has` at leisure.
- **R6 —** `replayExclusions` unasserted (above) · `!cutoverAborted` overtake exclusion marker-shadowed · `14_:122-123` · lap-lock wx-create (already registered) — hygiene, one-liners.

## (3) STAGE-1 FREEZE: **YES**

**Judged against the r68 lists, as the contract requires:** Codex's 5-item minimal falsifiable set — 1 ✓ executed, 2 ✓ executed, 3 ✓ probe-confirmed, 4 ✓ design-replaced (residual multi-process interleavings runbook-closed per contract §3), 5 ✓ 98/0 first run on frozen bytes I hash-verified 8/8. Custody's mandatory fold list 1-3 ✓ all landed (4 "at leisure" partially, registered). Gate's nit list: 1 ✓, 2 half (registered), 3 ✓ superseded by the stronger fence, 4 ✓ protocol replaced, 5 partial (registered). Accuracy's set: 1 ✓, 2 ✓, 3 two-of-three + one register add, 4 ✓. **What I attacked and could not break:** the fence admits only no-history-moved resumes and fails closed on every degenerate snapshot; the repair self-exclusion cannot leak a foreign problem (runId charset + trailing-space prefix) and the report-vs-reality check refuses an overtaken repair before the law needs to; the reaper claim has no restore path to clobber anyone and the pathname is never removed unproven; the postFlip leniency keeps the layer sha in the chain so nothing settles green unverified. Everything that remains is register-class under the bar — **and per the contract, if only register-items remain, the answer is YES.**

**Stage-2 opening order:** the standing order holds — B1 `--full` → dark build with the DF2-10 duties (incl. the teacher-edit preimage duty) → emulator matrix (inherits the register: two-process negatives, first-append window, **R1 reaper-claim crash injection, R2 unparseable-reap case, replayExclusions assert**) → dark deploy → 25WT rehearsal → shadow audit. Fold R1's runbook line (or the pid-check) before the 26SM B3 EXECUTE leg — crash-during-recovery is exactly what live operations will eventually exercise.

---

## LENS: gate

All verification is complete. Probes confirm the settlement mechanics (failed-completion orphan keeps its sha in `appliedLayerShas` — B4 forces value settlement; pre-flip stays blocking; skips orphan too). Composing the verdict.

# DEEPFIX2 r69 — panel review. LENS: the post-flip gate final form. CONTRACT: acknowledged and applied

**Basis (measured on this tree; HEAD `15fe0f8`, r69 delta uncommitted):** solo-lap law honored — `pgrep -f b-emulator-lap` matched only my own probe shell; I took the verification seat, lap NOT re-run. **Evidence sha16 recomputed myself: 8/8 MATCH** the artifact (`b-baseline 64ff0087faf34f7c`, `b3 088000b61c445c11`, `b-emulator-lap d298ec3aff5d0d37`, `b4 77ee0c7abbbb5923` unchanged, others unchanged-and-matching). Frozen-bytes timeline holds: last bound source mtime 07:57:42Z < artifact write 08:19:29Z. Artifact `checks:98` ≡ 98 lap `ok()` sites (82 prior + exactly 16 new: repair-resume 6, pre-intent fence 5, mixed-(d) 3, orphan-gate 2). Executed: `delta-chain-fixture.mjs` **84/0** (81 + 3 new) · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` clean 11/11. `git diff 15fe0f8` maps hunk-for-hunk to the handoff claims (6 in-scope files; batons/settings/changelog account for the rest; no smuggled changes). Five offline reducer probes from scratchpad (repo untouched). `b4-verify.mjs` and `b1-replay-lib.mjs` are byte-identical to the r68-executed versions — my lens's r68 closures (three-conjunct, census, edited pin) carry forward unchanged and their cases still execute inside the 98/0.

## (1) r68 findings — CLOSED / MISS

| r68 finding | Verdict | Evidence |
|---|---|---|
| **Codex A1 — repair-crash permanent brick** | **CLOSED, lap-executed** | Both repair scans are one closure now filtering the run's own problems (`b3:172` — same trailing-space prefix as the admission gate; runId charset forbids spaces, r68-established); foreign problems still brick; the under-lease admission/overtake law governs the rest (`:301`, `:311-330`). Lap: repair crashes post-intent → the SAME bound `--repairExtras --resume` completes → the extra is deleted → final B4 PASS. Codex's literal "assert B4 FATAL mid-crash" and "byte-equal unrelated state" asserts are folded into executed equivalents (crash2 asserts the dangling-intent FATAL on the same reducer path; the final value-level B4 PASS subsumes byte-equality). Substance of the required closure: met. |
| **Codex A2 — pre-intent stale resume bypasses OVERTAKEN** | **CLOSED, lap-executed in Codex's exact sequence** | The manifest snapshots `ledgerAppliedShasAtStart` at publication (`b3:450-453`; written only when `!RESUME` at `:461` — repeated crash-resume cycles keep the ORIGINAL authority snapshot, the correct semantics); a RESUME with `ownIntentIdx === -1` compares the snapshot to now — any NEW applied sha ⇒ exit 2 (`:322-328`). Lap: pre-intent crash → M1 admitted (nothing dangles — the complementary property is asserted too) → stale resume REFUSED with "pre-intent" in output → final B4-with-M1 PASS. Refusal is at admission, pre-phase-2 — "before any write" holds. Fail-closed edges verified by code read: corrupt-ledger-at-manifest ⇒ `[]` snapshot ⇒ everything reads as new ⇒ refuse; pre-r69 manifests (`|| []`) same direction; cutover-aborted layers excluded from both sides and marker-shadowed anyway. `b3:226`'s unconditional "use --resume" instruction is now SAFE to follow (the tool self-refuses when superseded) — the accuracy lens's soften-ask is moot-by-fence. |
| **Codex B1 — counter shapes evade arithmetic checks** | **CLOSED** | Reducer `b-baseline:195-197`: all three counters must be present non-negative integers on EVERY completion (normal AND terminal — the check precedes the cutover branch). Probes: missing-counters THROW, NaN THROWS; fixture negatives for string and negative land (84/0). In-law producers always write real integers (`b3:521`, `:558`) — no legacy chain exists pre-production. Custody NEW-3 closes with it. |
| **Codex B2 — three-contender ABA not exclusive** | **CLOSED at the design level — Codex's own prescription, literally** | Stale reaping is serialized under a per-original wx REAPER CLAIM (`b3:275-297`); under the claim the lease BYTES are re-read and compared to the assessment; changed ⇒ stand down; the authoritative pathname is never removed before identity is proven; **no restore path exists** — nothing can clobber a successor (r68 gate NEW-3(b) dies with it). The aged-unparseable first-takeover FATAL wart (custody NEW-2 / accuracy NEW-3(i)) is gone: "still-unparseable" satisfies the unchanged check. The reaper HAPPY path is lap-executed many times over (every post-crash invocation reaps the dead crashed run's lease — crash2/3/4, overtake, repair, pre-intent cases, all green). The multi-process starter negative Codex also asked for goes to the register per contract §3 — sequential single-operator. Residue found INSIDE the fix: register R1/R2 below. |
| **Codex closure-set item 5 — re-lap on final bytes, 8/8** | **CLOSED** | 98/0 first run; 8/8 recomputed by me; mtimes precede the artifact. |
| **Custody fold list 1-3 (mandatory)** | **ALL CLOSED** | 1 = A2's fence (landed MORE precisely than custody's two-line version — refuses iff history actually moved; the admit-when-snapshot-covers case is safe because plain runs can't start over applied layers, so a covering snapshot implies chain-resolved planning); 2 = B1; 3 = the reaper rewrite. All three changed chain bytes ⇒ the demanded solo re-lap happened (98/0). Custody's leisure items: postFlip-orphan lap case and mixed-(d) LANDED (beyond the promise); `!cutoverAborted` comment and intent-per-completion did not — register R6. |
| **Gate (my lens) nit list 1-5** | **1 CLOSED · 2 PARTIAL-per-its-own-scoping · 3 CLOSED · 4 CLOSED · 5 mostly CLOSED** | 1: mixed-(d) executes as TRULY mixed (corruption + concurrent actionable delta → exit 7 → driver stops 5 on the survivor → corrected → PASS) — the 02_ card letter "must remain a FINAL failure" is now execution-matched; 9/9 card letters. 2: the crash→flip→`flipOrphanedRuns` lap case LANDED (NOTE asserted via merged stdout+stderr, verdict reached); the `replayExclusions` assert did NOT land — my r68 wording itself scoped both to the stage-2 matrix, so this is a carried register item (R3), not a broken commitment. 3: the fence IS the hardening. 4: restore-free protocol + byte-level unchanged check. 5: the orphaned-layer operator sentence landed (`14_:111` [r69]); lap-lock wx and CARRIED completeness are now register lines; `14_:122-123` roster wording remains and remains unlisted — register R7. |
| **Accuracy minimal set 1-4 (the r68 NO)** | **1, 2, 4 CLOSED · 3 partial (register-grade remainder)** | 1: fence + lap case (the stronger form of the asked fixture row — the fence lives in b3, not the reducer, so lap-level is the right execution layer) + the instruction made safe. 2: the asymmetric brick is dead — probes: post-flip failed completion ⇒ `problems=0, orphans=1`, counts surfaced; SKIPS (reset-locked, the routine trigger) orphan too; PRE-flip both stay blocking problems (correct asymmetry — resume exists there); fixture row asserts it. Landed as the existing `orphans`/`flipOrphanedRuns` disposition rather than a new `flipStrandedRuns` name — same published settlement. 4: 98/0 + 8/8. 3: mixed-(d) executed ✓, lap-lock wx registered ✓, `14_:122-123` still unlisted — R7. |

**My lens's settlement trace on the NEW orphan class (attacked, HELD):** a post-flip FAILED delta completion keeps its sha in `appliedLayerShas` (probe-confirmed) ⇒ `b4:71-73` FATALS loudly if the layer is omitted from `--appliedDelta` — the operator is FORCED into value settlement; with the layer passed, written students value-match, unwritten/skipped students fail conjunct 3 (disk ≠ layer) ⇒ blocking DIFFS ⇒ exit 5, published rows. The failed run cannot manufacture a tail excuse: where M0 ≡ M1 for a student-field there is no run-attributable diff at all, and genuine writerless tail events classify under the ratified published-undercount law. Intent-orphans (sha never applied) settle exactly as r68 traced. Pre-flip admission still parses WITHOUT `postFlip` (`b3:313`) — orphans never grease admission. Every invocation shape is fail-closed; no false green exists.

## (2) NEW items — none meets the blocking bar; ALL to the register with dispositions

- **R1 (the one real find): the reaper-claim stand-down leaks its claim.** `b3:292`'s `process.exit(2)` runs INSIDE the try — Node does not execute `finally` on `process.exit` (verified empirically) — so the "lease changed / live successor" stand-down leaves `exec-<sha16>.reaping` on disk; every later stale-reap then EEXISTs at `:279` forever ("stand down and retry" never succeeds) until a manual `rm`. A SIGKILL inside the claim window leaves it too. The comment's "release + stand down" is not what that path does. **Not blocking under the contract:** the path is reachable ONLY via a concurrent second claimant mutating the lease inside a same-process milliseconds window (the multi-process race class §3 runbook-closes) or a double-crash (stage-2 crash-injection class); the failure is a loud fail-closed refusal — no false PASS, no data write. Disposition: at the stage-2 fold, exit via a flag after the finally (or an age law on the claim) + one runbook line ("if `FATAL [r69 reaper]` persists with no live B3, remove the `.reaping` file") + align the comment.
- **R2:** the unparseable-lease unchanged-check is "still-unparseable", not byte-equality — two DIFFERENT garbage contents in the window count as unchanged. Requires an out-of-law garbage writer (in-law leases are tmp+fsync+link atomic) AND multi-process interleaving. Disposition: byte-compare `nowRaw` at the same fold.
- **R3:** `replayExclusions` published but still asserted nowhere (carried from my r68 list, self-scoped) — stage-2 matrix assert; fail-safe direction (an honesty counter sitting empty excuses nothing — the excusing mechanism is delta flags, which the driver cases execute).
- **R4:** the PARTIAL-write orphan settlement (written-clean + unwritten-blocking in one run) is component-proven (b4:71-73 force + conjunct-3 blocking executes in mixed-same-field) but not end-to-end lap-executed — stage-2 matrix case.
- **R5:** `14_:111`'s "an ORPHANED/ABORTED layer MAY be passed" — for the failed-completion subclass the sha is ledgered so B4 REQUIRES it (loud FATAL if omitted). One-word doc tightening ("MAY (MUST when its completion is ledgered)") at the next doc fold.
- **R6:** custody's leisure pair — intent-required-for-every-completion (hand-forged bytes only) and the `!cutoverAborted` overtake-exclusion comment (marker-shadowed) — stage-2 fold.
- **R7:** `14_:122-123` post-flip re-enrollment wording (LOW, r67) — add its one-line register listing.
- **R8 (standing hygiene):** artifact `gitHead:15fe0f8` while the bound bytes are the uncommitted r69 worktree — sha16 is the binding (8/8 verified); already registered.

## (3) STAGE-1 FREEZE: **YES**

**Under the contract this is not close.** Every r68 blocking finding — Codex A1/A2/B1/B2 and the accuracy lens's NO-set — is closed **by execution** in a 98/0 first-run lap whose bytes I hash-verified and whose new-assert arithmetic reconciles to the digit (82+16=98; 81+3=84). The three panel closing lists are satisfied on every load-bearing item; the leftovers are exactly the items the lists themselves scoped to stage-2 or doc-leisure. My lens's final-form trace holds: the gate reaches a published verdict in every post-flip shape (dangling intent, failed completion, skips, cutover-abort, partial-write × layer passed/omitted), always fail-closed, never silent. The new items above are register-grade without exception — R1 is a genuine defect I verified empirically, but it is a loud availability corner gated behind multi-process or double-crash preconditions the contract explicitly runbook-closes, with a one-line recovery. Only register items remain ⇒ **the contract answer is YES.**

**What this YES binds (my commitment):** the freeze certifies the executed laws as-laped (98/0 on sha16-bound bytes). R1-R7 carry to the stage-2/operational register with the dispositions above; none may be re-escalated to reopen stage 1, and I will not manufacture new stage-1 corners — anything further from me must meet the false-PASS/corruption bar.

**Stage-2 opening order (standing order, unchanged):** B1 `--full` (read-only first real 26SM baseline — census + exclusion surfaces land there) → dark build with the DF2-10 duties (stamping predicate + §6b grading preimage + the teacher-edit per-row preimage, verbatim from the card) → emulator crash-injection matrix (inherits first-append/torn/two-process/three-contender negatives + R1-R4 + the partial-write orphan case) → dark deploy → 25WT rehearsal → shadow audit → flip choreography with the final pre-flip micro-lap. The R1 runbook line and the R5 doc word belong in the first stage-2 doc fold; no chain-code change is required before B1 `--full`.

---

## LENS: closure

# DEEPFIX2 r69 — panel review. LENS: holistic freeze-readiness (packet ≡ diff, evidence computed, register completeness, the contract answer)

**Basis (measured on this tree; repo root /app; HEAD `15fe0f8`, the r69 delta uncommitted on the working tree):** solo-lap seat honored — `pgrep -f b-emulator-lap` matched only my own probe shell; the lap re-writes `docs/plans/deepfix2/evidence/emulator-lap-result.json` (a repo file), so under never-edit-repo-files I took the VERIFICATION seat and did NOT re-run it. Executed myself: `delta-chain-fixture.mjs` **84/0** · `rotation-cyclicity-fixture.mjs` **2,688/0** · `node --check` clean **11/11** · **evidence sha16 recomputed: 8/8 MATCH** (`b-baseline 64ff0087faf34f7c` · `b3 088000b61c445c11` · `b-emulator-lap d298ec3aff5d0d37` changed; `b4 77ee0c7abbbb5923` and four others unchanged-and-matching — b4 untouched this round, consistent with git status) · frozen-bytes timeline HOLDS: latest bound-source mtime 07:57:42Z (lap) < artifact write 08:19:29Z · artifact `checks:98` ≡ 98 lap `ok()` sites ≡ 82 (r68-verified) + exactly the 16 new sites in the diff (6 repair-resume + 5 pre-intent + 3 mixed-(d) + 2 flip-orphan) · fixture 84 ≡ 81 + 3 new asserts (2 counter negatives + 1 postFlip-failed orphan) · baton round 69 / rev 210 / turnOwner codex, handoff + ready-marker identity verified. **Diff accounting: in-scope delta = exactly 6 files +132/−20** (`b3`, `b-baseline`, `lap`, `fixture`, `14_`, evidence JSON); every hunk maps to a handoff claim; the 10 out-of-scope files are batons/logs/reviews/settings — **no smuggled changes**. Full read: b3 gate/lease/reaper/manifest regions, reducer v4 region, both r68 lap blocks, fixture 9f additions, 14_:111, Codex r68 + full r68 panel.

## (1) r68 findings — CLOSED / MISS

| r68 finding | Verdict | Evidence (file:line) |
|---|---|---|
| **Codex A1 — repair-crash recovery brick** | **CLOSED — executed** | The single `repairRealityScan` closure now filters the run's OWN problems (`b3-backfill-writer.mjs:172`) and is invoked at BOTH sites (pre-lease `:176`, under-lease `:301`), so a repair resume reaches the generic admission/overtake law (`:315-329`), exactly Codex's required shape. Lap: repair crashes post-intent (exit 99) → the SAME bound command `--resume` completes (exit 0) → the extra verified deleted by direct Firestore read → final B4 PASS. Codex's "unrelated chain state byte-equal" ask is subsumed by the final value-defined B4 PASS (stronger over the checked surface); the intermediate "B4 FATAL" assert is not repeated in this case but is mode-agnostic reducer behavior already lap-proven. Substance fully closed. |
| **Codex A2 — pre-intent crash bypasses OVERTAKEN** | **CLOSED — executed in Codex's exact sequence** | Run manifest v4 snapshots `ledgerAppliedShasAtStart` at publication (`b3:448-453`); a RESUME with `ownIntentIdx === -1` compares snapshot→now — any NEW applied sha ⇒ FATAL exit 2 at the gate, pre-write (`:322-328`). "Absence of an intent must not mean absence of history" is now literal code. Fail-closed for legacy/absent snapshots (`|| []` ⇒ any applied sha refuses). Lap: M0 → plain P crashes PRE-intent (durable manifest, no ledger position, asserted) → M1 executes (admitted, asserted) → stale P resume REFUSED ("pre-intent" asserted) → final M1 B4 PASS. This also closes the gate lens's NEW-2 variant (intent eaten by the carried first-append window ⇒ snapshot {M0} vs now {M1} ⇒ refused). I attacked the fence for a bar-meeting false-admit and failed: plain-after-layers dies at the plain guard, delta resumes re-plan against their own chain, a same-M0 interleaved plain is idempotent-convergent, and the cutover-aborted exclusion is marker-shadowed (below). |
| **Codex B1 — counter types/ranges** | **CLOSED** | All three counters required non-negative integers on EVERY completion — the check (`b-baseline.mjs:195-197`) precedes the cutover branch, so normal AND terminal records are covered; strings/NaN/negatives THROW. Fixture: string + negative negatives; the old fixture rows had to gain a `{...Z}` spread — proof the requirement binds. The `(o.txnFailures || 0)` arithmetic at `:231` is now schema-guarded. |
| **Codex B2 — three-contender ABA** | **CLOSED at the protocol level; residue register-disposed under contract §3** | The rename/restore protocol is REPLACED by Codex's own prescribed alternative: stale reaping SERIALIZED under a per-original wx REAPER CLAIM (`b3:276-283`); under the claim the lease is re-read and must be UNCHANGED vs assessment (parsed-value equality, or STILL-unparseable when assessed unparseable — the r68 panel's parse-throw wart is structurally gone); changed ⇒ stand down; unchanged ⇒ rename → `takeLease` (`:284-299`). The authoritative pathname is never removed before identity is proven and NO restore path exists — nothing can clobber a successor; a raced `takeLease` fails wx and stands down. The "real multi-process starter negative" remainder is the register's two-process/three-contender line (runbook-closed, stage-2 matrix retains crash-injection rehearsal) — a legitimate §3 close on this single-operator sequential system. |
| **Codex closure-set item 5 — re-lap on final bytes, 8/8** | **CLOSED** | 98/0 ran on the final code bytes (all bound mtimes precede the artifact; the sole post-lap change is the 14_ doc line, and contract §4 exempts doc-only folds); 8/8 computed by me. |
| **Panel custody NEW-1 / accuracy NEW-1 / gate NEW-2 (pre-intent anchor)** | **CLOSED** | = A2 above; the custody lens's two-line fix shape landed as the stronger snapshot fence + lap case. |
| **Panel custody NEW-2 (unparseable-reap verify fatal)** | **CLOSED** | The old verify is deleted with the redesign; an unparseable-assessed lease reaps via the unchanged-check without ever parsing the reaped bytes. |
| **Panel custody NEW-3 (JS-coerced counters)** | **CLOSED** | = B1. |
| **Panel accuracy NEW-2 (asymmetric brick — failed/skips completion post-flip)** | **CLOSED** | Under `postFlip` a failed/skipped latest completion becomes a PUBLISHED orphan WITH its outcome counts (`b-baseline.mjs:231-236`), never a fatal; fixture asserts problems=0/orphans=1/counts-surfaced. Settlement is fail-closed: the failed completion's layer sha IS in `appliedLayerShas` (`:238`) so the chain law demands it at the gate, and skipped students' disk ≠ layer expectation ⇒ non-tail diffs BLOCK — a loud value verdict, no false green. B3's gate parses WITHOUT `postFlip` (`b3:169,313,337` — two-arg calls), so pre-flip admission still refuses until resumed. The lens's separate-`flipStrandedRuns` shape was folded into the orphans list — an equivalent published disposition. |
| **Panel gate NEW-1(a) (orphan gate wiring unexecuted)** | **CLOSED** | New lap case: crash post-intent → flip → `b4 --postFlip` → exit 0 + "flip-orphaned" NOTE asserted — the b4 publication path executes end-to-end. (b) `replayExclusions` remains unasserted → register (fail-safe honesty counter; the handoff does not claim it). |
| **Mixed-(d) (thrice-carried card letter)** | **CLOSED BY EXECUTION** | New lap case: structural corruption + actionable delta coexist → B4 exit 7 → driver exit 5 (processes the delta, STOPS on the surviving structural diff) → correction → full-chain B4 PASS. The card letter's mixed case now has its executed instance (9/9). |
| **Panel gate NEW-4 (orphaned-layer operator law)** | **CLOSED** | `14_:111`: "an ORPHANED/ABORTED layer MAY be passed to `--appliedDelta` at the final gate so its PARTIALLY-written students settle by value instead of blocking [r69 — operator law]" — the exact missing sentence. |

No blocking-level MISS exists. Nit-level leftovers from the r68 "at leisure"/nit lists that did not land: the `!cutoverAborted` overtake exclusion comment, intent-required-for-every-completion, the `replayExclusions` assert, `14_:122-123` — all register-class (below).

## (2) NEW items — bar-tested; none blocks, all registered

Bar = false PASS at the final gate, or student-data corruption under the documented runbook. I could construct neither from any of these.

- **R1 (reaper-claim crash residue):** SIGKILL inside the claim window leaves `exec-<sha16>.reaping` behind (the `finally` at `b3:297` never runs); every later reap of a stale lease then refuses with "another claimant is reaping … retry" — which will not self-heal. Fail-closed refusal, never a false pass or a write; single-operator manual out is one `rm`. Disposition: add the rm line to the stage-2 runbook; consider an age law on the claim; fix the misleading "retry" wording.
- **R2 (reap-through path zero-executed):** the lap exercises only the LIVE-lease refusal (`lap:359-372`); fixture 9h covers the pure `assessLease` law; the claim→verify→rename→take branch has no executed case anywhere. A runtime defect there surfaces as a loud fail-closed refusal during rehearsal, not a false pass (a wrong reap-through needs a concurrent live writer — runbook-excluded). Disposition: one dead-pid stale-lease case in the stage-2 emulator matrix.
- **R3 (marker-shadowed cutover exclusions):** both the r68 overtake predicate (`b3:320` `!r.cutoverAborted`) and the new fence's `appliedLayerShas`-based compare exclude cutover-aborted layers; unreachable in-law — cutover-abort ⇒ durable marker ⇒ every B3 (incl. the resume) dies at the marker guard first. Disposition: comment or drop (carried custody NEW-4, now two instances).
- **R4 (sibling-shape coverage):** the failed-completion orphan traverses b4's publication only via the dangling-intent lap case (identical code path; reducer shape fixture-asserted). Disposition: optional matrix case.
- **R5 (doc/wording hygiene):** `14_:122-123` roster leftover (thrice-ruled LOW); `b3:226`'s unconditional "use --resume" — now substantively safe (the fence turns a superseded resume into a refusal), wording only; change-log r69 line says "byte-equal" where the executed assert is extra-deleted + final value-PASS (the handoff itself states it correctly); handoff's "re-verifies the lease BYTES" is parsed-value equality / unparseable-class match — materially equivalent in-law, wording nit; custody NEW-5 (completion-without-intent, out-of-law bytes) still open as defense-in-depth.

**Register completeness (my lens duty):** the handoff's seven register lines are genuine and correctly disposed; the residue above (R1-R5) belongs beside them and none of it meets the bar. With these additions the register is complete against the full r66-r69 review union as far as I can enumerate it.

## (3) STAGE-1 FREEZE: **YES — unconditional under the contract**

**Why YES:** every r68 blocking finding (Codex A1/A2/B1/B2, accuracy NEW-1/NEW-2) is closed in code AND in executed evidence — the two authority holes that justified r68's NO now have their exact demanded sequences running green inside the 98/0 lap on bytes I hash-verified myself. The packet told the truth end-to-end: every load-bearing claim maps to a real hunk, all three counts (98/84/2,688) reproduce or triangulate exactly, 8/8 computed, frozen-bytes timeline holds, no smuggled changes. Everything that survives is register-class — fail-closed refusals, unreachable-in-law corners, unexecuted fail-safe wiring, doc polish — and the contract's own rule ("if only register-items remain, the answer is YES") decides the rest. Per contract §1 a YES carries no closing-condition list; my YES binds to the eight frozen sha16s — any chain-code byte change re-triggers exactly one solo lap + 8/8 republish (§4), doc-only folds do not.

**Stage-2 opening order:** the standing order holds — B1 `--full` (read-only) → dark build with the DF2-10 duties (stamping predicate + §6b preimage + the teacher-edit preimage duty verbatim from the card) → emulator matrix (crash-injection rehearsals + register items R2/R3/R4 + the carried first-append/torn/two-process cases) → dark deploy → 25WT rehearsal → shadow audit. The prior rounds' precondition — the admission law landed before any real 26SM B3 EXECUTE — is satisfied pre-freeze; R1's runbook line should land in `SUPPORT_RUNBOOK`/14_ before the 26SM leg.

Key paths: /app/docs/plans/loop/handoffs/claude_to_codex_deepfix2_r69.md · /app/scripts/deepfix2/b3-backfill-writer.mjs (:172, :276-299, :315-329, :448-453) · /app/scripts/deepfix2/b-baseline.mjs (:195-197, :231-236) · /app/scripts/deepfix2/b-emulator-lap.mjs (:428-492 new cases) · /app/docs/plans/deepfix2/evidence/emulator-lap-result.json (98/0, 8/8 verified).