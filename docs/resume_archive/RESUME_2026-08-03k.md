# RESUME — DEEPFIX2 (2026-08-03: **CODEX r79 = YES** · deploy order WRITTEN + HELD · typed fold in flight)

## PRODUCTION (live-verified)
Indexes 43 ✅ · functions 24 ✅ (zero removed) · `system_config/review_v2` SEEDED DARK ✅ · rules **NOT
deployed — the order is written and held, see below** · client PINNED `ce09792` · **Netlify BUILDS
STOPPED** · NOTHING ACTIVATED. Students see no difference. **WSL cannot push — every push is a
WinClaude order** [[wsl-claude-has-no-git-push]]. Read-only remote checks DO work:
`git -c http.sslBackend=gnutls ls-remote origin refs/heads/main` (the shared checkout pins Windows
`schannel`); a fresh session also needs `git config --global --add safe.directory /app`.

## START HERE EVERY TURN
`bash scripts/deepfix2/session-start.sh` · before publishing ANY claim or issuing ANY order:
`node scripts/deepfix2/gate.mjs` · plan a fold with `--plan` BEFORE editing.
Rationale: docs/plans/deepfix2/EXECUTION_DISCIPLINE.md. Four skills auto-fire: folding-review-findings ·
verifying-agent-work · verifying-published-claims · ordering-deploys. The baton watcher now lives IN THE
REPO (`scripts/deepfix2/baton-watcher.sh`) — the old per-session /tmp copy died with its session while
session-start printed "relaunched".

## THE RULES LEG — **CLEARED BY CODEX r79 (YES)**
Artifact `audit/deepfix/task3/live_baseline/firestore.merged.rules` · **244/244 matrix · 15/15 mutants**
· frozen sha16 `f40f91fce3693b82` · on origin/main at `be1981f`. Review:
`docs/plans/loop/codex_reviews/codex_deepfix2_r79.md` — they re-ran the r78 probe and got the inverse
result (the answers update now 403, rows intact). Evidence re-executed independently twice; the second
run was a from-scratch harness and the mutants report regenerated **byte-identical**.

**THE DEPLOY ORDER IS WRITTEN AND VERIFIED BUT NOT ISSUED:**
`docs/plans/loop/win/handoffs/claude_to_winclaude_097.md` (carries a ⛔ NOT-ISSUED banner).
Held because `gate.mjs` fails while the typed-fix-audit ledger has open rows. **TO ISSUE: flip the win
baton to `winclaude`, `taskId: RULES_DEPLOY_R79`, handoff = that file, revision +1** — once the typed
fold closes and the gate is clean. Pre-verified at write time: every named file read in full · live
freshness re-checked = **NO DRIFT** (ruleset `d8f3e0d0…`, `44914b60858a1dcd`, 211 lines) · diff = 6
declared hunks, 24 match blocks vs the base's 20, **none lost**, 7 removed lines all inside 3 rules
replaced in place by guarded versions. The order re-proves freshness at execution time regardless of
how long the hold lasts. **The deploy slot `/app/firestore.rules` holds the P10d trap**
(sha16 `752981b78f532ebd`, `isTeacher()`→custom claim nobody is backfilled with, 165/244) — the order
snapshots it to `firestore.p10d.rules` first, since no such snapshot exists beside p6/p10c.

## IN FLIGHT RIGHT NOW
**typed-fix-audit** — fold ledger at `<scratch>/typed-fix-audit-fold-ledger.md` (gate `--plan` ACCEPTED;
it REJECTED the first draft for a missing bypass set). Authoring DELEGATED to an independent agent
(brief: `<scratch>/typed-fix-brief.md`); an auditor brief is pre-written at
`<scratch>/typed-fix-audit-brief.md` — launch it on the implementer's return, then verify by
re-execution. **The blocker is confirmed in code, not assumed:** the live `gradeTypedTest` takes its
job key from client-supplied `attemptDocId` (`functions/index.js:1048-1051`), the client holds its own
`presentationId` (`reviewV2Client.js:152`), and the live payload shape (`index.js:1136-1141`) satisfies
the engine's cached-grade test (`typedGrading.js:102-104`) — so a student can pre-seed
`rv2_{presentationId}` and the engine consumes a grade of their choosing.

## AWAITING DAVID
**Anyone can self-register as a teacher** (`Signup.jsx:124-149` → `db.js:254`), and the LIVE ruleset lets
ANY teacher read/write EVERY student's subcollections regardless of class membership. Live today, NOT
caused by DEEPFIX2. Three options carded at the bottom of NEED_TO_FIX.md. **His call.**

## THEN, in order
typed-fix-audit → **issue order 97 (the rules deploy)** → engine-key-provenance-scan (Codex r79: the
artifact comment's "an engine key's presence proves server authorship" is true GOING FORWARD but not
historically — cohort scan carded in NEED_TO_FIX, and the comment repair is deliberately POST-deploy so
the certified sha stays valid) → DF2-51 client cutover → 25WT rehearsal → shadow audit → **David's
backfill go** → **David's flip**. DF2-10 adoption legs stay DEFERRED until after the rehearsal.
