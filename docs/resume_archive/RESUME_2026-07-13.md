# RESUME — current active work

> **This is the canonical resume file.** When the user says "resume," read this first, then any
> linked spec. **Rotate at each save-state:** copy this file to `docs/resume_archive/RESUME_<date>.md`
> (copy, don't move), then overwrite the active-stream section below with the new state. History lives
> in `docs/resume_archive/` (see its README).

---

## ▶ ACTIVE STREAM (updated 2026-07-13): #11 review-only fix (BUILT, converged, NOT deployed) + live CS surge

**Big picture.** The dominant live problem across the whole 26SM cohort is **NEED_TO_FIX #11 — the review-only
day deadlock**: a student who finishes a list (list-end) OR throttles on low reviews gets `newWordCount=0`, and
the Day-2+ completion gate demands a new-word test that can't exist → frozen, "review won't submit." We
**designed, implemented, and code-converged** the fix this cycle (Codex GO), plus a Playwright acceptance audit
(design converged, harness built) that has **not been run**. Meanwhile a surge of live CS tickets (7/13) forced
manual fixes and revealed the scope (169 students at the #11 wall).

### ✅ DONE — Phase 1 code fix (LOCAL-ONLY on `main`, UNCOMMITTED, NOT deployed)
Review-only-day completion fix in **`src/services/studyService.js`** (completeSessionFromTest: `reviewOnlyDay`
confirmed-reason predicate — `LIST_SCOPED_RECON && Number.isFinite(cfgNewWordCount) && cfgNewWordCount<=0 &&
(allocation.newWords<=0 || isListComplete || startPhase===REVIEW_STUDY)`; wordsIntroduced clamped 0;
`!reviewOnlyDay &&` gate skip; literal-null session-state keyed on attempt-absence) + **§5 list-end terminal** in
`DailySessionFlow.jsx` (CompletePhase "🎉 You finished the list!" + empty-review non-recording terminal at :819)
+ **`Dashboard.jsx`** finished-hero (`listFinished = wordsLeft===0`). Loop: 3-agent audit + Codex REVIEWONLY_IMPL
**r1 NEEDS_FIXES(ROI-1 stale-0)→r2 NEEDS_FIXES(ROI2-1 fake-completion)→r3 GO (0/0/0/0)**. `docs/plans/PLAN_review_only_day_completion.md`.

### ✅ DONE — Playwright acceptance-audit DESIGN converged; harness BUILT; NOT run
`docs/plans/PLAN_reviewonly_playwright_audit.md` v2 — Codex REVIEWONLY_AUDIT_DESIGN **r1 NEEDS_FIXES(4B/2H/2M)→r2
GO (0/0/0/0)**, Codex conceded RAD-3 (seed-list-end is safe) to code evidence. HYBRID (E2E RA1-9 + white-box
W-RA3/4/4b for the un-drivable stale-0 gate). Harness FILES BUILT (not executed — WSL can't run Playwright: `/app`
is a 9p Windows mount, node_modules has Windows binaries): `audit/playwright/lsr_reviewonly.mjs`,
`lsr_reviewonly_fb.mjs`, `lsr_reviewonly_whitebox.mjs`, `REVIEWONLY_HARNESS.md`, + `lsr_ui.mjs` BASE→localhost
import-time guard. **Codex CAN run it from its Windows env** (has browsers + serviceAccountKey) — capability
confirmed (`docs/plans/loop/codex_reviews/codex_review_audit_run_capability_001.md`); Codex prepped
(REVIEWONLY_AUDIT_PREP). Not run — paused for the CS surge.

### ▶ NEXT (blocked on David's call — see "OPEN decisions" below)
1. **Deploy #11 fix?** It's code-converged but NOT acceptance-tested. Scan found **169 students at the #11 wall**
   (all list-end; 0 throttle right now) — 100 active-need, 63 still-to-move, 5 finished-everything. Owner deploys
   (Claude can't). Options: finish audit→deploy / deploy-now→audit-follow-up / keep manual-fixing.
2. **When deploying:** UPDATE `scripts/cs/data-integrity-sweep.mjs` — every review-only day will trip its
   `reviewNoNewPass` check (benign, but noisy; Junseo already shows it).
3. **Feature David requested (2026-07-13):** list-end **choice terminal** ("완료! start over OR advance to next
   list?") + **continuous next-list** + **linked lists within a class** (explicit per-class order; today "next"
   is inferred Base→Ascent→Summit by convention). = §5 terminal (built) + cycling capstone (`docs/plans/loop/x/plan.md`)
   + a NEW list-linking data layer. Needs a design doc → 3-agent → Codex loop before implementing.

### ✅ CS work 2026-07-13 (all verified, swept clean, logged in SUPPORT_RUNBOOK CS-2026-07-13*)
Individual: 오하린(carry-correct), 김동현(#11→Summit), 안이연·유혜준·Lucy(#12 carry→reconciled),
Kaila(csd-undercount 2→3), Junseo(#11 throttle→completed), 손지성(carry-correct), Bridge TOP ×3(#11→Ascent),
정카일라(=Kaila). Systemic: **ensured all 32 26SM classes have all 3 lists** (`scripts/cs/ensure-all-lists.mjs`,
backdated+mirrored+backed-up) + **batch-advanced 87 list-end finishers** (`batch-advance-listend.mjs`, config-only
primaryFocus). **63 finishers STILL to advance** (report `next-list-by-class.md`; awaiting David go / or deploy).
New CS scripts in `scripts/cs/`: diag-reviewonly-cases, sweep-ascent-carry, reconcile-ascent-carry,
move-kimdonghyun-to-summit, fix-kaila-junseo, scan-reviewonly-frozen, report-next-list, ensure-all-lists,
batch-advance-listend.

### 📋 Chat-log triage (2026-06-30→07-13) — `docs/audits/TA_CHATLOG_TRIAGE_2026-07-13.md`
Whole week = two themes (#11 list-end + cross-class carry), both = our work. NEW issues surfaced →
**NEED_TO_FIX #13** (test-size mis-gen at boundaries: 이혜성 day-1 10≠30 VERIFIED, 김호형 retake 30≠35 OPEN),
**#14** (permanent-fail: grader false-neg + token exhaustion + no override — 2nd stuck-state class; challenge
replenish is 30d not weekly; promoted students lack grade permission), **#15** (no review-retake; low-reviews-
still-pass feeds #11 throttle).

### Loop mechanics (reference)
`docs/plans/loop/` — `baton.json` turn-token; Claude self-wakes via `lib/baton-watch.sh`; Codex reviews via its
long-turn session (writes `codex_reviews/`, flips the baton). Baton currently: REVIEWONLY_AUDIT_PREP done.

**Standing constraints:** NO source change until Codex signs off AND David gives explicit go-ahead (reverting
counts too). Owner deploys (Claude can't build/deploy). Never commit/branch without say-so. Code→`change_action_log.md`,
CS/data→`SUPPORT_RUNBOOK.md`. Sandbox only for audits (`lsr_*`/25WT/fresh) — NEVER 26SM. 26SM = real active cohort;
this #11 fix cycle was LOCAL-ONLY (no live deploy while students are on it). Admin CS scripts DO run from WSL
(`NODE_PATH=/app/node_modules node scripts/cs/…`, serviceAccountKey.json gitignored); only the Vite/Playwright
build can't run here.
