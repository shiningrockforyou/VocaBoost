# RESUME — current active work

> **Canonical resume pointer.** When the user says "resume," read this first. The FULL plan of record is
> **`docs/plans/MASTER_TASK_LIST.md`** (canonical, verified) + action log **`docs/plans/MASTER_TASK_TRACKER.md`**.
> This RESUME is a thin pointer. **Rotate at each save-state:** copy → `docs/resume_archive/RESUME_<date>.md` (copy,
> don't move), then overwrite here.

---

## ▶ ACTIVE STREAM (rotated 2026-07-18b): D3/P4 CERTIFIED + COMMITTED → now planning the D3.5 pre-migration audit

**READ FIRST:** `docs/plans/MASTER_TASK_LIST.md` (plan A→E, per-item status §2, execution queue §4) +
`docs/plans/MASTER_TASK_TRACKER.md` (every action, ACT-001…049). Convergence record (frozen):
`docs/plans/loop/CONVERGENCE_REPORT_v4.md`. Audit plan: `docs/plans/D3.5_RECOVERY_AUDIT_PLAN.md`.

### Where we are (2026-07-18 ~13:05 UTC)
- **Through D3 is DONE.** After a mid-cutover crash → 5-way convergence (CLOSED) → **D3/P4 behaviorally CERTIFIED**
  (emulator 10/10, `deepfix_p4_behavioral_cert_0ddbb34.json`, WSL-verified) → **everything committed + pushed
  (`e20b532`)**, R-4 closed. Client `6bffe1c` / functions `0ddbb34` live; server-authoritative progress path LIVE + certified.
- **Verdict on the cutover:** GO-HOLD held → now CERTIFIED. Deployed reversible; no rollback signal.

### Current frontier — D3.5: pre-migration recovery + adversarial audit (BEFORE the one-way D4/P5)
Plan = `docs/plans/D3.5_RECOVERY_AUDIT_PLAN.md`. **Sandbox (25WT) only — never a 26SM write.**
- **Parts:** A (13 synthetic broken-state recoveries) · **A2 (clone all 211 July-ticketed students → exact pre-fix state
  from `backups_throttle/relief/csd` + the `dup-real-students-for-audit.mjs` engine)** · B (22 adversarial UI: button-spam,
  races, reloads, empty-submit) · C/D (happy-path baseline + forced-pathway policy) · E (known-open, observe-only) ·
  **F (20 WSL-invented edge configs)**.
- **Scope map (folded into the plan):** the July CS families the deployed fixes FIX (confirm recovery) vs NOT (observe-only).

### DECISIONS this session (David-approved)
- **Do D3.5 on LIVE PROD before D4/P5** — validate the real UI while cutover rollback is still cheap.
- **TIER REWEIGHT (key insight):** tiers 1/2 (emulator + live-callable) **bypass the browser → prove the SERVER, not the
  UX**; they can't test the client-routing the cutover changed, and most July tickets were client/UX symptoms. →
  **Tiers 1/2 = fast pre-filter over all 211; Tier 3 (live UI) = THE audit**, expanded to assert the client-side fixes
  (empty-submit guard, re-entry render, "93% shows fail" threshold display, modals, "does the screen actually advance").
  UX pass/fail lives in tier 3.
- **Scale:** 211 real students + ~70 scenario defs; ~1–1.5 working days recommended (max-exhaustive ~2–2.5).

### NEXT
1. **Completeness-critic pass** on `D3.5_RECOVERY_AUDIT_PLAN.md` (Codex + 3 Fable: "what ticketed student / interaction
   style / failure mode are we missing?") → fold gaps.
2. **Build** `scripts/audit/clone-ticketed-prefix.mjs` (+ synthetic seeders) + the Playwright harness (**fix the r37
   `joinClass` enrollment gap** first — it blocked the r37 smoke).
3. **Run** tiers 1/2 as pre-filter over all 211 → tier 3 (live UI) as the real audit → triage → report.
4. Then D4/P5 pre-work (fresh 26SM backup + 25WT rehearsal + a fresh Codex-GO'd, David-authorized plan).

### David's STANDING instructions
- **26SM = real cohort — READ-ONLY, no writes without explicit authorization.** 25WT = sandbox.
- **Commit at milestones:** at each milestone, commit everything together (via WinClaude — WSL has no push/firebase
  creds), then report full task-list + progress. `git add -A` is hazardous (`.gitattributes = * text=auto`) → targeted
  adds / `git add --renormalize .`.
- **Never trust an agent blindly** — verify every claim vs code/live evidence. Keep Codex + WinClaude in every round; if
  either goes silent, tell David.
- **Save state + log decisions:** rotate RESUME at each save-state; fold discussion insights/decisions into the master
  task-list markdowns (this rotation did so).

Prior streams: `docs/resume_archive/RESUME_2026-07-18b.md` (this stream's pre-D3.5-planning snapshot),
`RESUME_2026-07-18.md` (stale pre-crash 07-17c); earlier in `docs/resume_archive/`.
