# RESUME — DEEPFIX2 (paused 2026-08-03, post-checkpoint, mid-dark-deploy)

## PRODUCTION REALITY: STUDENTS SEE NOTHING DIFFERENT. Verified path-by-path:
client PINNED at `ce09792` (Netlify auto-publish LOCKED — pushes build, never ship) · the 7 reviewV2
callables are unreferenced by src/ AND flag-off AND server-dark · `reviewChallenge`/`overrideAttempt`
dormant by their own FALSE flags · indexes additive (zero removals) · rules NOT deployed · the config
doc is read only by the dormant engine. **THE ONE LIVE-CODE CHANGE: `resetProgress`** — behavior
preserved by `RESET_V2_ENABLED=false` (legacy branch), response only GAINED fields the client ignores
(it reads `success` + `deleted`). If anything looks odd, look there first; the revert is one const.

## STATE
- **STAGE-2 CHECKPOINT CONVERGED** — Codex r77 YES + Opus r74 YES; engine lap 220/220 sha-bound.
- Git: `b54c6e5` pushed (20 clean pushes) + whatever ORDER 92 commits. Codex baton idle (rev 227,
  turnOwner=claude). **ORDER 92 WAS IN FLIGHT AT PAUSE** (win baton rev 176, turnOwner=winclaude):
  functions leg + config-doc leg + a commit of the halt record and the client wrapper. **FIRST ACTION ON
  RESUME: relaunch the watcher, then read the win baton for ORDER 92's outcome** — partial completion is
  inert either way.
- Dark deploy: **indexes SHIPPED** (additive-verified) · functions + config doc = ORDER 92 ·
  **rules REFUSED and re-scoped** (17_ §7b).

## NEXT, in order
1. **THE RULES WORKSTREAM (mine).** The repo's `firestore.rules` is an unshipped P10-cutover end-state
   that self-declares undeployable; `firebase.json` points the deploy command at it. Correct sequence:
   fetch the LIVE ruleset (Firebase Rules REST API) as the merge base → AUTHOR the review_v2 clauses as
   real rule text (the artifact is 131 lines of SPEC, not deployable text) → run its mandated 10-case
   matrix on the MERGED file incl. the regression sweep → diff-review → its own order. Needed before the
   flip, NOT before the rehearsal.
2. **DF2-12/13 typed grading** — typed review currently returns `typed_modality_deferred`.
3. **DF2-51 client legs** — STARTED: `src/services/reviewV2Client.js` (the one call surface, decodes every
   typed status) + `REVIEW_V2_CLIENT=false` in featureFlags. Next: the session-flow rewiring
   (DailySessionFlow 2,666 lines; studyService 2,023) behind that flag.
4. **25WT rehearsal** → shadow audit (16_) → **David's backfill go** → **David's flip**.
5. **DEFER the DF2-10 adoption legs** (uniform evidence reader · D-1 throttle removal · force-pass
   resolver) until AFTER the rehearsal — they are the only remaining work that touches live student paths.

## STANDING (memories carry detail)
Perpetual watcher, relaunch first thing each wake · fold-ledger discipline + OTHER-LEG rule + **it covers
DEPLOY ORDERS (read every file an order names — the r91 near-miss)** · absolute paths, verified in-call ·
commit-then-marker (worktree frozen during reviews) · calibration alerts each round · zero git while
WinClaude holds a turn · Netlify locked, branch rule retired · R2-51 ratified · RESET_V2 flip = rehearsal
phase · THE FLIP IS DAVID'S ALONE.
