# RESUME — current active work

> **Canonical resume pointer.** When the user says "resume," read this first.
> **Rotate at each save-state:** copy → `docs/resume_archive/RESUME_<date>.md` (copy, don't move), then overwrite here.

---

## ▶ ACTIVE STREAM (rotated 2026-07-26 KST): DEEPFIX 2 — the consolidated forward program

**⚡ READ SECOND (mandatory, in this order):**
1. **`docs/session_state/2026-07-26_4817fc5a/STATE_BRIEF.md`** — the FULL context capture of the 07-24→07-26
   mega-session (saved for a --rm container restart). It carries: David's decisions D-1..D-4 + pass-to-advance,
   the Codex r43 findings still to fold, the LAST-EXCHANGE material that exists NOWHERE else (review-pool
   projections + the shadow-orbit starvation discovery + the attempt-write windows), the work queue, and what's
   awaiting David. **The next session is expected to be 100% aware of everything in that brief.**
2. **`docs/plans/deepfix2/02_TASK_LIST.md`** (v3) — THE plan of record (waves, DF2-IDs, §4 decisions register
   incl. the D-1..D-4 block, §6/§7 convergence records).
3. `docs/plans/deepfix2/00_ORIENTATION.md` — goals, pickup point, constraints, convergence protocol.

**⓪ STEP ZERO on resume — restore memory (the wipe is CERTAIN, verified 2026-07-26):** `~/.claude` lives on the
container's ephemeral overlay (wiped by `--rm`), while `/app` is the mounted Windows C: drive (persists). So the
FIRST action of the new session: `cp -rn docs/session_state/2026-07-26_4817fc5a/memory/. /home/ubuntu/.claude/projects/-app/memory/`
(`-n` = don't clobber anything newer that somehow exists). This restores the standing preferences — including
"never suggest committing plan docs."
**Session-state backup** (transcript 24MB + memory + sim scripts): `docs/session_state/2026-07-26_4817fc5a/`.
Transcript = targeted grep only (lookup tips in the brief §10); refresh script in the same dir.

### Where things stand (one breath)
DEEPFIX 2 designed + 3 convergence rounds run (r41 UNSOUND → r42 SOUND-WITH-GAPS → **r43: plan ACCEPTED, Wave-0
partial GO, Wave-1 needs fixes**). David then decided: **remove the review-score throttle entirely (D-1)**,
graduation only on passed review tests (D-2, within-pass rule pending his pick), all-attempts-recorded invariant
(D-3, windows verified + closure path identified), gate-off = nothing retroactive (D-4). **Immediate work = fold
r43 + D-1..D-4 + the last-exchange findings into deepfix2 v4, then the bounded re-review.** Nothing shipped;
26SM untouched; live state unchanged since 07-20 (client `6094cdd`, core pinned `0ddbb34`, throttle still live).

### Deployed state (unchanged)
- client `6094cdd` (Firebase hosting + Netlify) · `submitChallenge` `6094cdd` · `gradeTypedTest` `0992f5f` ·
  **`completeSession`/`resolveListProgress` PINNED `0ddbb34`** (certified core — pin-move discipline in deepfix2 §0)
- M4 shadow clock ends ~2026-08-01 · BlindSpot hide + quick-wins NOT yet built (Wave 0 pending)

### Prior stream (07-20, superseded): `docs/resume_archive/RESUME_2026-07-26.md` — grader r63 + weekly tokens
SHIPPED; review-pass gate banked (now = deepfix2 Wave 1). Deepfix1 record: `docs/plans/MASTER_TASK_LIST.md`
(historical — do not plan from it; its top pointer routes to deepfix2).

### Standing (binding — full list in the brief §9 + memory dir)
26SM read-only · deploys via WinClaude · never `git add -A`, never branch · never suggest committing plan docs ·
convergence = 3 Fable + 2 Opus + Codex (baton `docs/plans/loop/baton.json`, r43 answered, turnOwner=claude).
