# RESUME — current active work

> **Canonical resume file.** When the user says "resume," read this first, then the linked spec. **Rotate at each
> save-state:** copy to `docs/resume_archive/RESUME_<date>.md` (copy, don't move), then overwrite below.

---

## ▶ ACTIVE STREAM (rotated 2026-07-17, post-drift-correction): the **deepfix** program + CS fixes, reconciled

**READ FIRST: `docs/plans/CONSOLIDATED_ROADMAP_2026-07-17.md`** — the single reconciled roadmap, grounded in the LIVE
deploy. It supersedes `CONSOLIDATION_ROADMAP_2026-07-17.md` (which ran on a FALSE "undeployed" premise) and folds in the
CS work + the FREENAV gate onto the deepfix DEPLOY_ORDER spine.

### VERIFIED live deploy state (4 Fable agents + prod probes, 2026-07-17) — do NOT regress to "undeployed"
- **Client = `4b8452a`, live since 07-15 22:46Z** (== HEAD == origin/main; prod bundle build-stamp confirmed).
- **Functions = the deepfix tree, DEPLOYED-DORMANT** (prod `completeSession` → FAILED_PRECONDITION, deepfix-only string).
  All 11 FOUNDATION_FLAGS false; **`ANCHOR_VALIDATION_SHADOW=false` → the 14-day M4 shadow clock has NOT started.**
- **Rules = pre-deepfix** (P6/R1, R2, R3 un-deployed; repo `firestore.rules` = the P10d FINAL artifact — NEVER bare-deploy).
- Client flags: only `SERVER_ATTEMPT_WRITE` + `LIST_SCOPED_RECON` true (rest false + DCE'd from the bundle).
- **DEPLOY_ORDER position: between rows 2 and 3** — initial release SHIPPED (hosting+functions); server-authoritative
  activation (P3 flag flips → P4 → P5 → R1/P6 → P7; P8/P9/P10) all remains. See `audit/deepfix/task3/DEPLOY_ORDER.md`.

### Deepfix program status
- Tasks 1–5 COMPLETE; **Task 3 impl (P0–P10+P7) signed off**, Codex-converged, 13-finding final review folded.
- **Task 6 (audit RUN): SUBSTANTIALLY RUN, NOT CLOSED** — 6 matrices certified (M-STATIC/CALL/RULES/MIG-dry/NET/UI-flagoff).
  RESIDUE: flag-ON M-UI pass, W-* classification, single-runId cert→CERTIFIED, **Codex end-gate (HARD — never self-approve)**,
  `TASK6_REPORT`, the BLOCKED prod-smoke STEP1 (Windows permission-classifier denial — David's call or WSL runs it), and the
  **RED M-STATIC flag-table** (`lsr_deepfix_static.mjs:143` GRADE_TOKEN_MINT stale → blocks the next gated deploy).
- Live consequences of the deployed P1: **#16** review-racing (David: self-heal); the **DSF `dayGuardRejected` recovery firing
  on ~23 real 26SM students, 124 events/14d** (`DailySessionFlow.jsx:1529`, tracked carry-forward, server-fix dormant → closes
  at P4 — NOT the fixed #10). Carry-forwards open: CS-toolchain retarget (before P5), F-9 challenge-ledger, F-11 enrolledClasses.

### THE ONE OPEN DECISION — close the FREENAV gate (`docs/plans/loop/baton.json` rev 85, turnOwner=claude, since 07-16)
De-facto decided but never recorded: David put free-nav **ON HOLD** (`FORCED_PATHWAY_FIX_PLAN_2026-07-16.md:3`) and locked the
**BINARY throttle** (overrides the lighter-gate "floor throttle"). Repo carries TWO contradictory throttle designs until closed.
**Server-authority infra (P3–P5) is direction-INDEPENDENT** (free-nav needs it too); only the POLICY layer forks. David to ratify
one sentence ("binary supersedes floor; Practice-Mode-v2 + day-simplify absorbed; free-nav = gated north-star") + answer the
pass-to-advance product fork + the P8 continuation shape.

### CS fixes (the live 07-17 tickets) — designed, NOT written
`docs/plans/CS_2026-07-17_ROOT_CAUSE_EFFORT.md`: **PR-1** (`REVIEW_PAIRING_V2`+`REENTRY_GUARD`+`RECOVERY_GUARD`+F2) =
**direction-independent, David's SOLE remediation for the 14 truly-stuck** (census-locked 13/14+0FP, `scripts/cs/census-i4-pairing.mjs`);
**PR-2** (engagement stamp + I6 clamp + foundation mirrors OC-1/2/3/4) = functions redeploy folding into the deployed P3 artifact,
before P4; **PR-3** (`FORCED_PATHWAY` binary throttle) = direction-dependent, gate on FREENAV closure. David's locks: WI-5 unstick
HELD, skip-only→retake, grandfather pre-deploy docs.

### NEXT (Track 0, no deploy, direction-independent): refresh the RED M-STATIC flag-table · build the invariant suite (repo has
ZERO tests) · get David's FREENAV closure + PR-1 go. Then Track 1 (PR-1) + Track 2 (close Task 6 → P3 activation). Await David's go.

---

## Standing constraints (binding)
- 3-agent convergence with **Fable agents** (`subagent_type: general-purpose`, `model: fable`); **NO codex** for MY loops, but the
  **deepfix Codex end-gate is a HARD gate** (never self-approve a deepfix phase; wait for GO). **Never trust agents — verify every
  file:line + live fact myself.** Ground plans in live-Firebase/prod evidence.
- Git: commit on `main`, **NEVER branch**; commit/push only on David's say-so. `git checkout` on a src/functions/rules file destroys
  nothing now (tree==HEAD) but a checkout of `firestore.rules` yields the **P10d draft** — do not deploy it.
- Live Firebase: diagnose READ-ONLY; **NO 26SM writes without explicit authorization** (25WT/`lsr_*`/dups = sandbox). CS scripts from
  `/app` via `NODE_PATH=/app/node_modules node`. Deploys are owner actions (client→Netlify push; functions→`firebase deploy --only functions`).
- Logging: code → `change_action_log.md`; CS/data → `SUPPORT_RUNBOOK.md`. WSL runs Admin scripts + Playwright-vs-prod-URLs (not Vite/localhost).

Prior streams: `docs/resume_archive/RESUME_2026-07-17.md` (deepfix pre-drift), `RESUME_2026-07-17b.md` (this session's stale CS/consolidation RESUME, corrected here).
