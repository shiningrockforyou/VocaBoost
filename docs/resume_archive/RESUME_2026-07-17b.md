# RESUME — current active work

> **This is the canonical resume file.** When the user says "resume," read this first, then any
> linked spec. **Rotate at each save-state:** copy this file to `docs/resume_archive/RESUME_<date>.md`
> (copy, don't move), then overwrite the active-stream section below with the new state. History lives
> in `docs/resume_archive/` (see its README).

---

## ▶ ACTIVE STREAM (rotated 2026-07-17): CS root-cause fixes → consolidation roadmap

Two linked workstreams for VocaBoost (prod = **Netlify** `vocaboostone.netlify.app`; Firebase = functions/firestore only).

### 1. CS-2026-07-17 root-cause effort — PLAN CONVERGED, decisions locked, ready to build
**READ FIRST: `docs/plans/CS_2026-07-17_ROOT_CAUSE_EFFORT.md`** — the full 3-phase (3-Fable-agent + orchestrator
verify) effort: PHASE 1 (investigation plan) → PHASE 2 (investigation) → PHASE 3 (implementation plan + issue→fix
report) → David's LOCKED decisions → the read-only pairing census result.

**8 issues I1–I8 behind today's tickets** (이아연 runaway, Bridge-TOP can't-enter, 김재경 off-by-one, throttle
whack-a-mole, >100% scores). Root theme: **the same fact ("did you finish day N?") is computed in ~4 divergent
places** → they disagree → students strand. Fixes packaged as:
- **PR-1** (`REVIEW_PAIRING_V2`+`REENTRY_GUARD`+`RECOVERY_GUARD`): I4 pairing predicate + I3 re-entry + I6 client + F2 warn.
- **PR-2** (functions, dormant-safe): engagement/answeredCount stamp + I6 clamp + all `foundation.js` mirrors.
- **PR-3** (`FORCED_PATHWAY`): binary throttle + hold-csd + engagement readers + F2 teeth (I1+I2+I5). ≥7-day soak.
- Sequence: CS-data(HELD) → PR-1 → PR-2 → PR-3. Flag-gated, ship dark, flip after sandbox E2E. NEVER bare-deploy P10d rules.

**David's LOCKED decisions (2026-07-17):** (1) **WI-5 unstick HELD — fix in code first** (no 26SM writes; the ~14
stuck drain via PR-1). (2) **skip-only anchor days → real retake** (not auto-credit). (3) **grandfather pre-deploy
docs** for the F3 engagement gate. ⇒ PR-1 is the SOLE remediation → TAs hand-make tests in the interim.

**Census LOCKED the reader predicate** (`scripts/cs/census-i4-pairing.mjs`, READ-ONLY, ran 2026-07-17): **14** truly-stuck
(not ~24 — agents over-counted). Agent A's simple leg drains 3/14; **Agent C's tiered predicate + exact-range
temporal-drop = 13/14, 0 false-pairs** (1 residual is a skip-only day → retake, by design). `reviewPairsWithAnchor`
definition is in the doc's PHASE-3 census block — mirror it in `db.js:3726`, `foundation.js:568`, `studyService.js:240`.

**NEXT: build PR-1** (awaiting David's go), OR he reviews the doc first.

### 2. Consolidation roadmap — IN PROGRESS (same 3-phase / 3-Fable-agent methodology, no codex, verify all)
**READ: `docs/plans/CONSOLIDATION_ROADMAP_2026-07-17.md`** (shared input + converged phase outputs).
David asked for a strategic **strangler-fig consolidation roadmap** — the CS fixes are its first increments. Diagnosis:
NOT spaghetti; it's **missing single-source-of-truth + divergent duplication + shotgun surgery + inconsistent state
ownership** (attempts vs class_progress vs session_state as 3 truths; throttle recomputed at `studyService.js:325` yet
read stored at `db.js:3033`). Concepts to unify: day-completion, review-pairing, throttle-mode, session-phase/lifecycle.
**Position: COMPLETE — all 3 phases converged + orchestrator-verified; the 13-phase roadmap (0a→R12) is in the doc's PHASE 3
OUTPUT.** Verdict: NOT a rewrite — the big C5 state-ownership consolidation IS the staged-but-undeployed deepfix P0–P10; roadmap =
prove (0a flag-table + 0b invariant suite) → deploy staged deepfix in order → fold CS PR-1/2/3 INTO it → unify the rest (C2/C4/C6–C10)
on the server-authoritative base → retire. Minimum-shippable-this-week: 0a → deploy deepfix P1 (the committed **#10 fix for 23 live
students** + #11 wall) → CS PR-1 dark. Live findings surfaced: #10 self-race LIVE (124 events/14d, 23 real 26SM students —
NEED_TO_FIX #10 "sandbox-only" is STALE); no invariant test suite exists (Step 0b builds it); M-STATIC flag-table RED today.

---

## Standing constraints (binding)
- **Methodology:** 3-agent convergence per phase with **Fable agents** (Agent tool, `subagent_type: general-purpose`,
  `model: fable`); **NO codex**; **never trust agents — verify every file:line + claim against code/data before acting.**
- **Ground in empirical data** (live-Firebase read) where a claim is checkable, not just code/docs.
- **Git:** commit on `main`, **NEVER branch**; commit/push only on David's say-so. Never commit `serviceAccountKey.json`/.env.
- **Live Firebase:** diagnose READ-ONLY first; **NO 26SM writes without explicit authorization** (25WT/`lsr_*`/dup accounts =
  sandbox). CS scripts run from `/app` via `NODE_PATH=/app/node_modules node scripts/cs/…`.
- **Logging:** code → `change_action_log.md`; CS/data interventions → `SUPPORT_RUNBOOK.md`.
- **Env:** this WSL runs Admin CS scripts + Playwright-against-prod-URLs; it CANNOT run Vite/localhost.
- **Deploy:** owner action (client → Netlify git push; functions → `firebase deploy --only functions`).

Prior stream archived: `docs/resume_archive/RESUME_2026-07-17.md` (the deepfix program state, pre-rotation).
