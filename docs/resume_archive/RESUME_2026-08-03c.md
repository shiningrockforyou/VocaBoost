# RESUME — DEEPFIX2: CHECKPOINT ENDGAME (2026-08-03; orchestrator switching Fable → Opus xhigh)

## THE ONE PENDING EVENT: Codex r75 (baton rev 222, turnOwner=codex, judging EXACTLY its five r74 items)
- **On YES**: the checkpoint CLOSES (Opus already YES at r74). Next: WinClaude THE DARK-DEPLOY ORDER
  SERIES per docs/plans/deepfix2/17_DEPLOY_ORDER_REQUIREMENTS.md (functions+rules+indexes via CLI, all
  enabled:false — genuinely zero-delta: RESET_V2 const false, callables dark). Then 25WT rehearsal
  (client legs DF2-51 built ON A BRANCH — Netlify auto-deploys main!), shadow audit (16_), David's
  backfill go, David's flip. Baton flip contract: revision++ + full marker schema + marker last +
  file-read verify + sync (memory: codex-baton-flip-contract).
- **On NO**: classify per David's calibration standing order. Items INSIDE its five = fold narrowly.
  Items OUTSIDE its five = THE OVER-AUDIT ALERT (David-directed): tell David, recommend closing on
  Opus's YES + evidence instead of iterating.

## State (all durable)
- Git: e1c20ba pushed (17 clean pushes). Lap v3: 211/211 (receipt sha-bound, evidence/). Sweep receipt
  committed (46 lists, 0 dup/0 gap). Codex r70-r74 + Opus r70-r74 all archived in loop/ dirs.
- David rulings (receipts 15_ §3b): R2-51 RATIFIED (catch-up live, completedTwi absolute) · RESET_V2
  flip = rehearsal phase · calibration alerts standing · fold-ledger discipline standing · perpetual
  watcher standing · Netlify = every main push ships the client (17_ §8, memory).
- ORDER 88 owes: staging the DELETION of functions/change_action_log.md (already rm'd; the r75 row now
  lives in the ROOT change_action_log.md) + this RESUME rotation.
- The perpetual watcher: scratchpad/baton-watcher.sh (relaunch FIRST THING on every wake; exactly one
  alive — task birxogzmp at handoff time). Opus workflows: NONE running (Opus closed at r74).
- Review roster from here: Codex + Opus-xhigh lanes; the ORCHESTRATOR is now ALSO Opus xhigh — when
  spawning the review lane, note the orchestrator/reviewer separation still holds (fresh agent, no
  shared context, adversarial prompt as before; the Workflow tool per the standing memory).

## Working truths a successor needs (not in docs)
- Both r74 lanes converged; the remaining tail is narrow. Codex accepts nothing without file:line;
  every claim in a handoff MUST carry diff+fixture refs (three unsupported-claim incidents — never again).
- The OTHER-LEG rule is now part of the ledger discipline (fixture the leg you did NOT change).
- cwd persists across Bash calls: ABSOLUTE PATHS for every living-log/doc write, verify in-call.
- 25WT rehearsal design intent: rehearsalClassIds = the sandbox class ids; RESET_V2_FOR_TEST is
  emulator-gated only. THE FLIP IS DAVID'S ALONE (flip-review-v2.mjs refuses without his flag).
