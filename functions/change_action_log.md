| 2026-08-03 | THE r75 FOLD — composer/completion/config/callables.js + sweep receipt + NEED_TO_FIX/17_ corrections + lap | **Codex r74's 5 + Opus r74's carries closed. LAP: 211/211.** #1 resetLockActive REVERTED to fail-closed on ANY lock (Codex right: a crashed reset leaves a partially-deleted graph; liveness = TAKEOVER only) — the SERVES fixture replaced with THE SEQUENCE (stale ⇒ REFUSES ⇒ takeover reset re-fences/cleans/owner-clears ⇒ serves at the new epoch); resolves the Codex/Opus disagreement via Opus's own publish branch. #2/N-12 the new-test rows/posture fences HOISTED inside the engine (epoch) branch — the published legacy exemption now TRUE in code; engine posture requires the COMPLETE frozen shape (configVersion ≥ 1 + non-empty source, both fixtured); THE LEGACY DAY fixture (epoch-less posture-free new + presentation-less consumed complete a day together, postureSource completion_legacy). #3 the PARENT assignments container plain-map-checked BEFORE lookup (array-indexed-by-"0" + parent-Timestamp + entry-GeoPoint all fixtured HOLD). #4 the un-assignment race through the WRAPPED callable (afterPreflight hook). #5 the sweep emits a COMMITTED receipt (project/at/counts/scriptSha16 — evidence/list-position-sweep-receipt.json; re-run: 46/0/0) + BOTH cards' mechanism corrected (deleteWord does NOT reindex — delete leaves a gap; add allocates from the decremented count ⇒ collision; addWordsBatch mints RUNS; fix = max(position)+1). N-16 one-word in-txn identity. THE OTHER-LEG RULE adopted into the ledger discipline (would have caught 3 consecutive one-leg-greens). One placement red mid-build: a legacy fixture with VALID evidence completed day 5 out from under the happy path — relocated to its own day 10 as the honest end-to-end assertion |
