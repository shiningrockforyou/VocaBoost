# RESUME — DEEPFIX2 (2026-08-05: DEPLOY ORDER 105 IS OUT — David executes; grader fix + typed leg certified 452/452 ×3)

## ⚡ FIRST ACTION — arm the monitor, then session-start, then READ THE BATON
```
Monitor({command: "bash /app/scripts/deepfix2/baton-monitor.sh",
         description: "DEEPFIX2 baton returns (win + codex)", persistent: true, timeout_ms: 3600000})
```
Then `bash scripts/deepfix2/session-start.sh`. **The win baton is OUT at rev 203, round 105 = THE DARK
FUNCTIONS DELTA DEPLOY (order `claude_to_winclaude_105.md`, pinned sha c7d7cc7, DAVID EXECUTES —
"prepare now, I run ASAP" ruling).** If it returned DEPLOYED: verify per the order's WSL follow-ups
(dark-config admin read + ONE authorized 25WT typed probe: "answer"×20 must score 0), fold, then the
critical path advances (playwright-suite → b0-baseline → rehearsal). If REFUSED/finding: read the
review FIRST. **A parallel CS session shares this repo (David 2026-08-05)** — expect living-log dirt +
index.lock races; stage explicitly, never chase their files. **Codex baton: David says it may not
return — dispatch NO codex rounds; ratifications re-route internal/David.**

## WHERE THINGS STAND (all committed local; origin catches up at order-105 Step 0's push)
- **NTF-26 grader fix LANDED 2dfdfa6** (assessment → fix → 2-round opus audit GO → softening round):
  the 20×"answer" schema-confusion exploit (+ varied-filler class) closed; prompt 7345c8d4a2d92ada
  add-only; ≥8-identical pre-AI guard (`_uniformFiller`); harness batch cases (a)-(g); residual
  rejoinder-gloss false-reject = PRE-EXISTING + ~absent from live data (66k-word scan). NEED_TO_FIX 26
  is the full record. **Production still runs the EXPLOITABLE prompt until order 105 lands.**
- **Deploy-cert lap REPAIRED + green 452/452 ×3 runs** (e61264d): 3 fixtures modeled the superseded
  rv2-gradeContext design; reworked/retired honestly; CASE GR now pins the mouth guard. All deploy
  evidence re-certified against the exact candidate bytes (index.js 6b650d2fdc71a8f2).
- **df2-07(a/d/e) + df2-33 LANDED + VISUAL-CLEAN (win 104)**: threshold copy proven with the class's
  real 92; dashboard one-affordance (dayStatusAuthority) level-4 verified. df2-07(b) joins DF2-32; (c) unclaimed.
- **David's 4 rulings (de-jargonized round, recorded in queue + NTF-28)**: deploy=prepare-now/run-ASAP
  ✓done; retests=BUILD ai_metering FIRST then launch both; df2-51 scope=MOCKUPS FIRST (both wireframes
  DELIVERED via SendUserFile, a2109e2 — his ruling pending); gradedIsCorrect=SCAN FIRST ✓done.
- **gradedIsCorrect sweep (3882af7): 1.04M rows, ZERO unexplained** — 23 mismatches = our own
  CS-2026-07-23b fix; field exists on 51 rows. Gate-4 recommendation recorded in the queue row:
  backfill ignores the field + adjudication/manualOverride carve-out. David decides at gate 4.

## NEXT — AUTONOMOUS (whats-next.mjs authoritative)
- **`ai-metering-build`** — NOW UNBLOCKED (ntf26 landed). David-ruled pre-df2-51-typed-launch. Needs
  the orchestrator design pass FIRST: read 15_ §6 in full (aiCallCount · ai_metering/{uid} · _global),
  brief an opus implementer (functions/ live-path), opus auditor after. Deliberately DEFERRED at
  2026-08-05 session wrap (context depth; deploy was the critical gate) — top of the next session.
- **df2-51**: awaiting David's scope ruling (wireframes in his panel) + the metering build for the
  typed leg. Design draft 22_ has 5 PROPOSED decisions ready to ratify at implementation-brief time.
- **playwright-suite / b0-baseline / rehearsal-25wt** — unblock when order 105 returns DEPLOYED.
- Blocked-on-David: flip-thresholds ratify · backfill-go · flip-go · gradedIsCorrect (data in) ·
  teacher-registration · df2-51 scope · rules-leg deploy (17_ §7b, own workstream, before GATE 4).

## STANDING FACTS
- Dev build talks to REAL prod Firebase ⇒ 25WT only for UI checks; typed tests bill real tokens
  (≤200 authorized). WSL: no vite, no push ⇒ WinClaude orders. Emulator runs: `PATH="$HOME/jre/
  jdk-21.0.12+8-jre/bin:$PATH" ~/fbtools/node_modules/.bin/firebase emulators:exec --only firestore
  --project vocaboost-879c2 "node <script>"`; ONE lap at a time (audit/deepfix/emulator-lap.lock).
- Grader key for probes: Secret Manager via admin SA (fetch-grader-key.mjs pattern, scratchpad only,
  never print/commit).
- Netlify: builds STOPPED, prod pinned ce09792 — pushes are inert client-side.
- gate.mjs residual reds = audit/deepfix/task3 + 17_ NUMBERS/CLAIMS only (pre-existing; engine-lap +
  cutover-b/c/d + namespace + streak receipts ALL re-certified 2026-08-05).
- Before scoping any fold from a queue line, READ THE FULL SPEC; before starting a queue item, grep
  docs/ for a concurrent session's uncommitted draft.
