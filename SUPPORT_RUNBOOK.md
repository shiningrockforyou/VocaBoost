# VocaBoost — Support / CS Runbook

The single place for: (1) **defined CS scripts** (what to run for which problem), and (2) the **CS event log** (what we did, when, and why — so it's referenceable later).

**Golden rules**
- All admin scripts read Firebase Admin creds from `scripts/serviceAccountKey.json` (gitignored — never commit).
- Run from `/app`: `NODE_PATH=/app/node_modules node scripts/cs/<script>.mjs ...`. (Container can't run the Vite build; that's fine for admin scripts.)
- **Diagnose read-only first; write only with a derived/verified value.** Sweep before and after any write-bearing fix.
- 26SM = the REAL active cohort. 25WT = audit sandbox (`ta@` owner). Touch 26SM only for genuine CS fixes, never audits.
- A passed `new` attempt is the CSD/TWI **reconciliation anchor** — any script that writes one MUST set `newWordEndIndex` (+`newWordStartIndex`,`wordsIntroduced`,`testId`) or it creates an "invalid anchor" (see CS-2026-06-21).

---

## 1. Defined scripts (`scripts/cs/`)

| Script | Purpose | Mode | Usage |
|---|---|---|---|
| `data-integrity-sweep.mjs` | Scan a cohort for all known corruption signatures (dup/orphan progress, docId mismatch, implausible CSD, TWI>list, **invalid anchors**, `no_class` attempts, review-without-new-pass, ghost progress, missing programStartDate). | **READ-ONLY** | `node scripts/cs/data-integrity-sweep.mjs [classNameRegex=26SM]` |
| `manual-pass.mjs` | Unstick a student by writing a `passed:true` new-word attempt for a day — with a **VALID anchor** (full field set; derives word range from the student's pace). Replaces the old ad-hoc manual scripts that omitted `newWordEndIndex`. | WRITE (has `--dry`) | `node scripts/cs/manual-pass.mjs <email> <classId> <listId> <studyDay> <score> [--dry]` |

**Existing ad-hoc helpers in `scripts/`** (pre-catalog; use the `cs/` ones above when overlapping): `find-stuck-students.js`, `fix-stuck-students.js`, `advance-student-to-day.js`, `check-single-student.js`, `get-student-attempts.js`, `list-student-progress.js`, `delete-student-attempts.js`. ⚠️ `fix-stuck-students.js`/`advance-student-to-day.js` predate the valid-anchor requirement — verify they write `newWordEndIndex`, or migrate their callers to `cs/manual-pass.mjs`.

**Related app-side observability:** the CSD anomaly logs (`csd_anchor_invalid`, `csd_implausible`, `csd_anchor_query_error`) land in the `system_logs` collection on session load (shipped commit `9c162f6`/`7b5010e`). Watch them to catch invalid anchors / corrupt CSD as they occur, instead of only via sweeps.

---

## 2. Common problems → fix

- **"Grading Failed" loop** → root-caused + fixed in app (commit `1771876`: recovery snapshots dropping word definitions). If a student is still stuck on an old failure: `cs/manual-pass.mjs` for the affected day, then re-run the sweep.
- **Student stuck / day won't advance** → diagnose with `get-student-attempts.js` + `list-student-progress.js`; reconciliation rule = CSD derived from the most-recent **passed** new attempt (`twi = newWordEndIndex+1`; `csd = reviewExists(anchorDay) ? anchorDay : anchorDay-1`). If the anchor is malformed → `cs/manual-pass.mjs`.
- **Suspected data corruption (cohort-wide)** → `cs/data-integrity-sweep.mjs`.

---

## 3. CS event log

### CS-2026-06-21 — Invalid reconciliation anchors from manual-pass attempts (26SM)
- **Trigger:** read-only data-integrity sweep of all 26SM students (309) requested after the dashboard work.
- **Finding:** cohort overwhelmingly clean (0 across dup/orphan/docId/CSD/TWI/attribution/stuck). **3 students** (`hgk2480@gmail.com` Adv A1, `kimseongyun2024@gmail.com` Adv A2, `noaa.kimm@gmail.com` Inter A1) had `passed:true` new-word attempts **missing `newWordEndIndex`** → "invalid reconciliation anchors."
- **Root cause:** these were **manual-override attempts** (docId `..._day{N}_typed_new_manual`, fields `manualOverride`/`manualReviewNote`, no `testId`/`newWordStartIndex`/`newWordEndIndex`/`wordsIntroduced`) — written by a now-deleted scratch manual-pass script during live support (likely unsticking grading-failure victims, dates 6/09–6/12). The scratch script omitted the anchor fields.
- **Impact:** none broken. 2 students superseded by a later good anchor; 1 (kimseongyun, whose most-recent was the bad one) was protected by the existing `Math.max(storedCSD,csd)` reconciliation — not stuck.
- **Fix (write, authorized):** backfilled `newWordStartIndex`/`newWordEndIndex`/`wordsIntroduced` on the **4** manual attempts across the 3 students, derived from each student's verified pace (80/day) + neighbor attempts; stamped `anchorBackfilledAt`/`anchorBackfillNote`. Post-fix sweep = **0 findings, CLEAN**. (noaa day-10 anchorTWI 800 vs stored 720 is normal mid-day-10 lag, not corruption.)
- **Prevention:** created `scripts/cs/manual-pass.mjs` (always writes a valid anchor) + `scripts/cs/data-integrity-sweep.mjs` (reusable read-only sweep) + this runbook. The app-side `csd_anchor_invalid` log will now surface any future invalid anchor automatically.
- **Follow-up:** audit `scripts/fix-stuck-students.js` / `advance-student-to-day.js` for the same omission.

## CS-2026-06-23 — Typed-grading malform fix deployed; formerly-stuck students verified healthy

- **Context:** The "Grading Failed" loop (gradeTypedTest rejecting typed submissions as `invalid-argument`, 230 rejections / 14 real 26SM students on 2026-06-22) was fixed and deployed today (functions + client). Root cause: crash-recovery marker thinned the word pool, dropping definitions; fix = persist defs in the marker + server-authoritative definition resolution + write-path backfill. See change_action_log.md (2026-06-23) and PLAN_typed_grading_malform_fix_v1.md (v5).
- **Post-deploy validation (no data writes to real students):** server E2E 10/10 vs deployed callables (thin-payload resolves, forged-def ignored, unassigned rejected, backfill+writtenBy); browser E2E happy path (EN/KO, listId/classId sent, writtenBy:cloud-function) + Retry-Save UX (modal renders, no re-grade). All via audit student in 25WT sandbox, cleaned up.
- **Formerly-stuck students — NO intervention needed:**
  - 서준혁 (gejh0131@gmail.com) — manually unstuck 2026-06-22 (see that day's entry).
  - 정다인 (da.xn1j@gmail.com, uid Zuc4iziJ…) — SELF-RECOVERED: has a passed Day-1 new typed attempt (score 100, writtenBy client), session_state phase=review-study, newWordsTestPassed=true. Healthy; can proceed with review.
- **Orphan allowlist note (for class owner):** `KNOWN_ORPHAN_WRITES` in functions/index.js hardcodes 2 students (W3MUFXDb, fc8sBxnz) writing to list `7Is5UdS4P4` under class `teKHajON` — that list was unassigned mid-progress. The allowlist keeps them working. ACTION: the teacher should reassign the list or let them finish; remove the allowlist entries once done.

## CS-2026-06-23b — Add ASCENT to Base-Camp-only INT classes (prevent list-swap orphaning)

- **Why:** Students get "list reset to Day 1" when a class's list is **swapped** (Base Camp removed, Ascent added) instead of **added alongside** — their progress is keyed by (classId,listId) and becomes unreachable when the list is unassigned (e.g., 박한별/bergkamp0202: Base Camp Day-6 progress stranded under B2 after B2 went Ascent-only). Root cause = explicit unassign (assignListToClass only ADDS; unassignListFromClass is a separate confirmed action whose dialog misleadingly says "Student progress is saved"). Fix/prevention = every INT class should assign BOTH Base Camp + Ascent so the selector covers both and no one is stranded.
- **Action (authorized by David):** Added Ascent (dVliNv0p9jqZYp9rfLpN) to the 4 INT classes that had Base Camp but not Ascent — **A1, A2, B1, O** — copying each class's own Base Camp settings (typed, thr=92, pace=80, new=30, opts=6). Backups: `dsg-edits/srv_validate/class_backups/<id>.json`. Verified all 4 now have both. No student progress touched (config-only). Current Base Camp students undisturbed; Ascent becomes available when they reach Day 16.
- **Audit result (11 INT classes):** 10/11 now have both. **B2 (CKDTCx) is the anomaly — Ascent-ONLY, missing Base Camp** (someone unassigned Base Camp). PENDING DECISION: re-add Base Camp to B2 → recovers 박한별 + other B2 Base-Camp stragglers naturally (progress docs intact, just hidden). Re-add must be to B2 (where progress lives), with settings copied from a peer (B1).
- **Follow-up:** reword the unassign confirm in ClassDetail.jsx:389 ("Student progress is saved") → warn that mid-progress students lose access until re-assigned.

## CS-2026-06-24 — 이가온 (Final A) advanced past stuck Day-11 typed test

- **Student:** 이가온 / gaonlee0909@gmail.com / uid TGJNuQ1v… / class qeCkS6Bp (26SM SAT Final A) / list dVliNv0p (Ascent, typed, pace 100).
- **Symptom:** Day-11 단어시험 stuck on the LOADING screen, won't proceed; reproducible across devices (home too). Stuck since ~6/22.
- **Diagnosis (read-only):** list + Day-11 segment 100% clean (1600 contiguous words, 100 valid Day-11 words); session_state consistent (studied all 100, dismissed IDs all match the Day-11 segment). 0 grading logs since 6/22 → hangs at LOAD, before submit. Other Final A students pass Day 11+ (peer at csd 13) → student-specific, NOT a list/class issue. Root cause = an unreproduced client-side load hang on this account's Day-11 state — could NOT pin from DB alone. Note: Days 1-9 were in-app; Day 10 was already a manual pass (older script, incomplete anchor — missing testId/wordsIntroduced), so typed-test trouble is recent (Days 10-11).
- **Action (authorized by David, "advance 가온"):** `scripts/cs/manual-pass.mjs gaonlee0909@gmail.com qeCkS6Bp dVliNv0p 11 100` → valid anchor (nwsi 1000/nwei 1099/wIntro 100/testId set/manualOverride). Then session_state → phase=review-study, newWordsTestPassed=true, csd=11. data-integrity-sweep "Final A" CLEAN before & after.
- **2026-06-24 follow-up (David: "send her to day 13"):** advanced further to Day 13. Wrote Day-12 new anchor (manual-pass, nwsi 1100/nwei 1199) + Day-11 & Day-12 mcq review attempts (manual); class_progress → csd 12/twi 1200; session_state → Day 13 new-words-study (dismissed cleared). Reconciliation verified: most-recent passed-new = Day 12 + Day-12 review present → csd 12, twi 1200 → student lands on Day 13. Sweep CLEAN before & after. NOTE: she will now hit the Day-13 typed test — if the load hang recurs, she'll be stuck again (get the console screenshot then).
- **⚠️ Root cause NOT fixed — bypassed.** If the Day-11 review (MCQ) or Day-12 new (typed) hangs again, escalate. NEXT TIME they hit the loading hang, get a **browser-console screenshot** (F12) — that error pinpoints the actual bug. This student has now needed manual passes for Day 10 AND 11, so watch for recurrence at Day 12.

## CS-2026-06-24b — 박시은 (Inter E) → Ascent default + skip to Ascent Day 3

- **Student:** 박시은 / sieunprida@gmail.com / uid jJAodkRO… / class jDDw2GEu (26SM SAT Inter E, has Base Camp + Ascent, pace 80 typed).
- **Issue:** TA mis-guidance + stress; mother asked to start from Day 3. Was split across two lists — Base Camp Days 1-2 passed (Day-2 review pending) AND failing Ascent Day 1 x3. Root cause of the Ascent confusion: the getPrimaryFocus FALLBACK picks the most-recently-assigned list (Ascent, added 2026-06-22) for any student without an explicit selection, ignoring existing progress — so adding Ascent silently bumped Inter students onto the hard list. (See nice-to-haves.)
- **Action (David: "make ascent her default and let her start on day 3"):** manual-pass Ascent Day 1 + Day 2 (nwsi 0/79, 80/159) + Ascent Day-1/Day-2 reviews; class_progress(Ascent) csd 2/twi 160; session(Ascent) → Day 3 new-words-study; settings.primaryFocusListId → Ascent. Verified: anchor Day 2 + Day-2 review → csd 2 → Day 3. Sweep CLEAN before & after. Base Camp progress untouched (just no longer default).
- **Follow-up:** the getPrimaryFocus fallback (newest-assigned, progress-blind) is the systemic stress cause when a list is added — should prefer the list with active progress. Added to scripts/cs/nice-to-haves.md.

## CS-2026-06-25 — 조예서 (Inter B2) → accept TA-graded answer, unstick Day-7 typed test

- **Student:** 조예서 / 0exey.7@gmail.com / uid 9fkKxuF0… / class CKDTCxTTvscEZAY1DbUN (26SM SAT Inter B2) / list **Base Camp** (RmNNkuLP, typed, pace 80, passThreshold 92).
- **Symptom (TA report):** in yesterday's word test the TA hand-graded `amnesty`="대사" as correct and passed that item, but VocaBoost still showed the whole test as not-passed, so the student couldn't advance to the next Day.
- **Diagnosis (read-only):** Days 1-6 all passed; the **Day-7 new typed attempt** (…408748_8fhos08hc) scored **90% = 27/30, passed=false** (threshold 92). Three items the AI marked wrong: `amnesty`="대사", `migratory`="animal that migrates", `synthetic`="통합적인". The AI grade was technically correct (대사 = ambassador/dialogue, not amnesty/사면), but the TA chose to accept it. Accepting **just amnesty** → 28/30 = **93% ≥ 92 → passes**. The attempt already carried a valid anchor (newWordEndIndex 559, testId set), so no fabrication needed.
- **Action (per TA decision):** corrected the single disputed answer in the **real** Day-7 attempt — `amnesty.isCorrect=true` (note: "CS override 2026-06-25: TA accepted 대사"), recomputed 28/30 → score 93, passed=true, retention 0.9333, manualOverride flag + manualReviewNote. Then session_state(Base Camp) → phase=review-study, newWordsTestPassed=true so she proceeds to the Day-7 review and on to Day 8. **Did NOT touch migratory/synthetic** (TA only accepted amnesty). data-integrity-sweep "Inter B2" CLEAN (0 findings) before & after; invalidAnchor 0.
- **Note (not a bug):** this is a grading *judgment* discrepancy, not a VocaBoost fault — the system graded a wrong answer correctly; the teacher overrode it. Preferred resolution is to correct the actual attempt (honest re-grade) rather than write a synthetic manual-pass. If TAs frequently want to override single items, a teacher-facing "accept this answer / re-grade" control would remove the need for CS scripts (candidate nice-to-have).

## CS-2026-06-28 — Grading-failure audit (26SM): 06-22 crash-recovery "malform" incident (already fixed), 0 data loss, + error-modal UX gaps

- **Scope:** read-only audit (no data touched). "Have any gradings *truly* failed for real students?" Cross-referenced `system_logs` (client-observed) + Cloud Functions logs (server-side) against `attempts`, 26SM only, full history.
- **Findings — client side (`system_logs`):** 660 `grading_attempt_failed` events, 06-18→06-28 — but mostly **retry-storms**; dedupe → **42 distinct failed test-sessions across 48 students**. Cross-ref vs `attempts` (match on `sessionType`+studyDay, plus "csd advanced past the failed day"): **36 graded-anyway, 6 advanced-past, 0 still stuck → 0 permanent lost grades.**
- **Root cause — the bulk (06-22 incident):** Cloud Functions logs (06-21→06-23) show **315 × HTTP 400**, of which **313 = "Malformed grading payload (likely client recovery dropped definitions)"** across 21 students, spiking at class hours (06h=93, 08h=80). This is the **typed-grading crash-recovery malform bug**: the test-phase recovery marker thinned the word pool to `{id, word}`, dropping `definition` → on mid-test reload the grade payload had `correctDefinition=undefined` for every word → whole batch rejected (all-or-nothing) → "Grading Failed" retry loop. **Already root-caused + FIXED** (change_action_log 2026-06-23: client persists `definition/definitions/partOfSpeech`; server `resolveAnswerDefinitions` backfills from the list + softened to auto-mark only unresolved rows wrong, throw only if EVERY row unprocessable). Logs confirm the fix landed: 400s collapse **313 (06-22) → ~2 (06-23) → ~12 total (06-23→06-28)**.
- **This explains CS-2026-06-24 (이가온/TGJNuQ1v, "stuck on LOADING" Day-10/11):** that account has many `grading_attempt_failed` on d10 (06-19) / d11 (06-22) — the same malform/grading failures, not a separate load hang. The manual advance bypassed it; root cause was the recovery-malform bug (now fixed).
- **Residual after the fix (the one open item):** ~12-16 failures = **`listId: null` → "Unresolvable grading payload"** (server can't backfill defs without a listId, so a thin payload still hard-fails) + 3 × `401 auth/id-token-expired` (session token expired mid-test). → NEED_TO_FIX.
- **Error-modal UX (answer to "what should students do on a grading pop-up"):** there are actually **two** modals, and they already distinguish the main cases:
  - **"Couldn't Save Your Results"** (yellow, `submitError`) = grading SUCCEEDED but the durable save failed → button **"Retry Save"** re-runs the write only (no re-grade). Copy correctly reassures "your answers are safe." → student should click Retry Save; their grade is captured.
  - **"Grading Failed"** (red, `gradingError`) = the grade call itself failed after 3 auto-retries → button **"Try Again"** (idempotent).
  - **Student guidance:** click the button — retries are **idempotent/safe** (won't double-count or lose work; a successful retake is detected). If "Try Again" fails *immediately* and repeatedly (deterministic error, e.g. the old malform case), **reload the test page and resubmit** — that rebuilds the payload with definitions, which is the actual fix. Progress is not lost.
  - **Gaps (→ NEED_TO_FIX):** (a) the red "Grading Failed" title is alarming/categorical and its body "Your answers are saved" is misleading (no graded attempt was written when grading truly failed); (b) on a transient/timeout error the server may have graded but the client still shows "Grading Failed" (no durable attempt yet → retry needed, but the copy implies loss); (c) deterministic errors (`invalid-argument`) auto-retry + "Try Again" loop uselessly — should detect and tell the student to reload instead.
- **Net:** truly-failed *calls* happened (06-22 incident, ~21 students) but were **already fixed**; **0 permanent lost grades**; residual is the `listId:null` tail + modal-copy clarity.
- Artifacts (read-only, gitignored): `dsg-edits/srv_validate/grading_failure_audit.mjs`, the two Cloud-Functions log exports.

## CS-2026-06-28b — Add SUMMIT to all ADV/FINAL 26SM classes without bumping anyone off ASCENT

- **Goal (David):** make SUMMIT (list `AObYOowhLoOOHx9wW2Sq`, 800 words) available on all 11 ADV/FINAL 26SM classes, WITHOUT changing any student's current list. ASCENT (`dVliNv0p`) = the active list (1600 words; ADV 80/day, FINAL 100/day).
- **Footgun (verified):** the default-list picker `getPrimaryFocus` (Dashboard.jsx:992) uses an explicit `users/{uid}.settings.primaryFocusListId` if set, else falls back to the **most-recently-assigned** list. Read-only audit: **200 of 215** enrolled students had NO explicit default → assigning SUMMIT (newest `assignedAt`) would have bumped all 200 onto SUMMIT Day 1, off their ASCENT progress (many at Day 15). Only 15 safe (11 explicit ASCENT, 4 other); 3 had finished ASCENT. Same class of bug as CS-2026-06-24b (박시은). **Do NOT add the list via the teacher UI** (stamps assignedAt=now).
- **Action (both safeguards, authorized; `dsg-edits/srv_validate/add_summit_safe.mjs --commit`):** (1) **BACKDATE** — assigned SUMMIT to all 11 classes with `assignedAt` = ASCENT's minus 1 day (2026-05-30 < 2026-05-31), settings (pace/threshold/testMode) copied from each class's ASCENT assignment; added to `assignments` map + `assignedLists`. (2) **PIN** — set `settings.primaryFocusListId=ASCENT` + `primaryFocusClassId` for the 200 fallback students (merge; experience unchanged — they already land on ASCENT). Left the 15 explicit-default students untouched.
- **Verify:** SUMMIT present 11/11, backdated < ASCENT 11/11; 200/200 pinned. Post re-check: **0 students on fallback, 0 classes where SUMMIT outranks ASCENT** → nobody exposed. data-integrity-sweep CLEAN before & after.
- **⚠️ Future:** re-assigning SUMMIT via the teacher UI would re-stamp it newest and re-expose any *future* unpinned student → re-backdate via the script if SUMMIT ever needs re-touching. Root fix = change the fallback to prefer the list with active progress (logged nice-to-have; folding into Phase 2 progress=student+list rework).

## CS-2026-06-29 — Save-failure incident (grade-token), grader "restating Korean def" bug, + 3 student fixes

Live class-time incident day. Four threads, all 26SM. data-integrity-sweep 26SM CLEAN before & after all writes.

### A. "Couldn't Save Your Results" hitting many students (durable-write reject) — MITIGATED
- **Symptom (TA channel):** multiple students across INT B2/B3 + 9 classes got the yellow `submitError` modal; **Retry Save did not fix it.**
- **Diagnosis (read-only):** 128 `attempt_write_failed_client` in 18h; **118 = `permission-denied` "requires a valid, fresh server grade token."** Token WAS minted (serverGraded), fresh (<24h), createdAt finite → the **HMAC verify at `submitVocabAttempt` rejected the save** under `GRADE_TOKEN_ENFORCED`. Retry Save can't fix it (re-sends same token, never re-grades). Root mechanism: the token binds `attemptDocId` (a localStorage-persisted nonce, `getOrCreateAttemptNonce`); when localStorage can't persist (in-app webviews/private/refresh) the grade-time docId ≠ save-time docId → HMAC mismatch. Confirmed via prod data: 4 students re-graded the SAME test under diverging nonces.
- **Action:** owner deployed **`GRADE_TOKEN_ENFORCED = false`** (functions/index.js:45) mid-day 06-29 (~11:14 KST). Verified live: new attempts now write `correctnessSource:null` (gate off) → saves succeed. **Engineering follow-up (NEED_TO_FIX, before re-enabling enforcement):** submit with the server-returned `attemptDocId` (don't let client regenerate it) + harden the nonce (sessionStorage/in-memory fallback). I cannot deploy (no CLI / admin SDK ≠ deploy creds) — owner deploys.

### B. Grader marked correct Korean answers wrong ("restating the Korean definition") — FIXED in deploy, but provenance gap
- **Symptom (TA):** 박시은 INT-E Ascent Day 6 — correct answers marked wrong; TA hand-graded 30/30.
- **Diagnosis:** her stored Day-6 (id …fh5rk4ri, 60% = 18/30) had **10 of 12 wrong = answers that matched VocaBoost's own Korean definitions** (표준화/복종/온순한/식민주의/다학제적인, verbatim KO-def fragments for adept/qualify/pristine/legitimacy/consequential). Only `alas` (genuinely wrong) + `oppressive` (negation typo) are real misses → true score **28/30 (93%)**. **Replaying her exact 30 answers through the LIVE grader now = 2/30 wrong, deterministic (4/4 runs)** → the bug is gone in the current deployment. Current prompt has the rule "answers matching the provided Korean definition → CORRECT" (committed 2026-03-10 in the mislabeled commit `0de81fb "apboost audit and updates"`).
- **Provenance gap (⚠️):** production emitted the "restating" failures **06-28 20:19 → 06-29 11:14 KST** (28 attempts), incl. her 10:19 test — i.e. the deployed grader did NOT match committed code until the ~11:14 KST 06-29 redeploy. The June-28 deploy shipped a stale grader artifact. → verify what commit each deploy builds from; the grader fix was hidden in an "apboost audit" commit.
- **Cohort sweep:** 28 restating-flagged failed attempts → only **1 still stuck** besides 박시은: `mkicsb0618@gmail.com` (유라시아 SAT Top, Day 6, 83%). **Re-graded with current grader = same 83% (29/35), still wrong on cavalier/maximal/imaginable/ethos/ornamental/glib → GENUINELY below threshold, NOT a grader victim** (string-match over-counts; re-grade is the real test). Everyone else recovered on retake. **Net grader-bug stuck-victims = 박시은 only.**

### C. Student fixes (manual-pass.mjs, valid anchors, sweep CLEAN before/after)
- **박시은 / sieunprida@gmail.com / class jDDw2GEu (26SM SAT Inter E [English only]) / Ascent (dVliNv0p):** grader-error correction. `manual-pass … jDDw2GEurOj5kD9JswI7 dVliNv0p 6 93` (28/30 verified score; nwsi 400/nwei 479, pace 80). Was stuck CSD=5. (This is the TA's "INT E Day 6.")
- **황정민 / jungminacc170078@gmail.com / class CKDTCxTT (26SM SAT Inter B2) / Base Camp (RmNNkuLP):** lost-save (incident A) — took Base Camp Day 6, save failed, closed the window before Retry (7 write-fails logged, all Day6/new/Base Camp), no cached grade. David: "pass 황정민." `manual-pass … CKDTCxTTvscEZAY1DbUN RmNNkuLPectBlBPiLbAJ 6 100` (manual, answers:[]; nwsi 400/nwei 479).

### D. 김선아 / hanna0801ha@gmail.com / class 6F0PX2E3 (26SM SAT Adv A1) — investigate only (no fix)
- **Symptom:** "Submit Test?" confirm modal — Go Back **and** Submit both dead (먹통). Screenshot = the pre-grade `ConfirmModal`.
- **Findings:** modal code is correct (clean `onConfirm`/`onCancel`, no disabled/overlay). Her saves WORK (3 gradings today, 2 attempts written, no write-fails). Scores 43→47→73% are **GENUINE** — wrong answers are real (speculate→"감독하다", blanks on caucus/zenith/caliber, abate→"힘", arrears→"폐합금") and post-fix (18:13 KST). NOT the grader bug. → dead buttons = **client-side freeze** (browser/tab/in-app webview), same environmental family as incident A. Recommend hard-refresh / real browser (not KakaoTalk in-app). Defensive UI hardening = candidate NEED_TO_FIX; couldn't repro from data.

## CS-2026-06-30 — 이주헌 class-change day-reset (Ascent duplication) → carry-forward + drop old classes

- **Student:** 이주헌 / jooheon0923@gmail.com / uid OCzwBwAb… . data-integrity-sweep 26SM CLEAN before & after.
- **Symptom (TA):** changed classes, his study DAY reset → words duplicated (re-studying already-known Ascent words).
- **Diagnosis (read-only):** enrolled in 3 classes. Did **Ascent days 1–8 in Adv A2** (Nys1FfB9, CSD=8/twi=640, real 100% passes). Moved to **Adv A1** (6F0PX2E3, same Ascent list) which started fresh at **CSD=1/twi=80** → fed him Ascent words 1–80 again. Root cause = **progress is keyed by (class, list)**, so a class change on the same list resets the day-counter (the Phase-2 "progress should be student+list" gap; same family as the getPrimaryFocus footgun). **study_states are student+list scoped** (doc id = wordId, has `listId`, NO `classId`) — verified he has **640 Ascent study_states** — so his *mastery* already follows him; only the counter was wrong. Cleanest possible carry-forward.
- **Action (David: "adv a1 is canonical, carry ascent to day 8, drop other classes after"):** (1) `manual-pass jooheon0923@gmail.com 6F0PX2E3gXetiI0Yw275 dVliNv0p 8 100` (his real Adv-A2 Day-8 score; nwsi 560/nwei 639, valid anchor). (2) set Adv A1 class_progress CSD 1→8, twi 80→640 (immediate). (3) cleared Adv A1 session_states (recompute → Day 9). (4) **dropped Adv A2 (Nys1FfB9) + Inter A2 (TyBnqbcc)**: removed from each class's `studentIds`, removed from `enrolledClasses`, deleted their class_progress + session_states under the user. Left historical attempts intact.
- **Verify:** enrolledClasses now = **only Adv A1**; progress = only `6F0PX2E3_Ascent` CSD=8/twi=640; most-recent passed-new = Day 8 → resumes **Day 9 (words 641+)**, no duplication; 640 Ascent study_states make reviews skip mastered words. Sweep CLEAN (ghostProgress 0, invalidAnchor 0, noClassAttempt 0).
- **Same-day investigate-only (no writes):** **박시준** (herosijun@gmail.com, INT A2) — "submit didn't count": both Day-1 submits DID save (67% real + 0% blank/accidental retake, 24/30 empty); stuck only because INT-A2 Base Camp threshold is **92%** and he hit 67% → retake or teacher-pass decision, NOT a fault. **조은호** (ellycho016@gmail.com, 유라시아 SAT Top) — "test words ≠ book 1201-1300": he's correctly on Ascent Day 1 (book numbers Ascent from 1201 = list position 0); list clean/contiguous; pending book-vs-app word comparison (sent the 100-word list).

## CS-2026-07-02 — 곽경훈 (Final B): accept aesthetics="미적", pass Day-4 Ascent

- **Student:** 곽경훈 / kwaknorinori@gmail.com / uid ikEf8P7U… / class KKQ0x7ki (26SM SAT Final B) / list Ascent (dVliNv0p, 100/day, passThreshold 92). Sweep CLEAN before & after.
- **Request (David):** "mark aesthetics/미적 as correct, pass him."
- **State:** latest typed = **Day 4 new**, 32/35 = **91% → failed** (threshold 92). The `aesthetics` row (studentResponse "미적") was **already student-challenged and teacher-REJECTED** (`challengeStatus:"rejected"`, reviewer 9OcxdnY…) — David is overriding that to accept. Other misses left as-is: `faithful`="신앙적인" (wrong), `supple`=blank.
- **Action:** corrected the real attempt (id …_new_1782964686497_6n8bwjrlh) — `aesthetics.isCorrect=true`, `challengeStatus:"accepted"` + note; recomputed **33/35 → score 94, retention 0.9429, passed=true**; `manualOverride:true` + manualReviewNote. Then session_state(Ascent) → `phase=review-study, newWordsTestPassed=true` so he proceeds to the Day-4 review → Day 5. class_progress left at CSD 3 (advances when he does the Day-4 review). Same pattern as CS-2026-06-25 (조예서).
- **Note:** this reverses a prior teacher challenge-rejection at David's instruction — recorded here for traceability.

## CS-2026-07-02b — 손진욱 (→Adv A1) & 박주하 (→Inter A2): class-change Ascent carry-forward

Both class-change carry-forwards (same family as CS-2026-06-30 이주헌: progress is (class,list)-keyed; study_states are student+list so mastery carries). Sweep CLEAN before & after.

- **손진욱 / sonjinug72@gmail.com / uid bn8NwX7J** — was enrolled ONLY in Adv B2 (`qIrJTbwl`), did Ascent Days 1–9 there (CSD=8/twi=640); TA (신동현) asked to move him to Adv A1. Action: **enrolled Adv A1** (`6F0PX2E3`, studentIds + enrolledClasses), **carried Ascent** → Adv A1 CSD=8/twi=640 (manual-pass Day-8 anchor score 97, nwei 639) → resumes **Day 9**; **dropped Adv B2** (unenrolled + deleted its class_progress/session). 720 Ascent study_states carry. default → Adv A1/Ascent.
- **박주하 / bagjuha477@gmail.com / uid fXsusIOb** — enrolled Adv A2 + Inter A2; did Ascent Days 1–5 in Adv A2 (CSD=5); Inter A2 had only BaseCamp Day 1. TA (김시연) asked Inter A2 Ascent to start Day 6. Action: **carried Ascent** Adv A2 → **Inter A2** CSD=5/twi=400 (manual-pass Day-5 anchor score 93, nwei 399) → resumes **Day 6**; default → Inter A2/Ascent. **Adv A2 left enrolled** (not dropped — TA only asked for the Day-6 carry; drop later if wanted). 400 Ascent study_states carry.
- **Self-caught issue:** I first built the new class_progress docs by spreading an existing student's (이주헌's) doc as a template — its only non-empty personal field was `programStartDate`, which leaked (both got 2026-06-29). Corrected: 손진욱→2026-06-24 (his earliest Ascent attempt), 박주하→2026-06-22 (his real Adv-A2 start). All other template fields were already empty/default (recentSessions [], streakDays 0, stats {null avgs}, no snapshot) = same as a fresh doc, so no further contamination. **Lesson: don't spread another student's progress doc as a template — set explicit fields.**

## CS-2026-07-03 — 김나연 (Inter A1): 93% shown as "fail" (retakeThreshold default bug + dual-class)

- **Student:** 김나연 / nayunkim777@gmail.com / uid y70xrW… . Sweep CLEAN before & after.
- **Symptom (TA):** got 28/30 vocab but screen shows **fail** and won't advance.
- **Diagnosis:** her Day-14 Base Camp test = **28/30 = 93%**, **server stored passed=true** (Inter A1 Base Camp threshold **92**), and her progress already advanced (CSD=14, session csd=15). But the client results screen showed "fail" and looped her into retakes (two Day-14 passed attempts today). Root cause = `TypedTest.jsx` `retakeThreshold` **defaults to 0.95** and only drops to the real 0.92 once it loads the class `passThreshold`; that load fell back to the default, so it compared 93% < 95% → "fail". Trigger: she was enrolled in **TWO Base Camp classes** — Inter B2 (`CKDTCx`, unused CSD=0) + Inter A1 (`k0j59bXv`, real CSD=14) — which breaks the client class/threshold resolution.
- **Action (David: "drop B2, make sure she's in A1"):** ensured Inter A1 enrollment (studentIds + enrolledClasses), **dropped Inter B2** (unenrolled + deleted its Base Camp class_progress CSD=0 + session_state), set default → Inter A1 / Base Camp. A1 progress (CSD=14/twi=1120) untouched. → single class now resolves threshold to 92 → her 93% reads as pass. She should reload (already on Day 15).
- **Follow-up:** the `retakeThreshold=0.95` default is a real client bug (higher than class thresholds → any threshold-load failure turns a genuine 92–94% pass into a displayed "fail"). Filed in NEED_TO_FIX. If she still sees "fail" after reload, escalate to that.

## CS-2026-07-03b — 김호형 (Adv E) 93%-shows-fail → cohort-wide newWordRetakeThreshold fix (no deploy)

- **Student:** 김호형 / **hperaszz**@gmail.com (TA typo'd "hoeraszz") / uid HMp1Qz / 26SM SAT Adv E [English only] (single class, Ascent). Symptom: 93% shows fail, TA thought the cutoff became 95% after a class promotion.
- **Diagnosis — where the 95% comes from:** NOT a real cutoff. Adv E's `passThreshold` is **92** (server uses it → his attempts are all `passed=true`, he's advancing CSD=9→Day 10). The **client** new-word gate uses a SEPARATE field, `newWordRetakeThreshold`, sourced at `studyService.js:267` (`assignment.newWordRetakeThreshold || DEFAULT_RETAKE_THRESHOLD`=**0.95**). **Class assignments never stored `newWordRetakeThreshold`**, so it fell back to 0.95 → `DailySessionFlow.jsx:1316` + `TypedTest.jsx:291`/results screen fail 92–94% scorers. (Code already comments this at studyService.js:1282.) Same root as CS-2026-07-03 (김나연); dual-class was a red herring — 김호형 is single-class. **No data fix for the student** (he's passing server-side).
- **Action (David: "do this for all classes, 0.92"):** wrote `newWordRetakeThreshold = passThreshold/100` onto **every class assignment** — **39 classes / 61 assignments**, 0 left missing. **Per-assignment, NOT a flat 0.92** (David said 0.92 but the cohort isn't uniform: distribution = 92×47, 90×10, plus 74/93/95/1). Forcing 0.92 on the 90- and 74-threshold classes (e.g. Inter B2 Ascent=74, Inter B4=90) would have re-created the exact server(<92)/client(0.92) mismatch, so each got its own: 92→0.92, 90→0.9, 74→0.74, 93→0.93. Verified (Adv E 0.92, Inter B2 Ascent 0.74, Inter B4 0.9, Final B 0.92).
- **Note:** takes effect on the **next session build** (student reload / next test) — the value is read when the session config is created, so anyone mid-session with a 0.95-baked sessionConfig should reload to pick it up. Durable code fix still open → NEED_TO_FIX #5.

<!-- Add new events above this line, newest first: CS-YYYY-MM-DD — <title> -->

## CS-2026-07-04 — 박혜린 (helenepark2010@gmail.com) "92% 안 됐는데 pass" → B2 Ascent 74%-threshold outlier fixed
- **Report (David):** SAT Inter B3 student 박혜린 — a sub-92% word test showed **pass**; teacher suspected a bug.
- **Diagnosis (read-only):** NOT a per-student bug. 박혜린 (uid KCTcG3bJ…) is actually in **26SM SAT Inter B2** (CKDTCxTT), not B3 (not in B3 roster — likely teacher misremembered). She studies the **Ascent** list (dVliNv0p), currently Day 5. Her passing tests: Day3 90%, **Day4 90% (27/30)**, Day5 97% — all `passed=true`. Correct, because **B2 Ascent's passThreshold was 74** (the "92" the teacher expected is Base Camp's value). Day-4 attempt carries a valid anchor (nwei 319, testId set); progression clean.
- **Cohort investigation (David: "2 and 3" — investigate then fix):** full Ascent threshold distribution = **92×21, 90×3, 74×1, 93×1**. B2's **74 is the sole sub-90 outlier**; mode is 92; **B3 itself is 92**. So 74 is a genuine mis-set, and 92 is the cohort-consistent + teacher-expected target.
- **Action (authorized, option 2):** set B2 Ascent (CKDTCxTT/dVliNv0p) **passThreshold 74→92, newWordRetakeThreshold 0.74→0.92**. Config-only; Base Camp (RmNNkuLP) untouched (92). Backup `dsg-edits/srv_validate/class_backups/CKDTCxTTvscEZAY1DbUN_prethresh.json`. Affects all 33 B2 students going forward; does NOT retroactively un-pass 박혜린's existing 90% (she's already Day 5).
- **OPEN (flagged, not changed):** a 90%-threshold tier remains — Ascent: 제주 CORE, B4, 제주 TOP (90); Base Camp: 제주 BRIDGE, Bridge TOP, B4, 미주 Bridge, Bridge CORE (90); plus 미주 Inter Ascent=93. These are internally consistent (nwt matches) and may be intentional per-class. Await David's call before touching.

## CS-2026-07-04b — Normalize ALL 26SM thresholds to 92% (cohort-wide, David)
- **Directive (David):** "All 26SM classes/lists threshold should be 92%."
- **Action:** set `passThreshold=92` + `newWordRetakeThreshold=0.92` on every 26SM class assignment where it differed. **8 classes / 18 field-writes** (the 90/0.9 tier + 미주 Inter Ascent 93/0.93): 제주 BRIDGE, Bridge (TOP), 제주 CORE, Inter B4 (both lists), 미주 Inter (Ascent 93→92), 제주 TOP, 미주 Bridge, Bridge (CORE). (B2 Ascent 74→92 already done in CS-2026-07-04.) Per-class backups `dsg-edits/srv_validate/class_backups/<id>_thresh92.json`.
- **Verify:** all **54/54** 26SM assignments now `92/0.92`, distribution `{"92/0.92":54}`, 0 outliers.
- Config-only; no student progress/attempts touched. Does not retroactively re-grade past attempts (server stored `passed` at submit time); applies to future tests.

## CS-2026-07-06 — 구기현 (Adv→Final promotion): class-change Ascent+AObYOowh carry-forward
Same family as CS-2026-06-30 (이주헌) / CS-2026-07-02b (손진욱·박주하): progress is (class,list)-keyed, so promoting to a new class on the same list resets the day-counter; `study_states` are student+list scoped so mastery carries. **4th live instance of this cluster** (the one `LIST_SCOPED_RECON` fixes; Run L flag-off gate certified 2026-07-06, flag still OFF). data-integrity-sweep (미주 Adv|Final) CLEAN before & after.

- **Student:** 구기현 / nodnarb72351@gmail.com / uid `sjrmkEa6zsZbkPCPBzf13etjjhR2`. Enrolled in **26SM 미주 SAT Adv** (`o00h5if6`) + **26SM 미주 SAT Final** (`EcLMtUCc`), both real memberships.
- **Diagnosis (read-only):** did **Ascent (dVliNv0p) Days 1–3 in the Adv class** (Day1 93%, Day2 100%, Day2 review 100%, Day3 93% — real passes; Adv+Ascent csd 2/twi 240, session Day-3 review-study passed). Also **AObYOowh Day-1 in Adv** (97%; csd 1/twi 80). Promoted to **Final**, whose Ascent `class_progress` was fresh (csd 0/twi 0) → the reset. 240 Ascent study_states already carry his mastery. TA said "인터반" but his work is in the class named "26SM 미주 SAT Adv." — data unambiguous.
- **Action (David: "Final is canonical, carry Ascent to Day 3" + "carry it over" for AObYOowh; Adv NOT dropped — not authorized):**
  1. `cs/manual-pass … EcLMtUCc dVliNv0p 3 93` → valid Final Ascent Day-3 anchor (nwsi 160/nwei 239/wi 80).
  2. `cs/manual-pass … EcLMtUCc AObYOowh 1 97` → valid Final AObYOowh Day-1 anchor (nwsi 0/nwei 79/wi 80).
  3. Final+Ascent class_progress → csd 2/twi 240; session → Day-3 review-study, newWordsTestPassed true (0.93). Mirrors Adv exactly (resumes Day-3 review, which is genuinely pending — no Day-3 review attempt anywhere).
  4. Final+AObYOowh class_progress CREATED → csd 1/twi 80; session → Day-2 new-words-study. Mirrors Adv.
- **Verified:** Final Ascent reconciles anchor Day-3 (nwei 239 → twi 240, csd 2 since no Day-3 review) → lands on Day-3 review-study. Sweep CLEAN. **Adv class left intact** (David didn't authorize dropping; student remains enrolled in both — flag if the duplicate should be dropped later, per the 이주헌 pattern).

## CS-2026-07-06b — 김재민 (Final A): grader false-negatives on correct Korean ("restating the word" bug recurs)
Grader-ACCURACY issue, NOT a data problem. **Investigate/flag only — no data fix (student already passed).**

- **Student:** 김재민 / gimjaemin960@gmail.com / uid `uhJ41qPBOERagqTjOe9mb26ravr2` (26SM 미주 SAT Final A). TA: wrote textbook Korean meanings, marked wrong.
- **Diagnosis (read-only):** Day-11 new (Ascent) taken 5× (77→89→86→91→**97 PASS** 07-06 06:07) — NOT stuck, day advanced, no fix needed. Reviewed all distinct wrong-marked answers:
  - **CLEAR grader BUGS (correct Korean rejected):** `autobiographical ← 자전적인` and `indifferent ← 무관심한` — both EXACT correct translations; grader reasoning literally *"just restating the word in Korean form — explain what it means."* This is **CS-2026-06-29 B recurring** (the "restating the Korean definition" false-negative that was supposedly deploy-fixed).
  - **Harsh-but-defensible:** `dissonance ← 불혐화음` (typo of 불협화음), `precept ← 교휸` (typo of 교훈), `proliferate ← 널리 퍼진` (right meaning, grader wanted verb form).
  - **Genuinely wrong (grader correct):** culminate←요점, serpentine←골격형태의, dispel←"to express disapproval", decadence←폐지, doctrine←"government paper", slander←"to make a statement", lurid←gibberish.
- **Root cause:** grading Cloud Function PROMPT rejects a correct direct Korean translation as "just restating the word," demanding a paraphrase. Same class as CS-2026-06-29 B; fix incomplete/regressed. **Code fix (grader prompt), owner-deployed — not a CS data write.** Candidate: soften the "restating" heuristic to accept a correct dictionary-equivalent Korean term; also consider typo-tolerance (1-char) and adjective/verb-form leniency.
- **Intended student recourse meanwhile:** the per-answer **challenge** system (challengeStatus field) — student disputes, teacher accepts. Confirm TAs know to use it for these.
- **Open:** (a) breadth — is this hitting other Final/Adv students? (cohort grep of recent false-negatives worth a scan). (b) whether CS-2026-06-29 B's fix regressed or was always partial.

## CS-2026-07-07 — 최도훈 (Inter B4): lost-save on Day-16 new test → corrupted session, stuck → restore
Lost-save family (CS-2026-06-29 incident A / 황정민). Sweep (Inter B4) CLEAN before & after.
- **Student:** 최도훈 / dohoonchoi0405@gmail.com / uid `IRcn5KsbjkdSKwfEl9pzOYUss9U2` / 26SM SAT Inter B4 (`YAkjxuasyhNy03uS1GlG`) / Base Camp (`RmNNkuLPectBlBPiLbAJ`).
- **Diagnosis (read-only):** Day 15 fully complete (new 07-03, review 07-06). **Day-16 new-word test MISSING** (0 Day-16 attempts) though session was in **impossible state** `day16 / phase review-study / newWordsTestPassed=FALSE` — i.e., he passed Day-16 new in-session but the attempt write failed AND the pass flag didn't set → stuck (a Day-16 review with no Day-16 new anchor can't reconcile forward; stayed csd 15). Matches TA report exactly ("passed yesterday but no test in gradebook, only review; today won't advance").
- **Action (David: "restore the pass"):** `cs/manual-pass … RmNNkuLP 16 93` (valid anchor nwsi 1200/nwei 1279; score 93 = representative of his real range — he scored 93 five times; class threshold 92, so 90 wouldn't have written passed=true). Then session → `newWordsTestPassed=true, phase review-study, day 16`; class_progress twi 1200→1280 (csd stays 15, Day-16 review pending). He now resumes the Day-16 review → completes Day 16 → Day 17. Sweep CLEAN.

## CS-2026-07-07b — 조준모 (→Inter A2): 3-class consolidation + Base Camp carry-forward to Day 9 (cross-pace)
Class-change carry-forward family (이주헌 CS-2026-06-30 etc.). Sweep CLEAN before & after. **5th+ instance of the (class,list)-key reset cluster.**
- **Student:** 조준모 / junmo0405@gmail.com / uid `CYgY0clwYqZKWbMjr9cqhhU32HP2`. Was in **3 classes**: Bridge CORE (`wRoGjp2Q`, Base Camp **pace 60**, Day-10 review pending), Inter B3 (`sO7jN85m`, pace 80, Day 7), Inter A2 (`TyBnqbcc`, pace 80, FRESH — target).
- **Diagnosis (read-only):** real Base Camp coverage = **600 words** (words 0–599; 600 study_states + max nwei 599). His furthest was Bridge (10 days × pace 60 = 600). **Cross-pace wrinkle flagged to David:** TA asked "Day 9," but Inter A2 pace is 80 → Day 9 = 640 words, 40 MORE than his 600 → he skips Base Camp words 600–639.
- **Action (David: "do (a) / drop"):** (a) honor Day 9 literally. `cs/manual-pass … TyBnqbcc RmNNkuLP 8 96` (valid Day-8 anchor, pace 80, nwsi 560/nwei 639/wi 80). Inter A2 Base Camp class_progress → csd 8/twi 640; session → **Day 9 new-words-study**. **Dropped Bridge + Inter B3** (arrayRemove studentIds + studentCount-1; deleted enrolledClasses entry + their class_progress/session_states; attempts kept). default focus → Inter A2/Base Camp.
- **Accepted trade-off (a):** twi 640 credits words 600–639 which he hasn't studied (study_states still 600 = words 0–599); he resumes at word 640 (Day 9), skipping 600–639. Minor, per David's call. Mastery (600 study_states) carries. Verified: enrolled ONLY Inter A2, csd 8/twi 640, Day-9 new-words-study, anchor valid. Sweep CLEAN.

## CS-2026-07-08 — 남세이/Sei Nam (Int E→Adv E): Ascent carry-forward to Day 3 (review-pending)
Class-change carry-forward family. Sweep (Adv E|Inter E) CLEAN before & after.
- **Student:** 남세이 / snam2113@gmail.com / uid `8BvZR67fe1QcOloBGi6VkvZLot53`. Int E (`jDDw2GEu`, Base Camp+Ascent) + Adv E (`BY3cZduMAdqTtfCe8U5d`, Ascent+Summit). Ascent pace 80 both.
- **Diagnosis (read-only):** did Ascent Days 1–2 (+Day-2 review) and **Day-3 new (07-08 06:16, 100%)** all in Int E; Day-3 review NOT done (confirmed — session Day-3 review-study, newPass true). Adv E Ascent fresh. 240 Ascent study_states carry. (TA thought he was due Day 2/3; he'd actually just passed Day-3 new — his screenshot was 6 min before that attempt.)
- **Action (David: "former [Day 3] if review not done; keep/drop int per confirmation"):** confirmed review NOT done → Day 3 review-pending + **kept Int E** (drop condition unmet). `cs/manual-pass … BY3cZduM… dVliNv0p 3 100` (valid Day-3 anchor nwsi 160/nwei 239). Created Adv E Ascent class_progress csd 2/twi 240 + session Day-3 review-study/newPass true. Verified. He resumes Day-3 review in Adv E → Day 4. NOTE: Int E Ascent class_progress still stale twi 160 (reconciles to 240 on load) — left as-is; Int E not dropped.

## CS-2026-07-08b — 신예나/Yena Shin (Adv E→Final B): Ascent cross-pace carry-forward to Day 13 (student-reported repeating)
Class-change cross-pace carry-forward (조준모 family). Student directly reported she was re-studying mastered words. Sweep CLEAN before & after.
- **Student:** 신예나 / yennashinbest@gmail.com / uid `RfKCFSHfP2VRpCWM9KtQagJB3QK2`. Adv E (`BY3cZduMAdqTtfCe8U5d`, pace 80) → Final B (`KKQ0x7kivEgRlEVnXrkW`, pace 100).
- **Diagnosis (read-only):** Ascent Days 1–15 @ pace 80 in Adv E = **1200 words (0–1199)**; 1200 Ascent study_states confirm mastery. Final B reset her to Day 1 @ pace 100 → she'd redone Days 1–2 (words 0–199). Clean boundary: 1200 words ÷ pace 100 = exactly Day 12 → **Day 13** start (400 new Ascent words 1200–1599 remain).
- **⚠ CAUTION (self-note):** first --dry used the TRUNCATED classId `KKQ0x7ki` → manual-pass couldn't find her Final-B day1 attempt, defaulted to pace 80 (wrong nwei 959) and targeted a nonexistent class. Caught in --dry. **Always use the FULL classId.** Redid with `KKQ0x7kivEgRlEVnXrkW` → correct pace 100, nwei 1199.
- **Action (David: "Day 13, drop adv e"):** `cs/manual-pass … KKQ0x7ki(full) dVliNv0p 12 97` (pace 100, nwsi 1100/nwei 1199 → twi 1200). Final B Ascent class_progress csd 12/twi 1200, session Day 13 new-words-study. **Dropped Adv E** (unenroll + deleted her Adv E class_progress/session; attempts kept). default → Final B/Ascent. Verified: enrolled only Final B, Day 13 new-words. 1200 study_states carry.

## CS-2026-07-09 — 이서현/이서현G (dual-enroll A3+B3): consolidate to B3, migrate greater Base Camp progress, drop A3
Started as a "15-question test" ticket → turned out to be dual-enrollment redundancy (live example of the class-keyed-progress problem the student-owned model fixes). Sweep CLEAN before & after.
- **Student:** 이서현 / emily1004enfj@gmail.com / uid `fc8sBxnzARfKWDMQevma1J3OjNq2`. Dual-enrolled Inter A3 (`teKHajONWBMe0YJSAeGl`) + Inter B3 (`sO7jN85mL5ZsaBY1BbGm`), both assign Base Camp + Ascent.
- **The "15 questions" (not a bug):** she took Base Camp Day-9 new 4× total — 2× in A3 (06-25: fail 83%, PASS 97%) and 2× in B3 (07-09: fail 90%, then a 15-question RETAKE PASS 100%). The 15 was the retake within B3's Day 9. She was re-studying the SAME Base Camp days in two classes because progress is `{classId}_{listId}`-keyed.
- **A3 Base Camp (greater):** csd 10/twi 800, Day-10 done, on Day 11 (Day-11 new failed). B3 was at Day 9. 880 Base Camp study_states (mastery, student+list) carry.
- **Action (David: "she's b3. migrate greater progress to B3, drop A3"):** `cs/manual-pass … sO7jN85m… RmNNkuLP 10 97` (Day-10 anchor nwsi 720/nwei 799 → twi 800). B3 Base Camp class_progress csd 10/twi 800, session Day 11 new-words-study (mirrors A3). **Dropped A3** (unenroll + deleted A3 class_progress/session; attempts kept). default → B3/Base Camp. Verified: enrolled only B3, csd 10/twi 800. She resumes Day 11 in B3, no more dual-study.

## CS-2026-07-09b — 이지후/justin2jihool (26SM 미주 SAT Inter.): "Grades → no results" is a GRADEBOOK UI BUG, not data (NO fix applied)
Read-only diagnosis only. **No data intervention** — his data is pristine; the cause is a client/query defect (→ NEED_TO_FIX #8).
- **Ticket (David):** Students tab shows Day 8, but clicking **Grades** → *"Your search returned no results"* and Gradebook detail is empty.
- **Student:** 이지후 / justin2jihool@gmail.com / uid `OlQQGaPVWgc6Hq8PTF7R7A6vtmq2`. Class 미주 SAT Inter. `hRgY5rOTdEZ4Ik7q9Dku`, teacher `9OcxdnYCCGZYOrzfs09pUTUoDOR2`, Base Camp csd 7/twi 560 (= Day 8).
- **Data verdict — CLEAN:** enrolled only in the correct class (isStudent), in `studentIds` (40) AND `members` subcollection (displayName "이지후"). **14 attempts, all** `graded:true`, correct `teacherId`+`classId`, `testId` set, `submittedAt` present as **Timestamp** on all 14. The exact gradebook query (`where teacherId== + orderBy submittedAt desc`) **returns all 14** server-side. Name resolves to his own uid (no collision on him).
- **Root cause (query architecture, not data):** `queryTeacherAttempts` (`db.js:1858`) fetches `limit(50)` ordered `submittedAt desc` filtered only by teacherId, then applies the **Name→studentId filter client-side on that 50-row page** (`db.js:1982`). His last attempt is **2026-06-09**; he ranks **17,236/20,029 teacher-wide (page 345)**, **660/753 class-scoped (page 14)**. Page 1 has none of his → post-filter → "no results". "Students" shows Day 8 because it reads `class_progress`, a different source. **General bug** — hits any student inactive ~a month.
- **Action:** none (no data fix warranted). Logged as **NEED_TO_FIX #8** (server-side student-scoped query fix). **Interim TA workaround:** in Gradebook filter by **Class + a Date range covering early June** (or page forward) — his grades are all there.

## CS-2026-07-13 — Cross-class Ascent carry: 3 promoted ADV[한] students reconciled; carry gap root-cause pending

- **Trigger:** teacher reported 2 INT→ADV promoted students (안이연/lisayiyeon, 유혜준/yuhyejun37) seeing Ascent "Day 1" in the new **26SM 미주 SAT Adv. [한국어 혼용]** class (`OBtxUKiB`) — asked whether progress should carry or they should restart.
- **Answer:** carry-over IS correct/intended. Reconciliation anchor is STUDENT+LIST scoped across all classes (`db.js:3250` `getMostRecentPassedNewTest` — `where studentId + listId`, no classId filter). Both old + ADV classes share the same Ascent list `dVliNv0p`, so progress should carry. Students should NOT restart from Day 1.
- **Read-only sweep** (`scripts/cs/sweep-ascent-carry.mjs`, NEW): flagged 11 ADV-class students whose Ascent doc trailed their earned anchor — but VALIDATION found most are FALSE POSITIVES: 10 are actively progressing in a **Final** class (legit moves; carried correctly — verified 6 Final-movers + Sarah Sung's ADV[한] carry) or caught mid-day (홍승연). Only **3 genuine carry-misses**, all in the teacher's ADV[한] class: 안이연 (started, stuck at 0), 유혜준 (not started), Lucy Son/luckyjiu1004 (re-doing at day4/twi320 despite earned day11/twi880).
- **Root cause (partial):** NOT a missing index (composite index #13 `studentId,listId,sessionType,passed,newWordEndIndex,submittedAt` present) and NOT the anchor query (returns correct cross-class anchors, valid types). The cross-class carry APPLIES on most loads (홍승연 Inter→Adv, 6 Final-movers) but INTERMITTENTLY MISSED for these 3 (Lucy is the clearest: active in ADV[한] for 4 days, anchor 879 in Inter[한], doc stuck at ADV[한]-native 320 — reconciliation ran but didn't apply the cross-class anchor). Looks like a first-entry/transient reconciliation-apply gap. **Code root-cause investigation IN PROGRESS (David authorized).**
- **Fix (write, authorized by David):** `scripts/cs/reconcile-ascent-carry.mjs` (NEW, has `--dry`) wrote derived csd/twi (from each student's EXISTING valid Inter-class anchor — NO fabricated attempt) into their ADV[한] Ascent `class_progress` + cleared the stale ADV[한] `session_state`. Results: 안이연 csd=5/twi=420 (caught her live day-6 mid-write), 유혜준 csd=10/twi=800, Lucy csd=11/twi=880. `data-integrity-sweep "미주 SAT Adv"` = 0 findings CLEAN before & after. Only ADV[한] docs touched; no active Inter/Final sessions disturbed (non-demoting recon self-corrects if they study more).
- **No action** for the 8 Final-active (legit moves) or 구기현/Sarah (self-healing mid-day lag). Teacher note: 안이연 is still actively studying in her OLD Inter class today — she should switch her focus to ADV[한] (now shows correct progress).

## CS-2026-07-13b — 김동현 (Adv B2) moved to SUMMIT (finished Ascent, frozen on #11 list-end wall)
- **Student:** 김동현 / kimdongdongsuper1@gmail.com / uid 0BhMnVjxmG / class qIrJTbwlDOnCKLaTYoz4 (26SM SAT Adv B2).
- **Symptom:** review test blocked "이 날을 완료하려면 먼저 새 단어 시험을 통과해야 합니다 (Day not complete — pass the new-word test first)" (screenshot). Diagnosis: he FINISHED Ascent (`dVliNv0p`) — csd=20, twi=1600/1600 — over 20 legit study-days (2026-06-15→07-10, some catch-up sessions e.g. 3 days on 7/01); now frozen on day 21, a review-only/list-end day where the Day-2+ completion gate demands a new-word test that can't exist at list-end. **This is NEED_TO_FIX #11 live** (the review-only-day deadlock; fix built this cycle but LOCAL-ONLY, not deployed). Confirmed: a day-21 review attempt was written today (7/13, score 7, passed) but csd stayed frozen at 20 — textbook #11 (review recorded, completion blocked).
- **Action (David: "move him to SUMMIT"):** `scripts/cs/move-kimdonghyun-to-summit.mjs --commit` set `settings.primaryFocusListId=AObYOowh (SUMMIT)` + `primaryFocusClassId` (SUMMIT already assigned to Adv B2). Config-only — Ascent progress (1600/finished) left intact. He starts SUMMIT fresh at Day 1, sidestepping the #11 Ascent wall. data-integrity-sweep "SAT Adv B2" CLEAN. NOTE: if he ever switches back to Ascent he'll re-hit #11 until the fix deploys.

## CS-2026-07-13c — More class-move/#11 stuck students + a 26SM #11 scan (169 at the list-end wall)
- **Kaila Chung** (kailachung2008, 미주 SAT Final): cross-class csd UNDERCOUNT — Final Ascent reconciled to csd=2 while Day 3 was complete (d3 new+review exist in her old Adv class), because the position-range review-pairing failed cross-class → "Day 3 complete" phantom loop, couldn't reach Day 4. FIX: set Final Ascent csd=3 (non-demoting → sticks). `scripts/cs/fix-kaila-junseo.mjs`.
- **김준서 Junseo** (junseogim728, SAT Inter B3): **#11 THROTTLE variant** (NOT list-end, NOT cross-class). His last-3 review scores [0.27,0.10,0.40] avg 0.26 → LIVE intervention=1.00 → day-5 allocation.newWords=round(80×0)=0 → review-only day → completion gate blocked his review ("already done"/won't submit). FIX (manual application of the undeployed #11 fix): completed his frozen day-5 review-only (csd 4→5) with his best real day-5 review (0.70) → interv→0.78 → day 6 gets ~18 new words. TEMPORARY — he stays review-heavy until his reviews improve. **This created a `reviewNoNewPass` sweep finding (day 5) — BENIGN (a legitimate review-only day); the data-integrity-sweep's reviewNoNewPass check must be updated to recognize review-only days when the #11 fix deploys.**
- **손지성 Logan** (theloganson.09, 미주 SAT Final): NOT a bug — carry working correctly. Did Ascent Day 1 in his Inter class on 2026-06-29 (20 words, nwei 19), then today's Final session correctly continued as Day 2 (words 20-119, pace 100). 120 introduced = 20+100; Day 2 complete → Day 3. He forgot his 6/29 session. (Offered a reset-to-Day-1 if teacher prefers.)
- **★ 26SM #11 SCAN** (`scripts/cs/scan-reviewonly-frozen.mjs`, READ-ONLY): 1029 started students → **169 currently on a review-only day (newWordCount≤0) = at the #11 wall.** Breakdown: **169 LIST-END** (finished their list, twi≥listSize — e.g. Adv A1/B1 students at csd=20 twi=1600/1600), **0 throttle** (throttle #11 is rare — needs 3 consecutive low reviews like Junseo; most students don't). → **The dominant #11 impact is LIST-END: 169 top students who finished their list and now hit the review-only dead-end.** The Phase-1 fix (review-only completion + §5 finished terminal) unfreezes all of them. Strong deploy case. NOTE: deploying will turn every review-only day into a `reviewNoNewPass` sweep hit — update the sweep alongside.

## CS-2026-07-13d/e — Ensure all classes have 3 lists + batch-advance list-end students (before afternoon sessions)
- **Scope reasoning (David):** the 169 at the #11 wall aren't "done" — many just haven't started today's (afternoon) session. Cut to: **active last 7d but NOT last 6h + at the wall = 100** (session still coming). Also: 50 studied in last 6h (mid-session), 20 idle >7d. The "stuck" definition (verified on 6 samples): they FINISHED the whole list (last day new✓+review✓, twi=listSize); the NEXT day has no new words → review-only → #11 gate blocks it. NOT "did new, skipped review."
- **(e) ENSURE ALL 3 LISTS** (`scripts/cs/ensure-all-lists.mjs --commit`): all 32 26SM classes were missing ≥1 list (Adv/Final missing Base Camp; Inter missing Summit; Bridge missing Ascent+Summit). Added the 37 missing assignments with props MIRRORED from each class's existing assignment + **BACKDATED assignedAt** (1 day older than the oldest existing → the newest-assigned default-focus fallback never picks them → NO student bumped, per CS-2026-06-28b). Backups: `dsg-edits/srv_validate/class_backups/<id>_pre-ensure3lists.json` (32). VERIFY: 32/32 now have all 3 lists.
- **(d) BATCH-ADVANCE** (`scripts/cs/batch-advance-listend.mjs --commit`): advanced **87** target list-end students to their NEXT list (58→Ascent, 29→Summit) by setting `settings.primaryFocus{ListId,ClassId}` — forward-progression (highest-finished + 1: Base→Ascent→Summit; NOT "first unfinished", which after (e) would've sent Ascent-finishers back to Base Camp). Config-only; finished progress untouched; explicit focus also pins them against the fallback. **4 stranded = Summit-finishers** (see below) → deploy only.
- **SUMMIT FINISHERS (5, done all lists — need the deploy/§5 terminal):** 함지민 (jhamsters9, 미주 Final), Soul Kim (soulkim0805, Adv A2), 유찬 (yuchanchon, Adv[한]), Gaon Lee (gaonlee0909, Final A), Young Cho (choyoung8767, Final A).
- **STILL OPEN:** the 50 mid-session + ~116 dormant/others + the 5 Summit-finishers all still need the #11 fix DEPLOYED (batch-advance was only the urgent target group). Root cause = #11 (built, not deployed). Also: deploying will make every review-only day a `reviewNoNewPass` sweep hit → update the sweep then.

## CS-2026-07-13f — Bridge TOP "단어시험 오류" = #11 list-end (3 students advanced) + 허은서 not-#11
- **Report:** teacher (26SM SAT TA 93) — 브릿지 탑반 한예진/최다온/허은서 word test keeps erroring; login OK, test won't take.
- **Diagnosis (screenshot + data):** the screenshot is 최다온 on "Review Test — Day 21" getting **"이 날을 완료하려면 먼저 새 단어 시험을 통과해야 합니다 (Day not complete — pass the new-word test first)"** = **#11 list-end**. 최다온 (Daon Choi, 53202853@students.isqchina.com) + 한예진 (=Annette Han, annette.han@pcakorea.org) both finished Base Camp (1200/1200) and are on the Day-21 review-only wall. Their dashboard shows Ascent/Summit (from CS-2026-07-13e) but they keep clicking Start Session on the finished Base Camp card → wall. **NOT caused by the ensure-all-lists change** (their finished state predates it; added lists were backdated).
- **허은서 (eheo5115@gmail.com): NOT #11** — she's at 840/1200 (day 14), did day-14 new (passed 100), mid-day-14 review; the gate would let her through. No data issue. Likely transient/grading error or mis-group → reload + capture console error if it persists. (Her review scores are very low, 17%, but that doesn't block her.)
- **Fix:** advanced 최다온 + 한예진(Annette Han) + 최우성(ryanking5719, unreported but same #11 wall, active today) → `primaryFocus=Ascent` + cleared their stale Base Camp review-only session. (Yena Choi already advanced in the earlier batch.) Bridge TOP had 4-5 Base Camp finishers at the wall — this is the recurring #11 pattern; every finisher hits it until the fix deploys.

## CS-2026-07-13g — Created (LAP2) Ascent+Summit lists (non-destructive "start over" for #11 finishers); assign to adv/fin/top PENDING
- **Context:** the #11 list-end wall (CS-07-13c: 169 finishers). Batch-advance (CS-07-13d) drained the Base→Ascent/Ascent→Summit finishers, but the **Ascent+Summit "finished everything" finishers have nowhere to advance** (5 named Summit-finishers + more). David's chosen fix: a **non-destructive "start over" via duplicate lists**, NOT a destructive progress reset.
- **Write (David-authorized), READ-ONLY-verified first:** `scripts/cs/create-lap2-lists.mjs --commit` created two lists owned by veterans@ (9OcxdnYC, same as originals; private): **`(LAP2) 26SM VZIP 3K (Ascent, 1600)` = `7ybGZUS7S9CjjVMyK1hH`** (1600/1600 words verified) and **`(LAP2) 26SM VZIP 3K (Summit, 800)` = `alo8NR5sksoQ1eYOE9X6`** (800/800 verified). Verbatim word copies (`clonedFrom`/`lapCopy`/`lapOf` provenance). **Additive only — no existing list, student, or class mutated.**
- **DONE (David: "go"):** `scripts/cs/assign-lap2-lists.mjs --commit` assigned both LAP2 lists to **14** 26SM **adv/fin/top** classes (8 Adv + 3 Final + 3 TOP — **Bridge classes excluded per David**, incl. the (TOP)-named `26SM SAT Bridge (TOP)` `JFCtimk2`), mirroring each class's existing Ascent/Summit props with a **backdated `assignedAt` (2026-05-28)** so no student's focus auto-switches; **14 per-class backups** → `scripts/cs/lap2_class_backups/`. Verified: idempotent re-dry = all 14 ALREADY; the 3 finisher classes (`EcLMtUCc`/`OBtxUKiB`/`qeCkS6Bp`) each carry both LAP2 lists.
- **NEXT (separate, pending David):** point the 4 finished-both students' `settings.primaryFocus{List,Class}Id` at `(LAP2) Ascent` (`7ybGZUS7…`) so they start over on the fresh list — the non-destructive alternative to a reset. Candidates (verified finished-both): 함지민 (`EcLMtUCc`), 유찬 (`OBtxUKiB`), Gaon Lee + Young Cho (`qeCkS6Bp`).
- **Related:** the finished-Ascent+Summit reset/LAP2 candidate list is being produced read-only → `audit/deepfix/task1/firebase/PREEMPTIVE_FIXES_2026-07-13.md`.

## CS-2026-07-14 — 정지수 (Adv A2) #11 THROTTLE deadlock unstuck + cohort scan (throttle = her only)
- **Student:** 정지수 / jisusophia@gmail.com / uid `eGBgVXlIekahvOS98nUHNOOaFrs1` / 26SM SAT Adv A2 / Ascent (dVliNv0p, pace 80). data-integrity-sweep "Adv A2" CLEAN before & after (4 pre-existing reviewNoNewPass list-end day-21/11 findings, unchanged).
- **Report (TA→David):** "vocaboost 화면이 안 넘어감; new word test 이미 패스로 뜨는데 review test submit이 안 된다." = the exact #11 review-only deadlock I flagged the day before.
- **Diagnosis (read-only):** csd=5, twi=400/1600 (NOT list-end). Last-3 review scores [0.23,0.17,0.23] → interventionLevel→1.0 → Day-6 newWordCount=round(80×0)=0 → **Day 6 is review-only** → the Day-2+ completion gate ("pass the new-word test first") blocks a review-only day → csd frozen. She re-took **Day-6 review 10×** (best **97%**, last 20 min before fix), never advancing. **#11 THROTTLE variant** — same family as 김준서 Junseo (CS-2026-07-13c).
- **Fix (write, authorized by David "yes, apply"; `scripts/cs/fix-jisusu-throttle.mjs --commit`):** manual application of the undeployed #11 fix — COMPLETED her frozen Day-6 review-only using her **best real Day-6 review (0.97)**: csd 5→6, appended `recentSessions` {day6, reviewScore 0.97, wordsIntroduced 0}, interventionLevel recomputed **1.0→0.65** (last-3 now [0.17,0.23,0.97]), cleared `session_state`. twi stays 400 (review-only introduces no new words); the passed-new Day-5 anchor untouched (no fabricated attempt). **Result: Day 7 now allocates ~28 new words** → real exit from the wall, not a one-day nudge. Verified: csd=6, interv=0.65, session rebuilt.
- **★ Cohort scan (`scripts/cs/scan-throttle-stuck.mjs`, NEW, READ-ONLY):** swept all 26SM for the same THROTTLE condition (interv→0 newWordCount, wordsRemaining>0, ≥2 frozen-day reviews + phase review-study). **정지수 was the ONLY throttle-stuck student** (0 others, 0 borderline) — throttle needs 3 consecutive low reviews, genuinely rare (matches the 7/13 finding of ~0 throttle).
- **Separate — LIST-END #11 (NOT this fix):** the scan also counted **47** at the *list-end* review-only wall (finished the list; identical student symptom but the remedy is next-list advance, CS-2026-07-13d — NOT the throttle csd-bump). Triage: **25 already pinned to a next list** (handled), 1 actively at the wall now (정윤서/yunseojeoung1), 11 idle, 35 active this week. Left for David's call on re-running batch-advance-listend (root fix = the pending #11 deploy).
- **⚠ Caveat:** 정지수's retention is genuinely low (~20% reviews); Day 7 unblocks her but she may re-hit throttle until reviews improve or #11 deploys — flagged for teacher attention.

## CS-2026-07-14b — 김우주 (Bridge CORE) "Practice again→dashboard" + off-by-one csd undercount sweep (4 fixed)
- **Trigger:** 김우주 / rkim2090809@gmail.com (26SM SAT Bridge CORE, Base Camp pace 60) — "Practice again 눌러도 대시보드로 돌아감, 새로고침·와이파이 정상." David: "yes, fix. then sweep."
- **Diagnosis:** completed Day 6 (d6 new 100% + d6 review 23% passed, twi 360=6×60) but **csd frozen at 5** → app still shows Day 6, sees it done → bounces to dashboard, csd never ticks to 6 → can't reach Day 7. Same **off-by-one review-pairing gap** as 박혜린 (cross-class) and 문서윤/김건우 (same-class). NOT throttle, NOT list-end. Under LIST_SCOPED_RECON=true the twi/anchor carry correctly but csd lands on anchorDay-1.
- **Fix (write, authorized; `scripts/cs/fix-csd-undercount.mjs <email> --commit`, NEW):** set currentStudyDay = twi/pace (POSITION-space, pace-aware), non-demoting (sticks), clear active session_state. Guards (all required): cleanBoundary (twi%pace==0), undercount (expectedCsd>csd), notListEnd, twiCarried (twi==anchorNwei+1), gap<=2, dayComplete (anchor day's review passed). 김우주 csd 5→6.
- **★ Family sweep (`scripts/cs/sweep-crossclass-undercount.mjs`):** flagged **32** "credited-less-than-earned," but the sweep uses studyDay so it **over-flags cross-pace** students (Adv pace80→Final pace100 relabels a "day"). The corrector's **pace-aware** guard (expectedCsd=twi/pace) is authoritative: only **4** are genuine clean off-by-ones → all fixed: **김우주** (5→6), **김건우/geonwoo20100607** Bridge TOP (5→6), **문서윤/syunmm9** Adv A2 (5→6), **박혜린/helenepark2010** Adv B1 (4→5, her original 7/13 ticket, finally applied).
- **The other 28 (NOT fixed — not this bug):** ~11 show expectedCsd==csd = **NOT actually undercounted** (studyDay artifact only; their csd is correct — e.g. 신예나, 강현준, yuposoccer, 이진후); ~11 are **twiCarried=false = class-move carry-not-applied** (need per-student canonical-class decisions; some already handled — 박주하 CS-2026-07-02b, Sei Nam CS-2026-07-08); ~4 mid-day/cross-pace (cleanBoundary=false); 1 list-end (정윤서). None auto-fixed.
- **Verify:** all 4 now csd==expectedCsd; data-integrity-sweep Adv B1 CLEAN, Adv A2/Bridge TOP/Bridge CORE csdImplausible/invalidAnchor/twiOverList = 0 (residual reviewNoNewPass = pre-existing list-end day-21). Backups: `scripts/cs/backups_csd/<uid>.json`.
- **⚠ Recurrence:** CS bypass of a code bug (reconciliation review-pairing off-by-one). Each may re-freeze on completing their NEXT day → re-run the sweep/corrector. Root fix = code (NEED_TO_FIX).

## CS-2026-07-14c — 송유주 (Adv A2) list-end wall + batch-advance list-end finishers (52 advanced)
- **Trigger:** 송유주 / yuju021724@gmail.com (Adv A2) — "단어시험 다 응시했는데 제출이 안 됨." Diagnosis: finished Ascent (twi 1600/1600, csd=20), on Day-21 review-only wall; primaryFocus still on the finished Ascent → the #11 list-end deadlock (completion gate demands a new-word-test pass that can't exist at list-end). Grading/save logs clean. Same as CS-2026-07-13b (김동현). David: "batch advance go."
- **Fix (write, authorized; `scripts/cs/batch-advance-listend-0714.mjs --commit`, NEW):** config-only — set `settings.primaryFocus{ListId,ClassId}` to the NEXT assigned list (forward progression Base→Ascent→Summit; furthest-finished+1). Refinement of CS-2026-07-13d: window = active last **7d** (dropped the 6h exclusion → catches students stuck at the wall right now, e.g. 정윤서), and writes **ONLY real changes** (current focus ≠ next → skips already-pinned no-ops).
- **Result: 52 advanced** (28→Ascent from finished BaseCamp, 24→Summit from finished Ascent); 108 already-pinned no-ops skipped; **6 stranded** (Summit/all-lists done — pETMPXmY, 4muHZXgX, FR3TfAmH, mX0tjLDm, TGJNuQ1v, A2a7wgrL — need the #11 deploy / finished-terminal). Finished-list progress untouched. Verified: 송유주 & 정윤서 → Summit; re-run dry = 0 real advances (idempotent).
- **Note:** config-only (no progress/attempts touched) → data-integrity signatures unaffected. Root fix still the pending #11 deploy (this only moves finishers forward; a genuinely all-lists-done student still hits the terminal wall until deploy).

## CS-2026-07-15 — #11 THROTTLE WAVE: 16 stuck students reset (unstick + intervention recalibration)
- **Context:** a wave of tickets (유아현·임서진 INT B1, 하연우·roy Bridge CORE, 정이찬 Adv B2, 진현수/werewolfhunter Final B, + 7 Adv B1: 김현우·정유라·제민국·김시연·강소윤·안겸·김나영, + jiw/pjha/yehyun) all the SAME symptom: "new words 건너뛰고 review로 넘어감 / review 다 해도 시험 시작 안 됨 / 단어시험 먼저 봐야 된다." = the **#11 throttle review-only deadlock**. All had last-3 review avg ≤0.30 → `calculateInterventionLevel`→1.0 → newWordCount=0 → review-only day → completion gate blocks → csd frozen, re-taking the same review. `scan-throttle-stuck` jumped **1 (7/14) → 12 actively-stuck + 13 at-risk (7/15)**.
- **David directive:** "for those who are throttled — unstick them AND set intervention such that a ≥50% next review won't throttle them."
- **Fix (write, authorized; `scripts/cs/fix-throttle-reset.mjs <emails> --commit`, NEW):** allocation reads `calculateInterventionLevel(recentSessions)` LIVE (studyService.js:325; <3 valid reviews→0, else last-3 avg). So reset = set `recentSessions` to **3 baseline review entries at 0.50** → interv **0.556** (moderate ~44% throttle, current day re-allocates ~36 new words @80 / 27 @60 / 44 @100 → review-only→normal day, deadlock GONE) + `interventionLevel=0.556` + clear `session_state`. Next real review makes last-3 `[0.5,0.5,R]` → **R≥0.50 keeps avg≥0.50 → not throttled**; R<0.50 re-throttles. NO csd/twi/anchor touched; real attempts/gradebook untouched (recentSessions is the intervention signal only). Backups `scripts/cs/backups_throttle/<uid>.json`.
- **Result: 16 fixed**, actively-stuck throttle **12→0**. Verified interv=0.556 on samples; data-integrity-sweep Adv B1/Final B csdImplausible/invalidAnchor=0 (no corruption). Side effect: overwriting recentSessions resets streak/weekly-words display + shows 3 synthetic 0.5 entries in the teacher's PreviousSessionCell (cosmetic, rebuilds as they study).
- **Follow-up sweep (David: "scan again and fix"):** `scan-throttle-stuck.mjs --emails` (NEW flag) → piped all throttle candidates into `fix-throttle-reset.mjs --commit`. **18 more reset** (a distinct set from the first 16 — 6 in Bridge CORE/wRoGjp2Q, several Inter B2/CKDTCxTT, + qeCkS6Bp/k0j59bXv/etc.). **Post-sweep: throttle candidates = 0** (verified). Total this incident: **34 students reset** (16 + 18). (list-end review-only = 44, separate — batch-advance/#11 deploy.)
- **SYSTEMIC:** this wave = the undeployed #11 fix + the throttle zeroing new words at ≤30% reviews. Band-aiding per-student is unsustainable. Root fixes: (1) **deploy #11** (review-only days completable → no deadlock), (2) reconsider throttle aggressiveness cohort-wide (interv 1.0 = 0 new words is a hard trap for chronically-low-review students). Also note: the reset is a workaround; genuinely low-retention students (many here at 10-20%) still need teacher attention.

## CS-2026-07-15b — TA batch: throttle wave cont'd (20 reset) + 김유림 승반 carry + unresolved flags
- **Throttle #11 (same as CS-2026-07-15), reset via `fix-throttle-reset.mjs`:** TA-named 홍윤성(INT A1)·하승현(Final A "frozen screen")·허은서·송예서·김예린·김하엘(Bridge TOP)·조준모·윤규보(INT A2) + 이지현(Final A; TA email typo — actual **tigeregina00**@gmail.com) + 11 scan-caught regen incl. 유지민/kayliejyoo(INT A3). Post-sweep throttle=0. Running incident total ~57.
- **김유림 / kyrpp12@gmail.com (승반 Adv B2→Adv A2)** — NOT throttle. Real Ascent progress Day 12 (twi 960) was under **Inter A2** (TyBnqbcc) → Adv A2 (canonical per David/chat) showed no progress ("진도 확인 안 됨"). Fix (`fix-kimyurim-carry.mjs --commit`): replicated her OWN Inter A2 Ascent class_progress into **Adv A2** (Nys1FfB9) — csd 12/twi 960, cleared Adv A2 session, pinned primaryFocus→Adv A2/Ascent. Copies her own doc (no cross-student field leak). Resumes Day 13 in Adv A2. Sweep "Adv A2" csdImplausible/invalidAnchor=0 before & after. **Inter A2 + Adv B2 left enrolled** (drop not explicitly authorized; recommend dropping to avoid dual-class threshold-resolution issues — flag).
- **Unresolved (can't fix from data):** 송유주/yuju021724 — data HEALTHY (Summit Day 3, gets new words, not stuck); "site 작동 안 함"+photo = client-side, need screenshot/console. 이하람(INT A1) — no email given, no exact displayName match; if throttle she's already swept, else need email/exact name.

## CS-2026-07-15c — Blanket challenge-token reset to 5 (26SM)
- **Directive (David):** reset everyone's challenge tokens to 5 (TAs reported students asking when challenges refill; #14 exhaustion).
- **Mechanic:** tokens = `max(0, 5 − activeRejections)` where active = `challenges.history` entries `status:'rejected'` with `replenishAt` in the future (30-day auto-replenish). Stored as an unsigned array on `users/{uid}.challenges.history`; both client (db.js:188) and server callable (functions/index.js:624) recompute from it (no HMAC) → an Admin write is honored end-to-end.
- **Action (`scripts/cs/reset-challenge-tokens.mjs 26SM --commit`, NEW):** backdated `replenishAt` to the past on every active rejected entry (stamped `tokenResetAt`/`tokenResetNote`; history PRESERVED for audit — no fabricated/cleared entries). **69 of 824** 26SM students were below 5 (383 had any challenge history); 4 fully exhausted at 0 (김호형/김재민/이은유/이서현G — 5–6 rejections, the grader-false-negative #14 victims). All → 5. Verified: 0 below 5 post-reset.
- **Note:** removes the anti-spam rate-limit for those 69 (justified for the grader-false-negative drains). Root fix still #14 (grader calibration + teacher override). Re-runnable per-student by email as TAs report (`reset-challenge-tokens.mjs <email> --commit`).

## CS-2026-07-16 — Bridge students: throttle-reset side-effects surface post-#11-deploy (2 off-by-one + 1 phantom-record)
- **Context:** #11 fix now deployed. Three Bridge tickets, all students I throttle-reset on 07-15 — the resets are now generating side-effect tickets as students move past them.
- **이찬희 (chanheewhitetiger10, Bridge CORE) "start→dashboard" & 서혜빈 (hyebinseo3383, Bridge CORE) "Day 9 안뜬다":** OFF-BY-ONE. Both did their current day's new AND review but in REVERSE order (review first — from when the day was review-only pre-reset — then new after the reset gave them words), so day-completion (fires on review submit) never ran → csd stuck one short. 이찬희 completed d18/csd=17; 서혜빈 completed d8/csd=7. twi non-clean (partial 27-word reset days) so fix-csd-undercount's cleanBoundary guard skips them. Fix (`scripts/cs/fix-csd-to-completed.mjs --commit`, NEW): set csd = max day with passed-new AND passed-review (non-demoting) + clear session. 이찬희→18, 서혜빈→8. Verified.
- **김예린 (yxrin10, Bridge TOP) "test recorded but no gradebook entry":** NOT off-by-one (csd=17 correct, on day 18, 27 new words available). Her `recentSessions` are the SYNTHETIC [d15/16/17 @0.5] entries my reset wrote → they render as phantom "sessions" with no matching attempt = "봤다고 기록됐는데 기록 없음." Cosmetic; self-heals as real sessions push them out of the last-10 window. She should reload + do her day-18 new-word test.
- **★ SYSTEMIC:** the ~57 throttle-reset band-aids are now OBSOLETE and HARMFUL post-deploy — (a) synthetic recentSessions = phantom records, (b) partial 27-word reset days + review-before-new = off-by-one completion failures. STRATEGIC DECISION PENDING (David): reconcile all reset students to reality (rebuild recentSessions from real attempts → honest display + real throttle, now completable via the deployed fix — but low-retention students get held on review, no new words) vs leave the resets (keep new words, keep getting per-student glitch tickets).

## CS-2026-07-16b — Reconciled all 58 throttle-reset students to reality (post-#11-deploy, David: option a)
- **Action (`scripts/cs/reconcile-reset-students.mjs --commit`, NEW):** for every 26SM class_progress carrying the 07-15 throttle-reset `csFixNote` (58 students), rebuilt `recentSessions` from REAL attempts (per-day new/review summaries, last 10), recomputed `interventionLevel` from REAL reviews, set csd non-demoting = max(csd, genuine-complete-day), cleared `csFixNote` + session_state, stamped `reconcileNote`. Backups: `scripts/cs/backups_reconcile/<uid>.json`.
- **Effects:** (a) SYNTHETIC [0.5×3] recentSessions → real → **phantom-record class of tickets (김예린) eliminated**; (b) **16 latent off-by-ones fixed** (non-demoting csd+1 — same review-before-new pattern as CS-2026-07-16 이찬희/서혜빈, caught before they ticketed); (c) intervention now reflects reality — **~40 of 58 recompute to interv 1.0** (genuinely low-retention). Those will throttle to review-only days, but the DEPLOYED #11 fix makes those **completable** → they progress + recover as reviews improve (not frozen).
- **⚠ Implication for TAs:** low-retention students (many of the 58, e.g. 김예린/yxrin10 at ~10% reviews) will now correctly be **held on review with NO new words** — the fix working as designed, not a bug. TAs may report "새 단어가 안 떠요" for them; the correct answer is "reviews too low; keep doing review, new words return above ~30%." A per-student new-words override remains available only on explicit request.
- **Net:** the 07-15 reset band-aids are fully unwound; state is now honest and the deployed fix owns throttling/recovery. `scan-throttle-risk.mjs` is the ongoing monitor.

## CS-2026-07-16c — "Empty-review throttle" hypothesis INVESTIGATED and DISPROVED (no write)
- **Trigger:** Adv B1 TA (강우현) reported 이아연 (Ifd4xPmoIEOJuPi3N1cwPaHeHZ12, Ascent list dVliNv0p) "jumping" Day 8→9→10→11 review with 0%s, no new words. Hypothesized a NEW bug: empty review slices (`computeUnmasteredSegmentIds` returning 0 words) recorded as `reviewScore=0`, fabricating failures that pin intervention. Drafted a mop-up to null those `0`s across ~23 "affected" 26SM students.
- **DISPROVED** (code-fidelity agent + direct raw-attempt check): `answers.length===0` is NOT an empty-served test — it's a real 30-question test the student **SKIPPED** (`totalQuestions:30, skipped:30, answers:[], score:0`, on REAL segments — 이아연 day9 seg=[405..464], day10 [524..583]). TRUE empty-served tests (`totalQuestions:0`) record `score:100, passed:true` → recentSessions `reviewScore:null`, NEVER 0 (app already correct). So the `0`s are **genuine review skips**; the throttle is working as designed (same class as CS-2026-07-16b). The mop-up would have erased real skip-signal + UNDONE the 16b reconciliation (e.g. WVUUNcbC skipped every review day 3–17 → would have wrongly gotten full new words).
- **Outcome:** NO write. Invalid scripts (`fix-empty-review-throttle.mjs`, `empty-review-sweep.mjs`) deleted; plan `docs/plans/MOP_UP_empty_review_throttle_2026-07-16.md` marked WITHDRAWN. 이아연 = partial review-skipper (scores 73–77% when she DOES take them; skips others) → correct guidance is CS-16b's: stop skipping in-app reviews; reviews above ~30% return new words. TA manual test is a fine stopgap.
- **Real deferred question (NOT a data fix):** the deployed #11 fix advances csd even when a review-only day's review is **skipped** (score 0) — that is what runs the day counter up (8→11) while the student engages nothing. Whether a *skipped* review-only day should count as completing/advancing the day is a genuine product/code question for the deferred root work — distinct from the (non-existent) empty-slice bug.
- **Lesson:** `reviewScore=0` in recentSessions ≠ empty test. Discriminate empty-served (`totalQuestions===0`, records null/100) from skipped (`skipped===totalQuestions`, records real 0) BEFORE treating any `0` as fabricated.

## CS-2026-07-17 — Cohort throttle relief + runaway-csd reconcile (144 students, David-authorized band-aid)
- **Trigger:** flood of "day won't advance / 새 단어 안 뜸 / review→dashboard" tickets (김준형, 이서현, 박주하, 조연우, 천예슬, 이아연, + wave). Root cause (verified by 4 Fable agents + code + live data): students **manually submit empty reviews** ("Submit 0/30" is always enabled — a **regression**: the empty-submit guard `db.js:1246` was dropped when SERVER_ATTEMPT_WRITE flipped 2026-06-22/23, and `submitVocabAttempt` (functions/index.js:495) never replicated it; TypedTest still blocks it, MCQ doesn't). Empty review **passes** (`MCQTest.jsx:537`/`functions/index.js:403`) → completes the day → **csd+1** each time (`progressService.js:600`) → self-accelerating **runaway** on throttle days (0 new words). Behavioral + **class-concentrated** (Adv B1 26% full-skip vs 0% in 10 classes — a build bug can't do that), so NOT a render/generation bug. Separate small defects: 3 `score>100` docs (MCQ recovery-merge overflow, unclamped server score), the re-entry "review doesn't render → dashboard" dead-end (`handleReEntryRetake` empty reviewQueue). **NOTE: the earlier "strip empty-test 0-scores" mop-up is DISPROVED/HARMFUL — there are no empty tests; the 0s are real skips (see CS-2026-07-16c). Do NOT run it.**
- **David directive:** "move all students off throttle for the day" (heavily-stuck = anyone seeing fewer words or blocked), "and reconcile csd according to twi for these students."
- **Action (`scripts/cs/throttle-relief-cohort.mjs --commit`, NEW; David-authorized):** for every throttled 26SM doc (interv>0, words remaining): (a) NULL reviewScores in recentSessions → interv recomputes to 0 → full new words (NO synthetic scores); (b) interventionLevel=0, stats recomputed, session_state cleared; (c) csd reconciled DOWN to last passed-new day (twi anchor) ONLY when inflated (runaway) — non-stranding, leaves legit-throttled csd alone. **Result: 144 off throttle, 44 runaway-csd pulled back (e.g. 박주하 21→15, wisdomram11 17→8, hjimin 20→12), 3 list-end skipped.** Backups: `scripts/cs/backups_throttle_relief/`. Sweep 26SM identical before/after (0 corruption, csdImplausible=0). Spot-verified: recomputed interv=0.00 on 박주하/wisdomram11/천예슬.
- **⚠ TEMPORARY BRIDGE — this REWARDS skipping and re-throttles after ~3 real reviews.** It is a band-aid to stop today's flood while the durable forced-pathway CODE fix is built (`docs/plans/FORCED_PATHWAY_FIX_PLAN_2026-07-16.md`): restore the empty-submit guard (server + MCQ), review-required-to-advance, hold csd on throttle review-only days, don't count 0-answer reviews into intervention, clamp score≤100, fix the re-entry dead-end. Until deployed, students CAN re-skip-race; TAs: tell students to actually take the review (answer the questions, don't Submit early).
- **Follow-up (NOT done):** 3 list-end students need next-list advance (`batch-advance-listend`); the ~120+ cumulative empty-submitters + Adv B1 class culture is a teacher/pedagogy conversation.

## CS-2026-07-17b — Full reinstatable READ-ONLY backup of ALL 26SM data (pre-migration safety snapshot)
- **Trigger / directive (David):** a one-way progress migration is imminent; "back up all data so they can be reinstated in a worst-case scenario." A prior backup attempt died mid-way on a transient API error — started fresh with a retry-hardened, paged, streaming script.
- **Scripts (NEW, `scripts/cs/`):** `backup-all-26sm.mjs` (READ-ONLY on Firestore — ZERO writes; dumps local JSON) + `restore-from-backup-26sm.mjs` (companion; **dry-run default**, `--commit --dir=<dir>` to write back). Both re-runnable; every backup run makes a fresh timestamped dir → idempotent.
- **Scope backed up** (per `/26SM/i` = 40 classes, 823 distinct students): each student's `users/{uid}` root doc + all 5 subcollections (`class_progress`, `session_states`, `study_states`, `list_progress`, `progress_meta`); every `attempts` doc where `studentId` ∈ cohort; the 26SM `classes/*`; every assigned + progress-referenced `lists/*`. Each collection → its own `[{path,id,data}]` JSON file (one entry/line, stream-parseable). Firestore Timestamps stored losslessly as `{__ts_millis__,__ts_seconds__,__ts_nanoseconds__}` (+ DocumentRef/GeoPoint/Bytes markers) so restore reconstructs them exactly. study_states (896,885 docs) flagged HUGE>100k and paged (limit 1000, orderBy documentId) so memory stays flat.
- **Run result:** dir `scripts/cs/backups_full_26sm_20260717-165840/` (~730 MB). **0 failures. Spot-check PASS** — re-queried 40 classes + 6 random students × 6 collections each; all counts matched live. Counts: classes 40, lists 13, users 823, class_progress 1499, session_states 951, study_states 896885, list_progress 0, progress_meta 0, attempts 25640. (list_progress/progress_meta are legitimately empty across 26SM — anchor lives in class_progress per db.js §5.1.) All 9 files independently full-parsed to their manifest counts; Timestamp serialize→deserialize round-trip verified nanosecond-precise. Restore script `node --check` clean and, per directive, NOT executed. git HEAD at backup: `4b8452a`.
- **REINSTATE (worst case):** dry-run first, then commit:
  `NODE_PATH=/app/node_modules node scripts/cs/restore-from-backup-26sm.mjs --dir=scripts/cs/backups_full_26sm_20260717-165840` (add `--commit` to write back). Overwrites current docs with the snapshot; docs created after the backup are left untouched. Confirm with David before `--commit` against live 26SM.
- **Re-run the backup anytime:** `NODE_PATH=/app/node_modules node scripts/cs/backup-all-26sm.mjs` (fresh timestamped dir; safe — read-only on Firestore).

## CS-2026-07-19 — 3 day-jump tickets (김재경/오윤권/이해섭): pre-PR-3 runaway tail, NOT audit/cutover damage (diagnosis read-only; 1 fix pending David)
- **Tickets (TAs, 07-18 오후 KST):** 김재경/sunnyjaekyung (Bridge TOP) "day 5 차례인데 day 6"; 오윤권/otaewon44 (Inter B1) "day 9 → 풀어서 넘겼더니 13; day 5로"; 이해섭/joeymxlee (INT E) "Day 12여야 하는데 Day 8로 되돌아감".
- **AUDIT-INTERFERENCE CHECK (David's question "have these students' data been changed?"): NO — verified 3 ways.** (1) None of the 3 uids appear in any audit roster (the D3.5 audit wrote ONLY minted `lsr_*` sandbox uids; per-run guard artifacts record writesToNonSandbox=0). (2) No audit marker fields (`csProvenance`/`csSynthetic`/`csFixNote`) on any of their docs. (3) Their last data changes are **2026-07-17 UTC** — a full day BEFORE the audit's first sandbox write (07-18 15:13Z) and before the P4 client cutover (07-18 08:46Z). No CS/audit write touched them.
- **Timeline context:** all three incidents occurred **07-16/17 KST afternoon — BEFORE PR-3's skip-hold went live (07-18 09:16 KST)** — i.e. the tail of the KNOWN pre-fix empty-review runaway family (CS-2026-07-17), reported a day later.
- **오윤권 (uid OWyqVHx…): TEXTBOOK runaway, needs the one data fix.** 8 reviews for days 5→12 submitted in **8 minutes** (07-17 07:16–07:24Z), scores 0/0/0/0/0/10/7/17 — each empty/low review completed a review-only day under the then-live code → csd raced to 12 (session day=13). His GENUINE progress: passed-new days 1–4 only (anchor d4 nwei=319; twi=320 @pace80 = exactly 4 days). **Proposed fix (pending David): reconcile csd 12→4 + clear the stale session → he lands on Day 5 (exactly the TA's ask).** PR-3 (now live) prevents recurrence — his low reviews will HOLD, not runaway; note he'll be review-held until reviews improve (~30%+).
- **이해섭 (uid 5cA8PQD…): NO fix needed — the system already corrected him.** Same runaway on 07-16 (d8→d11 reviews in minutes, scores 17/3/17/90, no new tests) → he *appeared* to reach Day 12. Reconciliation then pulled him back to his real anchor (day 8) — the "went back to Day 8" he reported IS the anchor-based correction working. He then **legitimately re-did d8 and d9 with real new+review passes on 07-17** (d8 new 100/rev 13; d9 new 100/rev 90) → honest position now csd=9 → **Day 10 next**. "Day 12" was the inflated runaway position, not real progress. Recommend: explanation to the TA, no data change.
- **김재경 (uid 3TyFPJw…): NO fix needed — review-only-day mechanics, not corruption.** Her review scores are very low (d2 7%, d3 10%, d4 27%) → day 5 was a THROTTLE review-only day (0 new words). She TOOK the day-5 review on 07-17 (score 30%) → the deployed review-only-completion advanced the day → Day 6 shows. Nothing was skipped content-wise (day 5 had no words by design). Under now-live PR-3 she'll be review-held until her reviews improve. Recommend: TA explanation ("day 5 was a review day and she completed it; she needs better review scores to get new words").
- **NOT a P4-cutover regression signal:** all three trace to pre-cutover events; the post-cutover monitoring (CS-07-18) remains clean.
- **Trigger:** a crash interrupted the D3/P4 client→server cutover; verify the LIVE 26SM cohort is safe on the new server write path (part of the 5-way convergence de-risk) before certifying.
- **READ-ONLY scans (ZERO 26SM writes):**
  - `scripts/cs/data-integrity-sweep.mjs 26SM` → **CLEAN**: `invalidAnchor:0`, all structural signatures 0; `reviewNoNewPass:68` (benign list-end/review-only days, DOWN from the documented 72 baseline). Persisted: `audit/playwright/findings/deepfix_dataintegrity_sweep_26sm_postcutover.txt`.
  - `scripts/cs/scan-syslog-since.mjs` (**NEW**, read-only) → **NO-SPIKE** since 08:46Z: 9 `resolve_list_progress` + 2 `csd_twi_reconciled` (26SM, server-written), ZERO `anchor_rejected`/`csd_anchor_invalid`/`reviewonly_derivation_mismatch`/`day_guard_*`. `deepfix_syslog_sweep_postcutover.json`.
  - `scripts/cs/scan-canonical-writepath.mjs` (**NEW**, read-only) → canonical `users/{uid}/list_progress` **EMPTY (0/0)**; `csd_twi_reconciled` `writtenBy=cloud-function`; all write-path/error signatures 0. `deepfix_canonical_writepath_postcutover.json`.
  - WinClaude GCP Cloud Logging → **zero CF-runtime errors** post-cutover; `completeSession`/`resolveListProgress` invoked error-free. `deepfix_cf_runtime_logcheck_r39.json`.
- **Result:** the live cutover is **data-safe** (no corruption, no error spike, canonical empty). The forced-pathway hold-csd branch was separately **behaviorally CERTIFIED** in the emulator (10/10, `deepfix_p4_behavioral_cert_0ddbb34.json`, pinned `0ddbb34`). **No 26SM writes performed.** Full record: `docs/plans/MASTER_TASK_LIST.md` + `docs/plans/loop/CONVERGENCE_REPORT_v4.md`.
- **Work-queue (data-safety, non-urgent):** the `attempt_day_fallback` emitters (`MCQTest.jsx:612`, `TypedTest.jsx:872`) carry no `userId` → the event is invisible to cohort attribution; add `userId` if this signal is wanted for 26SM monitoring.

## CS-2026-07-19b — 최도훈 (Inter B4): PHANTOM day-16 anchor from a list-end manual-pass → TWI>list; fixed + tooling hardened
Surfaced by the D3.5 audit (choi_a12 clone "FAIL"); David: "list was never trimmed — dig into every CS fix and every previous state and figure it out."
- **Student:** 최도훈 / dohoonchoi0405@gmail.com / uid `IRcn5KsbjkdSKwfEl9pzOYUss9U2` / 26SM SAT Inter B4 (`YAkjxuasyhNy03uS1GlG`) / Base Camp (`RmNNkuLPectBlBPiLbAJ`).
- **Forensic finding (full attempt history read):** Base Camp is genuinely a **15-day / 1200-word list (NOT trimmed)** — his own day-15 new attempt introduced words **1120-1199, the last 80 words** (`words` subcollection count = 1200, indices 0-1199). He completed days 1-15 legitimately (all `ans=30`). **There is no day-16 new test.** His CS-2026-07-07 "lost-save on Day-16 new" was a **misdiagnosis** — it was a list-end case.
- **Root cause:** the CS-2026-07-07 `manual-pass … RmNNkuLP 16 93` computed the word range from `day×pace` (16×80 → nwsi 1200 / **nwei 1279**) **without checking the list length** — writing a `passed:true` anchor for **words 1200-1279 that don't exist**. That anchor is the twi source (`twi = nwei+1`), so it **inflated twi to 1280 on a 1200-word list (TWI>list)**. The old `data-integrity-sweep` MISSED it (it compared twi to the class's *first-assignment* list — Ascent 1600 — not Base Camp; multi-list blind spot).
- **What the deployed system does (proven by the audit clone):** a list-end student's csd advances on the **review** day (15→16) with twi **capped at 1200** — no phantom, no inflation. So the manual-pass was not only unnecessary, it was mildly corrupting.
- **Scope scan (full 26SM, improved sweep):** phantomAnchor = **1 — ONLY 최도훈** (every other manual-pass was mid-list, so its range is within the list). Isolated.
- **FIX (David: "do all 1-4"; `scripts/cs/fix-phantom-anchor.mjs`, backup `dsg-edits/phantom_fix/IRcn5Ksb_RmNNkuLP_commit.json`):** deleted the phantom anchor `..._day16_typed_new_manual`; set class_progress **twi 1280→1200** (the real value = last real anchor nwei 1199+1); **csd left at 16** (his real day-16 review is genuine; non-demoting). Sweep BEFORE: twiOverList=1, phantomAnchor=1. Sweep AFTER: **twiOverList=0, phantomAnchor=0** (corruption cleared). His day-16 review now shows as one **benign `reviewNoNewPass`** — the documented list-end/review-only category (a list-end review legitimately has no new-word pass), NOT corruption.
- **Tooling hardened (code — see change_action_log):** (1) `manual-pass.mjs` now REFUSES a day whose `newWordEndIndex ≥ list word count` (fabricated phantom day). (2) `data-integrity-sweep.mjs` now compares twi + anchors to **each doc's OWN list** (actual `words` count, not the class's first list) and added a `phantomAnchor` check. (`reviewNoNewPass` was left exactly as-is — its documented 68-benign baseline is untouched.)
- **No other 26SM writes.** csd=16/twi=1200 verified durable (no re-inflation, since the phantom anchor that justified 1280 is gone).

## CS-2026-07-19d — Reconciled 5 runaway-inflated stragglers (surfaced by the fixed detector)
Follows CS-2026-07-19c (R2 detector fix). The old `csdImplausible` (`csd>twi+7`, days-vs-words) was structurally blind and reported 0; the corrected anchor-based per-list detector surfaced **5** currently-inflated students the 07-17 mass reconcile had missed (all THROTTLED → dormant, none was minting an anchor).
- **Fix (David-authorized "reconcile"):** `scripts/cs/reconcile-inflated-csd.mjs` — set `currentStudyDay` to the deployed resolver's value (`reviewExists(anchorDay)?anchorDay:anchorDay-1`, foundation.js:943; twi already correct = anchor nwei+1, kept) + cleared the stale `session_states`. Backups: `dsg-edits/inflated_csd_fix/`.
  - kOWbnPCu (Ascent) csd 7→4 · C8P1fmyq (Ascent) 22→15 · erJVsQPW (Base Camp) 18→8 · WwOuwekT (Base Camp) 17→15 · **오윤권/OWyqVHxt (Base Camp) 12→4** (the deferred CS-2026-07-19 ticket, now done).
- **Sweep after:** Inter B1 `csdImplausible=0` (오윤권 cleared); full-26SM re-sweep pending. Each student resumes at their true day; the runaway source (empty-review inflation) is fixed by the live PR-3 hold-csd, so no recurrence. Benign `reviewNoNewPass` leftovers (old runaway review attempts) remain — harmless (no longer inflate csd).

## CS-2026-07-19e — 윤여진 grader false-negatives: reconstructed + replayed against the LIVE prompt → deployed grader is CLEAN for this pattern (read-only; no defect)
David: "populate with the problematic inputs (윤여진) and see if it's fixed … make sure the input is exactly how the app would send it (check code beforehand)."
- **Student:** 베트남-윤여진 / yjy682245@gmail.com / uid `31WgOWbh3tP6bkvxF5m3VA4EpbA2`. One of the "정답과 똑같이 써도 오답" cohort (TA triage 07-13; NEED_TO_FIX #14).
- **Finding (read-only pull of 42 attempts):** of 55 wrongly-marked non-empty answers, **37 (67%) were correct ENGLISH definitions — often the verbatim answer key — rejected** as *"you've restated the English definition, translate to Korean."* Only 8 were Korean responses (mostly legitimately wrong). So her real problem was NOT the r63 Korean-translation false-negative; it was English-definition rejection.
- **Code check (why fidelity mattered):** the grader is answer-key-authoritative server-side (`functions/index.js:761-792`) — it re-resolves `english=word.definition` + `korean=word.definitions.ko` from `lists/{listId}/words/{wordId}`; the model sees THOSE canonical defs, not the client's. Reconstructed all 25 unique rejected words with canonical defs (real Korean, not stubs) → `scripts/fixtures/yyj_grader_input.json`.
- **Replay (WinClaude r64, has the ANTHROPIC secret; `scripts/grader-replay-yyj.mjs`):** sent them EXACTLY as `index.js` does (same wordsJson shape, one batch, temp 0.1, max_tokens 4096, live deployed prompt `0992f5f`). **Result: all 25 ACCEPTED (×3 stable); 3 genuinely-wrong controls stay rejected.** `correct-defs rejected: 0/25`.
- **Timeline cross-check (read-only):** her 37 rejections were **all recorded 2026-07-08** (one session) under an OLDER deployed prompt. The live grader (`0992f5f`) accepts them → **no live defect for this pattern**; the deployed grader now clears BOTH false-negative families (Korean via r63 + English defs).
- **Caveat:** tests the grading model + deployed prompt with the canonical answer key (the app's grading step), not a re-run of her original browser session. Open (optional): replay vs the pre-r63 prompt `0ddbb34` to learn whether the cohort was unstuck on 07-18 (0ddbb34 deploy) or only 07-19 (r63).
- **No 26SM writes.**

## CS-2026-07-20 — 김지훈/Jeehoon Kim (SAT Adv E): incomplete Google signup (no users doc) → couldn't join despite correct code; enrolled
- **Student:** 김지훈 / Jeehoon Kim / 05372@eis.ac.th / uid `XbNempZhf7N12UBWzkTp7GouC6G3` / target **26SM SAT Adv E [English only]** (`BY3cZduM`, joinCode `JGGV7M`).
- **Symptom (TA):** entered the correct join code but can't get in to take a test.
- **Diagnosis (read-only):** Google auth account EXISTS (created 2026-07-20 06:38 UTC, email verified, provider google.com) but **`users/{uid}` Firestore doc DID NOT EXIST** — signup didn't complete (Google auth ran; the profile/user-doc creation step never did). App `joinClass` (db.js:1029) **throws "User profile not found. Please complete signup first." when there's no user doc** → the correct code can't enroll him. State: enrolledClasses=0, NOT in studentIds, no class_progress.
- **Root cause:** incomplete signup — Google sign-in created the auth identity but the app never wrote `users/{uid}` (likely closed the tab / didn't finish the profile form). Not a join-code problem.
- **Fix (David: "add him to adv e"; `scripts/cs/enroll-student.mjs` NEW — faithfully replicates `createUserDocument` db.js:223 + `joinClass` db.js:1012):** created `users/XbNempZh` (role=student, profile.displayName="Jeehoon Kim" from Google, app defaults, `csEnrollNote`) + `classes/BY3cZduM/members/{uid}` + studentIds arrayUnion + studentCount +1 + enrolledClasses[BY3cZduM]={name,joinedAt}. Backup `dsg-edits/enroll_fix/`. Adv E `data-integrity-sweep` **0/CLEAN before (29) & after (30)**. Verified: role=student, in studentIds, member doc, enrolledClasses=[Adv E]. class_progress:0 (created on first session — normal). Lists resolve from the class's 5 assignments (his enrolledClasses entry is {name,joinedAt} per the app shape — NOT the empty-assignedLists #7).
- **Result:** he can now log in → SAT Adv E + its lists appear → take tests. No further action.
- **Prevention:** incomplete-signup students (Google auth, no users doc) will hit "can't join / profile not found." Reusable `enroll-student.mjs` handles it; ideally the signup flow should guarantee `users/{uid}` is created on first authenticated load (candidate NEED_TO_FIX).

## CS-2026-07-20b — 조은서/Lucy (Inter E): lost-save on Day-11 new-word test → stuck on review, no new words → restored
Lost-save family (CS-2026-06-29 A 황정민 / CS-2026-07-07 최도훈). Inter E sweep: invalidAnchor/twiOverList 0 before & after (reviewNoNewPass 4 = pre-existing benign baseline, unchanged).
- **Student:** 조은서 / Lucy / e1nssxx@gmail.com / uid `1YvklH6J3LctWthufkuyFyy32MM2` / 26SM SAT Inter E [English only] (`jDDw2GEurOj5kD9JswI7`) / Base Camp (`RmNNkuLPectBlBPiLbAJ`, pace 80, thr 92).
- **Symptom (TA):** "새로고침 했음에도 채점이 안되고 새 단어도 안 들어가진다" (even after reload, grading doesn't work and new words won't go in).
- **Diagnosis (read-only):** csd=10, twi=800/1200, NOT throttled (interv=0), grading works (all reviews saved, writtenBy=cloud-function). Her **session_state = phase review-study, newWordsTestPassed=TRUE, newWordsTestScore=1 (100%)** — but **NO Day-11 new-word attempt exists** (only two Day-11 *reviews*, both anchored to the phantom nwei=879). So she passed the Day-11 new test 100% in-session, the **attempt WRITE was lost** ("채점이 안됨"), her session advanced her to the Day-11 review (which saved), but the day can't complete without the new-word anchor → csd frozen at 10 → she keeps getting the Day-11 review, no new words ("새 단어 안 들어감").
- **Fix (David: "yes"):** (1) `manual-pass … jDDw2GEu RmNNkuLP 11 100` (restore the pass she earned; valid anchor nwsi 800/nwei 879/wi 80/testId, passed=true — 100% = her real session score). (2) non-demoting reconcile class_progress csd 10→11, twi 800→880 (= anchor nwei+1; reviewExists(d11) → csd 11) + **deleted the stale session_state** so it rebuilds to Day 12. Backup `dsg-edits/lostsave_fix/1YvklH6J_pre.json`. `csFixNote` stamped. Verified: csd=11/twi=880 → lands **Day 12** (new words 880–959). No corruption (invalidAnchor/twiOverList 0).
- **Root cause (why lost-saves happen):** grading (Cloud Function) and the durable attempt WRITE are two steps; the grade succeeded but the write didn't land. Common causes: in-app browsers (KakaoTalk/Instagram) not persisting the grade-token nonce (localStorage) → save rejected (CS-2026-06-29 A); network drop mid-write; closing the tab before Retry Save. Systemic fix = NEED_TO_FIX (harden save with server-returned attemptDocId); mitigation = use a real browser (now in the Help FAQ).

## CS-2026-07-20c — 김지훈/Jeehoon Kim (SAT Adv E): account ERASED (David: re-signup) — reverses CS-2026-07-20
- **David:** "erase this account. ill have him sign up again." (Reverses the CS-2026-07-20 manual enroll — David opted for a clean student re-signup instead.)
- **Action (`scripts/cs/erase-account.mjs 05372@eis.ac.th --commit`, NEW):** removed from 26SM SAT Adv E [English only] (`BY3cZduM`) studentIds + studentCount −1 + deleted members doc; deleted `users/XbNempZh…` (+ all 5 subcollections, 0 docs — never studied); **deleted the Firebase Auth identity**. Backup `dsg-edits/erase_account/XbNempZh_backup.json`.
- **Verify:** users doc gone, `getUserByEmail` → auth/user-not-found, not in Adv E studentIds; Adv E back to **29 students, sweep 0/CLEAN**.
- **Next:** he re-signs-up with Google (05372@eis.ac.th) → fresh auth + user doc → join code `JGGV7M`. TA guidance: complete the FULL signup (Student role + profile → dashboard) BEFORE entering the join code (the profile step is what got skipped originally, leaving no user doc → the CS-2026-07-20 join failure).

## CS-2026-07-22 — 소지훈 (INT B3): "went to review test without a word test" = THROTTLE review-only day (by design, NOT a bug; read-only, no fix)
- **Student:** 소지훈 / sojihun0915@gmail.com / uid `XPyVanhI…` / 26SM SAT Inter B3 (`sO7jN85m`). Focus = Ascent (dVliNv0p).
- **Symptom (TA):** "단어 시험을 응시하지 않았는데 바로 Review test로 넘어가졌다" (skipped the new-word test, jumped straight to review).
- **Diagnosis (read-only):** Ascent Day 8. His review scores are very low — d6 rv **20%**, d7 rv **17%** → `deriveThrottleMode` last-3 <0.30 → `reviewMode=true` → Day-8 newWordCount=0 → **review-only day → there is NO new-word test by design**, so the app served review directly. Same family as CS-2026-07-14 (정지수) / Bridge TOP / 박연서. Data CLEAN: csd=7, twi=560 = 7×80 (no inflation), Day-8 reviews all ans=30 (not empty-submit runaways). Also finished Base Camp (list-end, twi 1200/1200) on a second list — focus correctly on Ascent.
- **Recovery status:** latest Day-8 review = **100%**, but last-3 avg = **0.41**, still in the hysteresis dead-band ([0.30,0.50] holds priorMode=true) → still review-only. New words return once last-3 clears ~0.50.
- **Action:** NONE (read-only; working as designed). TA explanation given: reviews too low → new words paused → keep scoring reviews well (avg ~50%+) and new words return automatically. No per-student new-words override applied (not requested). Root product question (whether skipped/low review-only days should advance) tracked in the broader forced-pathway / unified-state work.

## CS-2026-07-22b — 신수민 (Adv A1): Ascent Day-8 "only 59 words" — data CLEAN + not throttled (should be 80); likely STALE client session (read-only; no fix yet)
- **Student:** 신수민 / suminshin377@gmail.com / uid `dRXoaFje…` / 26SM SAT Adv A1 (`6F0PX2E3`) / Ascent (dVliNv0p, pace 80). Single class.
- **Symptom (TA):** "Ascent Day 8 리스트에 59개만 뜹니다" (only 59 words show on Day 8).
- **Diagnosis (read-only):** csd=7, twi=**543**/1600, coverage CONTIGUOUS 0..542 (no gap, no inflation, valid anchors). NOT throttled — last-3 reviews [d5 0.00, d6 0.97, d7 0.93] avg **0.633** > 0.50 exit, `reviewMode`=false → binary `FORCED_PATHWAY=true` → interv 0 → **Day 8 should allocate the FULL 80 new words**. All 80 Day-8 words (indices 543..622) exist as `NEW` study_states. So server-side she should see 80; **the 59 is NOT reproducible from stored data.**
- **Root of the odd twi:** her **Day-5 introduced only 63 words** (attempt nwsi 320/nwei 382/wi 63) instead of 80 — a historical **graduated-throttle partial day from BEFORE the 2026-07-18 binary cutover** (her d5 review was 0%). That offset her twi to 543 (not 7×80=560), so she's ~17 words behind the round-number schedule. Harmless, not corruption. (Instance of the known allocation/test-size-at-boundaries issue, NEED_TO_FIX #13/N1.)
- **Most likely cause of the live 59:** a STALE cached session (browser `sessionStorage.dailySessionState` or the Firestore `session_state`) computed under the OLD graduated throttle when her reviews were bad → a partial count. A clean reload should rebuild Day 8 to the full 80.
- **Action:** NONE yet (read-only). Recommend: hard-refresh / log out+in and restart the session → Day 8 should show 80. If 59 PERSISTS after a clean reload → need a screenshot + browser console (live-computed value, not in Firestore). Optional belt-and-suspenders: clear her Firestore `session_state` to force a server-side fresh build (pending David).
- **RESOLUTION (David asked to reset her to a clean Day-8 IF she hadn't tested):** the clean-slate script's GUARD correctly ABORTED — she took the Day-8 new-word test at 07-22 08:18 (during/after the ticket): **score 100%, passed, nwsi=543 nwei=622 wi=80 — the FULL 80 words, not 59.** So the "59" was a stale pre-test display; when she actually tested she got the proper 80. Her data is CLEAN: mid-Day-8 (new passed, review pending), twi reconciles 543→623 on load (Day-8 anchor nwei 622+1), csd stays 7 until she does the Day-8 review → then Day 9. **NO fix needed, NO 26SM write made** (guard prevented an unnecessary reset that would have cleared a session holding a valid 100% Day-8 pass). twi=543 offset from the historical d5=63 short day is cosmetic (contiguous coverage). Nothing further.

## CS-2026-07-23 — 홍석현 (INT B4): grader marks VERBATIM-CORRECT English definitions WRONG ("restated the English definition") — LIVE grader false-negative, 0992f5f fix INCOMPLETE
- **Student:** 홍석현 / seokhyunhong37@gmail.com / uid `HavPhdjx…` / INT B4 (also Bridge CORE + INT B3) / Base Camp Day 19.
- **Symptom (TA):** "답안이랑 똑같이 적어도 전부 오답 처리" (writes exactly the answer, all marked wrong).
- **Diagnosis (read-only):** Day-19 new test — he wrote the **EXACT correctAnswer verbatim** for ~23 of 30 (fraudulent/ample/rhythmic/rupture/quantitative/comparable/lure/diminish/temperament/reluctance/scholarly/whereabouts…), grader marked them WRONG with *"You've restated the English definition word-for-word. Provide the meaning in your own words or in Korean."* **REAL score ~100% (7 ok + 23 verbatim-correct rejected, 0 genuinely wrong); grader gave 23%.** Korean defs EXIST on these words (사기성의 etc.) → not a missing-def issue; it's the model over-extending Rule 1 ("restating the word") to "restating the DEFINITION."
- **⚠️ Re-opens the grader work — my CS-2026-07-19e "no live defect" was OVER-OPTIMISTIC.** The 윤여진 replay (r64) happened to ACCEPT verbatim English defs (one sample), but Haiku is INCONSISTENT: today it REJECTS them for multiple students. The deployed 0992f5f fix scoped Rule 1 to English-only + added Korean examples but NEVER explicitly blessed "a correct English definition, even verbatim, is CORRECT" → the false-negative persists intermittently.
- **COHORT scope (newest 1200 attempts since 07-22):** 4 students hit hard by verbatim-English-def rejection: **홍석현 (30/30), 이세나 (미주 Final, 31+), Jiin Lee (Final A, 7), 송준원 (INT B1/Adv A2, 3).**
- **Tension to confirm with David:** the prompt's own reasoning ("provide the meaning in your own words") implies verbatim-copy was meant to be INSUFFICIENT (paraphrase required). But David ruled 2026-07-19 "a correct English definition should be CORRECT." These conflict — confirm the intended behavior before the grader fix.
- **Action:** NONE yet (read-only). PROPOSED: (1) immediate unstick 홍석현 (+이세나) — accept their correct answers / pass the day (real score ~100%); (2) grader PROMPT fix — explicitly "an answer matching the provided English definition, even verbatim, IS the meaning and CORRECT; 'restating' applies to the WORD only, never the definition" + positive examples + regression fixture; Codex input (grader=codex-only); redeploy via WinClaude. Pending David.

## CS-2026-07-23b — TA batch triage (10 tickets) + 3 fixes: 2 class-change carries + 1 session-clear
Triaged the 07-22/23 TA batch (read-only via scripts/cs/batch-triage.mjs). Classification: 3 real fixes · 2 finished-list (→LAP2) · 1 throttle · 3 resolved/clean · 1 environmental. **3 David-authorized fixes applied** (INT A2 sweep 0 real-corruption before & after; 5 reviewNoNewPass = pre-existing benign):
- **조예서 / 0exey.7@gmail.com** (INT B2 → **INT A2** `TyBnqbcc`): class-change carry. Her Ascent progress was keyed to INT B2 → INT A2 showed Day 1. `carry-progress.mjs`: wrote INT A2 Ascent csd=13/twi=762 (real anchor d14 nwei 761; she'd progressed to Day 14 since the ticket), cleared session, pinned focus → lands **Day 14** (day-14 review pending). Backup dsg-edits/carry_fix/.
- **김나연 / nayunkim777@gmail.com** (INT A1 → **INT A2** `TyBnqbcc`): same carry. INT A2 Ascent csd=16/twi=990 (anchor d16 nwei 989, d16 review done) → lands **Day 17**. (TA said "Day 16" but her real progress = Day 16 complete → Day 17; carried real, non-demoting.) Backup saved.
- **김지훈 / jeehoonkim@gmail.com** (Adv E): "Progression 튕김" after a disconnect = stale/crashed Day-1 session. Cleared his session_state (guard: 0 attempts → zero loss) → fresh Adv E Day-1 rebuild on next login. NB: `05372@eis.ac.th` is a DEAD duplicate (incomplete signup, no user doc) — he must use **jeehoonkim@gmail.com** in a real browser. Backup dsg-edits/session_clear/.
- **Triage-only (no fix):** 오윤세(helluuu281)+신동윤(sindongyun239)=finished Ascent+Summit → LAP2 (신동윤 mis-triaged as throttle; it's LIST-END). 소지훈=throttle (CS-2026-07-22). 강라원(recovered)/김강훈(did Day 7)/신수민(passed Day-8, the 59=review set)=resolved. 송연아=login/env (no email). 홍석현=grader false-negative (CS-2026-07-23, pending).
- **FIX APPLIED (David: "fix 홍석현's grade, make it 100%"):** corrected his latest Day-19 new attempt (`764520_5z18zlzc9`, INT B4 Base Camp, anchor nwsi 1140/nwei 1199, testId present) — TA/WSL-verified ALL 30 answers genuinely correct (0 real wrong) → flipped the 23 grader false-negatives to isCorrect=true, correctCount=30, score=100, retention=1.0, passed=true, manualOverride + note. Advanced session (newWordsTestPassed=true, phase=review-study). Backup dsg-edits/grade_fix/. Inter B4 sweep invalidAnchor/twiOverList/csdImplausible=0 before & after. He proceeds to the Day-19 review; anchor nwei 1199 → twi reconciles to 1200 = Base Camp COMPLETE → he then moves to his next list (Ascent). **NB:** his answers were verbatim-EXACT copies of the answer key for all 30 — defensible (the app teaches these exact defs in flashcards) but unusually precise; integrity is the teacher's call, not a grader issue. **Prompt fix still PENDING (David: "then we'll deal with the prompt").**

### CS-2026-07-26 — Graduation-validity probe (READ-ONLY diagnostic, DEEPFIX2 item 15)
New defined script `scripts/cs/graduation-validity-probe.mjs` (read-only; usage `NODE_PATH=/app/node_modules node scripts/cs/graduation-validity-probe.mjs [classNameRegex=26SM] [limitStudents]`). Classifies every answered review-test word-row by prior evidence (proven / afterWrong / untested-fresh / untested-aged) and measures accuracy — the evidence classes graduation policies trust (direct graduated-word join impossible: `returnMasteredWords` nulls `masteredAt`, picks unlogged). Ran on 26SM (907 students, ~341k rows): proven 75.0% vs untested-aged 70.8% (Δ4.2pp; proven21 Δ9.0pp); afterWrong 32.1%. Output: `docs/plans/deepfix2/evidence/graduation-validity-26SM.json`. No writes.

### CS-2026-07-27 — Cycling/continuation usage probe (read-only; DEEPFIX2 D7/P9 reconciliation)
- **What:** `scripts/cs/cycling-usage-probe.mjs` (new defined script) scanned all `classes/*.assignments.*` for `cyclingEnabled:true` and `nextListId`.
- **Result:** 302 classes scanned — **0 assignments with cycling enabled, 0 with continuation links.** No student can be mid-cycle (flag-gated unlock).
- **Why:** David's D7/P9 question — is same-list cycling retireable under the universal model (restudy = the past-day browser)? Zero live usage ⇒ retirement has no data/CS blast radius.
- **Writes:** none (read-only).

## CS-2026-07-27 — TA batch (3): 김우주 = throttle review-only (by design) · 임유이 = NEW "throttle-EXIT stranded" deadlock (fix pending David) · retake-the-review guidance verified
Read-only diagnosis; **no 26SM writes yet**. New defined script `scripts/cs/scan-throttle-exit-stranded.mjs`; `batch-triage.mjs` now takes `name:email` CLI args.

- **김우주 / rkim2090809@gmail.com / uid `GL7SXBWC…` / 26SM SAT Bridge (CORE) `wRoGjp2Q` / Base Camp (pace 60)** — TA (류민진): "step 1, 2를 응시하지 않았는데 3부터 뜬다."
  **THROTTLE review-only day, by design — data CLEAN, no fix.** csd=12, twi=720=12×60, valid anchors, day 12 complete (d12 new 100% 07-24 + d12 review 10% 07-27). `reviewMode=true` (last-3 reviews [0.17, 0.20, 0.10] avg **0.156** < 0.30 ENTER) → Day 13 allocates `round(60×0)=0` new words → the flow starts at `SessionSteps` step 3 (Study New Words / New Words Test / **Review Past Words** / Review Test / Complete). Same family as CS-2026-07-22 (소지훈), CS-2026-07-19 (김재경).

- **임유이 / Yui Lim / yulimn30@gmail.com / uid `RbkZOmEL…` / 26SM SAT Inter B3 `sO7jN85m` / Base Camp (pace 80)** — TA (홍예원): "new word day 8을 안 하고 review로 넘어갔는데 리뷰 제출하려니 new word를 안 해서 안 된다고 뜬다."
  **★ NEW SIGNATURE — "throttle-EXIT stranded."** Sequence, all reconstructable from her docs: (1) 07-26 08:46 her last-3 reviews were [0.23, 0.17, 0.17] → review mode ON → **Day 8 built review-only** (0 new words) → app served the review test directly, no new-word test (her three 07-26 d8 reviews carry `nwsi 560 / nwei 559` = the empty new-range signature). PR-3 hold-csd correctly held csd=7. (2) She re-took the d8 review — 13% → **77% → 83%** — each appended to `recentSessions` → last-3 avg **0.578 > 0.50 EXIT** → `reviewMode` flipped **false**. (3) Day 8 is now a NORMAL 80-word day (today's two d8 reviews carry `nwei 639` = a full day-8 range), so `completeSession`'s F-4 evidence check (`functions/foundation.js:1410`) needs a passed day-8 `new` anchor that **can never exist** → `no_evidence` → the gate message on every submit. She has re-submitted 5× (last 07-27 02:42, 88%) and cannot advance. **No corruption:** csd=7 / twi=560 = 7×80 correct, anchors valid, no day-8 `new` attempt was ever written (not a lost-save).
  **Recoverability:** routing is attempt-authoritative (`DailySessionFlow.jsx:822` "ATTEMPTS ARE THE SOLE AUTHORITY") → a genuinely clean load routes her to `PHASES.NEW_WORDS` (80 words, 560–639). The stale `session_state` (`phase=review-study, newWordsTestPassed=false`) is what keeps re-serving the review test.
  **FIX APPLIED (David: "fix 임유이"; `scripts/cs/clear-session-state.mjs --commit`, NEW):** deleted her Base Camp `session_state` (`sO7jN85mL5ZsaBY1BbGm_RmNNkuLPectBlBPiLbAJ`). Guard passed — **no passed day-8 `new` attempt to lose** (the script REFUSES a clear when the session claims `newWordsTestPassed:true` with no matching attempt = the lost-save signature, CS-2026-07-20b). Backup `dsg-edits/session_clear/RbkZOmEL_RmNNkuLP_pre.json`. Precedent: CS-2026-07-23b (김지훈 session-clear).
  **Verified post-fix:** session doc gone (her other two, Ascent/Summit Day-1, untouched); class_progress unchanged at csd=7 / twi=560 / `reviewMode=false` / interv=0; last-3 reviews [0.13, 0.77, 0.83] avg 0.578 → `deriveThrottleMode=false` → **Day 8 rebuilds with the full 80 new words (560–639)** → Study New Words → New Words Test → Review → Day 9. Sweep "Inter B3" BEFORE = 3 findings, all benign `reviewNoNewPass` (incl. her own day-8, which clears when she takes the day-8 new-word test); post-fix sweep not re-run (config-free, single-doc delete). **No code change** (David: "just fix her state — don't fix the code").

- **★ Cohort scan (`scan-throttle-exit-stranded.mjs`, READ-ONLY, 51 26SM classes / 927 students):** 178 sessions parked in `review-study` with `newWordsTestPassed!==true` → **exactly 1 stranded: 임유이.** The rest are the known benign populations (157 list-end, 15 legitimately still in review mode, the remainder mid-day with no review submitted yet). **Isolated, not a wave — but see below.**

- **David's TA guidance — "review만 뜨면 여러번 통과해서 review 평균 올리라고 해주세요" — VERIFIED CORRECT, with one required addendum.** Mechanically it works: on a throttle review-only day every engaged review submit appends a `recentSessions` entry via the hold-csd path (csd does NOT advance), so 2–3 good retakes lift the last-3 average past the 0.50 EXIT bar and new words return (`deriveThrottleMode`, `src/utils/forcedPathway.js:86`). 임유이 is the live proof — and also the live proof of the trap: **the retake that crosses 0.50 turns that same day into a normal day, and the student still sitting in the review test then hits "먼저 새 단어 시험을 통과해야 합니다."** Correct script for TAs: *"복습을 다시 봐서 평균을 올린 뒤 **페이지를 새로고침**하세요. '새 단어 시험을 먼저 통과해야 합니다'가 뜨면 그건 평균이 회복됐다는 신호입니다 — 새로고침하면 새 단어가 나오고, 단어 시험을 먼저 보면 됩니다."* Without the reload step, every student who follows the advice reproduces 임유이's deadlock. (Note, factually: 3 retakes of the SAME day's review clear the throttle — the average is over the last 3 review *scores*, not 3 distinct days.)
  **Root fix, not CS:** the day's review-only bit is re-derived on every submit instead of being decided once at session build and persisted per day. Either make it sticky per study-day, or have the client turn `no_evidence` into "new words are back — reload and take the word test first." DEEPFIX2 **D-1 (remove the review-score throttle entirely)** eliminates this bug class outright.

## CS-2026-07-27b — 김도은 (Inter E → Adv E): "everything reset to Day 1" = the getPrimaryFocus fallback, NOT a progress reset (read-only; decision pending David)
- **Student:** 김도은 / ddoeun824@gmail.com / uid `2twna6gqPGdV9si9CY0SIw9YteW2`. Enrolled **26SM SAT Inter E [English only]** (`jDDw2GEu`, joined 07-06) **+ 26SM SAT Adv E [English only]** (`BY3cZduM`, joined **today 07-27 05:12**).
- **Report (TA):** promoted from Inter to Adv E; her word-test progress reset and she starts from Day 1.
- **NOTHING IS LOST (read-only verify):** all her work is **Base Camp** (`RmNNkuLP`) under Inter E — **38 attempts, days 1–16**, `class_progress` csd=15 / twi=1148 (reconciles to **1200** on load; anchor = day-16 `new` PASSED 97% on 07-24, nwei 1199), **1200 Base Camp `study_states`** (mastery, student+list scoped). Base Camp is a 1200-word list ⇒ **she has finished it**; only the **day-16 review** is pending. Adv E has **no class_progress at all**, and she has **zero** Ascent/Summit attempts or study_states.
- **Root cause of what she sees — the `getPrimaryFocus` fallback (CS-2026-06-24b / CS-2026-06-28b footgun), not the (class,list) reset.** Her `settings.primaryFocus{ListId,ClassId}` are **UNSET**, so the dashboard falls back to the **most-recently-assigned** list across her classes. Assignment dates: Inter E {Ascent **2026-06-22**, Base Camp 05-31, Summit 05-30} · Adv E {Summit 05-30, Ascent 05-31, Base Camp 05-29, LAP2 ×2 05-28}. Newest = **Inter E's Ascent** → she lands on **Ascent Day 1**, a 1600-word list she has never touched. Joining Adv E today is what made her notice.
- **Note:** "Day 1" is *arithmetically* correct for Ascent — but it resolves to Ascent **under her OLD class (Inter E)**, so studying there would strand that progress if Inter E is later dropped. A pin is required either way.
- **UPDATE (same day, TA: "they selected the class/list correctly and it still happens" + 2 more students):** see the amended finding below — the cause is a **LIST CHANGE (Base Camp → Ascent)**, not the focus fallback and not a carry failure.

### CS-2026-07-27b (amended) — Inter E → Adv E "everything reset to Day 1" ×3: it is a LIST CHANGE, not lost progress; same-list carry verified WORKING
- **Students:** 김도은 `2twna6gq` · 윤주니엘 `NGZkMFjZ` · 혜연/celina kim `gAb2b5r0`. **No data lost for any of them; no writes made.**

| student | enrolled | real progress | Adv E progress |
|---|---|---|---|
| 김도은 ddoeun824@ | Inter E + Adv E | **Base Camp d16, twi 1148→1200/1200 — list FINISHED** (d16 new passed 97%; d16 review pending) | none |
| 윤주니엘 Junielyoun@ | **Inter E ONLY — never added to Adv E** (`studentIds`/`members`/`enrolledClasses` all false) | **Ascent d15, twi 1200/1600** (+ stray Base Camp d3) | n/a |
| 혜연 kim.celina.starz101@ | Inter E + Adv E | **Base Camp d4, twi 400/1200** | Ascent doc csd=0/twi=0 (0 Ascent attempts — correct) |

- **ROOT CAUSE: Adv E studies ASCENT; Inter E students were on BASE CAMP.** Progress is per-LIST, so there is nothing to carry between two different lists — Ascent Day 1 is arithmetically correct. The forward progression is Base→Ascent→Summit (CS-2026-07-13d), so Ascent IS the right next list; what is missing is any UI that tells a promoted student "this is a new book, not your old one reset."
- **Same-list cross-class carry is NOT broken — verified live.** `scripts/cs/sweep-ascent-carry.mjs` over the ADV cohort: **0 genuine carry-misses** (all 12 flagged = ACTIVE-ELSEWHERE, i.e. stale old-class docs for students progressing in another class — expected, harmless). The deployed server resolver `resolveListProgress` (functions/foundation.js:1737) enumerates **every** class_progress doc for the list (`where listId ==`, incl. dropped classes) and anchors via `computeAnchorPosition(uid, listId)` — student+list, **no classId filter** — so a promotion on the SAME list keeps its day. Corroborated in-cohort: 황준혁/Yujin Yang/Emily bae/민준/Sei Nam all carry or progress correctly in Adv E.
- **⚠ CORRECTION (same day, 22:15 KST — CS-2026-07-27c 황정민):** the "0 genuine carry-misses" reading above is **too strong**. The sweep only covers ADV-class Ascent students and ran before the live failure occurred. A confirmed **post-cutover** carry failure exists — 황정민/jungminacc170078 entered 유라시아 SAT Top at 13:02:05 today, his Ascent doc was created **0/0 and never reconciled** despite a valid day-4 anchor (nwei 319) in Adv B2, and the session built **day=1**. See CS-2026-07-27c. NEED_TO_FIX #12 is LIVE, not historical.
- **Historical carry-miss (pre-cutover, for the record):** 민준/mjnkwak@ did **Ascent days 1–5 in Adv B1** (`7PDT5jGK`, from 06-22) then **re-did days 1–10 in Adv E** (from 07-06) = 400 words re-studied. That predates the P4 server cutover (07-18) and is NEED_TO_FIX #12. **No post-cutover instance found.**
- **Not the focus fallback either:** `getPrimaryFocus` rule 2a (Dashboard.jsx) now PREFERS a list with active progress (csd>0, ranked by last activity) and only falls back to newest-assigned when NO list has progress. All three have real progress, so an untouched dashboard lands them on their true Base Camp/Ascent day — the Day 1 appears only when they open Adv E's **Ascent** card themselves (which matches the TA's "they selected it correctly").
- **Actions proposed (pending David):** (1) **윤주니엘 — enrollment never happened**; add her to Adv E (`enroll-student.mjs` pattern) — her Ascent d15 then carries automatically (same list). Nothing in her data produces "Day 1"; if she still reports it, get a screenshot of which card she tapped. (2) **김도은** — finished Base Camp, so Ascent Day 1 is correct; optionally have her close the pending Base Camp d16 review first. (3) **혜연** — only Base Camp d4 (400/1200); moving her to Ascent Day 1 abandons 800 unstudied Base Camp words → teaching call, not a data fix.
- **Superseded:** the options (a)/(b)/(c) below were written before the list-change cause was established; (b)'s "carry Base Camp into Adv E" is still available if David wants her Base Camp record to live in her canonical class.

- **Options put to David (no writes yet, SUPERSEDED — see amendment above):** (a) pin focus → **Adv E / Ascent** = start Ascent Day 1 as a promoted Adv student (Base Camp history stays visible under Inter E; the pending day-16 Base Camp review is abandoned); (b) same, but first carry her Base Camp record into Adv E (`carry-progress.mjs`, csd 15 / twi 1200 from her OWN anchor — no fabricated attempt) so the day-16 review can be closed out in her canonical class; (c) pin → Inter E / Base Camp for one session to finish the day-16 review, then flip to Adv E / Ascent. Dropping Inter E is a separate call (not authorized).

## CS-2026-07-27c — 황정민 (유라시아 SAT Top): LIVE post-cutover cross-class carry FAILURE (NEED_TO_FIX #12) + in-person Day-5 credit → Day 6
First **confirmed post-P4-cutover** instance of #12. Sweep "유라시아 SAT Top" IDENTICAL before & after (2 benign `reviewNoNewPass` day-17 list-end, neither him; invalidAnchor/phantomAnchor/twiOverList/csdImplausible = 0).
- **Student:** 황정민 / Jungmin Kodjo Hwang / jungminacc170078@gmail.com / uid `3sn4KPjhlLYxsnxmlgZvkDN0RNz1`. Three classes: Inter B2 (06-22), Adv B2 (07-13), **유라시아 SAT Top `cKtB3HVMrQfS6lLe9vLe` (joined today 11:25)**. `primaryFocus` already = Ascent / 유라시아 Top. Ascent pace **100** there (his history was pace **80** in Adv B2).
- **Ticket (TA 이서연/전민정 via David):** in-person until 2 weeks ago, first online week; new class restarted VocaBoost at Day 1; Ascent through **Day 5** already taken in person → "set today's test to Day 6." Screenshot: `DAY 1 · STEP 1 · Learn 100`, **WORDS LEFT 1,600**, list dropdown open.
- **TA's other question — "can the test day be set from this screen?" → NO.** The dashboard dropdown switches the LIST only; there is no student- or teacher-facing day control anywhere. Day is derived from progress.
- **THE CARRY GENUINELY FAILED (not a stale card — my first read was wrong):** his 유라시아 Top Ascent `class_progress` was **created 2026-07-27 13:02:05 at csd=0/twi=0 with `updatedAt` still 13:02:05** (never reconciled), and the `session_state` written **13:02:14** built **day=1** — i.e. he entered a session and the app served him Day 1. His anchor is valid and student+list scoped: **Adv B2 d4 new passed 97%, nwei 319**, plus **320 Ascent study_states**. `safeValuesForDoc` should have applied `safeCSD=max(0,4)=4 / safeTWI=320`. Evidence the server resolver never ran for that entry: **zero `resolve_list_progress` logs for his uid** (48 written for other students in the same window), and his doc's `programStartDate` == `createdAt` (13:02:05) — the **legacy client** create shape, not the server's `mondayOfWeekTimestamp()` create-on-miss. Mechanism not pinned from data alone; the OUTCOME is unambiguous. → **NEED_TO_FIX #12 is LIVE post-cutover** (this corrects the "0 genuine carry-misses" reading in CS-2026-07-27b, whose sweep covers only ADV-class Ascent and predates this).
- **★ Blast-radius scan (READ-ONLY, ad hoc, 954 26SM students):** **53 zero (csd=0/twi=0) `class_progress` docs shadowing real progress the same student has on the SAME list in ANOTHER class** — oldest 2026-07-02, newest his today. **CANDIDATES, not confirmed strandings:** a zero doc is inert when the student's focus/session sits on their real doc (verified inert for e.g. 민준/mjnkwak, Emily bae — both progressing normally in Adv E). 황정민 is the one CONFIRMED live case (focus + session_state both on the zero doc). Sizing the real impact needs an intersect of the 53 against active `primaryFocus` + `session_states` — **not yet run.**
- **FIX APPLIED (David: "go") — 3 writes, repairs the carry AND the Day-6 request:**
  1. `cs/manual-pass.mjs jungminacc170078@gmail.com cKtB3HVMrQfS6lLe9vLe dVliNv0p 5 97` → valid Ascent **Day-5 anchor nwsi 320 / nwei 399 / wi 80 / testId set / manualOverride**. **Pace 80 deliberately** (the script defaults to 80 with no day-1 attempt in the target class = his real lineage). At pace 100 the day-5 range would have been 400–499 → twi 500 → **silently skipping words 320–499 (180 unstudied)**; the 80-pace range makes words 0–399 fully contiguous — only the single in-person Day 5 (320–399) is credited on the TA's word.
  2. 유라시아 Top Ascent `class_progress` → **csd=5, twi=400** (+`csFixNote`/`csFixAt`). Needed because there is no Day-5 review, so derived csd = anchorDay−1 = 4; stored 5 is non-demoting (`safeCSD = max(stored, anchor.csd)`) and sticks. Backup `dsg-edits/carry_fix/3sn4KPjh_cKtB3HVM_dVliNv0p_pre.json`.
  3. `cs/clear-session-state.mjs … --commit` → deleted the stale day-1 `session_state` (guard passed; nothing to lose). Backup `dsg-edits/session_clear/3sn4KPjh_dVliNv0p_pre.json`.
- **Verified:** anchor chain contiguous d1(0-79)→d2→d3→d4(240-319)→**d5(320-399)**; twi 400 = nwei+1; csd=5 → he resumes **Day 6 = words 400–499 @ pace 100**. Only the 유라시아 Top session doc removed (his old Inter B2 day-2 stub left alone).
### WHY it happened — root-cause investigation (read-only, same day)
Established by evidence, in order:
1. **His session entry never reached the server resolver.** `system_logs` has **ZERO `resolve_list_progress` entries for his uid**, while **48** were written for other students in the same window (12:30→13:10Z). The resolver logs EVERY resolution (the #12 tripwire, FIX_PLAN §6.1), so absence = it did not run for him.
2. **It fell back to the LEGACY CLIENT path** (`getOrCreateClassProgress`, progressService.js:111-165). Proven by the doc's **create signature**: the client sites (:158, :632, :734) override `programStartDate` to `Timestamp.now()` (== `createdAt` to the second), whereas the server's create-on-miss stamps `mondayOfWeekTimestamp()` (midnight KST). His doc: `programStartDate == createdAt == 13:02:05`. Cohort-wide the signature cleanly separates the P4 cutover — **every** `class_progress` created before 2026-07-19 is client-shaped; from 07-20 on it is **365 server / 17 client ≈ 4.4% still taking the legacy path**.
3. **On that path the reconciliation produced nothing.** His doc's `updatedAt == createdAt` (13:02:05) — the recon write (progressService.js:280) only fires when `safeCSD !== storedCSD || safeTWI !== storedTWI`, so it computed **csd=0 / twi=0** against stored 0/0. With `hasValidData=false` the formula is `safeCSD = max(0,0)`, `safeTWI = max(0,0)` ⇒ silent no-op. I.e. `getMostRecentPassedNewTest` returned no usable anchor **at that moment**.
4. **NOT a data, index, or reset-epoch problem — all three ruled out.** Replaying the EXACT two client anchor queries against his data succeeds: the position query (`studentId,listId,sessionType,passed,newWordEndIndex>=0` order `newWordEndIndex desc, submittedAt desc`, index **#32**) and the studyDay fallback (index **#33**) both return his anchor. The F-6 reset filter is **inert cohort-wide** — `progress_meta` = **0 docs across all 954 students**, so `resetMs` is always null and `notPreReset` always true. So the anchor was findable; the lookup simply did not deliver on that call (transient failure ⇒ `status:'query-error'` ⇒ `hasValidData=false` ⇒ the no-op above).
5. **Which condition sent him down the fallback is NOT determinable from data.** The gate is `if (SERVER_PROGRESS_WRITE && auth.currentUser?.uid === userId)` (progressService.js:118). Two candidates: the flag being false in the bundle that session loaded, or **`auth.currentUser` not yet hydrated at call time** (a cold-load / token-refresh race) — the latter fits a 4.4% intermittent rate. Distinguishing them needs a client-side signal that does not exist today.
6. **Why it reads as "intermittent" (#12) rather than broken:** the legacy path has **no server-side observability**. The resolver logs every resolution; the client fallback logs only to the browser console. So when the fallback is taken AND its anchor lookup returns nothing, the student is silently seated at Day 1 and **nothing lands in `system_logs`** — undetectable except by a TA ticket or the shadow-zero scan.
7. **Rarity confirmed:** of the **20** post-cutover client-path creates, only **3** ended at 0/0 — and 2 of those (cindy.choi504, minseoshin1016 ×2) had **NO prior anchor**, so 0/0 was correct. **황정민 is the ONLY post-cutover case where a real prior anchor existed and the doc still landed 0/0.**
- **Net:** the carry logic itself is sound (server resolver AND the client path both carry — e.g. hrkimiwna 07-23 client-path create carried d18/nwei1079 → csd 20/twi 1200). The defect is the **unobserved fallback**: a ~4.4% slice of session entries still runs the legacy client path, and on that path a single transient anchor-query failure silently seats the student at Day 1 with no log. **Code-side remedies (NOT done — flagged only, per David's "fix state, don't fix the code"): (i) log the fallback (`progress_resolver_bypassed`) so the tripwire covers both paths; (ii) treat `query-error` on a FRESHLY-CREATED 0/0 doc as retryable rather than committing 0/0; (iii) close the `auth.currentUser` race at the gate.** → NEED_TO_FIX #12.

- **Flagged, NOT done:** (a) he **skips every review** — d2/d3/d4 reviews submitted with 0/30, 1/30, 0/30 answers. His new class starts with clean `recentSessions` so no throttle now, but 3 more empty reviews put him on review-only days. TA should tell him to actually take them. (b) He is in **3 classes** (Inter B2 Base Camp finished 1200/1200 + Adv B2 Ascent d4 + 유라시아 Top) — the multi-class threshold-resolution risk of CS-2026-07-03; recommend dropping Inter B2 + Adv B2. Not authorized.

## CS-2026-07-30 — 유지현/Jihyun (Inter A1): "Day 13·14 안 봤는데 14 리뷰부터 뜬다" = Day 13 WAS taken + Day 14 is a THROTTLE review-only day (read-only; NO fix)
- **Student:** 유지현 / Jihyun / hoon10312007@gmail.com / uid `k1VHRQ5k…` / 26SM SAT Inter A1 (`k0j59bXv`) / Ascent (`dVliNv0p`, pace 80). Also finished Base Camp (1200/1200); primaryFocus correctly = Ascent.
- **Report (TA):** 보캡 Day 13·14를 안 봤는데 14 리뷰부터 뜬다 — 오류인지 확인.
- **Premise is half wrong — Day 13 WAS taken:** new-word test **07-29 05:23, score 93, PASSED**, words 960–1039. It is in her record; only Day 14's word test is absent.
- **Day 14 has no word test BY DESIGN (throttle review-only).** Her d12 review = **17%**, d13 review = **20%** (both fully answered, 29–30/30). After the d13 review the last-3 average fell below the 0.30 ENTER bar → `reviewMode=true` → Day-14 allocation `round(80×0)=0` → the app serves the Day-14 review directly, with no Day-14 new-word test to take. Same family as CS-2026-07-22 (소지훈) / CS-2026-07-27 (김우주).
- **Data CLEAN, not stuck-by-corruption:** csd=13, twi=**1040 = 13×80** exactly; anchor = her own d13 passed-new (nwei 1039 → twi 1040); csd held at the review-only day by the deployed PR-3 hold-csd (by design). No empty-submit runaway (every review answered 29–30/30), no lost save, no invalid anchor.
- **Recovery is imminent:** her Day-14 review scored **87%**, lifting last-3 to **[0.167, 0.20, 0.867] = 0.411** — inside the 0.30–0.50 hysteresis band, which HOLDS the prior mode. One more review above **~44%** (>0.433) tips the average past the 0.50 EXIT bar → review mode clears → new words return automatically.
- **⚠ She is one good review away from the CS-2026-07-27 trap.** Her `session_state` is `day=14 / phase=review-study / newWordsTestPassed=false` — the exact 임유이 shape. The retake that crosses 0.50 converts Day 14 into a normal 80-word day, and the completion gate then refuses her review submit ("먼저 새 단어 시험을 통과해야 합니다"). **TA message must include the reload step:** "복습을 한 번 더 44% 이상 받으면 새 단어가 다시 나옵니다 — 그 다음엔 페이지를 새로고침하세요. '새 단어 시험을 먼저 통과해야 합니다'가 뜨면 회복됐다는 신호이니, 새로고침 후 단어시험부터 보면 됩니다."
- **Action: NONE** (read-only; working as designed). No 26SM writes.

### CS-2026-08-01 — Track B2 database-shape probe (read-only; DEEPFIX2 backfill pipeline)
- **What:** `scripts/deepfix2/b2-database-investigation.mjs` — count() volume aggregations + 30-student deterministic sample of attempts/study_states shapes (26SM cohort filter).
- **Result:** cohort 960 students · 41,074 attempts · 1,415,723 study_states · scores 0-100 uniform (over-100 corruption class confirmed present) · blanks: absent answer rows carry no wordId → historical blank-fails per-word unattributable (published as B1 undercount) · resetEpoch not stamped on attempts; 0 pre-epoch orphan attempts in sample · no existing review* label fields.
- **Writes:** none (read-only).


### CS-2026-08-03 — reset law: pre-P5 fence scope (BL-A) + the RESET_V2 gate
The DEEPFIX2 §9 reset rebuild (fence-first owned lock) is GATED behind `RESET_V2_ENABLED=false` in
functions/foundation.js — production `resetProgress` keeps the legacy law until David flips the const at
a deploy. When it flips: (1) the fence/lock lives on `users/{uid}/progress_meta/{listId}` ONLY while
LIST_PROGRESS_CANONICAL is false — NEVER create `users/{uid}/list_progress/{listId}` pre-P5 by hand
either: both live progress readers prefer that doc on EXISTENCE, and a csd/twi-less doc freezes the
student at day 0 (the r71 BL-A defect class). If a stray pre-P5 list_progress doc is ever found, delete
it (after confirming it carries no currentStudyDay) and the legacy readers recover. (2) Concurrent resets
now reject `reset_already_running`; a crashed reset's lock is takeover-eligible after 10 minutes —
re-running resetProgress self-heals it. (3) Claimed grading_jobs for the list are cancelled
(`cancelled_reset`) with their answer rows redacted.
