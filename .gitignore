# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated at deploy time by scripts/stamp-build.mjs (still bundled into the functions
# deploy via firebase.json — gitignored only to avoid committing a churning file).
functions/buildInfo.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?



.env
scripts/serviceAccountKey.json

# Firebase Admin SDK service account — never commit
service-account.json
service-account*.json
*-credentials.json
gcp-key.json

# Audit-generated credentials (contains real Firebase passwords)
audit/playwright/seeded_accounts.json
audit/playwright/seeded_*.json
audit/playwright/findings/evidence/
audit/playwright/findings/findings_*.md
audit/playwright/findings/SUMMARY.md
audit/playwright/findings/RECOMMENDATIONS.md
audit/playwright/findings/EVIDENCE_INDEX.md

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# Live-data admin harnesses, per-student backups, and audit reports (own git repo,
# contains student uids/emails — never commit to the app repo)
dsg-edits/

# Windows shell redirect artifact
nul

# Audit run artifacts — regenerable, noisy, or sandbox creds (keep scripts/docs, not these)
audit/playwright/findings/*.png
audit/playwright/findings/*.json
audit/playwright/lsr_accounts.json
audit/playwright/lsr_prep_state.json
audit/playwright/wordmap.json
.claude/scheduled_tasks.lock
